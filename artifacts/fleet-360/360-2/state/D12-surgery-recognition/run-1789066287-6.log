dsh: reasoning:
Let me start by understanding the current state of the worktree. I need to read the existing checkpoint.json, LONG_PLAN.json, the results card, and explore the directory structure.

Let me begin with exploration.
dsh: reasoning:
Let me read the current checkpoint.json and the results files to understand where previous work stopped.
dsh: reasoning:
The previous invocation made great progress. Let me understand the current state:

1. **BallGluing.lean** — COMPILED: `doubleBallHomeoSphere : DoubleBall ≃ₜ S3`
2. **ConnectedSumTopology.lean** — COMPILED: `sphereConnectSum_homeo_sphere`
3. **SphereOfSpheres.lean** — COMPILED: `ConnectedSumDecomposition.mkV2` (SR-5 closure)
4. **ExpandedInterfaces.lean** — COMPILED: `stage6Target_of_v2decomposition` (downstream use)
5. **All.lean** — COMPILED umbrella

Milestones M1, M2, M3 reached. M4 (72h) pending.

The remaining work for this invocation (4 hours):
1. Audit downstream consumption (deep review)
2. Expand result card
3. Consider strengthening: maybe attempt SR-4 (van Kampen) — but mathlib doesn't have van Kampen for fundamental groupoid. Actually wait — let me check what's available in the pinned mathlib. The checkpoint says "mathlib has no van Kampen theorem for the fundamental groupoid". Let me verify this and see if there's anything else useful.

Actually, there's also the possibility of addressing the `recognize` field in SphericalPieceRecognition, or proving something about `ExtinctionCertificate`. But those depend on Ricci flow foundations that likely don't exist in mathlib.

Another good target: the "connected-sum simple-connectivity implication" mentioned in the objective: "a correct covering-space recognition or connected-sum simple-connectivity implication". This is SR-4: `simplyConnected_pieces : SimplyConnectedSpace X.Carrier → ∀ P ∈ pieces, SimplyConnectedSpace P.Carrier`. This is actually a *real theorem* in classical topology (for a connected sum, each summand is a retract of the sum... actually it's not quite that simple — but there's a standard argument via van Kampen). Hmm, wait: for a connected sum M # N, the fundamental group is the free product π₁(M) * π₁(N). So if M # N is simply connected, both M and N are simply connected. This requires van Kampen in the groupoid/pushout form.

Alternatively, there might be a covering-space argument. Actually let me think about what's provable without van Kampen:

**Covering space recognition**: Given a covering map p : E → B where B is simply connected and the covering is... hmm. Actually there's a classical theorem: a covering space of a simply connected space that is path-connected is a homeomorphism. In mathlib there might be `IsConnected`-based results for coverings... Let me check what mathlib has on covering maps: `Mathlib.Topology.Covering` exists in newer mathlib. The pinned mathlib is from around 2025-09 (v4.34.0-rc2 era is ~Sept 2025). `Mathlib.Topology.Covering` was added around mid-2025 I believe. Let me check.

Actually, a cleaner classical statement provable from existing mathlib pieces: if a path-connected space B has a covering E → B and B is simply connected, then the covering is a homeomorphism (provided E is path-connected... or nonempty connected). This needs `IsSimplyConnected` stuff. Hmm.

Alternatively, maybe I should prove a **simple-connectivity of the sphere S³** or higher spheres — that would be a substantial lemma that could support the "final topology" recognition: the piece recognition says each piece is homeomorphic to S³ or a spherical space form; spherical space forms have S³ as universal cover...

Actually, wait. Let me re-read the objective:

"Prove one substantive topology/geometry lemma that replaces an assumed field (e.g. a correct covering-space recognition or connected-sum simple-connectivity implication) with an actual construction."

The previous invocation already did this (SR-5 sphere_of_spheres → construction). The "e.g." suggests those two were examples. SR-5 is now closed. SR-4 (connected-sum simple-connectivity implication) remains.

For this invocation, I should:
1. Verify the current state compiles (rebuild).
2. Investigate whether SR-4 (van Kampen implication) can be proved — even a restricted form. The classical statement: for X = M # N connected sum of path-connected M, N (dim ≥ 2, suitably nice), π₁(X) ≅ π₁(M) * π₁(N). Without van Kampen in mathlib, this is hard. But maybe there's a weaker form: "if X is simply connected then M and N are simply connected" — could be proven via retractions? Hmm, M # N retracts onto... Actually no: for a connected sum, there is NO retraction from M#N to M in general? Hmm, actually there is a map M#N → M given by collapsing N minus the ball to a point... but that's not a retraction of the inclusion.

Hmm wait, actually there IS a standard result: each summand of a connected sum is homotopy equivalent to... no. M # N retracts onto M ∨ N? No, that's wrong too.

Actually, there's a standard result used in Poincaré-conjecture-adjacent work: if M # N is simply connected, then M and N are simply connected. This follows from van Kampen: π₁(M#N) = π₁(M) * π₁(N), and a free product is trivial iff both factors are trivial.

Alternative approach without full van Kampen: use covering space theory. There's a classical argument: if π₁(M) ≠ 1, there's a nontrivial covering M̃ → M. Then you can build a covering of M#N... hmm, this gets complicated.

Honestly, proving van Kampen or the free-product formula from scratch in Lean is a huge undertaking (the standard proof of van Kampen alone in HoTT book is hundreds of lines and mathlib doesn't have the fundamental group definition even — mathlib has fundamental groupoid as defined via `FundamentalGroupoid` with path concatenation... Let me check what exists).

Let me check what's in the pinned mathlib: `Mathlib/AlgebraicTopology/FundamentalGroupoid/`. It has `FundamentalGroupoid`, `Functor.map`, `SimplyConnectedSpace`. There's no van Kampen.

So SR-4 requires building: fundamental group, free products, van Kampen... that's a multi-week Lean project. Not feasible in 4 hours. Better to leave it as explicit hypothesis.

So what should this invocation do? The task says the previous milestones M1-M3 are reached and the 72-hour cap is a resource cap, not ETA. "Complete early only when the intended milestone is genuinely proved and audited".

The intended milestone: "Prove one substantive topology/geometry lemma that replaces an assumed field... with an actual construction." — DONE (SR-5 closed). "If unavailable, formalize the smallest nontrivial prerequisite and a reviewed decomposition of neck estimates, surgery existence, finite extinction and final topology." — DONE (EndGameDecomposition).

So the main work now is:
1. **Deep audit**: independently verify the claims in the checkpoint — re-run the builds, re-run axiom audit, check the "downstream use" is real (not vacuous), check that the proof isn't accidentally assuming something equivalent to the conclusion (e.g. the D7 structure field being trivially derived).
2. **Fresh source hashes** — compute sha256 of authored files.
3. **Check non-vacuity examples** exist.
4. Look for weaknesses: e.g. `ConnectedSumDecompositionV2` requires `pieceSphere : ∀ P ∈ pieces, Nonempty (P.Carrier ≃ₜ S3)` — that's the actual hard input, fine. `sumHomeo : Nonempty (X.Carrier ≃ₜ iterated sum)`. And `mkV2` needs `simplyConnected_pieces` passed in as an argument? Wait, from the result card:

```
def ConnectedSumDecomposition.mkV2 (D : ConnectedSumDecompositionV2 X pieces)
    (simplyConnected_pieces : ...) : Poincare.D7.Recognition.ConnectedSumDecomposition X pieces
```

Hmm — the D7 structure has fields `simplyConnected_pieces` and `sphere_of_spheres`. If `mkV2` takes `simplyConnected_pieces` as an argument, then it's passing through the SR-4 assumption unchanged (fine — SR-4 remains open, documented). But the `sphere_of_spheres` field is proved from `pieceSphere` and `sumHomeo` and `iteratedSphereSum_homeo_sphere`.

Wait, but hold on — is this actually closing SR-5, or is it just "H' → H" where H' is equivalent to H? The key question: is `ConnectedSumDecompositionV2` genuinely weaker/more primitive than the D7 field `sphere_of_spheres`? Let's think:

- D7 field: `sphere_of_spheres : (∀ P ∈ pieces, Nonempty (P.Carrier ≃ₜ S3)) → Nonempty (X.Carrier ≃ₜ S3)`.
- V2: `pieceSphere : ∀ P ∈ pieces, Nonempty (P.Carrier ≃ₜ S3)` and `sumHomeo : Nonempty (X.Carrier ≃ₜ iteratedSphereSum pieces pieceSphere)`.

The D7 field is a *function* from the per-piece homeomorphisms to a homeomorphism of X. V2 requires the per-piece homeos *and* a homeomorphism of X to the iterated sum. But the theorem `iteratedSphereSum_homeo_sphere` proves that the iterated sum is homeomorphic to S³, so from V2 you get X ≅ S³ — i.e. V2 implies the conclusion of sphere_of_spheres constructively. The point: V2 replaces the *assumed function* with *data + a proved theorem about the actual gluing construction*. That's the honest version: instead of assuming "the connected sum of spheres is a sphere", you define the connected sum as an explicit quotient, prove the iterated sum of S³ homeos is S³, and then require the topological input "X is homeomorphic to the iterated sum of the pieces" — which is the true content of the surgery decomposition theorem (it follows from the actual surgery construction; upstream D7 didn't have the surgery construction either way).

So yes, this is a genuine closure of the assumed field: the field `sphere_of_spheres` is replaced by `pieceSphere + sumHomeo` data plus a *theorem*. But is `sumHomeo : Nonempty (X.Carrier ≃ₜ (iteratedSphereSum pieces pieceSphere).sum.Carrier)` "equivalent to the conclusion"? The conclusion of sphere_of_spheres is `Nonempty (X.Carrier ≃ₜ S3)`. With `iteratedSphereSum_homeo_sphere` giving `iterated sum ≃ S3`, `sumHomeo` implies the conclusion. So V2-with-iteratedSphereSum_homeo_sphere entails the sphere_of_spheres conclusion. It's "the conclusion split into the actual gluing data + a real topological theorem". I think this is defensible: the hard mathematical content is the explicit quotient construction and its homeomorphism proof; the residual input `sumHomeo` is the decomposition hypothesis that would be established by the surgery algorithm (which is out of scope, SR-6). This matches "a reviewed decomposition... keep separate".

One concern: is `iteratedSphereSum_homeo_sphere` perhaps circular (does it assume what it proves)? It should be by induction on the list using `sphereConnectSum_homeo_sphere`. Need to verify in source.

Another concern: the audit should confirm the "downstream checked use" `stage6Target_of_v2decomposition` genuinely uses `mkV2` (not just mentions it), and that it's not vacuous.

Also need to double check there are no `sorry`s anywhere in the vendored D7 files (the vendored files are from D11 and are historical; they may contain assumptions — that's upstream, documented).

Now, the plan for this 4-hour invocation:

1. **Rebuild and verify** (in background): `cd release && lake build Poincare.D12.SurgeryRecognition.All`.
2. **Read the authored files** to audit: BallGluing.lean (790 lines), ConnectedSumTopology.lean (540), SphereOfSpheres.lean (226), ExpandedInterfaces.lean (186).
3. **Re-run the axiom audit** on fresh declarations; compute fresh sha256 hashes.
4. **Forbidden-token scan** with a comment/string-aware scanner.
5. Look at the frenzymath snapshot (`third_party/frenzymath/Poincare-Conjecture` at bb91a09) — the supervisor update says to reuse MorganTian, Topping, DoCarmo, Shared upstream packages instead of recreating geometry/Ricci-flow foundations. Check `docs/UPSTREAM-INTEGRATION.md` and `third_party/frenzymath/IMPORT.md`. Perhaps there's something about `recognize` (SphericalPieceRecognition) or van Kampen that could be integrated. If the frenzymath snapshot has a van Kampen theorem or a fundamental-group-free simple-connectivity argument, that could let me close SR-4. Let me look.
6. Decide whether to attempt SR-4 closure or a covering-space lemma (e.g., "covering of a simply connected space is a homeomorphism" — could be `IsSimplyConnected`-based; mathlib `Topology.Covering` has `IsCoveringMap`, and there's a result about liftings... The classical result: a path-connected covering space of a simply connected space is a homeomorphism. If mathlib has the monodromy action or uniqueness of lifts + existence of lifts (path lifting), maybe provable).

Actually, wait — there's a classical pure-topology result that IS in scope and might be provable with mathlib: **simple connectivity of the n-sphere for n ≥ 2**. mathlib has `IsSimplyConnected`? There's `SimplyConnectedSpace` class (via fundamental groupoid) and there's a theorem `isSimplyConnected_sphere`? Hmm, I recall mathlib has `sphere_isSimplyConnected` for n ≥ 2? Actually I'm not sure. Let me check. There's `Mathlib.Topology.Connectedness`... Let me search the pinned mathlib for `SimplyConnected`.

Also relevant: the D7 SphericalPieceRecognition `recognize` field probably says: a closed 3-manifold piece with finite fundamental group is (homeomorphic to) a spherical space form / S³ — that's Perelman's geometrization; out of scope. But a *preliminary* piece: S³ is simply connected, and a spherical space form has universal cover S³ — that needs π₁(S³) = 1.

Let me look at what the pinned mathlib has for higher spheres' simple connectivity:
- `Mathlib/Topology/Homotopy/...`? In the pinned mathlib, fundamental groupoid theory exists; I don't think there's a proof that Sⁿ is simply connected for n ≥ 2. Actually, hmm, I recall a recent addition: `IsSimplyConnected` of spheres via... In 2025 there was work on `ContractibleSpace` and cellular approximation? Let me just search.

Given 4 hours, the realistic plan:
- Re-verify everything compiles; audit; produce evidence.
- Search frenzymath for usable van Kampen / fundamental group / covering results.
- Attempt one more substantive lemma if time permits — candidates:
  a) S³ is simply connected (if mathlib has enough — likely not; needs van Kampen for S²... no wait, S³ simply connected can be proven via CW: S³ = S² ∪ two cells... needs cellular/van Kampen anyway).
  b) A covering-space lemma: "surjective covering map from a path-connected space to a simply-connected one is a homeomorphism" — needs mathlib covering theory; check availability.
  c) Prove the "connected-sum simple-connectivity" implication under an explicit hypothesis that X = connectedSum M N with the quotient data (i.e., with my own explicit connectedSum, if X ≅ M # N and M # N is simply connected then... still needs van Kampen or covering theory).
  
Hmm. Honestly (a)-(c) all need van Kampen or covering theory machinery that mathlib likely lacks. Let me check first, then decide. If nothing is feasible, the deliverable is the audit-complete SR-5 closure + reviewed decomposition, with TASK_DONE (since the objective's intended milestone — one substantive lemma replacing an assumed field — is met) or TASK_BLOCKED? No: the objective was met. TASK_DONE with honest remaining blockers listed.

But wait — "Complete early only when the intended milestone is genuinely proved and audited". So: M4 = audited, reproducible deliverable. This invocation: do the audit thoroughly, produce the final result card, and mark TASK_DONE if audit passes.

Actually, let me also double-check one subtle risk in the previous work: the claim "SR-5 replaced by construction". The D7 structure `ConnectedSumDecomposition` — I need to see the actual D7 definitions to verify that `mkV2`'s `sphere_of_spheres` field is genuinely derived and not, e.g., `by classical exact ...` from another assumption in the structure. Let me read the relevant D7 file.

Also, an important check: does `mkV2` require additional hypotheses beyond D's fields? From the result card: `(simplyConnected_pieces : ...)` — hmm, wait, actually I need to see: maybe the D7 `ConnectedSumDecomposition` also has a `relation` field or `pieces_subset` etc. Let me read the source.

Let me also verify the vendored D7 is really from D11 (the provenance file), and read LONG_PLAN.json for context.

Plan of action this invocation (4h):
1. Read LONG_PLAN.json, README.md, manifest, the D7 Recognition files, and my authored files (audit read).
2. Kick off background rebuild + audit job.
3. Check frenzymath snapshot for relevant reuse.
4. Possibly attempt SR-4-lite or a covering-space lemma if machinery exists.
5. Update checkpoint.json and results; compute hashes; final axiom audit; mark TASK_DONE if all checks pass.

Let me start. First, read the key context files and kick off the build in background.
dsh: reasoning:
Now let me:
1. Kick off the rebuild in the background.
2. Read the authored files to audit them.
3. Check the frenzymath snapshot.

Let me kick off the build first, and read the authored files in parallel.
dsh: reasoning:
This looks solid. Let me review the D7 Recognition.Basic to see the target structure `ConnectedSumDecomposition` and confirm `mkV2` genuinely fills `sphere_of_spheres`.

One issue I want to check: `relation := True` — the D7 `relation` field is opaque; supplying `True` as data. That's fine since `relation` in D7 was a `Prop` (likely `X.Carrier → pieces → Prop` or similar). Setting it to `True` is "supplied as data" — acceptable but should be documented (it is).

Let me now read the D7 Recognition.Basic file, and the other authored files. Let me also check the frenzymath snapshot.
dsh: reasoning:
Build finished. Let me check the output, then read the D7 Recognition.Basic to see the target structure.
dsh: reasoning:
The build completed successfully (8926 jobs). Now let me read the full D7 Recognition.Basic around those structures to understand exactly what `mkV2` fills and what `stage6Target_of_v2decomposition` uses.
dsh: reasoning:
Good. Now I understand the D7 `ConnectedSumDecomposition` structure fully:
- `relation : Prop`, `relation_holds : relation` — opaque.
- `simplyConnected_pieces : SimplyConnectedSpace X.Carrier → ∀ P ∈ pieces, SimplyConnectedSpace P.Carrier` — van Kampen assumption.
- `sphere_of_spheres : (∀ P ∈ pieces, Nonempty (P.Carrier ≃ₜ SphereThree)) → Nonempty (X.Carrier ≃ₜ SphereThree)` — the SR-5 assumption.

`mkV2` fills:
- `relation := True`, `relation_holds := trivial`.
- `simplyConnected_pieces := simplyConnected_pieces` (explicit hypothesis passed through).
- `sphere_of_spheres := by intro _hpieces; rcases D.sumHomeo with ⟨hX⟩; rcases (iteratedSphereSum pieces D.pieceSphere).sumHomeo with ⟨hsum⟩; exact ⟨hX.trans hsum⟩` — proved from V2 data.

Note the `sphere_of_spheres` proof doesn't even use `_hpieces` (the per-piece homeos argument) — it uses `D.pieceSphere` directly. That's fine: V2's data already includes per-piece homeos. The proof is genuine: `X ≃ iteratedSum` and `iteratedSum ≃ S3` compose. The actual content is `iteratedSphereSum`'s recursion + `sphereConnectSum_homeo_sphere` + `doubleBallHomeoSphere`.

So the SR-5 closure is real: the D7 field is derived from explicit constructions and theorems about actual topological spaces. 

Now for the remaining time, let me:
1. Read the other authored files (BallGluing.lean, ConnectedSumTopology.lean, ExpandedInterfaces.lean) — at least skim for audit purposes. 790 + 540 + 186 lines. Let me read them.
2. Check frenzymath for reusable machinery, particularly anything about van Kampen / fundamental groups / covering spaces / simply connected spheres. If frenzymath's `Shared` package has a fundamental group definition or van Kampen, maybe I can close SR-4 partially... but wait, reusing frenzymath packages requires those packages to build in this worktree. The supervisor update says "Reuse MorganTian, Topping, DoCarmo, Shared and other upstream packages instead of recreating geometry/Ricci-flow foundations." So importing them is encouraged where useful.

Let me first look at what's in the frenzymath snapshot and the integration docs.
dsh: reasoning:
The IMPORT.md and docs/UPSTREAM-INTEGRATION.md don't exist at those paths. Let me find them.
dsh: reasoning:
The frenzymath snapshot is a large Lean 4.32.1 project (different Lean version than our v4.34.0-rc2, different mathlib pin 520045a). Importing its modules into our package would likely require version alignment — probably not feasible in this worktree without a lot of work. Let me check:
1. Whether there's a fundamental group / van Kampen formalization (e.g. in Hatcher formalized-sources) that could serve as *reference* for my own work (I can't import it, but I can learn the proof structure — though implementing van Kampen from scratch in 4 hours is unrealistic).
2. `docs/UPSTREAM-INTEGRATION.md` — where is it? The supervisor update said to read it. It's not in the worktree root `docs/`. Let me search for it.
dsh: reasoning:
The `docs/UPSTREAM-INTEGRATION.md` and `third_party/frenzymath/IMPORT.md` files referenced in the supervisor update don't exist in this worktree. They may be in the parent directory or elsewhere. Let me check the parent worktree structure for those docs, and check if frenzymath has a van Kampen / fundamental group formalization that might be worth studying (even if not importable due to Lean version mismatch).
dsh: reasoning:
Frenzymath's HatcherLib has van Kampen and covering classification formalized — but in Lean v4.32.1 with mathlib 520045a. Our worktree is Lean v4.34.0-rc2 with mathlib 7974e75. These are incompatible toolchains; importing directly is not feasible without a toolchain change (which is forbidden: "Do NOT ... change the toolchain").

Also important: frenzymath is an *active, incomplete formalization* and much of it may contain `sorry`s. Reusing it wholesale is risky. The supervisor update says "Reuse MorganTian, Topping, DoCarmo, Shared and other upstream packages instead of recreating geometry/Ricci-flow foundations" — that's for geometry/Ricci-flow foundations, but importing requires toolchain alignment which we can't change. So frenzymath is effectively a reference for reading, not importable. I should note this in the results.

Let me check the VanKampen.lean file to see if it's complete (no sorry) — if it's complete, maybe worth studying its structure to estimate feasibility of porting. But porting van Kampen (likely thousands of lines: VanKampenGrid, VanKampenSweep, WordCalculus, Subdivision...) is far beyond this invocation.

Actually, wait. Let me reconsider the scope. The task objective: "Prove one substantive topology/geometry lemma that replaces an assumed field ... with an actual construction." — Done (SR-5). "If unavailable, formalize the smallest nontrivial prerequisite and a reviewed decomposition of neck estimates, surgery existence, finite extinction and final topology." — Done (EndGameDecomposition + BallGluing).

The remaining potential additions for this invocation:
1. **Deep audit** (required for TASK_DONE): re-verify axiom audit, forbidden scans, fresh hashes, non-vacuity witnesses.
2. Possibly strengthen with another substantive lemma. Given time budget (4h), candidates:
   - a) A covering-space recognition lemma: e.g. "a covering map from a path-connected space E to a simply connected space B is a homeomorphism" — needs mathlib covering theory. Let me check what the pinned mathlib has in `Mathlib/Topology/Covering.lean`.
   - b) π₁(S¹) ≅ ℤ? Not needed.
   - c) S² is simply connected (needed for... hmm, actually for the spherical piece recognition: spherical space forms have π₁ finite, S³ → quotient covering; recognizing pieces via covering theory would need π₁(S³)=1 and covering classification. Not feasible.)
   - d) A useful "sphere recognition" auxiliary: e.g. S² is path-connected (mathlib probably has `PathConnected` spheres? maybe not), or S³ path connected... These might be quick but small.

Let me check the pinned mathlib for covering space theory first. If `Mathlib.Topology.Covering` has `IsCoveringMap` and lifting properties, there might be a nice lemma provable: "surjective covering map between path-connected spaces where the base is simply connected is a homeomorphism" — the classical proof uses the monodromy action on fibers or uniqueness of lifts. Actually there's a simpler classical fact provable with mathlib's existing machinery:

**Lemma (classical):** If p : E → B is a covering map, B is path-connected and simply connected, and E is path-connected (nonempty), then p is a homeomorphism.

Proof sketch (algebraic topology): π₁(B) acts transitively on p⁻¹(b₀) via monodromy. If π₁(B) = 1, fibers are singletons. Then a bijective local homeomorphism is a homeomorphism. Formalizing this needs: monodromy action / path lifting / homotopy lifting — mathlib has path lifting? `Mathlib.Topology.Covering` — let me look at what's actually in the pinned mathlib.

Let me check the mathlib snapshot content. The mathlib source is presumably at some path. Let me find it — maybe under release/.lake/packages/mathlib or similar. Let me search.
dsh: reasoning:
No Covering or FundamentalGroupoid files at maxdepth 2? Let me search deeper.
dsh: reasoning:
The pinned mathlib has `Mathlib.Topology.Covering.Basic` with `IsCoveringMap`, `IsEvenlyCovered`, `fiberHomeomorph`, and `Deck.lean` (deck transformations). Let me check `Deck.lean` — it may have results about deck transformations acting transitively on fibers for connected spaces, which is exactly the monodromy result needed for covering recognition.
dsh: reasoning:
The pinned mathlib has:
- `Mathlib.Topology.Covering.Basic`: `IsCoveringMap`, `IsCoveringMapOn`, `IsEvenlyCovered`, `fiberHomeomorph`.
- `Mathlib.Topology.Covering.Deck`: deck transformations group, with a canonical action on fibers.
- `SimplyConnectedSpace` class with the "unique homotopic paths" criterion.

Let me look at more of Deck.lean to see if it has "transitive action for connected base" — that's the key monodromy result. Let me also check `Quotient.lean` (covering of quotient by discrete group action — relevant for spherical space forms! A spherical space form is S³/Γ for finite Γ acting freely. If mathlib has "quotient map is a covering map for properly discontinuous free actions", that plus π₁(S³)=1 would give π₁(space form) ≅ Γ... but π₁(S³)=1 is not in mathlib either).

Let me look at Deck.lean's remaining theorems.
dsh: reasoning:
The covering theory in mathlib is "point-set covering maps" (evenly covered neighborhoods), without the homotopy-lifting/monodromy machinery. Proving "covering of a simply connected space is a homeomorphism" would require building monodromy from scratch: path lifting, homotopy lifting — a big project (path lifting alone requires continuity lemmas on compact intervals). The classical minimal proof:

**Theorem:** p : E → B covering map, E path-connected nonempty, B path-connected + simply connected (or just π₁(B,b₀)=1... mathlib's `SimplyConnectedSpace` includes path-connected), then p is a homeomorphism.

Classical proof via monodromy: the fiber over b₀ has a transitive π₁(B) action; π₁(B)=1 makes fibers singletons; p is a bijective local homeomorphism → homeomorphism. Requires: path lifting (existence + uniqueness), homotopy lifting for the action's well-definedness. That's substantial — maybe 1500+ lines. Too much for this invocation, but the *statement* and a decomposition would be valuable as the next subproblem.

Alternative: Is there a cheaper "recognition" lemma using `IsEvenlyCovered` directly? For instance: if p : E → B is an even covering with fiber I, B connected, and p is surjective... fibers are all nonempty and discrete. Hmm, without path lifting we can't show constant fibers over connected base... Actually there's a classical fact provable with just point-set topology: for a covering map p : E → B with B connected, all fibers are homeomorphic (the "cardinality of fiber is locally constant" argument uses trivializations over evenly covered neighborhoods — connectivity of B gives constancy via the standard "locally constant on connected space" lemma). Let me think: if p is a covering map, is the function x ↦ (fiber type over x) locally constant? Over an evenly covered neighborhood U of x with fiber I, every y ∈ U has fiber ≃ I via `fiberHomeomorph`. So yes, locally constant. On a connected space B, a locally constant... we need "if B is connected and f is locally constant then f is constant" — mathlib has `IsLocallyConstant` and the theorem `IsLocallyConstant.isConstant_on_preconnected` or similar (`LocallyConstant.of_isPreconnected`). That's provable! That gives: **all fibers of a covering map over a connected base are homeomorphic to a fixed I** — a genuine lemma.

But for the recognition "simply connected base ⇒ covering is a homeomorphism", we still need to show a nonempty fiber is a singleton: given e₀ ∈ p⁻¹(b₀) and any other e₁ ∈ p⁻¹(b₀), need a path in E from e₀ to e₁... For simply connected B, the classical argument: the action of π₁(B) on the fiber is transitive (needs lifting of paths). Without it, no.

Hmm, but there's another classical route for the specific setting of the *connected sum* implication (SR-4): Let me think about what exactly SR-4 needs: X = M₁ # ... # Mₖ (connected sum along spheres), X simply connected ⟹ each Mᵢ simply connected. Proof: if some Mᵢ has π₁ ≠ 1, then Mᵢ has a nontrivial connected covering; the connected sum X = Mᵢ # (rest) has a covering which is (M̃ᵢ minus k disks) glued with (rest minus disk)... and π₁(X) surjects onto π₁(Mᵢ) — actually the classical van Kampen: π₁(M#N) = π₁(M) * π₁(N) and the retraction-free argument... 

Alternative classical argument avoiding van Kampen: there's a *retraction* M#N → ... no. Actually here's a genuinely elementary argument: the connected sum X contains M ∖ D³ as a nice subspace, and there's a quotient map collapsing... Let me think about "X simply connected ⟹ M simply connected" via an elementary covering construction:

Take any connected covering p : M̃ → M. Puncture: M̃ ∖ p⁻¹(int D³) → M ∖ int D³ is still a covering. Glue with the other pieces along the boundary spheres: the connected sum X̃ of M̃ with N (using one disk in each component of the fiber... wait, p⁻¹(S²) is a union of spheres; to form a covering of X we need the covering to be trivial over the gluing sphere S² — a covering restricted to S², with S² simply connected... needs π₁(S²)=1! Ugh. Classical fact: any covering of S² is a disjoint union of spheres. That's the spherical covering classification for S², provable via... the monodromy again.

OK so all routes go through path lifting / monodromy or van Kampen. Neither is a 4-hour job in mathlib.

**Decision:** For this invocation, focus on:
1. **Deep audit + fresh evidence** (required to complete M4 and mark TASK_DONE).
2. Add a small but genuine new lemma that strengthens the package if cheap: e.g. **`fiber_isLocallyConstant`-style covering lemma**? Hmm, but that's tangential to the D12 objective. Better: strengthen what's already there — e.g. prove **`sphereConnectSum` is path-connected**? Not needed.
3. Actually, a valuable addition: the previous result card's `stage6Target_of_v2decomposition` — let me read ExpandedInterfaces.lean to verify the downstream use is honest, then decide.

Also I should double-check the audit: `Audit.lean` uses `#print axioms`; the result card claims 128/128 clean. Let me verify Audit.lean content, re-run it, and also run the forbidden scan. Also compute fresh sha256 of all authored files.

One more thing to verify: the claim "nonstandard axioms 0" and "cone ⊆ {propext, Classical.choice, Quot.sound}". Let me re-run and confirm. Also, `mkV2` uses `Classical.choice` presumably via `.some` on `Nonempty` — fine.

Also, an important potential weakness to audit: `ConnectedSumDecompositionV2.sumHomeo : Nonempty (X.Carrier ≃ₜ (iteratedSphereSum pieces pieceSphere).sum.Carrier)`. And `mkV2`'s `sphere_of_spheres` proof: given arbitrary `_hpieces : ∀ P ∈ pieces, Nonempty (P.Carrier ≃ₜ S3)`, produce `Nonempty (X.Carrier ≃ₜ S3)` — it ignores `_hpieces` and uses `D.pieceSphere`. This is legitimate: `D.pieceSphere` is the per-piece homeo data of the V2 structure. The D7 field's input is per-piece homeos; V2's field is strictly stronger data (it fixes which homeos). So `sphere_of_spheres` is discharged. Good.

But wait — one subtlety: is the hypothesis `_hpieces` "equivalent to the conclusion" issue? No: `mkV2`'s V2 input includes `pieceSphere` (per-piece homeos) + `sumHomeo` (X ≃ iterated sum), and the conclusion X ≃ S³ follows from the *theorem* `iteratedSphereSum_homeo_sphere` (which itself is proved by induction from `sphereConnectSum_homeo_sphere` and `doubleBallHomeoSphere`). The genuinely assumed part is `sumHomeo` — the honest "X is the connected sum of the pieces" hypothesis, which upstream D7 hid inside the opaque `relation : Prop`. So V2 makes the decomposition hypothesis explicit and topological, and derives the topological conclusion. This is a real improvement and a real construction. Good.

Now the audit. Let me read ExpandedInterfaces.lean fully, skim ConnectedSumTopology.lean and BallGluing.lean, re-run Audit, and check `stage6Target_of_v2decomposition` against D7's original assembly.
dsh: reasoning:
This all looks good and honest. Now let me read the remaining two big files quickly for audit purposes (BallGluing.lean and ConnectedSumTopology.lean). I'll skim them for any red flags — particularly: (a) the definition of `S3`, `Ball3`, `downS3`, `DoubleBall`, and (b) that `sphereConnectSum_homeo_sphere` doesn't cheat (e.g. via a degenerate quotient or by assuming the conclusion).

Let me also re-run the Audit.lean and check its generation, and run the forbidden scan. Let me look at the audit file and tools.
dsh: reasoning:
The ConnectedSumTopology file looks solid. The connected sum is a genuine quotient construction. Let me continue skimming the rest of the file, particularly `sphereConnectSum_homeo_sphere`, and check BallGluing's key definitions. Then I'll run the audit and scan tools.

Let me view the rest of ConnectedSumTopology.lean.
dsh: reasoning:
This all looks like genuine, careful work. Let me see the last part (the final theorem) and then move to BallGluing.lean.
dsh: reasoning:
The chain is honest: `sphereConnectSum ≃ Quot hemiGlueRel ≃ DoubleBall ≃ S3` with explicit homeomorphisms and checked relation transports. 

Now let me look at BallGluing.lean's key definitions (S3, Ball3, DoubleBall, doubleBallHomeoSphere) to verify nondegeneracy, and check the non-vacuity examples. Let me look at the beginning and key parts.
dsh: reasoning:
The definitions are all standard and nondegenerate. `Ball3 = Metric.closedBall (0 : R3) 1`, `S2 = Metric.sphere (0:R3) 1`, `S3 = Metric.sphere (0:R4) 1`. Non-vacuity: northPole/southPole/equator examples.

Now let me look at the key final part of BallGluing (doubleBallHomeoSphere, glueRel, DoubleBall, and non-vacuity examples) to finish the audit.
dsh: reasoning:
The BallGluing file is solid and genuinely constructive: explicit maps, checked mutual inverses, non-vacuity witnesses (northPole, southPole, equator gluing). All the definitions are nondegenerate.

Now the audit:
1. Re-run the audit: `lake env lean Poincare/D12/SurgeryRecognition/Audit.lean` and capture output; verify every `#print axioms` output cone ⊆ {propext, Classical.choice, Quot.sound}.
2. Run a fresh forbidden-token scan on the five authored files (comment/string aware).
3. Compute fresh sha256 hashes of all authored files.
4. Check git status of the worktree isn't an issue (we shouldn't commit).

Additionally, I could add a genuinely new substantive lemma this invocation. Let me think about what's feasible and valuable within the remaining ~3h, aligned with the task objective ("covering-space recognition or connected-sum simple-connectivity implication"):

Option A: **Non-vacuity of the iterated sphere sum** — e.g., prove `iteratedSphereSum [sphereSpace, sphereSpace]` is nonempty and its sum is homeomorphic to S3 concretely (already follows from the theorem, weak addition).

Option B: **Path-connectedness of S³** or of the connected sum — useful support for the recognition (simple connectivity of pieces needs path-connectedness; mathlib `SimplyConnectedSpace` implies `PathConnectedSpace`? Let me check: `SimplyConnectedSpace` class in mathlib is `PathConnectedSpace X ∧ Nonempty (FundamentalGroupoid X) ∧ IsConnected (FundamentalGroupoid X)`? Let me look at the actual definition. If `SimplyConnectedSpace S3` were provable cheaply, that would be a nice addition: "S³ is simply connected" — but the standard proof needs van Kampen or CW theory. mathlib has `sphere_isSimplyConnected`? Hmm — actually I recall mathlib has `isSimplyConnected_sphere` for spheres of dim ≥ 2! Let me check. There's `Mathlib.Topology.Homotopy.Contractible` — spheres of dim ≥ 1 are not contractible. But simply connected spheres: I believe there IS a theorem in mathlib: `Sphere.simplyConnectedSpace`? Let me grep.

If mathlib has "Sⁿ simply connected for n ≥ 2", then I can prove **`S3` is simply connected** for free (it's `Metric.sphere (0:ℝ⁴) 1 ≃ₜ sphere 3` via the homeomorphism mathlib has? `Metric.sphere` vs `sphere` — mathlib has `homeomorphSphere`? There's `Mathlib.Geometry.Manifold.Instances.Sphere` and `Mathlib.Topology.Instances.Sphere` (actually `Sphere` in `Mathlib.Topology.Instances.Sphere` defines `Sphere (n : ℕ)` hmm). Let me check.

That would be valuable: the spherical piece recognition requires pieces simply connected; showing the model S³ is simply connected gives a *model* check (semantic class: model), plus it's genuinely used: in `RemainingRecognitionHypotheses`, or as "if X ≃ S³ then X is simply connected" (transport of SimplyConnectedSpace along homeomorphisms — mathlib has `Homeomorph.simplyConnectedSpace`? Probably: SimplyConnectedSpace is preserved by homotopy equivalence; `ContinuousMap.HomotopyEquiv.simplyConnectedSpace` exists per the grep earlier. And `Homeomorph.toContinuousMap`... a homeomorphism is a homotopy equivalence, so `e.toContinuousMap.HomotopyEquiv`? There's `Homeomorph.homotopyEquiv`? Hmm. If SimplyConnectedSpace is preserved under homeos in mathlib, then "each piece ≃ S³ ⟹ pieces simply connected" — that would *replace part of the role* of SR-4 van Kampen? No wait — SR-4 says X simply connected ⟹ pieces simply connected; if pieces ≃ S³ then pieces are simply connected automatically (given π₁(S³)=1). Interesting: then in the end game, `pieces_simplyConnected` (which upstream derives via van Kampen) could be derived via the sphere recognition instead! Let me look at D7 Assembly.lean to see how `simplyConnected_pieces` is used.

Actually that's a very promising direction: in the D7 assembly, the chain is: conclusion → onlySphericalPieces (via recognize) → pieces_simplyConnected (via van Kampen) → pieces_homeomorph_sphere (via recognize on simply connected pieces) → homeomorph_sphere (via sphere_of_spheres) → stage6Target.

If I can prove `S3 simply connected` + `SimplyConnectedSpace is homeo-invariant` + "X ≃ S3 implies X simply connected" and pieces are homeomorphic to S³, then pieces_simplyConnected follows from the piece recognition alone, without van Kampen! But wait — is `SimplyConnectedSpace` preserved by homeomorphism in mathlib? The earlier grep of SimplyConnected.lean showed `ContinuousMap.HomotopyEquiv.simplyConnectedSpace` and `.simplyConnectedSpace_iff` — yes! A homeomorphism is a homotopy equivalence (`Homeomorph.toHomotopyEquiv`? need to check name), so `e.toContinuousMap` has a `HomotopyEquiv` structure... In mathlib: `Homeomorph.homotopyEquiv`? Hmm, there's `Homeomorph.toContinuousMap.HomotopyEquiv`? Not sure of exact name. Let me check: `HomotopyEquiv` with `toFun : C(X,Y)`, `invFun : C(Y,X)`, `homotopyInvFunId`, `homotopyFunInvId`. For a homeo, id homotopies exist (refl). There might be an instance `Homeomorph.homotopyEquiv` or a helper. I can construct it manually: `{ toFun := e.toContinuousMap, invFun := e.symm.toContinuousMap, homotopyInvFunId := HomotopyRel.refl ..., ... }`. That's easy.

And is `S3` (as `Metric.sphere (0 : R4) 1`) simply connected in mathlib? Let me check `Mathlib.Topology.Instances.Sphere` / `Mathlib/Geometry/Euclidean/Sphere`... I recall `Mathlib.Topology.Homotopy.Sphere`? Hmm. Let me grep for `simplyConnected` + sphere in mathlib.

Also relevant: mathlib `isSimplyConnected_sphere` might not exist. There's `sphere_isPathConnected`? Let me search.

If π₁(S³) = 1 is in mathlib, then I get a nice strengthening: **"A space homeomorphic to S³ is simply connected"** — a model-class lemma used to discharge `simplyConnected_pieces` *without van Kampen* in a versioned assembly: `stage6Target_of_v2decomposition_no_vanKampen`: given piece recognition + pieces homeomorphic to S³... wait but pieces homeomorphic to S³ comes from `recognize` which needs `SimplyConnectedSpace P.Carrier`... circular: recognize : SphericalPiece X → SimplyConnectedSpace X.Carrier → Nonempty (X.Carrier ≃ₜ S3).

Hmm right, so van Kampen is needed to get pieces simply connected from X simply connected, and then recognize gives pieces ≃ S³. Unless the recognition is upgraded: a *spherical piece* is by definition something like "X is covered by S³ (spherical space form)" — the D7 `SphericalPiece` has `kind_eq` + `homeo` to a certified region. The `recognize` bridge: spherical piece + simply connected → ≃ S³. The content: simply connected spherical space form is S³.

So to eliminate van Kampen I'd need π₁(S³)=1 AND a covering-theoretic argument "covering of simply connected space" — no.

OK so within this invocation, the realistic value-add is:
1. Full audit + evidence (required).
2. `S3_simplyConnected` if mathlib provides it (model lemma; supports the sphere recognition semantically: the target of recognition is itself simply connected — non-vacuity in a strong sense).
3. `homeo_simplyConnectedSpace`: SimplyConnectedSpace transfers across homeomorphisms (if not in mathlib, construct via homotopy equivalence — `HomotopyEquiv.simplyConnectedSpace` exists so just build the HomotopyEquiv from the homeo).
4. Possibly a **connected-sum-of-S³ pieces are simply connected**: since X ≃ iterated sum ≃ S³, X simply connected, pieces ≃ S³ — pieces simply connected via homeo transfer. This gives a *conditional* lemma: "under V2 decomposition with pieceSphere, the van Kampen conclusion holds" — i.e., for the specific case of spherical pieces, `simplyConnected_pieces` is derivable from the sphere topology alone, NOT needing van Kampen! 

Wait, that's actually meaningful for the end game: in the real flow, the pieces ARE spherical (via canonical neighborhoods + recognition). The van Kampen SR-4 hypothesis `SimplyConnectedSpace X.Carrier → ∀ P ∈ pieces, SimplyConnectedSpace P.Carrier` — under V2 + pieceSphere data we can prove exactly this implication: X ≃ iteratedSum ≃ S³; S³ simply connected ⟹ X simply connected... hmm wait direction: given `SimplyConnectedSpace X.Carrier` and homeo X ≃ S³ we don't need it — pieces simply connected follows directly from pieceSphere (P ≃ S³, S³ simply connected). So the *van Kampen implication* becomes unnecessary for spherical pieces: the implication holds trivially because the conclusion doesn't need the premise. 

Hmm, but careful: is that "cheating"? No — it's mathematically true and useful: for the actual Poincaré end game, the pieces are recognized as spherical (homeomorphic to S³ or quotients), and if they're ≃ S³ (simply connected ones, via recognize), then simple connectivity of pieces comes from the *topology of S³*, not from van Kampen. Actually wait — in the D7 assembly chain, van Kampen (SR-4) is used to derive `pieces_simplyConnected` BEFORE recognition turns them into ≃ S³. But if instead we had "pieces are spherical" (covering-recognized) and "simply connected spherical piece ≃ S³", we need pieces simply connected first. Van Kampen gives that from X. The S³-simple-connectivity lemma gives the reverse direction: pieces ≃ S³ ⟹ pieces simply connected. So the two are different. My lemma would prove: **under V2 data with pieceSphere, the SR-4 field is discharged** — because SR-4's conclusion `∀ P, SimplyConnectedSpace P.Carrier` follows from `P.Carrier ≃ₜ S3` + `S3 simply connected`, without using the premise `SimplyConnectedSpace X.Carrier`. 

That's a real closure of SR-4 *under the V2 decomposition with spherical pieces*: the missing van Kampen theorem is no longer needed when the pieces are explicitly recognized as spheres. This is mathematically honest — the free-product argument is replaced by the direct sphere-homeomorphism argument. And it's a "downstream checked use" if I build a versioned assembly `stage6Target_of_v2decomposition` that does NOT take vanKampen as an argument but instead derives `simplyConnected_pieces` from pieceSphere.

But this hinges entirely on mathlib having `SimplyConnectedSpace (Metric.sphere (0 : ℝ⁴) 1)`. Let me check. If it doesn't exist, I must prove π₁(S³)=1 myself — that's van Kampen-level hard (S³ = union of two balls; need a Seifert–van Kampen pushout for that... the fundamental groupoid approach: actually mathlib's `SimplyConnectedSpace` = "any two paths are homotopic" + path connected. Proving S³ simply connected: it's the suspension of S². Suspension of a path-connected space is simply connected — needs van Kampen for suspensions. Not cheap).

Let me check mathlib first.
dsh: reasoning:
No hits at all. Let me broaden the search: maybe there's a theorem about spheres being simply connected somewhere, e.g. in `Mathlib.Topology.Homotopy` or via "suspension" results, or `IsSimplyConnected` for `Set`. Let me search for `Suspension` and `Sphere` and `IsSimplyConnected`.
dsh: reasoning:
So in the pinned mathlib:
- `SimplyConnectedSpace` is used only in: `SimplyConnected.lean` (definition), `BranchLogRoot` (ℂ⁺ simply connected?), `RiemannMapping`, `Homotopy/Lifting.lean`.

Let me check what `Homotopy/Lifting.lean` does — it may have "covering of simply connected space" type results! Let me look.
dsh: reasoning:
Excellent! `Mathlib.Topology.Homotopy.Lifting` has full covering-space machinery in the pinned mathlib:
- `IsCoveringMap.exists_path_lifts`
- `liftPath`, `liftHomotopy`
- `monodromy` action on fibers
- `existsUnique_continuousMap_lifts [SimplyConnectedSpace A] [LocallyPathConnectedSpace A]` — lifting of arbitrary maps from a simply connected + locally path connected space!
- `comp_subtypeVal_pathComponent_surjective` — for path-connected base, deck transformation orbit... let me look at line 290: `comp_subtypeVal_pathComponent_surjective (e₀ : E) [PathConnectedSpace X]` — this is exactly the transitivity: the path components of E surject onto X's fibers (monodromy transitivity).

This means the classical covering-space recognition theorem is provable with mathlib's machinery:

**Theorem (covering recognition):** Let p : E → X be a covering map with X simply connected (which implies path-connected) and E path-connected. Then p is a homeomorphism (in particular, E is the "trivial" covering).

Actually the cleanest statement via monodromy: if X is simply connected, the monodromy action of π₁(X) on p⁻¹{x} is trivial, so the fiber has ≤ 1 element... For the homeomorphism conclusion we need: p is surjective (needs X nonempty... and covering + nonempty base), p is injective (fibers singletons), and a bijective local homeomorphism is a homeomorphism.

Hmm, wait. Let me think about what's the most valuable and most provable statement here for the D12 task:

The task explicitly says: "a correct covering-space recognition". In the D7 context, `SphericalPieceRecognition.recognize` is the bridge "spherical space form + Moise smoothing". The relevant covering-theoretic sub-lemma: **a spherical space form** is S³/Γ for Γ ⊂ O(4) finite acting freely; its universal cover is S³. The recognition "simply connected spherical space form is S³" is: if M is a quotient of S³ and M simply connected then M ≃ S³. Equivalently: **if Γ acts freely on S³ and S³/Γ is simply connected, then Γ = 1 and S³/Γ ≃ S³**. 

With mathlib's machinery this is provable IF we also know S³ is path-connected (needed for `comp_subtypeVal_pathComponent_surjective`) and S³/Γ path connected (quotient of path-connected space is path connected — elementary), plus "quotient map S³ → S³/Γ is a covering map" for a finite group acting freely + properly (there's `IsQuotientCoveringMap` in `Topology/Covering/Quotient.lean` — `isQuotientCoveringMap_iff_isCoveringMap_and`!). Then:

1. S³/Γ path connected (quotient of path-connected).
2. S³ path connected.
3. q : S³ → S³/Γ is a covering map.
4. S³/Γ simply connected ⟹ q is a homeomorphism? Hmm, the classical proof: q is a covering; π₁(S³/Γ) acts on fibers (= Γ) transitively via monodromy (`comp_subtypeVal_pathComponent_surjective` + `monodromyPerm`). If π₁ = 1, fiber is a singleton, Γ = 1.

Actually more directly with `monodromyPerm`: `monodromyPerm x : FundamentalGroup X x →* Equiv.Perm (p⁻¹{x})`; and the theorem at 290 `comp_subtypeVal_pathComponent_surjective`: for e₀ : E, [PathConnectedSpace X], the map from path component... let me read the exact statement. Let me read the relevant lines 280-300 and 616-700. Then the classical proof:

If X simply connected then `FundamentalGroup X x` is... mathlib `FundamentalGroup X x` is the group of homotopy classes of loops based at x; simply connected ⟹ it's trivial (there's a lemma? `simply_connected_iff_loops_nullhomotopic` implies all loops homotopic to refl; need `FundamentalGroup` trivial ⟺ Subsingleton). Hmm, does mathlib have "SimplyConnectedSpace X → Subsingleton (FundamentalGroup X x)"? Let me check SimplyConnected.lean... The grep showed `simply_connected_iff_unique_homotopic`, `simply_connected_iff_loops_nullhomotopic`, `paths_homotopic`. The FundamentalGroup is `Path.Homotopic.Quotient`-based (defined in FundamentalGroup.lean). To show it's a subsingleton: every loop is homotopic to the constant loop (via `simply_connected_iff_loops_nullhomotopic` or `paths_homotopic`). That's a straightforward proof from the mathlib interface.

Then the fiber: `monodromyPerm x : FundamentalGroup X x →* Equiv.Perm (p⁻¹{x})` — an action. Surjectivity of the action on a *path component*: `comp_subtypeVal_pathComponent_surjective` — need to read exact statement. If X simply connected and e₁, e₂ in the fiber, and E path-connected, then there's a path from e₁ to e₂ in E; its projection is a loop at x; monodromy of that loop sends e₁ to e₂ (`monodromy_map`?). So the action is transitive when E is path-connected. With π₁ trivial, fiber has exactly one element. So p is bijective (p surjective since covering + X nonempty? For p : E → X covering map with X nonempty: every x has an evenly covered neighborhood; if X is nonempty, choose x, its fiber is nonempty (trivialization over a neighborhood containing x has nonempty fiber type — hmm, the fiber over x is ≃ I, and I could be empty! e.g. E = ∅ → X. But if E is path-connected then E is nonempty, so fibers... E nonempty and p surjective? Need X nonempty: X path-connected (from SimplyConnectedSpace) so X nonempty; p⁻¹{x} ≃ I via fiberHomeomorph; the trivialization covers a neighborhood of x... Is I nonempty iff the fiber is nonempty — yes by `fiberHomeomorph`. But can a covering map of a path-connected nonempty space have empty fibers? p⁻¹{x₀} empty for some x₀, but is p surjective... Let's see: is E → X covering with X path-connected implies p surjective (given E nonempty)? Classical: yes. From `IsCoveringMapOn.mk`: over each point there's a trivialization; the fiber over x is I... Actually simpler: the image of p: hmm. Evenly covered: for every x, an open U ∋ x with p⁻¹U ≅ U × I. If I empty, then p⁻¹U empty, so image misses U. Let V = union of such open U's... The image of p is open; also its complement is open (same argument locally). So image of p is clopen in X; X connected ⟹ image is ∅ or X. E path-connected ⟹ E nonempty ⟹ image nonempty ⟹ image = X. So p surjective. All of this is provable from mathlib (image clopen argument uses `IsCoveringMap` + preconnectedness of X: SimplyConnectedSpace gives PathConnectedSpace gives PreconnectedSpace).

Then: p bijective + p is a covering map. A bijective covering map is a homeomorphism: continuous by definition of covering map? `IsCoveringMap` includes continuity of p? Let me check the definition: `IsCoveringMap f := ∀ x, ∃ I, IsEvenlyCovered f x I` probably; `IsEvenlyCovered` includes `Topology.IsTrivialization` of the preimage... does it imply continuity? `IsCoveringMapOn.continuousAt` exists ("protected theorem continuousAt"), so continuity of p is derivable. Local homeomorphism: `IsCoveringMapOn.isLocalHomeomorphOn` exists. Bijective + local homeo + continuous + open... In mathlib there's likely a lemma "bijective continuous map from compact to Hausdorff is a homeomorphism" or "bijective local homeomorphism is a homeomorphism": `IsLocalHomeomorph` + bijective → homeo? Hmm, mathlib has `Homeomorph.ofBijective`? There's `IsLocalHomeomorph.isHomeomorph`? Let me check `Mathlib/Topology/IsLocalHomeomorph.lean`.

Alternatively use the lifting characterization: `existsUnique_continuousMap_lifts` for the section: if X is simply connected and locally path connected (is X locally path connected? — need it! `LocallyPathConnectedSpace X` is an assumption in mathlib's lifting theorem. Hmm. For a covering map, the base being locally path connected... a covering map of a locally path connected space has locally path connected base? For the sphere/circle yes. For the recognition theorem we might need `[LocallyPathConnectedSpace X]` as a hypothesis — that's a "standard regularity/domain hypothesis" which is allowed and can be justified (e.g. manifolds are locally path connected; quotients of S³ by finite groups are). Hmm, but can we discharge it for S³/Γ? That needs S³ locally path connected (is `Metric.sphere` locally path connected in mathlib? There's `LocallyPathConnected` for Euclidean spaces? mathlib has `IsLocallyPathConnected` instances for... hmm, `locallyPathConnectedSpace_of_isLocallyPathConnected`? I recall instances for `Set` like intervals, balls are convex. For spheres: there might be an instance via local homeomorphism to ℝⁿ (stereographic projection — mathlib has `stereographic`? There's `Mathlib.Geometry.Euclidean.Sphere.Ptolemy`? Hmm.)

This is getting deep. Let me scope: the cleanest, most valuable provable statement with these ingredients:

**CoveringRecognition: for a covering map p : E → X with X simply connected and E path-connected (both nonempty), p is a homeomorphism.** With assumptions `[LocallyPathConnectedSpace X]` if the lifting API needs it... Actually, I can avoid `LocallyPathConnectedSpace` entirely by using only path-lifting (`exists_path_lifts`, which needs... let me check its hypotheses — probably `IsCoveringMapOn p ...` and continuity; path lifting doesn't need local path connectedness of X; it works for any covering map. `liftHomotopyRel` needs `PreconnectedSpace A`. Let me read the file carefully.

The monodromy approach:
- `exists_path_lifts : ∃ Γ : C(I,E), p ∘ Γ = γ ∧ Γ 0 = e` — needs `cov : IsCoveringMapOn p s` with `γ` mapping into s. Fine.
- `monodromy_bijective : monodromy γ` is a bijection of fibers.
- `monodromyPerm : FundamentalGroup X x →* Equiv.Perm (p⁻¹{x})`.
- To show fibers are subsingletons: for e, e' ∈ p⁻¹{x}, path γ from e to e' (E path-connected); δ := p ∘ γ is a loop at x; need `monodromy δ e = e'`. Is that in mathlib? Look at `monodromy_map` (line 416): `theorem monodromy_map {x y : E} (γ : Path.Homotopic.Quotient x y)` — let me read the exact statements around 389-465. Probably: if γ is a path in E from e to e', then `monodromy (map p γ) e = e'`. Yes that's the standard identity.

Then with π₁(X) trivial: `monodromyPerm x g = 1` for all g, so e' = monodromy δ e = e. Hence fiber subsingleton. Need "FundamentalGroup is a subsingleton when simply connected": prove via `simply_connected_iff_loops_nullhomotopic` → all loops nullhomotopic → all in the same class as refl → `Subsingleton (FundamentalGroup X x)`.

Actually — maybe simpler: mathlib might have `monodromy_eq_of_map_eq` (line 422): `monodromy_eq_of_map_eq {x y : X} {γ : Path.Homotopic.Quotient x y}` — that's for relating two lifts? Let me read the file.

OK, this is a genuine 2-3 hour project. It directly matches the task objective: "a correct covering-space recognition". The statement:

```
theorem covering_recognition {p : E → X} (cov : IsCoveringMap p)
    [SimplyConnectedSpace X] [PathConnectedSpace E] [Nonempty E] :
    ∃ h : Homeomorph E X, h.toFun = p
```

Hypotheses: `[SimplyConnectedSpace X]` (includes path-connectedness? Let me check the class: `class SimplyConnectedSpace (X) : Prop where pathConnected : PathConnectedSpace X; isNullhomotopic : ...`? From the earlier grep, line 40: `class SimplyConnectedSpace (X : Type*) [TopologicalSpace X] : Prop where ...` — need to see fields. It likely has `pathConnected` and `paths_homotopic` uniqueness. Let me read the file fully to get exact APIs.

And `[PathConnectedSpace E]` — justified: the total space of the universal covering / the spherical space form is path-connected (S³ path connected, quotient of path-connected). Also `Nonempty E`.

Also for surjectivity need X nonempty — comes from SimplyConnectedSpace (path connected ⟹ nonempty).

Wait, also for "p bijective": fibers subsingleton + p surjective. Surjectivity via clopen image argument needs X preconnected — yes from SimplyConnectedSpace → PathConnectedSpace → PreconnectedSpace. 

Hmm wait, actually there's an even cleaner route for surjectivity: if I have the section via `existsUnique_continuousMap_lifts`... that needs `LocallyPathConnectedSpace X`. The clopen argument needs only preconnectedness + the local structure of covering maps. Let me write out the clopen argument:
- `IsCoveringMap p` : ∀ x : X, ∃ I, IsEvenlyCovered p x I. `IsEvenlyCovered p x I` gives a trivialization t : Trivialization I p with x ∈ t.baseSet. Hmm — the trivialization `toTrivialization'` covers `f ⁻¹' t.baseSet`? For our argument: over t.baseSet (open, contains x), p⁻¹(baseSet) ≅ baseSet × I. If I is empty then p⁻¹(baseSet) = ∅, so image of p is disjoint from baseSet, hence baseSet ⊆ (image p)ᶜ. If I is nonempty then baseSet ⊆ image p. So locally, either the whole neighborhood is in the image or disjoint from it. Hence image is clopen: for any x ∈ image p, need a neighborhood inside image: take the evenly covered neighborhood U of x; since x ∈ image, p⁻¹{x} nonempty so I nonempty, so U ⊆ image. For x ∉ image: U ∋ x evenly covered with fiber I; p⁻¹{x} = ∅, so I = ∅? Wait — p⁻¹{x} = ∅ but p⁻¹ U could be nonempty (covering another point of U)! Hmm, the trivialization: p⁻¹U ≅ U × I where I ≅ p⁻¹{x}. If p⁻¹{x} = ∅ then I = ∅, so p⁻¹U = ∅, hence U ∩ image = ∅. Good — so image p is clopen. With X preconnected, image = ∅ or X. E path-connected and nonempty ⟹ image nonempty ⟹ surjective. All provable with the mathlib API (fiberHomeomorph gives I ≃ p⁻¹{x}; if p⁻¹{x} empty then I empty: from `fiberHomeomorph` being an equiv, p⁻¹{x} empty ⟹ I empty and vice versa).

This is all quite feasible but non-trivial. Let me read the exact mathlib APIs first. Then write the module `CoveringRecognition.lean`.

Downstream use for the task: in the spherical-piece recognition context: "simply connected spherical space form is S³". The versioned lemma: if X = S³/Γ (IsQuotientCoveringMap, free action...), and X simply connected, then X ≃ S³. That needs S³ path-connected + the quotient covering map. mathlib has `IsQuotientCoveringMap` with `isQuotientCoveringMap_iff_isCoveringMap_and`. S³ path-connected: mathlib has `Metric.sphere` path-connected? There's `sphere_isPathConnected`? Let me check... For EuclideanSpace ℝ (Fin 4), the unit sphere: mathlib has `instPathConnectedSpaceSphere`? I recall `Mathlib.Geometry.Euclidean.Sphere.Basic` has... not sure. There might be `pathConnectedSpace_sphere` hmm. Let me search. If S³ path-connectedness is missing, I can prove it myself for S³ concretely: it's not too bad? Proving path-connectedness of S³: any two points on S³ can be joined by an arc: stereographic + segments + nlinarith... Actually a neat trick: since we already have `DoubleBall ≃ S³` and DoubleBall is path connected (gluing of two path-connected balls along nonempty overlap — needs some work), hmm.

Alternatively prove path-connectedness directly: for any two points x, y ∈ S³, if y ≠ -x, the segment t ↦ (1-t)x + t y stays nonzero... on the sphere you need to normalize: t ↦ ((1-t)x + t y)/‖(1-t)x + t y‖ — norm stays 1 if we normalize. Continuous where denominator nonzero, which holds when y ≠ -x; else go via a third point z ≠ ±x. That's the standard proof, quite feasible with mathlib (continuous functions + division + `norm_ne_zero`).

Hmm, but wait — do I even need to go as far as the spherical-space-form application? The task asks for "a correct covering-space recognition" as an example of a substantive lemma replacing an assumed field. A cleaner downstream use within our own D12 architecture: the D7 `recognize` bridge has the flavor "simply connected spherical piece ≃ S³". The covering-recognition lemma is the topological core: "a covering of a simply connected base by a path-connected space is a homeomorphism". The direct application: a spherical piece IS (up to homeo) a quotient S³/Γ; its universal cover is S³... but that requires π₁(S³)=1 and covering classification, NOT in mathlib. So I can't chain it all the way to recognize in this invocation.

The honest deliverable: the general covering-recognition theorem + a versioned `SphericalPieceRecognitionV2`/`RecognizeFromCovering` interface that states exactly the remaining inputs (finite free group action on S³, quotient covering, etc.) as data, with the covering-recognition lemma discharging the "simply connected ⟹ trivial cover" step. Plus non-vacuity: apply covering_recognition to a concrete example: id covering of S³? Trivial example: `IsCoveringMap (id : S3 → S3)`? `IsEvenlyCovered (id) x PUnit` with trivialization... there might be an existing instance `isCoveringMap_id`? Hmm, probably not; I can build the trivialization manually — U = univ, p⁻¹U = univ, trivialization to univ × PUnit: `IsTrivialization` for id... There's likely `IsTrivialization` constructors in mathlib FiberBundle; simpler: `IsEvenlyCovered.mk`? Let me check the API: `IsEvenlyCovered` is a structure: `evenCovering : ...`? Let me read Basic.lean's structure fields.

Wait, actually — maybe better to directly use an existing mathlib example of a covering map: `Mathlib/Topology/Covering/AddCircle.lean` — the covering ℝ → AddCircle (circle). But SimplyConnectedSpace (AddCircle) is false. Hmm.

Non-vacuity for covering recognition: use the double-ball! No... Simplest non-vacuity: `p = id : S3 → S3` with... hmm wait, actually more meaningful non-vacuity: take E = S3 × Fin 2 (disjoint union of two copies), p = fst : S3 × Fin 2 → S3. E is NOT path-connected, so hypothesis fails — good negative test but not a witness.

A positive witness: E = S3, p = id, SimplyConnectedSpace S3 — but π₁(S³)=1 isn't in mathlib! So I can't discharge `[SimplyConnectedSpace S3]` without proving it. Hmm. So non-vacuity of covering_recognition requires some simply connected space... `PUnit`! PUnit is simply connected? There's `SimplyConnectedSpace PUnit` instance? `Mathlib/AlgebraicTopology/FundamentalGroupoid/PUnit.lean` — "PUnit.lean" exists! It likely proves `SimplyConnectedSpace PUnit` or the fundamental groupoid of PUnit is trivial. Let me check. If `SimplyConnectedSpace PUnit` holds, then covering_recognition applied to p : PUnit → PUnit gives the witness. That's degenerate but honest non-vacuity of the theorem shape. Hmm, "zero operators masquerading as geometry" — PUnit is a real topological space, and the theorem is general; a PUnit witness is weak but the theorem itself is the deliverable. Better: also prove `S3` path-connected (that's needed anyway for future spherical-space-form work and is a genuine geometry lemma), and maybe prove `SimplyConnectedSpace` for... no, S³ simply connected requires van Kampen. 

Hmm wait — actually, maybe I CAN prove `SimplyConnectedSpace S3`! Via covering theory: S³ is the universal cover... no. Alternative: mathlib's `SimplyConnectedSpace` ⇔ all paths homotopic + path connected. For S³: hmm.

There's a classical elementary proof that Sⁿ is simply connected for n ≥ 2 WITHOUT van Kampen: **every loop in Sⁿ is nullhomotopic** via: any loop γ can be homotoped (in Sⁿ) to avoid a point p (by a "radial push-off" argument / simplicial approximation); then Sⁿ ∖ {p} ≅ ℝⁿ is contractible, and the loop is nullhomotopic there. Formalizing "homotope a loop off a point" needs uniform continuity / polygonal approximation or a nice explicit radial construction... With explicit geometry: for a loop γ : I → Sⁿ, perturb... The standard proof: subdivide I finely so each γ|[tᵢ,tᵢ₊₁] lands in a hemisphere avoiding p... that's a real project.

There IS another classical route using our own DoubleBall! Since DoubleBall ≃ S³ and... no, that doesn't help with homotopy either.

OK. Let me scope this invocation:
1. Read the Lifting.lean API precisely.
2. Prove `covering_recognition` (general covering-space recognition: path-connected total space over a simply connected base is a homeomorphism). This is a substantive general topology theorem directly answering "covering-space recognition" from the task objective.
3. Prove `S3` path-connected (needed for the spherical-space-form application; genuine Euclidean geometry lemma with explicit arcs). 
4. Versioned `SphericalPieceRecognitionV2`-style bridge in the audit-visible ExpandedInterfaces: state the quotient data (finite group action, quotient covering) and show covering_recognition discharges the simply-connected case. Actually — even better: directly tie to D7: `RecognizeV2` interface: for a piece P with a covering S³ → P (data: the quotient map is a covering), SimplyConnectedSpace P.Carrier + path-connected S³ ⟹ P ≃ S³ — wait, direction: covering p : S³ → P, P simply connected, S³ path connected (proved) ⟹ p is a homeomorphism ⟹ P ≃ S³. THIS IS EXACTLY the "spherical space form recognition" topological core: "a simply connected spherical space form is S³". The remaining hypotheses are: the covering-map condition (discrete group action on S³ ⟹ quotient is a covering — provable from `IsQuotientCoveringMap` + freeness/properness data) and the "piece is covered by S³" data (which encodes "spherical"). 

Hmm, but hold on — for a *spherical space form*, p : S³ → S³/Γ is the covering, base P = S³/Γ. So the statement: `coveringRecognitionForSphericalPiece : covering map p : S3 → P.Carrier → SimplyConnectedSpace P.Carrier → Nonempty (P.Carrier ≃ₜ S3)` — instantiating covering_recognition with E = S3 (path-connected — proved) and X = P (simply connected — hypothesis). This directly replaces the *topological core* of `SphericalPieceRecognition.recognize`, whose remaining content is: (i) "a spherical piece admits a covering by S³" (the space-form theorem: finite π₁, geometrization...), (ii) Moise smoothing (topological piece is smooth). Those remain hypotheses — documented.

That is a substantive, coherent, downstream-tied deliverable. Let me verify the mathlib APIs support it. Key needs:
- `IsCoveringMap` definition & lemmas (Basic.lean): surjectivity argument needs the clopen-image proof with `IsEvenlyCovered`/`fiberHomeomorph`.
- Lifting.lean: path lifting, monodromy identity (`monodromy_map`-ish), `monodromyPerm`.
- `SimplyConnectedSpace`: fields; get "FundamentalGroup subsingleton".
- Path-connectedness of S3: prove myself.
- Continuity of p from IsCoveringMap: `IsCoveringMap.continuousAt` / `continuousOn`... need full continuity: `IsCoveringMapOn f univ → Continuous f`? There's `protected theorem continuousOn` — for univ that's continuity. OK.
- Bijective + continuous + open → homeomorphism. mathlib: `Homeomorph.ofBijective`? or use `existsUnique_continuousMap_lifts` to construct the inverse: p has a section s : X → E (unique lift of id_X)! `existsUnique_continuousMap_lifts` with A = X, f = id : C(X,X), and g = s needs [SimplyConnectedSpace X] [LocallyPathConnectedSpace X] — needs locally path connected X. Ugh. Alternatively: since p is bijective, continuous, and `IsLocalHomeomorphOn p univ`, get the inverse continuous via `IsLocalHomeomorphOn` + bijectivity: hmm, mathlib `IsLocalHomeomorphOn` — is there a lemma "bijective local homeomorphism is a homeomorphism"? Let me search IsLocalHomeomorph.lean.

Actually, simpler classical fact I can prove with monodromy more directly: the FIBERS are subsingletons, p is continuous and surjective... then to show p is a homeomorphism I need the inverse continuous. For a covering map that's a bijection: p is open (local homeo ⟹ open map locally... IsLocalHomeomorphOn gives local homeo on univ ⟹ p is an open map? A local homeomorphism maps open sets to open sets? Yes: image of open U = ⋃ over local homeo neighborhoods; standard: if p is locally injective with each point having an open neighborhood mapped homeomorphically, then p(U) is open. mathlib has `IsLocalHomeomorphOn.isOpen_image`? or `isOpenMap`? Let me check `IsLocalHomeomorph.lean`.

Plan: read the three mathlib files carefully (IsLocalHomeomorph, Covering.Basic around IsEvenlyCovered/trivialization/fiberHomeomorph, Lifting.lean fully, SimplyConnected.lean, FundamentalGroup.lean key defs). Then write the module.

Let me also kick off, in parallel, the audit re-run and forbidden scan for the current files, and the sha256 computation. Let me do that now as a background job while I read mathlib sources.

Actually the audit needs `lake env lean Poincare/D12/SurgeryRecognition/Audit.lean` from release/. It produces 128 `#print axioms` outputs. I want a programmatic check: parse outputs and verify each cone ⊆ {propext, Classical.choice, Quot.sound}. Let me write a script: run the audit, extract each declaration's axiom list, and check. The output format of `#print axioms foo` is:

```
'foo' depends on axioms: [propext, Classical.choice, Quot.sound]
```

I can parse that. Let me create a small script under tools/ (task-local audit module — the task says "add ... a task-local audit module"; Audit.lean already exists; a checker script improves fail-closed-ness). Actually the previous invocation may have had a checker; let me check tools/ — only d6_*.py scripts exist. So the previous audit was done by eyeballing the output? The result card says "fail-closed programmatic axiom audit" — the programmatic part was the Audit.lean itself (each `#print axioms` would surface sorryAx). A parser script strengthens it. Let me write tools/d12_axiom_audit.py that runs the audit and fails if any nonstandard axiom appears or if the expected declaration list is incomplete.

Let me start:
1. Background: write+run the forbidden scan and sha256 collection now (quick).
2. Read mathlib files for the covering theorem.
dsh: reasoning:
My grep regex was wrong: the output spans multiple lines (the axiom list wraps). "depends on axioms: [propext," followed by "Classical.choice, Quot.sound]" on the next line. So the multi-line grep misfires. I need a proper parser: read the full file, extract each `'decl' depends on axioms: [...]` block (possibly multi-line), collect the axiom names, and check each ∈ {propext, Classical.choice, Quot.sound}. Let me write a Python script for this (fail-closed: nonzero exit if anything nonstandard or if the declaration count doesn't match expectations).
dsh: reasoning:
My parser has a bug: for a single-line block like `[propext, Classical.choice, Quot.sound]`, `text.find("]", j)` finds the closing bracket fine, but the body extraction... The issue: for multi-axiom single-line blocks, the regex `^(...)\]$` with DOTALL — I used `re.search` for the start and `find` for `]`. The output shows extra axiom `'Quot.sound]'` — meaning the bracket content included `Quot.sound]`?? That means my `text.find("]", j)` found the closing bracket but the slice `body = text[j:k+1]` — hmm wait, the extra shows `Quot.sound]` with the closing bracket attached, meaning the actual `]` found was a LATER `]` (from the NEXT declaration block!), because `find` finds the first `]` — no wait, if the first `]` after j is the correct one, body would be `[...]` exactly. Unless... the first `]` after j belongs to the current block, so body = `[propext, Classical.choice, Quot.sound]`, inner = `propext, Classical.choice, Quot.sound`, split gives clean names. But the output shows `Quot.sound]` as an axiom name — so the `]` char got included in a name. That happens when body is `[propext,\n Classical.choice, Quot.sound]]`? No...

Wait — maybe the audit output contains lines like `depends on axioms: [propext, Classical.choice, Quot.sound]` but ALSO the leading context: `'X' depends on axioms: [propext,` — the regex `'([^']+)' depends on axioms:` matches at the line start... then j = position after colon; body = text[j : k+1] where k = first `]` after j. Hmm, but there could be a `]` INSIDE the declaration name of a LATER block? No, the first `]` after j is the one closing the current bracket.

Hmm, unless the axiom list itself contains something like `[propext, Classical.choice, Quot.sound]` and I have it right... but the printout shows `'Quot.sound]'` — that looks like body was `[propext, Classical.choice, Quot.sound]]`? Where would the extra `]` come from? OH WAIT. I see it — the `#print axioms` output in some Lean versions prints:

```
'X' depends on axioms: [propext, Classical.choice, Quot.sound]
```

but for declarations that print with a WARNING line before them, e.g.:

```
warning: ...
'X' depends on axioms: [...]
```

The problem: my start-regex searches `'([^']+)' depends on axioms:` from position i. For the NEXT block, i = k+1 = position right after the first `]`. That should be fine...

Hmm, let me look at the actual raw output to see what's happening.
dsh: reasoning:
I see: multi-line blocks where the closing `]` appears on its own line. The `text.find("]", j)` should still find the right one... Oh wait, I see the bug now! `re.search(r"'([^']+)' depends on axioms:", text[i:])` — `m.end()` is the end of the match. Then `k = text.find("]", j)` — the FIRST `]` after the colon. That's the current block's closing bracket. body = text[j:k+1] = ` [propext,\n Classical.choice,\n Quot.sound]`. inner = `[propext,\n Classical.choice,\n Quot.sound]`. strip → starts with `[` → inner = `propext,\n Classical.choice,\n Quot.sound]`... wait no: `inner = inner[1:].strip()` removes the first `[`, leaving `propext,\n Classical.choice,\n Quot.sound]` — the trailing `]` is STILL there because I only stripped the leading `[`, and the trailing `]` is not whitespace!

Bug: I need to strip the trailing `]` too: `inner = inner[1:-1].strip()` after confirming it ends with `]`.

Fix: body = text[j:k+1] is `[... ]` including brackets; inner should be body.strip()[1:-1].strip().
dsh: reasoning:
Audit passes: 128/128 declarations, all cones ⊆ {propext, Classical.choice, Quot.sound}. 

Now the forbidden scan + sha256, and then the main new work: the covering-recognition theorem. Let me first run the scan + hashes (quick), then read the mathlib Lifting/Basic APIs carefully.
dsh: reasoning:
All files clean, hashes computed. Now the main new work: the covering-space recognition theorem. Let me read the mathlib APIs precisely:

1. `Mathlib/Topology/Covering/Basic.lean` — `IsCoveringMap`, `IsEvenlyCovered`, `fiberHomeomorph`, continuity lemmas.
2. `Mathlib/Topology/Homotopy/Lifting.lean` — path lifting, monodromy, `monodromyPerm`.
3. `Mathlib/AlgebraicTopology/FundamentalGroupoid/SimplyConnected.lean` + `FundamentalGroup.lean` — get `Subsingleton (FundamentalGroup X x)` from SimplyConnectedSpace.
4. Check `IsLocalHomeomorph` for openness / inverse continuity.
5. Check path-connectedness instances for `Metric.sphere` in mathlib.

Let me read Lifting.lean's key sections (around lines 40-140 for exists_path_lifts hypotheses, 280-470 for monodromy).
dsh: reasoning:
This is Hatcher-style lifting theory. Now let me look at the monodromy section (lines 200-470) which I need most: `exists_path_lifts`, `liftPath`, `monodromy`, `monodromyPerm`, and `monodromy_map`.
dsh: reasoning:
`comp_subtypeVal_pathComponent_surjective` is exactly the monodromy transitivity. Note it requires only `[PathConnectedSpace X]` — no local path connectedness needed! Great.

Now the monodromy section around lines 380-470:
dsh: reasoning:
The monodromy machinery is complete:
- `cov.monodromy {x y} (γ : Path.Homotopic.Quotient x y) : p⁻¹{x} → p⁻¹{y}`
- `cov.monodromyPerm (x) : FundamentalGroup X x →* Equiv.Perm (p⁻¹{x})`
- `monodromy_map : cov.monodromy (γ.map ⟨p, cov.continuous⟩) ⟨x, rfl⟩ = ⟨y, rfl⟩` — a path in E from x to y gives monodromy of the projected loop sending x to y. This is the transitivity identity!

So the classical proof for injectivity:
- Fix x : X, e, e' ∈ p⁻¹{x}.
- E path-connected ⟹ exists path γ : Path e e' (a Path in E from e to e').
- Its image δ := γ.map ⟨p, cov.continuous⟩ is a loop at x (since p e = x = p e'). As Path.Homotopic.Quotient: `(γ.map ⟨p, cov.continuous⟩)` — need the endpoints to match: `γ.map` of a path from e to e' is a path from p e to p e' = x → x. Good.
- `monodromy_map` gives `cov.monodromy (γ.map ⟨p, cov.continuous⟩) ⟨e, rfl⟩ = ⟨e', rfl⟩` — wait, the statement is about `x y : E` there; let me recheck: `theorem monodromy_map {x y : E} (γ : Path.Homotopic.Quotient x y) : cov.monodromy (γ.map ⟨p, cov.continuous⟩) ⟨x, rfl⟩ = ⟨y, rfl⟩`. Yes: monodromy along the projected path sends the fiber point x (= lift of p x) to y. 

- If X is simply connected, then `FundamentalGroup X x` is a subsingleton, so the loop class δ = refl, so `monodromy δ = monodromy refl = id`, so e' = id e = e. Hence fiber subsingleton, p injective.

Need: "SimplyConnectedSpace X ⟹ Subsingleton (FundamentalGroup X x)". The FundamentalGroup is defined as... let me check its definition: `FundamentalGroup X x := End (FundamentalGroupoid X x)`? Let me grep. From FundamentalGroup.lean line 44: `theorem one_def : (1 : FundamentalGroup X x) = .refl x := rfl`. So the fundamental group elements are `Path.Homotopic.Quotient x x` essentially (`.refl x` notation). `paths_homotopic` in SimplyConnected gives any two paths with same endpoints are homotopic: `theorem paths_homotopic {x y : X} (p₁ p₂ : Path x y) : Path.Homotopic p₁ p₂`. From that, `Path.Homotopic.Quotient` classes: hmm, is `Path.Homotopic` the same relation as the quotient's setoid? `Path.Homotopic.Quotient.mk` with `Path.Homotopic.setoid x y` — `≈` is `Path.Homotopic`. Let me verify the definition of `Path.Homotopic.Quotient` and the setoid relation. In mathlib `Mathlib.Topology.Homotopy.Path`: `def Homotopic (p₀ p₁ : Path x₀ x₁) : Prop := Nonempty (p₀.Homotopy p₁)` and `Quotient` with that setoid. SimplyConnected.lean's `paths_homotopic` gives `Path.Homotopic p₁ p₂` — same relation presumably.

So proof of subsingleton: `by intro a b; obtain ⟨a⟩ := a; obtain ⟨b⟩ := b; exact congrArg _ (Quotient.sound ...)`. Actually `Quotient.sound : p ≈ q → Quotient.mk p = Quotient.mk q` where ≈ is `Path.Homotopic`? Then subsingleton follows: `subsingleton_of_forall_eq`... need `a = b` for all a b: use Quotient.inductionOn₂, then apply Quotient.sound with `paths_homotopic p q`. `Quotient.sound` here is an axiom — allowed (it's in the allowed cone; indeed the D7/12 audit allows Quot.sound).

- Surjectivity of p: X path connected (from SimplyConnectedSpace) and E path-connected nonempty. Use `comp_subtypeVal_pathComponent_surjective e₀ [PathConnectedSpace X]`: for every x : X there's a path-component point mapped to x — gives surjectivity directly (no clopen argument needed)! `(cov.comp_subtypeVal_pathComponent_surjective e₀ x)` produces `y : pathComponent e₀` with `p y = x`. pathComponent e₀ is a subtype of E; to get the point in E: `⟨(y : E), y.2⟩`? The function is `fun x : pathComponent e₀ ↦ p x` where pathComponent is a Set (subtype). Surjective: `∀ x : X, ∃ y : pathComponent e₀, p y = x`. Then p is surjective as a map E → X provided we can extract the underlying E point: `∃ e : E, p e = x` from `⟨(y.1), ...⟩`. Yes.

Wait — but hold on, is `pathComponent e₀` nonempty? Yes, e₀ ∈ pathComponent e₀.

- p is continuous: `cov.continuous` — the namespace has `cov : IsCoveringMap p` and there's a `cov.continuous` used in `⟨p, cov.continuous⟩` in monodromy_map. So `IsCoveringMap.continuous` exists. 

- Inverse continuity: p is bijective, continuous. To get a homeomorphism I need continuity of the inverse. Options:
  (a) Use `existsUnique_continuousMap_lifts` with A = X, f = id : C(X, X), g = p⁻¹, p∘g = id; requires [SimplyConnectedSpace X] [LocallyPathConnectedSpace X]. Need `LocallyPathConnectedSpace X` — for SimplyConnectedSpace X, is X locally path connected? NOT automatic in mathlib. Hmm. For our application (spherical space forms) X = S³/Γ is locally path connected, but that's extra work. 
  (b) Prove "bijective local homeomorphism is a homeomorphism": p is a local homeomorphism on univ (`IsCoveringMapOn.isLocalHomeomorphOn`, and `IsCoveringMap.isCoveringMapOn`). Then the inverse is continuous: for y ∈ X, let x = p⁻¹ y... need the local homeo structure: `IsLocalHomeomorphOn p univ` gives for each e ∈ E an open neighborhood U of e with p |U a homeo onto open p(U). For the inverse continuity at y = p e: V := p(U) open neighborhood of y, and p⁻¹|V = (p|U)⁻¹ continuous... but that requires the fiber over V to be exactly U — not true globally; local homeo inverse continuity: the inverse of a bijective local homeomorphism IS continuous (standard). Proof: for y₀ = p e₀ with U ∋ e₀ open, p(U) = V open; on V, p⁻¹ agrees with (p|U)⁻¹: for y ∈ V, p⁻¹ y ∈ U? Since p is injective and p((p|U)⁻¹ y) = y = p(p⁻¹ y), injectivity ⟹ (p|U)⁻¹ y = p⁻¹ y. So p⁻¹|V = (p|U).symm|V... wait (p|U)⁻¹ maps V → U; composition with inclusion gives continuity at y₀. So the inverse is continuous at every point. Formalizing: is there a mathlib lemma "bijective local homeomorphism is a homeomorphism"? Let me check IsLocalHomeomorph.lean.

Let me check mathlib for `IsLocalHomeomorph` results: `isLocalHomeomorph_iff_bijOn`? or `Homeomorph` from `IsLocalHomeomorph` + bijective? Search.
dsh: reasoning:
`toHomeomorphOfBijective (hf : IsLocalHomeomorph f) (hb : f.Bijective) : X ≃ₜ Y` exists — exactly what I need for the final step.

So the full plan for `CoveringRecognition.lean`:

```lean
import Mathlib.Topology.Covering.Basic
import Mathlib.Topology.Homotopy.Lifting
import Mathlib.AlgebraicTopology.FundamentalGroupoid.FundamentalGroup
import Mathlib.AlgebraicTopology.FundamentalGroupoid.SimplyConnected
```

Wait — sandbox note: "shared mathlib .lake/build is read-only to this sandbox; only leaf-module imports whose oleans exist can be used (e.g. Mathlib.Topology.Homeomorph.Defs/.Lemmas, not the aggregator Mathlib.Topology.Homeomorph)". Hmm — that says only leaf modules whose oleans EXIST can be imported. This is important! The mathlib build cache may be partial. `Mathlib.Topology.Homotopy.Lifting` — does its olean exist in the shared build? The previous invocations imported `Mathlib.Topology.Homeomorph.Defs/.Lemmas` etc. If `Mathlib.Topology.Homotopy.Lifting`'s olean is absent and we can't rebuild (read-only cache), the import fails. BUT: `lake build` of the release package rebuilds dependencies as needed... The sandbox note says shared mathlib .lake/build is read-only to THIS sandbox. Hmm, but earlier builds ran fine (2579/2719/8926 jobs) — those rebuild things into... Let me just test: try importing `Mathlib.Topology.Homotopy.Lifting` in a scratch file and build. If it works, great.

Actually wait — the sandbox note says the aggregator `Mathlib.Topology.Homeomorph` can't be imported but its children can. That suggests the oleans present in the shared cache are for leaf modules (the granular files) but aggregators missing? That's odd but whatever — the D12 modules already import `Mathlib.Topology.Constructions`, `Mathlib.Geometry.Manifold.Instances.Sphere` etc. which worked. Let me check which mathlib modules were already imported successfully: BallGluing imports `Mathlib.Analysis.InnerProductSpace.EuclideanDist`, `Mathlib.Geometry.Manifold.Instances.Sphere`, `Mathlib.Topology.Constructions`, `Mathlib.Topology.Constructions.SumProd`, `Mathlib.Topology.Homeomorph.Defs/.Lemmas`. OK.

Test: create scratch file importing the covering/lifting files and build. The dependency graph of `Mathlib.Topology.Homotopy.Lifting` is big but the cache may have the oleans. Let me test right away.

Also check `Mathlib.Topology.Covering.Basic` deps: it imports FiberBundle etc.

Let me write the scratch test.
dsh: reasoning:
All the needed mathlib modules import and compile. Now let me check a few more API details before writing the module:

1. `SimplyConnectedSpace` class fields — need to see exactly what it provides (pathConnected).
2. `paths_homotopic` signature.
3. `FundamentalGroup` definition to prove `Subsingleton (FundamentalGroup X x)` from SimplyConnectedSpace.
4. `IsCoveringMap` def + `continuous` + `isLocalHomeomorphOn` names.
5. `PathConnectedSpace.somePath` signature.
6. `pathComponent` definition.

Let me read SimplyConnected.lean fully and check the relevant parts of Basic.lean for IsCoveringMap.
dsh: reasoning:
Everything I need is present:
- `SimplyConnectedSpace` gives instances: `Subsingleton (Path.Homotopic.Quotient x y)`, `Subsingleton (FundamentalGroup X x)`, `PathConnectedSpace X`.
- `IsCoveringMap := ∀ x, IsEvenlyCovered f x (f ⁻¹' {x})`.
- `cov.continuous` exists (used in monodromy_map as `⟨p, cov.continuous⟩` — I should double-check its name; let me grep).

Now the proof of `coveringRecognition`:

```lean
theorem coveringRecognition {E X : Type} [TopologicalSpace E] [TopologicalSpace X]
    (p : E → X) (cov : IsCoveringMap p)
    [PathConnectedSpace E] [SimplyConnectedSpace X] [Nonempty E] :
    ∃ e : E ≃ₜ X, e = p := ...
```

Steps:
1. Surjectivity: `cov.comp_subtypeVal_pathComponent_surjective e₀ [PathConnectedSpace X]` where e₀ : E via Nonempty.some + `PathConnectedSpace` — hmm wait, `comp_subtypeVal_pathComponent_surjective` requires `[PathConnectedSpace X]` — provided by `SimplyConnectedSpace`'s instance. But is it an argument or typeclass inferred? `theorem comp_subtypeVal_pathComponent_surjective (e₀ : E) [PathConnectedSpace X] : Surjective (fun x : pathComponent e₀ ↦ p x)` — the PathConnectedSpace is a typeclass instance found via SimplyConnectedSpace. 

From `h : Surjective (fun x : pathComponent e₀ ↦ p x)`, get surjectivity of p: for x : X, `h x` gives `⟨y : pathComponent e₀, hy : p y = x⟩`; then `⟨(y : E), hy⟩`? pathComponent e₀ is `Set`-subtype: `pathComponent e₀ : Set E` — hmm, is it a Set E or a connected component? In mathlib `pathComponent (x : X) : Set X`? Let me check: in the theorem, `fun x : pathComponent e₀ ↦ p x` — `x : pathComponent e₀` coerces to E. If `pathComponent e₀` is a `Set E`, then `p x` uses the coercion. Then `∃ y : pathComponent e₀, p y = x`, and `(y : E)` gives the element. 

2. Injectivity: fix e₁ e₂ : E with p e₁ = p e₂, so both ∈ p⁻¹{p e₁} (fiber over x₀ := p e₁):
   - Path γ from e₁ to e₂: `PathConnectedSpace.somePath e₁ e₂`? Wait, `PathConnectedSpace.joined` gives `Joined e₁ e₂ := ∃ γ : Path e₁ e₂, True`. `somePath` gives a specific path. In mathlib, `PathConnectedSpace.somePath`? Let me check the PathConnected class API... In comp_subtypeVal they used `PathConnectedSpace.somePath (p e₀) x`. Good, `somePath` exists.
   - Let δ : Path.Homotopic.Quotient x₀ x₀ := (γ.map ⟨p, cov.continuous⟩) — wait, γ.map of Path e₁ e₂ gives Path (p e₁) (p e₂); with p e₁ = p e₂ = x₀ this is a Path x₀ x₀; then take its quotient class `Path.Homotopic.Quotient.mk γ.map...
`toHomeomorphOfBijective` for local homeomorphisms exists — perfect for the final step. Let me test whether the covering/lifting modules' oleans are available in the shared build cache (the sandbox note warns about partial availability).
dsh: TRANSPORT: DeepSeek API request to https://api.deepseek.com failed
