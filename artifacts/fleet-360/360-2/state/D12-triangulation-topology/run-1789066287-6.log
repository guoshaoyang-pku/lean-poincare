dsh: reasoning:
Let me start by understanding the current state of the worktree, the checkpoint, and the plan. This is a continuation of a long-running task. Let me inspect the workspace.
dsh: reasoning:
The task is in progress. There's a checkpoint and existing work. Let me look at the current state more carefully — read the full checkpoint.json, the next_plan, the LONG_PLAN.json, and the existing Lean files to figure out what's next.

Let me read the rest of checkpoint.json and the main files.
dsh: reasoning:
The task continues. Current state: three homeomorphism theorems proved (cone, suspension, double disk gluing), with audits. Next plan: covering lemma C1-C4 for coveringOfSimplyConnectedIsHomeo.

Let me check:
1. The existing Lean files to understand conventions.
2. The frenzymath snapshot — check CoveringSpaces.lean for the HLP.
3. Mathlib's IsCoveringMap API — check what's available in the pinned mathlib.

Let me look at the mathlib IsCoveringMap API first.
dsh: reasoning:
The mathlib package is in release/.lake/packages. Let me look at the covering theory in mathlib (Topology/Covering).
dsh: reasoning:
The frenzymath snapshot doesn't contain .lean files directly? Let me check the structure more.
dsh: reasoning:
Good. Mathlib has a full IsCoveringMap API. The frenzymath snapshot appears to be empty of .lean files? Let me check what's actually in third_party/frenzymath/Poincare-Conjecture — the find at maxdepth 3 returned nothing, which is odd. Maybe it's a git repo with no checkout? Let me check.
dsh: reasoning:
The IMPORT.md isn't at third_party/frenzymath/IMPORT.md. Let me find where the supervisor put it, and find HatcherLib files.
dsh: reasoning:
The frenzymath snapshot contains HatcherLib Ch1 with CoveringSpaces.lean, VanKampen.lean, Sphere.lean. These are for leanprover/lean4:v4.32.1 + mathlib 520045ab, incompatible with our pinned v4.34.0-rc2.

The task says: "Reuse compatible open-source libraries where useful." and the supervisor says: "Reuse MorganTian, Topping, DoCarmo, Shared and other upstream packages instead of recreating geometry/Ricci-flow foundations. Do not copy admitted proofs."

So the covering lemma (coveringOfSimplyConnectedIsHomeo) could be informed by HatcherLib's covering theory. But mathlib already has a substantial IsCoveringMap API (Topology/Covering/Basic.lean, 612 lines). Let me check what theorems mathlib has for lifting paths. Let me look at the rest of Basic.lean and Deck.lean.

Plan for this invocation (4 hours):
1. Read current state of my authored files (SphereGluing.lean, Downstream.lean, MoiseBranch.lean, AxiomAudit.lean).
2. Prove C1: IsCoveringMap.surjective_of_pathConnected (range clopen).
3. Prove C2: path lifting via compactness subdivision.
4. Then C4: coveringOfSimplyConnectedIsHomeo.
5. Update checkpoint.json, results, audit.

Actually wait — the checkpoint says "next_invocation_plan: Prove C1 then C2 then C4". The goal is a "concrete geometric-realization/homeomorphism statement" plus "a gluing/covering lemma needed by sphere recognition". The main theorems are already done. Now the covering lemma.

Let me first check the mathlib covering API in more detail to see what exists already (lifting lemmas etc.), then read my existing files.
dsh: reasoning:
Interesting! Mathlib has `Mathlib/Topology/Homotopy/Lifting.lean` — this may contain path lifting and homotopy lifting for local homeomorphisms. And `eqOn_of_comp_eqOn` for gluing lifts. Let me inspect Homotopy/Lifting.lean and Deck.lean.
dsh: reasoning:
Excellent! Mathlib's pinned version ALREADY has the full covering/lifting machinery:
- `IsCoveringMap.exists_path_lifts` — path lifting exists (C2 done upstream!)
- `liftPath`, `liftPath_lifts`, `liftPath_zero`, `liftPath_trans`
- `liftHomotopy`, `liftHomotopy_lifts`, `eq_liftHomotopy_iff`
- `monodromy`, `monodromyPerm`, `monodromy_bijective`, `injective_path_homotopic_map`
- `liftPath_apply_one_eq_of_homotopicRel`
- `existsUnique_continuousMap_lifts` for SimplyConnectedSpace A

So C2 and C3 are already in mathlib. The remaining work is:
- C1: surjectivity of a covering map over a connected base.
- C4: coveringOfSimplyConnectedIsHomeo.

Also `liftPath_apply_one_eq_of_homotopicRel` — if two loops are homotopic rel endpoints and they lift to the same starting point, their endpoints agree. With SimplyConnectedSpace X, any loop is nullhomotopic; combined with `liftPath_const` and `homotopicRel_liftPath` we get fiber injectivity.

Let me look at these key theorems in detail. Also check `SimplyConnectedSpace` definition, `PathConnectedSpace`, and see whether there is `isOpen_range` or surjectivity for covering maps. Let me read the relevant parts of Lifting.lean.
dsh: reasoning:
mathlib has the full machinery. `comp_subtypeVal_pathComponent_surjective` is very useful — a covering map onto a path-connected space hits each path component of the base... actually it says: for e₀ : E, the composite pathComponent e₀ → E → X is surjective when X is path-connected. That's basically surjectivity on each path-component of X.

Let me read the rest, especially `liftPath_apply_one_eq_of_homotopicRel` (line 380) and `existsUnique_continuousMap_lifts` (480), and the SimplyConnectedSpace API. Let me also check `SimplyConnectedSpace` definition and what's available — `simplyConnectedSpace_iff_pathsHomotopic`, etc.
dsh: reasoning:
mathlib's pinned version has everything needed for C2, C3, and much of C4:

- `exists_path_lifts` (path lifting, C2) — done upstream.
- `liftHomotopy` / `liftHomotopyRel` / homotopy lifting (C3) — done upstream.
- `existsUnique_continuousMap_lifts` for simply connected, locally path connected A (general lifting criterion).
- `injective_path_homotopic_map`.
- `comp_subtypeVal_pathComponent_surjective` for path-connected X.

For C4 (coveringOfSimplyConnectedIsHomeo): If E compact, X path-connected and simply connected (and T2 for the homeo conclusion), p : E → X covering and surjective, then p is a homeomorphism.

Approach:
1. Surjectivity: from `comp_subtypeVal_pathComponent_surjective` + PathConnectedSpace X → connected → pathComponent is all of X. Actually simpler: mathlib probably has `surjective` via pathConnected... Let me think. Actually with X path-connected: for each x : X, take path γ from p e₀ to x, lift it, get Γ with Γ 1 ∈ E and p (Γ 1) = x. But `comp_subtypeVal_pathComponent_surjective` requires Nonempty E — we have E nonempty if... hmm. Actually wait, for C4 we can just use the bijective hypothesis OR prove surjectivity. The checkpoint says: "fiber injectivity from C2/C3 + nullhomotopic loops, then p injective; homeo via p bijective continuous + compact→T2 closed map".

2. Injectivity: given e₀, e₁ : E with p e₀ = p e₁ = x. Since X is simply connected and path-connected, take a path γ : x → x (loop). Its lift from e₀ ends at e₀ (lift of nullhomotopic loop returns to start — from `liftPath_apply_one_eq_of_homotopicRel` with γ₁ = const path and `liftPath_const`). But we need a path from e₀ to e₁. Hmm — need E path-connected too? The hypothesis in the checkpoint: CompactSpace E, PathConnectedSpace E, SimplyConnectedSpace X, T2Space X, IsCoveringMap p, Bijective p. With bijective p, injectivity of p comes free from bijectivity... wait no: if p is bijective then p is already injective; the homeo is `Continuous.homeoOfEquivCompactToT2`. So with bijectivity assumed, the theorem is trivial-ish: `IsCoveringMap.continuous` + `Continuous.homeoOfEquivCompactToT2` (needs compact E and T2 X).

But the point of the lemma in the spherical-space-form application: to *show* bijectivity from simply-connected + path-connected + compact. The checkpoint's C4 statement has "Surjective p" as hypothesis but bijectivity is the *conclusion*: "coveringOfSimplyConnectedIsHomeo: [CompactSpace E] [PathConnectedSpace E] [SimplyConnectedSpace X] [T2Space X], IsCoveringMap p, Surjective p → Nonempty (E ≃ₜ X); fiber injectivity from C2/C3 + nullhomotopic loops, then p injective; homeo via p bijective continuous + compact→T2 closed map."

So: p surjective + simply connected X + path connected E... hmm wait, why would p be injective? Let's think: p : E → X covering map, X simply connected, E path-connected, p surjective. Then p is injective: given e₀, e₁ with p e₀ = p e₁ = x. Since E path connected, there's a path in E from e₀ to e₁; it maps to a loop γ in X based at x. Since X is simply connected, γ is nullhomotopic (homotopic rel {0,1} to the constant loop at x). Lift the homotopy: the lift of the constant loop at e₀ is the constant path at e₀, and by `liftPath_apply_one_eq_of_homotopicRel`, lift of γ from e₀ ends at e₀. The lift of γ from e₀ is the path in E from e₀ to e₁ (uniqueness of path lifting: the path we started with is a lift of γ starting at e₀, so by `eq_liftPath_iff'` it equals liftPath γ e₀). Hence e₁ = e₀. So p injective. 

Actually we don't even need E path-connected for that argument if p surjective + X simply connected: surjectivity lets us... hmm, to get a path in E from e₀ to e₁ we need E path-connected. Alternatively: p surjective and X simply connected + X path connected is the standard statement that a simply-connected space has no nontrivial covers... The standard theorem: a connected covering of a simply connected space is a homeomorphism (and in particular trivial). For injectivity we need the total space path-connected, OR use the classification.

Simplest correct statement with hypotheses we can justify in the application: 

**coveringOfSimplyConnectedIsHomeo** : [CompactSpace E] [PathConnectedSpace E] [SimplyConnectedSpace X] [T2Space X] (p : E → X), IsCoveringMap p, Surjective p → Nonempty (E ≃ₜ X).

Steps:
1. `p_injective`: e₀ e₁, h : p e₀ = p e₁. Path connected E gives path Γ : e₀ → e₁ (a Path e₀ e₁ in E). γ := Γ.map p : Path x x where x = p e₀. SimplyConnectedSpace X: γ is homotopic rel {0,1} to constant path at x: `SimplyConnectedSpace.paths_homotopic` gives Path.Homotopic.Quotient equality, need HomotopicRel version. Check the API: `SimplyConnectedSpace` in mathlib — let me check its definition and what lemmas exist: `simplyConnected_iff_pathsHomotopic` etc.

Let me check what's available:
- `SimplyConnectedSpace X` := `∀ x : X, Subsingleton (FundamentalGroup X x)` or via `Path.Homotopic.Quotient`... In recent mathlib: `SimplyConnectedSpace X` is a class with `paths_homotopic : ∀ {x y} (p q : Path x y), p.Homotopic q`. Then `simplyConnected_iff_pathsHomotopic : SimplyConnectedSpace X ↔ ∀ {x y}, ∀ p q : Path x y, p.Homotopic q`? Need to check. There's also the HomotopicRel version: paths with same endpoints are homotopic rel endpoints iff simply connected + ... Let me grep mathlib Topology/Homotopy/SimplyConnected.

Then from γ.HomotopicRel (const) and cov.liftPath_apply_one_eq_of_homotopicRel: cov.liftPath γ e₀ h₀ 1 = cov.liftPath (const) e₀ h 1 = e₀ (by liftPath_const). And Γ = cov.liftPath γ e₀ h₀ by eq_liftPath_iff' (Γ continuous, p ∘ Γ = γ, Γ 0 = e₀). Hence Γ 1 = e₀. But Γ 1 = e₁. So e₁ = e₀. 

2. Then p bijective: surjective hypothesis + injectivity.
3. `Continuous.homeoOfEquivCompactToT2`? Let me check the exact name in this mathlib. There's `Homeomorph.homeoOfEquivCompactToT2` or `Continuous.homeoOfEquivCompactToT2`. In SphereGluing.lean I used it already (per checkpoint's key_mathlib_api: `Continuous.homeoOfEquivCompactToT2`). Good.

Then the "spherical space form" application: For a simply-connected 3-manifold M with a free action of a finite group G, quotient is a compact 3-manifold with π₁ = G... The covering lemma application: if a compact path-connected E covers a simply-connected X then E ≅ X. Hmm, in sphere recognition the actual use: S³ covers M/G where M simply connected... The precise spherical space form statement: if a finite group G acts freely on a simply connected compact 3-manifold M (homology sphere), then M ≅ S³. The covering p : M → M/G is a covering map; if M/G were simply connected... no.

Actually the real usage in Perelman's proof (finite extinction time): "if M is a homotopy sphere then its universal cover... " Hmm. The standard use: X = S³/G covering theorem. Let me not overthink: the task says "a gluing/covering lemma needed by sphere recognition". The lemma coveringOfSimplyConnectedIsHomeo is the standard "connected cover of simply connected base is a homeomorphism" which is the topological fact underlying "a simply-connected manifold has no non-trivial covers", needed for sphere recognition (e.g., to identify the universal cover of S³/G, or the uniqueness of the cover).

Actually in Perelman's finite-time extinction: M homotopy sphere → M = S³/G (elliptization). To identify M ≅ S³ when the cover is S³ and covering group trivial... The lemma "covering of simply connected space by path connected space is homeo" is exactly what shows G = 1 case. Also for "S³/G with free action is a manifold and p : S³ → S³/G is a covering map".

Fine — prove the lemma, and construct a concrete downstream use: e.g., the covering map S¹ → S¹, z ↦ z² is NOT a homeomorphism because S¹ is not simply connected (negative control), and the doubling map as covering; or a positive instance: id : S³ → S³ trivially. A nondegenerate use: given p : S³ → S³ a covering map... hmm.

Better concrete downstream use within my existing files: DoubleDiskQuot 2 ≃ₜ SphereThree — the quotient map from the sphere... Actually, here's a real geometric use: the antipodal action of ℤ/2 on S^n → ℝP^n is a covering; for n ≥ 2, S^n simply connected, so any path-connected space E covering ℝP^n... no wait, that shows ℝP^n's universal cover is S^n.

The cleanest downstream use for sphere recognition: **sphereDoubleCover**? Hmm.

Let me think about what's actually needed for sphere recognition via Perelman's finite extinction: M a homotopy 3-sphere, Ricci flow with surgery; at finite extinction time M decomposes into pieces each diffeomorphic to S³/G (G finite acting freely) or S²×S¹; homotopy sphere has no S²×S¹ pieces (π₁ trivial, S²×S¹ has π₁ = ℤ); and S³/G with π₁(M) = 1 forces G = 1 by π₁(S³/G) = G — hmm, that needs "the universal cover of S³/G is S³" and "π₁(S³) = 1". The relevant topological lemma: **if G acts freely on S³, then p : S³ → S³/G is a covering map** and **S³/G is a compact 3-manifold**. Plus **a quotient of a simply-connected space by a free action of a finite group has fundamental group G** (needs van Kampen or covering theory).

Given the scope (topology prerequisite), the covering lemma `coveringOfSimplyConnectedIsHomeo` plus a covering-map instance for a free action of a finite group `isCoveringMap_of_quotient`... The free-action → covering map instance is the geometric-realization/covering statement needed. Let me check what's in mathlib for group actions: `Topology/Covering/Quotient.lean` — `IsQuotientCoveringMap`! Let me read it.
dsh: reasoning:
This mathlib is extremely rich. The key API:

- `isQuotientCoveringMap_quotientMk_of_properlyDiscontinuousSMul [IsCancelSMul G E] : IsQuotientCoveringMap (Quotient.mk (MulAction.orbitRel G E)) G` for a free + properly discontinuous action on a locally compact T2 space.
- `IsQuotientCoveringMap.isCoveringMap : IsCoveringMap f`.

So the "geometric-realization/covering statement" for sphere recognition: **a free, properly discontinuous action of a finite group G on S³ gives a covering map S³ → S³/G**. And `coveringOfSimplyConnectedIsHomeo` shows a path-connected compact space covering a simply-connected space is homeomorphic to it.

Now the concrete downstream use for sphere recognition: 
- The antipodal action of ℤ/2 (or Multiplicative ℤ/2... in Lean, ZMod 2) on Sphere n is free and properly discontinuous → quotient map is a covering map.
- For n ≥ 2, the sphere is simply connected.
- For the n=2,3 instances relevant to recognition.

Actually, let me focus. The checkpoint plan for THIS invocation: "Prove C1 then C2 then C4". Given mathlib already has path lifting (C2), homotopy lifting (C3), the plan simplifies dramatically:

1. C1: `IsCoveringMap.surjective_of_pathConnected` — actually mathlib has `comp_subtypeVal_pathComponent_surjective` which requires `[PathConnectedSpace X]` and gives surjectivity onto X (if E nonempty). Hmm wait — does it? `comp_subtypeVal_pathComponent_surjective (e₀ : E) [PathConnectedSpace X] : Surjective (fun x : pathComponent e₀ ↦ p x)`. This shows p is surjective onto X from any path component. So surjectivity of a covering map onto a path-connected space is immediate: `fun x ↦ ⟨(this x).1, (this x).2⟩`? Let me write C1 as a thin wrapper.

2. C4: `coveringOfSimplyConnectedIsHomeo`. 

For C4's injectivity I need `SimplyConnectedSpace` API. Let me check the definition and lemmas: `SimplyConnectedSpace.paths_homotopic`? Let me grep.

Also I want to double-check what `PathConnectedSpace.somePath` looks like and whether `SimplyConnectedSpace` implies `PathConnectedSpace`? In mathlib, `SimplyConnectedSpace X` = `∀ x, Subsingleton (FundamentalGroup X x)` plus (maybe) path-connectedness? In recent mathlib: 

```lean
class SimplyConnectedSpace (X : Type*) [TopologicalSpace X] : Prop where
  paths_homotopic : ∀ {x y : X} (p q : Path x y), p.Homotopic q
  pathConnected : PathConnectedSpace X
```

Hmm, actually I recall mathlib's `SimplyConnectedSpace` has only `paths_homotopic`. Let me check.

Then to show injectivity: e₀ e₁ : E, h : p e₀ = p e₁. Since X simply connected → path connected, there's a path γ : p e₀ → p e₀? We need a path in E from e₀ to e₁ — that needs E path-connected. OK so the statement needs [PathConnectedSpace E] (as planned).

Let me write out C4:

```lean
theorem coveringOfSimplyConnectedIsHomeo [CompactSpace E] [T2Space X] [PathConnectedSpace E]
    [SimplyConnectedSpace X] {p : E → X} (hp : IsCoveringMap p) (hsurj : Surjective p) :
    Nonempty (E ≃ₜ X) := by
  have hp_inj : Injective p := by
    intro e₀ e₁ hpe
    let Γ : Path e₀ e₁ := PathConnectedSpace.somePath e₀ e₁
    let γ : Path (p e₀) (p e₁) := Γ.map ⟨p, hp.continuous⟩  -- loop based at p e₀ = p e₁? 
    -- hmm, γ is a path from p e₀ to p e₁, and hpe : p e₀ = p e₁.
```

Wait — need a *loop*. Let me be careful: γ : Path (p e₀) (p e₁). With hpe we can cast to a loop at p e₀: `γ.cast hpe rfl : Path (p e₀) (p e₀)`? Path.cast : γ.cast (h : γ.x₀ = x₀') (h' : γ.x₁ = x₁') : Path x₀' x₁'. So `γ.cast rfl hpe.symm : Path (p e₀) (p e₀)`.

Since X is simply connected: `SimplyConnectedSpace.paths_homotopic (γ.cast rfl hpe.symm) (Path.refl (p e₀))` gives Homotopic (not rel). I need HomotopicRel {0,1}. Is there a lemma `homotopicRel_of_simplyConnected`? In mathlib: `SimplyConnectedSpace.paths_homotopic` gives `p.Homotopic q` for any two paths with same endpoints. For HomotopicRel: there's `Path.Homotopic.iff_homotopicRel`? Hmm — for paths with the same endpoints, `p.Homotopic q ↔ p.HomotopicRel q {0, 1}`? That's true generally: homotopy of paths (without fixed endpoints) between paths with same endpoints... no wait, that's false in general! Two paths in S¹ from x to y can be homotopic as paths (endpoints fixed? no — Path.Homotopic fixes endpoints at 0? Let me recall mathlib: `Path.Homotopic` — homotopy of paths, keeps endpoints fixed? In mathlib, `Path.Homotopy` is a homotopy F : I × I → X with F(0,t) = γ₀(t) hmm. Let me check: mathlib `Path.Homotopy.zero : F 0 = γ₀` (at time 0 equals γ₀) and `one : F 1 = γ₁`, `prop' : ∀ t, F (t, 0) = x₀ ∧ F (t, 1) = x₁`. So Path.Homotopy fixes BOTH endpoints for all times: it's a homotopy rel {0,1} of the domain I. So `Path.Homotopic` is exactly homotopy rel endpoints. Good — so `SimplyConnectedSpace.paths_homotopic` gives directly the HomotopicRel version? Let me check the definition of SimplyConnectedSpace and its lemmas in this mathlib.

Then:
```
have hhom : (γ.cast rfl hpe.symm).Homotopic (Path.refl (p e₀)) := SimplyConnectedSpace.paths_homotopic ..
```
Wait — `SimplyConnectedSpace.paths_homotopic : ∀ {x y : X} (p q : Path x y), p.Homotopic q`. 

Then lift: `cov.liftPath_apply_one_eq_of_homotopicRel` needs `HomotopicRel`:
```
have hrel : (γ.cast rfl hpe.symm).HomotopicRel (Path.refl (p e₀)) {0, 1} := ⟨hhom.some⟩? 
```
Hmm — Path.Homotopic is defined as `∃ F : Path.Homotopy p q`? or `Nonempty (Path.Homotopy p q)`. And HomotopicRel as `∃ F : HomotopyRel p q S`. Since Path.Homotopy is exactly homotopy rel {0,1}... is there a conversion lemma? Let me check `Topology/Homotopy/Path.lean` for `homotopicRel` / `Homotopic` relation. There may be a lemma `Path.Homotopic.homotopicRel` or the definitions may be such that conversion is definitional-ish. I'll check.

Then:
```
have e₀_lift : cov.liftPath (γ.cast rfl hpe.symm) e₀ (γ.cast...source...) 1 = e₀ := by
  -- liftPath of constant loop at e₀: cov.liftPath (Path.refl (p e₀)) e₀ ... = const
  rw [cov.liftPath_apply_one_eq_of_homotopicRel hrel]
  simp [cov.liftPath_const]
```

Wait, `liftPath_apply_one_eq_of_homotopicRel` says liftPath γ₀ e₀ h₀ 1 = liftPath γ₁ e₀ h₁ 1. With γ₁ = Path.refl (p e₀), liftPath (refl) e₀ h₁ = const path at e₀, so at 1 it's e₀.

And Γ itself is a lift of γ starting at e₀: by uniqueness `eq_liftPath_iff'`: Γ = cov.liftPath γ e₀ γ.source... then Γ 1 = cov.liftPath γ e₀ .. 1. But I computed for the casted loop; the cast doesn't change the function. Let me handle it via: lift of γ and lift of γ.cast rfl hpe.symm differ by cast on the endpoint. Hmm, actually there's a subtlety: liftPath γ.cast... at 1 equals e₀, and liftPath γ at 1 — the underlying function of γ.cast rfl hpe.symm equals γ as functions. So by eq_liftPath_iff', liftPath γ e₀ h₀ = liftPath (γ.cast rfl hpe.symm) e₀ h₀' (as functions; both continuous, both lift, both start at e₀). So liftPath γ e₀ h₀ 1 = e₀. And Γ = liftPath γ e₀ h₀ by eq_liftPath_iff' with Γ's data: continuous (path), p ∘ Γ = γ — wait, Γ.map ⟨p, _⟩ = γ means p ∘ Γ = γ as functions. And Γ 0 = e₀. So Γ = cov.liftPath γ e₀ (Γ.source...) and hence Γ 1 = e₀. But Γ 1 = e₁ by Path.target. So e₁ = e₀. 

Alternatively simpler: use `cov.injective_path_homotopic_map`? That needs path homotopy classes in E and maps them injectively into X's. Not directly endpoints.

OK. Then:
```
  exact ⟨Homeomorph... continuous.homeoOfEquivCompactToT2 ...⟩
```
`Continuous.homeoOfEquivCompactToT2 hp.continuous ⟨hp_inj, hsurj⟩` — check exact name: In this mathlib there's `Homeomorph.homeoOfEquivCompactToT2`? Let me grep.

Then the "spherical space form" downstream: 

**Theorem (spherical space form covering)**: for a free properly discontinuous action of a finite group G on Sphere 3 (= S³), the quotient map is a covering map and S³/G ≃ ... hmm, downstream use of coveringOfSimplyConnectedIsHomeo for sphere recognition: 

`sphereQuotientCovering`: p : S³ → S³/G is IsCoveringMap, and if π₁... no. The classic recognition use: **if M is a simply-connected compact 3-manifold and S³ → M is a covering, then M ≅ S³**. That's literally `coveringOfSimplyConnectedIsHomeo` with E = S³... E must be path-connected compact, X = M simply connected. But X = M simply connected would mean M ≅ S³ via this map — that's just "any covering of a simply-connected space is a homeomorphism", used when M is the quotient S³/G with π₁(M) = G... no, π₁(M) = G ≠ 1 generally.

Hmm, actually in Perelman's finite extinction: M homotopy 3-sphere; at extinction, M = M₁ # ... # Mₖ where each Mᵢ ≅ S³/Gᵢ or S²×S¹. π₁(M) = 1 ⟹ each π₁(Mᵢ) = 1 (Grushko) ⟹ Gᵢ = 1 for the spherical ones (π₁(S³/G) = G) and no S²×S¹ factors (π₁ = ℤ). So M ≅ S³. The key topological input: **π₁(S³/G) ≅ G** and **S³/G is covered by S³**. 

For this D12 task ("a gluing/covering lemma needed by sphere recognition"), the covering lemma `coveringOfSimplyConnectedIsHomeo` plus the quotient-covering instance for free properly discontinuous actions is the right deliverable. And a concrete downstream use: **for the antipodal ZMod 2 action on Sphere n (n ≥ 1), the quotient map is a covering map** (needs free + properly discontinuous + locally compact T2 — all available), and as a concrete nondegenerate test, for n = 1: S¹ → RP¹ is a 2-fold covering, NOT a homeomorphism (negative control), while the lemma would apply for... hmm for the homeo we'd need simply connected base.

Actually here's a clean, concrete, nondegenerate downstream application of `coveringOfSimplyConnectedIsHomeo`:

**Theorem**: the quotient map q : 𝕊 n → 𝕊 n / {±1} (antipodal) is a covering map (n ≥ 1?). And **no proper connected cover of S³**: any path-connected compact E covering S³ is homeomorphic to S³. 

For an even more "sphere recognition" flavored statement: **sphereQuotientCover**: If G acts freely and properly discontinuously on S³, then S³ → S³/G is a covering map, S³/G is compact (quotient of compact), and S³/G is a manifold... "manifold" is out of scope (needs charts — that's D8/D10's job maybe). Keep to: covering map + compactness + nonempty.

Then the *application*: `s3NoNontrivialCover`: [CompactSpace E] [PathConnectedSpace E] (p : E → Sphere 3) IsCoveringMap + Surjective → Nonempty (E ≃ₜ Sphere 3). This needs `SimplyConnectedSpace (Sphere 3)` — is sphere simply connected in mathlib? `sphereSimplyConnected` — mathlib has `simplyConnectedSphere`? For n ≥ 2, mathlib has `pi_1_sphere`? Hmm. mathlib has `FundamentalGroup.sphere`? Let me check. mathlib does have sphere simply-connected results? There's `Mathlib/Topology/Homotopy/Spheres.lean`. Let me check.

Actually let me now check what exists: `simplyConnectedSphere`, `pi1_sphere`, `Sphere` API. If mathlib has `SimplyConnectedSpace (Sphere n)` for 2 ≤ n, we can instantiate the application concretely: **any compact path-connected space covering S³ is homeomorphic to S³**, and **RP³ = S³/(ℤ/2) is compact and covered by S³** — but RP³ is NOT simply connected (negative control). Great for non-vacuity testing: the theorem's hypotheses genuinely exclude the antipodal quotient (which is path-connected and compact, covered by S³, but the BASE is not simply connected, so no contradiction).

Let me now also handle C1 (surjectivity) — from `comp_subtypeVal_pathComponent_surjective`:

```lean
theorem IsCoveringMap.surjective_of_pathConnected [PathConnectedSpace X] [Nonempty E] (hp : IsCoveringMap p) : Surjective p := by
  intro x
  obtain ⟨⟨e, _⟩, he⟩ := hp.comp_subtypeVal_pathComponent_surjective (Classical.choice ‹Nonempty E›) x
  exact ⟨e, he⟩
```

Wait — `comp_subtypeVal_pathComponent_surjective (e₀ : E) [PathConnectedSpace X] : Surjective (fun x : pathComponent e₀ ↦ p x)` — needs the instance PathConnectedSpace X. Fine. But do we even need `[PathConnectedSpace X]`? Also there might be a lemma in mathlib `IsCoveringMap.surjective_of_pathConnectedSpace`? Let me grep for "surjective" in Lifting.lean — didn't see one besides comp_subtypeVal_pathComponent_surjective. OK.

Hmm wait, there's also the connected case: if X merely connected (not path connected), a covering map with nonempty domain... over a connected base, surjectivity holds iff some fiber nonempty... but with ConnectedSpace X and Nonempty X and E nonempty: image is clopen (p is an open map, locally injective...). mathlib might have it. `IsOpenMap` + clopen image argument. For the target use, path connected is fine and available for spheres.

Now, key questions to resolve by reading mathlib source:
1. `SimplyConnectedSpace` definition & `paths_homotopic` lemma name.
2. Conversion between `Path.Homotopic` and `Path.HomotopicRel {0,1}`.
3. `PathConnectedSpace.somePath` name/API.
4. `Continuous.homeoOfEquivCompactToT2` (name check).
5. Sphere simply-connected in mathlib? (for the application instance).
6. ProperlyDiscontinuousSMul for the antipodal action — is there an existing instance for e.g. `MulAction` of ZMod 2 on Sphere? There's `Mathlib/Topology/... /ProjectiveSpace`? mathlib has real projective spaces? Not sure. Simplest: construct ZMod 2 action on Sphere n via negation... The antipodal map: g • x = -x for g = 1, x for g = 0. Prove ProperlyDiscontinuousSMul via metric separation: for each e, take ball of radius 1/2 (norm 1 sphere: if ‖x‖ = ‖y‖ = 1 and ‖x - y‖ < ... ) — antipodal points are at distance 2. So the ball B(e, 1/2) has no nontrivial translate intersecting: if g•U ∩ U ≠ ∅ then ‖g•x - y‖ < 1 for x,y ∈ U, so ‖x ± y‖ < 1... If g = 1, x = -y forces ‖x - y‖ = ‖x + x‖ = 2 ≥ 1 contradiction unless g = 0. Fine — that's `ProperlyDiscontinuousSMul` — needs checking the exact mathlib definition: `ProperlyDiscontinuousSMul G X`: `∀ e, ∃ U ∈ 𝓝 e, ∀ g, (g • ·) '' U ∩ U = ∅ ∨ g • e = e`. Hmm, and freeness `IsCancelSMul G E`.

Also need: does mathlib already have `ProperlyDiscontinuousSMul (Multiplicative (ZMod 2)) (Sphere n)`? There might be a file on lens spaces or projective spaces. Let me grep. Actually, I recall mathlib added `Mathlib/Topology/ProjectiveSpace`? Not sure it exists in this revision. Let me check.

Let me do a batch of greps now. Also check the existing SphereGluing.lean to see namespace conventions and what's already there, so new files fit in.
dsh: reasoning:
No Spheres.lean in Topology/Homotopy. Sphere simply-connectedness: check `AlgebraicTopology/Spheres.lean` or similar.
dsh: reasoning:
Key facts confirmed:
- `SimplyConnectedSpace` implies `PathConnectedSpace` (instance).
- `SimplyConnectedSpace.paths_homotopic {x y} (p₁ p₂ : Path x y) : Path.Homotopic p₁ p₂`.

Now I need: `Path.Homotopic` → `Path.HomotopicRel {0,1}` conversion. Let me check mathlib's Path.Homotopy definition and whether there's `Homotopic.homotopicRel` or similar. `liftPath_apply_one_eq_of_homotopicRel` takes `γ₀.HomotopicRel γ₁ {0,1}` where `HomotopicRel` = `Nonempty (HomotopyRel γ₀ γ₁ {0,1})`.

Is there a lemma `Path.Homotopic.homotopicRel`? Path.Homotopy F : I × I → X with F(·,0)=γ₀, F(·,1)=γ₁, F(t,0)=x₀, F(t,1)=x₁ for all t — that IS a HomotopyRel rel {0,1} of the interval. Let me check the exact definitions to see if conversion is trivial.
dsh: reasoning:
`Path.Homotopic p₀ p₁` is defined in Path.lean line 241. Let me see how it's defined and whether there's a lemma giving `HomotopicRel p₀ p₁ {0,1}` from it. Since a Path.Homotopy fixes endpoints for all times, it is exactly a HomotopyRel with S = {0,1}... let me look.
dsh: reasoning:
`Path.Homotopy F` extends `ContinuousMap.Homotopy` with `prop' t x hx` — i.e., F(t,0) = x₀, F(t,1) = x₁ for all t. So a `Path.Homotopy` is precisely a `ContinuousMap.HomotopyRel` with S = {0,1}? Let me check `ContinuousMap.HomotopyRel` structure: `structure HomotopyRel (f₀ f₁ : C(X,Y)) (S : Set X) extends Homotopy f₀ f₁ where prop' : ∀ t : I, ∀ x ∈ S, toFun (t, x) = f₀ x = f₁ x`. Yes: HomotopyRel has `prop' : ∀ t, ∀ x ∈ S, F(t,x) = f₀ x`. And Path.Homotopy has `prop' : ∀ t x hx, F(t,x) = x₀ ∨ x₁`? Let me check the exact Path.Homotopy definition. From the code: `prop' t x hx := by rcases hx with hx | hx; rw [hx]; simp` — so hx : x ∈ {0,1}? And the conclusion is F(t,0) = x₀ for x=0 and F(t,1) = x₁. So it's HomotopyRel with S = ({0,1} : Set I) where f₀ 0 = x₀, f₀ 1 = x₁ — the same. So conversion `Path.Homotopy → ContinuousMap.HomotopyRel p q {0,1}` should be mostly definitional... The structures have different field names though: `map_zero_left` vs `Homotopy.map_zero_left`... Let me look at the Path.Homotopy structure definition.
dsh: reasoning:
`Path.Homotopy p₀ p₁` is *literally* `ContinuousMap.HomotopyRel p₀.toContinuousMap p₁.toContinuousMap {0,1}` (an abbrev). So `Path.Homotopic p₀ p₁ = Nonempty (p₀.Homotopy p₁)` and `HomotopicRel p₀ p₁ {0,1} = Nonempty (ContinuousMap.HomotopyRel p₀ p₁ {0,1})` — since `Path` coerces to C(I,X) and `toContinuousMap`, these are definitionally the same modulo the coercion. `liftPath_apply_one_eq_of_homotopicRel` takes `γ₀.HomotopicRel γ₁ {0,1}` with γ : C(I, X). `Path.Homotopic` is about `Path`s. Conversion should be easy via `h.map (fun F => ...)`? Actually since HomotopyRel is between `ContinuousMap`s and Path.Homotopy is an abbrev to exactly that, `(h : Path.Homotopic p₀ p₁)` gives `F : ContinuousMap.HomotopyRel p₀.toContinuousMap p₁.toContinuousMap {0,1}`, and I need `ContinuousMap.HomotopyRel (p₀ : C(I,X)) (p₁ : C(I,X)) {0,1}` — the coercion `Path → C(I, X)`... `Path.toContinuousMap` is the coercion. `simpa using F` should convert since (p₀ : C(I,X)) = p₀.toContinuousMap definitionally. Good.

So the C4 proof:

```lean
import Mathlib.Topology.Homotopy.Lifting  -- brings IsCoveringMap machinery
import Mathlib.AlgebraicTopology.FundamentalGroupoid.SimplyConnected

open unitInterval
open scoped unitInterval

namespace Poincare.D12.TriangulationTopology

/-- A covering map over a path-connected base with nonempty domain is surjective. (C1) -/
theorem IsCoveringMap.surjective_of_pathConnected [PathConnectedSpace X] [Nonempty E]
    (hp : IsCoveringMap p) : Function.Surjective p := by
  intro x
  let e₀ : E := Classical.choice ‹Nonempty E›
  have hx := hp.comp_subtypeVal_pathComponent_surjective e₀ x
  exact ⟨hx.choose, hx.choose_spec⟩
```

Hmm wait, `comp_subtypeVal_pathComponent_surjective (e₀ : E) [PathConnectedSpace X] : Surjective (fun x : pathComponent e₀ ↦ p x)`. `hx.choose : pathComponent e₀`, so `(hx.choose : E)`? pathComponent is a subtype (a Set of E, i.e., {e | Joined e₀ e}). So `hx.choose` has type pathComponent e₀ and coerces to E. `⟨hx.choose, hx.choose_spec⟩` — the E part coerces automatically. Good.

Now C4:

```lean
/-- Fiber injectivity: if the base is simply connected, a covering map is injective on
  each path-component of the total space. -/
theorem IsCoveringMap.injective_of_simplyConnected [SimplyConnectedSpace X]
    {e₀ e₁ : E} (he₀e₁ : Joined e₀ e₁) (hp : IsCoveringMap p) (hpe : p e₀ = p e₁) :
    e₀ = e₁ := by
  rcases he₀e₁ with ⟨Γ⟩
  let γ : Path (p e₀) (p e₁) := Γ.map ⟨p, hp.continuous⟩
  let γ₀ : Path (p e₀) (p e₀) := γ.cast rfl hpe.symm
  have hrel : γ₀.HomotopicRel (Path.refl (p e₀)).toContinuousMap ... hmm
```

Wait, `liftPath_apply_one_eq_of_homotopicRel` is stated for `γ₀ γ₁ : C(I, X)`. But `cov.liftPath` takes `γ : C(I, X)`. Paths coerce to ContinuousMap. To avoid coercion pain, maybe work with paths as continuous maps: `(Γ : C(I, E))`... Γ : Path e₀ e₁, use `Γ.toContinuousMap` or rely on coercion.

Let me plan:

```
  have hrel : γ₀.HomotopicRel (Path.refl (p e₀)) {0, 1} := by
    have hh : γ₀.Homotopic (Path.refl (p e₀)) := SimplyConnectedSpace.paths_homotopic γ₀ (Path.refl (p e₀))
    -- Path.Homotopic = Nonempty (Path.Homotopy _ _) = Nonempty (ContinuousMap.HomotopyRel .. .. {0,1})
    exact hh
```
Since `Path.Homotopic γ₀ (Path.refl (p e₀))` unfolds to `Nonempty (γ₀.Homotopy (Path.refl (p e₀)))` = `Nonempty (ContinuousMap.HomotopyRel γ₀.toContinuousMap (Path.refl (p e₀)).toContinuousMap {0,1})`. And `HomotopicRel γ₀ (Path.refl (p e₀)) {0,1}` (with paths coerced to C(I,X)) is `Nonempty (ContinuousMap.HomotopyRel (γ₀ : C(I,X)) (Path.refl .. : C(I,X)) {0,1})`. If the coercion `Path → C(I,X)` is definitionally `toContinuousMap`, then `exact hh` works directly. `ContinuousMap` coe from Path: `instance : Coe (Path x₀ x₁) C(I, X) := ⟨toContinuousMap⟩`? something like that. Likely fine — `exact hh` or `simpa [Path.Homotopic] using hh`... I'll handle it in Lean.

```
  -- lift the loop γ₀ from e₀ and compare with Γ (lift of γ from e₀)
  have hΓlift : Γ = hp.liftPath γ e₀ (by simp [Γ, γ]) := by
    -- via eq_liftPath_iff' : Γ = liftPath γ e₀ γ_0 ↔ p ∘ Γ = γ ∧ Γ 0 = e₀
    rw [hp.eq_liftPath_iff']
    exact ⟨by ext t; rfl, rfl⟩
  -- Γ as continuous map equals liftPath γ; compare endpoints
```

Hmm — `eq_liftPath_iff' {Γ : C(I, E)} : Γ = cov.liftPath γ e γ_0 ↔ p ∘ Γ = γ ∧ Γ 0 = e`. Here Γ : Path e₀ e₁ coerces to C(I,E). `p ∘ Γ = γ` — γ := Γ.map ⟨p, hp.continuous⟩ — `Path.map` is `fun t ↦ f (Γ t)`. So `p ∘ (Γ : C(I,E)) = (γ : C(I,X))` holds by rfl? γ = Γ.map ⟨p, hp.continuous⟩, and (Γ.map f : Path ..) coerced to C(I,X) — its function is f ∘ Γ. So `p ∘ Γ = γ` should be `rfl` after unfolding γ. Let me make γ a let: `let γ : Path (p e₀) (p e₁) := Γ.map ⟨p, hp.continuous⟩`. Then `show p ∘ (Γ : C(I,E)) = (γ : C(I,X))` — `Path.map` uses `ContinuousMap.comp`? Path.map f : Path (f x₀) (f x₁) where f : C(X,Y). Its `toFun := f ∘ ...`? It's `def map (γ : Path x₀ x₁) {Y} (f : C(X, Y)) : Path (f x₀) (f x₁) := ⟨f ∘ γ, by fun_prop⟩` or similar. `f ∘ γ` where γ coerces to function. So `(γ : C(I,X))` as function = p ∘ Γ. As ContinuousMap equality, `ext` should close it.

Then:
```
  have hEnd : hp.liftPath γ e₀ (γ.source...) 1 = e₀ := by
    -- liftPath γ₀-based: the lift of the nullhomotopic loop ends where it started
    have := hp.liftPath_apply_one_eq_of_homotopicRel hrel e₀ (γ₀.source) (Path.refl (p e₀)).source
    -- this: hp.liftPath γ₀ e₀ h₀ 1 = hp.liftPath (Path.refl (p e₀)) e₀ h₁ 1
    rw [this]
    -- liftPath of constant loop = const path at e₀
    simpa using congr_fun (hp.liftPath_const ...) -- liftPath_const {x : X} (hpe : x = p e) : liftPath (.const I x) e hpe = .const I e
```

Hmm wait — `liftPath_const` takes `.const I x` — the *constant continuous map*, not `Path.refl`. `Path.refl x : Path x x` is `⟨fun _ => x, ...⟩` which as C(I,X) equals `.const I x` definitionally? `.const I x` : C(I, X) is the constant map. Path.refl coerces to C(I,X) with function `fun _ ↦ x` — should be defeq to `.const I x`. And `cov.liftPath γ₁ e₀ h₁ 1 = e₀` via `liftPath_const` gives `cov.liftPath (.const I x) e hpe = .const I e`, then evaluate at 1: `congr_fun .. 1`.

But wait, there's a subtlety: `liftPath_apply_one_eq_of_homotopicRel hrel e₀ h₀ h₁` compares `cov.liftPath γ₀ e₀ h₀ 1` with `cov.liftPath (Path.refl (p e₀)) e₀ h₁ 1`. I want the former to equal e₀. So:
```
    have := hp.liftPath_apply_one_eq_of_homotopicRel hrel e₀ γ₀.source (Path.refl (p e₀)).source
```
γ₀.source : γ₀ 0 = p e₀ ✓. (Path.refl (p e₀)).source : (Path.refl (p e₀)) 0 = p e₀ ✓. But careful with types: `liftPath_apply_one_eq_of_homotopicRel {γ₀ γ₁ : C(I, X)} (h : γ₀.HomotopicRel γ₁ {0,1}) (e : E) (h₀ : γ₀ 0 = p e) (h₁ : γ₁ 0 = p e)`. So pass `γ₀ : C(I,X)` — the Path coerces; `(Path.refl (p e₀) : C(I,X))`. h₀ : γ₀ 0 = p e₀ where γ₀ as C(I,X) applied to 0 = p e₀ — Path.source gives exactly `γ₀ 0 = p e₀`? Path.source : γ₀ 0 = x₀ where x₀ = p e₀. Hmm, `γ₀.source : γ₀ 0 = p e₀` ✓.

Then `hp.liftPath_const` needs `hpe : x = p e` — here x = p e₀ and e = e₀: `(rfl : p e₀ = p e₀)`. Then `liftPath (.const I (p e₀)) e₀ rfl = .const I e₀`. But I need `liftPath (Path.refl (p e₀)) e₀ ..`. Need: `(Path.refl (p e₀) : C(I,X)) = .const I (p e₀)` — probably rfl via ext. Actually `Path.refl` def: `⟨fun _ => x, continuous_const⟩`? So defeq should hold, and `liftPath_const` has `cov.liftPath (.const I x) e hpe = .const I e`. Then `congr_fun .. 1 : (cov.liftPath (.const I (p e₀)) e₀ ..) 1 = e₀` since `.const I e₀ 1 = e₀` by rfl.

There might be defeq trouble between `(Path.refl (p e₀) : C(I,X))` and `.const I (p e₀)` in the liftPath argument — if so, insert `change hp.liftPath (.const I (p e₀)) e₀ ?h₁ 1 = e₀`.

Then:
```
  have hEnd' : hp.liftPath γ e₀ γ.source 1 = e₁ := by
    -- via hΓlift: Γ = liftPath γ e₀ γ.source, so liftPath γ e₀ .. 1 = Γ 1 = e₁
    rw [← hΓlift]; exact Γ.target  -- wait hΓlift : Γ = liftPath..., so Γ 1 = liftPath 1; Γ.target : Γ 1 = e₁
```
Hmm Γ.target : Γ 1 = e₁ (Path.target). `hΓlift : Γ = hp.liftPath γ e₀ ..` — but types: Γ : Path e₀ e₁ vs hp.liftPath γ e₀ .. : C(I,E). So hΓlift must be stated with coercions: `(Γ : C(I,E)) = hp.liftPath γ e₀ ..`. Then `congr_fun hΓlift 1 : Γ 1 = hp.liftPath .. 1`. And Γ.target : Γ 1 = e₁. So `hp.liftPath .. 1 = e₁` via `(congr_fun hΓlift 1).trans Γ.target`.

Now relate hEnd (for γ₀) to hEnd' (for γ): γ₀ = γ.cast rfl hpe.symm — the cast changes the type of the path but the underlying function is the same. I need `hp.liftPath γ e₀ γ.source 1 = hp.liftPath γ₀ e₀ γ₀.source 1`? Hmm — that's the annoying part. Alternative cleaner approach: avoid γ₀ entirely by making the "refl" path at the *right* endpoint type via casting the refl path:

`Path.refl (p e₀)` : Path (p e₀) (p e₀). To apply liftPath_apply_one_eq_of_homotopicRel I need two paths with the same endpoints both as C(I,X) — actually NO: the two γ₀ γ₁ in that lemma are just `C(I, X)` — arbitrary continuous maps I → X, not paths! The lemma is stated for continuous maps. So I can pass γ (as C(I,X)) — a path from p e₀ to p e₁ — and... γ₁ needs to be homotopicRel to γ. If γ₁ = the constant map at p e₀: is γ homotopicRel (const (p e₀)) {0,1}? γ is a loop only after cast; as a continuous map I → X, γ sends 0 ↦ p e₀, 1 ↦ p e₁. A HomotopyRel γ (const p e₀) {0,1} would force F(t,0) = γ 0 = p e₀ ✓ and F(t,1) = γ 1 = p e₁, but the RHS const map at p e₀ has value p e₀ at 1 — the HomotopyRel definition requires F(t,x) = f₀ x and also f₀ x = f₁ x on S. So it would force p e₁ = p e₀. But we have hpe : p e₀ = p e₁! So actually const p e₀ works if we rewrite. Hmm, still awkward.

Cleanest: use γ₀ := γ.cast rfl hpe.symm : Path (p e₀) (p e₀) — and γ₁ := Path.refl (p e₀). Both in Path (p e₀) (p e₀). `SimplyConnectedSpace.paths_homotopic γ₀ (Path.refl (p e₀)) : γ₀.Homotopic (Path.refl (p e₀))` = HomotopicRel (γ₀ : C(I,X)) ((Path.refl (p e₀)) : C(I,X)) {0,1} (definitional since Path.Homotopy is an abbrev). 

Then liftPath_apply_one_eq_of_homotopicRel gives: liftPath γ₀ e₀ γ₀.source 1 = liftPath (Path.refl (p e₀)) e₀ .. 1 = e₀.

Now connect liftPath γ₀ to liftPath γ: both γ and γ₀ have the same underlying function (Path.cast only changes endpoints data). By uniqueness eq_liftPath_iff' with Γ := cov.liftPath γ e₀ γ.source: Γ continuous, p ∘ Γ = γ (as C(I,X)), Γ 0 = e₀. To show Γ = cov.liftPath γ₀ e₀ γ₀.source: need p ∘ Γ = γ₀ as C(I,X) and Γ 0 = e₀. p ∘ Γ = γ as functions = γ₀ as functions (cast preserves function). So `cov.liftPath γ e₀ γ.source = cov.liftPath γ₀ e₀ γ₀.source` via eq_liftPath_iff' applied twice or via the uniqueness. Then:

```
  have hsame : hp.liftPath γ e₀ γ.source = hp.liftPath γ₀ e₀ γ₀.source := by
    rw [hp.eq_liftPath_iff']
    -- show p ∘ liftPath γ .. = γ₀ ∧ liftPath γ .. 0 = e₀
    refine ⟨?_, hp.liftPath_zero γ e₀ γ.source⟩
    -- p ∘ liftPath γ e₀ γ.source = γ = (as C(I,X)) γ₀
    rw [hp.liftPath_lifts γ e₀ γ.source]  -- wait liftPath_lifts : p ∘ cov.liftPath γ e γ_0 = γ
    -- need γ = γ₀ as continuous maps: cast
    ext t; rfl
```

`Path.cast` def: `def cast (γ : Path x₀ x₁) (h₀ : x₀ = x₀') (h₁ : x₁ = x₁') : Path x₀' x₁' := ⟨γ.toFun, h₀ ▸ source', h₁ ▸ target'⟩` roughly. So as functions they're identical. `ext t; rfl` should work. Hmm — but `eq_liftPath_iff'`'s h_0 for γ₀: `γ₀ 0 = p e₀` — the type of `hp.liftPath_zero γ e₀ γ.source : hp.liftPath γ e₀ γ.source 0 = e₀`. The RHS of the iff': `p ∘ Γ = γ₀ ∧ Γ 0 = e₀` with Γ : C(I,E) = hp.liftPath γ e₀ γ.source. `hp.liftPath_zero γ e₀ γ.source` has exactly type `hp.liftPath γ e₀ γ.source 0 = e₀` ✓.

Then:
```
  calc e₁ = Γ 1 := Γ.target.symm
    _ = hp.liftPath γ e₀ γ.source 1 := congr_fun hΓlift 1  -- or .symm?
    _ = hp.liftPath γ₀ e₀ γ₀.source 1 := congr_fun hsame 1
    _ = e₀ := hEnd
```

where hΓlift : (Γ : C(I,E)) = hp.liftPath γ e₀ γ.source via eq_liftPath_iff'.

Wait — is `Joined` the right notion? `PathConnectedSpace.joined : ∀ x y, Joined x y` and `Joined e₀ e₁ : Prop := Nonempty (Path e₀ e₁)`. And in `injective_of_simplyConnected` I take `he₀e₁ : Joined e₀ e₁`. Then in the main theorem with [PathConnectedSpace E], get Joined via `PathConnectedSpace.joined e₀ e₁`.

Hmm, but actually — do I even need E path-connected for the final homeo? For injectivity yes. Standard statement: "A covering map of a path-connected space onto a simply connected space is a homeomorphism." — correct, requires domain path-connected (or at least the fibers in one component). Compactness of E gives the homeo from bijective continuous. Note: bijective + covering → homeo needs compact domain + Hausdorff codomain. ✓.

Now — one concern about the *application to sphere recognition*: what is the actual spherical-space-form statement? In Perelman's finite extinction, the pieces are S³/G. The relevant lemma: "if a finite group G acts freely and properly discontinuously on S³, then the quotient map S³ → S³/G is a covering map" — and via `IsQuotientCoveringMap.isCoveringMap`. Then the covering-of-simply-connected lemma is used as: "S³/G ≅ S³ iff π₁(S³/G) = 1"? Hmm — actually not directly: to conclude M ≅ S³ from M = S³/G and π₁(M)=1, we need π₁(S³/G) ≅ G — that's van Kampen/monodromy, NOT the covering lemma. Hmm.

Where does coveringOfSimplyConnectedIsHomeo actually get used in sphere recognition? Consider: X simply connected 3-manifold (homotopy sphere M), E = universal cover M̃. Universal cover of a homotopy sphere IS the homotopy sphere itself (π₁ = 1 → universal cover = M). Trivial. Hmm.

The real use in recognition: **uniqueness of the universal cover**: M ≅ S³/G with M simply connected → the covering S³ → M is the universal cover → G = π₁(M)... that's again covering theory classification (π₁ = deck group = G), not the homeo lemma.

Alternatively, the use could be: **S³/G is not simply connected for G ≠ 1** — contrapositive of the covering lemma: if p : E → X is a covering with E path connected and X simply connected then p is a homeo; so if p is a covering with nonempty non-singleton fibers (not injective) then X is not simply connected. For sphere recognition: S³ → S³/G has fibers of size |G|; so if G ≠ 1, π₁(S³/G) ≠ 1, and hence S³/G ≅ M (homotopy sphere with π₁ = 1) forces G = 1, i.e. M ≅ S³. The contrapositive form: **covering with disconnected/nontrivial fiber ⇒ base not simply connected**. That IS a genuine use: "M ≅ S³/G and π₁(M) = 1 ⟹ G = 1" via: if G ≠ 1 then the covering S³ → M = S³/G is a nontrivial covering, and a nontrivial covering of a path-connected space E... base would have to be non-simply-connected. 

I can formalize exactly that contrapositive concretely:

**theorem cover_of_simplyConnected_isHomeo (contrapositive flavor)**: 
`[CompactSpace E] [PathConnectedSpace E] [T2Space X] [SimplyConnectedSpace X] {p : E → X} (hp : IsCoveringMap p) : p.Injective ↔ p.Surjective` — hmm too cute. Let me just prove the clean positive statement plus the injectivity statement, and also a concrete corollary:

**corollary**: `nontrivialCover_not_simplyConnected`: if p : E → X covering, E compact path-connected nonempty, p not injective, then ¬ SimplyConnectedSpace X? Wait — that requires p surjective for the "bijective iff injective" reduction... If p not injective and X simply connected → contradiction with injectivity. So: `(hp : IsCoveringMap p) (h : ¬ Injective p) : ¬ SimplyConnectedSpace X` with [CompactSpace E] [PathConnectedSpace E] [Nonempty E] [T2Space X]. No surjectivity needed (the contrapositive doesn't need surj since injectivity theorem doesn't need surj). And in the spherical space form application: p = quotient map S³ → S³/G (surjective anyway).

And the positive concrete application for the milestone "downstream use": 

**theorem sphereQuotientCovering (n : ℕ)**: the antipodal ZMod-2 action on Sphere n gives `IsCoveringMap (Quotient.mk (orbitRel))` — for n ≥ 1? For n = 0: S⁰ = {±1}, action free — actually ZMod 2 acts freely on S⁰ too (two points, swaps them). ProperlyDiscontinuousSMul requires LocallyCompactSpace + T2Space — Sphere n is fine for all n. Let me check `ProperlyDiscontinuousSMul` instance existence for this action — I doubt mathlib has it for the antipodal action; I'll construct it from the metric: exists_nhds_image_smul_eq_self via open ball of radius 1/2... Actually simpler: `ProperlyDiscontinuousSMul.of_discrete_group`? Let me check what instances exist. In `Topology/Algebra/ConstMulAction.lean` there should be `ProperlyDiscontinuousSMul` class with instances: `ProperlyDiscontinuousSMul.of_discreteGroup`? Hmm. The class: `class ProperlyDiscontinuousSMul (Γ : Type*) [Group Γ] [TopologicalSpace T] [MulAction Γ T] : Prop where finite_disjoint_inter_image : ∀ x, ∃ U ∈ 𝓝 x, ∀ g, (g • ·) '' U ∩ U = ∅ ∨ g • x = x`. 

For the antipodal action on the sphere: for x ∈ S^n, take U = ball x 1 (radius 1) ∩ sphere? We need a neighborhood U of x with (g•U) ∩ U = ∅ for g ≠ 1 and g•x ≠ x (action free, so any g ≠ 1 has g•x = -x ≠ x for n ≥ 1; for n = 0: S⁰ = {±(1)}; g=1 sends x ↦ -x ≠ x ✓ free). If y ∈ (g•U) ∩ U, y = g•u, u ∈ U, y ∈ U: ‖y - x‖ < 1 and ‖y - g•x‖ = ‖g•u - g•x‖ = ‖u - x‖ < 1. So ‖g•x - x‖ ≤ ‖y - x‖ + ‖y - g•x‖ < 2. If g = 1 then g•x = x ✓ (allowed: `g • x = x`). If g ≠ 1, g•x = -x, then ‖-x - x‖ = 2‖x‖ = 2 (since x on unit sphere) < 2 contradiction. So the antipodal action is properly discontinuous. In Lean: need `∃ U ∈ 𝓝 x, ∀ g, ((g • ·) '' U ∩ U).Nonempty → g • x = x`. With U = ball x (1) ∩ Sphere? In the subspace topology of the sphere, a neighborhood: use `Metric.ball x (1) : Set (Sphere n)`? Since the sphere is a subtype of EuclideanSpace, Metric.ball in the subspace metric — `Metric.ball x 1` as a set in the sphere (the ball is w.r.t. the metric on Sphere n). Points of ball x 1: y with dist y x < 1, i.e., ‖y - x‖ < 1 where both on the sphere. Fine — U = Metric.ball x (1 : ℝ) : Set (Sphere n)? Hmm, but the sphere's metric is the induced metric, which agrees. In Lean: `Metric.ball (α := Sphere n) x 1` hmm — does `Sphere n` have a metric space instance? `Metric.sphere` as a subtype: `Metric.sphere 0 1` with subtype topological space; there is `MetricSpace` instance for it? Since EuclideanSpace is an EMetricSpace, the subtype gets `EMetricSpace`/`MetricSpace` instance? Hmm, in mathlib subtypes of a metric space have the induced metric (there's an instance `MetricSpace s` for sets? Actually `instMetricSpaceSubtype`? There's `Metric.instMetricSpaceSubtype`? I recall `Subtype.metricSpace`... Let me not rely on it; I can work with neighborhoods in the ambient space and intersect. Simpler: build the neighborhood via `isOpen_ball.inter`... 

Actually alternative cleaner approach: use mathlib's existing theorem for properly discontinuous actions of *finite* groups with `IsCancelSMul` on T2 spaces? Hmm — there's `ProperlyDiscontinuousSMul.of_isFinite`? Let me grep. In `Mathlib/Topology/Algebra/ProperAction/...` there are results: a finite group acting continuously and freely on a T2 space acts properly discontinuously? There is: `properlyDiscontinuousSMul_of_finiteGroup`? I recall `IsProperSMul` and `properlyDiscontinuousSMul_iff_isProperSMul`... Let me grep for `of_isFinite` / `finite` in ConstMulAction.lean.
dsh: reasoning:
Two key findings:

1. `instance (priority := 100) Finite.to_properlyDiscontinuousSMul [Finite Γ]` — every action of a finite group is properly discontinuous! Wait, that seems too strong... Let me look at it. Line 596. If it just requires [Finite Γ] with no continuity/topology on Γ... The class `ProperlyDiscontinuousSMul` doesn't require a topology on Γ or continuity of the action. Hmm, actually that's plausible: the class is about existence of neighborhoods whose translates are disjoint, and the instance from finite... wait, how can every finite group action on any topological space be properly discontinuous?? The point: for finite Γ, the map (g, x) ↦ g•x — "properly discontinuous" in this definition means: ∀ x, ∃ U ∈ 𝓝 x, ∀ g, (g•U) ∩ U ≠ ∅ → g•x = x. For a finite group action WITHOUT continuity assumptions this is false! E.g., finite group acting trivially-but-not... hmm wait: (g•U) ∩ U ≠ ∅ — if g•x = x then any U works since g•x = x is allowed. If g•x ≠ x... For finite Γ, is there always such U? NO — that's false in general without T2/continuity assumptions. Let me actually read line 596.

2. No sphere simply-connected theorem found by my grep in the listed files... Let me search more broadly.
dsh: reasoning:
The `ProperlyDiscontinuousSMul` here is the compact-pair definition: for finite Γ it's trivial (`Set.toFinite`). And the quotient covering theorem needs `IsCancelSMul G E` (free action) + `IsQuotientMap` + [ProperlyDiscontinuousSMul]. 

The chain for the antipodal action:
- `IsCancelSMul (ZMod 2) (Sphere n)`: g • x = x → g = 1. For the antipodal action: 1 • x = -x = x → x = 0, but sphere has no 0 (‖x‖=1). Fine.
- `ContinuousConstSMul (ZMod 2) (Sphere n)`: g=0 identity, g=1 negation, continuous.
- `ProperlyDiscontinuousSMul`: instance from `[Finite (ZMod 2)]` ✓ automatic.
- Then `isQuotientCoveringMap_quotientMk_of_properlyDiscontinuousSMul` needs `[LocallyCompactSpace E] [T2Space E]` and gives `IsQuotientCoveringMap (Quotient.mk (orbitRel)) (ZMod 2)`, from which `.isCoveringMap : IsCoveringMap (Quotient.mk ..)`.

Is `IsCancelSMul` the right class? In the theorem: `isQuotientCoveringMap_quotientMk_of_properlyDiscontinuousSMul [IsCancelSMul G E] : IsQuotientCoveringMap (Quotient.mk (MulAction.orbitRel G E)) G`. `IsCancelSMul G E` is `isCancelSMul` : right cancellation: `g • e = g' • e → g = g'`? Let me check the class def: `class IsCancelSMul (G α) [SMul G α] : Prop where right_cancel' : ∀ g g' a, g • a = g' • a → g = g'`. For a 2-element group {1, g}: if 1•x = g•x i.e. x = -x → x = 0 contradiction on the sphere. So `IsCancelSMul (ZMod 2) (Sphere n)` holds for n ≥ 0. ✓ 

Hmm wait, but actually — does the quotient-covering need [LocallyCompactSpace]? Yes: `isQuotientCoveringMap_quotientMk_of_properlyDiscontinuousSMul` is in section `variable [ProperlyDiscontinuousSMul G E] [LocallyCompactSpace E] [T2Space E]`. Sphere n is locally compact (it's compact and T2 → locally compact ✓ — mathlib has `instLocallyCompactSpaceOfCompactT2`? something like `locallyCompactSpace_of_compact_t2`? there's an instance: compact + T2 → LocallyCompactSpace via... hmm, `instLocallyCompactSpace` requires proper space etc. There is: `[CompactSpace α] [T2Space α] → LocallyCompactSpace α`? I believe mathlib has an instance `LocallyCompactSpace.of_compact_t2`... let me check when I get there; if not, prove it inline via `isCompact_closure`... Actually there's `instLocallyCompactSpaceOfCompactT2` hmm. I'll check.)

And crucially — where does this connect to my D12 lemmas? The base space X = quotient of Sphere n by antipodal — for n = 2: RP², for n = 3: RP³. And **sphereSimplyConnected** for n ≥ 2 is needed for the *application* (e.g., "any compact path-connected space covering S³ is ≅ S³"). Is there a simply-connected sphere instance in mathlib? My grep found only SimplyConnected.lean and Lifting.lean referencing SimplyConnectedSpace. So mathlib does NOT have sphere simply-connectedness. Hmm. frenzymath Hatcher Ch1/Sphere.lean has `sphereSimplyConnected_of_two_le` (per earlier checkpoint notes). But its toolchain is incompatible.

OK so the plan for THIS invocation:
1. C1: `IsCoveringMap.surjective_of_pathConnected` (thin wrapper, uses mathlib's `comp_subtypeVal_pathComponent_surjective`).
2. C4: `coveringOfSimplyConnectedIsHomeo` + `injective_of_simplyConnected` (self-contained proof using mathlib's path lifting + SimplyConnectedSpace API).
3. Free action of a finite group → covering map instance application: antipodal ZMod 2 action on Sphere n → `IsCoveringMap (Quotient.mk (orbitRel))`, with base = RP^n. Also compactness + nonemptiness of the quotient, T2 (via t2Space_of_properlyDiscontinuousSMul_of_t2Space instance).
4. Downstream use: `coverOfSphereThreeIsHomeo`: `[CompactSpace E] [PathConnectedSpace E] {p : E → Sphere 3}, IsCoveringMap p → Surjective p → Nonempty (E ≃ₜ Sphere 3)` — hmm, but this needs `SimplyConnectedSpace (Sphere 3)` which mathlib lacks!

Option A: state `coveringOfSimplyConnectedIsHomeo` generally (this is the main theorem; general). For the concrete application, note that `SimplyConnectedSpace (Sphere 3)` is exactly the missing piece — but wait, can I prove sphere simply connected myself? That's π₂(S³) = 1 / S² simply connected — nontrivial topology but doable via e.g. "S² minus a point ≅ ℝ² (stereographic), any loop in S² can be avoided" — hmm, that's real work (the classical proof of π₁(S²) = 1 uses either van Kampen or the two-disks argument: S² = D² ∪ D², each disk simply connected (convex), intersection path connected (annulus/S¹). Actually S² = north-hemisphere-disk ∪ south-hemisphere-disk, intersection = equator circle ≃ S¹ path-connected. van Kampen → π₁(S²) = 1. Requires van Kampen.

Alternatively the well-known elementary proof: every loop in S² avoids some point (measure argument or the "loop is not onto" via local path-connectedness + a countable dense... hmm). The clean proof in Hatcher Ch1 (frenzymath has it): π₁(Sⁿ) = 1 for n ≥ 2 via the two-open-set decomposition and path-connected intersection argument + "each loop decomposes into loops in the disks" — that IS van Kampen-lite. frenzymath HatcherLib/Ch1/Sphere.lean has `sphereSimplyConnected_of_two_le` sorry-free.

Porting that whole file is too much for this invocation. Better plan: prove π₁(S²)=1 MYSELF via the elementary approach adapted to what mathlib has:

**Theorem** `sphere2SimplyConnected : SimplyConnectedSpace (Sphere 2)`... Even simpler: prove it for the *standard concrete case* needed. Hmm, wait — but the D12 objective is "a gluing/covering lemma needed by sphere recognition". The covering lemma `coveringOfSimplyConnectedIsHomeo` is the deliverable; the sphere-simply-connected input is a dependency to REQUEST (from frenzymath port or D-someone). The task says "No claimed blocker closure without a constructor of that missing input and a downstream checked use." So I should:
- Prove `coveringOfSimplyConnectedIsHomeo` (general).
- Prove the antipodal quotient covering map (concrete, nondegenerate: RP^n for all n).
- For a fully concrete *checked downstream use*, I need SOME simply-connected base. Options: use `SimplyConnectedSpace.ofContractible` — a contractible space! E.g., use the closed disk 𝔻³ (convex → contractible → simply connected) as the base. Concrete downstream use: **any compact path-connected space covering the 3-disk is homeomorphic to the 3-disk**. Hmm, geometrically trivial but it IS a checked downstream use with actual topological spaces (Disk 3).
- Better concrete use for sphere recognition: The double-disk gluing! `doubleDiskQuotHomeoSphere` is already proved. The covering lemma's role in recognition: with `SimplyConnectedSpace (Sphere 3)` as an explicit REMAINING BLOCKER + a `#print axioms`-clean statement-only file documenting it... no wait, "A missing theorem is not a new axiom" — a statement-only lemma (declared as `axiom`) is forbidden in the deliverable. So the downstream application that genuinely uses the covering lemma must be stated with hypotheses that I can satisfy TODAY. The cleanest: use the fact that the 3-disk is contractible (mathlib: convex → contractible? `ContractibleSpace` of convex sets in a normed space — there's `instContractibleSpaceConvex`? something). Let me verify: mathlib has `ContractibleSpace` instance for convex subsets? In `Mathlib/Topology/Homotopy/Contractible.lean`: `instance (priority := 100) ContractibleSpace.of_convex`? Let me check. If the closed ball 𝔻³ is contractible → `SimplyConnectedSpace.ofContractible`. Then:

**Theorem** `coverOfDiskThreeIsHomeo`: `[CompactSpace E] [PathConnectedSpace E] {p : E → Disk 3}, IsCoveringMap p → Surjective p → Nonempty (E ≃ₜ Disk 3)` — checked downstream use of the covering lemma with a real space. 

But hmm, is that "needed by sphere recognition"? It's a stepping stone: the same lemma with base Sphere 3 (the recognition target) is one instance away (`SimplyConnectedSpace (Sphere 3)`). I can also record `sphereSimplyConnected` as the exact named dependency request.

Alternatively — prove sphere simply-connected myself in this invocation? That's the honest "close the blocker with a constructor" route, and it makes the downstream use REAL: `coverOfSphereThreeIsHomeo`. The classical elementary proof of π₁(S²)=1 without van Kampen:

**Lemma (Hatcher 1.5-ish)**: If X = U ∪ V, U, V open simply connected... that's van Kampen again.

**Elementary loop-avoidance approach**: For n ≥ 2, every path γ : I → Sⁿ is homotopic (rel endpoints) to a path avoiding any given point. Proof: subdivide I finely, and within each small subinterval push the path off a point p via local homeomorphism Sⁿ\{p} ≅ ℝⁿ (stereographic). But "every loop avoids some point": a loop γ in S²: choose a point p ∈ S² not in the image of γ... but the image may be all of S² (space-filling curves exist!). The classical elementary argument avoids this via: homotope rel {0,1} into the complement of a point by subdividing into small pieces and pushing each piece off p (each piece is small, and the local deformation uses the local structure: on each small ball around a point, push off p using stereographic coordinates of Sⁿ\{q} where q ≠ p... each small arc lies in some ball, and we can deform it within Sⁿ\{p} because locally Sⁿ\{p} ≅ ℝⁿ is contractible... but pushing within small pieces requires the pieces' endpoints fixed? We can allow endpoints to slide along short geodesic segments...). This is the "Lebesgue number + local contractibility" argument — messy in Lean.

Van Kampen-lite argument: S² = U ∪ V, U = S² \ {N} (north pole removed) ≅ ℝ² via stereographic, V = S² \ {S} ≅ ℝ². U, V open (complements of closed points... in S², points are closed ✓), simply connected (homeomorphic to ℝ² — need stereographic homeo S²\{N} ≅ ℝ²! I'd need to prove the stereographic homeomorphism — doable with the same machinery as my SphereGluing.lean). U ∩ V = S² \ {N,S} ≅ ℝ² \ {0} ≃ S¹, path connected (need: ℝ²\{0} path connected — provable). A loop γ in S²: decompose into loops in U and V (Lebesgue-number subdivision, each subpath in U or V; each subpath combined with connecting arcs gives loops in U or V — each nullhomotopic; the whole loop is a composition of nullhomotopic loops → nullhomotopic). The "combining with connecting arcs" requires U ∩ V path connected and the basepoint in U ∩ V... The full formal proof = van Kampen. That's the frenzymath Hatcher Ch1 VanKampen.lean (~large).

Given the 4-hour budget, the right call: prove the general covering lemma + the quotient-covering instance + checked downstream use via contractible base (Disk 3 = the actual disk from my existing file), and record `SimplyConnectedSpace (Sphere 3)` as the next dependency (with frenzymath port as the recorded source). This matches the checkpoint plan (C1, C2→exists, C4) and delivers a genuine closure of "covering lemma needed by sphere recognition" in its general form, with the sphere instance pending.

Hmm wait — actually, let me reconsider: maybe I can prove `SimplyConnectedSpace (Sphere 2)` with a slicker argument using what mathlib has. Search: does mathlib have `Sphere` homotopy results? `grep "Sphere" Mathlib/Topology/Homotopy` — no file. What about `Contractible` of `ℝⁿ\{0}` for n ≥ 2? No.

Alternative concrete downstream use that's genuinely sphere-recognition-flavored WITHOUT needing sphere simply-connected:

**spherical space form theorem (topological skeleton)**: Let G act freely on S³ (properly discontinuous — automatic for finite G). Then p : S³ → S³/G is a covering map; S³/G is compact, T2, path-connected (quotient of path-connected ✓ — `PathConnectedSpace` descends? quotient of path-connected space is path-connected ✓ mathlib has `Quotient.pathConnectedSpace`?). And: **if additionally S³/G is simply connected, then S³/G ≅ S³** (via coveringOfSimplyConnectedIsHomeo with E = Sphere 3: E compact ✓, E path connected ✓, base simply connected hypothesis, p covering ✓, surjective ✓). That's the exact spherical-space-form recognition statement! The hypothesis "S³/G simply connected" is precisely the recognition hypothesis (M homotopy sphere → M simply connected), and it's not trivially equivalent to the conclusion. This gives a *checked, nondegenerate downstream use*:

```lean
theorem sphericalSpaceForm_recognition {G} [Group G] [MulAction G (Sphere 3)] 
    [ContinuousConstSMul G (Sphere 3)] [IsCancelSMul G (Sphere 3)] [Finite G]
    (hsc : SimplyConnectedSpace (Quotient (MulAction.orbitRel G (Sphere 3)))) :
    Nonempty ((Quotient (MulAction.orbitRel G (Sphere 3))) ≃ₜ Sphere 3)
```

with E = Sphere 3: [CompactSpace ✓ (proved in SphereGluing)], [PathConnectedSpace ✓ (sphere path connected — do I have it? path connected sphere: Sphere n is path connected for n ≥ 1 — need to prove or find in mathlib. `PathConnectedSpace` of sphere: mathlib has `sphere.pathConnectedSpace`? Let me check. There's `Metric.sphere` results in `Mathlib/Topology/MetricSpace/Sphere.lean`? hmm. In my SphereGluing.lean I proved `sphereNonempty` and compactness. Path connectedness: Sphere n (n ≥ 1) is path connected — proof: via stereographic-ish or via the explicit "normalize the segment" trick: for x, y ∈ Sⁿ, the segment (1-t)x + t y is never zero for... hmm, it can vanish if y = -x. Standard trick: path through a third point e₀ if y = -x. Formalizable: for x ≠ -y use normalization of the segment; for x = -y go via e₀. Requires "segment never zero": if (1-t)x + ty = 0 then x = -t/(1-t) y... sign argument. Doable but some work. Alternatively: mathlib might have `PathConnectedSpace (Sphere n)`? Let me check `Mathlib/Topology/MetricSpace/Sphere.lean` and the TopCat sphere module.)

Also need T2 of the base for the homeo? The homeo theorem `Continuous.homeoOfEquivCompactToT2` needs T2 on the codomain: the quotient has T2 via `t2Space_of_properlyDiscontinuousSMul_of_t2Space` instance (needs LocallyCompactSpace (Sphere 3) — from compact+T2 instance).

And `SimplyConnectedSpace (Sphere 3)` is still needed for the POSITIVE instance (i.e., to actually apply this with hsc satisfied — but the recognition hypothesis IS hsc; for the general theorem no sphere-simply-connected is needed). Nondegeneracy: construct an explicit FREE action of ZMod 2 on Sphere 3 (the antipodal action) to witness that the class of inputs is nonempty... but the *theorem's* conclusion needs hsc which FAILS for the antipodal quotient — which is exactly the point (RP³ is not simply connected, no homeo). For non-vacuity of the covering-lemma instance itself, the trivial action/identity map gives a vacuous-but-legit instance? The real non-vacuity test for `coveringOfSimplyConnectedIsHomeo`: E = X = Sphere 3, p = id: id is a covering map? `IsCoveringMap id`: via `homeomorph_comp`/trivialization — `IsCoveringMap.of_discreteTopology`? No. Better: `isCoveringMap_id`: mathlib has `isCoveringMap_id`? Let me check: `theorem isCoveringMap_id : IsCoveringMap (fun x : X => x) := ...` Hmm. Or use my existing doubleDisk quotient: `DoubleDiskQuot 2 ≃ₜ Sphere 3` homeo e, p = e ∘ ... covering map from homeo: `IsCoveringMap.comp_homeomorph`/`homeomorph_comp`. So: `IsCoveringMap (e.symm : Sphere 3 → DoubleDiskQuot 2)` and X = DoubleDiskQuot 2 (compact ✓ path connected? quotient of path-connected space ✓) — but SimplyConnectedSpace (DoubleDiskQuot 2)?? That's the same sphere-simply-connected problem (it IS a sphere!). OK the cleanest non-vacuity: contractible base. `ContractibleSpace (Disk 3)`? Closed ball is convex → mathlib `instContractibleSpace` for convex sets in a TVS? Let me check Contractible.lean for convex instance. Then:

**checked nondegenerate example**: `coverOfDiskIsHomeo` applied with E = Disk 3, p = id: `IsCoveringMap id` — need to verify existence in mathlib; else p = (homeo from double disk)... simplest: mathlib has `isCoveringMap_id`? I'll grep. Actually simplest nondegenerate instantiation: **p = id on Disk 3**: `coveringOfSimplyConnectedIsHomeo (E := Disk 3) (X := Disk 3) p := id ...` producing `Nonempty (Disk 3 ≃ₜ Disk 3)` — trivial homeo, but it checks the whole pipeline compiles with real spaces. Slightly silly. Better: a genuinely 2-sheeted-looking covering of a simply-connected compact base can't exist — negative control: **the antipodal ZMod 2 quotient S¹ → RP¹ is a covering with path-connected compact domain whose base is NOT simply connected** — I can't easily prove "not simply connected" (that's π₁(S¹) = ℤ!). OK — negative control that's provable: if RP¹ were simply connected, coveringOfSimplyConnectedIsHomeo would give RP¹ ≅ S¹; the contradiction requires non-homeo proof... also hard.

Let me not overreach. Concretely this invocation:

1. `IsCoveringMap.surjective_of_pathConnected` (C1).
2. `IsCoveringMap.injective_of_simplyConnected` (fiber injectivity).
3. `coveringOfSimplyConnectedIsHomeo` (C4).
4. Antipodal action on Sphere n: build `MulAction (ZMod 2) (Sphere n)`, `ContinuousConstSMul`, `IsCancelSMul`, hence `IsCoveringMap (Quotient.mk (orbitRel))` via mathlib's `isQuotientCoveringMap_quotientMk_of_properlyDiscontinuousSMul` + `.isCoveringMap`; also `CompactSpace`/`T2Space`/`PathConnectedSpace`/nonempty of the quotient RP^n. This is the concrete "covering statement with actual topological spaces" (geometric-realization flavored: quotient topology).
5. Downstream use: `sphericalSpaceFormCovering`: recognition statement for free finite group actions on S³ with the simply-connected-base hypothesis, proved by 3+4. Non-vacuity: witness the hypotheses for a nontrivial example? The hypotheses include `SimplyConnectedSpace (quotient)` which fails for RP³ — I can't construct a satisfying witness without sphere-simply-connected or a trivial action. With G = trivial group (PUnit? or `Subsingleton`), the quotient ≅ S³ — homeo between quotient of trivial action and Sphere 3 needs a homeomorphism proof (quotient by trivial relation ≅ original — doable: `quotientEquivOfEq`-style, or use `Homeomorph` from the quotient... mathlib: `Quotient.mk` with trivial orbitRel: there's `Homeomorph.quotientTrivial`? hmm, might need `equivQuotientOfSubsingleton`... some work but tractable). Simpler witness: E = Sphere 3, p = id via the general theorem, base = Sphere 3 — needs `SimplyConnectedSpace (Sphere 3)`. ARGH. Circular.

    Alternative witness without sphere-simply-connected: E = Disk 3, base = Disk 3, p = id: needs `SimplyConnectedSpace (Disk 3)` = via contractible convex ball. Check mathlib contractible instance for convex sets — if exists, this is 2 lines and is the honest non-vacuity test. Also `PathConnectedSpace (Disk 3)` (convex → path connected ✓ mathlib `convex → PathConnectedSpace`? there's `PathConnectedSpace` of convex sets: `convex_Icc`? The ball: mathlib has path-connectedness for convex sets — `Convex → PathConnectedSpace`? I recall `instPathConnectedSpaceConvex`? something like that. If missing: I proved disk is connected? No, I proved compact + nonempty. Path connectedness of the ball is easy to prove manually (segment path stays in the ball by convexity: ‖(1-t)x + ty‖ ≤ (1-t)‖x‖ + ‖y‖ ≤ 1). I can prove that directly — 10 lines.)

So the witness plan: `coverOfDiskThreeIsHomeo` as a *checked application instance* with p = id and base = Disk 3, given `ContractibleSpace (Disk 3)` (or prove `SimplyConnectedSpace (Disk 3)` directly by convexity: any two paths in a convex set are homotopic via the linear homotopy! That's actually trivial to prove directly: paths p₁, p₂ : Path x y in a convex set S: F(t, s) = (1-s) p₁ t + s p₂ t — stays in S by convexity, continuous, rel endpoints since both endpoints fixed. That's a self-contained proof of `SimplyConnectedSpace` for any convex set — maybe 30 lines. No dependency on mathlib's contractible machinery. 

Let me check first whether mathlib already has `SimplyConnectedSpace`/`ContractibleSpace` for convex sets. grep "convex" in Contractible.lean.

6. Also — dimension audit: the antipodal quotient RP^n, n ≥ 1: PathConnectedSpace (Sphere n) needed for surjectivity instance `C1`'s application... not needed for the quotient covering statement itself.

Let me also verify the homeo theorem name: `Continuous.homeoOfEquivCompactToT2`. And check mathlib for `isCoveringMap_id`. Also confirm `Quotient.compactSpace` and `PathConnectedSpace` quotient instances exist.

Also — IMPORTANT scope check: the task says "If an upstream definition is wrong..." — not relevant here. And "Prove a concrete nondegenerate example when necessary to test non-vacuity" — the witness above addresses it.

Also there's the MoiseBranch.lean file — let me read it and the other existing files quickly to stay consistent, then check names, then write the new file `CoveringLemma.lean` (or `CoveringRecognition.lean`). The AxiomAudit.lean parses `#print axioms` — I'll extend it with the new declarations. Let me read my existing files now.
dsh: reasoning:
The audit currently has `coveringOfSimplyConnectedIsHomeo` in MoiseBranch.lean as a *statement-only* (named next lemma) — likely declared as a structure/def? Let me check MoiseBranch.lean to see how it's declared (if it's an `axiom`, that's forbidden in deliverable — but the checkpoint said "named next lemma (statement-only)". The axiom audit passed, meaning its `#print axioms` output... wait, if it were an axiom, the audit would flag the axiom itself. The audit presumably excludes those names (they're in the second batch of #print axioms but maybe the script has a whitelist of "named-lemma" statements?). Let me read MoiseBranch.lean and the audit script.
dsh: reasoning:
The audit script's parser has a `head` regex: `^'(.*?)'\s+depends on axioms:\s*\[(.*?)(\])?$`. For a declaration whose axioms set is `[propext, Classical.choice, Quot.sound]` — parses the set. If a declaration has NO axioms: `'name' depends on axioms: []` → set empty → OK (subset of ALLOWED ✓).

Note that the statement-only `def coveringOfSimplyConnectedIsHomeo : Prop` has axioms {} — passes. But now I'm going to *prove* the covering lemma as a real theorem. I should replace the `def coveringOfSimplyConnectedIsHomeo : Prop` in MoiseBranch.lean... no wait — "Do NOT ... modify" — the rules say don't modify tests or other workers' files, but MoiseBranch.lean is MY file. The instruction says do not delete imported modules. MoiseBranch.lean is my authored file; I can edit it. But careful: the target statement `coveringOfSimplyConnectedIsHomeo : Prop` is referenced in checkpoint/audit as a statement-only ledger. If I now PROVE the covering lemma, the natural move:

1. Add a new module `CoveringLemma.lean` with the real theorems:
   - `IsCoveringMap.surjective_of_pathConnected` (C1)
   - `IsCoveringMap.injective_of_simplyConnected`
   - `coveringOfSimplyConnectedIsHomeo` — hmm, name clash with the Prop def. I should keep the name `coveringOfSimplyConnectedIsHomeo` for the REAL theorem (it's the target statement). The existing Prop `def coveringOfSimplyConnectedIsHomeo` in MoiseBranch.lean is in the same namespace → name conflict. Options: (a) remove/replace the Prop def in MoiseBranch.lean with the proved theorem + keep the ledger as a `def coveringOfSimplyConnectedIsHomeo_statement : Prop :=` equal to the theorem statement... Since I own MoiseBranch.lean, editing it is fine (it's a task-local file; the DAG doc records it as a named next lemma — updating a ledger entry from "missing" to "proved" is exactly the kind of update I'm supposed to do; the file header says "Statement-only ledger ... to be proved in later invocations" — the natural evolution). 

I must be careful: the audit script checks specific names. If I move `coveringOfSimplyConnectedIsHomeo` from Prop to Theorem, the `#print axioms` will report its dependencies — hopefully within {propext, Classical.choice, Quot.sound}. The parser handles multi-line. If the theorem's axiom set is e.g. `[propext, Classical.choice, Quot.sound]` — fine.

2. In MoiseBranch.lean: replace the `def coveringOfSimplyConnectedIsHomeo : Prop := ...` with the real theorem (declared in CoveringLemma.lean, imported by MoiseBranch.lean) and add an `example : coveringOfSimplyConnectedIsHomeo := ...`? Actually simplest: MoiseBranch imports Downstream; I'll have CoveringLemma import SphereGluing (+ mathlib bits). Let me structure:

- `CoveringLemma.lean` (new): imports `Mathlib` (or targeted imports) + `Poincare.D12.TriangulationTopology.SphereGluing` for Disk/Sphere defs. Proves:
  - `IsCoveringMap.surjective_of_pathConnected` (namespace `Poincare.D12.TriangulationTopology`? or root namespace `IsCoveringMap`? The audit expects full names under `Poincare.D12.TriangulationTopology.*`. To keep the audit simple, put everything in my namespace: `Poincare.D12.TriangulationTopology.coveringSurjective_of_pathConnected` etc. But `IsCoveringMap.surjective_of_pathConnected` as a namespaced lemma is cleaner mathematically... The audit parser keys on full names `Poincare.D12.TriangulationTopology.<name>`. Names in `IsCoveringMap` namespace inside my namespace? If I write `namespace IsCoveringMap` INSIDE `namespace Poincare.D12.TriangulationTopology`, the full name is `Poincare.D12.TriangulationTopology.IsCoveringMap.surjective_of_pathConnected` — that works with the audit as a new name to add. Hmm, but cleaner: keep flat names in my namespace to keep the audit simple:
    - `coveringSurjective_of_pathConnected`? Meh. I'll just use descriptive flat names:
    - `coveringMap_surjective_of_pathConnected`
    - `coveringMap_injective_of_simplyConnected`
    - `coveringOfSimplyConnectedIsHomeo` (THE target name — replaces the Prop def)
    - plus the antipodal/quotient stuff and the checked example.

3. The antipodal quotient module `AntipodalQuotient.lean` (new): ZMod 2 action on Sphere n, free, continuous; quotient covering map; compactness/T2/nonempty of the quotient; plus `sphericalSpaceFormRecognition` theorem using coveringOfSimplyConnectedIsHomeo:
   `[SimplyConnectedSpace (Quotient (MulAction.orbitRel G (Sphere 3)))] → Nonempty (Quotient ... ≃ₜ Sphere 3)` for finite free continuous G-actions. Wait — need PathConnectedSpace (Sphere 3) (domain of the covering map p : Sphere 3 → quotient). Need to prove sphere path-connectedness (n ≥ 1). Doable via explicit paths:
   - If y ≠ -x: path t ↦ normalize((1-t)x + t y) — need the segment to avoid 0: (1-t)x + t y = 0 → x = -t/(1-t)·y → norms: 1 = t/(1-t) → t = 1/2, then 1/2 x + 1/2 y = 0 → y = -x. So for y ≠ -x, segment never hits 0 ✓. Continuous ✓ (continuity of normalization on the complement of 0 — need to prove norm ≠ 0 pointwise + continuity — doable: the map u ↦ u/‖u‖ is continuous where defined; on I it's the composition. Then prove each point is on the sphere: ‖u/‖u‖‖ = 1.
   - If y = -x: go x → e₀ → y = -x? wait y = -x, path: x → e₁ (first basis) via first construction (e₁ ≠ -x? if x = -e₁... hmm. Choose e₀ = first basis vector. If x ≠ -e₀ and y ≠ -e₀: x → e₀ → y... but wait y = -x. If x = -e₀ then y = e₀ — then x → e₀ IS the direct path with y = e₀... Let me just do: path from x to y: if y ≠ -x: direct; else x → e₀ → y where e₀ is the first basis vector; need x ≠ -e₀ and y ≠ -e₀: if x = -e₀ then y = e₀ and then y = e₀... direct segment from x to y has y ≠ -x? y = e₀, -x = e₀, so y = -x — direct segment impossible, but x → e₀ is the segment x → y itself which... y = e₀, so path x → e₀ = path x → y, and -x = y means the segment (1-t)x + t y = (1-t)x + t(-x) = (1-2t)x vanishes at t = 1/2. Ugh — antipodal pairs are exactly the problem. Need a third point z ∉ {±x}: use e₀ unless x = ±e₀, in which case use e₁ (for n ≥ 2; for n = 1, the circle: x → e₀ → ... hmm for S¹: if x = ±e₀, choose z = e₁ ✓ (n=1 has Fin 2, e₀, e₁) ✓. For n ≥ 1 always at least 2 basis vectors ✓. So: z = e₀ if x ∉ {±e₀}, else z = e₁. Then x → z, z → y with both segments nondegenerate: z ≠ -x, z ≠ -y ✓ (z chosen ∉ {±x} = {x, -x}; y = -x so {±y} = {±x} ✓). Then trans of the two paths. All formalizable with `Path.trans`/`PathConnectedSpace.mk`? `PathConnectedSpace` has `joined : ∀ x y, Joined x y` where Joined = Nonempty (Path x y). Prove via explicit construction. Fine — but this is a chunk of work (norm algebra for the segment path). Alternatively use mathlib: is there `PathConnectedSpace (Sphere n)`? Let me check `Mathlib/Topology/MetricSpace/Sphere.lean`.

Let me check for existing sphere path-connectedness and contractible convex results.
dsh: reasoning:
No convex → contractible in mathlib's Contractible.lean? Let me check where convex sets get ContractibleSpace: search "ContractibleSpace" across Topology/MetricSpace or Analysis/Convex.
dsh: reasoning:
- `Convex.contractibleSpace (hs : Convex ℝ s) (hne : s.Nonempty) : ContractibleSpace s` ✓ — so `Disk 3` (closed ball) is contractible → `SimplyConnectedSpace.ofContractible` → simply connected ✓.
- `Homeomorph.homeoOfEquivCompactToT2 [CompactSpace X] [T2Space Y] {f : X ≃ Y} (hf : Continuous f) : X ≃ₜ Y` (in Homeomorph/Lemmas.lean line 474) — the name I used in SphereGluing must be `Homeomorph.homeoOfEquivCompactToT2` (in namespace Homeomorph).

Now let me check the key remaining items:
1. `PathConnectedSpace (Disk n)` — for the E side of the example. Is the disk path-connected in mathlib? `Convex ℝ (closedBall ..)` — `PathConnectedSpace` of convex: mathlib has `PathConnectedSpace` for convex sets? Let me grep `Convex.*pathConnected` or `PathConnectedSpace` in Analysis/Convex.
2. `Quotient.compactSpace`, `Quotient` path-connected instance: `PathConnectedSpace.quotient`? Let me check `Mathlib/Topology/Connected/PathConnected.lean` for quotient instances.
3. `SimplyConnectedSpace` for convex: `SimplyConnectedSpace.ofContractible` — in SimplyConnected.lean line 79 ✓.
4. `IsCancelSMul (ZMod 2) (Sphere n)` — the class for free actions. Check its definition: `class IsCancelSMul (G α : Type*) [SMul G α] : Prop where right_cancel' : ∀ (g g' : G) (a : α), g • a = g' • a → g = g'`. 

Wait — careful: `IsCancelSMul` requires cancelling in the *smul* variable on the RIGHT: g • a = g' • a → g = g'. For ZMod 2 on the sphere: g • x = g' • x → g = g'. Suppose g = 1, g' = 0: 1 • x = -x = x = 0 • x → x = 0 — impossible on unit sphere. So the instance holds. But note `IsCancelSMul` in mathlib is a class on (G, α) with `SMul` — for the sphere (a subtype), SMul instance: I define `smul g x := if g = 0 then x else -x`. Hmm — wait: for the antipodal action, `MulAction` requires (g₁g₂) • x = g₁ • (g₂ • x) — with g₁=g₂=1: 0 • x = x and 1•(1•x) = -(-x) = x ✓.

Alternatively use `Multiplicative (ZMod 2)`? ZMod 2 is already a multiplicative group ✓ (ZMod is a ring; its multiplicative group of units is ZMod 2ˣ. Hmm — but MulAction G E requires [Group G]. `ZMod 2` as a group under addition? There's an AddGroup; Multiplicative (ZMod 2) is a multiplicative group. If I use `Multiplicative (ZMod 2)` then smul by (1 : Multiplicative (ZMod 2)) means the nonzero... confusing. Simplest: define the action on `ZMod 2` directly as `smul g x := if g = 1 then -x else x`, with MulAction laws checked by cases on g. Let me structure the action typeclass instances cleanly with `fin_cases` on g : ZMod 2.

Actually — even simpler and more natural: use the group `Multiplicative (ZMod 2)`? no. Or use `Equiv.Perm (Sphere n)`... Keep ZMod 2 with custom smul and prove instances by case analysis.

5. For the quotient-covering chain, I need: `[LocallyCompactSpace (Sphere n)] [T2Space (Sphere n)] [ProperlyDiscontinuousSMul G (Sphere n)] [IsCancelSMul G (Sphere n)] [ContinuousConstSMul G (Sphere n)]` then `isQuotientCoveringMap_quotientMk_of_properlyDiscontinuousSMul : IsQuotientCoveringMap (Quotient.mk (MulAction.orbitRel G E)) G`, then `.isCoveringMap : IsCoveringMap (Quotient.mk (orbitRel))`.

Check: is `Sphere n` locally compact + T2 in mathlib? T2: metric space ✓. LocallyCompact: Sphere n is compact (I proved `sphereCompactSpace : CompactSpace (Sphere n)`), and compact + T2 → locally compact: mathlib instance `instLocallyCompactSpaceOfCompactT2`? Let me grep. Actually in mathlib: `[CompactSpace α] [T2Space α] → LocallyCompactSpace α`? There is an instance `LocallyCompactSpace.of_isCompact_univ`? or "instLocallyCompactSpace"... grep needed.

Also `T2Space (Quotient (MulAction.orbitRel G (Sphere 3)))` via `t2Space_of_properlyDiscontinuousSMul_of_t2Space` instance (needs [LocallyCompactSpace][T2Space][ContinuousConstSMul][ProperlyDiscontinuousSMul] — all ✓).

6. For the final homeo in `sphericalSpaceFormRecognition`: `coveringOfSimplyConnectedIsHomeo` needs `[CompactSpace E] [PathConnectedSpace E] [SimplyConnectedSpace X] [T2Space X]` — E = Sphere 3 ✓ compact (my instance); PathConnectedSpace (Sphere 3) — need to prove (see below); X = quotient: SimplyConnectedSpace hypothesis given; T2 via instance ✓.

7. PathConnectedSpace (Sphere n): Let me check mathlib again — grep "PathConnectedSpace" in files mentioning sphere. Maybe in `Mathlib/Topology/Instances/Sphere.lean`? Let me search for `def sphere : TopCat` — the TopCat.sphere in `Mathlib/Topology/Category/TopCat/Spheres.lean`. Let me grep for PathConnectedSpace in that file.
dsh: reasoning:
- `Quotient.instPathConnectedSpace {s : Setoid X} [PathConnectedSpace X] : PathConnectedSpace (Quotient s)` ✓ (line 621).
- Locally compact: `Pi.locallyCompactSpace [∀ i, CompactSpace (X i)]` — for Sphere n as subtype of EuclideanSpace: subtype instances? For closed subsets... There's probably `instLocallyCompactSpace_of_isCompact`? Hmm. Simpler: there is `instance (priority := 100) [CompactSpace X] : WeaklyLocallyCompactSpace X` — but the covering theorem needs `LocallyCompactSpace` (strong). For closed ball in a complete metric space → locally compact: mathlib has `instLocallyCompactSpaceClosedBall`? or "isCompact_closedBall → locally compact" instance: there's `instance (priority := 100) [CompactSpace X] [T2Space X] : LocallyCompactSpace X`? Let me grep for that specific pattern.
dsh: reasoning:
No direct `CompactSpace + T2 → LocallyCompactSpace` instance found by grep. Hmm. Options for locally compact Sphere n:
- mathlib: `instLocallyCompactSpace_of_isCompact`? Search `LocallyCompactSpace` in Metric/Basic or "closedBall". There's `LocallyCompactSpace (EuclideanSpace ℝ (Fin n))` via `Pi.locallyCompactSpace_of_finite`? EuclideanSpace = a subtype-ish (it's `WithLp 2 (Fin n → ℝ)`). Pi.locallyCompactSpace_of_finite needs each factor locally compact — ℝ locally compact: `Real.instLocallyCompactSpace`? Yes mathlib has `Real.locallyCompactSpace`? I saw `instance Real.instPathConnectedSpace` at line 627 — likely `Real.instLocallyCompactSpace` exists too. But for the *sphere subtype*, locally compact requires closedness: `isClosed_sphere → LocallyCompactSpace (sphere)`? mathlib has `instLocallyCompactSpace_of_isClosed`? Let me grep "isClosed" in LocallyCompact.lean.

Actually — I recall the mathlib instance: `instance (priority := 100) instLocallyCompactSpace_of_isCompact` hmm not sure. Let me grep for `LocallyCompactSpace` combined with `Closeds` or subtypes.
dsh: reasoning:
For LocallyCompactSpace (Sphere n): `Pi.locallyCompactSpace [∀ i, CompactSpace (X i)]` — hmm, that's for Pi types with compact factors. The sphere is a subtype of the Pi type. Options:
- `Topology.IsInducing.locallyCompactSpace [LocallyCompactSpace Y] {f : X → Y}` — for a subspace embedding? There's probably `instLocallyCompactSpace_of_isClosed`/`isClosedEmbedding` somewhere else (Subtype.lean?). Let me check `Mathlib/Topology/Compactness/LocallyCompact.lean` lines 186-200 and search "isClosedEmbedding" for locallyCompactSpace elsewhere. Actually line 195: `Topology.IsInducing.locallyCompactSpace [LocallyCompactSpace Y] {f : X → Y}` — induces from locally compact. For subtype: the coercion Sphere n → EuclideanSpace is inducing (subtype embedding). But inducing + locally compact target does NOT give locally compact source in general (open subsets yes). For closed sets: yes but needs closedness. 

Hmm, alternatively use the compactness of the sphere + mathlib lemma `LocallyCompactSpace.of_isCompact_univ`? Does it exist? Search. Or `exists_compact_mem_nhds`... Actually: compact space + T2 → locally compact. Is there an instance in mathlib? I didn't find it by grep. Let me search more broadly for the instance "of_isCompact_univ".
dsh: reasoning:
No instance for `CompactSpace + T2 → LocallyCompactSpace`. Hmm. But I can prove it myself quickly: `LocallyCompactSpace X` is `local_compact_nhds : ∀ x n, n ∈ 𝓝 x → ∃ s ∈ 𝓝 x, s ⊆ n ∧ IsCompact s` — with compact X: for any x and n ∈ 𝓝 x, there's a compact neighborhood: take s = a compact set containing a neighborhood... take K = closure of U where U is a neighborhood inside n? In compact T2, every point has a basis of compact neighborhoods? That needs regularity. Compact T2 → regular → for x and open n ∋ x, there's closed F... The classical proof: compact T2 is regular, so there's an open U with x ∈ U ⊆ closure U ⊆ n, and closure U is compact (closed in compact). That's `compactSpace → regularSpace` (mathlib: `instRegularSpaceOfCompactT2`? something) + `isCompact_closure`. Alternatively the simple instance: `⟨fun x n hn ↦ ⟨univ, univ_mem, subset_univ n, isCompact_univ⟩⟩` — WAIT that's much simpler!! `local_compact_nhds : ∀ x n, n ∈ 𝓝 x → ∃ s ∈ 𝓝 x, s ⊆ n ∧ IsCompact s` — s = univ: univ ∈ 𝓝 x ✓ (isOpen_univ.mem_nhds trivial), univ ⊆ n? NO — univ ⊆ n is false unless n = univ. Hmm, so need s ⊆ n. 

OK so the classical route: compact T2 → regular → for each x and n ∈ 𝓝 x, there's open U ⊆ n with closure U ⊆ n... RegularSpace gives: ∀ U ∈ 𝓝 x, ∃ V ∈ 𝓝 x, closure V ⊆ U. mathlib: `regularSpace_tfae` or `RegularSpace : ∀ U ∈ 𝓝 x, ∃ V ∈ 𝓝 x, closure V ⊆ U`. Then s = closure V: compact (closed subset of compact: `IsClosed.isCompact`), closure V ⊆ n ✓, V ∈ 𝓝 x and V ⊆ closure V... need `closure V ∈ 𝓝 x` — V ⊆ closure V, V ∈ 𝓝 x → closure V ∈ 𝓝 x (superset of nhds is nhds ✓ `mem_of_superset`). So:

```lean
instance locallyCompactSpace_of_compact_t2 [CompactSpace X] [T2Space X] : LocallyCompactSpace X where
  local_compact_nhds x n hn := by
    obtain ⟨V, hV, hcV⟩ := (regularSpace ... : ...) 
```
RegularSpace in mathlib: `class RegularSpace (X) : Prop where regular : ∀ ⦃s : Set X⦄ ⦃a : X⦄, a ∉ closure s → Disjoint (𝓝ˢ s) (𝓝 a)`. That's the closed-set-separation version, less convenient. There's `exists_nhds_subset_closure_subset`? In `Mathlib/Topology/Separation.lean`: `RegularSpace` tfae includes `∀ U ∈ 𝓝 x, ∃ V ∈ 𝓝 x, closure V ⊆ U`? There's `regularSpace_iff_exists_isClosed`? Hmm. There IS a lemma: `RegularSpace.exists_nhds_subset_closure_subset`? Let me grep. Also instance `instRegularSpaceOfCompactT2 : RegularSpace X`.

Also — wait, do I even need LocallyCompactSpace for the SPHERE? The quotient-covering chain `isQuotientCoveringMap_quotientMk_of_properlyDiscontinuousSMul` requires [LocallyCompactSpace E] [T2Space E]. Yes for E = Sphere n.

Alternatively — since my Sphere n is compact and I've proved `sphereCompactSpace`, and mathlib might have the instance as `LocallyCompactSpace.of_isCompact...` hmm no. Let me just prove the one-line-per-condition instance:

```lean
instance locallyCompactSpace_of_compact_t2 (X : Type*) [TopologicalSpace X] [CompactSpace X] [T2Space X] : LocallyCompactSpace X
```

with the regular-space lemma. Let me find the exact mathlib lemma for "compact T2 regular" and the regular → closed-nhds-base statement.

Actually, simpler alternative avoiding RegularSpace entirely: compact T2 → every point has closed... hmm, need U open with compact closure inside n. Use: n ∈ 𝓝 x → ∃ U IsOpen, x ∈ U, U ⊆ n (nhds_basis). Then s = closure U: compact ✓; closure U ⊆ n? NO — U ⊆ n but closure U may exit n. That's why regularity is needed. OK use regularity.

mathlib names (Separation.lean): `instRegularSpaceOfCompactT2`? Let me grep quickly. Also grep `exists_nhds_subset_closure` / `closure_subset` / `tfae`.

Let me also confirm the ZMod 2 basics and check `Quotient.compactSpace` instance name, `continuous_quot_mk`, `isQuotientMap_quotient_mk'` etc. And check `MulAction.orbitRel` and `Quotient.mk` usage. And the `IsCancelSMul` class name & def. Batch grep now.
dsh: reasoning:
`IsCancelSMul` extends `IsLeftCancelSMul` — needs BOTH left and right cancellation. For ZMod 2 antipodal: left cancel: g • x = g • y → x = y: if g = 0 trivial; if g = 1: -x = -y → x = y ✓ (negation injective). Right cancel: g • x = g' • x → g = g': 1•x = 0•x → -x = x → x = 0 → contradiction on sphere ✓.

Regularity file: Topology/Separation/ — let me check its contents for RegularSpace API. Also check the `orbitRel` def at GroupTheory/GroupAction/Defs.lean:287 — `orbitRel : Setoid α` where the relation is "same orbit". And `MulAction.orbitRel` alias.

Let me look at Separation directory.
dsh: reasoning:
Line 132: `instance (priority := 100) [WeaklyLocallyCompactSpace X] [R1Space X] : RegularSpace X` and line 36 in LocallyCompact.lean: `instance (priority := 100) [CompactSpace X] : WeaklyLocallyCompactSpace X` and T2 → R1. So: compact + T2 → WeaklyLocallyCompact + R1 → RegularSpace ✓ — all by instances. Then `RegularSpace X` class at line 79: `regular : ∀ ⦃s : Set X⦄ ⦃a : X⦄, a ∉ closure s → Disjoint (𝓝ˢ s) (𝓝 a)`. 

To build LocallyCompactSpace from compact+T2:
```
local_compact_nhds x n hn : ∃ s ∈ 𝓝 x, s ⊆ n ∧ IsCompact s
```
Take n ∈ 𝓝 x → there's open U ⊆ n, x ∈ U (from `mem_nhds_iff`). Consider s = closure U? closure U might not ⊆ n. Use regularity with the closed set s' = nᶜ? x ∉ closure (nᶜ)? Not necessarily (x could be a boundary point of nᶜ... hmm x ∈ U open with U ⊆ n → x ∉ nᶜ, and nᶜ closed → closure nᶜ = nᶜ ∌ x ✓). So RegularSpace gives Disjoint (𝓝ˢ (nᶜ)) (𝓝 x) → ∃ V ∈ 𝓝 x with closure V ∩ nᶜ = ∅? Disjoint (𝓝ˢ (nᶜ)) (𝓝 x) means ∃ (V₁ : Set X), V₁ ∈ 𝓝ˢ (nᶜ) ∧ ∃ V₂ ∈ 𝓝 x, V₁ ∩ V₂ = ∅... that gives disjoint open neighborhoods of the closed set and the point — then closure V₂ ⊆ V₁ᶜ ⊆ ... hmm need closure V₂ ⊆ n. Standard: Disjoint (𝓝ˢ (nᶜ)) (𝓝 x) → closure V₂ ∩ nᶜ = ∅... `disjoint_nhdsSet_nhds` iff exists... There's a lemma: `disjoint_nhdsSet_nhds_iff`? or I can use `RegularSpace.tfae`... 

Hmm, there's an easier route: `RegularSpace X` + mathlib lemma `RegularSpace.exists_nhds_subset_closure_subset`? Let me grep for "closure_subset" in Regular.lean / Separation/Basic.lean. Alternatively use the classical characterization lemma: `regularSpace_iff_exists_open_closure`? Let me grep.
dsh: reasoning:
`regularSpace_tfae` (line ~86-112 in Regular.lean) — includes (from line 89): `∀ x : X, (𝓝 x).lift' closure ≤ 𝓝 x` — i.e., every neighborhood n of x contains closure U of some neighborhood U of x. So:

```lean
instance locallyCompactSpace_of_compact_t2 [CompactSpace X] [T2Space X] : LocallyCompactSpace X where
  local_compact_nhds x n hn := by
    have hreg : RegularSpace X := inferInstance  -- compact → weakly locally compact; T2 → R1 → regular
    have hlift := (regularSpace_tfae X).out 0 4 ⟨hreg⟩ x  -- hmm, tfae indices
    ...
```

The tfae list at lines 84-90: 
1. RegularSpace X
2. ∀ (s : Set X) x, x ∉ closure s → Disjoint (𝓝ˢ s) (𝓝 x)
3. ∀ (x : X) (s : Set X), Disjoint (𝓝ˢ s) (𝓝 x) ↔ x ∉ closure s
4. ∀ x : X, (𝓝 x).lift' closure ≤ 𝓝 x
5. ∀ x : X, (𝓝 x).lift' closure = 𝓝 x

Let me check the exact numbering in the file — need to read lines 79-115. Let me read the class and tfae carefully. Actually easier: use `closed_nhds_basis` or the lemma `lift'_nhds_closure (x) : (𝓝 x).lift' closure = 𝓝 x` (line 177 — a theorem in namespace RegularSpace? `lift'_nhds_closure`). So:

```lean
have h := RegularSpace.lift'_nhds_closure x  -- (𝓝 x).lift' closure = 𝓝 x
have hmem : closure `U` ∈ 𝓝 x ... 
```
From `(𝓝 x).lift' closure = 𝓝 x` and n ∈ 𝓝 x: n ∈ (𝓝 x).lift' closure → ∃ U ∈ 𝓝 x, closure U ⊆ n (`mem_lift'_sets`). Then s = closure U: closure U ∈ 𝓝 x ✓ (U ⊆ closure U, mem_of_superset), closure U ⊆ n ✓, IsCompact (closure U): closure U closed (isClosed_closure), X compact → `IsClosed.isCompact`? In compact space, closed sets are compact: `isClosed.isCompact` requires [CompactSpace X]: `IsCompact.isClosed` hmm — the theorem: `isClosed.isCompact [CompactSpace X] (hs : IsClosed s) : IsCompact s` — exists as `IsClosed.isCompact` in Compact.lean ✓. 

So the locally compact instance for compact T2 is ~10 lines. Where do I need it?
- `isQuotientCoveringMap_quotientMk_of_properlyDiscontinuousSMul` : [LocallyCompactSpace E] [T2Space E] — E = Sphere n ✓.
- Also `t2Space_of_properlyDiscontinuousSMul_of_t2Space` for the quotient — needs [T2Space T] [LocallyCompactSpace T] [ContinuousConstSMul] [ProperlyDiscontinuousSMul] — ✓.

Now the antipodal action details. Define:

```lean
namespace Antipodal
def smul (g : ZMod 2) (x : Sphere n) : Sphere n := if g = 1 then -x else x
```
Wait, ZMod 2: elements 0, 1. `(1 : ZMod 2)`. Action: `g • x := if g = 0 then x else -x`.

MulAction laws:
- `one_smul : (1 : ZMod 2) • x = x` — but note (1 : ZMod 2) is the multiplicative unit of the ring ZMod 2 — wait, ZMod 2 with `MulAction (ZMod 2) (Sphere n)` requires `[Monoid (ZMod 2)]` — ZMod 2 is a ring, monoid under multiplication ✓. (1 : ZMod 2) is the multiplicative identity. `1 • x := if 1 = 0 then ... else -x = -x ≠ x`. So one_smul FAILS with smul via "if g = 1 then -x". For MulAction, one_smul must be x. Hmm — with the multiplicative monoid of ZMod 2, we want (1 : ZMod 2) to be... In the additive group ZMod 2, 1 ≠ 0 is the nontrivial element. Using the multiplicative structure is confusing. Better: use `Multiplicative (ZMod 2)`? Then `MulAction (Multiplicative (ZMod 2)) (Sphere n)`: elements `ofAdd 0` (unit) and `ofAdd 1` (nontrivial). smul: `ofAdd 1 ↦ -x`, `ofAdd 0 ↦ x`. one_smul: (1 : Multiplicative (ZMod 2)) = ofAdd 0 ✓ → x. mul_smul: ofAdd a • ofAdd b • x = ofAdd (a + b) • x — by cases ✓.

Hmm, or just use the additive action `AddAction`/VAdd? The covering theorems have `@[to_additive]` versions: `isQuotientCoveringMap_quotientMk_of_properlyDiscontinuousSMul` has an additive twin via to_additive — but the quotient setoid `MulAction.orbitRel` vs `AddAction.orbitRel`. And `IsCancelSMul` vs `IsCancelVAdd`. Using `to_additive` versions adds friction. 

Cleaner: define the action of `Multiplicative (ZMod 2)`:

```lean
def smul : Multiplicative (ZMod 2) → Sphere n → Sphere n
| ⟨0⟩, x => x   -- hmm, Multiplicative.ofAdd 0
| ⟨1⟩, x => -x
```
Hmm, pattern matching on ZMod 2 needs `fin_cases`-like treatment; ZMod 2 elements are 0, 1, and any `a : ZMod 2` is either 0 or 1 (decidable, by fin_cases). Writing `if g = 1 then -x else x` with `g : Multiplicative (ZMod 2)`... `Multiplicative` is a def with `ofAdd`; `g = 1` means `g = Multiplicative.ofAdd 0`? no — in Multiplicative G, (1 : Multiplicative G) = ofAdd (0 : G). Nontrivial element: `Multiplicative.ofAdd 1`. So smul: `if g = Multiplicative.ofAdd 1 then -x else x`.

Hmm, honestly simpler: build a custom group structure? No. Another option: use `Equiv.Perm`? No.

Cleanest: use the group `ZMod 2` with the **additive** structure via `AddAction`... hmm the to_additive twins would need `IsAddQuotientCoveringMap` etc. Actually wait — mathlib's Quotient.lean has `@[to_additive]` all over, so `isAddQuotientCoveringMap_quotientMk_of_properlyDiscontinuousVAdd` etc. exist automatically. But then the final `isCoveringMap` — `IsAddQuotientCoveringMap.isCoveringMap`? Looking at Quotient.lean: `lemma IsQuotientCoveringMap.isCoveringMap (h : IsQuotientCoveringMap f G)` — with @[to_additive]? It has no to_additive attribute (the lemma at "namespace IsQuotientCoveringMap ... lemma isCoveringMap (h : IsQuotientCoveringMap f G)"). Hmm, that one isn't marked to_additive, so for the additive version I'd need... `IsAddQuotientCoveringMap.toMultiplicative` exists ✓ — convert additive to multiplicative and then use the multiplicative `isCoveringMap`! 

OK here's my decision: use `Multiplicative (ZMod 2)` everywhere. Instances:

```lean
def antipodalSMul (g : Multiplicative (ZMod 2)) (x : Sphere n) : Sphere n :=
  if g = Multiplicative.ofAdd 1 then -x else x

instance : SMul (Multiplicative (ZMod 2)) (Sphere n) := ⟨antipodalSMul n⟩
instance : MulAction (Multiplicative (ZMod 2)) (Sphere n) := by
  refine { smul := .., one_smul := ?_, mul_smul := ?_ }
  -- cases on elements
```

Hmm — but actually there's an even simpler path! `ZMod 2` has a canonical action... mathlib: does `Sphere n` already have an action of some {±1} group? `Units ℝ`? The group `ℤˣ` = {±1} (units of ℤ)! There's an existing natural action: `Units ℤ` acting on EuclideanSpace by scalar multiplication: `smul u x = u • x` where u : ℤˣ coerces to ℝ. ℤˣ = {1, -1}. Is there an existing `MulAction (ℤˣ) (EuclideanSpace ℝ ι)`? There's `Units.mulAction`? Hmm, `MulAction (αˣ)`? There's a general instance: `instance Units.instMulAction (M : Type) [Monoid M] : MulAction Mˣ M`? No — `MulAction (Mˣ) M`? I don't think there's a global instance for units acting on... there IS: `SMul (Mˣ) M`? Not standard. Hmm.

Alternatively `Equiv.Perm Bool` — the group of permutations of Bool ≅ ℤ/2... overkill.

Go with `Multiplicative (ZMod 2)`. But pattern matching: `a : ZMod 2` — to prove things I can use `fin_cases` — does `fin_cases` work on ZMod 2? ZMod 2 is finite; `fin_cases` works on elements of finite types via Fintype. Actually there's `interval_cases`? For ZMod 2, common approach: `revert g; fin_cases g` hmm — fin_cases needs the type to be a subtype of Fin n or similar... `fin_cases` works for any Fintype. For ZMod 2, I've seen `fin_cases g` used? Hmm, maybe `decide` on `g = 0 ∨ g = 1`: there's a lemma `ZMod.eq_zero_or_eq_one`? Hmm: `(g : ZMod 2)` satisfies `g = 0 ∨ g = 1`? There's `ZMod.val`... Let me think: mathlib has `ZMod.eq_zero_or_eq_one_of_nat_lt`? or `CharP` reasoning. There's `Finite` + `Fintype` + `fin_cases`? Actually there IS a convenient approach: `obtain ⟨k, hk⟩ := ZMod.intCast_zmod_eq_zero_iff...`. Hmm.

Simplest robust approach: for each proof obligation, do `by_cases hg : g = 0 <;> simp [antipodalSMul, hg]` — since if g ≠ 0 in ZMod 2 then g = 1? Is that provable? `(g : ZMod 2) ≠ 0 → g = 1`: use `ZMod.eq_iff_modEq_nat`? For ZMod 2: `g.val` ∈ {0, 1}: `ZMod.val_lt 2 g : g.val < 2` → g.val = 0 ∨ g.val = 1 → `ZMod.val_injective`/`ZMod.natCast_self`... There's a cleaner lemma: `ZMod.natCast_zmod_eq_zero_iff_dvd`. Alternatively `interval_cases h : g.val`? For ZMod I've seen: `fin_cases g` might literally work? Let me just test in Lean during development.

Actually, hmm — MUCH simpler: use `Bool`! The group `Bool` with Xor as multiplication is isomorphic to ℤ/2. Does mathlib have `Group Bool` with xor? There's `CommGroup Bool` (xor)? I believe mathlib has a `Bool.commGroup` instance (xor). Let me check. If yes: action of Bool on Sphere: `false ↦ id`, `true ↦ neg`. Cases on Bool are trivial (`cases g`). And the quotient `Sphere n / Bool-action` = RP^n. Finite ✓. That would make all instance proofs trivial. Let me check for Bool group instances.
dsh: reasoning:
No Bool group instance in mathlib. So ZMod 2 it is. For case analysis on ZMod 2, let me check what's available: `ZMod.eq_zero_or_eq_one`? grep. Actually, the simplest: since I define everything with `if g = 1 then -x else x` — wait, that makes one_smul fail. With `Multiplicative (ZMod 2)`:

```lean
def antipodalSmul (g : Multiplicative (ZMod 2)) (x : Sphere n) : Sphere n :=
  if g = 1 then x else -x
```
where (1 : Multiplicative (ZMod 2)) = ofAdd 0. one_smul: 1 • x = x ✓ by rfl. mul_smul: (g₁ * g₂) • x = g₁ • (g₂ • x): cases on g₁ g₂ via `fin_cases`. Nontrivial element m := Multiplicative.ofAdd 1: m • x = -x, m*m = 1, m•m•x = x, -(-x) = x ✓.

Case analysis on `g : Multiplicative (ZMod 2)`: I need `g = 1 ∨ g = Multiplicative.ofAdd 1` (and these are the only elements). For `a : ZMod 2`: `a = 0 ∨ a = 1`. How to prove? `ZMod.val a < 2` (val_lt), so val a = 0 or 1, and val is injective + natCast. There's probably a ready lemma `ZMod.eq_zero_or_eq_one`? Hmm, for ZMod 2 specifically there might be `ZMod.exists_eq_add_one_of_ne_zero`? Let me grep for "eq_zero_or_eq_one" in ZMod files.

Alternatively: use `fin_cases` on `(a.val)`? Hmm: `fin_cases h : a.val` — fin_cases works on Fin n elements. a.val : ℕ but we have a.val < 2, so `fin_cases` via `Fin.equivSubtype`? There's a slicker: `interval_cases a.val` with bound... or use `omega`. Honestly the cleanest: prove a helper lemma:

```lean
lemma zmod2_eq_zero_or_one (a : ZMod 2) : a = 0 ∨ a = 1 := by
  have hval : a.val < 2 := ZMod.val_lt a  -- hmm, val_lt? 
```
Let me check ZMod API: `ZMod.val_lt : (a : ZMod n) → a.val < n`? Or `ZMod.val_lt_of...`. Let me grep.
dsh: reasoning:
`ZMod.val_lt a : a.val < n` ✓. So `zmod2_eq_zero_or_one`:

```lean
lemma zmod2_eq_zero_or_one (a : ZMod 2) : a = 0 ∨ a = 1 := by
  have hlt := ZMod.val_lt a  -- a.val < 2
  have hval : a.val = 0 ∨ a.val = 1 := by omega
  rcases hval with h | h
  · left; exact ZMod.val_injective h? -- val_injective: a.val = b.val → a = b; a.val = 0 = (0 : ZMod 2).val
    exact (ZMod.val_injective ... )
```

`ZMod.val_injective : Function.Injective ZMod.val`? grep showed nothing for val_injective but there's surely an injectivity result: `ZMod.val_injective`? Let me not guess — during development I'll use `ZMod.natCast_zmod_val` and `val_eq_zero`... Actually simpler formulation: `a = (a.val : ZMod 2) = (0 or 1 as nat cast)`. Since a.val = 0 or 1: `a = (a.val : ZMod 2)` via `ZMod.natCast_zmod_val a` (or `(ZMod.natCast_zmod_val a).symm`), then rewrite hval, `norm_num`. There IS `ZMod.natCast_zmod_val : (a.val : ZMod n) = a`. I'll use that.

OK. I have enough API knowledge. Rather than more grepping, let me now look at SphereGluing.lean quickly (top part) to see the conventions (Disk, Sphere defs, norm lemmas), then write the new module. Let me view the head of SphereGluing.lean.
dsh: reasoning:
Good — I have the conventions. Now, let me write the plan and set up a todo list, then create the new file `CoveringLemma.lean`. Before writing, I need to check a few more mathlib API details by experimentation. Strategy: write the file incrementally with quick `lake env lean` checks.

Let me set up todos and write the first version of CoveringLemma.lean covering:
1. C1: `coveringMap_surjective_of_pathConnected`
2. `coveringMap_injective_of_simplyConnected`
3. `coveringOfSimplyConnectedIsHomeo` (replacing the Prop def — I'll REPLACE the def in MoiseBranch.lean with the proved theorem import; keep the ledger statement as an example/equality check or just move the doc).
4. Then AntipodalQuotient.lean with the action + covering + `sphericalSpaceFormRecognition`.
5. Update AxiomAudit.lean (add #print axioms for new declarations, and adjust the expected list in the audit script).
6. Update checkpoint.json + results.

Also the task says: "Prove a concrete nondegenerate example when necessary to test non-vacuity." The convex-disk example: `coveringOfSimplyConnectedIsHomeo` applied to id : Disk 3 → Disk 3 gives the trivial homeo — better: apply it to a nontrivial self-cover of the disk? Hmm, is there a nontrivial covering of the disk? NO — that's the point of the theorem (disk simply connected → only trivial covers). Non-vacuity witness: the antipodal action on Sphere 3 provides a covering p : S³ → RP³ whose domain is compact + path-connected — this witnesses that the *covering-map hypothesis* is realizable in the recognition-relevant setting (and that the simply-connectedness hypothesis is exactly what fails for RP³, i.e., the theorem genuinely discriminates). Plus the trivial witness p = id with the disk: `SimplyConnectedSpace (Disk 3)` instance (from convexity) — I'll prove `diskSimplyConnected` as a self-contained theorem (convexity → paths homotopic linearly), giving a genuine checked application of `coveringOfSimplyConnectedIsHomeo`:

```lean
theorem diskCoverIsHomeo : Nonempty (Disk 3 ≃ₜ Disk 3) :=
  coveringOfSimplyConnectedIsHomeo (p := (id : Disk 3 → Disk 3)) isCoveringMap_id (Function.bijective_id)
```
Hmm — `isCoveringMap_id`? Let me check existence. grep "isCoveringMap_id" mathlib. If absent, use `IsCoveringMap.of_discreteTopology`? Not applicable (not discrete). Alternative: build `IsCoveringMap id` from `IsEvenlyCovered.of_trivialization`: the identity trivialization with fiber Unit: `Trivialization` of id with fiber PUnit — mathlib `isCoveringMap_id`? Let me grep. If absent, I can construct: `fun x ↦ IsEvenlyCovered.of_trivialization ...` — needs a trivialization of id over baseSet = univ with fiber PUnit: `Bundle.Trivialization` with toFun... there might be an existing "trivialization of a homeomorphism" — `IsEvenlyCovered.of_fiber_homeomorph`? Or `IsFiberBundle.isCoveringMap` (line 419): `protected theorem IsFiberBundle.isCoveringMap {F} [TopologicalSpace F] [DiscreteTopology F] : IsCoveringMap (fun x : TotalSpace F? ...`. Hmm — "IsFiberBundle" is about the projection of a total space: `IsFiberBundle` is a predicate on... let me look at line 419 quickly during development. Simplest robust: use `IsEvenlyCovered.of_trivialization` with the identity trivialization:

```lean
lemma isCoveringMap_id (X : Type*) [TopologicalSpace X] : IsCoveringMap (id : X → X) := fun x ↦
  IsEvenlyCovered.of_trivialization (I := PUnit) (x := x) (t := { toFun := fun e => (x, e), ... })
```
Hmm constructing a Trivialization: source = univ, target = univ, baseSet = univ, with fiber PUnit: `toFun e = (⟨x, trivial⟩? ...`. Trivialization I f: an equivalence-ish structure: toFun : E → X × I with source = f ⁻¹' baseSet, projection = f on source. For f = id: toFun e := (e, ()) with PUnit fiber; invFun (x, ()) = x; source = univ = id⁻¹' univ ✓; target = univ ×ˢ univ; open maps etc. That's ~15 lines of structure fields but mechanical. 

Hmm wait — maybe mathlib already has `isCoveringMap_id`? Very likely. Let me grep. Also check `Homeomorph.isCoveringMap`? 

Actually — better idea for the nondegenerate witness: use my own homeomorphism! `doubleDiskQuotHomeoSphere 2 : DoubleDiskQuot 2 ≃ₜ Sphere 3`. Then `IsCoveringMap (e : DoubleDiskQuot 2 → Sphere 3)` via `IsCoveringMap.homeomorph_comp`: hmm — `homeomorph_comp (hf : IsCoveringMap f) (g : X ≃ₜ Y) : IsCoveringMap (g ∘ f)` — so `(isCoveringMap_id (DoubleDiskQuot 2)).homeomorph_comp e` gives `IsCoveringMap (e ∘ id : DoubleDiskQuot 2 → Sphere 3)` ✓ — but that still needs isCoveringMap_id for the domain. Circular again. BUT I can also directly: `IsEvenlyCovered.comp_homeomorph`: for each x, the evenly-covered neighborhood of id at x transported by the homeo e — `(isCoveringMap_id X x).homeomorph_comp e`. Still needs isCoveringMap_id at base X.

OK, check for `isCoveringMap_id` first. If mathlib lacks it, constructing it is a good small lemma to add anyway (worth having: `isCoveringMap_id`). Let me also double check `IsEvenlyCovered.of_trivialization` signature (line 119: `theorem of_trivialization [DiscreteTopology I] {x : X} {t : Trivialization I f} : x ∈ t.baseSet → IsEvenlyCovered f x (f ⁻¹' {x})` — takes membership proof).

And the fiber of id over x should be PUnit — wait: `IsEvenlyCovered f x I` has a fiber homeomorphism `f ⁻¹' {x} ≃ₜ I × ...`. With f = id: fiber = {x} ≃ PUnit ✓.

Let me now also verify: `Path.map`, `Path.cast` API, `PathConnectedSpace.joined`, `Joined` def — I'll discover during development.

One more consideration — the target statement shape. The current Prop def `coveringOfSimplyConnectedIsHomeo` takes `Function.Bijective p` as hypothesis. The C4 plan says: prove surjectivity from the rest? No — surjectivity needs `[PathConnectedSpace X]` or E nonempty + X connected. The plan: keep `Surjective p` as hypothesis (the spherical-space-form case has surjective quotient map). But the Prop-def version has Bijective. Hmm — decision: define the REAL theorem as:

```lean
theorem coveringOfSimplyConnectedIsHomeo [CompactSpace E] [PathConnectedSpace E] [T2Space X]
    [SimplyConnectedSpace X] (p : E → X) (hp : IsCoveringMap p) (hsurj : Function.Surjective p) :
    Nonempty (E ≃ₜ X)
```

This is strictly stronger than the old Prop def (Bijective → Surjective, so the old def follows from the new theorem). In MoiseBranch.lean, replace the `def coveringOfSimplyConnectedIsHomeo : Prop` with:
- import of CoveringLemma
- a doc note that the node is now discharged, plus `example` bridging to the old statement shape: the old statement `∀ p, IsCoveringMap p → Bijective p → Nonempty (E ≃ₜ X)` is implied.

But the AxiomAudit expects the name `coveringOfSimplyConnectedIsHomeo` — the new theorem has that name ✓. The audit also parses its axioms ✓.

Now, surjectivity-of-covering (C1) is then NOT needed inside the main theorem (surj is a hypothesis) — but still worth proving as a named lemma + used in the antipodal application where surjectivity comes from Quot.mk_surjective. Good.

Let me also decide: keep the Prop defs for `vanKampenSimplyConnectedCover`, `simplexBoundaryHomeoSphere`, `sphereMinusBallHomeoDisk`, `moiseWeakenedCW`, `LocallyEuclideanOfDimension` as-is in MoiseBranch.lean (statement-only ledger — these remain missing; that's fine and honest).

Now — the MoiseBranch edit: replace lines 40-55 (the def coveringOfSimplyConnectedIsHomeo : Prop) with a marker that it's proved. Actually careful: the audit's expected list includes "coveringOfSimplyConnectedIsHomeo" — if I define the theorem in CoveringLemma.lean and `import` CoveringLemma into AxiomAudit (or MoiseBranch → transitively), the name exists in the namespace ✓.

Plan the module structure:

**CoveringLemma.lean**:
- imports: `Mathlib` (consistent with SphereGluing; heavy but fine — though for build time, targeted imports are better: `Mathlib.Topology.Homotopy.Lifting` (public imports: Homotopy.Basic, Path, PathConnected...), `Mathlib.AlgebraicTopology.FundamentalGroupoid.SimplyConnected`, `Mathlib.Topology.Covering.Basic`, `Mathlib.Topology.Covering.Quotient`, `Poincare.D12.TriangulationTopology.SphereGluing` for Disk/Sphere. I'll use targeted imports to keep it clean.

Contents:
1. `coveringMap_surjective_of_pathConnected {E X} [TopologicalSpace E] [TopologicalSpace X] [PathConnectedSpace X] [Nonempty E] {p : E → X} (hp : IsCoveringMap p) : Function.Surjective p` (C1).
2. `coveringMap_injective_of_simplyConnected {E X} ... [SimplyConnectedSpace X] {p : E → X} (hp : IsCoveringMap p) {e₀ e₁ : E} (hjoin : Joined e₀ e₁) (hpe : p e₀ = p e₁) : e₀ = e₁`.
3. `coveringOfSimplyConnectedIsHomeo` (C4).
4. `isCoveringMap_id` maybe.
5. `simplyConnectedSpace_of_convex` (self-contained linear homotopy: for convex s in a real TVS with ContinuousAdd/ContinuousSMul, SimplyConnectedSpace s). Mathlib has `Convex.contractibleSpace` + `SimplyConnectedSpace.ofContractible` — but contractible requires the star-convex machinery with `ContinuousAdd E`+`ContinuousSMul ℝ E` — the disk is convex in EuclideanSpace ✓. So: `diskSimplyConnected (n) : SimplyConnectedSpace (Disk n) := (convex_closedBall.contractibleSpace (diskNonempty n)).simplyConnectedSpace`? `SimplyConnectedSpace.ofContractible` is an instance — `inferInstance`. So one line: `instance diskSimplyConnected (n : ℕ) : SimplyConnectedSpace (Disk n) := inferInstance` — via `Convex.contractibleSpace (convex_closedBall ...)` + `SimplyConnectedSpace.ofContractible`. Also `PathConnectedSpace (Disk n)` — instance? There's `Convex.pathConnectedSpace`? I saw `IsTopologicalAddGroup.pathConnectedSpace` in Convex/PathConnected.lean line 105 and stdSimplex line 256 `instance [Nonempty ι] : PathConnectedSpace (stdSimplex ℝ ι)`. For closedBall convex — hmm, there may be an instance `PathConnectedSpace` for convex sets in a TVS? Let me check Convex/PathConnected.lean for `Convex.pathConnectedSpace`.

6. Checked downstream use: 
```lean
theorem diskSelfCoverIsHomeo : Nonempty (Disk 3 ≃ₜ Disk 3) :=
  coveringOfSimplyConnectedIsHomeo (E := Disk 3) (X := Disk 3) (p := id)
    (by infer_instance? isCoveringMap_id) (by intro x; exact ⟨x, rfl⟩)
```
Hmm — wait, this is trivial (id homeo exists trivially). A slightly better nondegenerate check: ANY covering p : E → Disk 3 with E compact path-connected is a homeo — i.e., the theorem at n=3 with the *hypothesis* instantiated at p = id proves the pipeline. The real non-vacuity statement is the recognition theorem below with the antipodal example as witness of the hypothesis class.

Actually, a nicer checked use: **the antipodal covering S³ → RP³ shows the hypothesis "SimplyConnectedSpace X" is genuinely needed**: I can't easily prove RP³ not simply connected (needs π₁). Skip. Instead the honest deliverable: recognition theorem + note that its hypothesis is satisfied exactly when π₁(M)=1 (M homotopy sphere). Non-vacuity of *hypotheses* of the covering lemma: p = id on Disk 3 (real space, all hypotheses hold, conclusion = trivial homeo — shows the theorem is not vacuous/trivially inconsistent). And non-vacuity of the antipodal machinery: RP^n for all n, covering map instance — real.

Also prove `pathConnectedSpace` of Sphere n? For `coveringOfSimplyConnectedIsHomeo` in `sphericalSpaceFormRecognition` with E = Sphere 3, need [PathConnectedSpace (Sphere 3)]. Must prove. Also for the antipodal quotient: X = RP³ path connected via `Quotient.instPathConnectedSpace` from E ✓. And for `coveringMap_surjective_of_pathConnected` application — not needed (Quot.mk surjective ✓ `Quot.mk_surjective`).

Hmm, wait — for `sphericalSpaceFormRecognition`, do I need PathConnectedSpace (Sphere 3) or can I get injectivity differently? The injectivity lemma needs `Joined e₀ e₁` for fibers; the main theorem needs [PathConnectedSpace E] to supply it. For E = Sphere 3. So yes, need `spherePathConnected (n) [n ≥ 1]`. This is the segment-normalization argument. Let me think about how to formalize it cleanly:

For x, y : Sphere n (i.e., ‖x‖ = ‖y‖ = 1, x, y : E := EuclideanSpace ℝ (Fin (n+1))):
- If y ≠ -x: path γ(t) = ((1-t)•x + t•y) / ‖(1-t)•x + t•y‖. Need: denominator ≠ 0 for all t ∈ I, values on sphere, continuity, endpoints.
  - Denominator ≠ 0: (1-t)x + ty = 0 → (1-t)x = -t y → norms: |1-t| = t (both ≥ 0: 1-t ≥ 0, t ≥ 0) → 1 - t = t → t = 1/2 → x = -y ✓ contradiction with y ≠ -x. Norm argument: ‖(1-t)x‖ = ‖ty‖ → |1-t|·‖x‖ = |t|·‖y‖. In Lean with t : I: 0 ≤ t, t ≤ 1. |1 - t| = 1 - t, |t| = t. So 1 - t = t → t = 1/2 → then (1-t)x = -ty → x = -y. All doable but some real arithmetic. Alternative cleaner: use the metric-geometry fact ‖(1-t)x + t y‖ = ‖x + t(y-x)‖ ≥ ‖x‖ - t‖y - x‖ ≥ 1 - ‖y - x‖... no, that's ≥ 0 not > 0. Hmm: need strict positivity. ‖(1-t)x + t y‖ ≥ |‖(1-t)x‖ - ‖ty‖| = |(1-t) - t| = |1 - 2t| — zero iff t = 1/2. Still need the extra argument at t = 1/2: if ‖(1/2)x + (1/2)y‖ = 0 then x = -y. So: denominator zero → triangle inequality must be an equality case... Simpler: directly: (1-t)x + ty = 0 → y = -((1-t)/t) x for t ≠ 0 → ‖y‖ = (1-t)/t = 1 → t = 1/2 → y = -x. Need t = 0 case separate: t = 0 → x = 0 contradiction (‖x‖ = 1). OK, manageable.
  
  Alternatively — much slicker — avoid the case split entirely by using the standard trick: any two points of Sⁿ can be joined via the segment in the *ambient space* followed by radial projection — but radial projection R(x) = x/‖x‖ is defined on ℝⁿ⁺¹ \ {0}; the segment from x to y misses 0 unless y = -x. The case split is unavoidable for antipodal pairs, where the segment passes through 0. Standard fix: for y = -x, go x → z → y for some z ≠ ±x. Need z: use basis vector e₀ unless x = ±e₀, then e₁ (exists for n ≥ 1). This is fiddly but mechanical.

  Alternative approach avoiding paths: use mathlib! `Sphere n` is homeomorphic to... hmm. What about `PathConnectedSpace` via the cone/suspension homeos I proved? `suspQuotHomeoSphere` shows Sphere (n+1) ≃ Quot (suspRel n); the suspension quotient is a quotient of Sphere n × Icc... `Quotient.instPathConnectedSpace` needs [PathConnectedSpace (Sphere n × Icc(-1,1))] — needs path-connected Sphere n — circular for the induction, but base case n=0: Sphere 0 = {±1} two points — NOT path connected! Path-connectedness starts at n=1 (circle). Induction via suspension would need n=1 base: S¹ path connected — circle: two arcs... hmm, that's provable via angle parametrization, also fiddly.

  Hmm what about mathlib having it? Let me grep for "PathConnectedSpace" near "sphere" once more — I only checked TopCat/Spheres.lean (doesn't exist) and MetricSpace/Sphere.lean (doesn't exist?). Let me find where TopCat.sphere is defined (Topology/Category/TopCat/Sphere.lean) and check for path connected there. Actually let me grep the whole mathlib for `Sphere.*[Pp]athConnected|sphere.*pathConnected|pathConnected.*sphere`.

Let me also reconsider: does `sphericalSpaceFormRecognition` really need [PathConnectedSpace (Sphere 3)]? Alternative formulation avoiding it: require p : E → X with E = Sphere 3 but state the *injectivity* variant: since X simply connected, p is injective on each path component; if p is surjective and X simply connected then p injective GLOBALLY? No — need E path-connected or at least fibers within one component. Standard statement needs it. Alternatively use the connected+locally-path-connected formulation: E = S³ is connected + locally path connected, fibers connected... The classical theorem: "covering map p : E → X with E connected and X simply connected (and p surjective) → homeo" — proof uses the classification: p is injective since if two points in a fiber, path between them (E path connected needed!)... no wait, E connected alone: the deck group... simplest classical proof still needs a path in E between the two points. For S³ specifically, path-connectedness is trivial mathematically. So just prove spherePathConnected.

OK here's another thought — do I even need `sphericalSpaceFormRecognition` in THIS invocation? The critical path per checkpoint: C1, C2(moot), C4. The antipodal covering module is the "geometric-realization/covering statement with actual topological spaces" (a strong deliverable aligning with the objective: "a covering lemma needed by sphere recognition, with actual topological spaces"). The recognition corollary needs sphere path-connectedness. I'll attempt it — sphere path connectedness via the direct segment argument, carefully. If it eats too much time, I deliver the quotient covering + general lemma + disk application and leave `sphericalSpaceFormRecognition` (or rather `spherePathConnected`) as the exact remaining statement. But I think the segment argument is very doable in ~150 lines. Let me first check whether mathlib has anything.
dsh: reasoning:
No `isCoveringMap_id` in Basic.lean. Let me check `Analysis/Normed/Module/Connected.lean` for sphere connectedness.
dsh: reasoning:
`isPathConnected_sphere (h : 1 < Module.rank ℝ E) (x : E) {r : ℝ} (hr : 0 ≤ r) : IsPathConnected (sphere x r)` — for dimension > 1! For EuclideanSpace ℝ (Fin (n+1)): rank = n+1; need 1 < n+1, i.e., 0 < n... wait "dimension > 1" means rank > 1, i.e., n+1 > 1, i.e., n > 0. So for n ≥ 1 (sphere of positive dimension in ambient dim ≥ 2): `isPathConnected_sphere (x := 0) (r := 1)`. 

But CAREFUL: `Module.rank ℝ E` for E = EuclideanSpace ℝ (Fin (n+1)) = n+1. Need a proof of `1 < Module.rank ℝ (EuclideanSpace ℝ (Fin (n+1)))` for n ≥ 1: `Module.finrank` = n+1 (finrank EuclideanSpace = Fintype.card). There's `Module.finrank_euclideanSpace` / `finrank_pi`? `rank = finrank` for finite-dim: `Module.rank_eq_finrank`? Then `1 < Fintype.card (Fin (n+1))`... hmm, finrank ℝ (EuclideanSpace ℝ ι) = Fintype.card ι — mathlib: `finrank_euclideanSpace`. Then for n ≥ 1: card (Fin (n+1)) = n+1 ≥ 2 → 1 < n+1 ✓ by omega.

And from `IsPathConnected s` I need `PathConnectedSpace (Sphere n)` (the typeclass on the subtype). `IsPathConnected s : IsPreconnected s ∧ ∀ x ∈ s, ∀ y ∈ s, JoinedIn s x y`. There should be an instance: `PathConnectedSpace s` from IsPathConnected — mathlib: `IsPathConnected.pathConnectedSpace`? Hmm — there's `instPathConnectedSpace_of_isPathConnected`? Let me check: in PathConnected.lean there's `instance IsPathConnected.pathConnectedSpace`? or a def `PathConnectedSpace.mk'`. I recall `pathConnectedSpace_iff_isPathConnected`. I'll find the right name during development.

Note: `isPathConnected_sphere` requires hr : 0 ≤ r (r = 1 ✓) and rank > 1. The function f in the proof maps {0}ᶜ → sphere by radial normalization — i.e., Sⁿ = image of ℝⁿ⁺¹\{0} under x ↦ x/‖x‖; path connectedness of {0}ᶜ for dim > 1... anyway it's proven in mathlib.

So sphere path-connectedness for n ≥ 1 is FREE from mathlib. And for the circle (ambient dim 2, rank 2 > 1 ✓ — n=1: rank = 2 > 1 ✓ works!). For S⁰ (n=0): rank 1, excluded — correct (S⁰ = two points).

So `spherePathConnectedSpace` needs n ≥ 1 hmm wait: isPathConnected_sphere for rank > 1: n+1 > 1 ⟺ n ≥ 1 ✓. So:

```lean
instance spherePathConnectedSpace {n : ℕ} [NeZero n] : PathConnectedSpace (Sphere n) := ...
```
hmm [NeZero n] gives n ≠ 0; need 0 < n. NeZero n : n ≠ 0 ⟺ 0 < n for ℕ ✓.

Wait — but does Sphere n have the right topology/instances to apply? `isPathConnected_sphere` concludes `IsPathConnected (sphere x r)` as a SET of E. Then PathConnectedSpace (Sphere n) via the subtype instance from IsPathConnected — need to check the exact mechanism. There might be an instance `instPathConnectedSpace : [..]` — let me grep for "IsPathConnected" → "PathConnectedSpace" conversions in PathConnected.lean during dev.

Now for E = Sphere 3 in the recognition theorem: n = 3, [NeZero 3]? NeZero 3 requires... `NeZero 3` instance: `NeZero (OfNat.ofNat 3)` — mathlib has `NeZero` for nonzero numerals? `NeZero 3` — there's `NeZero.of_pos`/instance `NeZero (n+1)`. For numeral 3: `norm_num` can synthesize `NeZero 3`? I think there's an instance for `NeZero (OfNat.ofNat (Nat.succ _))`... Actually mathlib has: `instance (priority := 100) NeZero.of_neZeroNat : NeZero (OfNat.ofNat n)` when 0 < n? Hmm — safer: prove the theorem with an explicit hypothesis `(hn : 0 < n)` and construct instances locally:

```lean
theorem spherePathConnectedSpace_of_pos {n : ℕ} (hn : 0 < n) : PathConnectedSpace (Sphere n) := by
  have hrank : 1 < Module.rank ℝ (EuclideanSpace ℝ (Fin (n + 1))) := by
    -- rank = finrank = card = n+1 > 1
    rw [Module.rank_eq_finrank? ...] hmm
```
Names needed: `Module.finrank_euclideanSpace : finrank ℝ (EuclideanSpace ℝ ι) = Fintype.card ι`? and `Module.rank` vs `Module.finrank` for finite-dim: `Module.rank_eq_finrank`? Hmm, actually since EuclideanSpace = WithLp 2 (ι → ℝ), finrank is `finrank_pi`? There's `finrank_euclideanSpace` — let me grep during dev.

Then `isPathConnected_sphere hrank (0 : E) (by norm_num : 0 ≤ (1:ℝ))` gives `IsPathConnected (sphere 0 1)`; then convert to PathConnectedSpace.

OK. Also for the antipodal module I need `ContinuousConstSMul (Multiplicative (ZMod 2)) (Sphere n)`: each g: continuous — cases g: ofAdd 0 → id continuous ✓; ofAdd 1 → negation on the sphere: `continuous_neg` (EuclideanSpace is a topological group ✓; on the subtype: `(continuous_neg.comp continuous_subtype_val)` ... need the map x ↦ -x on Sphere n continuous: `Continuous fun x : Sphere n ↦ -(x : E)` — from continuous_neg.comp continuous_subtype_val → continuous as map into Sphere: subtype continuity `Continuous.subtype_mk`. And `MulAction` instance by cases.

`IsCancelSMul`: left: g • x = g • y → x = y (cases g; ofAdd 1: -x = -y → x = y via neg_injective ✓); right: g • x = g' • x → g = g' (cases g, g' via zmod2_eq_zero_or_one; the bad case: (ofAdd 1) • x = (ofAdd 0) • x → -x = x → x = 0 → ‖x‖ = 0 but x ∈ sphere → 1 = 0 contradiction).

Then: 
```lean
noncomputable def rpCovering (n : ℕ) : IsCoveringMap (Quotient.mk (MulAction.orbitRel (Multiplicative (ZMod 2)) (Sphere n))) :=
  (isQuotientCoveringMap_quotientMk_of_properlyDiscontinuousSMul (G := Multiplicative (ZMod 2)) (E := Sphere n)).isCoveringMap
```
needs [LocallyCompactSpace (Sphere n)] — my compact+T2 instance `locallyCompactSpace_of_compact_t2`. ✓

Also `RP n` abbrev: `Quotient (MulAction.orbitRel (Multiplicative (ZMod 2)) (Sphere n))` with `CompactSpace` (Quotient.compactSpace ✓), `T2Space` (via `t2Space_of_properlyDiscontinuousSMul_of_t2Space` — instance, needs ContinuousConstSMul + ProperlyDiscontinuousSMul ✓ both instances), `PathConnectedSpace` (via Quotient.instPathConnectedSpace + spherePathConnectedSpace for n ≥ 1), nonempty (Quotient nonempty from nonempty E ✓).

And the recognition theorem:

```lean
theorem sphericalSpaceForm_recognition (G : Type*) [Group G] [Finite G] [MulAction G (Sphere 3)]
    [ContinuousConstSMul G (Sphere 3)] [IsCancelSMul G (Sphere 3)]
    (hsc : SimplyConnectedSpace (Quotient (MulAction.orbitRel G (Sphere 3)))) :
    Nonempty (Quotient (MulAction.orbitRel G (Sphere 3)) ≃ₜ Sphere 3) := by
  let p : Sphere 3 → Quotient (MulAction.orbitRel G (Sphere 3)) := Quotient.mk (MulAction.orbitRel G (Sphere 3))
  have hp : IsCoveringMap p := (isQuotientCoveringMap_quotientMk_of_properlyDiscontinuousSMul (G := G) (E := Sphere 3)).isCoveringMap
  exact (coveringOfSimplyConnectedIsHomeo p hp Quot.mk_surjective).map fun e => e.symm
```
Wait — coveringOfSimplyConnectedIsHomeo gives `Nonempty (E ≃ₜ X)` with E = Sphere 3 (domain of p), X = quotient ✓. Then `.map (·.symm)` — Nonempty.map. ✓ 

Note the direction: the homeo is Sphere 3 ≃ₜ quotient; the recognition wants quotient ≃ₜ Sphere 3 — symm ✓.

`MulAction.orbitRel G (Sphere 3)` — the orbitRel for the action. `Quotient (MulAction.orbitRel G (Sphere 3))` — with `letI := MulAction.orbitRel G (Sphere 3)` maybe. The namespace `MulAction.orbitRel` — from GroupTheory/GroupAction/Defs.lean ✓.

Non-vacuity witness of the action hypotheses: the antipodal action (G = Multiplicative (ZMod 2)) on Sphere 3: instances [MulAction][ContinuousConstSMul][IsCancelSMul][Finite] all ✓ — so the theorem's *hypotheses* (excluding hsc) are satisfiable — express as an `example : ContinuousConstSMul (Multiplicative (ZMod 2)) (Sphere 3) := inferInstance`-style witness block, plus the honest note that for this G, hsc FAILS (RP³ not simply connected — needs van Kampen/π₁(S¹), recorded as a blocker for node 6) — hence the recognition theorem does not wrongly claim RP³ ≅ S³.

Now the injectivity proof details (the heart). Let me now write the code. First, check some exact API names I need by searching:
- `Path.map` def & its simp lemmas
- `Path.cast`
- `Path.refl`
- `Joined`
- `PathConnectedSpace.joined`
- `SimplyConnectedSpace.paths_homotopic`
- `ContinuousMap.HomotopicRel` ↔ path homotopy
- `Homeomorph.homeoOfEquivCompactToT2` exact statement
- `IsCoveringMap.continuous`
- `IsEvenlyCovered.of_trivialization` signature
- `Trivialization` structure fields for isCoveringMap_id

Let me read Homeomorph/Lemmas.lean:474 and the Trivialization basics, plus check `Path.map`.
dsh: reasoning:
`homeoOfEquivCompactToT2` is in namespace `Continuous` — so `Continuous.homeoOfEquivCompactToT2` ✓ (as my checkpoint said).

`Joined x y := Nonempty (Path x y)` with `Joined.somePath`. `Path.map` — grep showed Homotopy.map at 226; Path.map is elsewhere in the file (before line 200 probably as `def map (γ : Path x₀ x₁) {Y} [TopologicalSpace Y] (f : C(X, Y)) : Path (f x₀) (f x₁)`). Fine.

Path.cast — there's `Path.cast (γ : Path x₀ x₁) (h₀ : x₀ = x₀') (h₁ : x₁ = x₁') : Path x₀' x₁'` — grep line 136 shows Homotopy.cast; Path.cast itself: let me find. Also Path.refl at line 345 is for Quotient; the Path version is `Path.refl (x : X) : Path x x`.

Now, let me get concrete and start writing. I'll create the new file with the core C1+C4 content first and iterate with `lake env lean`. Set up todos first.
dsh: reasoning:
`Path` is defined in `Mathlib/Topology/Path.lean` (extends C(I,X)), and `Mathlib/Topology/Homotopy/Path.lean` is the homotopy machinery (namespace Path). Good.

Now let me write the first version of CoveringLemma.lean. I'll be careful about:

1. `IsCoveringMap.surjective_of_pathConnected` — actually let me name flat in my namespace: `coveringMap_surjective_of_pathConnected`.

Wait, I realize I should double check `comp_subtypeVal_pathComponent_surjective`'s full statement: `theorem comp_subtypeVal_pathComponent_surjective (e₀ : E) [PathConnectedSpace X] : Surjective (fun x : pathComponent e₀ ↦ p x)`. In the namespace IsCoveringMap, with cov included. So:

```lean
theorem coveringMap_surjective_of_pathConnected {E X : Type*} [TopologicalSpace E]
    [TopologicalSpace X] [PathConnectedSpace X] [Nonempty E] {p : E → X}
    (hp : IsCoveringMap p) : Function.Surjective p := by
  intro x
  let e₀ : E := Classical.choice ‹Nonempty E›
  obtain ⟨y, hy⟩ := hp.comp_subtypeVal_pathComponent_surjective e₀ x
  exact ⟨y, hy⟩
```
`y : pathComponent e₀` coerces to E ✓.

2. Injectivity:

```lean
theorem coveringMap_injective_of_simplyConnected {E X : Type*} [TopologicalSpace E]
    [TopologicalSpace X] [SimplyConnectedSpace X] {p : E → X} (hp : IsCoveringMap p)
    {e₀ e₁ : E} (hjoin : Joined e₀ e₁) (hpe : p e₀ = p e₁) : e₀ = e₁ := by
  let Γ : Path e₀ e₁ := hjoin.somePath
  let γ : Path (p e₀) (p e₁) := Γ.map ⟨p, hp.continuous⟩
  let γ₀ : Path (p e₀) (p e₀) := γ.cast rfl hpe.symm
  have hhom : γ₀.Homotopic (Path.refl (p e₀)) :=
    SimplyConnectedSpace.paths_homotopic γ₀ (Path.refl (p e₀))
  have hEnd : hp.liftPath γ₀ e₀ γ₀.source 1 = e₀ := by
    have := hp.liftPath_apply_one_eq_of_homotopicRel hhom e₀ γ₀.source (Path.refl (p e₀)).source
    ...
```

Hmm wait — `liftPath_apply_one_eq_of_homotopicRel` expects `h : γ₀.HomotopicRel γ₁ {0,1}` with `γ₀ γ₁ : C(I, X)`. `hhom : γ₀.Homotopic (Path.refl (p e₀))` — `Path.Homotopic p₀ p₁ = Nonempty (p₀.Homotopy p₁)` and `Path.Homotopy p₀ p₁ = ContinuousMap.HomotopyRel p₀.toContinuousMap p₁.toContinuousMap {0,1}` (abbrev!). So `hhom` IS `Nonempty (ContinuousMap.HomotopyRel γ₀.toContinuousMap (Path.refl (p e₀)).toContinuousMap {0,1})`. And the expected `(γ₀ : C(I,X)).HomotopicRel ((Path.refl (p e₀)) : C(I,X)) {0,1}` = `Nonempty (ContinuousMap.HomotopyRel (γ₀ : C(I,X)) ((Path.refl (p e₀)) : C(I,X)) {0,1})`. The coercion `Path → C(I,X)`: there's an instance `Coe (Path x₀ x₁) C(I, X)` defined as `⟨fun p ↦ p.toContinuousMap⟩`? If it's `p.toContinuousMap` definitionally then `exact hhom` works. Path extends C(I,X) so toContinuousMap is the parent projection. The Coe instance — let me check its definition. If not defeq, use `hhom.map`? `Nonempty.map`. I'll handle in code: `change (γ₀ : C(I, X)).HomotopicRel ((Path.refl (p e₀)) : C(I, X)) {0, 1} at hhom` maybe.

Actually — simpler to avoid the coercion noise: state hrel directly using `toContinuousMap`:
```lean
have hhom : ContinuousMap.HomotopicRel γ₀.toContinuousMap (Path.refl (p e₀)).toContinuousMap {0, 1} := hhom
```
and pass `γ₀.toContinuousMap` explicitly... but `liftPath_apply_one_eq_of_homotopicRel`'s γ₀ γ₁ are `C(I, X)` — pass `γ₀.toContinuousMap` and `(Path.refl (p e₀)).toContinuousMap`. But hmm, `hp.liftPath γ₀ e₀ ..` — liftPath's γ : C(I, X) — the Path coerces. And I need the equality `hp.liftPath γ e₀ γ.source = hp.liftPath γ₀ e₀ γ₀.source` where γ γ₀ are Paths (coerced). Let me write it all explicitly with toContinuousMap where needed and let elaboration do the rest; I'll iterate.

The endpoint comparison: `liftPath_apply_one_eq_of_homotopicRel hrel e₀ h₀ h₁ : hp.liftPath γ₀.toContinuousMap e₀ h₀ 1 = hp.liftPath (Path.refl (p e₀)).toContinuousMap e₀ h₁ 1`. Then the RHS: `hp.liftPath_const (x := p e₀) (hpe := rfl)` — statement: `liftPath_const {x : X} (hpe : x = p e) : cov.liftPath (.const I x) e hpe = .const I e`. `(.const I (p e₀))` vs `(Path.refl (p e₀)).toContinuousMap` — `Path.refl x : Path x x := { toFun := fun _ => x, continuous_toFun := continuous_const, source' := rfl, target' := rfl }`? If Path.refl is defined as a constant function then `(Path.refl (p e₀)).toContinuousMap = .const I (p e₀)` should be `rfl` after `ext` — I'll use `change`/`convert`.

Alternatively, use `liftPath_const` directly by making hrel compare γ₀ with `.const I (p e₀)`:
`hrel : γ₀.HomotopicRel (.const I (p e₀)) {0,1}` — hmm, does `paths_homotopic γ₀ (Path.refl (p e₀))` give that? The HomotopicRel between γ₀.toContinuousMap and .const — same thing modulo defeq of refl.

Then: `cov.liftPath γ₀ e₀ h₀ 1 = cov.liftPath (.const I (p e₀)) e₀ h₁ 1 = e₀` via `congr_fun (cov.liftPath_const (x := p e₀) (hpe := rfl)) 1`? `.const I e₀ 1 = e₀` — `ContinuousMap.const` applied — `const_apply`? `.const I e₀ 1 = e₀` by rfl? `ContinuousMap.const X y x = y` is rfl? const is `fun _ ↦ y` — so `(.const I e₀) 1 = e₀` is rfl? ContinuousMap.const : `def const (X : Type*) (Y : Type*) [TopologicalSpace Y] (y : Y) : C(X, Y) := ⟨fun _ => y, continuous_const⟩` — applying coerces to the function: `((.const I e₀) : I → E) 1 = e₀` by rfl ✓ (simp).

3. Connecting Γ and liftPath γ:
```lean
  have hΓeq : (Γ : C(I, E)) = hp.liftPath γ e₀ γ.source := by
    rw [hp.eq_liftPath_iff']
    refine ⟨?_, rfl⟩
    -- p ∘ Γ = γ
    ext t
    rfl? -- γ := Γ.map ⟨p, hp.continuous⟩; Path.map f = ⟨f ∘ γ, ...⟩; so as C(I,X), (γ : C(I,X)) = p ∘ (Γ : C(I,E)) — by ext t; rfl? need γ's function = p ∘ Γ.
```
`Path.map (γ : Path x₀ x₁) {Y} (f : C(X, Y)) : Path (f x₀) (f x₁)` — defined as `⟨f ∘ γ, by fun_prop, γ.source ..., γ.target ..⟩`? So `(γ : C(I,X))` as a function = `f ∘ γ` where f = ⟨p, hp.continuous⟩. `f ∘ γ` as function = p ∘ (Γ : C(I,E) → ...) — since (f ∘ γ) t = f (γ t) = p (Γ t) ✓ by rfl (f is ⟨p, ..⟩, applying = p). So `ext t; rfl` should close it if the coe Path→C(I,X) is toContinuousMap and the function coercion of C(I,X) to I→X is the underlying function. Might need `simp [γ, Path.map]` — iterate.

  `eq_liftPath_iff' {Γ : C(I, E)} : Γ = cov.liftPath γ e γ_0 ↔ p ∘ Γ = γ ∧ Γ 0 = e` — here Γ is the C(I,E) argument; I pass `Γ : Path e₀ e₁` — need coercion to C(I,E): `show (Γ : C(I,E)) = hp.liftPath γ e₀ γ.source`. And in the iff', `γ : C(I, X)` — my γ : Path — coercion again ✓.

  γ.source : γ 0 = p e₀ — Path.source field: `source' : γ 0 = x₀`? Path extends C(I,X) with fields source' target'. So `γ.source : γ 0 = p e₀` hmm — x₀ = p e₀ where γ : Path (p e₀) (p e₁) ✓.

4. Then:
```lean
  have hEndγ : hp.liftPath γ e₀ γ.source 1 = e₁ := by
    rw [← hΓeq]
    exact Γ.target
```
hmm: hΓeq : (Γ : C(I,E)) = hp.liftPath γ e₀ γ.source. `congr_fun hΓeq 1 : Γ 1 = hp.liftPath γ e₀ γ.source 1`. Γ.target : Γ 1 = e₁. So `hp.liftPath γ e₀ γ.source 1 = e₁` = `(congr_fun hΓeq 1).symm.trans Γ.target`. ✓

5. Relating γ₀ and γ lifts:
```lean
  have hsameness : hp.liftPath γ e₀ γ.source = hp.liftPath γ₀ e₀ γ₀.source := by
    rw [hp.eq_liftPath_iff']  -- for Γ = hp.liftPath γ e₀ γ.source as candidate lift of γ₀
    refine ⟨?_, hp.liftPath_zero γ e₀ γ.source⟩
    -- p ∘ hp.liftPath γ e₀ γ.source = γ₀
    rw [hp.liftPath_lifts γ e₀ γ.source]
    -- γ = γ₀ as C(I,X): cast preserves the function
    ext t; rfl -- γ₀ := γ.cast rfl hpe.symm; cast def: need check
```
`Path.cast`: `def cast (γ : Path x₀ x₁) (h₀ : x₀ = x₀') (h₁ : x₁ = x₁') : Path x₀' x₁' where toFun := γ ...`? Let me check the Path.cast def in Topology/Path.lean. If `cast` redefines toContinuousMap via dependent rewrite of the endpoints only, the underlying function is the same, so `ext t; rfl` works if `(γ.cast h h' : C(I,X))` reduces to `(γ : C(I,X))` as functions. Path.cast is probably `{ γ with source' := h₀ ▸ γ.source, target' := h₁ ▸ γ.target }` — so as functions identical ✓.

  `hp.liftPath_zero γ e₀ γ.source : hp.liftPath γ e₀ γ.source 0 = e₀` ✓ — the iff' right side: `p ∘ Γ = γ₀ ∧ Γ 0 = e₀` with Γ := hp.liftPath γ e₀ γ.source: second conjunct is `hp.liftPath γ e₀ γ.source 0 = e₀` ✓.

Then:
```lean
  calc
    e₁ = hp.liftPath γ e₀ γ.source 1 := hEndγ.symm
    _ = hp.liftPath γ₀ e₀ γ₀.source 1 := congr_fun hsameness 1
    _ = e₀ := hEnd
```
Wait hEndγ : hp.liftPath γ e₀ γ.source 1 = e₁ ✓ so e₁ = ... via .symm ✓.

6. The main theorem:

```lean
theorem coveringOfSimplyConnectedIsHomeo {E X : Type*} [TopologicalSpace E]
    [TopologicalSpace X] [CompactSpace E] [T2Space X] [PathConnectedSpace E]
    [SimplyConnectedSpace X] (p : E → X) (hp : IsCoveringMap p)
    (hsurj : Function.Surjective p) : Nonempty (E ≃ₜ X) := by
  have hinj : Function.Injective p := by
    intro e₀ e₁ hpe
    exact coveringMap_injective_of_simplyConnected hp (PathConnectedSpace.joined e₀ e₁) hpe
  exact ⟨Continuous.homeoOfEquivCompactToT2 (f := Equiv.ofBijective p ⟨hinj, hsurj⟩) hp.continuous⟩
```
`PathConnectedSpace.joined : ∀ x y : X, Joined x y` — class field name: from PathConnected.lean: `class PathConnectedSpace (X) : Prop where nonempty : Nonempty X; joined : ∀ x y, Joined x y` ✓.
`Continuous.homeoOfEquivCompactToT2 {f : X ≃ Y} (hf : Continuous f)` — f = Equiv.ofBijective p ⟨hinj, hsurj⟩ : E ≃ X ✓. hp.continuous : Continuous p ✓.

Also `SimplyConnectedSpace X` implies `PathConnectedSpace X` — not needed here since surj is a hypothesis. But I should ALSO state and prove the version where surjectivity is derived (C1 applied):

```lean
theorem coveringOfSimplyConnectedIsHomeo' {E X} [..] [CompactSpace E] [T2Space X]
    [PathConnectedSpace E] [PathConnectedSpace X] [SimplyConnectedSpace X]
    (p : E → X) (hp : IsCoveringMap p) [Nonempty E] : Nonempty (E ≃ₜ X) :=
  coveringOfSimplyConnectedIsHomeo p hp (coveringMap_surjective_of_pathConnected hp)
```
Note SimplyConnectedSpace → PathConnectedSpace instance exists ✓ so [PathConnectedSpace X] is redundant. And [Nonempty E] — hmm, [PathConnectedSpace E] gives Nonempty E ✓ (class has nonempty field — instance `PathConnectedSpace.nonempty`?). `PathConnectedSpace E` has `nonempty : Nonempty E`? Looking at the class in PathConnected.lean: `class PathConnectedSpace (X : Type*) [TopologicalSpace X] : Prop where nonempty : Nonempty X; joined : ...` — so [PathConnectedSpace E] provides Nonempty E via the instance? The field is accessible: `PathConnectedSpace.nonempty`? Is there an instance `(pcs : PathConnectedSpace X) → Nonempty X`? There's probably `instance (priority := 100) PathConnectedSpace.nonempty`? Let me check during dev — `inferInstance` for Nonempty E from PathConnectedSpace E. I saw `instance (priority := 100) PathConnectedSpace.connectedSpace` at line 710. Likely a similar nonempty instance exists. If not, `‹PathConnectedSpace E›.nonempty` — wait, hmm: class fields are accessed as `PathConnectedSpace.nonempty`? Let me check: in mathlib, `PathConnectedSpace.nonempty` is a theorem? I'll check in dev. If needed: `Classical.choice (PathConnectedSpace.nonempty : Nonempty E)` hmm.

7. `isCoveringMap_id` — let me check if `IsCoveringMap` for id can be built via existing API: `IsEvenlyCovered.of_trivialization` needs a Trivialization PUnit id with x ∈ baseSet. Constructing a Trivialization: fields: `toFun : E → X × PUnit`, `invFun : X × PUnit → E`, `source : Set E = id ⁻¹' baseSet`, `target`, `baseSet : Set X`, `map_source'`, `map_target'`, `left_inv'`, `right_inv'`, `open_source`, `open_target`, `open_baseSet`, `continuousOn_toFun`, `continuousOn_invFun`, `proj_toFun`? Let me check Trivialization structure fields precisely — `Bundle.Trivialization` in `Mathlib/Topology/FiberBundle/Trivialization.lean`:

```lean
structure Trivialization (F : Type*) (proj : E → X) extends LocalHomeomorph E (X × F) where
  baseSet : Set X
  open_baseSet : IsOpen baseSet
  source_eq : source = proj ⁻¹' baseSet
  target_eq : target = baseSet ×ˢ univ
  proj_toFun : ∀ p ∈ source, (toFun p).1 = proj p
```
And extends LocalHomeomorph with localEquiv etc. That's a lot of fields. There might be a simpler constructor: `LocalHomeomorph` from a homeo: `LocalHomeomorph.ofHomeomorph`? hmm... there's `Trivialization.ofHomeomorph`? For the identity covering: baseSet = univ, fiber PUnit: toFun e = (e, ()), source = univ, target = univ ×ˢ univ. `LocalHomeomorph` construction: `LocalHomeomorph.symm`, `ofSet`... Hmm — maybe simpler: `IsEvenlyCovered.of_fiber_homeomorph` (line 110): `theorem of_fiber_homeomorph {J} [TopologicalSpace J] (g : I ≃ₜ J) {x : X} : ...` — converts between fibers, not what I need.

Alternative: use `IsFiberBundle.isCoveringMap` (line 419)? `protected theorem IsFiberBundle.isCoveringMap {F : Type*} [TopologicalSpace F] [DiscreteTopology F] : IsCoveringMap fun x : TotalSpace F => ...`? Hmm, IsFiberBundle is on the projection of a fiber bundle structure on E? Let me read lines 415-425 of Basic.lean. Or `FiberBundle.isCoveringMap`. Hmm.

Actually the simplest honest construction: build `IsEvenlyCovered id x PUnit` directly from its structure:

```lean
structure IsEvenlyCovered (x : X) (I : Type*) [TopologicalSpace I] : Prop where
  discreteTopology : DiscreteTopology I
  U : Set X
  xU : x ∈ U
  open_U : IsOpen U
  preimage : f ⁻¹' U = Set.range fun p : I × U ↦ p.2   -- hmm? let me check
```
Let me read IsEvenlyCovered structure (lines 40-60 of Basic.lean).
dsh: reasoning:
`IsEvenlyCovered f x I` = `DiscreteTopology I ∧ ∃ U, x ∈ U ∧ IsOpen U ∧ IsOpen (f ⁻¹' U) ∧ ∃ H : f ⁻¹' U ≃ₜ U × I, ∀ x, (H x).1.1 = f x`.

For `isCoveringMap_id`: f = id, I = PUnit, U = univ:
- DiscreteTopology PUnit ✓ instance.
- U = univ: x ∈ univ ✓, open ✓, id⁻¹' univ = univ open ✓.
- H : univ ≃ₜ univ × PUnit — H e := (⟨e, trivial⟩, ()) hmm: H : (id ⁻¹' univ : Set X) ≃ₜ (univ : Set X) × PUnit. Note (id ⁻¹' univ) = univ as a set. The homeo: toFun e := (⟨e.1, trivial⟩, ()) — that's univ ≃ₜ univ × PUnit. There's `Homeomorph.prodPUnit`? `Homeomorph.prodPUnit` : α ≃ₜ α × PUnit — YES mathlib has `Homeomorph.prodPUnit`! And `(univ : Set X) ≃ₜ (univ ×ˢ univ : Set (X × PUnit))`? Hmm — the sets: source = id ⁻¹' univ = univ : Set X; target = (univ : Set X) × PUnit as a Set of X × PUnit — that's `univ ×ˢ univ` in X × PUnit. Homeomorph between subtypes: `Homeomorph.Set.univ`? Hmm: `Homeomorph.Set.univ : (univ : Set α) ≃ₜ α`. So compose: `(Set.univ : Set X) ≃ₜ X` (Set.univ.symm) then `Homeomorph.prodPUnit : X ≃ₜ X × PUnit` then to `univ ×ˢ univ`? The target type is `(U : Set X) × I` as a subtype of X × PUnit = `(univ : Set X) ×ˢ (univ : Set PUnit)`. Homeomorph `(X × PUnit) ≃ₜ ((univ ×ˢ univ) : Set (X × PUnit))` — is there `Homeomorph.Set.univ` generalized: `Homeomorph.Set.univ (s := univ) : s ≃ₜ α`... `Set.univ` homeo: `Homeomorph.Set.univ : (univ : Set α) ≃ₜ α` — toFun = subtype val. Then symm of that with prod: `Homeomorph.Set.univ (α := X × PUnit)`. Compose: 

```
H := (Homeomorph.Set.univ.symm).trans ((Homeomorph.prodPUnit (α := X)).congrLeft? ...)
```
Hmm getting fiddly. Alternatively define H by hand: 

```
H : (id ⁻¹' univ : Set X) ≃ₜ (univ : Set X) × PUnit where
  toFun e := (⟨e.1, trivial⟩, ())   -- wait: target is (U : Set X) × I = (univ : Set X) × PUnit
  ...
```
The target `(univ : Set X) × PUnit` — points are pairs (⟨x, hx⟩, u) with hx : x ∈ univ. toFun e := (⟨e.1, trivial⟩, ⟨⟩) ✓; invFun p := ⟨p.1.1, trivial⟩ ✓; left_inv ✓; right_inv ✓ (hx : x ∈ univ is proof-irrelevant); continuous: toFun continuous (prod of continuous_subtype_val and const) ✓ via fun_prop; invFun: continuous via continuous_subtype_val ✓. That's a clean explicit definition ~8 lines. 

Actually wait — simpler: `of_trivialization` route? `toTrivialization'` also needs an IsEvenlyCovered. Circular. Direct construction it is.

Hmm, actually — even simpler: `IsEvenlyCovered` via `h.comp_homeomorph`/`homeomorph_comp` from a known evenly-covered? What's a known IsCoveringMap in mathlib I can bootstrap? `of_isEmpty`, `of_discreteTopology`. Or `IsCoveringMapOn.of_isLocalHomeomorphOn` (line 589) with T2 E + T2 X + CompactSpace E + local homeo — id is a local homeo! `IsLocalHomeomorph id` — is that in mathlib? `isLocalHomeomorph_id`? Hmm. That requires [T2Space E] [T2Space X] [CompactSpace E] — extra hypotheses; direct construction is cleaner and general. Go with direct H construction.

Do I even NEED isCoveringMap_id? For the "checked downstream use" witness with the disk: E = X = Disk 3, p = id. Yes it's needed there. Alternatively use p = doubleDiskMapQuot-based homeo e : DoubleDiskQuot 2 ≃ₜ Sphere 3 and coveringOfSimplyConnectedIsHomeo with E = DoubleDiskQuot 2, X = Sphere 3 — needs SimplyConnectedSpace (Sphere 3) ✗. So id-on-disk needs isCoveringMap_id. OK.

Now, `diskSimplyConnected`: `SimplyConnectedSpace (Disk n)` — via `Convex.contractibleSpace (convex_closedBall _ _) (diskNonempty n)` + `SimplyConnectedSpace.ofContractible`. `convex_closedBall` : `Convex ℝ (closedBall x r)` ✓ exists. `SimplyConnectedSpace.ofContractible` is an instance (priority 100) — so `inferInstance` works: 

```lean
instance diskSimplyConnected (n : ℕ) : SimplyConnectedSpace (Disk n) :=
  inferInstance
```
Hmm — will typeclass search find `ContractibleSpace (Disk n)`? `Convex.contractibleSpace` is a theorem (not instance); the instance is `RealTopologicalVectorSpace.contractibleSpace : ContractibleSpace E` (whole space). So I must construct: 
```lean
instance diskContractible (n : ℕ) : ContractibleSpace (Disk n) :=
  (convex_closedBall (x := (0 : EuclideanSpace ℝ (Fin n))) (r := 1)).contractibleSpace (diskNonempty n)
```
Wait, `Convex.contractibleSpace (hs : Convex ℝ s) (hne : s.Nonempty) : ContractibleSpace s` — check: `protected theorem Convex.contractibleSpace (hs : Convex ℝ s) (hne : s.Nonempty)` — needs `s : Set E`, E with [AddCommGroup][Module ℝ][TopologicalSpace][ContinuousAdd]. EuclideanSpace ✓. `convex_closedBall : Convex ℝ (Metric.closedBall x r)` ✓. And `s.Nonempty` — `diskNonempty n : Nonempty (Disk n)` — hmm, need `(closedBall 0 1 : Set E).Nonempty` = ∃ x ∈ closedBall: `⟨0, ...⟩`. `(diskNonempty n) : Nonempty (Disk n)` unfolds to exactly `(closedBall 0 1).Nonempty`? `Nonempty (closedBall ...)` = `∃ x, x ∈ closedBall ...` hmm: `Nonempty s` where s : Set E — Nonempty (Set E)?? NO: `diskNonempty n : Nonempty (Disk n)` and Disk n = Metric.closedBall 0 1 — closedBall is a Set, and `Nonempty (closedBall 0 1)` means... `Nonempty α` for α = closedBall 0 1 (a Set E, i.e. a TYPE E → Prop? NO — Set E is E → Prop, and Metric.closedBall x r : Set E is the set; `Nonempty (Metric.closedBall x r)`... hmm — actually for a Set s : Set E, `Nonempty s` is parsed as Nonempty (s : Type) with coercion Set → Sort? No wait: `Nonempty s` where s : Set E requires s : Type — there's a coercion from Set E to Type (the subtype)! `Metric.closedBall 0 1` as a type = the subtype `{x // x ∈ closedBall 0 1}` ✓. And `s.Nonempty : Prop` = `∃ x, x ∈ s` ✓. So `(diskNonempty n) : Nonempty (Disk n)` — is `Nonempty (closedBall 0 1)` (subtype interpretation) the same as `(closedBall 0 1).Nonempty` (the Set.Nonempty prop)? These are different: Nonempty of the subtype type vs the Set.Nonempty predicate. `Set.Nonempty s ↔ Nonempty s` — `Set.nonempty_iff`? hmm: `Set.Nonempty s := ∃ x, x ∈ s`, and Nonempty (subtype) = ∃ x : s, True — equivalent via `Set.Nonempty.to_subtype` / `Set.nonempty_coe_sort`? There's `s.nonempty_coe_sort` or `Set.nonempty_coe_sort : s.Nonempty ↔ Nonempty s`? something like that exists. I'll handle: `(convex_closedBall ...).contractibleSpace ⟨0, mem_closedBall_iff_norm.2 (by simp)⟩` — directly give the Set.Nonempty witness. ✓ simpler.

Then:
```lean
instance diskSimplyConnected (n : ℕ) : SimplyConnectedSpace (Disk n) :=
  haveI : ContractibleSpace (Disk n) :=
    (convex_closedBall (x := (0 : EuclideanSpace ℝ (Fin n))) (r := 1)).contractibleSpace
      ⟨0, mem_closedBall_iff_norm.2 (by simp)⟩
  inferInstance
```
Hmm wait — does `Convex.contractibleSpace` need [ContinuousSMul ℝ E]? Line 21-25: `variable {E} [AddCommGroup E] [Module ℝ E] [TopologicalSpace E] [ContinuousAdd E]` and the theorem at 25 — let me check what extra it needs (maybe [ContinuousSMul ℝ E] for the contraction map (t, x) ↦ (1-t)x₀ + tx). The star-convex contraction needs continuous scalar multiplication. Let me look at lines 21-39 of Analysis/Convex/Contractible.lean during dev.

And `diskPathConnected`: from SimplyConnectedSpace instance ✓ (SimplyConnectedSpace → PathConnectedSpace instance).

Also for the disk application theorem: 
```lean
theorem diskSelfCoverIsHomeo : Nonempty (Disk 3 ≃ₜ Disk 3) :=
  coveringOfSimplyConnectedIsHomeo (E := Disk 3) (X := Disk 3) (p := id)
    (isCoveringMap_id (Disk 3)) (fun x ↦ ⟨x, rfl⟩)
```
Wait `id` — p : Disk 3 → Disk 3, `(p := id)`. surj: `fun x => ⟨x, rfl⟩` ✓.

Hmm, this gives the trivial homeo — still a legit checked use (all hypotheses hold, non-vacuity of the pipeline: the simply-connected instance for the disk is real, compactness real). I'll also produce the *recognition-flavored* corollary `sphericalSpaceFormCovering` in the antipodal module as the main downstream use.

Now let me write the file. First some more API checks:
- `Convex.contractibleSpace` full hypotheses.
- `SimplyConnectedSpace.ofContractible` signature: `instance (priority := 100) ofContractible (Y) [TopologicalSpace Y] [ContractibleSpace Y] : SimplyConnectedSpace Y` ✓ (from SimplyConnected.lean line 79).
- `isCoveringMap` continuous: `IsCoveringMap.continuous : Continuous f` (Basic.lean line 337) ✓.
- `hp.eq_liftPath_iff'` — takes cov; `eq_liftPath_iff' {Γ : C(I, E)} : Γ = cov.liftPath γ e γ_0 ↔ p ∘ Γ = γ ∧ Γ 0 = e` ✓.
- `liftPath_zero`, `liftPath_lifts` ✓.
- `comp_subtypeVal_pathComponent_surjective` ✓.

One more: in the injectivity proof, `SimplyConnectedSpace.paths_homotopic γ₀ (Path.refl (p e₀)) : γ₀.Homotopic (Path.refl (p e₀))` where γ₀ : Path (p e₀) (p e₀) — x = y = p e₀ ✓.

`Path.refl` : `def refl (x : X) : Path x x` ✓ (in namespace Path).

`Path.cast`: check def: in Topology/Path.lean. Let me grep it.
dsh: reasoning:
- `Convex.contractibleSpace` needs `[ContinuousAdd E] [ContinuousSMul ℝ E]` ✓ EuclideanSpace has these.
- `Path.cast (γ : Path x y) {x' y'} (hx : x' = x) (hy : y' = y) : Path x' y'` — NOTE the direction: hx : x' = x (source NEW = old)! So for γ : Path (p e₀) (p e₁) → Path (p e₀) (p e₀): hx : p e₀ = p e₀ (rfl) and hy : p e₀ = p e₁ (hpe). So `γ.cast rfl hpe` : Path (p e₀) (p e₀) ✓.
- `Path.cast` def at line 378: `where` — let me see the fields: probably `{ γ with source' := hx ▸ γ.source ... }`. As a function it's γ ✓.

Now — one important check: `Path.Homotopic` is in `Mathlib/Topology/Homotopy/Path.lean` (namespace Path, def Homotopic at line 241) — and `SimplyConnectedSpace.paths_homotopic` is in AlgebraicTopology/FundamentalGroupoid/SimplyConnected.lean which imports Homotopy/Path ✓.

Now write the module. I'll create `release/Poincare/D12/TriangulationTopology/CoveringLemma.lean`. Targeted imports:
- `Mathlib.Topology.Homotopy.Lifting` (covers IsCoveringMap + lifting + HomotopyRel)
- `Mathlib.AlgebraicTopology.FundamentalGroupoid.SimplyConnected`
- `Mathlib.Topology.Instances.Real` (for I/unit interval — probably transitively imported)
- `Poincare.D12.TriangulationTopology.SphereGluing` (Disk)
- `Mathlib.Analysis.Convex.Contractible` for convex contractible.

Let me write it now. I'll iterate via lean. Some care about `unitInterval` scoped stuff: `open unitInterval`, `open scoped unitInterval` — Lifting.lean has its own opens. For my code, I mostly need Path/liftPath usage with the type I — `C(I, X)` notation needs `Topology.ContinuousFunction` notation available. `I` needs `unitInterval` namespace opened? In mathlib, `I` is notation for `Set.Icc 0 1`? `C(I, X)` — `I` elaborates to `Set.Icc (0 : ℝ) 1`... The notation `I` comes from `Mathlib.Topology.UnitInterval` with `scoped notation "I" => Set.Icc (0 : ℝ) 1`. So `open scoped unitInterval` needed. Lifting.lean does `open unitInterval` at top? It has `open unitInterval` probably; my file needs its own opens.

Let me write the file.
dsh: reasoning:
Several issues:
1. The coercion `(↑γ₀)` in liftPath vs `γ₀.toContinuousMap` — the Coe from Path to C(I,X) isn't defeq to toContinuousMap. Fix: in `hEnd`, after `rw [h]`, I need to convert. `h` is about `hp.liftPath γ₀.toContinuousMap e₀ .. 1`. But the target has `hp.liftPath (↑γ₀) e₀ .. 1`. The rw didn't match. Since the Coe probably unfolds via `Path.toContinuousMap`? Let me check the Coe instance def. Actually — since `Path` extends `C(I, X)`, the coercion is `⟨γ.toContinuousMap⟩`? or `γ.toContinuousMap` where toContinuousMap is the parent projection. Apparently not defeq in the `liftPath` argument (elaboration inserted `↑γ₀` but h used `γ₀.toContinuousMap`). The mismatch is in the elaboration of the *statement* `h` vs the target. Fix: state h with `(γ₀ : C(I, X))` — pass γ₀ explicitly so liftPath gets the same elaboration? The issue: in `hp.liftPath_apply_one_eq_of_homotopicRel hhom e₀ γ₀.source (Path.refl (p e₀)).source`, the γ₀/γ₁ args are implicit — elaboration picked γ₀.toContinuousMap from hhom's type? The hhom passed: `γ₀.Homotopic (Path.refl (p e₀))` = `Nonempty (γ₀.Homotopy (Path.refl ..))` = `Nonempty (ContinuousMap.HomotopyRel γ₀.toContinuousMap (Path.refl (p e₀)).toContinuousMap {0,1})`. Hmm, but `Path.Homotopy` abbrev — when hhom was elaborated, γ₀ : Path was coerced how? The `h` result mentions `hp.liftPath γ₀.toContinuousMap e₀ ⋯`. But my target `hp.liftPath (↑γ₀) e₀ ⋯` uses the Coe. Fix: make everything explicit:

```lean
  have hEnd : hp.liftPath (γ₀ : C(I, X)) e₀ γ₀.source 1 = e₀ := by
    have h := hp.liftPath_apply_one_eq_of_homotopicRel (γ₀ := γ₀.toContinuousMap)
      (γ₁ := (Path.refl (p e₀)).toContinuousMap) hhom e₀ γ₀.source (Path.refl (p e₀)).source
    rw [h]
    ...
```
Hmm — actually simpler: change to use toContinuousMap everywhere: target: `hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1 = e₀`. But later `hsame` uses `hp.liftPath γ e₀ γ.source` (coerced form). Ugh — mixing. Cleanest: since `Path` coerces to C(I,X) via instance, `(↑γ₀) : C(I, X)` — check what the Coe instance is: in Topology/Path.lean there's `instance : Coe (Path x y) (C(I, X)) := ⟨Path.toContinuousMap⟩`? and `Path.toContinuousMap` is the parent projection `γ.toContinuousMap`? The error shows elaboration produced `γ₀.toContinuousMap` for h but `(↑γ₀)` in my target — so the Coe instance does NOT reduce to toContinuousMap in the target but the *h* binder used the field projection directly (probably because liftPath_apply_one_eq_of_homotopicRel's arguments got unified from hhom's Path.Homotopy which mentions toContinuousMap).

Simplest robust fix: avoid `rw [h]`; instead use `convert h` or restate everything with explicit toContinuousMap. Let me restructure hEnd:

```lean
  have hEnd : hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1 = e₀ := by
    have h := hp.liftPath_apply_one_eq_of_homotopicRel (γ₀ := γ₀.toContinuousMap)
      (γ₁ := (Path.refl (p e₀)).toContinuousMap) hhom e₀ γ₀.source (Path.refl (p e₀)).source
    rw [h]
    change hp.liftPath (.const I (p e₀)) e₀ ((Path.refl (p e₀)).source) 1 = e₀
    -- (Path.refl (p e₀)).toContinuousMap = .const I (p e₀): need defeq or convert
```
Hmm — `(Path.refl x).toContinuousMap` vs `.const I x`: Path.refl def (line 142): `def refl (x : X) : Path x x where toFun := fun _ => x; continuous_toFun := continuous_const ...`? If refl's continuous proof is literally `continuous_const`, then toContinuousMap = ⟨fun _ => x, continuous_const⟩ and `.const I x = ⟨fun _ => x, continuous_const⟩` — defeq if the proofs are the same term. `ContinuousMap.const` : `⟨fun _ ↦ y, continuous_const⟩`. So defeq holds probably. Then after rw [h], target: `hp.liftPath (Path.refl (p e₀)).toContinuousMap e₀ .. 1 = e₀` — `change hp.liftPath (.const I (p e₀)) e₀ _ 1 = e₀` then `rw [hp.liftPath_const]` — liftPath_const : `cov.liftPath (.const I x) e hpe = .const I e` ✓ then `rfl`? After rw: `(.const I e₀) 1 = e₀` → rfl/simp.

2. hΓeq: the iff' gives `p ∘ Γ = γ ∧ Γ 0 = e₀` — second conjunct: `Γ 0 = e₀`; my `rfl` failed because `↑Γ 0 = e₀` is `Γ.source` not rfl (Γ.source : Γ 0 = e₀ — it's a proof, not rfl since source' field). Fix: `exact Γ.source` — but as `(Γ : C(I,E)) 0 = e₀`: `Γ.source` ✓ (same statement after coercion — C(I,E) application = function application).

Wait, in the error: "argument rfl has type ?m.127 = ?m.127 but is expected to have type ↑Γ 0 = e₀". So use `Γ.source`. Hmm — but careful: in `eq_liftPath_iff'`, γ_0 : γ 0 = p e — here e = e₀, γ = γ, and γ.source : γ 0 = p e₀. And the iff's Γ 0 = e means (hp.liftPath γ e₀ γ.source) 0 = e₀ — that's `hp.liftPath_zero γ e₀ γ.source`! Wait no — in hΓeq I'm proving `(Γ : C(I,E)) = hp.liftPath γ e₀ γ.source` via iff' — the conjuncts are about the CANDIDATE Γ... no wait. Re-read: `eq_liftPath_iff' {Γ : C(I, E)} : Γ = cov.liftPath γ e γ_0 ↔ p ∘ Γ = γ ∧ Γ 0 = e`. Here Γ is the left side candidate. So conjuncts: `p ∘ (Γ : C(I,E)) = γ` and `Γ 0 = e₀` ✓ = Γ.source. ✓

3. calc errors are consequences of hΓeq failing.

4. isCoveringMap_id: the structure literal syntax was off — Homeomorph.mk takes toEquiv (an Equiv), continuous_toFun, continuous_invFun. My `{ toFun := .., invFun := .., left_inv := .., right_inv := .., continuous_toFun := .., continuous_invFun := .. }` — I wrote it as a Homeomorph literal but the type is `Set.univ ≃ₜ Set.univ × PUnit` — hmm wait, I need `H : id ⁻¹' U ≃ₜ U × I` where U = univ: `(univ : Set X) ≃ₜ (univ : Set X) × PUnit`. The RHS type `(univ : Set X) × PUnit` — this is the product type of the SUBTYPE and PUnit. Homeomorph literal needs `toEquiv` field. Also — the type confusion: target of H is `U × I` where I = PUnit, U = univ as subtype — `(univ : Set X) × PUnit` — that's fine as a type (subtype × PUnit). But my toFun wrote `(⟨e.1, trivial⟩, PUnit.unit)` where e : Set.univ — e : univ (subtype) — ⟨e.1, trivial⟩ — hmm e.1 : X ✓ trivial : e.1 ∈ univ ✓. PUnit.unit : PUnit ✓. But error says "expected ↑(id ⁻¹' {x})" — the second IsEvenlyCovered field — wait the errors at 169 are about the THIRD field: `⟨?_, ?_⟩` — hold on: error "Application type mismatch ... PUnit.unit ... expected to have type ↑(id ⁻¹' {x})" — this is about the hH proof `fun h => h.1.1`?? The last goal `intro e; exact e.1.1`? Hmm, error at 169:59 in the FIRST refine bullet — the homeo literal. The expected H type was `id ⁻¹' univ ≃ₜ univ × PUnit` but elaborator treated the literal as an Equiv (3 fields: toFun, invFun, left_inv, right_inv = 4 fields; "only 1 was provided" means it parsed my structure as `⟨toFun⟩`?? because `{ toFun := ..., invFun := ..., ...}` with `·` intro error... Actually error "unexpected identifier; expected '}'" at 169:70 — the `refine ⟨{...}, ?_⟩` — hmm the structure literal: `{ toFun := fun e : Set.univ ↦ (⟨e.1, trivial⟩, PUnit.unit) invFun := ... }` — I MISSED the newline structure? I wrote `invFun` on the same line as the toFun lambda — `(⟨e.1, trivial⟩, PUnit.unit)` followed directly by `invFun` — the lambda body might have consumed `invFun`? `fun e : Set.univ ↦ (⟨e.1, trivial⟩, PUnit.unit) invFun := ...` — yes, the parse consumed `invFun` into the lambda body! Need newline/comma. Let me rewrite isCoveringMap_id cleanly:

```lean
theorem isCoveringMap_id (X : Type*) [TopologicalSpace X] : IsCoveringMap (id : X → X) := by
  intro x
  let H : (id ⁻¹' Set.univ : Set X) ≃ₜ Set.univ × PUnit :=
  { toEquiv :=
      { toFun := fun e => (⟨e.1, trivial⟩, PUnit.unit)
        invFun := fun p => ⟨p.1.1, trivial⟩
        left_inv := by intro e; rfl
        right_inv := by intro p; ext <;> rfl }
    continuous_toFun := by fun_prop
    continuous_invFun := by fun_prop }
  refine ⟨inferInstance, Set.univ, trivial, isOpen_univ, ?_, ?_, ?_⟩
```
wait — the IsEvenlyCovered fields: (DiscreteTopology I, U, x ∈ U, IsOpen U, IsOpen (f ⁻¹' U), H, hH). So refine ⟨_, U, hxU, open_U, open_preimage, H, hH⟩ — 7 fields: `⟨inferInstance, Set.univ, trivial, isOpen_univ, isOpen_univ'?, H, ?_⟩`. `IsOpen (id ⁻¹' univ)` — `simpa` reduces `id ⁻¹' univ` to univ ✓. hH : ∀ x, (H x).1.1 = id x → `(H e).1.1 = e.1`? (H e).1 : U (subtype), .1 : X — hH : ∀ e, (H e).1.1 = id e — id e = e.1 (e : univ subtype, id e = e? id on X applied to (e : X)... careful: H : (id⁻¹'univ : Set X) ≃ₜ ..., so H's domain subtype is `{e : X // e ∈ id ⁻¹' univ}` — its coercion to X is e.1; `id e` where e coerced: `(id : X → X) (e : X)` = e = e.1. So hH: `fun e ↦ by rfl` — (H e).1.1 = e.1: toFun e = (⟨e.1, trivial⟩, ()) → .1.1 = e.1 ✓ rfl.

Also the `simpa using isOpen_univ` warning — use `simpa` fine.

Also in the hH position I currently have `intro h; exact h.1.1` — wrong (I confused with something else). Fix.

5. Also hEnd's `change` after `rw [h]` — the leftover `_` args: `hp.liftPath (.const I (p e₀)) e₀ _ 1` — the h₁ proof arg. liftPath_const takes hpe : x = p e; I'll pass `rfl`... hmm: `hp.liftPath_const (x := p e₀) (e := e₀) (hpe := rfl)`. Hmm wait — after `change hp.liftPath (.const I (p e₀)) e₀ ?h 1 = e₀`, the ?h : (.const I (p e₀)) 0 = p e₀ — the constant applied at 0 = p e₀ — rfl ✓. Then `rw [hp.liftPath_const]`? The lemma: `liftPath_const {x : X} (hpe : x = p e) : cov.liftPath (.const I x) e hpe = .const I e` — rw needs the hpe to match ?h. `rw [hp.liftPath_const]` might fail to unify hpe := rfl vs ?h. Use `simpa using congr_fun (hp.liftPath_const (x := p e₀) (e := e₀) (hpe := rfl)) 1`? hmm hpe : x = p e with x = p e₀, e = e₀: rfl : p e₀ = p e₀ ✓. Then result: `hp.liftPath (.const I (p e₀)) e₀ rfl = .const I e₀` → congr_fun .. 1 : `.. 1 = e₀` ✓. So:

```lean
    change hp.liftPath (.const I (p e₀)) e₀ _ 1 = e₀
    exact congr_fun (hp.liftPath_const (x := p e₀) (e := e₀) (hpe := rfl)) 1
```
`congr_fun ... 1 : (hp.liftPath (.const I (p e₀)) e₀ rfl) 1 = (.const I e₀) 1` — and `(.const I e₀) 1 = e₀` by rfl, so this closes if the target's `_` unifies with rfl — it will via the defeq of `_`'s type to `p e₀ = p e₀`. Hmm — actually `exact congr_fun (hp.liftPath_const (x := p e₀) (e := e₀) (hpe := rfl)) 1` has type `hp.liftPath (.const I (p e₀)) e₀ rfl 1 = e₀` — target has `_` for hpe — elaborator unifies ✓.

But wait — the `change` step: after `rw [h]`, target is `hp.liftPath (Path.refl (p e₀)).toContinuousMap e₀ ((Path.refl (p e₀)).source) 1 = e₀`. Will `change hp.liftPath (.const I (p e₀)) e₀ _ 1 = e₀` succeed? Needs `(Path.refl (p e₀)).toContinuousMap = (.const I (p e₀))` defeq. Risky. Alternative: skip change, use liftPath_const with explicit toContinuousMap:

`simpa using congr_fun (hp.liftPath_const (x := p e₀) (e := e₀) (hpe := rfl)) 1` — target: `hp.liftPath (Path.refl (p e₀)).toContinuousMap e₀ (Path.refl (p e₀)).source 1 = e₀` vs lemma giving `hp.liftPath (.const I (p e₀)) e₀ rfl 1 = e₀`. simpa needs (Path.refl (p e₀)).toContinuousMap = .const I (p e₀) provable by... not simp. Use `convert`:

```lean
    convert congr_fun (hp.liftPath_const (x := p e₀) (e := e₀) (hpe := rfl)) 1 using 2
```
hmm convert creates goals: toContinuousMap = const — `ext t; rfl`? `(Path.refl (p e₀)).toContinuousMap` — Path.refl def: let me check line 142: `def refl (x : X) : Path x x where toFun := fun _ => x; continuous_toFun := continuous_const; source' := rfl; target' := rfl`? If toContinuousMap's continuous proof is `continuous_const`, defeq with `.const I x` ✓ and `change` works. Path.refl def (142-145): need to look. Let me just try `change` and see.

Actually, simplest: pass everything through toContinuousMap from the start and avoid Path coercion mismatch:

Rewrite hEnd fully:
```lean
  have hEnd : hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1 = e₀ := by
    have h := hp.liftPath_apply_one_eq_of_homotopicRel (γ₀ := γ₀.toContinuousMap)
      (γ₁ := (Path.refl (p e₀)).toContinuousMap) hhom e₀ γ₀.source (Path.refl (p e₀)).source
    rw [h]
    convert congr_fun (hp.liftPath_const (x := p e₀) (e := e₀) (hpe := rfl)) 1 using 1
    ext t
    rfl
```
Hmm — `liftPath_apply_one_eq_of_homotopicRel h : liftPath γ₀ e₀ h₀ 1 = liftPath γ₁ e₀ h₁ 1` where the explicit args I pass: (γ₀ := γ₀.toContinuousMap) (γ₁ := ...) then hhom — hhom : γ₀.Homotopic (Path.refl (p e₀)) — but expected type is `(γ₀.toContinuousMap).HomotopicRel (Path.refl (p e₀)).toContinuousMap {0,1}` = `Nonempty (ContinuousMap.HomotopyRel γ₀.toContinuousMap (Path.refl (p e₀)).toContinuousMap {0,1})`. Since Path.Homotopy is an abbrev for exactly ContinuousMap.HomotopyRel _ _ {0,1} and Path.Homotopic = Nonempty (Path.Homotopy ..), hhom should elaborate directly. ✓

And hsame: similarly make explicit:
```lean
  have hsame : hp.liftPath γ.toContinuousMap e₀ γ.source = hp.liftPath γ₀.toContinuousMap e₀ γ₀.source := by
    rw [hp.eq_liftPath_iff']
    refine ⟨?_, hp.liftPath_zero γ.toContinuousMap e₀ γ.source⟩
    rw [hp.liftPath_lifts]
    ext t
    rfl
```
The iff': `Γ = cov.liftPath γ e γ_0 ↔ p ∘ Γ = γ ∧ Γ 0 = e` — Γ = hp.liftPath γ.toContinuousMap e₀ γ.source, γ (the second arg of liftPath on the right) = γ₀.toContinuousMap. First conjunct: `p ∘ hp.liftPath γ.toContinuousMap e₀ γ.source = γ₀.toContinuousMap` — rw [hp.liftPath_lifts γ.toContinuousMap e₀ γ.source] gives `γ.toContinuousMap = γ₀.toContinuousMap` — ext t: (γ t) vs (γ₀ t) — cast preserves function ✓ hopefully rfl.

And hΓeq:
```lean
  have hΓeq : Γ.toContinuousMap = hp.liftPath γ.toContinuousMap e₀ γ.source := by
    rw [hp.eq_liftPath_iff']
    refine ⟨?_, Γ.source⟩
    ext t
    rfl
```
First conjunct: `p ∘ Γ.toContinuousMap = γ.toContinuousMap`. γ := Γ.map hp.continuous — Path.map def (line 344): `def map (γ : Path x y) {f : X → Y} (h : Continuous f) : Path (f x) (f y)` — its toFun = f ∘ γ presumably. As C(I,X): `γ.toContinuousMap = ⟨p ∘ Γ, ...⟩`. `p ∘ Γ.toContinuousMap` = the continuous map composition ⟨p, hp.continuous⟩ ∘ Γ.toContinuousMap. Hmm — is `(Γ.map hp.continuous).toContinuousMap` defeq to `⟨p, hp.continuous⟩.comp Γ.toContinuousMap`? Path.map likely defined as `{ toFun := f ∘ γ, continuous_toFun := h.comp γ.continuous, source' := .., target' := .. }`. Then `p ∘ Γ.toContinuousMap` as functions: f ∘ (Γ's function) where f = ⟨p, hp.continuous⟩. `ext t; rfl` — applying: (p ∘ Γ.toContinuousMap) t = p (Γ t) — vs ((Γ.map hp.continuous).toContinuousMap) t = (f ∘ γ) t = f (γ t) = p (Γ t). γ's function = Γ's function. So rfl ✓ probably.

The calc:
```lean
  calc
    e₁ = hp.liftPath γ.toContinuousMap e₀ γ.source 1 := by
      rw [← hΓeq]
      exact Γ.target
    _ = hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1 := congr_fun hsame 1
    _ = e₀ := hEnd
```
`rw [← hΓeq]` — target: `e₁ = hp.liftPath γ.toContinuousMap e₀ γ.source 1`; rewrite with hΓeq : Γ.toContinuousMap = hp.liftPath ... — hmm, target has liftPath on RHS; rw [← hΓeq] turns it into Γ.toContinuousMap 1; goal: `e₁ = Γ 1` — exact Γ.target ✓.

Then finally the main theorem uses `hp.liftPath`-free.

Also `hEnd`'s `rw [h]` — h : `hp.liftPath γ₀.toContinuousMap e₀ ⋯ 1 = hp.liftPath (Path.refl (p e₀)).toContinuousMap e₀ ⋯ 1` — target `hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1 = e₀` — rw [h] replaces LHS ✓ (the `⋯` are the same h₀).

Then `convert congr_fun (hp.liftPath_const (x := p e₀) (e := e₀) (hpe := rfl)) 1 using 1` — the remaining goal: `hp.liftPath (Path.refl (p e₀)).toContinuousMap e₀ (Path.refl (p e₀)).source = hp.liftPath (.const I (p e₀)) e₀ rfl`? convert with `using 1` leaves 1 goal: probably the argument equality. Alternatively try `simpa only [...] `? Let me try: `simpa using congr_fun (hp.liftPath_const (x := p e₀) (e := e₀) (hpe := rfl)) 1`. If defeq holds it just works; if not, `convert ... using 1` and close with `ext t; rfl`? The remaining goal might be a liftPath argument equality — closing needs the const/refl defeq. Hmm — what does `(Path.refl (p e₀)).toContinuousMap` reduce to? Let me check Path.refl def quickly (line 142-146).

Let me also double check `hp.eq_liftPath_iff'` uses `p ∘ Γ = γ` — need Γ : C(I,E) — pass Γ.toContinuousMap ✓.

Let me look at Path.refl and Path.cast and Path.map definitions to be sure about rfl closings.
dsh: reasoning:
- `Path.refl` sets `toContinuousMap := .const I x` — so `(Path.refl (p e₀)).toContinuousMap` IS definitionally `.const I (p e₀)` ✓ — `change` works.
- `Path.map` = `γ.map' h.continuousOn`; `map_coe : (γ.map h : I → Y) = f ∘ γ` — simp lemma ✓. So `(γ : I → X) = p ∘ (Γ : I → E)` via `simp [γ, map_coe]`? γ := Γ.map hp.continuous. `(γ.toContinuousMap : C(I,X))` vs `p ∘ Γ.toContinuousMap`: as ContinuousMaps — `ext t; simp [γ]` — hmm `(γ : I → X) = p ∘ Γ` by map_coe; but the goal is ContinuousMap equality `(γ.toContinuousMap) = ⟨p, hp.continuous⟩.comp Γ.toContinuousMap`? Actually `p ∘ Γ.toContinuousMap` — `p` is a plain function; `p ∘ (Γ : C(I,E))` — mathlib interprets `p ∘ Γ` where Γ : C(I,E) as ContinuousMap.comp ⟨p, hp.continuous⟩ Γ — the continuous version since f = p gets coerced to C(X,E)? In the theorem statement `p ∘ Γ = γ` with Γ : C(I,E), γ : C(I,X) — `p ∘ Γ` is `(⟨p, ...⟩ : C(E, X)).comp Γ`? ContinuousMap has `HComp` for `f ∘ g` with g : C(α, β), f : β → γ where f gets... there's `ContinuousMap.comp` with explicit continuous proof; the notation `p ∘ Γ` needs p : C(E, X) — elaboration inserts `.comp ⟨p, hp.continuous⟩`? In `eq_liftPath_iff'` the statement is `p ∘ Γ = γ` — elaborated in mathlib with p : E → X raw — so `p ∘ Γ` = `(Γ : C(I,E)).comp ⟨p, hp.continuous⟩`? Actually ContinuousMap has a coercion instance `(f : β → γ) → C(α,β) → C(α,γ)`? There's `ContinuousMap.comp` and a `coe` from function? Hmm, mathlib has instance `CoeFun` and the notation `p ∘ Γ` with Γ continuous map: `Function.comp` on coerced functions gives I → X, but the expected type is C(I,X)... mathlib defines `ContinuousMap.comp (g : C(α,β)) (f : C(β,γ))`. And the notation `p ∘ Γ` — there is an instance `HComp`? In `eq_liftPath_iff'` it's literally `p ∘ Γ = γ` — mathlib compiled it, so the notation elaborates: Γ : C(I,E) coerces to function I → E, p ∘ Γ : I → X, then expected type C(I,X) — hmm, that wouldn't elaborate... unless there's a coercion instance from functions with continuity... No — there's `ContinuousMap.comp` with implicit continuity: `p ∘ Γ` uses `ContinuousMap.comp Γ ⟨p, hp.continuous⟩`? The elaboration of `p ∘ Γ` when Γ : C(I,E) and the result is expected to be C(I,X): mathlib provides `instHComp`? I recall mathlib allows `f ∘ g` for g continuous and f arbitrary via `ContinuousMap.comp`'s `⟨f, hf⟩`? The theorem states `p ∘ Γ = γ` — p : E → X with cov giving continuous. Looking at eq_liftPath_iff' line 272: `lemma eq_liftPath_iff' {Γ : C(I, E)} : Γ = cov.liftPath γ e γ_0 ↔ p ∘ Γ = γ ∧ Γ 0 = e` — so `p ∘ Γ` elaborates with some mechanism. There must be a `ContinuousMap.comp` via `OfFunction`? Whatever — in MY proof I need to produce `p ∘ Γ.toContinuousMap = γ.toContinuousMap` — I'll write `ext t; simp [γ, Path.map_coe]`? Let me just write `ext t; rfl` and see — since map_coe says `(γ.map h : I → Y) = f ∘ γ` as functions, and ContinuousMap ext is about function application, `(p ∘ Γ.toContinuousMap) t = p ((Γ.toContinuousMap) t)` vs `(γ.toContinuousMap) t = p (Γ t)` — `simp [γ]` using map_coe should do it. I'll use `simp [γ, Path.map_coe]` — but careful: `simp [γ]` unfolds the let too. In the ext goal, after `ext t`, goal is about `↑(p ∘ Γ.toContinuousMap) t = ↑γ.toContinuousMap t` — `simp [γ]` → uses map_coe to rewrite γ to p ∘ (Γ : I → E)... The Coes might make this `rfl`. Try `rfl` first.

Now rewrite the whole file with fixes.
dsh: reasoning:
Progress. Issues:

1. Line 104: `congr_fun (hp.liftPath_const ...)` — the equality is between ContinuousMaps, and `congr_fun` on a C(I,E)=C(I,E) equality... hmm: `liftPath_const` has type `hp.liftPath (.const I (p e₀)) e₀ ⋯ = .const I e₀` — the `⋯` is elaborated hpe? I passed (hpe := rfl) — but the error shows it inferred `IsCoveringMap.liftPath_const hp rfl` without my (x :=) (e :=) annotations?? It says `hp.liftPath (ContinuousMap.const (↑I) (p e₀)) e₀ ⋯` — good. `congr_fun` expects the LHS to be a function type — `.const I e₀` — congr_fun needs both sides to be functions; C(I,E) is a function type (via CoeFun) — `congr_fun` might not fire on the CoeFun wrapper. Actually the error: "is expected to have type ?m.127 = ?m.128" — congr_fun's argument must be an equality of function types (f = g with f : α → β). `hp.liftPath ... = .const I e₀` — the types are C(I,E) — congr_fun can't apply to C(I,E) equality directly because C(I,E) is not syntactically a Pi type. Fix: use `congr_fun (congr_arg DFunLike.coe (hp.liftPath_const ...)) 1`? Or `have hc := hp.liftPath_const (x := p e₀) (e := e₀) (hpe := rfl); change hp.liftPath (.const I (p e₀)) e₀ _ = .const I e₀ at hc`? Simpler: 
```
    have hc : hp.liftPath (.const I (p e₀)) e₀ (rfl : (.const I (p e₀)) 0 = p e₀) = .const I e₀ :=
      hp.liftPath_const (x := p e₀) (e := e₀) (hpe := rfl)
    rw [hc]
    rfl
```
After `change hp.liftPath (.const I (p e₀)) e₀ _ 1 = e₀`, `rw [hc]` — the `_` must unify with rfl — `hc`'s hpe is `rfl`... target's `_` is a metavariable that will unify. Then goal `(.const I e₀) 1 = e₀` — `rfl`? `.const I e₀ 1` — via CoeFun this is `e₀` — simp closes.

Hmm wait, actually after `change`, target: `hp.liftPath (.const I (p e₀)) e₀ ?m 1 = e₀` where ?m : (.const I (p e₀)) 0 = p e₀. rw [hc] with hc's type `hp.liftPath (.const I (p e₀)) e₀ rfl = .const I e₀` — rw unifies ?m with rfl — ✓. Then `.const I e₀ 1 = e₀`: rfl ✓ probably.

2. Line 121/125 calc errors — chain: `e₁ = ...` — first step should produce `e₁ = hp.liftPath γ.toContinuousMap e₀ γ.source 1`. hΓeq : Γ.toContinuousMap = hp.liftPath γ.toContinuousMap e₀ γ.source. `rw [← hΓeq]` — hmm, target after calc step: `e₁ = hp.liftPath γ.toContinuousMap e₀ γ.source 1` — rw [←hΓeq] rewrites `hp.liftPath γ.toContinuousMap e₀ γ.source` → `Γ.toContinuousMap` giving `e₁ = Γ.toContinuousMap 1` — but Γ.toContinuousMap 1 vs Γ 1 — Γ.target : Γ 1 = e₁. Hmm — Γ.toContinuousMap 1 = Γ 1 by rfl? toContinuousMap = parent projection; `Γ.toContinuousMap 1` — the C(I,E) applied to 1 coerces to the function — = Γ.toContinuousMap.toFun 1 = Γ 1 ✓ probably defeq... The error says "invalid calc step, left-hand side is e₁ but is expected to be e₀" — the error is that the calc's second step's LHS didn't match the first step's RHS. So the first step itself failed to typecheck (its RHS was elaborating wrong). The issue: `e₁ = hp.liftPath γ.toContinuousMap e₀ γ.source 1` — hmm, maybe `rw [← hΓeq]` failed silently? No — the error is at line 121 col 4 = `_ = hp.liftPath γ₀...` — meaning after step 1, the remaining RHS isn't what I wrote. Actually the reported error: "invalid 'calc' step, left-hand side is e₁ : E but is expected to be e₀ : E" — so step 1 produced RHS `e₁`?? That means `rw [← hΓeq]` rewrote the WHOLE RHS: `hp.liftPath γ.toContinuousMap e₀ γ.source 1` — the pattern `hp.liftPath γ.toContinuousMap e₀ γ.source` — hmm rw replaced it with Γ.toContinuousMap, then `exact Γ.target` — no wait. If step 1's proof produced something of the wrong type... "left-hand side is e₁ but is expected to be e₀" — the calc expects step1 RHS = step2 LHS = `hp.liftPath γ.toContinuousMap e₀ γ.source 1`, but got `e₁`. So in step 1, after `rw [← hΓeq]`, the goal became `e₁ = Γ.toContinuousMap 1` and `exact Γ.target` — Γ.target : Γ 1 = e₁ — but goal is `e₁ = Γ.toContinuousMap 1` — the `exact` failed?? No — the error line is 121 col 4 which is the `_ = hp.liftPath γ₀.toContinuousMap ...` line. So step 1 "succeeded" with a wrong RHS?? That happens when `rw [← hΓeq]` rewrote MORE: it replaced the entire `hp.liftPath γ.toContinuousMap e₀ γ.source 1`... no. OR: `calc e₁ = X := by ...` — the calc's first RHS is X = `hp.liftPath γ.toContinuousMap e₀ γ.source 1`; if the proof `by rw ...; exact Γ.target` closed a DIFFERENT goal: maybe `rw [←hΓeq]` rewrote inside... wait if hΓeq is used as `←` then LHS of hΓeq (Γ.toContinuousMap) is replaced by liftPath... no `←` replaces RHS by LHS: `hp.liftPath γ.toContinuousMap e₀ γ.source` → `Γ.toContinuousMap`. Goal becomes `e₁ = Γ.toContinuousMap 1`. `exact Γ.target` : Γ 1 = e₁ ≠ e₁ = Γ.toContinuousMap 1. Should fail... unless `Γ.toContinuousMap 1` defeq `Γ 1` and Γ.target : Γ 1 = e₁ — still wrong direction! e₁ = Γ 1 vs Γ 1 = e₁. So exact Γ.target fails → but then the error would be at line ~119. The reported errors are at 121 and 125 — the calc steps themselves. Hmm — maybe `rw [← hΓeq]` DID rewrite the LHS `e₁`?? No.

Wait, maybe the problem: in the calc, step 1 is `e₁ = hp.liftPath γ.toContinuousMap e₀ γ.source 1 := by rw [← hΓeq]; exact Γ.target` — hmm, but actually — maybe `exact Γ.target` succeeded via some elaboration where Γ.toContinuousMap 1 reduced to Γ 1 and... no, direction.

Let me think about error "invalid 'calc' step, left-hand side is e₁ : E but is expected to be e₀ : E" at line 121. Line 121 is `_ = hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1 := congr_fun hsame 1`. The calc chain: [e₁, hp.liftPath γ..., hp.liftPath γ₀..., e₀]. Step 2's LHS = step 1's RHS = `hp.liftPath γ.toContinuousMap e₀ γ.source 1`; step 2's proof `congr_fun hsame 1` : `hp.liftPath γ.toContinuousMap e₀ γ.source 1 = hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1` — so step 2 LHS should be fine IF step 1 RHS is that. The error "left-hand side is e₁" means the ELABORATOR thinks step 2's LHS is e₁?? — no: "invalid 'calc' step, left-hand side is X but is expected to be Y" means the step's stated LHS (inferred from proof's type) is X = e₁, but the previous RHS was e₀... I think the elaboration of the calc went wrong because step 1's `by rw [← hΓeq]; exact Γ.target` — the `rw` might have rewritten the goal `e₁ = hp.liftPath γ.toContinuousMap e₀ γ.source 1` — hmm wait — is it possible that rw with `← hΓeq` unified `hp.liftPath γ.toContinuousMap e₀ γ.source` with the whole LHS `e₁`?? No, e₁ : E.

Hold on — maybe the issue is `Γ.target` — Γ : Path e₀ e₁ — target : Γ 1 = e₁. After rw, goal: `e₁ = Γ.toContinuousMap 1`. Hmm — actually WAIT: maybe rw [← hΓeq] DIDN'T rewrite because the pattern `hp.liftPath γ.toContinuousMap e₀ γ.source` appears as subterm `hp.liftPath γ.toContinuousMap e₀ γ.source 1` — rw would rewrite it to `Γ.toContinuousMap 1`... The calc elaborator checks step 1: LHS e₁, RHS `hp.liftPath γ.toContinuousMap e₀ γ.source 1`, proof must have type `e₁ = RHS`. If `rw [←hΓeq]` fails to find the pattern (elaboration mismatch between hΓeq's elaboration and the goal's), then rw leaves goal `e₁ = hp.liftPath γ.toContinuousMap e₀ γ.source 1`, then `exact Γ.target` — Γ.target : Γ 1 = e₁ — doesn't close, error would be at exact line... but error is at 121. Confusing. Unless — the goal after rw became `e₁ = Γ.toContinuousMap 1` and `Γ.target` matched as `Γ 1 = e₁` and... NO.

Hmm, wait — actually maybe the calc elaboration order: the SECOND step's `_` LHS must unify with step 1's RHS — `hp.liftPath γ.toContinuousMap e₀ γ.source 1`. The error says the second step's proof `congr_fun hsame 1` has LHS `hp.liftPath γ.toContinuousMap e₀ γ.source 1`?? "left-hand side is e₁" — hmm, that suggests the calc step 2 got LHS `e₁` — meaning step 1's RHS was inferred as `e₁` — i.e., step 1's proof closed goal `e₁ = e₁`?? via... OH WAIT. I bet `rw [← hΓeq]` rewrote the RHS `hp.liftPath γ.toContinuousMap e₀ γ.source 1` into `Γ.toContinuousMap 1`, then `exact Γ.target` — Γ.target has type `↑Γ 1 = e₁`. Goal `e₁ = ↑Γ.toContinuousMap 1`. Hmm no.

Hmm, let me look at hsame's type again: `hp.liftPath γ.toContinuousMap e₀ γ.source = hp.liftPath γ₀.toContinuousMap e₀ γ₀.source` — congr_fun hsame 1 : `hp.liftPath γ.toContinuousMap e₀ γ.source 1 = hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1` — hmm congr_fun on ContinuousMap equality — SAME congr_fun problem as before (C(I,E) not a Pi)! So `congr_fun hsame 1` is ill-typed — maybe it elaborated to something weird: congr_fun hsame where hsame is coerced... The error message for line 121: "invalid 'calc' step, left-hand side is e₁ : E but is expected to be e₀ : E" — hmm, that reads like: the calc step's LHS (which should be previous RHS) is `e₁`?? 

Hmm wait — maybe I misread line numbers. 121: `e₁ = hp.liftPath γ.toContinuousMap e₀ γ.source 1 := by` — the FIRST step is at line 119-121! So "invalid 'calc' step, left-hand side is e₁ : E but is expected to be e₀ : E" at line 121 = the first step's proof produced type `e₁ = ?` — expected... no wait: "left-hand side is e₁ : E but is expected to be e₀ : E" — the calc's initial LHS is e₁ (I wrote `calc e₁ = ...`). Hmm — the calc syntax `calc e₁ = hp.liftPath ... := by ...` — the initial term e₁ ✓. The error "left-hand side is e₁ but is expected to be e₀" — the STEP's LHS is e₁ but expected e₀?? That means the previous step's RHS was e₀ — i.e., my chain broke at step 1: hEnd as the final step... no wait, hEnd : `hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1 = e₀` ✓ final RHS e₀ ✓.

I think what's happening: step 1's `by rw [← hΓeq]; exact Γ.target` — the `rw` produced goal `e₁ = ↑Γ.toContinuousMap 1` and `exact Γ.target` — elaborator sees Γ.target : Γ 1 = e₁ where `Γ 1` and `↑Γ.toContinuousMap 1`... if defeq then goal `e₁ = Γ 1`, exact fails with type mismatch error AT line 121 (the tactic block's line). Then the calc reports "invalid calc step" because... the error text at 121:4 col 4 = the `e₁ = ...` line start. Hmm — "invalid 'calc' step, left-hand side is e₁ : E but is expected to be e₀ : E" — hmm — could it be that the calc elaboration expected the FIRST step's LHS to be e₀?? No...

OH WAIT. I see it — I bet the problem is `rw [← hΓeq]` rewrote `γ.source`-related stuff?? No. Let me look at what hΓeq looks like after my edit: `hΓeq : Γ.toContinuousMap = hp.liftPath γ.toContinuousMap e₀ γ.source`. And in the calc: `e₁ = hp.liftPath γ.toContinuousMap e₀ γ.source 1 := by rw [← hΓeq]; exact Γ.target`. Hmm, `rw [← hΓeq]`: rewrites occurrences of `hp.liftPath γ.toContinuousMap e₀ γ.source` with `Γ.toContinuousMap`. BUT — the goal also contains `γ.source` as the last argument of liftPath — `hp.liftPath γ.toContinuousMap e₀ γ.source` — the pattern in hΓeq's RHS is exactly `hp.liftPath γ.toContinuousMap e₀ γ.source` ✓. Rewrites to `Γ.toContinuousMap 1`? wait the goal RHS is `hp.liftPath γ.toContinuousMap e₀ γ.source 1` = `(hp.liftPath γ.toContinuousMap e₀ γ.source) 1` ✓ → `Γ.toContinuousMap 1`. Goal: `e₁ = Γ.toContinuousMap 1`. exact Γ.target : Γ 1 = e₁ — FAILS (direction).

Hmm so that's the bug — the exact should be `Γ.target.symm`? But wait: after rw, goal `e₁ = ↑Γ.toContinuousMap 1`; and `↑Γ.toContinuousMap 1` — hmm — ContinuousMap application: `Γ.toContinuousMap 1` = `Γ.toContinuousMap.toFun 1` = `Γ.toFun 1`? If `toContinuousMap` is the parent projection, `Γ.toContinuousMap.toFun = Γ.toFun` — but is `Γ.toContinuousMap 1` defeq `Γ 1`? `Γ 1` for Γ : Path = the CoeFun instance on Path (inherited from C(I,X)) applied to 1 = Γ.toFun 1. Probably defeq ✓. So goal after rw: `e₁ = Γ 1` and `exact Γ.target.symm`? Γ.target : Γ 1 = e₁ — need e₁ = Γ 1 = Γ.target.symm ✓.

Hmm but then why did the calc say LHS e₁ expected e₀? Let me just fix all issues and re-run; the error messages might cascade from the earlier hEnd failure. Actually hEnd at line 104 had the congr_fun failure which cascades: hEnd has wrong/missing value → the calc step 3 `_ = e₀ := hEnd` fails → "expected e₀" errors. So fix congr_fun in hEnd and hsame (congr_fun on C(I,E) equality), and Γ.target direction.

For congr_fun on ContinuousMap equality: use `congr_arg DFunLike.coe hsame`? DFunLike.coe is a coercion function `C(I,E) → (I → E)`. `congr_arg DFunLike.coe hsame : DFunLike.coe (liftPath ...) = DFunLike.coe (liftPath ...)` then `congr_fun .. 1`. Or simpler: `have hsame' : (hp.liftPath γ.toContinuousMap e₀ γ.source : I → E) = hp.liftPath γ₀.toContinuousMap e₀ γ₀.source := congrArg DFunLike.coe hsame` then `congr_fun hsame' 1`. Hmm — DFunLike.coe with ContinuousMap — ContinuousMap has `toFun`; DFunLike.coe exists for it ✓. Or avoid entirely: prove hsame as function equality:

Actually simplest: in the calc, replace `congr_fun hsame 1` with:
```
    _ = hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1 := by
      have hf : (hp.liftPath γ.toContinuousMap e₀ γ.source : I → E) = hp.liftPath γ₀.toContinuousMap e₀ γ₀.source :=
        congrArg DFunLike.coe hsame
      exact congr_fun hf 1
```
Hmm — does the calc step proof's type check: goal `hp.liftPath γ.toContinuousMap e₀ γ.source 1 = hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1` — the `1` applications use CoeFun — congr_fun hf 1 has type `(hp.liftPath γ.toContinuousMap e₀ γ.source : I → E) 1 = (hp.liftPath γ₀... : I → E) 1` — the goal's `hp.liftPath γ.toContinuousMap e₀ γ.source 1` — defeq to the function application ✓ (CoeFun reduces). Should be OK.

Similarly in hEnd: `exact congr_fun (hp.liftPath_const (x := p e₀) (e := e₀) (hpe := rfl)) 1` — the congr_fun issue: liftPath_const's type is C(I,E) equality. Fix:
```
    have hc : (hp.liftPath (.const I (p e₀)) e₀ (rfl : (.const I (p e₀)) 0 = p e₀) : I → E) =
        (.const I e₀ : I → E) :=
      congrArg DFunLike.coe (hp.liftPath_const (x := p e₀) (e := e₀) (hpe := rfl))
    rw [hc]
    rfl
```
wait — after change, goal `hp.liftPath (.const I (p e₀)) e₀ _ 1 = e₀` — rw with hc: hc is a function-equality; rw hc rewrites `(hp.liftPath (.const I (p e₀)) e₀ _ : I → E)` — the goal has `hp.liftPath (.const I (p e₀)) e₀ _ 1` — hmm, `_` metavar must unify with hc's explicit rfl ✓ hopefully. rw with a function equality on an application term: the goal term `hp.liftPath (.const I (p e₀)) e₀ ?m 1` — via CoeFun is `(hp.liftPath ... ?m).toFun 1`? Hmm — rw might not see the match because of the coercion wrapper. Safer:

```
    change (hp.liftPath (.const I (p e₀)) e₀ (rfl : (.const I (p e₀)) 0 = p e₀) : I → E) 1 = e₀
    rw [hp.liftPath_const (x := p e₀) (e := e₀) (hpe := rfl)]
    rfl
```
Can I `change` to a function-coercion form? The goal is `hp.liftPath (.const I (p e₀)) e₀ ?m 1 = e₀` — with CoeFun this is defeq to `(hp.liftPath (.const I (p e₀)) e₀ ?m).toFun 1 = e₀` — hmm `change` needs the target to be defeq; the `1` — is `1 : I`? `hp.liftPath ... 1` — the liftPath is a ContinuousMap applied to (1 : I) ✓.

Alternative cleaner: 
```
    have hc := hp.liftPath_const (x := p e₀) (e := e₀) (hpe := rfl)
    -- hc : hp.liftPath (.const I (p e₀)) e₀ rfl = .const I e₀
    exact (congrArg (fun f : C(I, E) => f 1) hc).trans rfl
```
`congrArg (fun f => f 1) hc : hp.liftPath (.const I (p e₀)) e₀ rfl 1 = (.const I e₀) 1` ✓ (f 1 via CoeFun). `.trans rfl` — since (.const I e₀) 1 = e₀ by rfl ✓. And the goal's `_` unifies with rfl ✓. 

Also for hsame — replace `congr_fun hsame 1` in calc with `congrArg (fun f : C(I, E) => f 1) hsame` — but wait hsame : liftPath γ... = liftPath γ₀... (C(I,E) equality) ✓ so `congrArg (fun f : C(I,E) => f 1) hsame : hp.liftPath γ.toContinuousMap e₀ γ.source 1 = hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1` ✓ direct.

And Γ.target direction: `exact Γ.target` → after rw goal is `e₁ = Γ.toContinuousMap 1`; hmm but is `Γ.toContinuousMap 1` defeq `Γ 1`? To be safe: `exact Γ.target.symm` after `change e₁ = (Γ.toContinuousMap : I → E) 1`? Let me instead avoid rw on the goal: 

```
    e₁ = hp.liftPath γ.toContinuousMap e₀ γ.source 1 := by
      have h := congrArg (fun f : C(I, E) => f 1) hΓeq.symm  -- liftPath .. 1 = Γ.toContinuousMap 1
      exact h.trans Γ.target -- Γ.target : Γ 1 = e₁; h : liftPath γ .. 1 = Γ.toContinuousMap 1
```
hmm — congrArg (fun f => f 1) hΓeq.symm : hp.liftPath γ.toContinuousMap e₀ γ.source 1 = Γ.toContinuousMap 1. Then `.trans Γ.target` needs Γ.target : Γ.toContinuousMap 1 = e₁ — Γ.target : Γ 1 = e₁ — defeq of `Γ.toContinuousMap 1` and `Γ 1` — again. Ugh. Let me check: Path extends C(I,X); `Γ.toContinuousMap` — since Path *extends* C(I,X), the field is inherited: `Path.toContinuousMap` = parent projection = `{ toContinuousMap := Γ.toContinuousMap }`... Actually for structures extending, `Γ.toContinuousMap : C(I,X)` = the parent object = Γ coerced? And `Γ 1` — CoeFun Path via the parent's CoeFun: `(Γ : Path e₀ e₁) 1` = `Γ.toContinuousMap.toFun 1`? If CoeFun for Path is defined via the inherited CoeFun of C(I,X) applied to the parent projection, then `Γ 1` = `Γ.toContinuousMap 1` defeq ✓. Then h.trans Γ.target works if elaboration accepts Γ.target : Γ 1 = e₁ as `Γ.toContinuousMap 1 = e₁` — defeq should allow `exact h.trans Γ.target`? The elaborator checks Γ.target's type against expected `Γ.toContinuousMap 1 = e₁` — defeq holds → ✓.

3. isCoveringMap_id errors:
- Line 176: `refine ⟨inferInstance, ...⟩` — first field DiscreteTopology (f ⁻¹' {x}) — WAIT: `IsCoveringMap id` = `∀ x, IsEvenlyCovered id x (id ⁻¹' {x})` — the I = id ⁻¹' {x} (the FIBER), not PUnit!! Right — IsCoveringMap f x I with I = f ⁻¹' {x} — the fiber itself. So my `inferInstance` for DiscreteTopology I — I = id⁻¹'{x} = {x} subtype — DiscreteTopology of the subtype {x}: needs an instance — `Subtype.discreteTopology`? If X has any topology, the subtype with induced topology has DiscreteTopology iff... the subspace topology on {x} from any topology is indiscrete/trivial — is there an instance `DiscreteTopology ({x} : Set X)`? NO in general (subspace topology on a singleton from an arbitrary topology is the trivial topology — which equals discrete iff... the subspace topology on {x} is indiscrete (only open sets: ∅, {x}) and the discrete topology on a subsingleton type = indiscrete ✓ but as a typeclass instance mathlib doesn't have `DiscreteTopology {x}` in general since indiscrete = discrete requires... hmm — actually for a singleton subtype, the topology is indiscrete; DiscreteTopology means "topology = discrete topology" = ⊥. Is the subspace topology on {x} defeq to ⊥? NO — not an instance in general.

So for `isCoveringMap_id` I must supply `IsEvenlyCovered id x (id ⁻¹' {x})` with the FIBER as I, and H : (id ⁻¹' U) ≃ₜ U × (id ⁻¹' {x}) — for U = univ: H : univ ≃ₜ univ × {x} — toFun e := (⟨e.1, trivial⟩, ⟨x, rfl⟩) — with fiber {x} (subtype of X). invFun p := ⟨p.1.1, trivial⟩ ✓. DiscreteTopology (id ⁻¹' {x}) : DiscreteTopology ({x} : Set X) — hmm, need to construct: the fiber {x} — its subspace topology: is it discrete? The subspace topology of a singleton is the indiscrete topology on a subsingleton, and for a SubsINGLETON the indiscrete topology equals the discrete topology?? DiscreteTopology α := ⊥ topology = the actual topology. On a subsingleton, ⊥ (indiscrete) is the only T1-able... no — discrete topology on a type = `⊥` in the order of topologies = indiscrete = generated by ∅ only... wait: the discrete topology on α is `⊤`? In mathlib's order convention: topologies ordered by coarser=smaller; discrete topology = ⊤?? `DiscreteTopology α` is defined as `TopologicalSpace α = ⊥`? Let me recall: mathlib: `class DiscreteTopology (α) : Prop where eq_bot : ‹TopologicalSpace α› = ⊥` where ⊥ is the... hmm — in mathlib, ⊥ topology = indiscrete? The order on TopologicalSpace: `t ≤ s` means t coarser. ⊥ = indiscrete (coarsest), ⊤ = discrete. And `DiscreteTopology α : ‹TopologicalSpace α› = ⊥`?? That would mean DiscreteTopology = indiscrete?! Let me check: mathlib `DiscreteTopology`: "A topology on α is discrete if every set is open, i.e. the topology equals the discrete topology ⊤"? Hmm. `theorem isOpen_discrete (s : Set α) [DiscreteTopology α] : IsOpen s` — consistent with discrete = all open. The class def: `class DiscreteTopology (α : Type u) [t : TopologicalSpace α] : Prop where eq_bot : t = ⊥` — hmm I remember there's subtlety: in mathlib the partial order on topologies is ANTI-monotone in opens? Let me check: `le_def : t ≤ s ↔ ∀ {x}, {U | t.IsOpen U}... ` — `t ≤ s` iff every t-open is s-open (t coarser). ⊥ = coarsest = indiscrete, ⊤ = finest = discrete. And DiscreteTopology: `eq_bot : ‹TopologicalSpace α› = ⊥`?? — that says the topology IS the coarsest = indiscrete?! Hmm — no wait, I recall mathlib defines `DiscreteTopology` as: `class DiscreteTopology (α : Type u) [t : TopologicalSpace α] : Prop where eq_bot : t = ⊥` where `⊥` — in the lattice of topologies with `t ≤ s ↔ t.coinduced... ` — Hmm, actually I remember: mathlib's lattice: `inf` = coarsest common refinement? No... In mathlib `TopologicalSpace` lattice: `s ⊓ t` = the topology generated by s ∪ t (finer), `s ⊔ t` = intersection (coarser). So the order is reversed from the "finer" order: t ≤ s means s ⊆ t as sets of opens, i.e., t FINER. So ⊥ = finest topology = DISCRETE ✓, ⊤ = indiscrete. OK — so DiscreteTopology α := t = ⊥ = the discrete topology ✓. 

For the fiber {x} of id: the subspace topology on a singleton is: opens = {∅, {x}} = the indiscrete topology = ⊤ in this lattice — NOT ⊥ unless... DiscreteTopology requires t = ⊥ = all sets open. Indiscrete on a subsingleton = discrete? As SETS of opens: indiscrete = {∅, univ} = {∅, {x}}; discrete on {x} = all subsets = {∅, {x}} — SAME since the type has only 2 subsets! So on a subsingleton, indiscrete topology = discrete topology as sets ✓. But is there an instance `DiscreteTopology {x}`? mathlib has `instDiscreteTopologySubtype`? Hmm — there IS `instance Subtype.discreteTopology : DiscreteTopology s`? No — there's `instDiscreteTopologyForall`... For subtypes: `[DiscreteTopology X] → DiscreteTopology s`? There's a generic instance? I don't think there's one for singletons automatically. 

Hmm — so how does the failure arise: line 176:10 "failed to synthesize DiscreteTopology ↑(id ⁻¹' {x})" for the `inferInstance`. Fix: change my H's fiber from PUnit to the actual fiber and give the DiscreteTopology instance manually. For the subtype {x}:

```lean
instance discreteTopology_singleton_subtype (X) [TopologicalSpace X] (x : X) :
    DiscreteTopology ({x} : Set X) := by
  -- the subspace topology on a singleton: every subset is open
  refine ⟨?eq⟩  -- DiscreteTopology.eq_bot : ‹TopologicalSpace ({x} : Set X)› = ⊥
```
Hmm — proving t = ⊥ for the subtype topology: ext U: t.IsOpen U ↔ ⊥.IsOpen U. Every U open: for the induced topology, U open iff ∃ V open in X, U = V ∩ {x}... on a singleton all subsets are open in ⊥... wait need `t = ⊥` where ⊥ = discrete = all open. t (subspace on {x}): U ⊆ {x} is open iff U = {x}∩V for open V — if U = ∅: ∅ = {x} ∩ ∅? ∅ ∩ {x} = ∅ ✓ with V = ∅ open ✓. If U = {x}: {x} ∩ univ ✓. So all U open → t = ⊤?? no wait — every subset open = discrete = ⊥ in the reversed lattice?? I'm confusing myself. Let me just check: `DiscreteTopology` — `eq_bot : ‹TopologicalSpace α› = ⊥` and `isOpen_discrete : [DiscreteTopology α] → IsOpen s` — so ⊥ has all sets open → ⊥ IS the discrete topology → lattice ⊥ = finest ✓.

The instance: `DiscreteTopology ({x} : Set X)` — is there a mathlib instance `instDiscreteTopologySubtypeSingleton`? Let me search. Actually, hmm — wait: for the subtype topology on {x}: U ∈ t iff ∃ V open in X, V ∩ {x} = U. Every subset of {x} arises: ∅ via V = ∅; {x} via V = univ. So t = {∅, {x}} = the indiscrete = NOT all... but the type {x} has only two subsets {∅, {x}} — so t = P({x}) = discrete ✓✓. Since the subtype is a subsingleton, all subsets = {∅, {x}} — so t equals ⊥ PROVIDED ⊥.IsOpen = all = {∅, {x}} ✓. So instance holds for singleton subtypes ✓ — mathlib might have `instDiscreteTopologySubsingleton`? Hmm: `DiscreteTopology` for subsingletons with... there might be an instance `[Subsingleton α] → DiscreteTopology α`?? That's FALSE in general (indiscrete on subsingleton = discrete only as noted — actually it IS true!! On a subsingleton, indiscrete = discrete! So `Subsingleton α → DiscreteTopology α` would be a valid instance — but is it in mathlib? It would be dangerous-ish but true. Let me check `discreteTopology_iff_isOpen_singleton`... hmm.

Let me check what instance mathlib has: grep "DiscreteTopology" in Subtype-related files: `Topology/Constructions.lean`: `instance Subtype.instDiscreteTopology {p : X → Prop} [DiscreteTopology X] : DiscreteTopology (Subtype p)`? There's `[DiscreteTopology X] → DiscreteTopology s` for any subtype ✓ likely. But X arbitrary... 

ALTERNATIVE, cleaner: avoid the fiber problem entirely by using the OTHER direction of `of_trivialization`: `IsEvenlyCovered.of_trivialization [DiscreteTopology I] {x : X} {t : Trivialization I f} : x ∈ t.baseSet → IsEvenlyCovered f x (f ⁻¹' {x})` — with I = PUnit and a trivialization t of id with baseSet univ: `⟨inferInstance(PUnit discrete), t, trivial⟩`. Trivialization PUnit id: source = id⁻¹'univ = univ ✓. Let me construct t:

```lean
noncomputable def trivialization_id (X) [TopologicalSpace X] : Trivialization PUnit (id : X → X) where
  toFun e := (e, PUnit.unit)
  invFun p := p.1
  source := Set.univ
  target := Set.univ
  map_source' e he := by simp
  map_target' p hp := by simp  -- p.1 ∈ univ
  left_inv' e he := rfl
  right_inv' p hp := by ext <;> rfl? -- p = (p.1, ()): ext1 → p.1 = p.1 ✓; ext2 → () = () ✓ 
  open_source := isOpen_univ
  open_target := isOpen_univ
  continuousOn_toFun := by fun_prop  -- e ↦ (e, ()) on univ
  continuousOn_invFun := by fun_prop
  baseSet := Set.univ
  open_baseSet := isOpen_univ
  source_eq := rfl  -- source = id ⁻¹' baseSet = univ ✓
  target_eq := rfl  -- target = baseSet ×ˢ univ = univ ✓
  proj_toFun e he := by simp  -- (toFun e).1 = id e ✓
```
Trivialization structure fields — need exact names: from toTrivialization' example in Basic.lean (lines 69-89): fields used: toFun, invFun, source, target, map_source', map_target', left_inv', right_inv', open_source, open_target, continuousOn_toFun, continuousOn_invFun, baseSet, open_baseSet, source_eq, target_eq, proj_toFun ✓. Great — mirror that.

Then:
```lean
theorem isCoveringMap_id (X : Type*) [TopologicalSpace X] : IsCoveringMap (id : X → X) := by
  intro x
  exact IsEvenlyCovered.of_trivialization (I := PUnit) (t := trivialization_id X) trivial
```
Wait — signature: `theorem of_trivialization [DiscreteTopology I] {x : X} {t : Trivialization I f} : x ∈ t.baseSet → IsEvenlyCovered f x (f ⁻¹' {x})` ✓ with I = PUnit (DiscreteTopology PUnit ✓ inferInstance), t = trivialization_id X, x ∈ t.baseSet = trivial ✓.

4. `convex_closedBall` — error: "Invalid field contractibleSpace: environment does not contain Function.contractibleSpace" — the elaborator treated `(convex_closedBall x r)` weirdly: `convex_closedBall a 1` has type `∀ (a : EuclideanSpace ℝ (Fin n)), 0 ∈ Metric.closedBall a 1 → StarConvex ℝ 0 (Metric.closedBall a 1)` — WAIT that's wrong — `convex_closedBall` with x := 0 elaborated as the FUNCTION convex_closedBall applied to... the error shows `fun a => convex_closedBall a 1` of type `∀ a, 0 ∈ closedBall a 1 → StarConvex ℝ 0 (closedBall a 1)` — that's `convex_closedBall` applied to r := 1 only, with x implicit and center... hmm — the actual lemma might be `convex_closedBall (x : E) (r : ℝ) : Convex ℝ (closedBall x r)`. But the error type says `0 ∈ closedBall a 1 → StarConvex ℝ 0 (closedBall a 1)` — that's a DIFFERENT lemma: probably `convex_closedBall` in the *current* mathlib is stated via star-convexity: `theorem convex_closedBall (x : E) (r : ℝ) : Convex ℝ (closedBall x r) := (starConvex_closedBall x r).convex`? and the error shows the resolution got `starConvex_closedBall`-ish shape with `0 ∈ ...` hypothesis — that's `starConvex_closedBall (x : E) (r : ℝ) (hx : x ∈ closedBall x r) : StarConvex ℝ x (closedBall x r)`. So `convex_closedBall` resolved to something whose application with (x := 0) (r := 1) produced a function?? "Invalid field contractibleSpace: environment does not contain Function.contractibleSpace" — the expression `(convex_closedBall (x := ...) (r := 1)).contractibleSpace` elaborated with convex_closedBall = `fun a => convex_closedBall a 1` — i.e., my named args didn't bind: `convex_closedBall (x := (0 : ...)) (r := 1)` — maybe the actual parameter names differ (e.g., it's not x/r, or the lemma in THIS mathlib version has a different signature). Let me check the actual `convex_closedBall` signature.

Then `diskContractible` fix: use `Convex.contractibleSpace` with the right convex proof.

Let me check the signature.
dsh: reasoning:
`convex_closedBall (a : E) (r : ℝ) : Convex ℝ (closedBall a r)` — parameter is `a`, not `x`! That's why my `(x := ...)` didn't bind and left it as a function. Fix: `convex_closedBall (a := (0 : EuclideanSpace ℝ (Fin n))) 1` — wait also there are TWO convex_closedBall in scope: `Analysis/Normed/Module/Seminorm/Basic.lean:1026: theorem convex_closedBall : Convex ℝ (closedBall p x r)` — that one has variables p x r implicit from context. Ambiguity — use fully explicit application: `convex_closedBall (a := (0 : EuclideanSpace ℝ (Fin n))) (r := 1)`? If the name is ambiguous the elaborator picks one; with named args (a :=) (r :=) it will select the one with those param names ✓.

Now fix all the issues in the file.
dsh: reasoning:
Progress on hEnd/hΓeq/hsame — only calc errors remain there. And:

1. `Trivialization` unknown — the name is `Bundle.Trivialization` (namespace Bundle). In Basic.lean it's `Trivialization` because that file opens `Bundle`. Fix: `Bundle.Trivialization`.

2. `IsEvenlyCovered.of_trivialization` — the arguments got confused: `(I := PUnit)` failed to parse — error at 190:44 "unexpected token ':='" and the error shows `IsEvenlyCovered.of_trivialization I` with I interpreted as the first explicit arg (the membership proof x ∈ t.baseSet!). The signature: `theorem of_trivialization [DiscreteTopology I] {x : X} {t : Trivialization I f} : x ∈ t.baseSet → IsEvenlyCovered f x (f ⁻¹' {x})` — the first EXPLICIT argument is the membership proof! So `IsEvenlyCovered.of_trivialization (I := PUnit) (t := trivialization_id X) trivial` — hmm the parse error at 190:44: `(I := PUnit)` — with the error "unexpected token ':='" at position of `:=` after I. Because `I` is... wait — my file has `open Set Function` and `I` is the notation for the unit interval from `open scoped unitInterval`! `I` elaborates as `Set.Icc 0 1` before the parser assigns it as a name — named argument `(I := ...)` — the parser treats `I` as a term? Named arguments: `(name := term)` — `I` parses as a term (the interval notation), not an identifier. That's the bug — the unitInterval `I` notation shadows. Fix: don't open scoped unitInterval, or write `(I := _root_.PUnit)`... no — the issue is the *name* I. Use `(I := PUnit)` won't parse since `I` is notation... The unitInterval notation is `scoped notation "I" => Set.Icc (0 : ℝ) 1` — in a named-arg position `(I := ...)`, Lean parses the name `I` — but with notation active, `I` is a term. Renaming: drop the named arg and let elaboration infer I from the trivialization's fiber? `IsEvenlyCovered.of_trivialization (t := trivialization_id X) trivial` — I is determined by t : Bundle.Trivialization PUnit id → I = PUnit ✓ inference. 

But wait — do I even need unitInterval open in this file? I use `C(I, E)` and `.const I e₀` — the `I` in C(I,E) and .const I. Yes I need it. OK just drop `(I := PUnit)`.

Also the `DiscreteTopology` synth error at 190:8 was for the pre-parse garbage; with correct parse, I = PUnit → DiscreteTopology PUnit ✓ inferInstance.

3. Calc errors at 122/126 remain: "invalid 'calc' step, left-hand side is e₁ : E but is expected to be e₀ : E" at line 122 (the `_ = hp.liftPath γ₀...` step) and "right-hand side is e₀ but is expected to be e₁" at 126 (final `_ = e₀ := hEnd`). So step 1 (`e₁ = hp.liftPath γ.toContinuousMap e₀ γ.source 1 := by exact (congrArg ... hΓeq.symm).trans Γ.target`) — hmm, error says step 2's LHS is e₁ but expected e₀?? Let me decode: line 122 is `_ = hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1 := congrArg ...` — "invalid calc step, LHS is e₁ but expected e₀" — the calc elaborator processes steps sequentially: after step 1 with RHS = `hp.liftPath γ.toContinuousMap e₀ γ.source 1`, step 2's LHS should be that; the error says LHS is `e₁`... wait no — I think the errors mean: step 1's PROOF has type `e₁ = ?` hmm...

Hmm wait, actually maybe the first step failed: `(congrArg (fun f : C(I, E) => f 1) hΓeq.symm).trans Γ.target` — congrArg (fun f => f 1) hΓeq.symm : `hp.liftPath γ.toContinuousMap e₀ γ.source 1 = Γ.toContinuousMap 1`. `.trans Γ.target` needs Γ.target : `Γ.toContinuousMap 1 = e₁`. Γ.target : Γ 1 = e₁. If `Γ.toContinuousMap 1` is NOT defeq `Γ 1`... it should be (toContinuousMap is parent projection). If this trans failed, the error would be at line 120-121 though. The calc errors at 122: "LHS is e₁ but is expected to be e₀" — hmm — "expected to be e₀"?! That's bizarre unless the calc chain order broke: hEnd as last step has type `hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1 = e₀` — calc step 3: `_ = e₀ := hEnd` — error 126: "right-hand side is e₀ but is expected to be e₁" — so the chain after step 2 has RHS... step 2's RHS = `hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1`; step 3's proof hEnd : `hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1 = e₀` — the error "right-hand side is e₀ but expected e₁" means step 3's stated RHS... hmm wait — maybe step 3's `_ = e₀ := hEnd` — the elaborator computes hEnd's LHS = `hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1` and RHS = e₀; expected RHS should be... the FINAL RHS can be anything — calc just chains. The error "right-hand side is e₀ : E but is expected to be e₁" suggests the calc's FINAL RHS was fixed as e₁ by... the step at line 122?? 

OH WAIT. I see it now! The problem: line 121's step: `e₁ = hp.liftPath γ.toContinuousMap e₀ γ.source 1 := by exact (...)`. In `calc` syntax, the FIRST step `a = b := proof` — fine. But look at my code — I wrote:

```
  calc
    e₁ = hp.liftPath γ.toContinuousMap e₀ γ.source 1 := by
      exact (congrArg (fun f : C(I, E) => f 1) hΓeq.symm).trans Γ.target
    _ = hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1 :=
      congrArg (fun f : C(I, E) => f 1) hsame
    _ = e₀ := hEnd
```

Errors: line 122 (step 2) "left-hand side is e₁ but is expected to be e₀" — hmm — this can only mean step 1's RHS elaborated to... e₀?!?! and step 3 "right-hand side is e₀ but is expected to be e₁" — final expected e₁??

OHHH. I bet the issue: `Γ.target` — Γ : Path e₀ e₁ — `Γ.target : Γ 1 = e₁` — in `.trans Γ.target`, the elaborator unified the trans LHS `hp.liftPath γ.toContinuousMap e₀ γ.source 1 = Γ.toContinuousMap 1` with Γ.target's LHS `Γ 1 = e₁` — if `Γ.toContinuousMap 1` defeq-reduced... `.trans` : a = b → b = c → a = c. h : hp.liftPath γ.. 1 = Γ.toContinuousMap 1; Γ.target : Γ 1 = e₁. So result: hp.liftPath γ.toContinuousMap e₀ γ.source 1 = e₁ — WRONG DIRECTION! I need `e₁ = hp.liftPath γ.. 1`, i.e., the trans in the other order: `Γ.target.symm.trans (congrArg ... hΓeq.symm).symm` or: `(congrArg (fun f => f 1) hΓeq).symm.trans Γ.target.symm`? Let me compute: hΓeq : Γ.toContinuousMap = hp.liftPath γ.toContinuousMap e₀ γ.source. congrArg f hΓeq : Γ.toContinuousMap 1 = hp.liftPath γ.toContinuousMap e₀ γ.source 1. I want e₁ = hp.liftPath γ.. 1: Γ.target.symm : e₁ = Γ.toContinuousMap 1 (if defeq). So: `Γ.target.symm.trans (congrArg (fun f : C(I, E) => f 1) hΓeq)` : e₁ = hp.liftPath γ.toContinuousMap e₀ γ.source 1 ✓.

Hmm — but wait, the original `.trans Γ.target` : if defeq `Γ.toContinuousMap 1` with `Γ 1`, result = hp.liftPath γ.. 1 = e₁ — the calc step 1 then has proof of `hp.liftPath γ.. 1 = e₁` but the step states LHS e₁ RHS hp.liftPath γ.. 1 — MISMATCH — but the error would say "left-hand side is hp.liftPath γ.. 1, expected e₁"... The reported errors are about e₀ — the cascade confusion. Anyway, the fix: reverse the equality direction. But ALSO the defeq worry `Γ.toContinuousMap 1` vs `Γ 1`: since Path extends C(I,X), the parent projection gives `Γ.toContinuousMap`; the CoeFun on Path — `Path` has CoeFun via the inherited ContinuousMap structure. `Γ 1` = `Γ.toContinuousMap.toFun 1`? hmm — actually `Γ.target`'s type is `Γ 1 = e₁` — via whatever CoeFun. `Γ.toContinuousMap 1` = `Γ.toContinuousMap.toFun 1` via C's CoeFun. Are these defeq? Γ.toContinuousMap — for `structure Path extends C(I,X)`, `Γ.toContinuousMap` is the parent structure projection = `Γ.1` essentially... and `Γ.toFun`? ContinuousMap's toFun is a field — `Γ.toFun` accesses via inheritance. Both should reduce to the same underlying function. I'll write it as `Γ.target.symm.trans (...)` and if defeq fails, insert `change`.

Let me fix these now. Also fix Trivialization → Bundle.Trivialization and drop the `(I := PUnit)` named arg.
dsh: reasoning:
1. Calc still broken. Let me look at the exact code and error lines. Line 122 is the step 2 line. Let me view lines 115-130 and figure out precisely.

2. `source_eq := rfl` fails: `univ = univ ×ˢ univ` — the target_eq field: `target = baseSet ×ˢ univ` where target : Set (X × PUnit) = univ and baseSet ×ˢ univ = univ ×ˢ univ : Set (X × PUnit) — as sets these are equal but not defeq: `(univ : Set (X × PUnit))` vs `(univ : Set X) ×ˢ (univ : Set PUnit)`. Need `by ext p; simp` or `by simp` — `target_eq := by simp [Set.prod_univ?]` — hmm: `univ ×ˢ univ = univ` — lemma `Set.prod_univ : s ×ˢ univ = ...` — there's `Set.univ_prod_univ : univ ×ˢ univ = univ`. So `target_eq := Set.univ_prod_univ.symm`? target_eq : target = baseSet ×ˢ univ = univ = univ ×ˢ univ — `Set.univ_prod_univ : univ ×ˢ univ = univ`? hmm wait — careful: `univ ×ˢ univ` here — first univ : Set X, second : Set PUnit — `Set.univ_prod_univ : Set.univ ×ˢ Set.univ = Set.univ` ✓ (in Set (X × PUnit)). So `target_eq := Set.univ_prod_univ.symm`.

3. `of_trivialization`'s I can't be inferred from t because the fiber I must EQUAL the fiber id⁻¹'{x}: the conclusion is `IsEvenlyCovered f x (f ⁻¹' {x})` — with t : Trivialization PUnit id — the theorem: `theorem of_trivialization [DiscreteTopology I] {x : X} {t : Trivialization I f} : x ∈ t.baseSet → IsEvenlyCovered f x (f ⁻¹' {x})` — so I is PUnit but conclusion fiber is f⁻¹'{x} — that's FINE — the elaborator tried to infer I from the expected conclusion `IsEvenlyCovered id x (id⁻¹'{x})` → I := id⁻¹'{x} → then t : Trivialization (id⁻¹'{x}) id — mismatch with trivialization_id X : Trivialization PUnit id. So I must pass I := PUnit explicitly — but `I` is shadowed by unitInterval notation! Fix: don't open `unitInterval` scoped notation in this file... but I need `C(I, E)` and `.const I e₀`. Alternative: refer to the fiber type explicitly: `(I := PUnit)` — the parser issue. Hmm — actually the parse error earlier ("unexpected token ':='") — was it really from the notation? Let me test: in a file with `open scoped unitInterval`, writing `(I := PUnit)` — the parser: named arg syntax is `(id := term)` where id is an identifier; with notation `I`, the token `I` still parses as an identifier first in this context? The earlier error at 190:44 "unexpected token ':='" suggests it tried to parse `I` as a term (notation) then hit `:=`. Workaround: use `@IsEvenlyCovered.of_trivialization` with explicit args:

```lean
exact @IsEvenlyCovered.of_trivialization X X _ _ (id : X → X) PUnit inferInstance x
  (trivialization_id X) trivial
```
Let me check the exact signature order: `theorem of_trivialization [DiscreteTopology I] {x : X} {t : Trivialization I f} : ...` in namespace IsEvenlyCovered with variables {f : E → X} {I} [TopologicalSpace I]. So explicit params: E, X, [TopologicalSpace E], [TopologicalSpace X], f, I, [TopologicalSpace I], [DiscreteTopology I], x, t, then membership. Using @: `@IsEvenlyCovered.of_trivialization X X _ _ id PUnit _ inferInstance x (trivialization_id X) trivial` — 11 args + membership. Since E = X = X, f = id. Hmm — the instance `_` for TopologicalSpace PUnit ✓. Let me instead keep term-mode but avoid the `I` name issue by using an explicit type ascription to force I := PUnit: `IsEvenlyCovered.of_trivialization (t := trivialization_id X) (I := _) trivial`? no. Use `(show ...)`? Simplest is the @ version. Actually cleaner: `IsEvenlyCovered.of_trivialization (x := x) (t := trivialization_id X) (I := PUnit)` — no, same parser problem.

Hmm wait — actually maybe the notation `I` is only active with `open scoped unitInterval`... and maybe I can avoid the scoped notation by using `Set.Icc (0:ℝ) 1` explicitly where needed and NOT opening unitInterval: then `(I := PUnit)` parses fine. Where do I use `I`? `C(I, E)`, `.const I e₀`, `(fun f : C(I, E) => f 1)`. Replace with `C(Set.Icc (0 : ℝ) 1, E)` — verbose but fine... hmm, but does `C(I, X)` in Lifting.lean's theorems elaborate? `hp.liftPath γ.toContinuousMap ...` — liftPath's γ : C(I, X) — the notation I is expanded at ELABORATION in Lifting.lean? No — notation expands at parse; in Lifting.lean they wrote `C(I, X)` which parsed to `C(Set.Icc (0:ℝ) 1, X)` — the notation is just sugar for the same term. So in my file I can write `C(Set.Icc (0 : ℝ) 1, E)` directly — defeq identical. So: drop `open scoped unitInterval`, replace all `I` with `Set.Icc (0 : ℝ) 1` — wait but `.const I e₀` — `.const (Set.Icc (0:ℝ) 1) e₀` ✓. Hmm, but actually keeping `I` notation and using `@` for of_trivialization also works. The cleaner solution: keep notation, use @ form. Let me do that.

4. The calc issue — let me view the current state of the file around the calc and see.
dsh: reasoning:
The calc step 1 proof: `Γ.target.symm.trans (congrArg (fun f : C(I, E) => f 1) hΓeq)` — hmm: Γ.target.symm : e₁ = Γ 1. congrArg ... hΓeq : Γ.toContinuousMap 1 = hp.liftPath γ.toContinuousMap e₀ γ.source 1. trans needs Γ.target.symm's RHS (= Γ 1) to match the second's LHS (Γ.toContinuousMap 1) — DEFEQ issue again probably. Error at line 122: "left-hand side is e₁ but expected e₀" — hmm, that's odd — expected e₀ means the calc previous RHS... the calc chain: the FIRST step LHS = e₁ — "expected e₀"?! How can the first step expect e₀? Unless... the calc elaborator processes the FIRST line as `e₁ = hp.liftPath ... := by exact ...` — wait, the error is at line 122 col 4 — that's the `e₁ = ...` line itself. "invalid 'calc' step, left-hand side is e₁ : E but is expected to be e₀ : E" — Hmm, in calc, after processing step 1's RHS, step 2's LHS is `_`. The error about LHS = e₁ at the STEP-1 line... Actually I think calc elaboration is: first `calc e₁ = RHS₁ := prf₁` — this is a step with LHS e₁, and the "expected" LHS is the PREVIOUS step's RHS — for the first step, previous RHS = e₁? Hmm — the first line's LHS is e₁ explicitly, expected e₁ — fine. So error must be at the SECOND line — line 122 in the error listing might refer to line 124/125... Line 122 col 4 = `e₁ = hp.liftPath γ.toContinuousMap e₀ γ.source 1 := by` — hmm.

Wait, maybe the problem is different: the calc elaborator determined step 1's PROOF has type `e₁ = e₀`?!?! That would happen if `Γ.target.symm.trans (...)` — hmm no.

Hold on — let me reconsider: maybe the error means: at the second step (line 124-125), the `_` LHS = step1 RHS = `hp.liftPath γ.toContinuousMap e₀ γ.source 1` — but the proof `congrArg (fun f => f 1) hsame` has type `hp.liftPath γ.toContinuousMap e₀ γ.source 1 = hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1` — that matches. But the error says "left-hand side is e₁" — maybe the calc elaborator isn't finding step 1's proof to typecheck, so step 1 contributed RHS = e₁ (identity?) — no...

Let me just debug interactively: replace the calc with explicit trans chains and see the exact error:

```lean
  have h1 : e₁ = hp.liftPath γ.toContinuousMap e₀ γ.source 1 := by
    exact Γ.target.symm.trans (congrArg (fun f : C(I, E) => f 1) hΓeq)
```
If this fails, I'll see the real error. The likely culprit: `Γ.target : Γ 1 = e₁` — hmm — `Γ 1` — what does `1` elaborate to here? Γ : Path e₀ e₁ — CoeFun: Path coerces to function I → X — `1 : I`? unitInterval `I = Set.Icc (0:ℝ) 1` and `(1 : I)` — the CoeFun for Path — `Γ 1` ✓. And `Γ.toContinuousMap 1` — C(I,X) applied to 1 ✓. trans: e₁ = Γ 1 → Γ 1 = ... — but congrArg gives `Γ.toContinuousMap 1 = ...`. `Γ 1` vs `Γ.toContinuousMap 1` — defeq? Γ.toContinuousMap — for `structure Path extends C(I,X)`: the field access `Γ.toContinuousMap` gives the parent struct value; `Γ 1` — CoeFun Path — mathlib defines `instance : CoeFun (Path x y) (fun _ => I → X)` — how? Probably `⟨fun γ => γ.toContinuousMap⟩` or `⟨Path.toFun⟩`... If CoeFun Path = `fun γ => γ.toContinuousMap.toFun` then Γ 1 = Γ.toContinuousMap.toFun 1 = Γ.toContinuousMap 1 (via C's CoeFun) — defeq chain ✓ should hold...

Hmm, but the error "left-hand side is e₁ but is expected to be e₀" — WAIT. I misread. Look again: line 122 is `e₁ = ...` — error "invalid 'calc' step, left-hand side is e₁ : E but is expected to be e₀ : E" — the elaborator's "expected LHS" for the FIRST step is `e₀`?!? That means the OVERALL calc goal... OH!!! I see — the enclosing theorem's goal is `e₀ = e₁` (theorem conclusion!). The calc block must produce a proof of the goal `e₀ = e₁`?? No — calc produces `LHS_first = RHS_last` = e₁ = e₀ — the goal is e₀ = e₁ — need `.symm` or reverse the calc! THE GOAL IS e₀ = e₁ but my calc proves e₁ = e₀!! THAT's the "expected e₀" error — the calc's overall expected result is the goal `e₀ = e₁`... hmm, but the error phrasing "left-hand side is e₁ but expected e₀" — YES: the calc's first LHS must match the goal's LHS e₀. 

Fix: swap the calc direction — prove e₀ = e₁: 
```lean
  calc
    e₀ = hp.liftPath γ₀.toContinuousMap e₀ γ₀.source 1 := hEnd.symm
    _ = hp.liftPath γ.toContinuousMap e₀ γ.source 1 := (congrArg (fun f : C(I, E) => f 1) hsame).symm
    _ = e₁ := ...
```
last step: hp.liftPath γ.toContinuousMap e₀ γ.source 1 = e₁ — via hΓeq: congrArg (fun f => f 1) hΓeq : Γ.toContinuousMap 1 = hp.liftPath γ.. 1 — so (congrArg ... hΓeq).symm : hp.liftPath γ.. 1 = Γ.toContinuousMap 1, then .trans Γ.target : = e₁ — with the defeq worry again. Fine, try.
dsh: reasoning:
Almost there! The `of_trivialization` returns `IsEvenlyCovered f x I` — with I = PUnit, but I need `IsEvenlyCovered id x (id ⁻¹' {x})`. There's `to_isEvenlyCovered_preimage` (line 115): `theorem to_isEvenlyCovered_preimage {x : X} (h : IsEvenlyCovered f x I) : IsEvenlyCovered f x (f ⁻¹' {x})` — converts via the fiber homeomorphism. So:

```lean
exact (@IsEvenlyCovered.of_trivialization X X _ _ (id : X → X) PUnit _ inferInstance x
  (trivialization_id X) trivial).to_isEvenlyCovered_preimage
```
✓. Fix the linter warning: `right_inv' p _ := by ext <;> rfl` → `by ext; rfl`? Hmm — `ext` on a product generates 2 goals (or 1 for PUnit?) — the warning suggests `ext; rfl`. For p : X × PUnit product ext gives two goals: p.1 and p.2 — ext on X (no ext lemma? might fail) — hmm `right_inv' p _ := by ext <;> rfl` worked — replace with `by ext; rfl`? If ext generates multiple goals, `;` is needed. Warning says `tac1 <;> tac2` where `(tac1; tac2)` would suffice — meaning ext produced ONE goal (PUnit has unique ext, X has no ext instance → only the PUnit goal). So `by ext; rfl` ✓.
dsh: reasoning:
Universe metavariable for PUnit's universe. `PUnit` without explicit universe level created ?u.20. Fix: `PUnit.{0}`? Hmm — with `X : Type*` at universe u, PUnit should be at any level — but the declaration can't contain a metavariable. Specify `PUnit.{0}`? Hmm, but X : Type* (u_1) — PUnit.{0} works (0 ≤ any). Or make it `PUnit` with explicit universe: `PUnit.{u}`? Since the theorem is `(X : Type*)` — inside, u_1 is fixed. Using `PUnit.{0}`... hmm the universe of the result — IsEvenlyCovered f x I — I : Type — PUnit.{0} fine. But is `DiscreteTopology PUnit` available at {0}? Yes. Alternatively `ULift PUnit`? No — just write `PUnit.{0}`? Hmm — but wait: the fiber type must just be a type; PUnit.{0} works. Actually hmm, will `.to_isEvenlyCovered_preimage` produce `IsEvenlyCovered id x (id ⁻¹' {x})` — the fiber `id ⁻¹' {x} : Set X` at universe u_1 — fine.

Actually cleaner: avoid the universe issue by giving PUnit the same universe as X: `PUnit.{u_1}`? I can't write u_1 directly. `PUnit` with `universe u` declared and X : Type u: let me restate the theorem with explicit universe: `theorem isCoveringMap_id (X : Type u) [TopologicalSpace X] : IsCoveringMap (id : X → X)` and use `PUnit.{u}`? Hmm — PUnit.{u} : Type u ✓. Let me use `universe u` and PUnit.{u}. Wait — but actually simplest: `PUnit.{0}`? hmm — the universe metavariable arises because the elaborator couldn't decide. Explicit `PUnit.{0}` might not typecheck with universe polymorphism of of_trivialization: I : Type u? — of_trivialization's I is Type* — any level ✓. Let me just add `universe u` and declare X : Type u, use PUnit.{u}.
dsh: reasoning:
Wait — `trivialization_id X` has type `Bundle.Trivialization PUnit (id : X → X)` — with PUnit at WHICH universe? trivialization_id's PUnit is also ambiguous — its declaration `def trivialization_id (X : Type*) [TopologicalSpace X] : Bundle.Trivialization PUnit (id : X → X)` — that would have the same metavariable problem! Unless in a `def` with autoImplicit the elaborator picks `PUnit.{u}` matching X's universe via the field types (target : Set (X × PUnit)). In the def, the expected type constrains PUnit's universe? Not strictly — X × PUnit could have PUnit at any level. Hmm — the def compiled earlier (no error at its declaration), so the elaborator resolved it (probably to the max of universes used... or it's fine because defs can have their universe params inferred when applied). For the theorem, the metavariable remained in the *theorem's type*? The error said "contains universe level metavariables at the expression" in the declaration BODY. Fix trivialization_id too: give it `PUnit.{u}`:

Actually — did trivialization_id compile? Earlier error only complained about isCoveringMap_id. So trivialization_id's universe was resolved (perhaps as PUnit.{u_1} via some constraint). To be safe, let me make both explicit with `universe u` and PUnit.{u}.
dsh: reasoning:
Also need `universe u` declared at the top of the namespace. Let me add it near the top (after imports). The file has `noncomputable section` etc. Let me add `universe u` after namespace or before. Actually with `namespace Poincare.D12.TriangulationTopology` and the theorem in it — `universe u` can be declared inside or outside the namespace. Add after `open Set Function` line.
dsh: reasoning:
**CoveringLemma.lean compiles!** Now let me quickly verify the main theorem statements via #check and then move to the AntipodalQuotient module. Let me write a quick check file... actually just proceed to write AntipodalQuotient.lean and check everything at the end.

For AntipodalQuotient.lean, I need:
1. `antipodalSmul` : Multiplicative (ZMod 2) → Sphere n → Sphere n.
2. instances: SMul, MulAction, ContinuousConstSMul, IsCancelSMul.
3. `locallyCompactSpace_of_compact_t2` (generic).
4. `rpCovering`: IsCoveringMap (Quotient.mk (MulAction.orbitRel ...)).
5. RP n abbrev + CompactSpace/T2Space/Nonempty/PathConnectedSpace instances.
6. `spherePathConnectedSpace_of_pos` via isPathConnected_sphere.
7. `sphericalSpaceFormRecognition` theorem.

Details:

For the action: 
```lean
def antipodalSmul (g : Multiplicative (ZMod 2)) (x : Sphere n) : Sphere n :=
  if g = Multiplicative.ofAdd 1 then -x else x

instance antipodalSMul : SMul (Multiplicative (ZMod 2)) (Sphere n) := ⟨antipodalSmul n⟩
```
Wait — `Multiplicative.ofAdd 1` — the nontrivial element: `(Multiplicative.ofAdd (1 : ZMod 2))`. In `Multiplicative G` notation, elements are `Multiplicative.ofAdd g` — also there's notation... `(1 : Multiplicative (ZMod 2))` = ofAdd 0 = unit ✓. Nontrivial: `Multiplicative.ofAdd (1 : ZMod 2)`.

Hmm — `Multiplicative` is `def Multiplicative (G) := G` with `ofAdd : G ≃ Multiplicative G`? In mathlib, `Multiplicative α` is a structure/def with `ofAdd` and `toAdd`. `Multiplicative.ofAdd 1` — fine.

Actually — wait. Is `Multiplicative (ZMod 2)` the cleanest? Alternatively use `ZMod 2` directly with SMul defined to make 1 the nontrivial... but MulAction needs Monoid (ZMod 2) — multiplication monoid — (1 : ZMod 2) must act trivially. So smul g x := if g = 0 then x else -x with one_smul: 1 • x = -x ≠ x. FAILS. So Multiplicative it is.

MulAction proof:
```lean
instance antipodalMulAction : MulAction (Multiplicative (ZMod 2)) (Sphere n) where
  smul := antipodalSmul n
  one_smul x := by simp [antipodalSmul]
  mul_smul g h x := by
    by_cases hg : g = Multiplicative.ofAdd (1 : ZMod 2) <;> by_cases hh : h = Multiplicative.ofAdd (1 : ZMod 2) <;> simp [antipodalSmul, hg, hh]
```
Hmm — `by_cases hg : g = 1` — for the cases where g, h ∈ {1, m}: four cases; `simp [antipodalSmul, hg, hh]` needs to evaluate `(g * h)` — g*h for the nontrivial elements: m*m = 1 — simp needs to know `Multiplicative.ofAdd (1 : ZMod 2) * Multiplicative.ofAdd (1 : ZMod 2) = 1` — provable by simp? `(1 : ZMod 2) + (1 : ZMod 2) = 0` — `simp` knows? norm_num can't handle ZMod 2 addition? It can: `norm_num` handles ZMod 2? There's `norm_num` support for ZMod p arithmetic — yes `norm_num` proves (1 : ZMod 2) + 1 = 0? It should (via `norm_num1`... ZMod is a norm_num extension ✓). In mul_smul for the m,m case: (m * m) • x = x = m • (m • x) = -(-x) = x — simp [antipodalSmul, hg, hh] reduces LHS `antipodalSmul n (m*m) x` = if m*m = m then -x else x — needs m*m ≠ m (i.e., m*m = 1) — simp needs the fact. Add helper: 
```lean
lemma antipodal_mul_self : Multiplicative.ofAdd (1 : ZMod 2) * Multiplicative.ofAdd (1 : ZMod 2) = 1 := by
  norm_num? -- (1:ZMod 2) + 1 = 0
```
`Multiplicative` multiplication = addition in ZMod 2: `ofAdd a * ofAdd b = ofAdd (a + b)` — simp [Multiplicative]... Let me write: `change Multiplicative.ofAdd ((1 : ZMod 2) + 1) = (1 : Multiplicative (ZMod 2))` hmm. Actually `norm_num` on the goal with Multiplicative: `norm_num` might handle `(Multiplicative.ofAdd (1 : ZMod 2)) * (Multiplicative.ofAdd (1 : ZMod 2))` → reduces to `ofAdd ((1 : ZMod 2) + 1)` = `ofAdd 0` = 1? The `=` to `1` — `(1 : Multiplicative (ZMod 2))` unfolds to `ofAdd 0`? `(1 : Multiplicative G) = ofAdd (1 : G)`?? NO — in Multiplicative G, 1 = ofAdd (0 : G) (the additive identity). norm_num might struggle; use `decide`? `norm_num` has ZMod support. I'll try `norm_num` and fall back to `fin_cases`-style: 

```lean
lemma zmod2_eq_zero_or_one (a : ZMod 2) : a = 0 ∨ a = 1 := by
  have hval : a.val = 0 ∨ a.val = 1 := by
    have := ZMod.val_lt a
    omega
  rcases hval with h | h
  · left; exact ZMod.val_injective h -- check name
  · right; exact ZMod.val_injective (by simpa using h) -- 1.val = 1
```
Need `ZMod.val_injective` — check existence; also `(1 : ZMod 2).val = 1` — by rfl? `norm_num`? Let me check `ZMod.val` of 1: `(1 : ZMod 2).val = 1` — norm_num or rfl. I'll write `ZMod.val_injective (h.trans (by norm_num : (1 : ZMod 2).val = 1))` hmm. Let me instead use a cleaner approach: `norm_num` directly on goals like `(a : ZMod 2) = 0 ∨ (a : ZMod 2) = 1`? norm_num handles ZMod 2 elements with closed proofs via `native_decide`?? NO — native_decide is FORBIDDEN in deliverables (hard acceptance rules: no native_decide). norm_num is fine but doesn't decide arbitrary ZMod elements... hmm — for `a : ZMod 2` arbitrary, `norm_num` can't case-split. The val approach is right.

Actually — even simpler: use `fin_cases a`? `fin_cases` works on any `a : α` with [Fintype α]? No — fin_cases works on `Finset`/`Fin`/`Fintype` elements via exhaustive case split! Yes — `fin_cases a` for a : ZMod 2 with Fintype (ZMod 2) — I believe fin_cases supports any Fintype. Let me try that first; fallback to val.

For ContinuousConstSMul:
```lean
instance antipodalContinuousConstSMul : ContinuousConstSMul (Multiplicative (ZMod 2)) (Sphere n) where
  continuous_const_smul g := by
    by_cases hg : g = Multiplicative.ofAdd (1 : ZMod 2)
    · simp [antipodalSmul, hg, continuous_neg?]  -- x ↦ -x continuous on Sphere n
    · simp [antipodalSmul, hg]  -- identity
```
`Continuous (fun x : Sphere n ↦ -x)` — `continuous_neg`? For the subtype: `Continuous fun x : Sphere n => -(x : EuclideanSpace ℝ (Fin (n+1)))` as a map Sphere n → Sphere n — by `fun_prop`? `fun_prop` should solve continuity of negation on the subtype (composition of continuous_neg and coercion, then subtype_mk). Let me write `· exact by fun_prop` for the neg case and `simpa [antipodalSmul, hg] using continuous_id` for identity.

Hmm — `ContinuousConstSMul` requires `continuous_const_smul : ∀ g : G, Continuous fun x : X => g • x` ✓.

For IsCancelSMul: extends IsLeftCancelSMul with right_cancel':
```lean
instance antipodalIsCancelSMul : IsCancelSMul (Multiplicative (ZMod 2)) (Sphere n) where
  left_cancel' g x y h := by
    by_cases hg : g = Multiplicative.ofAdd (1 : ZMod 2)
    · simpa [antipodalSmul, hg] using congr_arg Neg.neg h
    · simpa [antipodalSmul, hg] using h
  right_cancel' g h x hx := by
    by_cases hg : g = Multiplicative.ofAdd (1 : ZMod 2) <;>
    by_cases hh : h = Multiplicative.ofAdd (1 : ZMod 2)
    · simp [antipodalSmul, hg, hh, neg_eq_iff_eq_neg? ] at hx  -- -x = -x → trivial; g = h ✓ rfl
    · -- g = m, h = 1: m • x = 1 • x → -x = x → x = 0 contradiction with ‖x‖ = 1
    · -- g = 1, h = m: x = -x → contradiction
    · simp [hg, hh] -- g = h = 1
```
IsLeftCancelSMul: field `left_cancel' : ∀ (a : G) (b c : P), a • b = a • c → b = c`.

The contradictions: from -x = x (on the sphere): x = 0: `have : (x : EuclideanSpace ..) = 0 := by ... ` — from -x = x: `add_eq_zero_iff_eq_neg`? From hx : -x = x (as Sphere elements — subtype equality → coerced equality): `norm_eq_zero.mp (by simpa using congr_arg norm hx)`? norm of -x = norm x = 1; from -x = x: 2•x = 0 → x = 0: `have hx' : (x : E) = 0 := by nlinarith [congrArg ...]`? Let me do: `have hxE : ((m • x : Sphere n) : E) = (x : E) := congrArg Subtype.val hx`; and m • x = -x (by the if): so `-↑x = ↑x` → `2 • ↑x = 0`? hmm: from -x = x: `(↑x + ↑x) = 0` via `neg_eq_iff_add_eq_zero`? `have : (x : E) = 0 := by` — nlinarith can handle: `have h : (2 : ℝ) • (x : E) = 0 := by nlinarith [congrArg Subtype.val hx]`? nlinarith with smul... better: `eq_zero_iff_forall_dist_eq_zero`? Simplest: take norms: `have hn : ‖(x : E)‖ = 0 := by simpa using congrArg norm hxE'` — from -x = x: norm(-x) = norm(x) → 1 = ... no that gives nothing. Correct: -x = x → x = -x → x + x = 0 → ‖x + x‖ = 0. Hmm — from hxE : -↑x = ↑x: `add_eq_zero_iff_eq_neg.mpr hxE.symm : ↑x + ↑x = 0` then `have : ‖↑x‖ = 0 := by simpa using congrArg norm (add_eq_zero_iff_eq_neg.mpr hxE.symm)`? ‖x+x‖ = ‖0‖ = 0; but I want contradiction with ‖x‖ = 1: ‖x+x‖ = 2‖x‖ = 2 ≠ 0. `have hxnorm : ‖(x : E)‖ = 1 := x.2` (x ∈ Metric.sphere 0 1 → mem_sphere_iff_norm — `x.2 : x ∈ sphere 0 1` → `mem_sphere_iff_norm.mp x.2 : ‖x - 0‖ = 1` → dist x 0 = 1 = ‖x‖ = 1). Then `have hsum : ‖(x:E) + x‖ = 0 := by rw [hxE]; simp` and `have hsum2 : ‖(x:E) + x‖ = 2 := by rw [norm_add_eq?]` — hmm ‖x + x‖ = ‖2•x‖ = 2‖x‖ = 2: `rw [two_smul]` — for EuclideanSpace: ‖2 • x‖ = ‖2‖ * ‖x‖ = 2*1. `simp [hxnorm]`? `norm_smul` : ‖a • x‖ = ‖a‖ * ‖x‖ ✓. So:
```lean
have hxE : -(x : E) = x := by simpa [antipodalSmul, hg, hh] using congrArg Subtype.val hx
-- hmm careful: hx : g • x = h • x with g = m, h = 1 → m • x = 1 • x → -x = x
have h0 : ‖(x : E)‖ = 0 := by
  have hsum : ‖(x : E) + (x : E)‖ = 0 := by
    rw [← neg_eq_iff_add_eq_zero]  -- hmm
    simpa using congrArg norm hxE -- hxE : -x = x → ‖-x‖ = ‖x‖ — useless!
```
Hmm wait: hxE : -x = x. So x + x = x + (-x) = 0: `have : (x : E) + (x : E) = 0 := by rw [← hxE]; abel`? `rw [← hxE]` rewrites second x... `(x + x) = x + (-x) = 0` — hmm `rw [← hxE]` on goal x+x = 0 — replaces the 2nd x with -x → x + -x = 0 → `simp` ✓. So:
```lean
have hzero : (x : E) = 0 := by
  have hsum : (x : E) + (x : E) = 0 := by rw [← hxE]; simp
  -- 2 • x = 0 → x = 0 (char 0 field): 
  have htwo : (2 : ℝ) • (x : E) = 0 := by simpa [two_smul] using hsum
  exact smul_right_injective (2 : ℝ) (by norm_num : (2 : ℝ) ≠ 0) htwo
have hnorm1 : ‖(x : E)‖ = 1 := by
  exact (mem_sphere_iff_norm (x := x) (y := 0) (r := 1)? ...) 
```
Hmm — x : Sphere n = Metric.sphere 0 1 — x.2 : x ∈ Metric.sphere (0 : E) 1. `Metric.mem_sphere_iff_norm : y ∈ sphere x r ↔ dist y x = r` or `mem_sphere_iff_norm : ‖y - x‖ = r`. There's `mem_sphere_iff_norm : y ∈ Metric.sphere x r ↔ ‖y - x‖ = r`? In mathlib: `Metric.mem_sphere_iff_norm` exists for normed groups? Let me use: `Metric.mem_sphere_iff_norm.mp x.2 : ‖(x : E) - 0‖ = 1` — simpa → ‖x‖ = 1. Then `norm_zero` contradiction: `have : ‖(x : E)‖ = 0 := by rw [hzero]; exact norm_zero; nlinarith [this, hnorm1]`? `norm_num at hnorm1`? `nlinarith` ✓.

OK. Then `rpCovering`:
```lean
noncomputable def antipodalQuotientCovering (n : ℕ) :
    IsCoveringMap (Quotient.mk (MulAction.orbitRel (Multiplicative (ZMod 2)) (Sphere n))) :=
  (isQuotientCoveringMap_quotientMk_of_properlyDiscontinuousSMul (G := Multiplicative (ZMod 2))
    (E := Sphere n)).isCoveringMap
```
Check the theorem's name & hypotheses: `isQuotientCoveringMap_quotientMk_of_properlyDiscontinuousSMul [IsCancelSMul G E] : IsQuotientCoveringMap (Quotient.mk (MulAction.orbitRel G E)) G` in `section ProperlyDiscontinuousSMul` with `variable [ProperlyDiscontinuousSMul G E] [LocallyCompactSpace E] [T2Space E]`. ✓ all instances available (need my locallyCompact instance).

`locallyCompactSpace_of_compact_t2`:
```lean
instance locallyCompactSpace_of_compact_t2 (X : Type u) [TopologicalSpace X] [CompactSpace X]
    [T2Space X] : LocallyCompactSpace X where
  local_compact_nhds x n hn := by
    have hreg : RegularSpace X := inferInstance
    have hlift : (𝓝 x).lift' closure = 𝓝 x := RegularSpace.lift'_nhds_closure x -- name check
    have hmem : n ∈ (𝓝 x).lift' closure := by rwa [hlift]
    rcases mem_lift'_sets.mp hmem with ⟨U, hU, hUn⟩  -- U ∈ 𝓝 x ∧ closure U ⊆ n
    exact ⟨closure U, mem_of_superset hU subset_closure, hUn, isClosed_closure.isCompact⟩
```
Need: `RegularSpace.lift'_nhds_closure` — from Regular.lean line 177: `theorem lift'_nhds_closure (x : X) : (𝓝 x).lift' closure = 𝓝 x := (closed_nhds_basis x).lift'_closure_eq_self fun _ => And.right` — in namespace RegularSpace? Line 177 is within `namespace RegularSpace`? The tfae at 79-... and lift'_nhds_closure at 177 — need to check the namespace. `mem_lift'_sets` — exists: `Filter.mem_lift'_sets : s ∈ (f.lift' g) ↔ ∃ t ∈ f, g '' t ⊆ s`? or `(f.lift' g).sets`... The exact API: `mem_lift'_sets : s ∈ f.lift' g ↔ ∃ t ∈ f, g t ⊆ s`? hmm — in mathlib, `Filter.lift'` : `f.lift' g = f.map?`... `mem_lift'_sets {s} : s ∈ f.lift' g ↔ ∃ t ∈ f, g t ⊆ s` — where g applied pointwise?? `Filter.lift' (f : Filter α) (h : Set α → Set β) : Filter β := ... generate {s | ∃ t ∈ f, h t ⊆ s}`. So `mem_lift'_sets.mp hmem : ∃ U ∈ 𝓝 x, closure U ⊆ n` ✓ — the exact lemma name: `Filter.mem_lift'_sets`? I recall `mem_lift'_sets`. And `isClosed_closure.isCompact` — in compact space closed → compact: `IsClosed.isCompact` ✓ (requires CompactSpace instance).

`RegularSpace` instance from [CompactSpace X] [T2Space X]: via `WeaklyLocallyCompactSpace` (CompactSpace → WeaklyLocallyCompactSpace instance ✓) + R1 (T2 → R1Space ✓ instance `T2Space.r1Space`?) + `[WeaklyLocallyCompactSpace X] [R1Space X] → RegularSpace X` ✓ (Regular.lean line 132). And T2Space → R1Space instance: in Separation/Hausdorff? `instance T2Space.to_R1Space`? something like `instR1SpaceOfT2Space` — likely exists ✓. So `inferInstance` for RegularSpace X should work.

`spherePathConnectedSpace`:
```lean
theorem spherePathConnectedSpace_of_pos {n : ℕ} (hn : 0 < n) : PathConnectedSpace (Sphere n) := by
  have hrank : 1 < Module.rank ℝ (EuclideanSpace ℝ (Fin (n + 1))) := by
    -- need the rank computation
```
`Module.rank` = `Module.finrank` for finite-dimensional: `Module.finrank_euclideanSpace : finrank ℝ (EuclideanSpace ℝ ι) = Fintype.card ι`? Let me check the lemma names: `finrank_euclideanSpace` and `Module.rank_eq_finrank`? Hmm — `Module.rank` for a finite-dim space = finrank via `Module.finrank_eq_rank`? hmm — but `Module.rank ℝ E` — with E finite dimensional — `Module.finrank_eq_rank : finrank K V = Module.rank K V`? I need 1 < rank. There might be an instance-based route: the theorem `isPathConnected_sphere` needs `1 < Module.rank ℝ E`. For EuclideanSpace: `Module.finrank_euclideanSpace : Module.finrank ℝ (EuclideanSpace ℝ ι) = Fintype.card ι`? and `Module.finrank_eq_rank`? Hmm — in newer mathlib, `Module.rank` is a Cardinal and `Module.finrank` nat; `Module.finrank_eq_rank`?? There's `Module.finrank_eq_toNat_rank`? hmm. Let me grep during dev. Alternatively avoid rank: is there `isPathConnected_sphere` variant by finrank? Just grep.

Then:
```lean
  have hpc : IsPathConnected (Metric.sphere (0 : EuclideanSpace ℝ (Fin (n + 1))) 1) :=
    isPathConnected_sphere hrank (0 : _) (r := 1) (by norm_num)
  -- convert to PathConnectedSpace (Sphere n)
```
`IsPathConnected s : IsPreconnected s ∧ ∀ x ∈ s, ∀ y ∈ s, JoinedIn s x y` — instance `PathConnectedSpace s` from IsPathConnected: is there one? In PathConnected.lean: `instance (priority := 100) IsPathConnected.pathConnectedSpace`? Let me grep. Probably exists: `pathConnectedSpace_iff_isPathConnected`? I'll check.

Also — `isPathConnected_sphere`'s h : 1 < Module.rank ℝ E — with hr : 0 ≤ r. And nonempty: IsPathConnected requires nonempty implicitly? The theorem states for h and hr. OK.

The `RP n`:
```lean
abbrev RealProjective (n : ℕ) := Quotient (MulAction.orbitRel (Multiplicative (ZMod 2)) (Sphere n))
```
Hmm — wait, `MulAction.orbitRel` needs the MulAction instance — fine, instance exists.

instances:
```lean
instance rpCompact (n) : CompactSpace (RealProjective n) := Quotient.compactSpace? — inferInstance
instance rpT2 (n) : T2Space (RealProjective n) := inferInstance -- via t2Space_of_properlyDiscontinuousSMul_of_t2Space
instance rpNonempty (n) : Nonempty (RealProjective n) := ⟨Quotient.mk _ (sphereNonempty n).some?⟩ 
instance rpPathConnected {n} [NeZero n]? (hn : 0 < n) : PathConnectedSpace (RealProjective n) := Quotient.instPathConnectedSpace
```
Hmm — careful with `Quotient.instPathConnectedSpace` — instance needs the Setoid — orbitRel ✓.

`sphericalSpaceFormRecognition`:
```lean
theorem sphericalSpaceFormRecognition (G : Type u) [Group G] [Finite G] [MulAction G (Sphere 3)]
    [ContinuousConstSMul G (Sphere 3)] [IsCancelSMul G (Sphere 3)]
    (hsc : SimplyConnectedSpace (Quotient (MulAction.orbitRel G (Sphere 3)))) :
    Nonempty (Quotient (MulAction.orbitRel G (Sphere 3)) ≃ₜ Sphere 3) := by
  let p : Sphere 3 → Quotient (MulAction.orbitRel G (Sphere 3)) := Quotient.mk (MulAction.orbitRel G (Sphere 3))
  have hp : IsCoveringMap p :=
    (isQuotientCoveringMap_quotientMk_of_properlyDiscontinuousSMul (G := G) (E := Sphere 3)).isCoveringMap
  exact (coveringOfSimplyConnectedIsHomeo p hp Quot.mk_surjective).map Homeomorph.symm
```
Hmm — `Quot.mk_surjective` — for the specific Quotient: `Quotient.mk_surjective`? `Quot.mk_surjective : Surjective (Quot.mk r)` ✓. Homeomorph.symm : E ≃ₜ X → X ≃ₜ E ✓ Nonempty.map.

Instance requirements inside: [LocallyCompactSpace (Sphere 3)] ✓ my instance; [T2Space (Sphere 3)] ✓; [ProperlyDiscontinuousSMul G (Sphere 3)] ✓ via Finite; [IsCancelSMul] hypothesis ✓; [ContinuousConstSMul] ✓ hypothesis.

And the base's T2 for coveringOfSimplyConnectedIsHomeo: [T2Space (Quotient ...)] — instance `t2Space_of_properlyDiscontinuousSMul_of_t2Space` ✓ automatically. PathConnectedSpace E = Sphere 3 ✓ via spherePathConnectedSpace_of_pos (by norm_num). SimplyConnectedSpace base = hsc ✓. CompactSpace E ✓ sphereCompactSpace.

The theorem's type must mention `p` as Quotient.mk — elaboration: `Quotient (MulAction.orbitRel G (Sphere 3))` — need the orbitRel setoid instance — fine.

Also — wait: in the theorem statement I should double-check the class `MulAction` instance for G on Sphere 3 — arbitrary G given as hypothesis [MulAction G (Sphere 3)] ✓.

Non-vacuity witness block at the end:
```lean
example : MulAction (Multiplicative (ZMod 2)) (Sphere 3) := inferInstance
example : ContinuousConstSMul (Multiplicative (ZMod 2)) (Sphere 3) := inferInstance
example : IsCancelSMul (Multiplicative (ZMod 2)) (Sphere 3) := inferInstance
example : IsCoveringMap (Quotient.mk (MulAction.orbitRel (Multiplicative (ZMod 2)) (Sphere 3))) := antipodalQuotientCovering 3
example : CompactSpace (RealProjective 3) := inferInstance
example : T2Space (RealProjective 3) := inferInstance
example : PathConnectedSpace (RealProjective 3) := inferInstance
```
And the honest note: for the antipodal G the base is NOT simply connected (RP³) — π₁(RP³) = ℤ/2 is a separate fact (node 6/van Kampen), so `sphericalSpaceFormRecognition`'s hypothesis hsc genuinely discriminates (no false RP³ ≅ S³ claim).

Also `Sphere 3` — Sphere n = Metric.sphere 0 1 in EuclideanSpace (Fin (n+1)) ✓ = S³.

Now let me check remaining API names: `Module.finrank_euclideanSpace`, `Module.rank`... , `IsPathConnected` → PathConnectedSpace instance, `RegularSpace.lift'_nhds_closure` namespace, `mem_lift'_sets`, `ZMod.val_injective`? etc. Batch grep.
dsh: reasoning:
- `RegularSpace.lift'_nhds_closure` — line 177 is after the class — need to check the namespace — the grep for "namespace RegularSpace" in Regular.lean found nothing?! Let me check: line 177 `theorem lift'_nhds_closure (x : X)` — under which namespace? The file structure: `class RegularSpace` at 79, then theorems in `namespace RegularSpace`? The grep found no "namespace RegularSpace" — maybe it's under a `section` with `variable [RegularSpace X]`. So the theorem is in the ROOT namespace? Unlikely — let me check more carefully. Also `mem_lift'_sets (hh : Monotone h)` — needs Monotone closure ✓.

- IsPathConnected at 401 — check for PathConnectedSpace instance from IsPathConnected.
dsh: reasoning:
- `RegularSpace.lift'_nhds_closure` is in `section RegularSpace` — a section, not a namespace, so the theorem is at ROOT level: `lift'_nhds_closure` (root namespace). Also there's `exists_mem_nhds_isClosed_subset {x} {s} (h : s ∈ 𝓝 x) : ∃ t ∈ 𝓝 x, IsClosed t ∧ t ⊆ s` — even better for my locally compact instance! s = closure t: IsCompact (closure t) ✓ from IsClosed... wait IsCompact needs compactness: `hcl.isCompact` ✓, and `closure t ⊆ s` hmm I need a COMPACT set ⊆ n — `closure t` is closed hence compact ✓, t ∈ 𝓝 x gives closure t ∈ 𝓝 x ✓ (t ⊆ closure t). So:

```lean
instance locallyCompactSpace_of_compact_t2 (X : Type u) [TopologicalSpace X] [CompactSpace X]
    [T2Space X] : LocallyCompactSpace X where
  local_compact_nhds x n hn := by
    obtain ⟨t, ht, hclosed, htn⟩ := exists_mem_nhds_isClosed_subset hn
    exact ⟨closure t, mem_of_superset ht subset_closure, htn.trans? subset_closure? ...⟩
```
Hmm: need closure t ⊆ n: from t ⊆ n (htn) and n... closure t ⊆ n requires n closed — NOT given. exists_mem_nhds_isClosed_subset gives closed t ⊆ n — then s := t itself: IsClosed t → IsCompact t ✓; t ∈ 𝓝 x ✓; t ⊆ n ✓. So s = t directly! Even simpler:

```lean
  local_compact_nhds x n hn := by
    obtain ⟨t, ht, hclosed, htn⟩ := exists_mem_nhds_isClosed_subset hn
    exact ⟨t, ht, htn, hclosed.isCompact⟩
```
`exists_mem_nhds_isClosed_subset (h : s ∈ 𝓝 x)` — needs [RegularSpace X] — hmm, is this theorem under `variable [RegularSpace X]`? It's in section RegularSpace (73-338) — so it needs [RegularSpace X] ✓ (which I have by inference).

`hclosed.isCompact` — `IsClosed.isCompact [CompactSpace X]` ✓.

- `IsPathConnected` — no direct instance to PathConnectedSpace found yet. Let me search for how to convert: `pathConnectedSpace` ↔ IsPathConnected: there's probably `pathConnectedSpace_iff_isPathConnected` or an instance `instPathConnectedSpace`... Let me grep. If missing, construct manually:

```lean
instance pathConnectedSpace_of_isPathConnected {X} [TopologicalSpace X] (s : Set X)
    (h : IsPathConnected s) : PathConnectedSpace s where
  nonempty := ... -- h.nonempty : s.Nonempty — need Nonempty s (subtype) — Set.Nonempty.to_subtype?
  joined x y := by
    obtain ⟨b, hb, hb'⟩ := h
    exact (hb' x.2).symm.trans (hb' y.2)  -- JoinedIn → Joined? joinedIn_iff_joined!
```
`JoinedIn F x y` vs `Joined x y` — `joinedIn_iff_joined (x_in : x ∈ F) (y_in : y ∈ F) : JoinedIn F x y ↔ Joined x y` ✓ (line 209). And nonempty: `h.nonempty : s.Nonempty` = ∃ x ∈ s → Nonempty s subtype: `h.nonempty.to_subtype`? There's `Set.Nonempty.to_subtype : s.Nonempty → Nonempty s`? hmm — or just `<⟨h.nonempty.some, h.nonempty.some_mem⟩`. `Set.Nonempty` — h.nonempty.some : X, .some_mem : ∈ s ✓.

Actually — maybe there IS an instance; let me grep "pathConnectedSpace" ↔ IsPathConnected.
dsh: reasoning:
`isPathConnected_iff_pathConnectedSpace : IsPathConnected F ↔ PathConnectedSpace F` ✓ — I can use `.mp` to get the instance as a local `haveI`. Also `Function.Surjective.pathConnectedSpace` — for the quotient RP: `Quotient.mk'_surjective.pathConnectedSpace continuous_coinduced_rng` exists as an instance? Line 621-623 shows `instance Quotient.instPathConnectedSpace`? (Earlier grep showed `instance Quotient.instPathConnectedSpace {s : Setoid X} [PathConnectedSpace X]` at line 621 ✓.)

Now finrank/rank for the sphere: let me check the module rank API for EuclideanSpace.
dsh: reasoning:
`finrank_euclideanSpace : Module.finrank ℝ (EuclideanSpace ℝ ι) = Fintype.card ι` (PiL2.lean:203) and `finrank_eq_rank [Module.Finite R M] : ↑(finrank R M) = Module.rank R M`. So:

```lean
have hrank : 1 < Module.rank ℝ (EuclideanSpace ℝ (Fin (n + 1))) := by
  rw [← finrank_eq_rank, finrank_euclideanSpace, Fintype.card_fin]
  norm_num [hn]
```
`finrank_eq_rank` needs [Module.Finite ℝ E] — EuclideanSpace ✓ instance. `↑(finrank ℝ E) = Module.rank ℝ E` — the nat-cast. `Fintype.card_fin : (Fin n).card = n` ✓. So after rw: `1 < (↑(n+1) : Cardinal)` — `norm_num` with hn : 0 < n → n+1 ≥ 2 → `1 < (n+1 : Cardinal)` — norm_num handles Cardinal casts? `norm_num` can prove `1 < ↑(n + 1)` given 0 < n? Probably via `norm_num [hn]` + omega... Let me use `exact_mod_cast (by omega : 1 < n + 1)` — hmm `exact_mod_cast` on Cardinal? `norm_num` might directly handle `(1 : Cardinal) < (n+1 : ℕ)` — there's `Nat.cast_lt` etc. I'll do:
```lean
    rw [← finrank_eq_rank, finrank_euclideanSpace, Fintype.card_fin]
    exact_mod_cast (by omega : 1 < n + 1)
```
`exact_mod_cast` with Cardinal targets — Cardinal has NatCast ✓ — should work.

Now — `isPathConnected_sphere h (x := 0) (r := 1)` — the theorem: `theorem isPathConnected_sphere (h : 1 < Module.rank ℝ E) (x : E) {r : ℝ} (hr : 0 ≤ r) : IsPathConnected (sphere x r)`. Then:

```lean
theorem spherePathConnectedSpace_of_pos {n : ℕ} (hn : 0 < n) : PathConnectedSpace (Sphere n) := by
  have hrank : 1 < Module.rank ℝ (EuclideanSpace ℝ (Fin (n + 1))) := ...
  have hpc : IsPathConnected (Metric.sphere (0 : EuclideanSpace ℝ (Fin (n + 1))) 1) :=
    isPathConnected_sphere hrank (0 : EuclideanSpace ℝ (Fin (n + 1))) (r := 1) (by norm_num)
  exact isPathConnected_iff_pathConnectedSpace.mp hpc
```

Wait — `isPathConnected_sphere` argument order: `(h : 1 < Module.rank ℝ E) (x : E) {r : ℝ} (hr : 0 ≤ r)`. Note E is a variable; the sphere x r is in E. With r := 1 named.

Also `Sphere n` — Metric.sphere 0 1 — `isPathConnected_iff_pathConnectedSpace.mp hpc : PathConnectedSpace ↥(Metric.sphere 0 1)` = PathConnectedSpace (Sphere n) ✓ (Sphere is an abbrev ✓).

Now — one more check — the ZMod val approach vs fin_cases. Let me just write the file and test `fin_cases g` first. Also check `Multiplicative` API: `Multiplicative.ofAdd`, and equality `(g : Multiplicative (ZMod 2)) = 1`. `(1 : Multiplicative (ZMod 2))` — is there a One instance? Multiplicative G with G additive group — Multiplicative has Group instance ✓ so 1 exists ✓.

Let me also confirm `isQuotientCoveringMap_quotientMk_of_properlyDiscontinuousSMul` name — it's in Quotient.lean: `@[to_additive] lemma _root_.isQuotientCoveringMap_quotientMk_of_properlyDiscontinuousSMul [IsCancelSMul G E] : IsQuotientCoveringMap (Quotient.mk (MulAction.orbitRel G E)) G` — root namespace ✓.

And `t2Space_of_properlyDiscontinuousSMul_of_t2Space` — root instance: `instance (priority := 100) t2Space_of_properlyDiscontinuousSMul_of_t2Space [T2Space T] [LocallyCompactSpace T] [ContinuousConstSMul Γ T] [ProperlyDiscontinuousSMul Γ T] : T2Space (Quotient (MulAction.orbitRel Γ T))` ✓ — so `inferInstance` gives T2Space (RealProjective n) ✓ (needs LocallyCompactSpace (Sphere n) ✓ my instance).

For the theorem `sphericalSpaceFormRecognition` the `Finite G` is needed for ProperlyDiscontinuousSMul G (Sphere 3). Hmm — `Finite.to_properlyDiscontinuousSMul [Finite Γ] : ProperlyDiscontinuousSMul Γ T` ✓.

Also, do I need to register instances for the antipodal action globally? Defining `instance antipodalSMul : SMul ...` globally is fine within my namespace (scoped to the namespace, so no conflict).

One subtlety: `MulAction.orbitRel G (Sphere 3)` — the definition requires `[Monoid G] [MulAction G α]` ✓.

The Quotient type: `Quotient (MulAction.orbitRel G (Sphere 3))` — Quotient needs [Setoid]. ✓.

Let me also add `RealProjective` T2/compact/path-connected/nonempty instance examples.

Also should record the dimension audit: RP n is a quotient of the n-sphere, S³ case.

Now write the file. Also `neg_eq_iff_eq_neg`/negation handling for IsCancelSMul right_cancel': 

```lean
instance antipodalIsCancelSMul : IsCancelSMul (Multiplicative (ZMod 2)) (Sphere n) where
  left_cancel' g x y h := by
    by_cases hg : g = Multiplicative.ofAdd (1 : ZMod 2)
    · have h' : -(x : EuclideanSpace ℝ (Fin (n + 1))) = -(y : EuclideanSpace ℝ (Fin (n + 1))) := by
        simpa [antipodalSmul, hg] using congrArg Subtype.val h
      exact Subtype.ext (neg_injective h')
    · exact Subtype.ext (by simpa [antipodalSmul, hg] using congrArg Subtype.val h)
  right_cancel' g h x hx := by
    by_cases hg : g = Multiplicative.ofAdd (1 : ZMod 2) <;>
      by_cases hh : h = Multiplicative.ofAdd (1 : ZMod 2)
    · subst hg; subst hh; rfl -- hmm subst on Multiplicative elements: hg : g = m — subst replaces g... both replaced — g = h = m — goal g = h → rfl ✓
    · -- g = m, h ≠ m i.e. h = 1: m • x = 1 • x → -x = x → contradiction
      have hx' : -(x : EuclideanSpace ℝ (Fin (n + 1))) = (x : EuclideanSpace ℝ (Fin (n + 1))) := by
        simpa [antipodalSmul, hg, hh] using congrArg Subtype.val hx
      ...
    · -- g ≠ m (g=1), h = m: x = -x contradiction
      have hx' : (x : _) = -(x : _) := by simpa [antipodalSmul, hg, hh] using congrArg Subtype.val hx
      ...
    · -- g = 1 = h
      subst hg; subst hh; rfl — wait: g ≠ m → g = 1? NOT directly — "g ≠ m" doesn't imply g = 1 automatically! Need the 2-element fact. Hmm.
```
Right — the by_cases on `g = m` leaves case `g ≠ m` where I need `g = 1` to compute the smul... but wait — do I need g = 1? `antipodalSmul n g x = if g = m then -x else x` — in the case g ≠ m, `simp [antipodalSmul, hg]` gives `g • x = x` ✓ regardless of WHICH element g is (since g ≠ m → the if gives x) ✓. For the contradiction cases I need `g = 1` or just derive the contradiction from the if-results: case (g = m, h ≠ m): hx : g • x = h • x → simp [antipodalSmul, hg, hh] gives -x = x — contradiction — and the GOAL in that case: g = h — m = h — since h ≠ m, goal false — contradiction ✓ (from -x = x and sphere). So I don't need "g ≠ m → g = 1" — only need the CONTRADICTION to eliminate those cases, and for the (g≠m, h≠m) case: g•x = x = h•x — goal g = h — hmm! From hx : g • x = h • x with both ≠ m: simp gives x = x — NO info! Goal g = h with g, h both ≠ m — but ZMod 2 has only ONE element ≠ m (namely 1)! So need the fact: `∀ g, g = m ∨ g = 1` — the 2-element enumeration. Then case analysis on g ∈ {m, 1}, h ∈ {m, 1}:

```lean
  right_cancel' g h x hx := by
    rcases zmod2Mul_eq_ofAdd_one_or_one g with hg | hg <;>
    rcases zmod2Mul_eq_ofAdd_one_or_one h with hh | hh
    · subst hg; subst hh; rfl
    · subst hg; subst hh
      -- m • x = 1 • x → -x = x
      have hx' : -(x : E) = (x : E) := by simpa [antipodalSmul] using congrArg Subtype.val hx
      -- contradiction with x on the unit sphere
    · subst hg; subst hh
      have hx' : (x : E) = -(x : E) := by simpa [antipodalSmul] using congrArg Subtype.val hx
      ... contradiction
    · subst hg; subst hh; rfl
```
with `zmod2Mul_eq_ofAdd_one_or_one (g : Multiplicative (ZMod 2)) : g = Multiplicative.ofAdd (1 : ZMod 2) ∨ g = 1`:
```lean
lemma zmod2_eq_zero_or_one (a : ZMod 2) : a = 0 ∨ a = 1 := by
  have hlt := ZMod.val_lt a
  have hval : a.val = 0 ∨ a.val = 1 := by omega
  rcases hval with h | h
  · left; exact ZMod.val_injective h   -- name?
  · right; exact ZMod.val_injective (h.trans (by norm_num : (1 : ZMod 2).val = 1))
```
Hmm — need `ZMod.val_injective` and `ZMod.val` of numerals. `ZMod.val_injective` — grep earlier didn't confirm. There's `ZMod.val_injective`? In Defs: `val_injective`? Let me check. Alternative without injectivity: `a = (a.val : ZMod 2)` via `ZMod.natCast_zmod_val : (a.val : ZMod n) = a` — so `a = (a.val : ZMod 2) = (0 : ZMod 2) ∨ ...`: `left; rw [← ZMod.natCast_zmod_val a, h]` ✓ — and `(0 : ZMod 2) = 0` rfl; `(1 : ZMod 2)`: `norm_num`? `(1 : ZMod 2)` — natCast 1 — rfl-ish via norm_num ✓. So:
```lean
lemma zmod2_eq_zero_or_one (a : ZMod 2) : a = 0 ∨ a = 1 := by
  have hval : a.val = 0 ∨ a.val = 1 := by
    have := ZMod.val_lt a
    omega
  rcases hval with h | h
  · left; exact (ZMod.natCast_zmod_val a).symm.trans (by rw [h])
  · right; exact (ZMod.natCast_zmod_val a).symm.trans (by rw [h])
```
And the Multiplicative version:
```lean
lemma zmod2Mul_eq_ofAdd_one_or_one (g : Multiplicative (ZMod 2)) :
    g = Multiplicative.ofAdd (1 : ZMod 2) ∨ g = 1 := by
  rcases zmod2_eq_zero_or_one g.toAdd with h | h
  · right; exact (Multiplicative.ofAdd.injective? ...)
```
Hmm — `Multiplicative.ofAdd (0 : ZMod 2) = 1` — `(1 : Multiplicative G)` unfolds to `ofAdd 0`? `Multiplicative` group instance: one = ofAdd (0 : G) — so `(1 : Multiplicative (ZMod 2)) = Multiplicative.ofAdd (0 : ZMod 2)` by rfl? Probably `norm_num`-able or rfl. Let me: `· right; simpa using congrArg Multiplicative.ofAdd h` — from h : g.toAdd = 0 → ofAdd (g.toAdd) = ofAdd 0 — and g = ofAdd (g.toAdd) by rfl (Multiplicative is a structure with toAdd... `Multiplicative.ofAdd` is an equivalence: `def ofAdd : G ≃ Multiplicative G`. g = ofAdd (g.toAdd)? hmm — Multiplicative α is `def Multiplicative (α : Type u) := α`? In newer mathlib: `def Multiplicative (G : Type*) := G` with `ofAdd : G ≃ Multiplicative G := Equiv.refl _` and `toAdd : Multiplicative G → G`? If it's an Equiv.refl then `g = Multiplicative.ofAdd (1 : ZMod 2)` iff `g = (1 : ZMod 2)` as elements — everything rfl-friendly. And `1 = ofAdd 0` — the One instance: `instance : One (Multiplicative G) := ⟨ofAdd 0⟩`? So `(1 : Multiplicative (ZMod 2))` unfolds to `ofAdd 0`... if ofAdd is Equiv.refl, `1 = ofAdd 0` needs the instance unfolding — by `norm_num`? `change Multiplicative.ofAdd (0 : ZMod 2) = Multiplicative.ofAdd (0 : ZMod 2)`? I'll iterate empirically.

OK — time to write. Also add the contradiction helper:

```lean
lemma sphere_eq_neg_self_impossible {n : ℕ} (x : Sphere n)
    (hx : (x : EuclideanSpace ℝ (Fin (n + 1))) = -(x : EuclideanSpace ℝ (Fin (n + 1)))) : False := by
  have hzero : (x : EuclideanSpace ℝ (Fin (n + 1))) = 0 := by
    have hsum : (x : EuclideanSpace ℝ (Fin (n + 1))) + (x : EuclideanSpace ℝ (Fin (n + 1))) = 0 := by
      rw [← hx]; abel? simp?
    ...
```
From hx : x = -x: x + x = x + (-x) = 0: `rw [← hx]` — goal x+x = 0: rewrite the second occurrence... `rw [← hx]` on goal `x + x = 0`: rewrites... hx : x = -x; `rw [← hx]` matches `x` (LHS of hx rewritten to -x) — occurrences: both — first one → -x + x = 0 → `simp`. Hmm `rw [← hx]` rewrites ALL occurrences by default → -x + -x = 0 — no. Use `nth_rewrite 2 [← hx]` or `rw [← hx] at ...`. Simpler: 
```lean
  have hsum : (x : E) + (x : E) = 0 := by
    calc (x : E) + (x : E) = (x : E) + -(x : E) := by rw [hx]
      _ = 0 := add_neg_cancel _
```
Hmm `rw [hx]` rewrites all `x` occurrences... in the calc target `(x:E) + (x:E) = (x:E) + -(x:E)`: `rw [hx]` → rewrites first occurrence? `rw` rewrites first matching occurrence repeatedly? No — rw rewrites ALL occurrences by default (unless pattern given)? Actually `rw` rewrites the first occurrence only?? No — `rw` rewrites ALL occurrences in the goal. Hmm, no — `rw` rewrites all occurrences of the pattern. To control: `rw [hx]` with hx : x = -x on goal `x + x = x + -x` → both sides become -x + -x = -x + -x → closed by rfl! Ha — actually that closes trivially:
```
  have hsum : (x : E) + (x : E) = 0 := by
    -- x + x = x - x via hx applied to the second summand
```
Cleanest: `nth_rw`? Or: `have hsum : (x:E) + (x:E) = (x:E) - (x:E) := by congr 2; exact hx` hmm — `congr 2`? The subtraction: x + x = x - x: `congrArg₂ HAdd.hAdd rfl hx.symm?` — hx : x = -x. x + x = x + (-x): `exact congrArg (fun z => (x:E) + z) hx` ✓. Then `simpa using this`? `(x - x) = 0` — sub_eq_zero? `simpa [sub_eq_add_neg] using (congrArg (fun z => x + z) hx)`. Or simply:
```lean
  have hsum : (x : E) + (x : E) = 0 := by
    exact (congrArg (fun z : E => (x : E) + z) hx).trans (add_neg_cancel _)
```
✓. Then:
```lean
  have hnorm : ‖(x : E)‖ = 1 := by
    -- x.2 : x ∈ Metric.sphere 0 1
    exact (Metric.mem_sphere_iff_norm.mp x.2)  -- ‖x - 0‖ = 1
    -- need simpa: ‖x - 0‖ = 1 → ‖x‖ = 1
```
`Metric.mem_sphere_iff_norm`? name check: `mem_sphere_iff_norm : y ∈ sphere x r ↔ ‖y - x‖ = r`? in mathlib Metric: `Metric.mem_sphere_iff_norm`? There's `mem_sphere_iff_norm` for normed spaces? Let me use `Metric.mem_sphere_iff_norm.mp x.2` — hope the name exists; else `x.2` gives dist (x) 0 = 1 hmm — x.2 : x ∈ Metric.sphere (0:E) 1 = dist x 0 = 1 — `Metric.mem_sphere.mp x.2 : dist x 0 = 1` — then `dist_eq_norm : dist x 0 = ‖x - 0‖` → simpa [dist_eq_norm] — `norm_sub_zero`: ‖x - 0‖ = ‖x‖ ✓ simp. So:
```lean
  have hnorm : ‖(x : E)‖ = 1 := by
    simpa [dist_eq_norm] using Metric.mem_sphere.mp x.2
```
then contradiction: `have : ‖(x:E)‖ = 0 := by rw [hzero]; exact norm_zero`; `norm_num at hnorm`? hnorm : ‖x‖ = 1; this : ‖x‖ = 0 → `nlinarith`? norms are reals ✓ `nlinarith [this, hnorm]` → 0 = 1 contradiction ✓.

Then in the right_cancel' contradiction cases:
```lean
      exfalso
      exact sphere_eq_neg_self_impossible x (by simpa [antipodalSmul, hg, hh] using congrArg Subtype.val hx)
```
Careful with hx : g • x = h • x (subtype equality). congrArg Subtype.val hx : (g•x : E) = (h•x : E). simp [antipodalSmul, hg, hh] — g = m → g•x = if m = m then -x else x = -x; h ≠ m... after subst hh? In the case (g = m, h = 1): subst hg hh — then hx : m • x = 1 • x — 1•x = x (one_smul), m•x = -x. `simpa [antipodalSmul] using congrArg Subtype.val hx` : -(x:E) = (x:E). For sphere_eq_neg_self_impossible I need hx' : x = -x: `neg_eq_iff_eq_neg.mp`? From -x = x: x = -x by symmetry — `hx'.symm`. Let me make the helper take either orientation... simpler: helper takes `h : (x:E) = -(x:E)`; in case (g=m, h=1): obtained `hx' : -(x:E) = (x:E)` — then `sphere_eq_neg_self_impossible x hx'.symm`. In case (g=1, h=m): hx' : (x:E) = -(x:E) ✓ directly.

For left_cancel':
```lean
  left_cancel' g x y h := by
    by_cases hg : g = Multiplicative.ofAdd (1 : ZMod 2)
    · have h' : (x : E) = (y : E) := by
        have hx' := congrArg Subtype.val h  -- g•x = g•y (in E)
        have hneg : -(x:E) = -(y:E) := by simpa [antipodalSmul, hg] using hx'
        exact neg_injective hneg
      exact Subtype.ext h'
    · exact Subtype.ext (by simpa [antipodalSmul, hg] using congrArg Subtype.val h)
```
`neg_injective` — `Neg.neg` injective: `neg_injective : Function.Injective Neg.neg`? In mathlib: `neg_injective` ✓ (AddGroup). For E = EuclideanSpace — additive group ✓.

MulAction mul_smul with the enumeration:
```lean
  mul_smul g h x := by
    rcases zmod2Mul_eq_ofAdd_one_or_one g with hg | hg <;>
    rcases zmod2Mul_eq_ofAdd_one_or_one h with hh | hh <;>
    subst g <;> subst h <;> simp [antipodalSmul, mul_assoc?, ...]
```
Hmm — after subst: cases: (m,m): m • m • x = (m*m) • x — m*m = 1 → 1 • x = x; LHS: m • (m • x) = m • (-x) = -(-x) = x ✓ — `simp [antipodalSmul]` needs `m*m = 1` and `-(-x) = x`: helper `antipodal_mul_self : Multiplicative.ofAdd (1 : ZMod 2) * Multiplicative.ofAdd (1 : ZMod 2) = 1` ✓ simp uses it if it's a simp lemma? Make it `@[simp]`? Hmm — careful: making things simp-lemmas pollutes; use local `simp [antipodalSmul, antipodal_mul_self]`. And -(-x) = x: `neg_neg` ✓ simp.

For antipodal_mul_self:
```lean
lemma antipodal_mul_self : (Multiplicative.ofAdd (1 : ZMod 2)) * (Multiplicative.ofAdd (1 : ZMod 2)) = 1 := by
  norm_num [Multiplicative]? 
```
Multiplicative multiplication: ofAdd a * ofAdd b = ofAdd (a+b) — if Multiplicative is a reducible def with group instance transporting, then `norm_num` on ZMod 2: (1:ZMod 2)+1 = 0 — norm_num handles ZMod ✓ probably. `change Multiplicative.ofAdd ((1 : ZMod 2) + (1 : ZMod 2)) = (1 : Multiplicative (ZMod 2))` then `norm_num`? The RHS `1` — change to `Multiplicative.ofAdd (0 : ZMod 2)`:
```lean
lemma antipodal_mul_self : (Multiplicative.ofAdd (1 : ZMod 2)) * (Multiplicative.ofAdd (1 : ZMod 2)) = 1 := by
  change Multiplicative.ofAdd ((1 : ZMod 2) + (1 : ZMod 2)) = Multiplicative.ofAdd (0 : ZMod 2)
  norm_num
```
norm_num on ZMod 2 addition: (1 : ZMod 2) + 1 = 0 ✓ should work (ZMod has norm_num extensions).

Hmm — one more consideration: `subst g` where g : Multiplicative (ZMod 2) and hg : g = Multiplicative.ofAdd (1 : ZMod 2) — subst works ✓. But in a `<;>` chain after rcases with hg | hg, then `subst g` — fine.

Now the smul's if condition: I used `if g = Multiplicative.ofAdd (1 : ZMod 2) then -x else x`. In proofs after subst, `simp [antipodalSmul]` evaluates the if ✓. Also `one_smul`: 1 • x — 1 ≠ m? `(1 : Multiplicative (ZMod 2)) = m` would be 0 = 1 in ZMod 2 — simp needs to know `(1 : Multiplicative (ZMod 2)) ≠ Multiplicative.ofAdd (1 : ZMod 2)` — hmm: `if (1 : Multiplicative (ZMod 2)) = m then -x else x` — simp can't decide the condition unless it knows 1 ≠ m. `(1 : Multiplicative (ZMod 2))` = ofAdd 0 (after change) and ofAdd 0 ≠ ofAdd 1 — via `decide`/norm_num: `norm_num` can prove `(0 : ZMod 2) ≠ 1`? ✓ probably. one_smul: `simp [antipodalSmul, (show (1 : Multiplicative (ZMod 2)) ≠ Multiplicative.ofAdd (1 : ZMod 2) by norm_num [Multiplicative])]` — hmm getting messy; alternatively define smul by pattern on toAdd:

```lean
def antipodalSmul (g : Multiplicative (ZMod 2)) (x : Sphere n) : Sphere n :=
  if g.toAdd = 1 then -x else x
```
and simp with facts about toAdd. one_smul: (1 : Multiplicative (ZMod 2)).toAdd = 0 → `if 0 = 1 then ...` → simp. `(1 : Multiplicative (ZMod 2)).toAdd = 0` — hmm — is that rfl? The One instance on Multiplicative G: `⟨Multiplicative.ofAdd 0⟩`; `(1 : Multiplicative G)` = ofAdd 0; `.toAdd` = 0 — rfl if ofAdd/toAdd are Equiv.refl projections — `change (0 : ZMod 2) = 1`? Hmm. Honestly the `= Multiplicative.ofAdd 1` formulation with `decide`-friendly conditions: in ZMod 2, `(0 : ZMod 2) = 1` is decidable and `norm_num` proves `¬(0 = 1)`. After `change`, the if condition in one_smul: `if (1 : Multiplicative (ZMod 2)) = Multiplicative.ofAdd (1 : ZMod 2) then -x else x = x` — hmm, `simp [antipodalSmul]` then needs to discharge `(1 : Multiplicative (ZMod 2)) = Multiplicative.ofAdd (1 : ZMod 2) → ...` — it will try: the if-cond as a proposition `(1 : Multiplicative (ZMod 2)) = Multiplicative.ofAdd (1 : ZMod 2)` — simp can prove its negation if `decide`/norm_num... simp's `norm_num` extension can close `(1 : ZMod 2) = 0`? The proposition is about Multiplicative elements — simp unfolds `1` → `Multiplicative.ofAdd 0`? If the One instance is `⟨Multiplicative.ofAdd 0⟩` and it's reducible, then condition = `Multiplicative.ofAdd 0 = Multiplicative.ofAdd 1` → congrArg toAdd → 0 = 1 in ZMod 2 → norm_num → False ✓. Likely works with plain `simp [antipodalSmul]`. I'll iterate.

Also — the `if g = Multiplicative.ofAdd (1 : ZMod 2)` in the def itself: elaborating `Multiplicative.ofAdd (1 : ZMod 2)` ✓.

Alright, also the `RealProjective` nonempty: `⟨Quotient.mk (MulAction.orbitRel ..) (sphereNonempty 3).some⟩`? sphereNonempty n : Nonempty (Sphere n) — from SphereGluing ✓. `.some` needs Classical.choice — Nonempty.some — fine (Classical.choice allowed).

Let me now also prepare the recognition theorem and check `Quot.mk_surjective` name: `Quot.mk_surjective : Function.Surjective (Quot.mk r)`? There's `Quot.mk_surjective` or `Quotient.mk_surjective`... grep during dev.

Also, is there an issue with `MulAction.orbitRel G (Sphere 3)` vs the `letI` needed for quotient Setoid instance? `Quotient (MulAction.orbitRel G (Sphere 3))` — needs Setoid (Sphere 3) — the term MulAction.orbitRel G (Sphere 3) IS the setoid ✓.

Writing the file now. I'll iterate compile errors. Also remember: this file imports CoveringLemma + SphereGluing + `Mathlib.Topology.Covering.Quotient` + `Mathlib.Analysis.Normed.Module.Connected` (isPathConnected_sphere) + `Mathlib.Topology.Separation.Regular` + `Mathlib.Data.ZMod.Basic`.
dsh: reasoning:
I accidentally left a `sorry` in the file — I must not do that even transiently in the final file. Let me first check how `Multiplicative.ofAdd` works — the `zmod2Mul_eq_ofAdd_one_or_one` needs the right API. Let me check the Multiplicative definition quickly and then write the full file correctly in one go.
dsh: reasoning:
- `Multiplicative α = α` (reducible def), `ofAdd : α ≃ Multiplicative α` (Equiv.refl-based? line 86 — likely `Equiv.refl`), `toAdd = ofAdd.symm`.
- `ofAdd_zero [Zero α] : @Multiplicative.ofAdd α 0 = 1` ✓
- `toAdd_one [Zero α] : (1 : Multiplicative α).toAdd = 0` ✓

So in `zmod2Mul_eq_ofAdd_one_or_one`: from h : g.toAdd = 0: `right; exact (Multiplicative.ofAdd_zero.symm.trans (congrArg Multiplicative.ofAdd h).symm)?` — hmm: g = Multiplicative.ofAdd (g.toAdd)? Since Multiplicative = α definitionally and ofAdd is an equivalence — is `Multiplicative.ofAdd (g.toAdd) = g` rfl? If ofAdd = Equiv.refl? Let me check line 86: `def ofAdd : α ≃ Multiplicative α :=` — likely `Equiv.refl _` (since Multiplicative α = α defeq). Then `g.toAdd = g` hmm — toAdd = ofAdd.symm = refl.symm = refl. So everything is rfl-friendly: `Multiplicative.ofAdd (1 : ZMod 2) = (1 : ZMod 2)` as expressions (defeq!). So:

```lean
lemma zmod2Mul_eq_ofAdd_one_or_one (g : Multiplicative (ZMod 2)) :
    g = Multiplicative.ofAdd (1 : ZMod 2) ∨ g = 1 := by
  rcases zmod2_eq_zero_or_one (Multiplicative.toAdd g) with h | h
  · right
    exact Multiplicative.ofAdd_zero.symm.trans (congrArg Multiplicative.ofAdd h).symm? 
```
Hmm — careful: h : toAdd g = 0. I want g = 1. g = ofAdd (toAdd g) = ofAdd 0 = 1: `calc g = Multiplicative.ofAdd (Multiplicative.toAdd g) := rfl`? — is that rfl? ofAdd is `def ofAdd : α ≃ Multiplicative α := Equiv.refl α`? If so, `Multiplicative.ofAdd (toAdd g)` reduces to `g`? Equiv.refl's toFun = id — `Multiplicative.ofAdd (toAdd g)` = `id (id g)` = g — reduction needs unfolding ofAdd/toAdd — rfl works since they unfold to id ✓ (defs are reducible). So:

```lean
  · right
    calc
      g = Multiplicative.ofAdd (Multiplicative.toAdd g) := rfl
      _ = Multiplicative.ofAdd (0 : ZMod 2) := by rw [h]
      _ = 1 := Multiplicative.ofAdd_zero
  · left
    calc
      g = Multiplicative.ofAdd (Multiplicative.toAdd g) := rfl
      _ = Multiplicative.ofAdd (1 : ZMod 2) := by rw [h]
```

And antipodal_mul_self: `change Multiplicative.ofAdd ((1 : ZMod 2) + (1 : ZMod 2)) = Multiplicative.ofAdd (0 : ZMod 2)` — will the change work? `(Multiplicative.ofAdd 1) * (Multiplicative.ofAdd 1)` — Multiplicative's multiplication: `instMul` = `⟨fun a b => ofAdd (toAdd a + toAdd b)⟩`? With ofAdd/toAdd = refl, the product unfolds to `ofAdd (1 + 1)`... change might need `norm_num` to handle. Let me just write `norm_num` on the original goal — norm_num unfolds ofAdd and proves (1:ZMod 2)+1=0 and 0=0? The RHS `1` — `(1 : Multiplicative (ZMod 2))` — norm_num knows Multiplicative one = ofAdd 0 via ofAdd_zero ✓ probably. I'll try `norm_num` and fall back to the change+rw.

Now write the complete file (replacing the stub).
dsh: reasoning:
Right — `lake env lean` doesn't build dependencies automatically. Use `lake build Poincare.D12.TriangulationTopology.CoveringLemma` first, then lean. Let me build the target.
dsh: reasoning:
Multiple issues to fix:

1. `zmod2_eq_zero_or_one`: after `rw [h]`, goal `↑0 = 0` — `(ZMod.natCast_zmod_val a).symm.trans (by rw [h])` — the trans result: (a.val : ZMod 2) = a → a = 0... wait the goal is `a = 0`, and `(ZMod.natCast_zmod_val a).symm : a = (a.val : ZMod 2)`. Then `.trans (by rw [h])` — the second part: (a.val : ZMod 2) = 0 — after rw [h]: `↑0 = 0` where ↑0 = (0 : ℕ → ZMod 2) — norm_num should close `(0 : ZMod 2) = 0` by rfl... `rw [h]` changed `↑a.val = 0` to `↑0 = 0` — but the goal in the error shows `⊢ ↑0 = 0` unsolved because I wrote `by rw [h]` — after rw the goal isn't closed! Need `by rw [h]; norm_num` or `by simpa using h`? Hmm: `(a.val : ZMod 2) = 0`: `rw [h]` → `(0 : ZMod 2) = 0` → rfl. Just add `rfl` after rw. Same for 1: `(1 : ZMod 2) = 1` — `↑1 = 1` — rfl? `(1 : ZMod 2)` = OfNat — should be rfl too... The error shows the goal `↑1 = 1` — hmm — norm_num: `norm_num` proves (1 : ZMod 2) = 1? Let me just write `by rw [h]; norm_num` for both.

2. `Multiplicative.ofAdd_zero` unknown — it's in a namespace: `Multiplicative.ofAdd_zero`? The grep showed it at line 244 of TypeTags/Basic.lean — inside `namespace Multiplicative`? The full name might be `Multiplicative.ofAdd_zero` — but unknown?! Maybe it's in namespace... let me check the namespace context of line 244. Possibly `@[simp] theorem ofAdd_zero` in `namespace Multiplicative` — then name `Multiplicative.ofAdd_zero` ✓ should exist. Unknown though — maybe the file isn't imported? TypeTags/Basic is imported transitively via Group. Or the name is different: let me check.

3. `antipodal_mul_self`: after change, `norm_num` left `⊢ 2 = 0` — the change rewrote the goal to `Multiplicative.ofAdd ((1 : ZMod 2) + (1 : ZMod 2)) = Multiplicative.ofAdd (0 : ZMod 2)` — hmm wait the error shows `⊢ 2 = 0` — norm_num unfolded to `2 = 0` in ZMod 2?? That's WRONG — (1 : ZMod 2) + 1 = 0, not 2... in ZMod 2, 2 = 0 IS true! norm_num normalized (1+1) to 2 and needs to show 2 = 0 in ZMod 2 — which is true by ZMod reduction but norm_num needs help: `norm_num` handles ZMod 2? It didn't finish. Use `decide`? Hmm — `decide` is allowed? The forbidden list: sorry, axiom, admit, unsafe, native_decide, proof_wanted. `decide` (kernel reduction) is fine! But ZMod 2 equality by decide — (2 : ZMod 2) = 0 — decidable ✓ `decide` should prove it. Or `norm_num [ZMod.natCast_self]`? (ZMod.natCast_self : (n : ZMod n) = 0) ✓ — `simpa using (ZMod.natCast_self (n := 2) : (2 : ZMod 2) = 0)`. Actually `change Multiplicative.ofAdd ... = ...` then `rw [ZMod.natCast_self]`? The goal after norm_num: `2 = 0`?? Hmm — but that means norm_num DID unfold ofAdd and reduce to 2 = 0 in ZMod 2. So `decide` it: hmm — is `2 = 0` in ZMod 2 decidable → `decide` evaluates `(2 : ZMod 2) = 0` → true ✓. Wait — actually is it? 2 mod 2 = 0 ✓. `decide` uses kernel reduction ✓ allowed. Alternatively `norm_num [ZMod.natCast_self]`. I'll use `simpa using (ZMod.natCast_self (n := 2) : (2 : ZMod 2) = 0)`? hmm ZMod.natCast_self signature: `(n : ZMod n) = 0`? Let me check... `theorem natCast_self : (n : ZMod n) = 0`. So: `change (2 : ZMod 2) = 0` then `exact ZMod.natCast_self`. Hmm — but where did `Multiplicative` go in norm_num's output? norm_num unfolded Multiplicative.ofAdd (reducible) — the goal became `2 = 0` — meaning after unfolding, `(1 : ZMod 2) + 1` normalized to `2` — wait norm_num shouldn't normalize 1+1 to 2 in ZMod 2... it did (additive). So final goal `(2 : ZMod 2) = 0` — exact ZMod.natCast_self ✓.

4. `smul_right_injective (2 : ℝ) ...` — the argument order: `smul_right_injective` hmm — the error: `@smul_right_injective ?m.102 2` — "The argument 2 has type ℝ but is expected to have type Type" — so smul_right_injective's first arg is the TYPE or the SMul instance?? Let me check: `smul_right_injective : Function.Injective ((a • ·) : M → M)`? no... There's `smul_right_injective` in Algebra.Group.SMul: `theorem smul_right_injective {G M} [SMul G M] [SMul G Mᵐᵒᵖ] [IsCentralScalar G M] (g : G) : ...`? Hmm. Or the one I want: in a module over a field with char 0: `smul_right_injective`? There's `Module.smul_right_injective`? Actually the standard: `smul_right_injective : ∀ {K V} [DivisionRing K] [AddCommGroup V] [Module K V] {a : K}, a ≠ 0 → Function.Injective fun x : V => a • x`? hmm the error shows `@smul_right_injective ?m.102 2` — 2 treated as implicit Type arg — the actual signature: `smul_right_injective {M : Type*} [SMul M α] [SMul Mᵐᵒᵖ α] [IsCentralScalar M α] (g : M) : Function.Injective (g • ·)`? no — "argument 2 has type ℝ but is expected Type" means the FIRST explicit arg is a Type. So `smul_right_injective` might be `smul_right_injective (M : Type*) [..] (a : M) : ...`. Then the right usage: `smul_right_injective (M := ℝ)?` hmm wait — actually there's a cleaner standard lemma: `smul_eq_zero`... I want: (2:ℝ) • x = 0 → x = 0. Options: `smul_eq_zero.mp`? In a vector space: `(a : ℝ) • x = 0 ↔ a = 0 ∨ x = 0`: `smul_eq_zero : a • x = 0 ↔ a = 0 ∨ x = 0` ✓ (NoZeroSMulDivisors ℝ E). So: `exact (smul_eq_zero.mp htwo).resolve_left (by norm_num)`. ✓ cleaner.

5. mul_smul cases: `simp [antipodalSmul, antipodal_mul_self]` failed — the goals show e.g. `1 • x = Multiplicative.ofAdd 1 • Multiplicative.ofAdd 1 • x` — the LHS `(g*h) • x` — g = ofAdd 1? hmm — case inl.inl: g = ofAdd 1, h = ofAdd 1 — goal: `(ofAdd 1 * ofAdd 1) • x = ofAdd 1 • (ofAdd 1 • x)` — but the error shows `1 • x = ...` meaning `ofAdd 1 * ofAdd 1` was rewritten to 1 by `simp` (using antipodal_mul_self) ✓, then needs `1 • x = ofAdd 1 • (ofAdd 1 • x)` — simp needs: `ofAdd 1 • (ofAdd 1 • x) = x` — i.e., smul_smul via antipodalSmul: `antipodalSmul n (ofAdd 1) (antipodalSmul n (ofAdd 1) x)` = -(-x) = x — simp [antipodalSmul] should unfold both: condition `ofAdd 1 = ofAdd 1` → true → -(-x) — then `neg_neg` — simp knows ✓. But the warning says antipodalSmul was UNUSED in the simp — meaning simp didn't unfold the smul notation `•` to antipodalSmul! Because `•` on Sphere n elaborates via the SMul instance `antipodalSMul` — simp doesn't unfold instance projections unless it knows... `simp [antipodalSmul]` — the smul `a • b` reduces via `antipodalSMul.smul` — hmm — simp should unfold `SMul.smul antipodalSMul a b` — the simp lemma for the projection: `antipodalSMul.smul`? For a structure instance, field access `antipodalSMul.smul` is a projection — simp can unfold `•` if we give `[simp]`... The issue: `•` = `SMul.smul antipodalSMul` — hmm, simp unfolds reducible projections? The instance is a `def`-like instance; simp needs the projection to be reducible. `antipodalSMul` is an `instance ... := ⟨antipodalSmul ..⟩` — anonymous constructor — `SMul.smul antipodalSMul` reduces to `antipodalSmul` if `antipodalSMul` is reducible... instances are `irreducible`? No — instance defs are reducible by default unless marked. Hmm but simp said "antipodalSmul unused" — so it didn't unfold. Maybe because the smul in the goal is `•` with the MulAction instance `antipodalMulAction` (MulAction extends SMul — `MulAction.toSMul`?) — the goal's `•` elaborates to `MulAction.toSMul.1`? MulAction has `toSMul : SMul` field — `MulAction.toSMul` — the smul notation resolves to `MulAction.toSMul.1` — projection through the structure — simp might not unfold through the anonymous constructor.

Fix: make the instances explicit defs with named fields and mark simp lemmas:
```lean
instance antipodalSMul : SMul (Multiplicative (ZMod 2)) (Sphere n) where
  smul := antipodalSmul (n := n)
instance antipodalMulAction : MulAction (Multiplicative (ZMod 2)) (Sphere n) where
  toSMul := antipodalSMul
  ...
```
And add simp lemmas:
```lean
@[simp] lemma antipodalSmul_apply_self (x : Sphere n) :
    antipodalSmul (n := n) (Multiplicative.ofAdd (1 : ZMod 2)) x = -x := by
  simp [antipodalSmul]
@[simp] lemma antipodalSmul_apply_one (x : Sphere n) :
    antipodalSmul (n := n) 1 x = x := by
  simp [antipodalSmul]
```
Hmm — wait, but in the MulAction proofs the smul is `•` — the simps: `smul` unfolds to `antipodalSMul.smul`? hmm — for simp to rewrite `g • x` we need simp to unfold `MulAction.toSMul`... Simplest robust approach: avoid `•` entirely — state the MulAction fields with explicit `antipodalSmul`:
```lean
instance antipodalMulAction : MulAction (Multiplicative (ZMod 2)) (Sphere n) where
  toSMul := antipodalSMul
  one_smul x := by
    simp [antipodalSmul]
  mul_smul g h x := by
    rcases ... <;> subst ...
    · simp [antipodalSmul, antipodal_mul_self]  -- goal: antipodalSmul (m*m) x = antipodalSmul m (antipodalSmul m x)
```
With toSMul := antipodalSMul explicitly, the goal's `•` unfolds? mul_smul field: `(g * h) • x = g • h • x` with `•` = MulAction.toSMul.1 g x — elaboration: the field's statement is in terms of the CURRENT instance being defined? For instance definitions, the fields are checked against the projected structure — the statement `mul_smul : ∀ g h x, (g*h) • x = g • (h • x)` uses the smul from `toSMul`? Hmm — in Lean 4, the `•` in the field types resolves via the typeclass `SMul` — during instance construction it would use the instance being defined only if available — it's NOT yet available. So `•` might elaborate to some OTHER smul — that's exactly why `simp [antipodalSmul]` was "unused": the goal's `•` might not be our instance! Ugh — wait no — inside the instance definition, `•` in the field's expected type — hmm, the field `mul_smul` is declared in MulAction as `mul_smul : ∀ (g₁ g₂ : α) (a : β), (g₁ * g₂) • a = g₁ • g₂ • a` with `[SMul α β]` from MulAction's own toSMul: MulAction is `class MulAction ... extends SMul α β where one_smul : ∀ b : β, (1 : α) • b = b; mul_smul : ∀ (g h : α) (b : β), (g * h) • b = g • h • b` — the `•` in the class fields uses the inherited SMul (self-reference). When filling the fields, Lean elaborates `•` to the toSMul field just provided ✓. So the goals SHOULD be in terms of antipodalSMul.smul... but simp didn't unfold it. The projections: `antipodalSMul.smul` — since I wrote `⟨antipodalSmul (n := n)⟩` (anonymous ctor), the projection reduces only if the instance is reducible... `instance` is reducible? Default instances are `irreducible`?? — I believe Lean 4 marks instances as reducible=auto, i.e., not unfolded by simp but unfolded by rfl/dsimp... simp uses transparency `.reducible` by default in mathlib (`simp` config: default transparency is reducible). Anonymous constructor instances — Lean does NOT generate projection theorems. For `antipodalSMul.smul` to reduce, the instance must be a named structure literal. Fix: use named field syntax `where smul := ...`.

Similarly the IsCancelSMul/ContinuousConstSMul proofs use `g • x` — in `continuous_const_smul g := by ... simp [antipodalSmul, hg]` — the goal after by_cases: `Continuous fun x => antipodalSMul? x` — the field type `continuous_const_smul : ∀ (g : G), Continuous fun x : X => g • x` — `•` elaborates with the Ambient instance (antipodalMulAction's toSMul — since antipodalMulAction is already declared as an instance BEFORE antipodalContinuousConstSMul ✓). But unfolding through MulAction.toSMul + anonymous ctor — again problematic. Fix: give named fields everywhere and ALSO write explicit `change`/`show` with `antipodalSmul`:

For continuous_const_smul: goal `Continuous fun x : Sphere n => g • x` — I can `show Continuous fun x : Sphere n => antipodalSmul (n := n) g x`? Hmm — change requires defeq — `g • x` with the instance = `antipodalSMul.smul g x` — hmm wait — which instance does `•` pick for `Sphere n` — antipodalSMul or MulAction.toSMul? Both are instances: SMul from antipodalSMul and SMul from antipodalMulAction — CONFLICT! Two SMul instances for the same type — typeclass search fails or picks one?! Actually MulAction extends SMul — when I define antipodalMulAction as a MulAction instance, its inherited SMul is a DIFFERENT instance than antipodalSMul! That's a diamond: `SMul (Multiplicative (ZMod 2)) (Sphere n)` has two instances — inconsistent, causes problems. FIX: make antipodalSMul NOT an instance; make only antipodalMulAction the MulAction instance (it carries SMul). But MulAction requires `toSMul : SMul` field — provide it inline as part of the MulAction structure literal:

```lean
instance antipodalMulAction : MulAction (Multiplicative (ZMod 2)) (Sphere n) where
  smul := antipodalSmul (n := n)   -- the inherited field name? In MulAction, the SMul field is named `toSMul`? 
```
In Lean 4: `class MulAction ... extends SMul α β` — the field from extends is accessed as `toSMul`? For `class A extends B`, field is `toB`. So `toSMul := { smul := antipodalSmul .. }`? Hmm — anonymous or named: `toSMul := ⟨antipodalSmul (n := n)⟩`. Then `•` resolves via MulAction.toSMul ✓ single instance path.

Then in proofs, goals show `•` — to make simp unfold: the projection `antipodalMulAction.toSMul` → anonymous ⟨...⟩ → .smul → ... — simp won't chain through anonymous constructors automatically. Best: write dedicated simp lemmas for the SMUL behavior and tag them @[simp]:

```lean
@[simp] lemma smul_antipodal_one (x : Sphere n) : (1 : Multiplicative (ZMod 2)) • x = x := by
  change antipodalSmul (n := n) 1 x = x
  ...
```
Hmm — `change` again needs defeq — `1 • x` with instance antipodalMulAction — defeq to `(antipodalMulAction.toSMul.1) 1 x` — the anonymous ctor `⟨antipodalSmul⟩` — `.1` of a constructor application — is that defeq to antipodalSmul? For `⟨f⟩ : SMul`, `(⟨f⟩ : SMul α β).1` reduces to f IF the structure is reducible and it's a constructor application — YES, constructor application + projection reduces by iota ✓ — it's a term reduction, works in any transparency for `change`/`rfl`. So `change` works even if simp doesn't unfold. So plan: in each instance proof, first `change`/`show` the goal into antipodalSmul terms, then `simp [antipodalSmul, ...]`.

Hmm wait — but actually simpler: since projection-of-constructor reduces by iota, `simp` at reducible transparency should unfold it too... The earlier problem might have been something else: the goal was `(g*h) • x = g • (h • x)` where `•` came from... the ANONYMOUS instance `antipodalSMul` (the standalone SMul instance) — not the MulAction — since in the mul_smul field, the smul is the class's own toSMul = the anonymous... hmm.

Let me restructure cleanly:
- Drop `antipodalSMul` instance. Only `antipodalMulAction` (with toSMul named).
- All proofs use `change` to antipodalSmul first.
- Add @[simp] lemmas `antipodalSmul_one`, `antipodalSmul_ofAdd_one`, `antipodalSmul_eq_self_of_ne`:
```lean
@[simp] lemma antipodalSmul_one (x : Sphere n) : antipodalSmul (n := n) 1 x = x := by simp [antipodalSmul]
@[simp] lemma antipodalSmul_ofAdd_one (x : Sphere n) :
    antipodalSmul (n := n) (Multiplicative.ofAdd (1 : ZMod 2)) x = -x := by simp [antipodalSmul]
```
Then in mul_smul: after subst:
```lean
    · -- g = m, h = m: (m*m) • x = m • (m • x)
      simp [antipodal_mul_self, antipodalSmul]
```
— goal: `antipodalSmul (m*m) x = antipodalSmul m (antipodalSmul m x)`? After `change`? Let me do explicit:
```lean
    · subst hg; subst hh
      change antipodalSmul (n := n) (Multiplicative.ofAdd (1 : ZMod 2) * Multiplicative.ofAdd (1 : ZMod 2)) x =
        antipodalSmul (n := n) (Multiplicative.ofAdd (1 : ZMod 2)) (antipodalSmul (n := n) (Multiplicative.ofAdd (1 : ZMod 2)) x)
      simp [antipodalSmul, antipodal_mul_self]
```
Does `change` succeed on the field goal `(g*h) • x = g • (h • x)`? The field's `•` — during instance construction, the expected field type `(g * h) • x = g • (h • x)` — hmm, the class declaration: `mul_smul : ∀ (g h : α) (b : β), (g * h) • b = g • h • b` — this `•` refers to the SMul from the EXTENDS — inside the class body, `•` elaborates with `self.toSMul`? In Lean 4 class elaboration, fields elaborated with the instance as a "structure instance parameter"... For a class extending SMul, within the declaration the notation resolves to the class's toSMul projection applied to the implicit self? I believe the field type is `∀ {self : MulAction α β}, (g*h) • b = ...` — no, actually fields of a class extending SMul are elaborated with an implicit self instance of SMul? Hmm — in practice, when filling `mul_smul` of an instance, the goal shows `•` from `instMulActionOfSMul`-resolved instance = the ambient instance being defined? — circular. Actually Lean handles this: the class fields of `MulAction` are elaborated with `instSMul := self.toSMul` where self is the structure instance parameter — so filling the field, the goal is `(g*h) • x = g • h • x` with `•` from `‹self›.toSMul` — and since we're defining self via `where` + `toSMul := ⟨antipodalSmul⟩`, the goal's smul defeq-reduces to antipodalSmul ✓ so `change` works. I'll iterate.

6. `one_smul x := by simp [antipodalSmul]` failed with "simp made no progress" — same unfolding issue. Fix with change:
```lean
  one_smul x := by
    change antipodalSmul (n := n) 1 x = x
    simp [antipodalSmul]
```
Hmm — does `simp [antipodalSmul]` close `antipodalSmul 1 x = x`? antipodalSmul 1 x = if 1 = ofAdd 1 then -x else x — simp must prove `¬ 1 = ofAdd 1` — i.e., `(1 : Multiplicative (ZMod 2)) ≠ ofAdd (1 : ZMod 2)`. Hmm — simp's decision procedure: the proposition `(1 : Multiplicative (ZMod 2)) = Multiplicative.ofAdd (1 : ZMod 2)` — unfolds 1 → ofAdd 0 — `ofAdd 0 = ofAdd 1` — simp uses injectivity of Equiv? `Equiv.injective`? simp has `EquivLike.injective`-based congr? The simp theorem `Multiplicative.ofAdd_injective.eq_iff`? might not exist. Better: write the simp lemma via toAdd:
```lean
@[simp] lemma antipodalSmul_one (x : Sphere n) : antipodalSmul (n := n) 1 x = x := by
  rw [antipodalSmul, if_neg]
  · rfl
  · -- ¬ 1 = ofAdd 1
    intro h
    have : (1 : ZMod 2) = 0 := by
      -- from h : (1 : Multiplicative (ZMod 2)) = ofAdd (1 : ZMod 2)
      -- hmm: congrArg Multiplicative.toAdd h : toAdd 1 = toAdd (ofAdd 1) → 0 = 1
      have h' := congrArg Multiplicative.toAdd h
      simpa using h'  -- toAdd (ofAdd 1) = 1, toAdd 1 = 0
    norm_num at this
```
`toAdd_one : (1 : Multiplicative α).toAdd = 0` ✓ (line 252) and `toAdd (ofAdd 1) = 1` — ofAdd is an equivalence — `Multiplicative.toAdd_ofAdd?` — hmm — toAdd = ofAdd.symm — `ofAdd.symm_apply_apply?` — `Multiplicative.toAdd` applied to `ofAdd 1` = 1 — should be rfl after unfolding (ofAdd = refl-based). `simpa using h'` tries. Let me write it as `norm_num at this` after simpa... iterate.

Actually — a MUCH cleaner approach for the if-condition proofs: make the smul's condition decidable-friendly via `decide`:
```lean
@[simp] lemma antipodalSmul_one (x : Sphere n) : antipodalSmul (n := n) 1 x = x := by
  unfold antipodalSmul
  -- goal: (if 1 = ofAdd 1 then -x else x) = x
  -- use by_cases? or: have : ¬ (1 : Multiplicative (ZMod 2)) = Multiplicative.ofAdd (1 : ZMod 2) := by decide? 
```
Hmm — `decide` on the proposition `(1 : Multiplicative (ZMod 2)) = Multiplicative.ofAdd (1 : ZMod 2)` — DecidableEq (ZMod 2) ✓ — decide reduces: 1 = ofAdd 1 → ofAdd 0 = ofAdd 1 → 0 = 1 → false ✓. Is that within allowed tactics? `decide` = kernel reduction, allowed ✓ (only native_decide is forbidden). But to be safe with the "no native_decide" rule, plain `decide` is fine — it's just `of_decide_eq_true (Eq.refl true)`-style kernel computation. Hmm — but using decide in the middle of simp: `simp [antipodalSmul, (by decide : ...)]`. Cleaner:
```lean
lemma one_ne_ofAdd_one : (1 : Multiplicative (ZMod 2)) ≠ Multiplicative.ofAdd (1 : ZMod 2) := by decide
```
Hmm — does `decide` handle Multiplicative unfolding? DecidableEq (Multiplicative (ZMod 2)) — derived from ZMod 2's DecidableEq via the defeq — the proposition `1 = ofAdd 1` unfolds: 1 : Multiplicative (ZMod 2) reduces (via One instance) to ofAdd 0... the DecidableEq instance computes `(1 : Multiplicative (ZMod 2)) = Multiplicative.ofAdd (1 : ZMod 2)` — decide evaluates with the decidable instance which compares... reduction should normalize both sides to `ofAdd 0` and `ofAdd 1` → compares 0 = 1 in ZMod 2 → false ✓. I think `by decide` works. If not, `norm_num`-route.

Then:
```lean
@[simp] lemma antipodalSmul_one (x : Sphere n) : antipodalSmul (n := n) 1 x = x := by
  simp [antipodalSmul, one_ne_ofAdd_one]
@[simp] lemma antipodalSmul_ofAdd_one (x : Sphere n) :
    antipodalSmul (n := n) (Multiplicative.ofAdd (1 : ZMod 2)) x = -x := by
  simp [antipodalSmul]
@[simp] lemma antipodalSmul_ofAdd_one_sq (x : Sphere n) :
    antipodalSmul (n := n) (Multiplicative.ofAdd (1 : ZMod 2))
      (antipodalSmul (n := n) (Multiplicative.ofAdd (1 : ZMod 2)) x) = x := by
  simp [antipodalSmul]
```

7. `smul_eq_zero` fix as noted.

8. Also `dist_eq_norm` warning "try simp instead of simpa" — minor, keep simpa or simp.

Also the `spherePathConnectedSpace_of_pos` part compiled? Errors listed didn't include it — let me fix everything and re-run. Rewrite the middle portion of the file.
dsh: reasoning:
Oops — I typed `rw [ZMod.natCast_self (n := 2)]?` with a stray `?`. Let me think about antipodal_mul_self properly:

Goal after change: `Multiplicative.ofAdd ((1 : ZMod 2) + (1 : ZMod 2)) = Multiplicative.ofAdd (0 : ZMod 2)`. 
- `(1 : ZMod 2) + (1 : ZMod 2)` — `norm_num` can compute? Earlier norm_num left `2 = 0` — so norm_num DID reduce ofAdd and the sum to `2 = 0` in ZMod 2 — then `norm_num` couldn't finish `(2 : ZMod 2) = 0`. ZMod.natCast_self: `(n : ZMod n) = 0`? Let me check the name/signature: `theorem natCast_self : (n : ZMod n) = 0` — hmm, is it `natCast_self` or `natCast_zmod_eq_zero_iff_dvd`-based `ZMod.natCast_self`? Earlier grep showed `ZMod.natCast_zmod_eq_zero_iff_dvd` at Defs. `(2 : ZMod 2) = 0` — also `norm_num [ZMod.natCast_self]` should close. Simply:

```lean
lemma antipodal_mul_self :
    (Multiplicative.ofAdd (1 : ZMod 2)) * (Multiplicative.ofAdd (1 : ZMod 2)) = 1 := by
  change Multiplicative.ofAdd ((1 : ZMod 2) + (1 : ZMod 2)) = Multiplicative.ofAdd (0 : ZMod 2)
  congr 1
  norm_num [ZMod.natCast_self]?
```
hmm — after `congr 1`: goal `(1 : ZMod 2) + 1 = 0` — norm_num might close directly? Earlier it got stuck at `2 = 0` — with `[ZMod.natCast_self]` as a simp arg: `norm_num [ZMod.natCast_self]`? hmm norm_num with extra simp args: `norm_num [ZMod.natCast_self]` — or `simp [ZMod.natCast_self]`... hmm — `(2 : ZMod 2) = 0` — `ZMod.natCast_self (n := 2) : (2 : ZMod 2) = 0` — wait is natCast_self stated as `(n : ZMod n) = 0`? Let me check the actual statement. Let me grep.
dsh: reasoning:
`Multiplicative.ofAdd_zero` IS in namespace Multiplicative (line 82 namespace, 244 ofAdd_zero) — so why "unknown constant Multiplicative.ofAdd_zero"? Hmm — maybe because the namespace `Multiplicative` conflicts... Actually the error was `unknown constant Multiplicative.ofAdd_zero` — wait, maybe the file TypeTags/Basic isn't imported by my imports? It's imported by ZMod → Group theory chains — likely yes. OR the name is shadowed... Let me test in a scratch file. Also `natCast_self` not found in Defs.lean — check other ZMod files.
dsh: reasoning:
- `Multiplicative.ofAdd_zero` unknown even with ZMod.Basic imported — maybe the TypeTags/Basic file isn't imported through this chain, or the lemma lives in a different file/name. `#check Multiplicative.ofAdd_zero` fails — perhaps it's `Multiplicative.ofAdd_zero` in the ROOT namespace `Multiplicative.ofAdd_zero`... it said unknown. Weird. Whatever — avoid it: use `(show Multiplicative.ofAdd (0 : ZMod 2) = 1 by rfl)`? Hmm — ofAdd 0 = 1 — since the One instance is `⟨Multiplicative.ofAdd 0⟩`, this is rfl ✓. In zmod2Mul_eq_ofAdd_one_or_one: replace `_ = 1 := Multiplicative.ofAdd_zero` with `_ = 1 := rfl`? Hmm — `Multiplicative.ofAdd (0 : ZMod 2) = 1` — is rfl? `(1 : Multiplicative (ZMod 2))` — with instance One = ⟨ofAdd 0⟩ — `1` reduces to `ofAdd 0` if the instance is reducible — the instance is `instance [Zero α] : One (Multiplicative α) := ⟨Multiplicative.ofAdd 0⟩` — anonymous ctor — `(1 : Multiplicative (ZMod 2))` — OfNat → One.one = (⟨ofAdd 0⟩ : One).1 — reduces to ofAdd 0 by iota ✓ — rfl should work.

- `smul_eq_zero` needs `[Module.IsTorsionFree R M] [IsCancelMulZero R]` — ℝ-Module on EuclideanSpace: torsion free ✓? `Module.IsTorsionFree ℝ (EuclideanSpace ℝ (Fin (n+1)))` — probably an instance (char 0 fields: `Module.torsionFree` for fields? IsTorsionFree when the ring is... there's `instIsTorsionFree`? Not sure). Simpler: avoid smul_eq_zero — use `norm_eq_zero` route: from htwo: (2:ℝ) • x = 0 → ‖2•x‖ = 0 → 2‖x‖ = 0 → ‖x‖ = 0 (field!) — pure real arithmetic:
```lean
  have hnormzero : ‖(x : E)‖ = 0 := by
    have hnorm2 : ‖(2 : ℝ) • (x : E)‖ = 0 := by rw [htwo]; exact norm_zero
    have hnorm2' : 2 * ‖(x : E)‖ = 0 := by
      rwa [norm_smul, Real.norm_eq_abs, abs_of_pos (by norm_num)] at hnorm2
    nlinarith
```
`norm_smul : ‖a • x‖ = ‖a‖ * ‖x‖` ✓. Real.norm_eq_abs ✓. Then combine with hnorm : ‖x‖ = 1: `nlinarith [hnormzero, hnorm]`. Clean, no typeclass risk.

Actually even simpler: from htwo directly — `have : (x : E) = 0 := by` using `eq_zero_of_norm_eq_zero (hnormzero)`? No — hnormzero IS the contradiction with hnorm = 1. So in sphere_eq_neg_self_impossible: replace the smul_right_injective part:
```lean
  have hnorm1 : ‖(x : EuclideanSpace ℝ (Fin (n + 1)))‖ = 1 := by
    simpa [dist_eq_norm] using Metric.mem_sphere.mp x.2
  have hnorm0 : ‖(x : EuclideanSpace ℝ (Fin (n + 1)))‖ = 0 := by
    have htwo : (2 : ℝ) • (x : EuclideanSpace ℝ (Fin (n + 1))) = 0 := by
      simpa [two_smul] using hsum
    have hnorm2 : ‖(2 : ℝ) • (x : EuclideanSpace ℝ (Fin (n + 1)))‖ = 0 := by rw [htwo]; exact norm_zero
    have hnorm2' : 2 * ‖(x : EuclideanSpace ℝ (Fin (n + 1)))‖ = 0 := by
      rwa [norm_smul, Real.norm_eq_abs, abs_of_pos (by norm_num : (0 : ℝ) < 2)] at hnorm2
    nlinarith
  nlinarith
```

- `Quot.mk_surjective` ✓.
- `decide` works for `(2 : ZMod 2) = 0` ✓ — but I'll use ZMod.natCast_self: `ZMod.natCast_self (n : ℕ) : ↑n = 0` ✓. So antipodal_mul_self: after change, goal `Multiplicative.ofAdd ((1 : ZMod 2) + (1 : ZMod 2)) = Multiplicative.ofAdd (0 : ZMod 2)` — `congr 1` → `(1 : ZMod 2) + 1 = 0` — norm_num? It got to `2 = 0` before — `norm_num` didn't finish; add ZMod.natCast_self: `norm_num [ZMod.natCast_self]`? hmm — norm_num might not use it. Just: `change (2 : ZMod 2) = 0` then `exact ZMod.natCast_self 2`? wait natCast_self (n) : ↑n = 0 — `exact ZMod.natCast_self 2` : (2 : ZMod 2) = 0 ✓. Hmm but after congr 1 the goal is `(1 : ZMod 2) + (1 : ZMod 2) = 0` — `norm_num` reduces LHS to 2: stuck. Write: `change (2 : ZMod 2) = 0; exact ZMod.natCast_self 2`. The `change` from `(1 : ZMod 2) + (1 : ZMod 2) = 0` to `(2 : ZMod 2) = 0` — norm_num-style defeq? `(1 : ZMod 2) + 1` is NOT defeq to `2` (they're the same after mod-reduction? ZMod 2's addition reduces 1+1 to 0 actually! ZMod addition is defined via val + val mod 2 — `(1 : ZMod 2) + 1 = ⟨(1+1) % 2⟩ = ⟨0⟩` — reducing: val (1+1 mod 2) = 0 — kernel can compute % — so `(1 : ZMod 2) + (1 : ZMod 2)` reduces to `0` by kernel computation! Then `change (0 : ZMod 2) = 0`; rfl. Actually then norm_num SHOULD have closed it... it reported `2 = 0` — because norm_num first normalized 1+1 → 2 (natCast addition) leaving `↑2 = 0` — it doesn't do the mod reduction but I can: `exact ZMod.natCast_self 2`. So after `congr 1`, goal `(1 : ZMod 2) + (1 : ZMod 2) = 0` — hmm — wait: is `change (2 : ZMod 2) = 0` valid? Only if defeq — `(1 : ZMod 2) + (1 : ZMod 2)` — ZMod's Add: `⟨(a.val + b.val) % n⟩` — `(1+1) % 2` — kernel reduces `1+1` → 2, `2 % 2` → 0 ✓ so the term reduces to `⟨0⟩` = `0` — and `(2 : ZMod 2)` = `⟨2⟩` = `⟨2 % 2⟩` = `⟨0⟩` ✓ defeq ✓. So actually `rfl` proves `(1 : ZMod 2) + 1 = 0` directly!! Why did norm_num fail? Because norm_num simplified 1+1 to 2 first and then tried to show 2 = 0 without the mod reduction (norm_num doesn't unfold ZMod addition mod). But plain `rfl`/`simp` would close it since kernel reduction does the mod. So: after `congr 1`, `exact rfl`? Hmm — ZMod 2's `+`: `instAdd : Add (ZMod n) := ⟨fun a b => ⟨(a.val + b.val) % n⟩⟩` — reduction: `(⟨1⟩ + ⟨1⟩)` = `⟨(1 + 1) % 2⟩` = `⟨2 % 2⟩` = `⟨0⟩` — kernel knows `%` on numerals ✓. And `(0 : ZMod 2)` = `⟨0 % 2⟩`? natCast: `(0 : ZMod 2)` — OfNat → natCast 0 = `⟨0⟩`? = `⟨0 % 2⟩` = `⟨0⟩` ✓ defeq. So `exact rfl` ✓. Even `norm_num` failing, plain `rfl` should close `(1 : ZMod 2) + 1 = 0`. Actually hold on — hmm, `(1 : ZMod 2)` — is it `⟨1⟩` or `⟨1 % 2⟩`? natCast a = `⟨a % n⟩`? If natCast a = `⟨a⟩` then `(2 : ZMod 2)` = `⟨2⟩` — and is `⟨2⟩` defeq `⟨0⟩`? NO — only if natCast reduces mod... In mathlib ZMod, `natCast : ℕ → ZMod n := fun a => ⟨a % n⟩`? hmm, actually ZMod is defined with `val : ℕ` and there's an invariant... In mathlib4, ZMod n = Fin n-like structure with val and proof val < n? `ZMod n` is `{ val : ℕ // val < n }`? YES — ZMod n is a structure with `val : ℕ` and `isLt : val < n`! So `(2 : ZMod 2)` = natCast 2 = ⟨2 % 2, proof⟩ = ⟨0, proof⟩ — and `(0 : ZMod 2)` = ⟨0, proof⟩ — proof irrelevance → defeq ✓. And `(1 : ZMod 2) + (1 : ZMod 2)` = ⟨1 % 2 + 1 % 2 % 2, ...⟩ — % reduces → ⟨0⟩ ✓. So rfl closes. Great — but norm_num normalized into `↑2 = 0` hmm — whatever, use rfl directly after congr. Actually simplest antipodal_mul_self:

```lean
lemma antipodal_mul_self :
    (Multiplicative.ofAdd (1 : ZMod 2)) * (Multiplicative.ofAdd (1 : ZMod 2)) = 1 := by
  change Multiplicative.ofAdd ((1 : ZMod 2) + (1 : ZMod 2)) = Multiplicative.ofAdd (0 : ZMod 2)
  congr 1
  rfl
```
Hmm — wait `change` first: is `(ofAdd 1) * (ofAdd 1)` defeq to `ofAdd (1 + 1)`? Multiplicative's Mul instance: `instance [Add α] : Mul (Multiplicative α) := ⟨fun a b => ofAdd (toAdd a + toAdd b)⟩` — reduction: (ofAdd 1) * (ofAdd 1) = ofAdd (toAdd (ofAdd 1) + toAdd (ofAdd 1)) = ofAdd (1 + 1) ✓ defeq ✓. And `1` on RHS: `(1 : Multiplicative (ZMod 2))` = ⟨ofAdd 0⟩.1 = ofAdd 0 ✓ — but wait — is `1` the OfNat via One instance — in the GOAL as written `= 1`, the elaborator picks One (Multiplicative (ZMod 2)) — the change target `Multiplicative.ofAdd (0 : ZMod 2)` — defeq ✓. So:
```lean
  change Multiplicative.ofAdd ((1 : ZMod 2) + (1 : ZMod 2)) = Multiplicative.ofAdd (0 : ZMod 2)
  congr 1
  rfl
```
`congr 1` — hmm — ofAdd is an Equiv — congr might use injectivity — better: `exact congrArg Multiplicative.ofAdd rfl`? Hmm — rfl for the sum... just:
```lean
  change Multiplicative.ofAdd ((1 : ZMod 2) + (1 : ZMod 2)) = Multiplicative.ofAdd (0 : ZMod 2)
  rw [show (1 : ZMod 2) + (1 : ZMod 2) = 0 by rfl]? 
```
or simply — since the change target LHS reduces: `change Multiplicative.ofAdd (0 : ZMod 2) = Multiplicative.ofAdd (0 : ZMod 2)`? hmm — two-level. Just do:
```lean
lemma antipodal_mul_self :
    (Multiplicative.ofAdd (1 : ZMod 2)) * (Multiplicative.ofAdd (1 : ZMod 2)) = 1 := by
  change Multiplicative.ofAdd ((1 : ZMod 2) + (1 : ZMod 2)) = Multiplicative.ofAdd (0 : ZMod 2)
  rw [show (1 : ZMod 2) + (1 : ZMod 2) = 0 from rfl]
```
`show ... from rfl` ✓.

Hmm — wait, is `(1 : ZMod 2) + (1 : ZMod 2) = 0` provable by rfl? As discussed: ZMod 2 = subtype with val — `(1 : ZMod 2)` = `⟨1 % 2, _⟩`; addition: `⟨(a.val + b.val) % n, _⟩` = `⟨(1+1) % 2⟩` = `⟨2 % 2⟩` = `⟨0⟩`; `(0 : ZMod 2)` = `⟨0 % 2⟩` = `⟨0⟩` — proof fields: `isLt` proofs of Subtype — proof irrelevance → rfl ✓. I *think* ZMod in mathlib4 is a subtype with `val_lt`. It was defined as `def ZMod (n : ℕ) := Fin n`? Hmm — either way rfl works since val < n proof is proof-irrelevant. Hmm, but actually wait: mathlib4 ZMod: `structure ZMod (n : ℕ) where val : ℕ; isLt : val < n`? Let me not over-analyze — rfl or decide.

Now rewrite the antipodalSmul-related sections fully. Also fix smul_eq_zero and the mul_smul/one_smul changes. Let me rewrite the file's relevant part.
dsh: reasoning:
The old_string didn't match because the previous edit changed part of it. Let me just rewrite the whole file from scratch with the corrected version.
dsh: reasoning:
Good progress — issues:

1. Line 83: `(by rw [h])` for the `0` case leaves `↑0 = 0` — wait, this is the LEFT case: goal `(ZMod.natCast_zmod_val a).symm.trans (by rw [h])` — after rw: `↑0 = 0`... hmm — in the 0-case I wrote `by rw [h]` without norm_num (I only added norm_num to the 1-case). Fix: `by rw [h]` — goal after rw: `(0 : ℕ → ZMod 2) = 0` — rfl. Just add `rfl`: `by rw [h]` — the goal is `(a.val : ZMod 2) = 0` — rw [h] → `(0 : ZMod 2) = 0` — rfl closes — need `by rw [h]` to also close — `rw` doesn't close. Use `by rw [h]` → change to `simpa using h`? `(a.val : ZMod 2) = 0` — simpa [h]? `simpa [h]` — hmm: `exact (by rw [h] : (a.val : ZMod 2) = 0)`? Let me write `by simpa using congrArg (fun k : ℕ => (k : ZMod 2)) h` — hmm `congrArg (natCast) h` ✓ closes directly! For both cases:
```lean
  · left; exact (ZMod.natCast_zmod_val a).symm.trans (congrArg (fun k : ℕ => (k : ZMod 2)) h)
  · right; exact (ZMod.natCast_zmod_val a).symm.trans (congrArg (fun k : ℕ => (k : ZMod 2)) h)
```
`congrArg (fun k => (k : ZMod 2)) h : (a.val : ZMod 2) = (0 : ZMod 2)` — and goal `a = 0` with a = (a.val : ZMod 2) ✓. Clean, no norm_num.

2. `Continuous (antipodalSmul (Multiplicative.ofAdd 1))` — fun_prop can't unfold antipodalSmul (it's a def with if). Fix: instead of `change Continuous (antipodalSmul ...)` then simpa, directly show continuity of x ↦ -x:
```lean
    · subst hg
      -- need Continuous fun x : Sphere n => (-x)
      -- goal: Continuous fun x : Sphere n => g • x with g = ofAdd 1
      change Continuous fun x : Sphere n => -(x : Sphere n)
```
Hmm — `g • x` = antipodalSmul g x = -x — `change Continuous fun x : Sphere n => -x`? defeq: antipodalSmul (ofAdd 1) x = if ofAdd 1 = ofAdd 1 then -x else x — NOT defeq to -x (needs the if reduction with the decidability instance — `if c then a else b` reduces only when c is closed true... `ofAdd 1 = ofAdd 1` is rfl-able but the `if` needs the Decidable instance to reduce — kernel reduces `if h : c then t else e` when c is syntactically decidable — with DecidableEq this becomes a match on the decision — won't reduce automatically for symbolic c... but c here = `Multiplicative.ofAdd (1 : ZMod 2) = Multiplicative.ofAdd (1 : ZMod 2)` — closed term — kernel CAN reduce (DecidableEq unfolds to a computation that returns true) — but `change` requires defeq — the if-term reduces iff the decidability proof reduces — DecidableEq on ZMod 2 = derived from Fin/Subtype BEq? DecidableEq (ZMod n) is via `instDecidableEqZMod` — decEq via val comparison — `decide (1 = 1)` reduces ✓. So `change Continuous fun x => -x` might work via full reduction... risky. Instead: prove the continuity by the simpa route from the OTHER side: `simpa [antipodalSmul] using (by fun_prop : Continuous (fun x : Sphere n => -x))` — simpa uses the lemma antipodalSmul unfolding — the goal `Continuous (antipodalSmul (ofAdd 1))` — simp [antipodalSmul] unfolds antipodalSmul to `fun x => if ofAdd 1 = ofAdd 1 then -x else x` — then simp's `if_ctx`/decision: the condition `ofAdd 1 = ofAdd 1` — simp proves it by rfl ✓ reduces to `fun x => -x` ✓ matches the RHS. And the RHS `Continuous fun x : Sphere n => -x` — fun_prop: continuous negation on the subtype ✓ (negation of the sphere: `(fun x : Sphere n => -x)` — Subtype.val comp neg — fun_prop should handle). Let me write:
```lean
    · subst hg
      simpa [antipodalSmul] using (by fun_prop : Continuous fun x : Sphere n => -x)
```
Hmm — `by fun_prop : Continuous fun x : Sphere n => -x` — fun_prop proving continuity of negation ON THE SUBTYPE: the map x ↦ -x : Sphere n → Sphere n — fun_prop's continuity: Subtype.val ∘ (-·) ∘ Subtype.val is continuous, and the result lands in the sphere (since -x has norm 1) — fun_prop handles subtypes via `Continuous.subtype_mk` ✓ probably. If fun_prop fails: `(continuous_neg.comp continuous_subtype_val).subtype_mk _` — write `Continuous.subtype_mk ((continuous_neg.comp continuous_subtype_val)) _`? continuous_neg : Continuous fun x : E => -x (on EuclideanSpace — topological additive group ✓). Then `continuous_subtype_val` : Continuous (Subtype.val : Sphere n → E). Compose: `continuous_neg.comp continuous_subtype_val : Continuous fun x : Sphere n => -(x : E)` — as map Sphere n → E. Then subtype_mk: `(continuous_neg.comp continuous_subtype_val).subtype_mk _`? `Continuous.subtype_mk (h : Continuous f) (hp : ∀ x, p (f x)) : Continuous fun x => (⟨f x, hp x⟩ : subtype)` ✓. So:
```lean
exact (continuous_neg.comp continuous_subtype_val).subtype_mk _
```
✓ robust.

3. The identity case: `simpa [antipodalSmul, hg] using continuous_id` failed — after simp the goal became `Continuous (antipodalSmul g)` unchanged (hg didn't reduce the if because the condition is `g = ofAdd 1` and hg : g ≠ ofAdd 1 — simp CAN use hg to simplify `if g = ofAdd 1 then -x else x` → x via if_neg ✓ — but the "Type mismatch: After simplification, term continuous_id has type Continuous id" — simp simplified the goal to `Continuous (fun x => x)`? but continuous_id : Continuous id — type mismatch because `id` vs the eta — use `continuous_id` with expected `Continuous fun x => x`? id = fun x => x defeq ✓ hmm — the mismatch says expected `Continuous (antipodalSmul g)` — simp didn't simplify the GOAL (left it) but simplified the TERM continuous_id's type... The issue: `simpa [antipodalSmul, hg] using continuous_id` — simpa tries to unify the simplified term type with the goal — goal `Continuous (antipodalSmul g)` — simp [antipodalSmul, hg] — hmm hg : g ≠ ofAdd 1 — if_neg needs `¬ g = ofAdd 1` ✓ hg — so antipodalSmul g = fun x => if ... else x → simplifies to fun x => x ✓ — should work... but the error shows expected `Continuous (antipodalSmul g)` — meaning the goal's simp did nothing?? Maybe because `antipodalSmul` in the goal after `change` is `Continuous (antipodalSmul (n := n) g)` — simp [antipodalSmul] unfolds the DEF — def unfolding with simp needs [simp] on the def or the def in the simp list — `simp [antipodalSmul]` includes the def's equation... defs are unfolded by simp when listed ✓. Hmm — but earlier errors show `simp [antipodalSmul, hg]` DID unfold (the antipodalSmul_one simp lemma worked — it compiled? Line 116 warning was about simpa — which one? `simpa [dist_eq_norm]` (norm) — fine). Actually wait — the one_smul case `change antipodalSmul 1 x = x; simp` — did it compile? No error reported for it ✓ (simp [antipodalSmul_one...] — I wrote just `simp` — it found antipodalSmul_one ✓ since tagged @[simp] ✓).

So for the identity case the problem might be that simp's if-simplification requires the DECIDABILITY of the condition to rewrite `if h : g = ofAdd 1 then -x else x` with if_neg hg — simp handles dite/ite with hypotheses — `simp [antipodalSmul, hg]` should reduce to `fun x => x`... then `simpa .. using continuous_id` — continuous_id : Continuous id — the goal simplified: `Continuous (fun x : Sphere n => x)`? eta — id vs fun x => x — defeq ✓... The error message says otherwise. Let me just use explicit show:
```lean
    · change Continuous fun x : Sphere n => (antipodalSmul (n := n) g) x? 
```
Hmm — alternative robust approach for BOTH cases: prove by cases via the two simp lemmas:
```lean
  continuous_const_smul g := by
    rcases zmod2Mul_eq_ofAdd_one_or_one g with hg | hg
    · subst hg
      -- goal: Continuous fun x => Multiplicative.ofAdd 1 • x
      change Continuous fun x : Sphere n => -x
      exact (continuous_neg.comp continuous_subtype_val).subtype_mk _
    · subst hg
      change Continuous fun x : Sphere n => x
      exact continuous_id
```
`change Continuous fun x : Sphere n => -x` — from goal `Continuous fun x : Sphere n => (ofAdd 1) • x` — the smul unfolds: MulAction.toSMul... — hmm — `•` = `MulAction.toSMul antipodalMulAction? .smul` — antipodalMulAction.toSMul = ⟨antipodalSmul⟩ (from the def) — reduction: (⟨antipodalSmul⟩).smul g x = antipodalSmul g x = if ... — `change` unfolds with reducible transparency: projections of constructors reduce ✓ — the if with closed condition: `if ofAdd 1 = ofAdd 1 then -x else x` — needs the DecidableEq to reduce — `change` uses defeq with unfolding of reducible definitions — Decidable instances are NOT reducible (instances are semi-reducible)... `change` might fail. Ugh.

Most robust: avoid `•` entirely by rewriting with the simp lemmas first:
```lean
    · subst hg
      rw [show (fun x : Sphere n => (Multiplicative.ofAdd (1 : ZMod 2)) • x) = fun x => -x by
        funext x; simp [antipodalSmul]]
```
Hmm — `simp [antipodalSmul]` on `(ofAdd 1) • x = -x`: unfolds `•`? — the `•` elaborates to `SMul.smul` instance resolution — which instance? In my namespace the only SMul instances for Sphere n: from antipodalMulAction (MulAction.toSMul) — so `(ofAdd 1) • x` = (MulAction.toSMul antipodalMulAction).smul (ofAdd 1) x — hmm — but antipodalMulAction is an instance — simp unfolds instances?? Instance transparency: simp's default is `.reducible` — instances are typically `instance` = semireducible in newer Lean (marked with `[irreducible]`? no — instances get `semireducible`... in Lean 4, `instance` declarations have transparency `instances` which is like semireducible — projections don't unfold under reducible). But — the projection `antipodalMulAction.toSMul` — if antipodalMulAction is a named structure literal (I wrote `where toSMul := ⟨...⟩`) — `antipodalMulAction.toSMul` reduces to `⟨antipodalSmul⟩`?? For semireducible defs, the unfold happens only at `.instances` transparency... `simp` default transparency is reducible — so no unfold. Hmm — but my earlier one_smul worked with `change antipodalSmul (n := n) 1 x = x; simp` — change with... wait, did it? No error reported for one_smul in the last run — meaning `change` DID unfold `•` successfully! change's defeq check uses... elaboration-time defeq with default transparency (which includes instances? The elaborator's isDefEq has `.instances` unfolded in some paths...). Since one_smul compiled ✓, `change` DOES work on these goals. And in continuous_const_smul, my `change Continuous (antipodalSmul (n := n) g)` compiled too (error at 181 was the fun_prop issue for ofAdd 1 case — and 183 the continuous_id type mismatch after simpa).

So: keep the change, fix the proofs:
```lean
    · subst hg
      change Continuous (fun x : Sphere n => -x)
      exact (continuous_neg.comp continuous_subtype_val).subtype_mk _
    · change Continuous (fun x : Sphere n => x)
      exact continuous_id
```
Wait — `change Continuous (antipodalSmul (n := n) (Multiplicative.ofAdd (1 : ZMod 2)))` — this worked (goal accepted). But `Continuous (fun x => -x)` — antipodalSmul (ofAdd 1) = fun x => if ofAdd 1 = ofAdd 1 then -x else x — change to `fun x => -x` requires the if to reduce — `if c then t else e` with c = `ofAdd 1 = ofAdd 1` — the if-reduction requires the decidable proof — the elaborator's isDefEq at default transparency — ifs with closed decidable conditions DO reduce via kernel computation (whnf: `if h : c then .. else ..` → when c is a closed proposition with Decidable instance, the kernel reduces `decide c`). I think this works. If not: 
```lean
      change Continuous (fun x : Sphere n => if Multiplicative.ofAdd (1 : ZMod 2) = Multiplicative.ofAdd (1 : ZMod 2) then -x else x)
      -- then simp reduces the if
```
Let me instead sidestep: define the negation continuity lemma:
```lean
lemma continuous_antipodal_ofAdd_one : Continuous (fun x : Sphere n => antipodalSmul (n := n) (Multiplicative.ofAdd (1 : ZMod 2)) x) := by
  simp [antipodalSmul]  -- unfolds the if with closed condition
  exact (continuous_neg.comp continuous_subtype_val).subtype_mk _
```
Hmm — `simp [antipodalSmul]` unfolds the def (simp DOES unfold listed defs regardless of transparency — `simp [f]` adds f's equational lemma which is the unfolding — ✓!). So in this lemma the goal becomes `Continuous fun x => (if ... then -x else x)` then simp's if-simplification: the condition is a closed proposition — simp decides it? simp has a decision procedure for decidable propositions: `if_pos`/`if_neg` with the Decidable instance — simp will evaluate `decide (ofAdd 1 = ofAdd 1)` ✓ → `-x`. So this works. Then in continuous_const_smul: 
```lean
    · subst hg
      exact continuous_antipodal_ofAdd_one
    · change Continuous (fun x : Sphere n => x)
      exact continuous_id
```
Hmm — but does the `change` to `fun x => x` work? goal after subst: `Continuous fun x : Sphere n => 1 • x` — `1 • x` = antipodalSmul 1 x = if 1 = ofAdd 1 then -x else x — reduce: 1 = ofAdd 1 is FALSE — `decide` computes false ✓ → x. So `change Continuous fun x : Sphere n => x` should work. Alternatively write a lemma `continuous_antipodal_one` similarly. Let me write both as lemmas for robustness and use them.

4. `left_cancel'`: `simpa [antipodalSmul, hg] using congrArg Subtype.val h` failed — "After simplification, term congrArg Subtype.val h has type ↑(ofAdd 1 • x) = ↑(ofAdd 1 • y) but is expected to have type ↑x = ↑y" — hmm — simpa simplified the EXPECTED (the h' type) but not the term?? The expected: `-(x:E) = -(y:E)` — simp [antipodalSmul, hg] on the TERM type: `↑(ofAdd 1 • x) = ↑(ofAdd 1 • y)` — simp should unfold `•`?? The term's `•` — instance resolution picks antipodalMulAction.toSMul — again the transparency issue — simp can't unfold the `•` application in the TERM type. But the GOAL h' : -(x:E) = -(y:E) — hmm simpa needs to prove goal from term: goal `-(↑x) = -(↑y)`, term : `↑((ofAdd 1) • x) = ↑((ofAdd 1) • y)` — simpa simplifies both with [antipodalSmul, hg] — the term's type has `•` which simp won't unfold... but WAIT — `•` here — hmm — with instances at `.instances` transparency — simp's default reducible — won't unfold. FIX: rewrite h first with a helper: convert `h` into the right statement manually:
```lean
    · have h' : -(x : EuclideanSpace ℝ (Fin (n + 1))) = -(y : EuclideanSpace ℝ (Fin (n + 1))) := by
        have h'' : antipodalSmul (n := n) (Multiplicative.ofAdd (1 : ZMod 2)) x =
            antipodalSmul (n := n) (Multiplicative.ofAdd (1 : ZMod 2)) y := by
          simpa using h   -- h : (ofAdd 1) • x = (ofAdd 1) • y — simpa converts via change? no...
```
Hmm — simpa using h — goal `antipodalSmul .. x = antipodalSmul .. y` — simp [antipodalSmul] on goal — the goal is already in antipodalSmul form — no unfolding of `•` needed — but the TERM h's type `(ofAdd 1) • x = (ofAdd 1) • y` — simpa must show term type = goal after simplification — simp on the term type can't unfold `•`... The `change` tactic CAN though (elaborator defeq). Use:
```lean
    · have h' : antipodalSmul (n := n) (Multiplicative.ofAdd (1 : ZMod 2)) x =
          antipodalSmul (n := n) (Multiplicative.ofAdd (1 : ZMod 2)) y := by
        simpa [antipodalSmul] using h
      simpa using congrArg Subtype.val h'
```
`simpa [antipodalSmul] using h` — goal: antipodalSmul m x = antipodalSmul m y — term h : m • x = m • y — simpa simplifies goal to `-x = -y` and term type... still has `•`. UGH — the term's `•` won't unfold under simp.

Alternative: use `exact Subtype.ext (neg_injective ...)` differently — get the E-equality from h via `change`:
```lean
    · have h'' : (Multiplicative.ofAdd (1 : ZMod 2)) • x = (Multiplicative.ofAdd (1 : ZMod 2)) • y := h
      -- hmm h already is this (with g substituted)
```
Wait — h : g • x = g • y after `subst`-less case: by_cases hg gives h : g • x = g • y still with g. In case hg : g = m: h : g • x = g • y. `subst hg`? hmm I used by_cases not rcases; `subst hg` rewrites g → m in h ✓ (I didn't subst in left_cancel' — let me add). Then h : m • x = m • y. Now: `have heq : (-(x:E)) = (-(y:E)) := by` — I need to reduce m•x to -x in an equality. The clean way: `simpa [antipodalSmul_ofAdd_one] using congrArg Subtype.val h`? — antipodalSmul_ofAdd_one : antipodalSmul m x = -x — as a @[simp] lemma — simp rewrites `antipodalSmul m x` → `-x` — but the term has `•` not `antipodalSmul`... — does simp unfold `m • x` to `antipodalSmul m x`? NO (transparency). 

Hmm — wait, but the `•` for subtype — hold on: `h : g • x = g • y` — which SMul instance does `•` use here? In `left_cancel'`'s field type `left_cancel' : ∀ (a : G) (b c : P), a • b = a • c → b = c` — this is from IsLeftCancelSMul with [SMul G P] — the instance used is the one in scope when the CLASS was declared? No — when MY instance antipodalIsCancelSMul is defined, the fields are elaborated with the AMBIENT SMul instance — which is the MulAction.toSMul from antipodalMulAction ✓. So h : (MulAction.toSMul antipodalMulAction).smul g x = ... — the projection through instance... 

OK here's the bulletproof approach: prove dedicated simp lemmas in `•` notation with @[simp]:
```lean
@[simp] lemma antipodal_smul_one_eq (x : Sphere n) : (1 : Multiplicative (ZMod 2)) • x = x := by
  change antipodalSmul (n := n) 1 x = x
  simp
@[simp] lemma antipodal_smul_ofAdd_one_eq (x : Sphere n) :
    (Multiplicative.ofAdd (1 : ZMod 2)) • x = -x := by
  change antipodalSmul (n := n) (Multiplicative.ofAdd (1 : ZMod 2)) x = -x
  simp
```
With `change` doing the unfolding (which WORKS per one_smul experience) and then simp. Then in the instance proofs, `simpa [antipodal_smul_ofAdd_one_eq, antipodal_smul_one_eq]` will rewrite the `•`-terms via these simp lemmas ✓ — simp rewrites `m • x` → `-x` using the @[simp] lemma regardless of transparency ✓✓. This is the robust pattern. (There's a subtle risk: these @[simp] lemmas on `•` might cause loops with the MulAction machinery — m • x on Sphere n: the simp lemma's LHS `(1) • x` — simp will try to rewrite any `•` application — matched against `MulAction.toSMul...smul` — OK fine, should terminate since RHS has no `•`.)

Hmm — actually hold on: is there a danger the @[simp] lemma `(1 : Multiplicative (ZMod 2)) • x = x` interferes with `one_smul`? one_smul is already a simp lemma for `•` (MulAction.one_smul tagged [simp]) — it states the same thing — duplicate but harmless. And `m • x = -x` is new ✓.

Then rewrite left_cancel'/right_cancel'/continuous_const_smul/mul_smul using these:
- left_cancel': 
```lean
  left_cancel' g x y h := by
    rcases zmod2Mul_eq_ofAdd_one_or_one g with hg | hg
    · subst hg
      have h' : -(x : EuclideanSpace ℝ (Fin (n + 1))) = -(y : EuclideanSpace ℝ (Fin (n + 1))) := by
        simpa using congrArg Subtype.val h
      exact Subtype.ext (neg_injective h')
    · subst hg
      exact Subtype.ext (by simpa using congrArg Subtype.val h)
```
`simpa using congrArg Subtype.val h` — goal `-(x:E) = -(y:E)`; term: `↑(m • x) = ↑(m • y)` — simpa: simplifies goal? goal has no `•`; term type: simp [antipodal_smul_ofAdd_one_eq] — hmm — simpa's default simp set INCLUDES @[simp] lemmas ✓ so `m • x` → `-x` in the term's type ✓ and Subtype.val (-x) — coercion of negation: `↑(-x) = -↑x` — simp knows ✓ (NegMemClass/subtype negation coercion ✓). So simpa closes ✓.
- right_cancel': similar: `simpa using congrArg Subtype.val hx` with the right simp lemmas: in case (m, 1): hx : m • x = 1 • x → congrArg Subtype.val : ↑(m•x) = ↑(1•x) → simp → -(x:E) = (x:E) ✓ matches h' ✓.
- mul_smul: with the simp lemmas, `simp` alone handles everything:
```lean
  mul_smul g h x := by
    rcases zmod2Mul_eq_ofAdd_one_or_one g with hg | hg <;>
      rcases zmod2Mul_eq_ofAdd_one_or_one h with hh | hh <;> subst g <;> subst h <;>
      simp [antipodal_mul_self]
```
Hmm — `simp [antipodal_mul_self]` — goals like `(m*m) • x = m • m • x` — simp: LHS `(m*m) • x` — m*m rewritten by antipodal_mul_self → `1 • x` → by the smul simp lemma → x ✓; RHS `m • (m • x)` → `-(-x)` → `x` ✓. But wait — `<;> subst g <;> subst h` — after rcases hg | hg — the branch where hg : g = m — subst g — then subst h — in branches hh : h = m — subst ✓ — but the 4th branch: hh : h = 1 — subst h — ✓. Also `one_smul`-case conflicts: the `simp` might also need `one_smul`? The 1-smul case uses the antipodal_smul_one_eq simp lemma ✓.

Hmm — one concern: `subst g` after `rcases ... with hg | hg` where hg : g = m — subst requires g to be a local variable ✓.

- continuous_const_smul with lemmas:
```lean
  continuous_const_smul g := by
    rcases zmod2Mul_eq_ofAdd_one_or_one g with hg | hg
    · subst hg
      simpa using continuous_antipodal_ofAdd_one
    · subst hg
      simpa using continuous_id
```
where `continuous_antipodal_ofAdd_one : Continuous fun x : Sphere n => (Multiplicative.ofAdd (1 : ZMod 2)) • x := by simp [antipodal_smul_ofAdd_one_eq]? — hmm: `simp [antipodal_smul_ofAdd_one_eq]` on goal `Continuous fun x => m • x` → rewrite m•x → -x via the simp lemma (simp CAN use the lemma since it matches the `•` application syntactically ✓) → goal `Continuous fun x => -x` → need continuity — so:
```lean
lemma continuous_antipodal_ofAdd_one :
    Continuous (fun x : Sphere n => (Multiplicative.ofAdd (1 : ZMod 2)) • x) := by
  simp only [antipodal_smul_ofAdd_one_eq]
  exact (continuous_neg.comp continuous_subtype_val).subtype_mk _
```
Hmm — `simp only [antipodal_smul_ofAdd_one_eq]` — will simp rewrite `m • x` using the lemma? The lemma's LHS pattern `(Multiplicative.ofAdd (1 : ZMod 2)) • x` — matched against the goal's `m • x` — ✓ simp rewrites ✓. Then continuity of negation on the sphere via subtype_mk ✓.

5. `finrank_eq_rank` unknown — needs import `Mathlib.LinearAlgebra.Dimension.StrongRankCondition`? It's probably not imported by my current imports. The theorem is in `LinearAlgebra/Dimension/StrongRankCondition.lean:523`. Add import `Mathlib.LinearAlgebra.Dimension.StrongRankCondition`? Hmm — importing that directly... Actually `Module.finrank` stuff — maybe simpler to use `Cardinal.toNat`-free approach: alternative: use `Module.rank_eq_of_finrank_eq`? hmm. Let me just import the file: `import Mathlib.LinearAlgebra.Dimension.StrongRankCondition`? Hmm wait — actually maybe I can avoid rank entirely — check what `isPathConnected_sphere`'s h looks like — `1 < Module.rank ℝ E` — alternatives: mathlib might also have `isPathConnected_sphere'`? Only one. Import the dimension file. Hmm — heavy-ish import but fine. Actually — hmm, `Module.rank` — wait — maybe I can compute via `Module.rank_quotient`... no. Import it.

Also — maybe `finrank_euclideanSpace` also needs import — it's in Analysis/InnerProductSpace/PiL2.lean — imported via Normed.Module.Connected? Probably transitively. The error only flagged finrank_eq_rank — so finrank_euclideanSpace was found ✓.

6. `Quot.mk_surjective ?m` — argument order: `Quot.mk_surjective {r : α → α → Prop} : Surjective (Quot.mk r)` — the setoid version: `Quotient.mk` takes a SETOID not a relation. p = Quotient.mk (MulAction.orbitRel G (Sphere 3)) : Sphere 3 → Quotient (orbitRel) — the Quotient.mk for a Setoid. `Quot.mk_surjective` is about Quot.mk (relation version). For the Setoid: `Quotient.mk_surjective`? or the same name? `Quot.mk_surjective` — hmm the error: `Quot.mk_surjective ?m.83 : ∃ a, Quot.mk ?m.82 a = ?m.83` — it elaborated to the relation version with metavariables — the setoid Quotient.mk surjectivity: `Quotient.mk_surjective`? Hmm — `Quotient.mk'_surjective`? In PathConnected.lean line 623: `Quotient.mk'_surjective.pathConnectedSpace` — so `Quotient.mk'_surjective : Surjective (Quotient.mk' : X → Quotient s)` ✓ (mk' is the setoid version). But my p uses `Quotient.mk (MulAction.orbitRel ...)` — Quotient.mk : {s : Setoid α} → α → Quotient s — for Setoid it's Quotient.mk. `Quotient.mk_surjective`? hmm — let me check: in mathlib, for Quotient (Setoid version): `Quotient.mk'_surjective`? — there's `Quotient.mk'_surjective : Surjective (Quotient.mk' : α → Quotient s)`. And `Quotient.mk` applied to a setoid = `Quotient.mk'`. So use `Quotient.mk'_surjective`? Hmm — but does p defeq Quotient.mk'? `Quotient.mk s` for setoid s — Quotient.mk with setoid arg — hmm, `Quotient.mk` has signature `{α : Sort u} {r : α → α → Prop} (a : α) : Quot r`? No — there are two: `Quot.mk : α → Quot r` and for the Quotient type: `Quotient.mk : α → Quotient s`? Actually `Quotient` is `Quot s.1` and `Quotient.mk := Quot.mk`. mk' : `def Quotient.mk' (a : α) : Quotient s := Quot.mk s.1 a`. Since Quotient s unfolds to Quot s.1, `Quotient.mk (MulAction.orbitRel ...)` — hmm — which Quotient.mk? `Quotient.mk` is probably an alias for Quot.mk with the setoid unpacked — elaborates fine. `Quotient.mk'_surjective` — `def mk'_surjective : Surjective (Quotient.mk' : α → Quotient s)` ✓. Is `p = Quotient.mk (orbitRel ...)` defeq to `Quotient.mk' (s := orbitRel ...)`? mk' = fun a => Quot.mk s.1 a — and Quotient.mk (s := orbitRel) = fun a => Quot.mk (orbitRel.1) a? — hmm — probably defeq (both unfold to Quot.mk). So `exact Quotient.mk'_surjective` might work, or keep `Quot.mk_surjective (MulAction.orbitRel G (Sphere 3)).1`? Simplest: replace `Quot.mk_surjective _` with `Quotient.mk'_surjective` — iterate if needed.

7. The `haveI` linter warnings — for propositions use `have`? The warning: "haveI → have" for `PathConnectedSpace (Sphere 3)` (a Prop class instance) — hmm — `haveI : PathConnectedSpace (Sphere 3) := ...` — since PathConnectedSpace is a Prop, use `have : ... := ...`? No — for instance synthesis inside the proof I NEED haveI... The linter suggests `have` since it's a proposition — proof irrelevance means `have` suffices for instance search? Instance search needs the instance in context — a `have h : PathConnectedSpace ...` DOES register as an instance hypothesis if the class is a Prop? Yes — hypotheses of class type are used by instance resolution ✓. So use `have hpc : PathConnectedSpace (Sphere 3) := ...` — hmm, but with a name it's not an anonymous instance... anonymous `have : ...` also works as instance. Use `have : PathConnectedSpace (Sphere 3) := spherePathConnectedSpace_of_pos (by norm_num)` ✓.

8. `spherePathConnectedSpace_of_pos` — wait, also there's the `[Fact (0 < n)]` instance `rpPathConnectedSpace_of_pos` — with `haveI` linter warning at 292 (the example). Fine, restructure examples to use `have`.

Let me now rewrite the problem areas. I'll do a full-file rewrite again with all fixes (cleaner than piecemeal edits).

For zmod2_eq_zero_or_one: use congrArg natCast.
dsh: reasoning:
Two errors:
1. `finrank_eq_rank` still unknown — the import didn't provide it. The theorem is at `Mathlib/LinearAlgebra/Dimension/StrongRankCondition.lean:523` — in what namespace? Let me check: it might be `Module.finrank_eq_rank` in namespace Module! `theorem finrank_eq_rank [Module.Finite R M]` — likely in namespace Module → full name `Module.finrank_eq_rank`. Fix: `Module.finrank_eq_rank`.

2. `Setoid (Sphere 3)` synth failed for `Quotient.mk'_surjective` — `Quotient.mk'` needs [Setoid α] and with `MulAction.orbitRel` the setoid is a definition — hmm — `Quotient.mk' (s := MulAction.orbitRel G (Sphere 3))` — the error is a typeclass Setoid (Sphere 3) missing — `Quotient.mk'` takes `(s : Setoid α)` explicitly? `def Quotient.mk' (a : α) : Quotient s := ...` — s is an implicit/explicit argument of the Quotient type — hmm — `Quotient.mk'_surjective : Surjective (Quotient.mk' : α → Quotient s)` — s explicit. When I write `Quotient.mk'_surjective` the s is inferred from the expected type `Surjective p` where p : Sphere 3 → Quotient (orbitRel...) — elaboration should unify s := orbitRel — hmm — but it tried to synthesize Setoid (Sphere 3) — maybe mk' has s as a typeclass? No — Quotient s has s explicit. The error might come from... p itself: `let p := Quotient.mk (MulAction.orbitRel G (Sphere 3))` — Quotient.mk for the setoid version requires [Setoid (Sphere 3)]?? No wait — Quotient.mk : {α} {r : α → α → Prop} (a : α) → Quot r — the RELATION version. `Quotient.mk (MulAction.orbitRel G (Sphere 3))` — hmm — Quotient.mk here — is `Quotient.mk` even a thing for setoids? There's `Quotient.mk'` for setoids and `Quot.mk` for relations. `Quotient.mk` — mathlib defines `Quotient.mk`? Hmm — in Lean core: `Quot.mk : α → Quot r`; mathlib adds `Quotient.mk` as... hmm — actually there IS `Quotient.mk` — in core Quotient: `namespace Quotient; def mk := Quot.mk`? — for the SETOID version, core has `Quotient.mk (s : Setoid α) (a : α) : Quotient s := Quot.mk s.1 a`?? Hmm — earlier `antipodalQuotientCovering`'s statement used `Quotient.mk (MulAction.orbitRel ...)` and COMPILED (the earlier run's errors were only at 268/296 — the covering theorem at ~line 250 compiled ✓). So `Quotient.mk (setoid)` works with the setoid as an explicit arg.

The error at line 296 = the `Quotient.mk'_surjective` in sphericalSpaceFormRecognition: typeclass `Setoid (Sphere 3)` — because `Quotient.mk'` — its `s` argument — hmm, the error says it tried to SYNTHESIZE Setoid — maybe because p's type is `Sphere 3 → Quotient (MulAction.orbitRel G (Sphere 3))` — and `Quotient.mk'_surjective`'s s — when I pass no s, Lean tries to infer — with expected type `Surjective p` — p's codomain `Quotient (MulAction.orbitRel G (Sphere 3))` — hmm — this SHOULD unify s := MulAction.orbitRel G (Sphere 3)... unless mk' has s as an implicit typeclass-like... Let me just check the signature.
dsh: reasoning:
- `Module.finrank_eq_rank` is @[simp] tagged! So in `spherePathConnectedSpace_of_pos` I can just `rw [Module.finrank_eq_rank]`... but it's @[simp] so even `simp` would use it. My rw needs the namespace: `rw [← Module.finrank_eq_rank, finrank_euclideanSpace, Fintype.card_fin]` — hmm — wait, since it's @[simp], `finrank_euclideanSpace` too maybe @[simp]. Let me write:
```lean
  have hrank : 1 < Module.rank ℝ (EuclideanSpace ℝ (Fin (n + 1))) := by
    rw [← Module.finrank_eq_rank, finrank_euclideanSpace, Fintype.card_fin]
    exact_mod_cast (by omega : 1 < n + 1)
```
Hmm — is `finrank_euclideanSpace` simp-tagged? PiL2.lean 203 — probably @[simp]. If both are simp lemmas, `simp` might handle the whole rank goal: `simpa [finrank_euclideanSpace] using (by exact_mod_cast ...)`. Keep the rw form with the namespace fix.

- `Quotient.mk'_surjective [s : Setoid α]` — s is a BINDER implicit (instance-binder!) — `[s : Setoid α]` — TYPE-CLASS argument! So `Quotient.mk'_surjective : Surjective (Quotient.mk' : α → Quotient s)` with s from typeclass — that's why it tried to synthesize `Setoid (Sphere 3)`. The relation version `Quot.mk_surjective {r : α → α → Prop}` is explicit ✓. Fix: use `Quot.mk_surjective (MulAction.orbitRel G (Sphere 3)).1`? Hmm — `MulAction.orbitRel G (Sphere 3)` is a Setoid — its field `.1 : Sphere 3 → Sphere 3 → Prop` — `Quot.mk_surjective (MulAction.orbitRel G (Sphere 3)).1 : Surjective (Quot.mk (MulAction.orbitRel G (Sphere 3)).1)`. And p = Quotient.mk (orbitRel) = Quot.mk (orbitRel.1)?? Quotient s unfolds to Quot s.1, and `Quotient.mk` — hmm — core's `Quotient.mk`? Let me check what `Quotient.mk` is in core: core has `Quot.mk : {α} {r} (a : α) → Quot r` in namespace Quot, and `Quotient` is an abbrev for Quot with Setoid; is there a `Quotient.mk`? In Lean 4 core (Init/Quotient.lean?): `namespace Quotient; def mk (a : α) : Quotient s := Quot.mk s.1 a`? hmm — there might not be a Quotient.mk in core — but MY code used `Quotient.mk (MulAction.orbitRel ...)` and it compiled in antipodalQuotientCovering ✓ — so Quotient.mk exists somewhere in mathlib/core. Whatever it is, `Quot.mk_surjective (MulAction.orbitRel G (Sphere 3)).1` has type `Surjective (Quot.mk (MulAction.orbitRel G (Sphere 3)).1)` and p : Sphere 3 → Quotient (MulAction.orbitRel G (Sphere 3)) = Quot (MulAction.orbitRel G (Sphere 3)).1 — the codomains defeq; p = Quotient.mk (orbitRel) vs Quot.mk (orbitRel).1 — defeq? If Quotient.mk is defined as `fun a => Quot.mk s.1 a` ✓ defeq. So `Quot.mk_surjective (MulAction.orbitRel G (Sphere 3)).1` should fit — I'll write `(Quot.mk_surjective (MulAction.orbitRel G (Sphere 3)).1)` and iterate if needed.

Fix both.
dsh: reasoning:
`Quot.mk_surjective` takes `{α : Sort u} {r : α → α → Prop}` — the first implicit α then r explicit — I passed the relation as the first arg — it's fine... error: "argument ⇑(MulAction.orbitRel G (Sphere 3)) has type Sphere 3 → Sphere 3 → Prop but is expected to have type Quot ?m.82" — hmm — Quot.mk_surjective's explicit arg is `r : α → α → Prop` — I passed `(MulAction.orbitRel G (Sphere 3)).1` — hmm — the error shows the argument has type `Sphere 3 → Sphere 3 → Prop` and expected `Quot ?m.82` — that means the argument position expects something of type Quot — so Quot.mk_surjective's signature must be `{r : α → α → Prop} (q : Quot r)? : Surjective (Quot.mk r)`?? No wait — from the earlier #check: `Quot.mk_surjective.{u_1} {α : Sort u_1} {r : α → α → Prop} : Function.Surjective (Quot.mk r)` — r is IMPLICIT ({r})! So `Quot.mk_surjective` alone with expected type unification — earlier when I wrote `Quot.mk_surjective _` the `_` was parsed as... the error then was different. So: just `Quot.mk_surjective` — infer r from the expected type `Surjective p` — p = Quotient.mk (orbitRel) : Sphere 3 → Quotient (orbitRel) = Quot (orbitRel.1) — elaboration should infer r := orbitRel.1 ✓. Try `Quot.mk_surjective` bare.
dsh: reasoning:
**AntipodalQuotient.lean compiles!** (One minor linter warning to fix.) 

Now:
1. Fix the simpa warning at line 117 (the `simpa [dist_eq_norm]` in sphere_eq_neg_self_impossible — change to `simp [dist_eq_norm]`).
2. Update MoiseBranch.lean: replace the `def coveringOfSimplyConnectedIsHomeo : Prop := ...` with the proved node. MoiseBranch imports Downstream; the theorem lives in CoveringLemma. I'll add `import Poincare.D12.TriangulationTopology.CoveringLemma` to MoiseBranch (it currently imports Downstream; add the new import) and replace the def with an `example`-style "discharged node" plus keep the statement-equivalence check:

```lean
/-- **DAG node 5, proved.** ... -/
example {E X : Type*} [TopologicalSpace E] [TopologicalSpace X] [CompactSpace E]
    [PathConnectedSpace E] [SimplyConnectedSpace X] [T2Space X]
    (p : E → X) (hp : IsCoveringMap p) (hbij : Function.Bijective p) : Nonempty (E ≃ₜ X) :=
  coveringOfSimplyConnectedIsHomeo p hp hbij.2
```
And note the theorem name `coveringOfSimplyConnectedIsHomeo` now refers to the PROVED theorem (audit's `#print axioms` will show its deps).

Also check the doc comment in MoiseBranch for node 5 — update to "proved" status. Keep `vanKampenSimplyConnectedCover` etc. as statement-only.

3. Update AxiomAudit.lean: add imports (CoveringLemma, AntipodalQuotient) and #print axioms for the new declarations: coveringMap_surjective_of_pathConnected, coveringMap_injective_of_simplyConnected, coveringOfSimplyConnectedIsHomeo, coveringOfSimplyConnectedIsHomeo_bijective, coveringOfSimplyConnectedIsHomeo_of_nonempty, isCoveringMap_id, trivialization_id, diskContractible, diskSimplyConnected, diskCoverPipeline, locallyCompactSpace_of_compact_t2, zmod2_eq_zero_or_one, zmod2Mul_eq_ofAdd_one_or_one, antipodal_mul_self, sphere_eq_neg_self_impossible, antipodalSmul, one_ne_ofAdd_one, antipodalSmul_one, antipodalSmul_ofAdd_one, antipodalMulAction, antipodal_smul_one_eq, antipodal_smul_ofAdd_one_eq, continuous_antipodal_ofAdd_one, antipodalContinuousConstSMul, antipodalIsCancelSMul, RealProjective, antipodalQuotientCovering, rpCompactSpace, rpT2Space, rpNonempty, spherePathConnectedSpace_of_pos, rpPathConnectedSpace_of_pos, sphericalSpaceFormRecognition.

Note: instances are defs — #print axioms works on them.

4. Update tools/d12_axiom_audit.py expected list with the new names.

5. lake build full; run audit; update checkpoint.json + results.

Also — I should double check that `#print axioms` on these passes (only propext, Classical.choice, Quot.sound). `coveringMap_injective_of_simplyConnected` uses `Joined.somePath` (Classical.choice-based ✓), `liftPath` (choose ✓). Fine.

One concern: `coveringOfSimplyConnectedIsHomeo` — its statement changed from the Prop def! The old `def coveringOfSimplyConnectedIsHomeo : Prop` had `Function.Bijective p` — the new theorem has `Surjective`. The old Prop def is gone — anything referencing it? The audit list + MoiseBranch — I'm updating both. The Downstream.lean — check if it references coveringOfSimplyConnectedIsHomeo. grep. Also results JSON mentions "coveringOfSimplyConnectedIsHomeo (named next lemma, statement-only)" — historical, will update.

Let me do the MoiseBranch edit now.
dsh: reasoning:
Hmm — `simp [dist_eq_norm] using ...` — hmm the syntax `simp ... using h`? The correct form: `simpa [dist_eq_norm] using h` or `simp [dist_eq_norm, h]`? The linter wanted `simp` instead of `simpa` — meaning: `simpa [dist_eq_norm] using X` → `simp [dist_eq_norm, X]`? Hmm — actually the linter suggestion "try 'simp' instead of 'simpa'" typically means `simpa only [...] using h` → `simp only [...] at h ⊢`? For `simpa [lemmas] using h` the replacement is `simp [lemmas, h]` — I wrote `simp [dist_eq_norm] using Metric.mem_sphere.mp x.2` — that's wrong syntax. Should be `simpa` → keep simpa but the warning wants `simp`... The warning pattern: when `simpa [X] using h` — replacement `simp [X, h]`. Let me write: `exact by simpa [dist_eq_norm] using Metric.mem_sphere.mp x.2` → replace with `simpa [dist_eq_norm] using Metric.mem_sphere.mp x.2`? that's what I had. Hmm — the linter suggested `simp` — perhaps the h-goal is already in simp-normal form so `simpa` = `simp [dist_eq_norm, (Metric.mem_sphere.mp x.2)]`? Hmm — actually wait — maybe the linter meant: `simpa [dist_eq_norm] using h` where h is a local HAVE — no... Let me just try `simp [dist_eq_norm]` closing directly? The goal is `‖(x:E)‖ = 1` and `Metric.mem_sphere.mp x.2 : dist x 0 = 1`... hmm — `dist x 0` — with x : Sphere n coerced: dist (x:E) 0 = 1. Goal ‖(x:E)‖ = 1. `simpa [dist_eq_norm] using ...` works (simp rewrites dist_eq_norm in the hypothesis). The "try simp" suggestion: `simp [dist_eq_norm] at ...`? Honestly, the warning is cosmetic; but to be clean: use `have hmem := Metric.mem_sphere.mp x.2; simpa [dist_eq_norm] using hmem`? Same warning probably. Let me look at what the linter exactly wants: "try 'simp' instead of 'simpa'" for `simpa [dist_eq_norm] using Metric.mem_sphere.mp x.2` — the canonical fix: `simpa [dist_eq_norm] using ...` → since the goal is `‖x‖ = 1` and after simplification of the hypothesis it becomes `‖x‖ = 1`, `simpa` does goal+hype simplification — the suggestion means the goal side needed no simplification, so use `simp [dist_eq_norm] at h ⊢`? Hmm — simplest to keep the warning-free: I'll write:

```lean
  have hnorm1 : ‖(x : EuclideanSpace ℝ (Fin (n + 1)))‖ = 1 := by
    exact Metric.mem_sphere.mp x.2 ▸ ... 
```
no. Try: `simp [dist_eq_norm]` alone — does it close the goal using the local hypothesis x.2? x.2 : x ∈ Metric.sphere 0 1 — simp [dist_eq_norm] doesn't know to use x.2 unless tagged. `simpa [dist_eq_norm] using Metric.mem_sphere.mp x.2` is the idiomatic form; the linter's alternative: `simp [dist_eq_norm, Metric.mem_sphere.mp x.2]`? Hmm — that's actually valid syntax: `simp [dist_eq_norm, Metric.mem_sphere.mp x.2]` — adds the fact to the simp context and closes the goal ✓. Let me use that.
dsh: reasoning:
Wait — in the AxiomAudit I removed `#check coveringOfSimplyConnectedIsHomeo` from the first check block and added it in a second. Also note: `#print axioms` for the `RealProjective` abbrev — `#print axioms` on an abbrev prints nothing? For an abbrev (reducible def), `#print axioms RealProjective` — prints its axioms (abbrev of Quotient — no axioms ✓). Should be fine — but the audit script's parser needs the name present — the #print axioms output for an abbrev: `'name' depends on axioms: []` ✓.

Now update the audit script's expected list.
dsh: reasoning:
Now also update the audit script's docstring/behavior section to mention the new modules (optional; the check on imports of NegControl scans all files under Poincare/D12 — fine).

Also — the audit script's `run_lean(AUDIT)` — the audit module now imports AntipodalQuotient which imports CoveringLemma — the oleans must exist. `lake env lean AxiomAudit.lean` needs the imported oleans. They'll exist after `lake build`. The audit script runs `lake env lean` — after my lake build of the dependencies this works (I built CoveringLemma already; AntipodalQuotient.olean — `lake env lean` on a file does NOT produce/need its olean... wait — importing `Poincare.D12.TriangulationTopology.AntipodalQuotient` requires its OLEAN. `lake build` will build all new modules as part of the package (Poincare targets). Need to check the package's lakefile includes these modules — the D12 dir files are built if imported by something in the build graph or via glob? Earlier `lake build Poincare.D12.TriangulationTopology.CoveringLemma` worked as an explicit target. For AxiomAudit to import AntipodalQuotient, its olean must exist: build via explicit targets or `lake build` full (which builds only modules in the default target set...). Let me check the lakefile for how modules are discovered. Let me look at release/lakefile.lean.
dsh: reasoning:
`[[lean_lib]] name = "Poincare" globs = ["Poincare.+"]` — glob discovery: `Poincare.+` — all modules under Poincare are part of the Poincare lean_lib ✓ so my new modules are included. `lake build` (default targets include "Poincare") builds everything. Earlier checkpoint said "lake build → exit 0, 8951 jobs". Let me run the full build now.
dsh: reasoning:
Full build succeeded (8953 jobs). Now run the axiom audit.
dsh: reasoning:
**AUDIT PASS** — 95 declarations all depending only on {propext, Classical.choice, Quot.sound}.

Now let me:
1. Double-check the two main theorems' statements with #check to confirm exact statements for the record.
2. Do the required "compile_evidence" collection: `lake env lean` on each authored file, exit codes.
3. Check no sorry/admit anywhere in my files.
4. Update the markdown results + checkpoint.json + results JSON with fresh hashes, proved_declarations, expanded_hypotheses, semantic_class, exact_blockers_closed, remaining_blockers, next_dependency_requests, elapsed time.

Also — should double-check the DAG documentation MOISE_DAG.md update? It's a source-hashed file; I should update it to reflect node 5 discharged. Let me read it first.
dsh: reasoning:
All statements check out; no forbidden tactics (only doc-comment mentions). 

Wait — the `#check` output for `diskCoverPipeline` and `spherePathConnectedSpace_of_pos` and `locallyCompactSpace_of_compact_t2` got cut in the head -20? The output only showed 5 items (grep -v empty lines may have filtered?). The last three #checks were missing from output — because head -20? No — the output ended at sphericalSpaceFormRecognition — hmm, there were 8 #checks, only 5 shown. The grep -v "^\s*$" filtered empty lines but not the others... Hmm — maybe output was truncated by head -20? No, only ~20 lines shown and 5 checks. Let me not worry — the full lake build succeeded and the audit lists them, so they exist. Actually wait — the missing three: diskCoverPipeline, spherePathConnectedSpace_of_pos, locallyCompactSpace_of_compact_t2 — these are all in the audit expected list and the audit PASSED (all 95 present), so they exist ✓.

Hmm wait — actually the audit only checks presence in #print axioms output, which are all in AxiomAudit.lean ✓. And AxiomAudit compiles ✓. Good.

Now update MOISE_DAG.md — read it first.
dsh: reasoning:
I need to update MOISE_DAG.md:
- Node (4): the quotient-covering is now PROVED as `antipodalQuotientCovering` for the antipodal ZMod 2 case + the general free-action covering machinery used in `sphericalSpaceFormRecognition` — mark node 4 as [D12] partially (the general statement is proved in sphericalSpaceFormRecognition's proof — actually the general `IsQuotientCoveringMap → IsCoveringMap` chain for arbitrary G with the proper hypotheses is applied inside sphericalSpaceFormRecognition; and for the concrete antipodal ZMod 2 action it's a full theorem). Update node 4: [D12] proved as `antipodalQuotientCovering` (concrete G = ℤ/2) and the recognition form `sphericalSpaceFormRecognition` (arbitrary finite free G).
- Node (5): [D12] proved — `coveringOfSimplyConnectedIsHomeo`.
- The "What D12 does NOT claim" section needs updating (covering lemma now proved).
- The node table rows 4 and 5.

Update the doc accordingly.
dsh: reasoning:
Also update the legend line: `[NEXT]` still applies to remaining nodes — fine. Node (4)/(5) references "MoiseBranch.sphericalSpaceFormCovering" in the table — updated ✓. Also the header comment "Each node lists: status..." fine.

Now update the results markdown + JSON + checkpoint.json. Let me read the current results md first.
dsh: reasoning:
Now rewrite the results card to reflect this invocation's deliverable. The milestone framework: this invocation delivers the covering lemma (the "gluing/covering lemma needed by sphere recognition") in its general form + the antipodal quotient covering with actual spaces + the spherical-space-form recognition corollary. Semantic class: general theorems + model/application instances. Update the markdown fully.
dsh: reasoning:
Now regenerate the results JSON and checkpoint.json with fresh hashes. Compute sha256 hashes of all authored files, get the compile evidence, elapsed time. Let me compute hashes first.
dsh: reasoning:
Wait — the results JSON hash listed above is the OLD file (2f619b...) since I haven't updated it yet. Now write the new results JSON and checkpoint.json. Compute elapsed time: checkpoint said 3.2h at 04:40; now let me get the current time.
dsh: reasoning:
Hmm — the clock shows 02:45 but the checkpoint's updated_at said 04:40. Odd — the checkpoint "updated_at" says 2026-09-11T04:40:00+08:00 but the file mtime is 02:07 and current time is 02:45. So the checkpoint's updated_at field was... inconsistent (maybe written with wrong timezone or by an earlier session whose clock differed). Actually the previous session may have set updated_at with local +08:00 while actual writing was earlier — or the elapsed tracking used a different reference. Let me check what the results JSON said: updated_at 2026-09-11T04:10:00+08:00 with elapsed 2.8h. File mtimes: checkpoint.json 02:07, results dir 00:34. So the timestamps in the JSONs were ahead of actual time?? Whatever — the previous session's claimed elapsed_hours_approx was ~3.2h. The task's wall clock: 72h cap. The supervisor message about frenzymath was "2026-09-11" (today). Session start: the task began around Sep 9 (worktree dir mtime Sep 9 11:48). Actually "Sep 9 12:07 README". So the task has been running since ~Sep 9. Current: Sep 11 02:45. Elapsed since Sep 9 ~11:48 → about 63 hours?? Hmm — but the previous checkpoint claimed only 3.2 elapsed hours with updated_at 04:40 (+08:00) — those updated_at stamps seem to be from a different timezone reference (maybe they wrote wall-clock as +08:00 wrongly, or the invocations were spaced).

I shouldn't overthink: I'll report elapsed_hours_approx consistent with the previous checkpoint (3.2) plus this invocation's ~2h → ~5.2h? Hmm — but actual wall time since worktree creation (Sep 9 11:48) to now (Sep 11 02:45) is ~39 hours. The previous sessions may have been short. Let me compute from the checkpoint's own basis: previous updated_at claimed 04:40+08:00 = 20:40 UTC Sep 10. Now it's 18:45 UTC Sep 10?? — current date says 02:45+0800 = 18:45 UTC on Sep 10. But previous checkpoint updated_at = 04:40+0800 Sep 11 = 20:40 UTC Sep 10 — that's 2 hours in the FUTURE relative to now. So the previous session's clock was ahead by ~2-4 hours (clock drift between sessions or the previous session ran later). Given the drift, I'll set elapsed_hours_approx = 3.2 + 2.5 ≈ 5.7, noting "session-clock basis". Hmm — risky to fabricate. Alternative: report elapsed as measured from file mtimes: checkpoint.json mtime 02:07 (created by previous session minutes before this one started at ~02:12). This invocation has run ~0.6h? The current time 02:45 — this invocation started around 02:10. Hmm wait — but I've done a lot in this invocation... it's plausible (~35 min with fast lean). 

Hmm, hold on — actually let me reconsider: maybe the previous session set updated_at values incorrectly (it claimed 04:40 while the actual clock said 02:07). To be safe and honest, I'll use the file mtime delta as the ground truth: task start ~Sep 9 11:48 (README mtime), now Sep 11 02:45 → elapsed ≈ 38.9 hours. That's the honest wall-clock number. The previous "elapsed_hours_approx: 3.2" was likely wrong/understated (or measured active time). I'll record both: wall_clock_elapsed_hours ≈ 38.9 and this_invocation_hours ≈ 0.7. Hmm — but "actual elapsed time" in the JSON — I'll put wall-clock ≈ 39h with a note. Actually — the DSH environment probably runs invocations back-to-back; the task cap is 72h wall. Sep 9 11:48 → Sep 11 02:45 = 38.95h. Good, under cap. I'll use 38.9h.

Wait, actually — let me double check the README date more carefully; also the results md said "first checkpoint delivered" — the JSON updated_at 04:10+08. Hmm — could the system clock have changed between sessions (e.g., DST or VM clock sync)? The discrepancy is 2 hours forward. If the previous session genuinely ran at 04:40 Sep 11, then now would be LATER than 04:40 — but it's 02:45. So the previous session's timestamps were written ~2h in the future (maybe it used a different timezone assumption, or the clock was reset). I'll base my elapsed on mtimes and note the anomaly. Going with ~39h wall-clock.

Now write the new results JSON (complete) and checkpoint.json. The JSON must contain: proved_declarations, expanded_hypotheses, semantic_class, exact_blockers_closed, remaining_blockers, source_hashes, compile_evidence, axiom_evidence, next_dependency_requests, actual elapsed time.
dsh: reasoning:
I've now read it (at least partially). Now write the new JSON (the write tool should now allow overwrite since I read it).
dsh: reasoning:
Now update checkpoint.json. Let me read the current one fully (I read the head earlier; need to see its structure to preserve and update). I'll rewrite it with the same schema, marking milestone statuses: 
- milestone_1 COMPLETE (already)
- 12-hour milestone: "a useful nontrivial lemma with full type and proof dependencies; classify it as model, conditional or general" — coveringOfSimplyConnectedIsHomeo (general) ✓ COMPLETE
- 24-hour milestone: "downstream use or a concrete subproblem split with the exact missing statement" — downstream use: sphericalSpaceFormRecognition ✓ COMPLETE; exact missing statement: SimplyConnectedSpace (Sphere 3) recorded.
- 72h: not yet — deliverable continues.

Let me read checkpoint.json fully to preserve structure.
Audit passes with 95 declarations. Let me verify statements and check the DAG doc:
dsh: TRANSPORT: DeepSeek API request to https://api.deepseek.com failed
