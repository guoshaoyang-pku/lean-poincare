#!/usr/bin/env bash
# Sanitize again after an editor or other commit tooling changes the message.
set -euo pipefail

hook_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec "$hook_dir/sanitize-commit-message" "$1"
