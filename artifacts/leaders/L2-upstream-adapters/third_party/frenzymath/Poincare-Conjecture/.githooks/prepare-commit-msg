#!/usr/bin/env bash
# Unlike commit-msg, prepare-commit-msg is not skipped by git commit --no-verify.
set -euo pipefail

hook_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec "$hook_dir/sanitize-commit-message" "$1"
