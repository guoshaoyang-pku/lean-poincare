{
  "id": "L4-child-bishop-gromov-interface",
  "group_id": "L4-geometric-critical-path",
  "parent_node": "U9",
  "deps": [
    "L4-child-ricci-to-doubling",
    "L4-child-gh-family-covers"
  ],
  "lane": "builder",
  "acceptance": "Make the curvature -> uniform-doubling interface EXACT and non-vacuous at the metric-measure level, without claiming a manifold measure. Consume release/Poincare/L4/Compactness/MeasureGrowthCovers.lean and release/Poincare/D12/ComparisonGeodesics/VolumeRatio.lean (bishopGromov_volume_le, bishopGromovVolumeRatio). Required content: (1) define an explicit hypothesis STRUCTURE (e.g. UniformBallGrowth) that packages exactly what the round-3 compactness assembly needs: a measure mu on each member, a lower density bound m on balls of a fixed scale, and a single-scale or all-scales reverse-doubling constant, with the fields documented as the Bishop-Gromov conclusion shape; (2) prove that the structure implies the uniform doubling hypothesis consumed by release/Poincare/L4/Compactness/DoublingToCovers.lean (coveringNumber r (closedBall c (2r)) <= n at every scale with ONE n), constructing n explicitly from the growth constants; (3) instantiate the structure non-vacuously on at least one explicit space (Euclidean R^d with Lebesgue measure, or a finite metric space with counting measure) and machine-check the resulting doubling constant; (4) state as a separate documented Prop (statement-only, no proof) the missing geometric realization 'curvature bound + kappa-non-collapsing => UniformBallGrowth', quoting the D12 Bishop-Gromov ODE input it would consume. Acceptance evidence: compiling modules with full signatures, source hashes, compile exits, fail-closed axiom audit with cones subset {propext, Classical.choice, Quot.sound}, non-vacuous witness, and an adversarial semantic review confirming that no hypothesis is conclusion-equivalent and that no manifold measure or curvature theorem is claimed.",
  "expected_evidence": "compiled interface structure + implication to the uniform doubling hypothesis + non-vacuous witness + statement-only realization Prop + hashes/exits/axiom audit/semantic review; semantic class model-conditional (metric-measure), explicitly not a manifold theorem",
  "host_pool": [
    "ophis-gpu"
  ],
  "requires_lean": true,
  "max_hours": 8,
  "max_rounds": 8,
  "worktree": "worktrees/L4-child-bishop-gromov-interface"
}
