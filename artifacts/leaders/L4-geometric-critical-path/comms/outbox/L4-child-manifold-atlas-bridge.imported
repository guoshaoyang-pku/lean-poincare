{
  "id": "L4-child-manifold-atlas-bridge",
  "group_id": "L4-geometric-critical-path",
  "parent_node": "U7",
  "deps": [],
  "lane": "builder",
  "acceptance": "Reduce the U7 blocker 'mathlib manifold -> D13 SmoothOverlapAtlas bridge' to a precise, independently verifiable interface, and deliver as much of it as compiles. Context: the D13 layer (release/Poincare/D13/ManifoldIBP/, byte-identical in this worktree's release tree) proves the weighted IBP on an abstract SmoothOverlapAtlas, but the release structure is NOT built from mathlib's ChartedSpace/IsManifold. Required content: (1) read the D13 structures (SmoothOverlapAtlas in Poincare/D13/ManifoldIBP/SmoothAtlas.lean, OverlapAtlas, ManifoldAtlasData in Transfer/GlobalMeasure) and mathlib's ChartedSpace/IsManifold/ModelWithCorners API at the pinned revision, and write a machine-checked bridge module that instantiates the D13 atlas structure from mathlib manifold data wherever the APIs actually meet (e.g. chart/index extraction, source/openness, transition C^2, global coordinate relation), leaving any genuinely missing piece as a named structure field or an explicit statement-only Prop with a documented reason; (2) if the full bridge is infeasible, the deliverable is still mandatory: a compiling 'blocker module' that states the exact missing mathlib API (name-level, with a #check probe file showing each missing/available ingredient), plus at least ONE genuine instantiation that does go through (a concrete 1-dimensional example, e.g. the real line or the dilation atlas from SmoothAtlasModel, constructed from mathlib ChartedSpace rather than from raw functions); (3) show one downstream consumer outside the D13 tree (e.g. a theorem in the new module consuming D13's manifoldWeightedIBP_of_atlasData or globalWeightedIBP_of_cover_partial_ae instantiated on the bridge data) so this is not a statement-only file. Acceptance evidence: compiling modules with full signatures, source hashes, compile exits, fail-closed axiom audit with cones subset {propext, Classical.choice, Quot.sound}, forbidden-token scan clean, an expanded-hypotheses list classifying every structure field as constructed/proved/assumed, and an adversarial semantic review confirming no manifold measure, Stokes or POU existence is claimed unless constructed. Do NOT edit the D13 worktree; copy needed upstream files byte-identically and record their hashes.",
  "expected_evidence": "compiling bridge or blocker module + #check API probe + one genuine instantiation + one downstream consumer + hashes/exits/axiom audit + expanded-hypotheses classification + semantic review; semantic class conditional/model, explicitly not a closed-manifold theorem",
  "host_pool": [
    "ophis-gpu"
  ],
  "requires_lean": true,
  "max_hours": 10,
  "max_rounds": 8,
  "worktree": "worktrees/L4-child-manifold-atlas-bridge"
}
