D10-bochner-euclidean — kernel verification summary (refreshed 2026-09-10)

A. Release-level checks (cwd `release/`)

lake env lean Poincare/D10/BochnerEuclidean/Basic.lean -> exit 0
lake env lean Poincare/D10/BochnerEuclidean/Bochner.lean -> exit 0
lake env lean Poincare/D10/BochnerEuclidean/Corollary.lean -> exit 0
lake env lean Poincare/D10/BochnerEuclidean/Compat.lean -> exit 0
lake env lean Poincare/D10/BochnerEuclidean/Axioms.lean -> exit 0
lake build Poincare.D10.BochnerEuclidean.Axioms -> exit 0
axiom audit -> exit 0   (40/40 declarations, cone [propext, Classical.choice, Quot.sound];
                         0 occurrences of sorryAx / native_decide / project axioms)
scan_forbidden -> exit 0  (hard_match_count = 0, soft_match_count = 0)

B. Worktree-wide compile gate check (cwd = worktree root, exactly the check run by
   `longrun/bin/dispatch360.py::compile_gate`)

lake env lean <abs path> for every `.lean` file in the worktree, excluding
`.lake`, `.git`, `.dshpkg` -> 69/69 exit 0, 0 failures.
Machine-readable per-file exit codes: longrun/logs/gate_replica.json
Full transcript: longrun/logs/gate_replica.log

C. Build infrastructure required by (B)

The gate invokes `lake env lean <absolute path>` with cwd = worktree root, so the
worktree root must be a Lake workspace: `lean-toolchain`, `lakefile.toml`,
`lake-manifest.json` and `.lake -> release/.lake` were added there.  In addition,
`release/.lake/build` had to contain the oleans of every module the gate compiles
(the previous run had built only the D10 target), so `lake build` was run once in
`release/` (exit 0, log longrun/logs/D10-full-build.log).  No mathematical source
outside `release/Poincare/D10/BochnerEuclidean/` was touched.
