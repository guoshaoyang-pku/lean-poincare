/-
D5-clean-rebuild — release check root.

This file is generated by the D5 clean-room verifier.  It imports **every** promoted
D1–D4 theorem cluster and audit driver of the Ricci-flow formalization program:

* D1  Probe/GeometryApi, Probe/PdeApi, Ledger/PerelmanDefinitions, Ledger/DefinitionSmoke
* D2  Poincare.Longrun.Geometry, Poincare.Longrun.PDE.*, Poincare.Longrun.CurvatureODE
* D3  Poincare.Longrun.Entropy, Poincare.Longrun.Topology.*, Poincare.Longrun.Surgery
* D4  Poincare.Longrun.Evolution, Audit/{GeometryAudit,CurvatureODEAudit,EvolutionAudit,
      CounterexampleAudit,PromotedEvolutionAudit}

It contains no mathematical content of its own.  The kernel-checked dependency audit
(forbidden `sorryAx`, project axioms, `unsafe`, `native_decide`, `proof_wanted`) is in
`ReleaseAudit.lean`, which imports this file.
-/

-- D1: mathlib API probes and the Perelman interface ledger
import Probe.GeometryApi
import Probe.PdeApi
import Ledger.PerelmanDefinitions
import Ledger.DefinitionSmoke

-- D2: geometry foundation, finite-grid PDE, finite-dimensional Ricci ODE
import Poincare.Longrun.Geometry
import Poincare.Longrun.PDE.HeatGrid
import Poincare.Longrun.PDE.DiscreteMaximumPrinciple
import Poincare.Longrun.PDE.Energy
import Poincare.Longrun.PDE.ContinuousInterface
import Poincare.Longrun.PDE.AxiomAudit
import Poincare.Longrun.CurvatureODE

-- D3: entropy interface, kappa-noncollapsing ledger, surgery ledger
import Poincare.Longrun.Entropy
import Poincare.Longrun.Entropy.AxiomAudit
import Poincare.Longrun.Topology.Basic
import Poincare.Longrun.Topology.CompactThreeManifold
import Poincare.Longrun.Topology.Noncollapsing
import Poincare.Longrun.Topology.NormalizedVolume
import Poincare.Longrun.Topology.Stage6Bridge
import Poincare.Longrun.Topology.MissingTheorems
import Poincare.Longrun.Topology.AxiomAudit
import Poincare.Longrun.Surgery
import Poincare.Longrun.Surgery.Axioms

-- D4: geometric evolution/monotonicity cluster and its independent audits
import Poincare.Longrun.Evolution
import Audit.GeometryAudit
import Audit.CurvatureODEAudit
import Audit.EvolutionAudit
import Audit.CounterexampleAudit
import Audit.PromotedEvolutionAudit

namespace D5ReleaseCheck

/-- Marker: a root module that only imports the promoted clusters, so that a successful
kernel check of this file means every promoted cluster compiled in the clean package. -/
theorem release_check_compiles : True := trivial

end D5ReleaseCheck
