# Swarm infrastructure

Adaptive, dependency-aware execution with isolated worktrees, checkpoint/result cards, compile gates and semantic audits. The external reference repository is locally tracked at `infra/swarm-materials`; no code is imported until its upstream snapshot is available.
