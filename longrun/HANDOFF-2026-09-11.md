# Fleet Handoff Note — 2026-09-11 (evening)

**For the incoming operator/model.** This note captures the fleet state after the
2026-09-11 afternoon repair session. Read `HANDOVER.md` (workspace root) first for
the full architecture and runbook; this file is the delta.

## Current fleet state (verified at ~17:15 CST)

- **Central queue (ophis `queue.json`)**: 77 verified / 3 running / 3 queued /
  3 remote_owned / 1 needs_review (D9, gate re-triggered, expected to clear).
- **ophis-gpu**: dispatcher alive (pid 2542302, restarted 16:05 after a quota
  crash). Three D13 workers running: `D13-manifold-ibp-volume-form`,
  `D13-heatkernel-bridge-d10-d7`, `D13-cross-audit-360-cards`.
- **360-1**: dispatcher + relay alive. `D12-tensor-maximum-bochner` running.
  `D13-cross-audit-ophis-cards` queued locally (host=360-1).
- **360-2**: dispatcher + relay alive. `D11-triangulation-3d` running;
  `D11-reduced-volume-euclidean` relaunched 16:53 (was budget-paused during the
  tunnel outage; its anomalous local `verified` label was corrected — it had no
  DONE/gate/card). `D12-triangulation-topology` completed locally, relayed, and
  centrally PROMOTED (gate 81 files) at 17:04.

## What broke today and what was fixed (all fixes are live)

1. **Tunnel outage (~13:06–14:27)**: 360 API/ssh forwards died; three tasks
   paused (8-failure rule). Tunnels are supervised by launchd
   (`com.guoshaoyang.poincare-tunnels`, script `~/.local/bin/poincare-tunnel-service.py`)
   and auto-restart; forwards verified healthy after.
2. **ophis disk quota FULL on /data3** (600G hard limit hit ~15:07): gates failed
   writing oleans, dispatcher crashed, workers died. Cause: relay-pushed
   worktrees lacked `release/.lake` (rsync excludes it), so their gates cloned
   mathlib locally (~16G each). Freed ~43G by replacing the clones with hardlink
   seeds from `D6_weekly_release/release/.lake`. **Still over soft quota
   (555G/500G, grace running) — the fleet works, but a structural GC of old
   worktrees is the top maintenance task.** `ngram-gap-exp` (337G) and
   `mlsbench` (90G) on /data3 are other projects of the user — do NOT delete
   without asking.
3. **relay_push.sh rewritten (v3)** on both 360 machines:
   - persistent ssh ControlMaster (`~/.ssh/relay-mux-10022`) — the fcloud edge
     randomly drops fresh dials (~30%); the mux needs one successful dial and
     all pushes ride it. This took push success from ~25% to immediate.
   - prepare step seeds `release/.lake` from the D6 baseline (prevents mathlib
     re-clones → prevents the quota incident from recurring).
   - `--delete-after` + `--exclude='third_party/frenzymath/'` (the 360 worktrees
     keep a symlink there; the ophis baseline has the real pinned content).
   - deploy/update procedure: edit on Mac, `scp` to 360-2, hop `360-2 → 360-1`
     (Mac↔360-1 direct ssh flaps; 360-2 is directly reachable as `ssh 360-2`).
4. **D12-semantic-ledger / D9-parabolic-maximum-principle mirror repairs**:
   central cards and `latest.log` evidence were missing on ophis (pushed before
   the evidence-sync fix). Cards/evidence were copied manually; both re-gated.
   Note: ophis `dispatch_loop.py` `card_status()` currently has NO `latest.log`
   fallback (the 360 dispatchers do) — a `TASK_DONE` line was appended to the
   D9 central card to satisfy it. Consider re-adding the fallback.
5. **Race caution**: relay pushes vs. manual state edits can interleave (a
   REMOTE_PAUSED landed once after a mirror reset). If you reset a mirror,
   also clear the corresponding state marker on ophis.

## Known-loose ends (safe for the successor to pick up)

- `D13-integrated-kernel-audit` stays queued until all 14 D12 mirrors turn
  terminal; 3 D12/D11 tasks are still running on the 360s. It will fire
  automatically once they promote.
- `D13-manifold-ibp-volume-form` reported I4 discharged; genuine remaining
  blocker **U7-global** (overlapping-atlas Riemannian measure + Green identity)
  with a concrete request: port `MorganTianLib/Ch01/RiemannianMeasure.lean` and
  `Ch02/GreenIdentity.lean` from the frenzymath snapshot (Lean v4.32.1) to the
  release pin (v4.34.0-rc2). This is the best next task to dispatch.
- 360 dispatchers' verified counts (13 + 11+) exceed the central mirror; the
  central gate is the source of truth for publication (77 verified).
- Evidence discipline reminder: all current PROMOTEs are `compiled_only` with
  semantic review pending. No unconditional Perelman result exists; do not let
  any report claim otherwise.

## Standard ops quick reference

- Fleet snapshot: `ssh ophis-gpu 'tail -15 ~/workdir/lean_poincare/longrun/logs/dispatch.log'`
  (same path pattern on 360-1/360-2 via `ssh 360-2`, and `ssh 360-2 'ssh 10.234.161.2 ...'` for 360-1).
- Resume a paused task: move `state/<task>/PAUSED` aside, set queue status to
  `queued` in the OWNING machine's local queue.json; for mirrors on ophis use
  `remote_owned`/`running` as appropriate.
- Dispatcher restart (ophis): `cd ~/workdir/lean_poincare/longrun && setsid
  nohup python3 bin/dispatch_loop.py >> logs/dispatch_stdout.log 2>&1 < /dev/null &`
- Quota watch: `ssh ophis-gpu 'quota -s guoshaoyang | grep nvme3'`.
