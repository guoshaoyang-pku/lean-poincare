# Independent semantic review — D12-connection-curvature

The synchronized card for D12-connection-curvature requests acceptance and claims an unconditional `leviCivitaExists` construction plus chart and model results. The source tree contains 74 Lean files and the card records a D12 audit claim, but the machine-readable card currently leaves `downstream_checked_use`, `axiom_audit`, `forbidden_scan`, and `verdict` null. Its checkpoint also says `status: in_progress` despite the TASK_DONE marker.

Review classification: **pending / conditional**. The abstract Milnor construction is a plausible general algebraic result, and the card records named downstream uses, but the current synchronized evidence is insufficient for promotion or exact blocker closure because the independent gate, axiom evidence and downstream checked-use fields are not machine-verifiable from the card JSON. The chart and conformal components explicitly retain coefficient-data and pinned-mathlib limitations; they are model or conditional interfaces, not a general manifold Levi-Civita theorem.

A local text scan confirms that forbidden-token strings occur in negative-control files and documentation, so a source-wide grep is not an acceptance gate. The required fail-closed release audit must instead report its scanned roots and declaration cones, exclude negative controls from authored release sources, and be independently rebuilt.

Exact mathematical blocker closure accepted by this review: `[]`. The card's claimed closure is retained as an upstream worker claim pending an independent compile gate, axiom audit, source hash and downstream semantic review. No Poincare theorem claim is made.

`TASK_DONE` (independent review request recorded; promotion withheld).
