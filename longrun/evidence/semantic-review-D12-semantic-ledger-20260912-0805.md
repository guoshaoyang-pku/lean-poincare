# Semantic review — D12 semantic ledger

Date: 2026-09-12 08:05 +0800

This review examines the preserved D12-semantic-ledger result card, machine-readable declaration inventory, checkpoint references, and stated compile/audit evidence. It is an evidence-level review of the delivered ledger; a fresh source-worktree replay remains desirable when the relay is stable.

## Verdict

The ledger is a valid adversarial audit artifact with three distinct outcomes. First, it proves a concrete defect in the D7 heat-kernel initial-condition interface on noncompact Euclidean space. Second, it classifies the D7 recognition chain as a conditional assembly whose load-bearing geometric inputs remain unproved. Third, it provides a machine-readable classification of existing D1–D11 claims. It does not prove or disprove the Poincare conjecture and claims no exact blocker closure.

## Defect validation

The counterexample uses the continuous unbounded test function `exp (y^4)` against the standard Gaussian on `ℝ`. The card records a kernel-checked proof that the integrand is not Lebesgue-integrable for every positive time, so the Bochner integral convention cannot converge to the value at the base point. This directly targets the real D7 `HeatKernelData.initialCondition` field, whose only test-function premise is continuity. The result is a proved interface defect, not a repair and not a proof that no corrected heat-kernel theory can be formalized.

## Recognition-chain boundary

The ledger correctly expands `stage6Target_of_certificates` into explicit antecedents: extinction, canonical neighborhoods, piece recognition, van Kampen for connected sums, and connected-sum sphere recognition. The implication assembly is kernel-checked, while those antecedents and the final homeomorphism construction remain unproved or statement-only. The presence of simply connectedness as an input is recorded as the Poincare hypothesis itself, not as a derived conclusion.

## Evidence and classification

The card reports a pinned Lean `v4.34.0-rc2` / mathlib `7974e751` build, 68/68 gate checks, separate D12 and real-module axiom audits restricted to `propext`, `Classical.choice`, and `Quot.sound`, and a negative control that detects forbidden axioms. The ledger classification vocabulary is preserved: genuine-general is scoped to the stated abstract setting, conditional rows retain their unproved antecedents, model rows are not upgraded to manifold results, and statement-only rows have no inhabitants.

The blocker recount (23 open semantic blockers plus documented/environment/informational entries) is an evidence inventory, not a completion denominator. Exact named mathematical blocker closures from this review: `[]`.

## Remaining acceptance

1. Independently replay the source and gate from the owned worktree when transport allows.
2. Consume the corrected heat-domain interface in a downstream Euclidean-to-manifold bridge only after its admissibility hypotheses are constructed.
3. Keep the recognition, compactness, surgery, and volume inputs conditional until their load-bearing constructors and downstream uses exist.
