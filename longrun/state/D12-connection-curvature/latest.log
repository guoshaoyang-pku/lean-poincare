dsh: reasoning:
Let me start by exploring the worktree to understand the current state, read the checkpoint, LONG_PLAN, docs, and understand the D7/D9/D10/D11 sources.
dsh: reasoning:
The task has been in progress before. Let me read the checkpoint fully, the results files, LONG_PLAN.json, and the docs. Let me understand the current state.
dsh: reasoning:
The task is well underway. Invocation 1 completed at 4 hours elapsed. This is invocation 2 (up to 4 hours). The main remaining work:

1. `ChartLeviCivitaSmooth`: Christoffel coefficients as `Smooth` functions of the chart point given smooth coefficient functions — the x-dependent smoothness module. This is the key remaining item to satisfy "Prove one substantive theorem constructing the smooth Levi-Civita connection or curvature from metric coefficients on a real chart".

2. Possibly a field-level connection (∇ on arbitrary smooth fields via fderiv).

3. Final re-audit + terminal result card/JSON with TASK_DONE or TASK_BLOCKED verdict.

Let me look at the existing source files to understand the current structure, and the upstream docs. Let me first check the state of the repo, build, and read the existing D12 files.
dsh: reasoning:
Interesting — `ChartLeviCivitaSmooth.lean` already exists. The docs directory is not at worktree root. Let me check whether the smooth module is complete, and look at its contents, and check the upstream integration docs location.
dsh: reasoning:
The `ChartLeviCivitaSmooth.lean` module already exists with 612 lines. So invocation 1 went much further than the checkpoint's "next_steps" suggested. The docstring mentions the curvature component formula as a remaining dependency. Let me:

1. Check the whole ChartLeviCivitaSmooth module compiles and see what's left.
2. Run the build to see current state.
3. Read the remaining part of the module.

The next big item: the curvature component formula from the Christoffel family — i.e., curvature of the smooth chart Levi-Civita connection, proving `R(X,Y)Z = ∇_X∇_Y Z − ∇_Y∇_X Z − ∇_{[X,Y]} Z` satisfies some identity, or deriving the coordinate curvature formula and checking it's nonzero in a non-flat model, plus a downstream Ricci/scalar identity on a chart.

Actually the task says: "Prove one substantive theorem constructing the smooth Levi-Civita connection or curvature from metric coefficients on a real chart, with a correctly proved coordinate transformation/naturality or contraction statement." and "Derive one downstream Ricci/scalar identity with theorem provenance."

Much is done. What remains most valuable:
1. Curvature of the chart Levi-Civita connection: compute the curvature operator from the Christoffel family, prove the component formula, and prove a nonzero-curvature example (e.g., a chart metric with nonvanishing curvature, e.g., round sphere in stereographic or something 2D like g = 1/(1+x²+y²)² · δ on ℝ² which is the sphere stereographic metric, has constant curvature 4). That would be a strong downstream result: nonzero curvature from metric coefficients on a real chart.

2. Downstream Ricci/scalar identity on a chart from the smooth chart curvature — but this requires the Ricci contraction at the manifold level on a chart. We already have general Ricci contraction in any orthonormal frame (RicciSymmetry). The chart model has a global chart; we could choose a coordinate-orthonormal frame at a point and use it.

Let me first check the build state and the audit state, then read the rest of the smooth module to see exactly what's proved, then plan the curvature-from-coefficients work.

Plan for this invocation (4 hours):
- Verify current build (lake build Poincare.D12) — establish baseline.
- Read remaining ChartLeviCivitaSmooth source.
- Decide: add curvature of the chart Levi-Civita connection: `chartCurvatureFamily` with the component formula, proving torsion-free metric-compatible curvature properties, and a 2D non-flat model (e.g., the sphere metric on ℝ² via stereographic projection: g(x,y) = 4/(1+x²+y²)² δ; Christoffel symbols computable; R(e1,e2)e1 = 4e2... need second derivatives of g though — Christoffel derivatives involve second derivatives of the metric). Hmm, second derivatives appear in curvature. In the module, dFamily is derived as fderiv, so to get curvature components ∂ₖΓ we need fderiv of the Christoffel family, which needs fderiv of gInv and fderiv of d, i.e., second derivatives of g. That's heavier but doable with the product rule infrastructure. The docstring says "The curvature component formula needs derivatives of the Christoffel family and is recorded as the next dependency".

Given the 4-hour window, an alternative substantive addition: a coordinate transformation/naturality statement — the task explicitly allows "a correctly proved coordinate transformation/naturality or contraction statement". We already have contraction (RicciSymmetry). A naturality statement for the chart construction could be: under a linear change of coordinates, the Christoffel family transforms by the classical law (tensor + inhomogeneous term). That's classical and provable in Lean given the algebraic structure, without second derivatives. Hmm, but it involves the chain rule for the derivative datum (d transforms linearly under a linear change of coordinates — nice: if y = L x then ∂/∂y = L^{-1}ᵀ ∂/∂x; for linear maps the Christoffel transformation law is exactly the classical one and needs no fderiv gymnastics beyond linearity).

Actually wait — what did invocation 1 leave? Let me look at the remaining blockers and next_steps:
- "Smooth x-dependent chart module" — appears DONE (ChartLeviCivitaSmooth exists and is imported).
- "Field-level connection" — appears DONE (nabla in the module).
- Remaining: manifold-level CovariantDerivative API (upstream), smooth inverse Gram (upstream), curvature component formula for the chart connection (own work possible).

So the substantive new work for this invocation: prove curvature of the smooth chart Levi-Civita connection from metric coefficients — the component formula Rⁱⱼₖₗ from derivatives of Christoffel symbols — plus a downstream non-flat model. That's ambitious within 4 hours but the infrastructure (fderiv product rules, sums) is in place.

Alternatively: prove the connection's curvature satisfies the algebraic Bianchi identity on the chart (needs second derivatives + Schwarz for the derivative parts — that's where Schwarz would be needed, but there's a classical cancellation). Hmm.

Let me first read the rest of ChartLeviCivitaSmooth to see what lemmas exist that I can reuse.
dsh: reasoning:
The build fails. `ChartLeviCivitaSmooth` does not compile. Note that "EXIT: 0" is because of the tail pipe; the actual lake build failed. Let me get the full error log.
dsh: reasoning:
There are several compilation errors in `ChartLeviCivitaSmooth.lean`. This module was apparently in a broken intermediate state when invocation 1 ended. This is a key finding: the smooth module needs fixing. That's the primary work of this invocation.

Errors:
1. Line 150: `rewrite` failed for double-sum distribution (sum_three_cycle context). Need `sum_add_distrib`-style rewriting for nested sums.
2. Line 292: `ContDiff.const_smul (1/2)` gives `(1/2) • ...` instead of `2⁻¹ * ...` — need a cast/simp to align; add `simpa` or change statement.
3. Lines 345, 362: `lieBracket` field notation — need to change to explicit `lieBracket c X Y` or make the parameter explicit.
4. Line 382: unsolved goal — constant function derivative `(fderiv ℝ (fun z => Y0 k) x) X0` should be 0 via fderiv_const.
5. Lines 396, 415: simp made no progress — need to fix those proofs.
6. Line 487: `DifferentiableAt.mul` associativity mismatch — need to change to `fun z => c.g z i j * (Y z i * Z z j)` or use mul three times differently.
7. Line 502: introN failed.

Let me read the whole file to understand and fix these errors.
dsh: reasoning:
Now I understand the state. The file has 7 compile errors to fix:

1. **Line 148-150** (`sum_add_split`): `rw [Finset.sum_add_distrib, Finset.sum_add_distrib]` failed because the pattern `∑ x ∈ s, ...` — the goal is `∑ i, ∑ j, (f i j + g i j) = ∑ i, ∑ j, f i j + ∑ i, ∑ j, g i j`. The inner sum: `∑ j, (f i j + g i j) = ∑ j, f i j + ∑ j, g i j` needs `Finset.sum_add_distrib`. The outer: `∑ i, (inner f i + inner g i) = ∑ i, inner f i + ∑ i, inner g i` also `Finset.sum_add_distrib`. The issue: `rw [Finset.sum_add_distrib]` tries to match the outer sum first, and the summand must be syntactically a sum `a + b`. The outer summand is `∑ j, (f i j + g i j)` which is not literally `A i + B i`. Simplest fix: use `simp [Finset.sum_add_distrib]` — simp can use it under binders. Or: `simpa using Finset.sum_add_distrib` twice with explicit functions. Better:

```lean
lemma sum_add_split (f g : ι → ι → ℝ) :
    (∑ i : ι, ∑ j : ι, (f i j + g i j)) = (∑ i : ι, ∑ j : ι, f i j) + ∑ i : ι, ∑ j : ι, g i j := by
  simp only [Finset.sum_add_distrib]
```

That should work since `simp` will rewrite both inner and outer.

2. **Line 292**: `ContDiff.const_smul (1 / 2 : ℝ)` produces `(1/2) • (...)`, expected `2⁻¹ * (...)`. The target is `ContDiff ℝ ∞ fun x => 2⁻¹ * (...)`. Options: change the const_smul to `ContDiff.const_mul` — is there a lemma `ContDiff.const_mul`? There's `ContDiff.const_mul` for multiplication by a constant? In mathlib: `ContDiff.const_mul` exists? Actually there is `ContDiff.const_mul (c : 𝕜)` for functions into a normed algebra. Let me check: `ContDiff.const_mul` — I recall `contDiff_const_mul`/`ContDiff.const_mul` exists for `𝕜 → 𝕜`-ish functions. If `ContDiff ℝ ∞ (f)` then `ContDiff.const_mul c` : `ContDiff ℝ ∞ (fun x => c * f x)`. Hmm, I need to verify exact names. Alternative simpler: after `rw [hΓ]`, use `simpa [one_div] using ...` hmm — the mismatch is `(1 / 2) • x` vs `2⁻¹ * x`. Note `(1 / 2 : ℝ) • x = (1/2) * x` definitionally for ℝ scalar? `smul_eq_mul` says `a • x = a * x` for ℝ. And `1/2 = 2⁻¹` definitionally? `1 / 2` unfolds to `1 * 2⁻¹ = 2⁻¹`... In Lean, `a / b = a * b⁻¹`, so `1/2 = 1 * 2⁻¹ = 2⁻¹`. simp should normalize. The issue is the type of the `ContDiff` term itself: `ContDiff.const_smul (1 / 2)` has type `ContDiff ℝ ∞ fun y => (1 / 2 : ℝ) • (...)` while the target after rw is `ContDiff ℝ ∞ fun x => 2⁻¹ * (...)`. These two propositions differ by definitional equality of the functions? `(1/2 : ℝ) • t` reduces to `(1/2) * t` = `(2⁻¹) * t`? Actually `a • b` for ℝ-action on ℝ is `a * b` by definition (since ℝ is a field acting on itself; `smul_eq_mul` is a theorem, but the instance `Algebra ℝ ℝ`... The module structure on ℝ: `instance : Module ℝ ℝ` — the smul is defined as multiplication? In mathlib, for `𝕜 = ℝ` acting on `ℝ` itself, `smul` is definitionally multiplication because the module structure is `isScalarTower` based... Hmm, I recall that for ℝ acting on ℝ, `a • b` is defeq to `a * b` (the instance is `Semiring.toModule` where smul := mul). Yes! `Semiring.toModule` defines `smul := HMul.hMul`. So `(1/2 : ℝ) • t` is defeq `(1/2) * t` and `1/2` defeq `2⁻¹`? `1 / 2` = `Div.div 1 2` = `1 * 2⁻¹` = `2⁻¹` by `one_mul`... but that's a theorem, not definitional. `1 * 2⁻¹` is not defeq to `2⁻¹` unless `one_mul` is definitional — multiplication is defined by recursion on naturals so `1 * a = a` is NOT definitional in general. Hmm. But for `ℝ`, `1` is `OfNat.ofNat 1` which for ℝ is... `Nat.cast 1 = (1 : ℝ)`; multiplication `1 * 2⁻¹` unfolds to addition-based multiplication `(1)*x = x`? For semirings, `HMul.hMul` is `mul`, and `Nat`-based `1 * x` reduces via `one_mul` being an instance reduction? Actually in Lean, `1 * a` for a monoid with `mul` defined by repeated addition: the elaborator's `OfNat` for `1` in ℝ is `Nat.cast`, and `Nat.cast_mul`... I think `(1 : ℝ) * x` does NOT reduce definitionally to `x` in general... wait, actually it does for `Monoid` where `nsmul`-based definitions: `1 * x` unfolds to `x` via `one_mul` which is `rfl`? Let me think: In `algebra`, `mul_one` and `one_mul` for `Nat` multiplication are `rfl` by the recursive definition (n+1 cases). For ℝ, multiplication is defined via Cauchy or via the field structure from rationals. `mul : ℝ → ℝ → ℝ` is not recursive on the left argument, so `1 * x` does NOT reduce by computation.

Simplest robust fix: replace `ContDiff.const_smul` with `ContDiff.const_mul` if it exists, or post-process with `simpa [one_div]`. Let me check what `ContDiff.const_mul` looks like in this mathlib revision. I'll grep mathlib source. Actually easier: change the target expression `hΓ` to use `•` or adjust the `simpa` at the end. Current structure:

```lean
  have hΓ : (fun x => c.christoffelFamily x k i j) = fun x =>
      ∑ l, c.gInv x k l * ((1 / 2 : ℝ) *
        (c.dFamily x i j l + c.dFamily x j i l - c.dFamily x l i j)) := by ...
  rw [hΓ]
  simpa using (ContDiff.sum ... (ContDiff.mul ... (ContDiff.const_smul (1 / 2 : ℝ) (...))))
```

Fix: `exact (ContDiff.sum ...).congr ...`? Or simply prove with `simpa [smul_eq_mul]`? The problem: after `rw [hΓ]` the goal is `ContDiff ℝ ∞ fun x => 2⁻¹ * (...)`. The term `ContDiff.const_smul (1/2) ...` has type `ContDiff ℝ ∞ (fun y => (1/2 : ℝ) • (...))`. `(1/2) • t` is defeq to `(1/2) * t` (Semiring.toModule) — and `(1/2)` is `1 / 2` = `Div.div (OfNat 1) (OfNat 2)`. Hmm, is `(1 / 2 : ℝ)` the same as `2⁻¹`? `a / b` elaborates to `Div.div a b` = `a * b⁻¹` — this is definitional (`instance Div` = `fun a b => a * b⁻¹`). So `1 / 2` = `(1 : ℝ) * (2 : ℝ)⁻¹`. And the target `2⁻¹` = `(2 : ℝ)⁻¹`. `1 * x` — since ℝ has `Nontrivial` ring structure where `one_mul` is a theorem... is it? For `CommRing` instances built on ℝ, `one_mul` is proved as a theorem, not rfl. So `1 * 2⁻¹ ≠ 2⁻¹` definitionally. So the types don't match. 

Simplest: add `simpa [one_div]` — `simpa [one_div] using (ContDiff.sum ...)` where the whole term is the const_smul version. But careful: `one_div` says `1 / a = a⁻¹`. So `simpa [one_div]` would turn `(1/2) • t`... hmm, `simp [one_div]` rewrites `1 / 2` to `2⁻¹`, and `•` to `*` if `smul_eq_mul` is in the simp set — `smul_eq_mul` is a simp lemma? Yes, `smul_eq_mul` is `@[simp]` I believe for actions where it holds... Actually `smul_eq_mul` is `[simp]`-tagged in mathlib (`Algebra.smul_def` / `smul_eq_mul`). Let me just try: 

```lean
  rw [hΓ]
  simpa [one_div] using (ContDiff.sum (s := (Finset.univ : Finset ι)) (by
    intro l hl
    refine ContDiff.mul ?_ ?_
    · exact c.gInv_component_smooth k l
    · exact ContDiff.const_smul (1 / 2 : ℝ)
        (ContDiff.add (c.dFamily_smooth i j l)
          (ContDiff.sub (c.dFamily_smooth j i l) (c.dFamily_smooth l i j)))))
```

If `simp` can't close the `•` → `*` gap, add `smul_eq_mul` explicitly. I'll test.

3. **Lines 345, 362**: `c.lieBracket X Y` — field notation fails because `lieBracket` has `(c : SmoothChartData ι)` as the only explicit param? Actually `lieBracket` is defined with `variable (c : SmoothChartData ι)` in scope: `def lieBracket (X Y : VectorField ι) : VectorField ι := ...` — so `c` is an implicit auto-bound? No wait — `variable (c : SmoothChartData ι)` makes `c` an auto-inserted argument: `lieBracket.{w} (c : SmoothChartData ι) (X Y : VectorField ι)`. The error "Function `lieBracket` does not have a usable parameter of type `SmoothChartData ...` for which to substitute `c`" — because with `variable (c)`, the binder order is `(c : SmoothChartData ι) (X : VectorField ι) (Y : VectorField ι)`? Hmm, field notation `c.lieBracket X Y` requires the first explicit parameter to have type `SmoothChartData ι`. Wait the error says it doesn't have a *usable* parameter. "Such a parameter must be explicit, or implicit with a unique name" — the parameter `c` exists but maybe it's named differently (like `c✝` or it's a section variable that's included implicitly with a name like `c` but marked "unusable" because the type is `SmoothChartData ι` where ι is... hmm.

Actually the issue might be that within `namespace SmoothChartData`, `variable (c : SmoothChartData ι)` declares `c` but `lieBracket` is defined inside the namespace... field notation for `c.lieBracket X Y`: Lean tries to find a parameter of type `SmoothChartData ?ι` among explicit params. The declaration should be `lieBracket (c : SmoothChartData ι) (X Y : VectorField ι)`. Hmm, but the error says the parameter is not usable. Maybe because when `variable (c : SmoothChartData ι)` is declared at namespace level and the def has `[Fintype ι] [DecidableEq ι]` as implicit args inserted before `c`, field notation can still work... The error "does not have a usable parameter of type `SmoothChartData ...` for which to substitute `c`" suggests the first parameter is `{ι : Type w}` `[Fintype ι]` etc. and then... hmm no.

Wait — the reason: `def lieBracket (X Y : VectorField ι) : VectorField ι`. Since `variable (c : SmoothChartData ι)` is in effect, the actual signature is `lieBracket (c : SmoothChartData ι) (X Y : VectorField ι) : VectorField ι`. For field notation to work, `c` must be *explicit*. But when a section variable is auto-included via `variable`, it's included as an implicit argument `{c : SmoothChartData ι}`? No — `variable (c : ...)` includes it explicitly before other explicit args... Actually the rule: `variable (c : T)` adds `c` as an explicit argument to definitions that use `c`. Hmm, but then field notation should work.

Let me look at how other theorems in the file use it: line 344: `c.lieBracket X Y` in theorem statement fails at 345. But `c.nabla X Y` works (line 315-316, 319-320, etc.). So why does `nabla` work and `lieBracket` not? Both defined the same way... 

Oh! I see. `nabla` is defined at line 309: `def nabla (X Y : VectorField ι) : VectorField ι := fun x k => ... c.christoffelFamily ...`. And `lieBracket` at 339: `def lieBracket (X Y : VectorField ι) : VectorField ι := fun x k => fderiv ... Y z k ... X ... - fderiv ... X z k ... Y ...`. The body of `lieBracket` does NOT mention `c`! So Lean auto-bound only includes variables that are actually used... no wait, with `variable (c)`, unused-but-declared variables are still included? No! `variable (c : T)` includes `c` in declarations *that use c*? Hmm, actually the semantics of `variable (c : T)`: it makes `c` available as a binder to be inserted automatically if the declaration *mentions* `c`. `lieBracket`'s body never mentions `c`, so `c` is NOT included as a parameter. That's exactly why field notation fails: `lieBracket` has no parameter of type `SmoothChartData ι`.

Fix: explicitly write `def lieBracket (c : SmoothChartData ι) (X Y : VectorField ι) : VectorField ι := ...`. Then `c.lieBracket X Y` works, and `unfold lieBracket` still works.

Similarly `nabla_smul` line 391 uses `c.nabla` (fine) and `unfold nabla` fine. And `lieBracket_smooth` at 344-345: `c.lieBracket X Y` — fix by explicit param.

Note: `nabla` also doesn't mention... wait, `nabla` DOES mention `c` (christoffelFamily). OK.

4. **Line 380-383** (`nabla_const_apply`): unsolved goal: `(fderiv ℝ (fun z => Y0 k) x) X0 + sums = sums`. `simp only [nabla, nablaOf_apply, fderiv_const, christoffelFamily]` — after simp, remaining goal shows fderiv term not simplified. `fderiv_const` states `fderiv 𝕜 (fun _ => c) x = 0`. The `simp only` with `fderiv_const` didn't apply because the function is `fun z : ChartPoint ι => Y0 k` where `Y0 k` is an application — `fderiv_const` matches `fun _ => c` only if the body is a variable or constant. `fun z => Y0 k` — the simplifier needs to know the function is constant, i.e., rewrite `fun z => Y0 k` to `fun _ => Y0 k` first. `simp only [fderiv_const]` — simp does eta reduction but not eta-expansion of the body to `_`? simp can simplify `(fun z => Y0 k)` to `(fun _ => Y0 k)`? The simplifier has a congruence lemma for `fun` that introduces `z` and rewrites `Y0 k` (no z) to itself; then `fderiv_const` requires the lambda to be `fun _ => c`. Hmm, actually `fderiv_const` is `fderiv 𝕜 (fun _ : E => c) x = 0`? Let me check: mathlib's `fderiv_const`: `fderiv 𝕜 (fun y : E => c) x = 0`. The binder name `_` vs `z` shouldn't matter (alpha-equivalence). So why didn't it apply? The goal after partial simplification shows `(fderiv ℝ (fun z => Y0 k) x) X0 + ∑... = ∑...` — that's the *unsolved* state, meaning `fderiv_const` didn't fire. Possibly because simp's congruence doesn't reduce `fun z => Y0 k` to constant form since `Y0 k` is not a *constant function expression* syntactically... but alpha-wise `fun z => Y0 k` IS `fun _ => Y0 k` (z not free). simp should match. Hmm, unless the issue is that `simp only [fderiv_const]` needs the form `fderiv 𝕜 (fun _ => c) x = 0` where the function's type is exactly. Or maybe the problem is different: after `simp only [...]`, there's still a goal — the error at 382:49 "unsolved goals" with the goal shown. So one of the `simp only` lemmas was unused (fderiv_const, warning at 383:35) and the fderiv didn't reduce.

Maybe the issue: `(fderiv ℝ (fun z => Y0 k) x) X0` — the `0` from `fderiv_const` is `0 : ChartPoint ι →L[ℝ] ℝ`, and `0 X0 = 0` requires `zero_apply`/`map_zero`-style simp: `(0 : E →L[ℝ] F) X0 = 0` is true by `ContinuousLinearMap.zero_apply`. If `fderiv_const` fired, goal would be `0 + sums = sums` — closed by `simp`. So probably `fderiv_const` did NOT fire at all.

Why? Let me think about `fderiv_const`'s statement in this mathlib revision: `@[simp] theorem fderiv_const (c : F) : fderiv 𝕜 (fun y : E => c) x = 0`. With `simp only [fderiv_const]`, matching `fderiv ℝ (fun z : ChartPoint ι => Y0 k) x` against `fderiv ?𝕜 (fun y : ?E => ?c) ?x` — unify `Y0 k` with `?c` — fine, `z` with `y` — fine. Should fire. Unless... the inner function type: `Y0 k : ℝ`? `Y0 : ChartPoint ι = ι → ℝ`, `Y0 k : ℝ`. Good.

Hmm wait, maybe the problem is that the function is `fun z : ChartPoint ι => Y0 k` — but after `simp only [nabla, nablaOf_apply, christoffelFamily]`, the goal's fderiv might be over a *different* reduced form like `fderiv ℝ (fun z => Y0 k) x` where the binder has type annotation dropped — still matches.

Actually, wait — re-reading the error output: "error: Poincare/D12/ConnectionCurvature/ChartLeviCivitaSmooth.lean:382:49: unsolved goals ... ⊢ (fderiv ℝ (fun z => Y0 k) x) X0 + ∑ i, ∑ j, ... = ∑ i, ∑ j, ..." and "warning: ...383:35: This simp argument is unused: fderiv_const". So yes `fderiv_const` didn't fire. Why? Perhaps because in this mathlib revision `fderiv_const` has different statement: `fderiv_const : fderiv 𝕜 (fun _ => c) x = 0` — hmm it should still unify.

OH WAIT. I bet the issue: `simp only [fderiv_const]` — the lemma is `[simp] theorem fderiv_const` and it IS applied, but only rewrites `fderiv ... x` (without application to X0)?? No, the goal shows `(fderiv ℝ (fun z => Y0 k) x) X0` unreduced.

Alternatively, maybe the fderiv here is `fderiv ℝ (fun z : ChartPoint ι => Y0 k) x` where the *chart space* — wait, in `nabla_const_apply`, `nabla` unfolds to `fun x k => fderiv ℝ (fun z : ChartPoint ι => Y z k) x (X x) + ...` with `Y = fun _ => Y0`. So `Y z k = (fun _ => Y0) z k = Y0 k` by beta. Then `fderiv ℝ (fun z : ChartPoint ι => Y0 k) x (X x)`. Should match fderiv_const.

Hmm, unless `fderiv_const` in this revision is stated for `𝕜`-module... let me just check the mathlib source. Let me grep for `fderiv_const` in the mathlib lake packages.

Actually — maybe the problem is that `simp only` is *not* doing beta-reduction of `(fun _ : ChartPoint ι => Y0) z k`? No, that's definitional.

Let me not over-analyze; I'll experiment in Lean directly. The fix might be:

```lean
theorem nabla_const_apply (X0 Y0 : ChartPoint ι) (x : ChartPoint ι) (k : ι) :
    c.nabla (fun _ : ChartPoint ι => X0) (fun _ : ChartPoint ι => Y0) x k =
      nablaOf (c.chartCoefficients x) X0 Y0 k := by
  change (fderiv ℝ (fun z : ChartPoint ι => Y0 k) x) (X x) ... 
```

Wait, `X` is `fun _ => X0`, so `X x = X0`. The goal: `fderiv ℝ (fun z : ChartPoint ι => Y0 k) x X0 + ∑ i, ∑ j, christoffel... * X0 i * Y0 j = nablaOf ... X0 Y0 k` and `nablaOf_apply` gives RHS = `formConnection`... the RHS after simp is the christoffel sum. So need LHS fderiv term = 0. 

Fix:
```lean
  simp only [nabla, nablaOf_apply, christoffelFamily]
  simp [fderiv_const]
```
or prove the fderiv term zero separately:
```lean
  have h : fderiv ℝ (fun z : ChartPoint ι => Y0 k) x X0 = 0 := by simp [fderiv_const]
```
Hmm but if fderiv_const isn't matching... Let me check the actual lemma name in this mathlib. It might be `fderiv_const` with `(c : F)` requiring... I'll check via grep in `.lake/packages/mathlib/Mathlib/Analysis/Calculus/FDeriv/Basic.lean`.

5. **Lines 394-396**: `hfun` proof: `simp only [Pi.smul_apply, smul_eq_mul]` made no progress. Goal: `(fun z : ChartPoint ι => (f • Y) z k) = fun z => f z * Y z k`. `(f • Y) z k` — `Pi.smul_apply` rewrites `(f • Y) z` to `f z • Y z`, then `smul_eq_mul` rewrites `f z • Y z k`? Wait `(f z • Y z) k = (f z • Y z) k` then `Pi.smul_apply` again: `(f z • Y z) k = f z • Y z k`? Hmm, `Pi.smul_apply : (a • F) x = a • F x`? There are two: `Pi.smul_apply` might be `(s • F) x = s • F x`? In mathlib, `Pi.smul_apply` is tagged `[simp]`: `(s • F) x = s • F x`. So `(f • Y) z k = ((f • Y) z) k = (f z • Y z) k` — wait `(f • Y) z` with `f : ChartPoint → ℝ` scalar field and `Y : ChartPoint → ChartPoint` — smul of function spaces: `(f • Y) z = f z • Y z`? For `Pi` instances, `smul` is pointwise: `(f • Y) z = f z • Y z` where the smul on the codomain is the ℝ-module structure of `ChartPoint`. So `(f • Y) z k = (f z • Y z) k = f z * Y z k` — here `f z • Y z : ChartPoint = ι → ℝ`, so `(f z • Y z) k = f z * Y z k` via `Pi.smul_apply` + `smul_eq_mul`. So simp should close... but it says no progress. Maybe the lemma name is different: in newer mathlib it's `Pi.smul_apply` or `smul_apply'`... Let me check. Could be that in this revision the theorem is `Pi.smul_apply : (s • F) x = s • F x`? I'll grep. Or simply `funext z; rfl`? Is `(f • Y) z k = f z * Y z k` definitional? `(f • Y) z` = `f z • Y z` — definitional? The Pi smul instance: `instance instSMulPi : SMul α (∀ i, β i)` with `smul s F i := s • F i`... yes Pi.smul is defined as `fun s F i => s • F i`, so `(f • Y) z = f z • Y z`? Hmm: `(f • Y) z` — `f • Y` is `fun i => f i • Y i`, so `(f • Y) z = f z • Y z` definitionally. Then `(f z • Y z) k = f z • Y z k`? `Y z : ChartPoint`; `f z • Y z` = `fun k => f z • (Y z k)` definitionally, so `(f z • Y z) k = f z • Y z k` definitional. And `f z • Y z k` = `f z * Y z k` — is scalar action on ℝ by ℝ definitionally multiplication? `Module ℝ ℝ` via `Semiring.toModule` — in this mathlib, for `ℝ` the module instance is... `instance (R) : Module R R := Semiring.toModule`? There might be specific instances. `smul_eq_mul` is a theorem, so NOT definitional in general. Hmm wait — actually for the `Module ℝ ℝ` instance used for `ChartPoint` components (each `ℝ`), which instance is found? The scalar is `f z : ℝ` acting on `Y z k : ℝ`. The instance is `instModuleRealToSemiringToAddCommMonoid...`? In mathlib, `Algebra ℝ ℝ`, module structure = `Ring.toModule`? For `ℝ`, `Semiring.toModule` defines `smul := fun r x => r * x`? Let me recall `Semiring.toModule`: `{ smul := (· * ·), ... }`. If the found instance is `Semiring.toModule`, then `f z • Y z k` is definitionally `f z * Y z k` and `funext z; rfl` closes it... except `(f • Y) z k`: we need `((f • Y) z) k` — beta: `(f • Y) z` where `f • Y` is Pi.smul `fun z => f z • Y z`, so `(f • Y) z = f z • Y z` (rfl). Then `(f z • Y z) k` — `Y z : ι → ℝ`, `f z • Y z` is again Pi.smul: `fun k => f z • Y z k` — so `(f z • Y z) k = f z • Y z k` (rfl). Then `f z • Y z k : ℝ` with `f z : ℝ` — instance `Semiring.toModule ℝ ℝ`? But wait — does ℝ's module over itself come from `Semiring.toModule`? There's also `Algebra.instModuleRatCast`... For ℝ, I believe the instance is `instModuleℝ` = `Semiring.toModule`? Hmm, actually in mathlib, `Semiring.toModule : Module R R` with `smul := Mul.mul`. But there are priority issues with `Algebra ℝ ℝ` — `Algebra.smul` is also multiplication. I think `f z • Y z k` is defeq `f z * Y z k`. In that case `funext z; rfl` would close `hfun`. The original author wrote `simp only [Pi.smul_apply, smul_eq_mul]` which made no progress — meaning simp already considered it closed? No — "made no progress" means after simp the goal was unchanged and no lemma applied... but if it were rfl, `funext z; rfl` works. Actually if simp made no progress, then the goal `(fun z => (f • Y) z k) = fun z => f z * Y z k` — simp does its usual thing: it would need to rewrite `(f • Y) z k`. Hmm. If Pi.smul_apply isn't the right name, it fails. Let me just check names in the pinned mathlib and fix accordingly. Simpler: use `funext z; rfl` — if defeq holds. Or `funext z; change (f z • Y z) k = f z * Y z k; rw [Pi.smul_apply, smul_eq_mul]` — I'll experiment.

6. **Line 411**: `trace_state` at 411 — a leftover debug command! Remove it. And line 415 `simp only [Pi.smul_apply, smul_eq_mul]` "made no progress" at 415 — similar issue: `(f • Y) x j` → `f x * Y x j`. Fix similarly.

Also line 424 `trace_state` — remove. And at 396 error and 415 error, plus 502 introN failed.

7. **Line 487**: `DifferentiableAt.mul (hdiff_g i j) (DifferentiableAt.mul (hdiff_Y i) (hdiff_Z j))` gives `DifferentiableAt ℝ ((fun z => c.g z i j) * ((fun z => Y z i) * fun z => Z z j)) x` but expected `DifferentiableAt ℝ (fun z => c.g z i j * Y z i * Z z j) x`. So the summand function is `(fun z => c.g z i j * Y z i * Z z j)` — parsed as `(c.g z i j * Y z i) * Z z j`? `a * b * c` is `(a*b)*c`. So need `DifferentiableAt.mul (DifferentiableAt.mul (hdiff_g i j) (hdiff_Y i)) (hdiff_Z j)`. Wait but expected type is `DifferentiableAt ℝ (fun z => c.g z i j * Y z i * Z z j) x` — and `DifferentiableAt.mul (A) (B)` produces `DifferentiableAt ℝ (A * B)` — hmm, how does `DifferentiableAt.mul` work? `DifferentiableAt.mul : DifferentiableAt 𝕜 f x → DifferentiableAt 𝕜 g x → DifferentiableAt 𝕜 (f * g) x` where `f * g` is pointwise multiplication of functions. So the result function is `fun z => f z * g z`. For the summand `fun z => c.g z i j * Y z i * Z z j = (c.g z i j * Y z i) * Z z j`, we need `DifferentiableAt.mul (DifferentiableAt.mul (hdiff_g i j) (hdiff_Y i)) (hdiff_Z j)`. That's the fix for both 487 and 503.

But wait — at line 487 the goal function in context is `(fun z => c.g z i j * Y z i * Z z j)` — hmm, but `DifferentiableAt.mul`'s result type is `DifferentiableAt 𝕜 (fun y => f y * g y) x`. With `f = fun z => c.g z i j * Y z i` and `g = fun z => Z z j`, result is `fun z => (c.g z i j * Y z i) * Z z j` = `fun z => c.g z i j * Y z i * Z z j`. 

8. **Line 502**: `rw [fderiv_sum]` then `· intro j hj` — "Tactic `introN` failed: There are no additional binders". `fderiv_sum` generates goals: for each j, `DifferentiableAt ...` AND the equality `fderiv ... = ∑ ...`. The structure after `rw [fderiv_sum]`: `rw` with `fderiv_sum` — `fderiv_sum : fderiv 𝕜 (∑ i in s, F i) x = ∑ i in s, fderiv 𝕜 (F i) x` — wait the lemma is `fderiv_sum : fderiv 𝕜 (fun y => ∑ i ∈ s, F i y) x = ∑ i ∈ s, fderiv 𝕜 (fun y => F i y) x`? Actually the standard: `theorem fderiv_sum (h : ∀ i ∈ s, DifferentiableAt 𝕜 (F i) x) : fderiv 𝕜 (∑ i in s, F i) x = ∑ i in s, fderiv 𝕜 (F i) x`. Hmm, with the sum over functions `∑ j ∈ univ, (fun z => c.g z i j * Y z i * Z z j)`. The goal at 500-501 is `fderiv ℝ (∑ j ∈ univ, (fun z => ...)) x` appearing after `rw [hbridge]`. Then `rw [fderiv_sum]` — hmm, `fderiv_sum`'s LHS is `fderiv 𝕜 (fun y => ∑ i ∈ s, F i y) x`. The goal has `fderiv ℝ (∑ j ∈ univ, (fun z => ...)) x` — a sum of *functions*, not `fun y => ∑ ...`. Need `fderiv_sum` for `∑ j ∈ univ, f_j` — the lemma might be `DifferentiableAt.sum` + `fderiv_sum` mismatch. The fix used elsewhere (line 478-487): 
```
rw [fderiv_sum]
intro i hi
have hfun : (fun z => ∑ j : ι, ...) = ∑ j ∈ ..., (fun z => ...) := by funext z; simp
rw [hfun]
exact DifferentiableAt.sum (fun j hj => ...)
```
So at line 501-503, after `rw [fderiv_sum]`, the goal is the equality (the differentiability goals come first as a side goal? `rw [fderiv_sum]` generates the hypothesis as a goal — `rw` creates a side goal for the hypothesis, then continues with the main goal). The structure:
```
rw [hbridge]
rw [fderiv_sum]
· intro j hj
  exact ...
· simp
  ...
```
The error at 502 says the first focused goal has no binders to introduce. That suggests after `rw [fderiv_sum]` the first goal is NOT `∀ j, j ∈ univ → DifferentiableAt ...`. Maybe `fderiv_sum` didn't apply to rewrite, leaving goals from the previous `rw [hbridge]`? Or the goal order differs.

Hmm. Let me look more carefully at line 494-510:

```lean
have hbridge : fderiv ℝ (fun z : ChartPoint ι => ∑ j : ι, c.g z i j * Y z i * Z z j) x
    = fderiv ℝ (∑ j ∈ (Finset.univ : Finset ι),
        (fun z : ChartPoint ι => c.g z i j * Y z i * Z z j)) x := by
  congr 1
  funext z
  simp
rw [hbridge]
rw [fderiv_sum]
· intro j hj
  exact (hdiff_g i j).mul ((hdiff_Y i).mul (hdiff_Z j))
· simp
  apply Finset.sum_congr rfl
  ...
```

`fderiv_sum` here — does it exist with that exact signature in this mathlib revision? If `rw [fderiv_sum]` fails to match, rw reports failure — but the error is at the `· intro j hj` line with "no additional binders", meaning `rw [fderiv_sum]` succeeded (or was a no-op?) and the first goal is something else. Perhaps `fderiv_sum` in this revision takes the differentiability as an *instance* argument? No... Or `fderiv_sum` might have LHS `fderiv 𝕜 (∑ i ∈ s, F i) x` where the function is written as `∑ i ∈ s, F i` — matches. Then rw generates side goals: first `∀ j, j ∈ univ → DifferentiableAt ℝ (fun z => ...) x` — that has binders `j hj` — intro should work...

Unless rw generates the side goal AFTER the main goal... rw puts side goals first. Hmm, "There are no additional binders or `let` bindings in the goal to introduce" means the first goal is a proposition with no leading ∀/let. What could it be? If `fderiv_sum` has the signature `fderiv 𝕜 (fun y => ∑ x ∈ s, F x y) x = ∑ x ∈ s, fderiv 𝕜 (fun y => F x y) x` (the lambda-form version), then matching `fderiv ℝ (∑ j ∈ univ, (fun z => ...)) x` against `fderiv 𝕜 (fun y => ∑ x ∈ s, F x y) x` fails because the goal's function is a sum of functions, not a lambda wrapping a sum. Hmm, but `rw` would then fail with "did not find instance"...

Wait, actually maybe the issue is `rw [hbridge]` left the goal with two goals and the focus structure got misaligned — i.e., `rw [hbridge]` succeeded, then `rw [fderiv_sum]` matched and the first subgoal is the DifferentiableAt obligation but with the specific form... Let me not over-analyze: I'll rewrite this proof section cleanly. Better approach for this step: use `fderiv_finset_sum` style. Let me look at what lemmas exist:

- `fderiv_sum (h : ∀ i ∈ s, DifferentiableAt 𝕜 (F i) x) : fderiv 𝕜 (∑ i ∈ s, F i) x = ∑ i ∈ s, fderiv 𝕜 (F i) x` — I believe this is the standard in mathlib (Analysis.Calculus.FDeriv.Add). The earlier use at line 480 compiled? It's in the same broken file... errors listed don't include line 480, so the earlier `rw [fderiv_sum]` + `intro i hi` pattern WORKED (error at 487 is inside that first block, at the `exact DifferentiableAt.sum` line). So `fderiv_sum` exists and works. Then why does line 502's `· intro j hj` fail?

Difference: at line 480, the goal being rewritten was `fderiv ℝ (∑ i ∈ (Finset.univ : Finset ι), (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j)) x` — wait no, at line 478-480 the goal is `(∑ i ∈ univ, fderiv ℝ (fun z => ∑ j : ι, ...) x) (X x)` hmm the rw [fderiv_sum] at 480 rewrites the inner `fderiv ℝ (fun z => ∑ j : ι, ...) x`? No wait:

Line 478-479: `_ = (∑ i ∈ (Finset.univ : Finset ι), fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x) (X x) := by rw [fderiv_sum] ...` — here the goal is `fderiv ℝ (fun z => ∑ i : ι, ∑ j : ι, ...) x (X x) = (∑ i ∈ univ, fderiv ...) (X x)`. The `rw [fderiv_sum]` — hmm, `fderiv_sum` LHS `fderiv 𝕜 (∑ i ∈ s, F i) x`? The term here is `fderiv ℝ (fun z => ∑ i : ι, ...) x` — a lambda. Hmm so `fderiv_sum`'s statement must be the lambda version: `fderiv 𝕜 (fun y => ∑ i ∈ s, F i y) x = ∑ i ∈ s, fderiv 𝕜 (F i) x`? Hmm no...

Let me check mathlib's fderiv_sum:

```lean
theorem fderiv_sum (h : ∀ i ∈ s, DifferentiableAt 𝕜 (F i) x) :
    fderiv 𝕜 (fun y => ∑ i in s, F i y) x = ∑ i in s, fderiv 𝕜 (F i) x := ...
```
Hmm, I actually think in mathlib it's:
```lean
theorem fderiv_sum {ι : Type*} {F : ι → E → F} (h : ∀ i ∈ s, DifferentiableAt 𝕜 (F i) x) :
    fderiv 𝕜 (fun y => ∑ i ∈ s, F i y) x = ∑ i ∈ s, fderiv 𝕜 (F i) x
```
Hmm, but that would need the goal to be `fderiv ... (fun y => ∑ i ∈ s, F i y)`. At line 480 the goal was `(∑ i ∈ univ, fderiv ℝ (fun z => ∑ j : ι, ...) x) (X x)` — hmm I need to see the actual goals. The first `rw [fderiv_sum]` at line 480 — wait, there's an earlier one at line 478? Let me re-read lines 471-491:

```
calc
  fderiv ℝ (fun z => ∑ i : ι, ∑ j : ι, c.g z i j * Y z i * Z z j) x (X x)
      = fderiv ℝ (∑ i ∈ (Finset.univ : Finset ι),
          (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j)) x (X x) := by
        congr 2
        funext z
        simp
  _ = (∑ i ∈ (Finset.univ : Finset ι),
          fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x) (X x) := by
        rw [fderiv_sum]
        intro i hi
        have hfun : ... := by funext z; simp
        rw [hfun]
        exact DifferentiableAt.sum (fun j hj => (hdiff_g i j).mul ((hdiff_Y i).mul (hdiff_Z j)))
```

So here `rw [fderiv_sum]` rewrites `fderiv ℝ (∑ i ∈ univ, (fun z => ∑ j : ι, ...)) x` → `∑ i ∈ univ, fderiv ℝ (fun z => ∑ j : ι, ...) x` with hypothesis `∀ i ∈ univ, DifferentiableAt ℝ (fun z => ∑ j, ...) x`. So `fderiv_sum`'s LHS is `fderiv 𝕜 (∑ i ∈ s, F i) x` — the "sum of functions" form. OK so fderiv_sum: `fderiv 𝕜 (∑ i ∈ s, F i) x = ∑ i ∈ s, fderiv 𝕜 (F i) x` with hypothesis. The rw at 480 creates subgoal `∀ i ∈ univ, DifferentiableAt ...` which the `intro i hi` handles. Good, that's consistent with the error at 487 being about the `DifferentiableAt.mul` associativity.

Then at line 500-503:
```
rw [hbridge]
rw [fderiv_sum]
· intro j hj
  exact (hdiff_g i j).mul ((hdiff_Y i).mul (hdiff_Z j))
```
hbridge rewrites `fderiv ℝ (fun z => ∑ j : ι, ...) x` to `fderiv ℝ (∑ j ∈ univ, (fun z => ...)) x`. Then `rw [fderiv_sum]` should produce subgoal `∀ j ∈ univ, DifferentiableAt ℝ (fun z => c.g z i j * Y z i * Z z j) x` first, then the main goal with the sum. The `· intro j hj` focuses the FIRST goal. "There are no additional binders" means the first goal has no ∀ binder...

Hmm wait — maybe `rw [fderiv_sum]` FAILED to match here, but `rw` with a failed... no, rw fails hard. Unless `rw [fderiv_sum]` matched somewhere unexpected, rewriting a subterm deep inside, producing a weird goal. The goal before `rw [fderiv_sum]` is the calc main goal: `fderiv ℝ (∑ j ∈ univ, (fun z => ...)) x = ∑ j ∈ univ, fderiv ℝ (fun z => ...) x`? No wait, this is inside `have hbridge...; rw [hbridge]; rw [fderiv_sum]` within the big `_ = ∑ i, ∑ j, (...) := by` block:

```
_ = ∑ i : ι, ∑ j : ι, (c.g x i j * ... + ... + ...) := by
    simp
    apply Finset.sum_congr rfl
    intro i hi
    have hbridge : ... := by ...
    rw [hbridge]
    rw [fderiv_sum]
    · intro j hj
```

The goal at this point: `fderiv ℝ (∑ j ∈ univ, (fun z : ChartPoint ι => c.g z i j * Y z i * Z z j)) x = ∑ j : ι, (c.g x i j * fderiv (Y i) + ...)` hmm — after `rw [hbridge]`, the goal should be the calc relation. `rw [fderiv_sum]` rewrites LHS. Side goals from rw with hypotheses are placed as new goals in order — first the hypothesis goals (one for each rewrite occurrence). So first goal: `∀ j ∈ univ, DifferentiableAt ℝ (fun z => c.g z i j * Y z i * Z z j) x`. `intro j hj` should work on `∀ j, j ∈ univ → ...`.

Hmm, unless `rw` generated the hypothesis goal as `∀ j : ι, j ∈ Finset.univ → ...` — still two binders for intro j hj.

Wait — maybe the issue: the error line is 502:20 which is `intro j hj` at the `·` after rw [fderiv_sum] — and the message "Tactic `introN` failed: There are no additional binders or `let` bindings in the goal to introduce". So the first goal is e.g. the main goal (no binders), meaning rw generated NO side goals, meaning `fderiv_sum` rewrote using... hmm. OR the rw matched the RHS `∑ j : ι, (...)`: after `simp` at line 504, the RHS is `∑ j : ι, (c.g x i j * fderiv... * Z x j + ...)`. If `fderiv_sum`'s hypothesis side goal comes AFTER... no.

Alternatively `rw [fderiv_sum]` could match under a different position — like rewriting `fderiv ℝ (∑ j ∈ univ, ...) x` — that's the only fderiv in the goal at that point (the RHS has fderivs of Y and Z! `fderiv ℝ (fun z => Y z i) x` and `fderiv ℝ (fun z => Z z j) x` appear on RHS). `rw [fderiv_sum]` rewrites ALL matching occurrences! The RHS occurrences `fderiv ℝ (fun z => Y z i) x` — do they match `fderiv 𝕜 (∑ i ∈ s, F i) x`? `fun z => Y z i` is a lambda, not a sum — no match. OK so only one match.

Hmm, let me just try it interactively. Actually, let me reconsider: maybe the issue is the previous `rw [hbridge]` — after that, the goal is:

`fderiv ℝ (∑ j ∈ (Finset.univ : Finset ι), (fun z : ChartPoint ι => c.g z i j * Y z i * Z z j)) x = ∑ j : ι, (c.g x i j * fderiv ℝ (fun z : ChartPoint ι => Y z i) x (X x) * Z x j + ...)`

Hmm wait no. Actually this calc step's RHS (the target of the `_ = ... := by` block) is `∑ i : ι, ∑ j : ι, (c.g x i j * fderiv ℝ (fun z : ChartPoint ι => Y z i) x (X x) * Z x j + c.g x i j * Y x i * fderiv ℝ (fun z : ChartPoint ι => Z z j) x (X x) + fderiv ℝ (fun z : ChartPoint ι => c.g z i j) x (X x) * Y x i * Z x j)`. After `simp; apply sum_congr; intro i hi`, the goal is `fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x = ∑ j : ι, (c.g x i j * fderiv (Y z i) (X x) * Z x j + ...)`.

Then `rw [hbridge]`: goal becomes `fderiv ℝ (∑ j ∈ univ, (fun z => c.g z i j * Y z i * Z z j)) x = ∑ j : ι, (...)`. Then `rw [fderiv_sum]` rewrites the LHS and adds side goal. First subgoal = differentiability `∀ j, j ∈ univ → ...`. `· intro j hj` should work.

Unless `fderiv_sum` in this revision is stated as:
```lean
theorem fderiv_sum {ι : Type*} [Fintype ι] {F : ι → E → F} {s : Finset ι} (h : ∀ i ∈ s, DifferentiableAt 𝕜 (F i) x) :
    fderiv 𝕜 (∑ i in s, F i) x = ∑ i in s, fderiv 𝕜 (F i) x :=
```
The side goal would be `∀ j ∈ univ, DifferentiableAt ℝ (fun z => c.g z i j * Y z i * Z z j) x`. Fine.

Hmm, wait — actually maybe the problem is that `rw [fderiv_sum]` is matching the term at a *different* place: `rw` works left-to-right outermost... only one occurrence. 

OK here's another thought: maybe the failure is that after `rw [hbridge]`, since hbridge is an equality of fderivs applied at x — hbridge: `fderiv ℝ (fun z => ∑ j : ι, ...) x = fderiv ℝ (∑ j ∈ univ, (fun z => ...)) x`. `rw [hbridge]` rewrites LHS → RHS... then `rw [fderiv_sum]`... Then the subgoals: `rw` puts the generated goals in order: [diff-goal, main-goal]. The `· intro j hj` handles diff-goal: `∀ j, j ∈ univ → ...` — should have binders!

I can't resolve this by pure thought; let me just load the file and test interactively. Actually the fastest path: rewrite this whole section more carefully. I'll fix the smaller errors first, then use `lake env lean` with error messages to iterate on the remaining ones.

Actually — wait. I wonder about the error at 502:20: `· intro j hj` — maybe `rw [fderiv_sum]` did NOT apply at all (failed to match) — but then rw would error at line 501... The error message for a failed rw is "Tactic `rewrite` failed: Did not find an occurrence..." at that line. The reported error is at 502, so rw succeeded.

Hmm, another possibility: `rw [fderiv_sum]` matched the RHS! On the RHS, inside the summand: `c.g x i j * fderiv ℝ (fun z => Y z i) x (X x) * Z x j + ...` — no sum-of-functions. No.

OK here's yet another possibility — `fderiv_sum`'s hypothesis uses `DifferentiableAt 𝕜` but the rewrite also requires an instance `NormedAddCommGroup` etc. — all present.

Let me just experiment. Let me plan the fixes:

1. `sum_add_split`: replace `rw` with `simp only [Finset.sum_add_distrib]` — but wait, does simp handle nested? `simp only [Finset.sum_add_distrib]` should rewrite both the outer and inner sums repeatedly until fixpoint. The outer summand `∑ j, (f i j + g i j)` is not a literal `+` of two sums, but simp's context: `simp` rewrites `∑ j, (f i j + g i j)` using `Finset.sum_add_distrib` directly (it applies the lemma to the inner sum). Yes, simp applies lemmas at any subterm. Good.

Actually even simpler: `exact Finset.sum_add_distrib ...`? The goal: `∑ i, ∑ j, (f i j + g i j) = (∑ i, ∑ j, f i j) + ∑ i, ∑ j, g i j`. Using `Finset.sum_add_distrib` twice: inner: `∑ j, (f i j + g i j) = ∑ j, f i j + ∑ j, g i j` — the lemma `Finset.sum_add_distrib : (∑ x ∈ s, (f x + g x)) = ∑ x ∈ s, f x + ∑ x ∈ s, g x`? The univ version: `Finset.sum_add_distrib : (∑ x in s, f x + g x) = ...`? Actually `sum_add_distrib` statement: `(∑ x in s, (f x + g x)) = (∑ x in s, f x) + ∑ x in s, g x`. With `s = univ`, `∑ x in univ` = `∑ x`. Hmm the form in goal is `∑ j : ι, (f i j + g i j)` which is notation for `∑ j, f i j + g i j` (bigOperators binds looser, so `∑ j, f i j + g i j` parses as `∑ j, (f i j + g i j)`). `simp only [Finset.sum_add_distrib]` should handle it. Try that first.

2. christoffelFamily_smooth (line 288-294): try `simpa [one_div] using (...)`. Hmm — but `simpa` is trying to prove goal `ContDiff ℝ ∞ fun x => 2⁻¹ * (...)`. The provided term has `(1/2 : ℝ) • (...)`. simp with `one_div` turns `1/2` into `2⁻¹`; `smul` on ℝ... `(a : ℝ) • b` where b : ℝ — the SMul instance for ℝ acting on ℝ: In mathlib the module `ℝ` over `ℝ` — `Semiring.toModule` defines smul := mul? Let me recall the code: 

```lean
instance (priority := 100) Semiring.toModule [Semiring R] : Module R R where
  smul := (· * ·)
```
Hmm, in recent mathlib: `instance (priority := 100) Semiring.toModule : Module R R := { smul := HMul.hMul, ...}`? If so `(1/2) • x` is defeq `(1/2) * x`. But `simpa` compares `ContDiff ℝ ∞ fun x => 2⁻¹ * (...) ` vs `ContDiff ℝ ∞ (fun y => (1 / 2 : ℝ) • (...))` — these are propositions `ContDiff ℝ n f` where `ContDiff` is about the function `f` — the two functions must be defeq. `fun x => 2⁻¹ * t` vs `fun y => (1/2) • t` where t matches — `(1/2) • t` vs `2⁻¹ * t`: if smul = mul defeq, need `(1/2)` defeq `2⁻¹`: `1/2` = `Div.div 1 2`, and `Div ℝ` instance: `instDivOfField`? For fields: `instance Div` := `fun a b => a * b⁻¹`. So `1 / 2 = 1 * 2⁻¹`. `1 * 2⁻¹` vs `2⁻¹` — is `1 * x` defeq `x`? For ℝ, `One.one = OfNat.ofNat 1`, and `OfNat.ofNat 1 = (1 : ℝ)` via `Nat.cast`. Multiplication on ℝ is NOT defined by recursion, so `1 * x` is not definitionally `x`. UNLESS there's a simp-normalization: simp knows `one_mul` — `simpa [one_div]` would simp `1/2` → `2⁻¹`? `one_div : 1 / a = a⁻¹` — simp rewrites `1 / 2` to `2⁻¹`. Then the function becomes `fun y => 2⁻¹ • t` vs target `fun x => 2⁻¹ * t`. If smul=·*· defeq — closed. If smul isn't defeq mul, simp rewrites `a • b` to `a * b` via `smul_eq_mul`? For ℝ specifically there's a lemma like `smul_eq_mul : a • b = a * b` tagged simp? I believe `smul_eq_mul` exists in `Mathlib.Algebra.Module.Basic`? Hmm, there's `Algebra.smul_def : a • b = a * b` and `[simp] theorem smul_eq_mul (a : A) (b : A) : a • b = a * b` in `Mathlib.Algebra.Algebra.Basic`. Yes, `smul_eq_mul` is `[simp]` there. But careful — `simp` will then also rewrite target's `2⁻¹ * t` to `2⁻¹ • t`! So both sides normalize to `fun x => 2⁻¹ • (...)`. Should close with `simpa [one_div]`.

3. `lieBracket` explicit `c` param: change `def lieBracket (X Y : VectorField ι) : VectorField ι :=` to `def lieBracket (c : SmoothChartData ι) (X Y : VectorField ι) : VectorField ι :=` — wait, but `c` is a section variable declared via `variable (c : SmoothChartData ι)`. If I write `(c : SmoothChartData ι)` explicitly in the binder, that's fine (shadows). Then field notation `c.lieBracket X Y` works.

Hmm wait, but actually there's a subtlety: with `variable (c : SmoothChartData ι)` at namespace level, ALL declarations in the namespace that mention `c` get `c` as an explicit parameter inserted BEFORE the explicit binders listed. `lieBracket` doesn't mention `c` in its body, hence no `c` parameter. Adding explicit `(c : SmoothChartData ι)` fixes it.

4. `nabla_const_apply`: need to kill the fderiv of a constant. Replace `simp only [...]` with:
```lean
theorem nabla_const_apply (X0 Y0 : ChartPoint ι) (x : ChartPoint ι) (k : ι) :
    c.nabla (fun _ : ChartPoint ι => X0) (fun _ : ChartPoint ι => Y0) x k =
      nablaOf (c.chartCoefficients x) X0 Y0 k := by
  unfold nabla nablaOf
  simp only [christoffelFamily, fderiv_const, zero_add]
```
Hmm — `fderiv_const` not firing was the issue before. Let me test what `fderiv_const` looks like. Actually, maybe the problem: `fderiv_const` is for `fderiv 𝕜 (fun y : E => c) x = 0` where c : F — the function `fun z : ChartPoint ι => Y0 k` — hmm, the simp congruence: `simp only [fderiv_const]` — when simp encounters `fderiv ℝ (fun z => Y0 k) x`, it tries to match `fderiv ?𝕜 (fun y => ?c) ?x` with the *syntactic* pattern — `fun z => Y0 k` where `Y0 k` — matching `?c` — yes. This should work...

Unless the goal's fderiv function is actually `fun z : ChartPoint ι => (fun _ : ChartPoint ι => Y0) z k` — not reduced! `c.nabla (fun _ => X0) (fun _ => Y0) x k` — `nabla` def: `fun x k => fderiv ℝ (fun z : ChartPoint ι => Y z k) x (X x) + ...`. With `Y = fun _ : ChartPoint ι => Y0`, after `unfold nabla`, the term is `fderiv ℝ (fun z : ChartPoint ι => (fun _ : ChartPoint ι => Y0) z k) x ((fun _ : ChartPoint ι => X0) x) + ...`. Definitional reduction would reduce `(fun _ => Y0) z k` to `Y0 k` — but simp only does this under certain conditions (beta reduction is automatic in the kernel, but the pattern match for `fderiv_const` is against the *whnf-reduced* term; `(fun _ => Y0) z k` whnf-reduces... `(fun _ => Y0) z` reduces to `Y0`, then `Y0 k` is not further reducible. So whnf of the function body is `Y0 k`. Matching `fun z => ?c` against `fun z => Y0 k`: unification `?c := Y0 k` — fine.

Hmm, so why didn't `fderiv_const` fire? Let me actually check the lemma statement in this mathlib rev. It might be:

```lean
theorem fderiv_const (c : F) : fderiv 𝕜 (fun y : E => c) x = 0 := by ...
```
Wait — in the error output, the unsolved goal shows:
```
⊢ (fderiv ℝ (fun z => Y0 k) x) X0 + ∑ i, ∑ j, (c.chartCoefficients x).christoffel k i j * X0 i * Y0 j =
    ∑ i, ∑ j, (c.chartCoefficients x).christoffel k i j * X0 i * Y0 j
```
So simp partially processed: `nabla` unfolded, `nablaOf_apply` simplified RHS, `christoffelFamily` unfolded. The fderiv remained. The warning said `fderiv_const` unused. So `fderiv_const` didn't match. Note: in the goal, the fderiv function is `fun z => Y0 k` — wait, `Y0 k` where `Y0 : ChartPoint ι`... hmm, `Y0` is a *local constant* — fine.

OH WAIT. I see it now! `fderiv_const` might be about `fderiv 𝕜 (fun _ : E => c) x = 0` — but in THIS mathlib revision, maybe the simp attribute: `fderiv_const` is `[simp]`, but the issue is different: the target `(fderiv ... x) X0` — simp rewrites `fderiv ... x` to `0` giving `0 X0`, then `0 X0` reduces to `0` via `ContinuousLinearMap.zero_apply`? `(0 : E →L[ℝ] F) X0 = 0` — that's `ContinuousLinearMap.zero_apply` which should be a simp lemma. If fderiv_const fired, `0 X0`... simp would close. Since the fderiv is still there, fderiv_const really didn't fire.

Hmm, let me check whether in this revision `fderiv_const` requires `NontriviallyNormedField`... all present.

Let me actually check the mathlib source to be sure. I'll grep.

5. `nabla_smul` hfun (line 394-396): replace with `funext z; rfl` if defeq; else `funext z; simp [Pi.smul_apply, smul_eq_mul]`. Let me check: is `(f • Y) z k` defeq `f z * Y z k`? `f • Y` where `f : ChartPoint ι → ℝ`, `Y : VectorField ι = ChartPoint ι → ChartPoint ι`. SMul instance: `Pi.instSMul` with `(f • Y) z := f z • Y z` — the instance `instSMul : SMul ℝ (ChartPoint ι)` via Pi again: `(f z • Y z) k := f z • Y z k`? Hmm wait — actually `SMul ℝ (ι → ℝ)`: scalar `f z : ℝ` — wait no. `f • Y : ChartPoint ι → ChartPoint ι` — the Pi instance for functions: `instSMulForAll : SMul M (∀ i, β i)` where `smul a F := fun i => a • F i`. So `(f • Y) z = f z • Y z`? No! Careful: `f • Y` where `f : ChartPoint ι → ℝ` is the scalar field — this is the module structure `Module (ChartPoint ι → ℝ) (VectorField ι)` = Pi module: scalar is a *function*. So `(f • Y) z = f z • Y z` — hmm, for Pi module over Pi semiring: `(f • Y) z := f z • Y z` where `f z : ℝ` scalar and `Y z : ChartPoint ι`. Yes. Then `(f z • Y z) k = f z • Y z k` (Pi smul again). Then `f z • Y z k : ℝ` — ℝ acting on ℝ — `Semiring.toModule`? If that instance is used, smul := mul so defeq. But WHICH instance is found? `Module ℝ ℝ` — there are several paths: `Algebra.toModule`? For ℝ: `instAlgebraReal...`? Hmm. There's a risk it's not defeq.

Let me check `smul_eq_mul` — `@[simp] theorem smul_eq_mul (a : A) (b : A) : a • b = a * b` for algebra A. If it's [simp], then `funext z; simp only [Pi.smul_apply, smul_eq_mul]` closes. The original code did exactly `simp only [Pi.smul_apply, smul_eq_mul]` without funext — the goal was `(fun z => (f • Y) z k) = fun z => f z * Y z k`. simp can't rewrite under the lambda binder without congruence... simp DOES rewrite under binders via congruence lemmas. For a lambda `fun z => (f • Y) z k`, simp enters the body with `z` as a new local constant, rewrites `(f • Y) z k` → `f z • Y z k`? With `Pi.smul_apply : (s • F) x = s • F x`? Hmm — if `Pi.smul_apply` in this revision has a different name... In mathlib, the lemma `Pi.smul_apply` : `(s • F) x = s • F x` exists? Let me grep to be sure. There's `smul_apply` in `Algebra.Module.Pi`: `theorem smul_apply : (s • F) x = s • F x`? Hmm. The names changed across revisions. In recent mathlib, `Pi.smul_apply` hmm. Let me check with grep.

6. Line 411: remove `trace_state` (debug). Line 424: remove `trace_state`.

7. Line 415 `hΓsmul` proof: `simp only [Pi.smul_apply, smul_eq_mul]` no progress — the goal: `∑ i, ∑ j, christoffel * X x i * (f • Y) x j = f x * (∑ i, ∑ j, christoffel * X x i * Y x j)`. Fix: 
```lean
  have hΓsmul : (∑ i : ι, ∑ j : ι, c.christoffelFamily x k i j * X x i * (f • Y) x j)
      = f x * (∑ i : ι, ∑ j : ι, c.christoffelFamily x k i j * X x i * Y x j) := by
    rw [Finset.mul_sum]
    ...
```
Hmm wait, the original had `simp only [...]` then `rw [Finset.mul_sum]` — the error was at the simp line (415) "made no progress". So `(f • Y) x j` — needs rewriting. `Pi.smul_apply` didn't fire — hmm. Let me check the actual lemma name. Maybe it's `smul_apply` in namespace `Pi`? Or `Pi.smul_apply` exists but has args ordered so simp doesn't use it? It's `[simp]`-tagged? If not [simp]-tagged, `simp only [Pi.smul_apply]` should still use it (simp only uses listed lemmas regardless of simp attribute). "made no progress" means it fired but result was the same goal?? Or didn't match at all (simp only silently ignores non-matching). Since it's `simp only` with exactly those lemmas, non-matching lemmas are just ignored, and then "made no progress" = goal unchanged.

So `Pi.smul_apply` doesn't match `(f • Y) x j`. Let me check: `Pi.smul_apply` might be stated as `(a • F) x = a • F x` — hmm that would match... Unless in this revision it's called `Pi.smul_apply'` or the theorem doesn't exist and there's `Fintype`-something. Let me grep mathlib.

Actually, hmm: in recent mathlib, for Pi types, the simp lemma is `smul_apply` — no wait. Let me check: `Mathlib/GroupTheory/GroupAction/Pi.lean`: `@[simp] theorem smul_apply (a : M) (F : ∀ i, β i) (i : ι) : (a • F) i = a • F i`? Hmm. And `Mathlib/Algebra/Module/Pi.lean` has `smul_apply`? Hmm, hmm. In the mathlib revision 7974e751 (late 2025?), `Pi.smul_apply`... Let me grep.

8. Line 487 and 503: `(hdiff_g i j).mul ((hdiff_Y i).mul (hdiff_Z j))` — need reorder: `((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j)`. Wait — expected: `DifferentiableAt ℝ (fun z => c.g z i j * Y z i * Z z j) x`. `fun z => c.g z i j * Y z i * Z z j` = `fun z => (c.g z i j * Y z i) * Z z j`. `DifferentiableAt.mul A B : DifferentiableAt 𝕜 (fun y => A y * B y)`? No wait — how is `.mul` defined: `DifferentiableAt.mul : DifferentiableAt 𝕜 f x → DifferentiableAt 𝕜 g x → DifferentiableAt 𝕜 (f * g) x` — hmm, `f * g` here is the pointwise product as a *function*: `(f * g) y = f y * g y`. So `.mul A B : DifferentiableAt 𝕜 (f * g) x` where `f * g` is `fun y => f y * g y`. So for the summand `fun z => (c.g z i j * Y z i) * Z z j`, we need f = `fun z => c.g z i j * Y z i` and g = `fun z => Z z j`; result type `DifferentiableAt ℝ ((fun z => c.g z i j * Y z i) * (fun z => Z z j)) x` which is defeq `DifferentiableAt ℝ (fun z => (c.g z i j * Y z i) * Z z j) x`. 

But wait — the reported type mismatch at 487:
```
has type
  DifferentiableAt ℝ ((fun z => c.g z i j) * ((fun z => Y z i) * fun z => Z z j)) x
but is expected to have type
  DifferentiableAt ℝ (fun z => c.g z i j * Y z i * Z z j) x
```
So `(fun z => c.g z i j) * ((fun z => Y z i) * fun z => Z z j)` vs `fun z => c.g z i j * Y z i * Z z j`. The first: `fun z => (c.g z i j) * ((Y z i) * (Z z j))` — associativity mismatch: `(a*b)*c ≠ a*(b*c)` defeq. Fix: `((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j)` gives `((fun z => c.g z i j) * fun z => Y z i) * fun z => Z z j` = `fun z => (c.g z i j * Y z i) * Z z j` — matches since target `fun z => c.g z i j * Y z i * Z z j` = `fun z => (c.g z i j * Y z i) * Z z j`. 

Same fix at line 503.

9. Line 502 `introN` failure — hmm. Given the above, maybe the issue is `rw [fderiv_sum]` generating the diff-goal but ALSO `rw [hbridge]` before it left the goal in a state where... Let me look at line 500-502 again:

```
rw [hbridge]
rw [fderiv_sum]
· intro j hj
```

Hmm — `hbridge` : `fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x = fderiv ℝ (∑ j ∈ univ, (fun z => c.g z i j * Y z i * Z z j)) x`. The current goal (after intro i hi at line 493): `fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x = ∑ j : ι, (...)`. `rw [hbridge]` — rewrites the LHS: goal `fderiv ℝ (∑ j ∈ univ, (fun z => ...)) x = ∑ j : ι, (...)`. `rw [fderiv_sum]`: `fderiv_sum`'s LHS pattern: `fderiv 𝕜 (∑ i ∈ s, F i) x`? But hold on — actually let me reconsider: maybe in this mathlib revision `fderiv_sum` is:

```lean
theorem fderiv_sum {ι : Type*} {F : ι → E → F} {s : Finset ι}
    (h : ∀ i ∈ s, DifferentiableAt 𝕜 (F i) x) :
    fderiv 𝕜 (fun y => ∑ i in s, F i y) x = ∑ i in s, fderiv 𝕜 (F i) x
```
If that's the case, then at line 480 the rw matched `fderiv ℝ (fun z => ∑ i : ι, ∑ j : ι, ...) x`? Hmm — `fun y => ∑ i in s, F i y` — the goal at 480 was `fderiv ℝ (fun z => ∑ i : ι, ∑ j : ι, c.g z i j * Y z i * Z z j) x (X x)` — hmm, with the inner sum `∑ j : ι` not `∑ j in s`. BigOperators: `∑ j : ι` = `∑ j ∈ Finset.univ`? No — `∑ x : ι, f x` is notation for `∑ x ∈ Finset.univ, f x`? Actually `∑ x, f x` for Fintype is `Finset.sum Finset.univ (fun x => f x)` which is defeq `∑ x in Finset.univ, f x`. And in the calc at 471-477, they `congr 2; funext z; simp` to convert `fun z => ∑ i : ι, ∑ j : ι, ...` into `∑ i ∈ univ, (fun z => ∑ j : ι, ...)`. Then rw [fderiv_sum] at 480 with LHS `fderiv ℝ (∑ i ∈ univ, (fun z => ∑ j : ι, ...)) x` — so `fderiv_sum`'s LHS IS the sum-of-functions form: `fderiv 𝕜 (∑ i ∈ s, F i) x`. Confirmed by the fact that the 480 block works (errors don't flag it, except 487 inside).

Then at 500-502, after rw [hbridge], goal: `fderiv ℝ (∑ j ∈ (Finset.univ : Finset ι), (fun z : ChartPoint ι => c.g z i j * Y z i * Z z j)) x = ∑ j : ι, (...)`. `rw [fderiv_sum]` matches LHS; generates side goal `∀ j ∈ univ, DifferentiableAt ℝ (fun z => c.g z i j * Y z i * Z z j) x` — wait, but maybe the hypothesis of fderiv_sum is an instance-implicit? If `fderiv_sum` takes `(h : ∀ i ∈ s, DifferentiableAt ...)` as an explicit argument, rw generates the side goal. `· intro j hj` should work.

Hmm, wait — maybe `rw [fderiv_sum]` ALSO rewrites the RHS sum `∑ j : ι, (...)`? RHS is `∑ j : ι, c.g x i j * fderiv ... + ...` — a sum of numbers, not fderiv of a sum. No.

OR: the hbridge rw didn't produce the expected goal because `rw` rewrote something else... 

You know what, I should stop theorizing. The most efficient approach: make the trivial fixes first, then let Lean tell me the remaining state interactively. Since building takes a while, I'll use `lake env lean` on the file directly for quick iteration.

Also, importantly: I should check `trace_state` — those are leftover debug commands that are errors (`trace_state` is not a valid tactic in production files? It is valid but as a "tactic command" — actually `trace_state` IS a valid tactic (from `Mathlib.Tactic.TraceState`?). It compiled (no error reported for line 411/424 — the errors listed include "error: ...:396:4: `simp` made no progress" and "error: ...:415:4" but no error for trace_state lines). `trace_state` prints the goal state. It's allowed but noisy; should remove for cleanliness.

Let me also check: is there maybe a `set_option linter.unusedSectionVars false` needed? The warnings about unused section vars are just warnings, not errors.

Plan:
1. Fix errors one by one with `lake env lean` fast iteration.
2. Once ChartLeviCivitaSmooth compiles, run full build + audit.
3. Then the substantive addition: curvature of the chart connection. Actually — given 4 hours and the existing breadth, first priority is fixing and auditing. Then, if time permits, add the curvature family (R from Γ) with a 2D model exhibiting nonzero curvature (e.g. hyperbolic or sphere stereographic metric). That would be a strong milestone for "constructing smooth Levi-Civita connection or curvature from metric coefficients on a real chart" — actually the task says "connection OR curvature", and the connection is done. The curvature from coefficients would be a valuable addition though.

Let me also check the remaining files quickly (ChartLeviCivita, ChartLeviCivitaForm, Milnor, Ricci, SoThree, ChartModel1D) for context. Actually those compile (build errors are only in ChartLeviCivitaSmooth). Let me get to work.

Let me first check the mathlib source for the lemmas in question: `fderiv_const`, `fderiv_sum`, `Pi.smul_apply`, `smul_eq_mul`.
dsh: reasoning:
Key findings:

1. `fderiv_const_apply (c : F) : fderiv 𝕜 (fun _ => c) x = 0` — the version matching `fun _ => c` at a point x. That's the one to use. So in `nabla_const_apply`, use `fderiv_const_apply`.

2. `fderiv_sum (h : ∀ i ∈ u, DifferentiableAt 𝕜 (A i) x) : fderiv 𝕜 (∑ i ∈ u, A i) x = ∑ i ∈ u, fderiv 𝕜 (A i) x` — wait, let me see the full statement at line 493.

3. `smul_apply` not found in Pi files — need to check other names. Let me look at what `(f • Y) z` simplifies with. Let me check `Pi.smul_apply` more broadly.
dsh: reasoning:
Findings:
- `fderiv_sum` exists with the sum-of-functions LHS. Good.
- `fderiv_const_apply (c : F) : fderiv 𝕜 (fun _ => c) x = 0` is the right lemma for the constant case.
- `smul_eq_mul` as a theorem — let me find the right name for Pi smul application. Let me check what `(f • Y) z k` normalizes to. In this mathlib, the Pi module: `Mathlib/Algebra/Module/Pi.lean` might have `smul_apply`... it didn't match. Let me grep for "· • F" simp lemmas: `Pi.smul_apply` hmm. Let me check quickly what lemma rewrites `(a • F) x`.
dsh: reasoning:
The Pi module instance: `Pi.distribMulAction` — the smul is defined in `Mathlib/GroupTheory/GroupAction/Defs.lean` or `Mathlib/Algebra/Group/Action/Defs.lean`. Let me find where `(a • F) x` simp lemma lives. Actually, let me just test in Lean directly what works. Faster: write a small test file.

Actually, let me think about what's fastest. The fixes:

1. `sum_add_split`: `simp only [Finset.sum_add_distrib]` — probably works.
2. `christoffelFamily_smooth`: change `simpa using` to handle `•` vs `*` and `1/2` vs `2⁻¹`. Option: rewrite the `hΓ` statement to match: state `hΓ` as `... = fun x => ∑ l, c.gInv x k l * ((1 / 2 : ℝ) • (...))`. Hmm, but the def unfolds to `*`. Alternative: after `rw [hΓ]`, `exact ContDiff.sum ...` won't typecheck due to the mismatch. Try: `simpa [one_div, smul_eq_mul] using (...)`.

Wait, `smul_eq_mul` — is it [simp]? Let me check its declaration. Grep didn't find "theorem smul_eq_mul" — maybe it's in `Mathlib/Algebra/Algebra/Basic.lean` as `@[simp] theorem smul_eq_mul`. Let me search for `smul_eq_mul` in the algebra files.

3. `lieBracket` explicit c param — simple.

4. `nabla_const_apply`: use `fderiv_const_apply`.

5. `hfun` in nabla_smul: `funext z` + need `(f • Y) z k = f z * Y z k`. Let me test what simp lemmas fire. `(f • Y) z k`: `(f • Y) z` reduces how? The module instance for `VectorField ι = ChartPoint ι → ChartPoint ι` is `Pi.module α` above with `smul := fun s F => fun i => s • F i`? Actually `Pi.distribMulAction` — the smul from `Mathlib/GroupTheory/GroupAction/Defs.lean`: `instance Pi.instSMul {α} {f : I → Type v} [∀ i, SMul α (f i)] : SMul α (∀ i : I, f i) where smul s F := fun i => s • F i`? Hmm, but here the scalar type is `ChartPoint ι → ℝ` itself (a Pi type of a *ring*), so `instSMul` is `Pi.instSMulPi`? In `Mathlib/GroupTheory/GroupAction/Defs.lean` there's `instance Pi.instSMul : SMul M (∀ i : I, f i)` where M is a monoid... but here scalar is `(∀ i, ℝ)` — a Pi of ℝ, a ring — so `Pi.module` above: `@Module α (∀ i : I, f i) r ...` with α = the semiring. Wait no — for `f • Y : VectorField ι` with `f : ChartPoint ι → ℝ`: the scalar semiring is `ChartPoint ι → ℝ` = `∀ i, ℝ`, and the module is `Pi.module (∀ i, ℝ) ...` on `∀ z : ChartPoint ι, ChartPoint ι` — so `(f • Y) z = f z • Y z`? No wait — the smul of `Pi.module` is `Pi.distribMulAction _` — its smul is the Pi smul: `(f • Y) z := f z • Y z` where `f z : ℝ` scalar acting on `Y z : ChartPoint ι`. Yes.

Then `(f z • Y z) k = f z • Y z k` — the Pi smul instance for `ChartPoint ι` over ℝ. Then `f z • Y z k : ℝ` where `f z : ℝ` acting on `Y z k : ℝ` — module ℝ over ℝ = `Semiring.toModule`? In mathlib, `Semiring.toModule : Module R R` with `smul := HMul.hMul`? Hmm — wait, actually I recall in mathlib: 

```lean
instance (priority := 100) Semiring.toModule [Semiring R] : Module R R where
  smul := (· * ·)
```
Hmm — no wait, this might be `smul := fun r x => r * x`. If so, `f z • Y z k` is defeq `f z * Y z k`. But there could also be `Algebra ℝ ℝ` with different instance path. The module instance found for `Y z k : ℝ` with scalar `f z : ℝ` — TC search: `Module ℝ ℝ` — the highest priority instance: there's `instModuleRealToSemiringToAddCommMonoidReal`? Or the generic `Semiring.toModule`? Whatever it is, `a • b` for a,b : ℝ — I'm fairly confident simp can handle via `smul_eq_mul`-style lemma if it exists and is [simp]. Let me grep the algebra basic file.

Let me just test quickly with a scratch file. Faster to experiment than theorize. Let me create a scratch test file in tmp/ and run `lake env lean` on it with the imports.
dsh: reasoning:
Very informative:

1. `sum_add_split` via `simp only [Finset.sum_add_distrib]` — PASSED (no error on line 6-8).
2. `fderiv_const_apply` — my syntax was wrong (I applied it at `X0` but the lemma is about fderiv AT x; `fderiv_const_apply (c := Y0 k) (x := X0)` gives `fderiv ℝ (fun _ => c) X0 = 0`. The type mismatch error: `fderiv ℝ (fun z => Y0 k) X0` has type `ChartPoint ι →L[ℝ] ℝ` — hmm that's odd: `fderiv ℝ f X0 : ChartPoint ι →L[ℝ] ℝ` — wait, that says `(fderiv ℝ (fun z => Y0 k) X0)` parsed as `fderiv ℝ (fun z => Y0 k) X0` with X0 as the `x` argument! So the target `(fderiv ℝ (fun z => Y0 k) X0 : ℝ)` — the elaborator parsed the annotation wrongly: `fderiv ℝ f x` where x = X0 gives the linear map. I wanted `(fderiv ℝ (fun z => Y0 k) X0) (X0)`? No — I wrote `fderiv ℝ (fun z => Y0 k) X0` where I intended x := X0 and result applied... it's ambiguous; the annotation `: ℝ` forces it to parse as `fderiv` with only 3 args? It parsed `X0` as the point `x`, giving `ChartPoint ι →L[ℝ] ℝ`. So my test was malformed, not the lemma. In the real code the term is `fderiv ℝ (fun z : ChartPoint ι => Y0 k) x (X0)` — 4 args, fine. `simpa using (fderiv_const_apply (c := Y0 k) (x := x))` would give `fderiv ℝ (fun _ => c) x = 0`, and applying to `(X0)`... need `(fderiv_const_apply ... )` congr-fun'd. Let me redo: in real code: `rw [fderiv_const_apply]`? The lemma: `fderiv_const_apply (c : F) : fderiv 𝕜 (fun _ => c) x = 0`. simp rewriting: target `(fderiv ℝ (fun z => Y0 k) x) (X0)` — `simp only [fderiv_const_apply]` should rewrite the fderiv part to 0. That's the fix for nabla_const_apply.

Wait — why didn't the previous author's `simp only [... fderiv_const ...]` work? They used `fderiv_const` whose statement is `fderiv 𝕜 (Function.const E c) = 0` — as a *function* equality, not applied at a point. The goal has `fderiv ... x`, so `fderiv_const` doesn't match. `fderiv_const_apply` is the one. 

3. `Pi.smul_apply` FAILED — "Did not find an occurrence of the pattern (?a • ?f) ?i". So the name `Pi.smul_apply` doesn't exist (or has a different form). The target `(f • Y) z k`. Hmm — what IS the smul def here? Scalar `f : ChartPoint ι → ℝ` — a Pi type over ℝ, a semiring. `(f • Y) : VectorField ι`. The instance: `Pi.module` with α = `∀ z, ℝ`... its smul comes from `Pi.distribMulAction _` — wait, that gives smul of a *monoid* α? `Pi.distribMulAction` is for monoids M acting on each component. The module instance `Pi.module (α) {r} {m} [∀ i, Module α (f i)]` has scalar type α a *semiring* and uses `Pi.distribMulAction _` — hmm, `Pi.distribMulAction` in `Mathlib/Algebra/GroupWithZero/Action/Pi.lean`? It takes `[Monoid α]`? and `[∀ i, DistribMulAction α (f i)]`. With α = `ChartPoint ι → ℝ` itself a `Monoid` (a ring). smul := pointwise. So `(f • Y) z = f z • Y z` and `(f z • Y z) k = f z • Y z k` — hmm wait, but that's what I assumed. The rewrite pattern didn't match though.

What's the simp lemma for this? In recent mathlib, `Pi.smul_apply` — hmm, apparently doesn't exist here. Let me search for the lemma that rewrites `(a • F) x` in the action files: maybe `smul_apply` in namespace Pi is named differently, or maybe the instance unfolds differently. Let me test: `change (fun i => f i • Y i) z k = f z * Y z k`? Or just try `simp` — plain simp might do it via the definition. Since the goal is a definitional-ish equality, maybe `rfl`? No — `f z • Y z k = f z * Y z k` is a theorem (smul_eq_mul), not rfl, unless smul := mul for ℝ.

Test: `example : (f • Y) z k = f z * Y z k := by simp` — if simp closes it, use `simp` in the code. But careful — in the original file, `simp only [Pi.smul_apply, smul_eq_mul]` made no progress, meaning `smul_eq_mul` is not enough alone (it needs the Pi reductions first). Let me test plain `simp` and also `rfl`.
dsh: reasoning:
Tests 1-4 all passed (only warnings). Test 5 has a syntax error: `ContDiff.const_smul (1 / 2 : ℝ) h` — the parser chokes on `(1 / 2 : ℝ) h`? "expected token" at 26:24 — `ContDiff.const_smul (1 / 2 : ℝ) h` — application of const_smul with two explicit args should be fine... wait, `ContDiff.const_smul (1 / 2 : ℝ)` then `h` — `(1 / 2 : ℝ) h` — hmm, the issue: `ContDiff.const_smul (1 / 2 : ℝ)` is a function, then ` h` applies. But the error "expected token" at col 24: `ContDiff.const_smul (1 / 2 : ℝ)` — hmm col 24 is around `(1 / 2 : ℝ)`. Maybe the parser sees `ContDiff.const_smul (1 / 2 : ℝ) h` fine... The error might be about something else. Actually maybe `const_smul` takes arguments in a different order: `ContDiff.const_smul : (c : 𝕜) → ContDiff 𝕜 n f → ContDiff 𝕜 n (fun x => c • f x)`? That's what the original code used: `ContDiff.const_smul (1 / 2 : ℝ) (ContDiff.add ...)` and the build error said the RESULT had type `ContDiff ℝ ∞ fun y => (1 / 2) • (...)` — so const_smul exists and gives `(1/2) •`. The syntax error in my test at 26:24 — let me count: `  simpa [one_div] using ContDiff.const_smul (1 / 2 : ℝ) h` — position 24 is within... "  simpa [one_div] using " is 24 chars! So the error is right at `ContDiff.const_smul`?? "expected token" at col 24 — after `using `, the parser expected a term but got... `ContDiff.const_smul` should be fine. Hmm — OH! The issue: there's ambiguity — `ContDiff.const_smul (1 / 2 : ℝ) h` — since `(1 / 2 : ℝ) h` could parse as `(1/2 : ℝ)` applied to `h`? No... Actually I bet the problem: `ContDiff.const_smul` needs `f : E → 𝕜`? Hmm no.

Wait, maybe the real issue: in test 5, my `h` has type `ContDiff ℝ ∞ (fun x => 1 : ℝ)` and `const_smul (1/2)` expects `ContDiff ℝ ∞ f` and produces `ContDiff ℝ ∞ (fun y => (1/2) • f y)`. That should elaborate. "expected token" suggests a parse error. Let me look again — is there a `•`? No. Hmm, "26:24: error: expected token" — line 26 is the `example (h : ...)` line? No — line 26 col 24: let me count lines: 1 import, 2 import, 3 import, 4 blank, 5 universe, 6 variable, 7 blank, 8 abbrev, 9 abbrev, 10 blank, 11 comment, 12 example, 13 `(f • Y) z k = f z * Y z k := by`, 14 simp, 15 blank, 16 example, 17 `(fun z ...`, 18 simp, 19 blank, 20 comment, 21 example, 22 `(fderiv ...`, 23 `simpa using congrFun (fderiv_const_apply (c := (Y0 k : ℝ)) (x := x)) X0`, 24 blank, 25 comment, 26 example, 27 `ContDiff ℝ ∞ (fun x : ChartPoint ι => 2⁻¹ * (1 : ℝ)) := by`, 28 `simpa [one_div] using ContDiff.const_smul (1 / 2 : ℝ) h`. Line 26 col 24: `example (h : ContDiff ℝ ∞ ...` — col 24 ≈ `(h : ...)`. Hmm "expected token"... Line 26: `example (h : ContDiff ℝ ∞ (fun x : ChartPoint ι => (1 : ℝ))) :` — that looks fine. Hmm wait — maybe the error is actually on line 28 and numbering is off by 2 because of the heredoc? The heredoc content starts at line 1 = "import Mathlib.Tactic". Let me recount: 1:import Mathlib.Tactic, 2:import ...Mul, 3:import ...Add, 4:blank, 5:universe, 6:variable, 7:blank, 8:abbrev ChartPoint, 9:abbrev VectorField, 10:blank, 11:comment, 12:example f•Y, 13:simp, 14:blank, 15:example fun z, 16:simp, 17:blank, 18:comment fderiv, 19:example, 20:simpa congrFun, 21:blank, 22:comment ContDiff, 23:example, 24:simpa [one_div] using ContDiff.const_smul (1 / 2 : ℝ) h, 25:blank, 26:comment 2⁻¹, 27:example, 28:simp. So line 26 col 24 = `-- 2⁻¹ vs 1/2 alignment` comment? No, col 24 of the comment line is `vs`. Hmm "expected token" on a comment line?? That means the parse error is actually *reported* late — the real error is in line 24: `simpa [one_div] using ContDiff.const_smul (1 / 2 : ℝ) h`. The parser error at 26:24 could be the *continuation* — the `example (x : ℝ) : 2⁻¹ * x = (1 / 2 : ℝ) • x := by simp` at line 27-28 — if line 24's parse consumed weirdly... 

Actually! I see it: `ContDiff.const_smul (1 / 2 : ℝ) h` — the type ascription `(1 / 2 : ℝ)` followed by `h`... The term `ContDiff.const_smul (1 / 2 : ℝ) h` is `((ContDiff.const_smul (1 / 2 : ℝ)) h)` — fine normally. BUT `ContDiff` is both a namespace and... `ContDiff.const_smul` — hmm. Or maybe `const_smul` has an implicit `f` first? No — the original code `ContDiff.const_smul (1 / 2 : ℝ) (ContDiff.add ...)` elaborated fine (its error was about the RESULT type, so parsing worked).

Hmm wait, maybe the issue is `(1 / 2 : ℝ)` — with expected-type inference... Let me just test differently. Actually — I wonder: is it because `const_smul`'s first argument is `c : 𝕜` implicit-ish... no.

Let me just try variants quickly.
dsh: reasoning:
Line 10 col 24: `example (h : ContDiff ℝ ∞ (fun x : ChartPoint ι => (1 : ℝ))) :` — col 24 is around `(fun x : ChartPoint ι => (1 : ℝ))`. Hmm wait — line 10 is the FIRST example line. So the problem is in the example statement itself, not const_smul!

`ContDiff ℝ ∞ (fun x : ChartPoint ι => (1 : ℝ))` — col 24 ≈ `(fun`? Let me count: `example (h : ContDiff ℝ ∞ (fun x : ChartPoint ι => (1 : ℝ))) :` — e=1,...,l=7, space 8, ( =9, h=10, space 11, :=12,13, space 14, ContDiff=15-22, space 23, ℝ=24. AH — `ContDiff ℝ ∞` — at col 24, after ℝ, it expects `∞`... "expected token". So `ContDiff ℝ ∞ (...)` fails to parse! Because `∞` — the order argument of ContDiff is `ℕ∞` and the notation `∞` needs `open scoped ENat`? No... In the original file, `open scoped ContDiff` at line 81 — that's it! The original file has `open scoped ContDiff` which brings the `∞` notation for `ℕ∞`. Without it, `∞` in type position fails to parse.

Good — so my scratch tests need `open scoped ContDiff`. The earlier test 2 line 26 had the same issue.

Let me redo test with `open scoped ContDiff`.
dsh: reasoning:
All tests pass now. So the fixes are:

1. `sum_add_split`: `simp only [Finset.sum_add_distrib]`.
2. `christoffelFamily_smooth`: `simpa [one_div] using ContDiff.const_smul (1 / 2 : ℝ) ...` — wait, but the const_smul produces `(1/2) • (...)` and target is `2⁻¹ * (...)`. In the test, `have h' := ContDiff.const_smul (1 / 2 : ℝ) h` then `simpa [one_div] using h'` worked — where h : `ContDiff ℝ ∞ (fun x => (1 : ℝ))` and target `ContDiff ℝ ∞ (fun x => 2⁻¹ * (1 : ℝ))`. `simpa [one_div]` normalized both `2⁻¹ * 1` and `(1/2) • 1` to the same form. Good. In the real code, the target after `rw [hΓ]` is `ContDiff ℝ ∞ fun x => 2⁻¹ * (c.dFamily x i j l + c.dFamily x j i l - c.dFamily x l i j)`. Wait — hΓ in the code says `((1 / 2 : ℝ) * ...)` and the goal shows `2⁻¹ * ...`. The hΓ proof itself passed (rw worked). So fix line 288-294: `simpa [one_div] using (ContDiff.sum ... (ContDiff.mul ... (ContDiff.const_smul (1/2 : ℝ) (...))))`.

3. `lieBracket`: add explicit `(c : SmoothChartData ι)` binder.

4. `nabla_const_apply`: `simp only [nabla, nablaOf_apply, fderiv_const_apply, christoffelFamily]` — hmm, does `fderiv_const_apply` fire under simp only? `fderiv_const_apply (c : F) : fderiv 𝕜 (fun _ => c) x = 0`. The target has `fderiv ℝ (fun z : ChartPoint ι => Y0 k) x` — hmm wait, in the goal `Y0 k` — need the function to match `fun _ => c` — `fun z => Y0 k` where z doesn't occur — alpha-equivalent, yes. In my scratch test, `simpa using congrFun (fderiv_const_apply (c := (Y0 k : ℝ)) (x := x)) X0` worked. But under `simp only [fderiv_const_apply]`, will it fire? `fderiv_const_apply` is likely `[simp]`-tagged and simp handles the lambda-matching via its congruence. Let me test: `simp only [fderiv_const_apply]` on `(fderiv ℝ (fun z : ChartPoint ι => Y0 k) x) X0 = 0`. Hmm, but wait — there's a subtlety: `Y0 k` — the function body `Y0 k` must be recognized as a "constant" — the congruence rule for function applications introduces `z` and rewrites the body `Y0 k` (doesn't mention z) — but for the pattern `fun _ => c` to match, the body must be rewritten to a form matching `?c` — `Y0 k` matches `?c` directly. Should fire. Let me include this in a test batch with the other fixes.

5. `nabla_smul` hfun: `funext z; simp only [Pi.smul_apply, smul_eq_mul]` — wait, test 4 passed `example (f Y z k) : (f • Y) z k = f z * Y z k := by simp` (plain simp). And test2's `simp` on the function equality passed. So just use `simp` (or `funext z; simp`).

6. `hΓsmul`: the goal `∑ i, ∑ j, christoffel * X x i * (f • Y) x j = f x * ∑ i, ∑ j, ...`. Original: `simp only [Pi.smul_apply, smul_eq_mul]` no progress. Fix: 
```
    simp only [Pi.smul_apply]  -- doesn't exist!
```
Replace with:
```lean
  have hΓsmul : (∑ i : ι, ∑ j : ι, c.christoffelFamily x k i j * X x i * (f • Y) x j)
      = f x * (∑ i : ι, ∑ j : ι, c.christoffelFamily x k i j * X x i * Y x j) := by
    rw [Finset.mul_sum]
    apply Finset.sum_congr rfl
    intro i hi
    rw [Finset.mul_sum]
    apply Finset.sum_congr rfl
    intro j hj
    rw [Pi.smul_apply] -- no
```
Hmm, `(f • Y) x j` — rewrite to `f x * Y x j`: `rw [show (f • Y) x j = f x * Y x j from by simp]` then `ring`. That works: 
```lean
    intro j hj
    have hs : (f • Y) x j = f x * Y x j := by simp
    rw [hs]
    ring
```
Wait — the plain `simp` closed `(f • Y) z k = f z * Y z k` in the test, so `by simp` works per-instance. But careful: `by simp` inside — the goal `(f • Y) x j = f x * Y x j` — matches test. 

7. Line 487/503: change `(hdiff_g i j).mul ((hdiff_Y i).mul (hdiff_Z j))` to `((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j)`.

Hmm wait — but does `.mul` produce `fun z => f z * g z` as claimed? `DifferentiableAt.mul` — in this mathlib: `theorem DifferentiableAt.mul (ha : DifferentiableAt 𝕜 a x) (hb : DifferentiableAt 𝕜 b x) : DifferentiableAt 𝕜 (fun y => a y * b y) x` — the function form. So `.mul A B : DifferentiableAt 𝕜 (fun y => a y * b y) x`. With A = hdiff_g i j : `DifferentiableAt ℝ (fun z => c.g z i j) x` and B = hdiff_Y i : `DifferentiableAt ℝ (fun z => Y z i) x`, `.mul A B : DifferentiableAt ℝ (fun z => c.g z i j * Y z i) x`. Then `((A.mul B).mul C) : DifferentiableAt ℝ (fun z => (c.g z i j * Y z i) * Z z j) x` — the expected type `DifferentiableAt ℝ (fun z => c.g z i j * Y z i * Z z j) x` — `c.g z i j * Y z i * Z z j` parses as `(c.g z i j * Y z i) * Z z j`. DEFEQ. 

8. Line 502 `intro j hj` failure. Since I now know `fderiv_sum (h : ∀ i ∈ u, ...)` has LHS `fderiv 𝕜 (∑ i ∈ u, A i) x`. After `rw [hbridge]` goal: `fderiv ℝ (∑ j ∈ (Finset.univ : Finset ι), (fun z : ChartPoint ι => c.g z i j * Y z i * Z z j)) x = ∑ j : ι, (c.g x i j * ... + ...)`. Hmm wait — actually I realize the goal after hbridge might be applied INSIDE a bigger context. Let me look at lines 490-510 again:

```lean
_ = ∑ i : ι, ∑ j : ι, (c.g x i j * fderiv ℝ (fun z : ChartPoint ι => Y z i) x (X x) * Z x j
      + c.g x i j * Y x i * fderiv ℝ (fun z : ChartPoint ι => Z z j) x (X x)
      + fderiv ℝ (fun z : ChartPoint ι => c.g z i j) x (X x) * Y x i * Z x j) := by
      simp
      apply Finset.sum_congr rfl
      intro i hi
      have hbridge : fderiv ℝ (fun z : ChartPoint ι => ∑ j : ι, c.g z i j * Y z i * Z z j) x
          = fderiv ℝ (∑ j ∈ (Finset.univ : Finset ι),
              (fun z : ChartPoint ι => c.g z i j * Y z i * Z z j)) x := by
        congr 1
        funext z
        simp
      rw [hbridge]
      rw [fderiv_sum]
      · intro j hj
        exact (hdiff_g i j).mul ((hdiff_Y i).mul (hdiff_Z j))
      · simp
        apply Finset.sum_congr rfl
        intro j hj
        rw [fderiv_mul_mul_apply ...]
        ring
```

The goal when entering this `by` block: `fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x = ∑ j : ι, (c.g x i j * fderiv ℝ (fun z => Y z i) x (X x) * Z x j + ...)` — after `simp` at the start of the block (line 504). Hmm wait, the `simp` comes AFTER `rw [fderiv_sum]`'s second bullet. Order of the block: `simp` (line 491) — first line of the `by` — no wait line 491 is `simp` at the block start. Then `apply sum_congr; intro i hi`. Then goal: `fderiv ℝ (fun z => ∑ j : ι, ...) x = ∑ j : ι, (...)`.

`rw [hbridge]`: LHS → `fderiv ℝ (∑ j ∈ univ, (fun z => ...)) x`. 

`rw [fderiv_sum]`: matches LHS: `fderiv ℝ (∑ j ∈ univ, A j) x` with A j = `fun z => c.g z i j * Y z i * Z z j`. Side goal: `∀ j ∈ univ, DifferentiableAt ℝ (A j) x`. Main goal: `∑ j ∈ univ, fderiv ℝ (A j) x = ∑ j : ι, (...)`. 

Focus bullets: first `·` = side goal `∀ j, j ∈ univ → DifferentiableAt ...`. `intro j hj` should succeed!! But it errored "There are no additional binders". 

Unless... `rw [fderiv_sum]` produced the main goal FIRST? `rw` orders new goals: side goals in the order the rewrites happened, then the main goal. Hmm, actually rw generates goals: for each rewrite with a hypothesis, the hypothesis goal is added — the order is: all new goals from the first rw, etc. Typically `rw` puts the generated subgoal FIRST, then the rewritten main goal. That's standard.

Hmm wait — but there's a catch: maybe `rw [hbridge]` did NOT rewrite the LHS as I assumed. `rw` rewrites ALL occurrences of the LHS pattern in the goal... only one occurrence. OK.

Hmm, actually, wait. Let me recount the error. The error at 502:20: `· intro j hj` — col 20. Line 502 is `    · intro j hj`. Hmm, is line 502 really the first bullet? From the read: line 500: `rw [hbridge]`, 501: `rw [fderiv_sum]`, 502: `· intro j hj`, 503: `exact (hdiff_g i j).mul ...`, 504: `· simp`, ...

Alternatively — maybe `rw [fderiv_sum]` FAILED to match, and `rw` (without failing??) — no, rw fails the proof if no match.

OR: `rw [fderiv_sum]` matched but the lemma's hypothesis `h` is an implicit instance-style argument?? `fderiv_sum (h : ∀ i ∈ u, DifferentiableAt 𝕜 (A i) x)` — h is explicit. rw generates side goal.

Hmm — wait, actually there's ANOTHER possibility: `rw [fderiv_sum]` — the unifier found the match in the goal but rw needs to also *solve* the motive — fine.

OR the issue is that hbridge's rw failed silently?? `rw [hbridge]` — if no occurrence found, error at line 500. Not the case.

Hmm, let me look at the error message again: "error: Poincare/D12/ConnectionCurvature/ChartLeviCivitaSmooth.lean:502:20: Tactic `introN` failed: There are no additional binders or `let` bindings in the goal to introduce". OK so the FIRST bullet goal after `rw [fderiv_sum]` has no binders. What if `rw [fderiv_sum]` created the side goal `∀ j, j ∈ univ → DifferentiableAt ...` — that HAS binders. What if rw's side-goal creation puts the DIFFERENTIABILITY goal in a different form — `∀ (j : ι), j ∈ Finset.univ → ...` — binders!

Unless the side goal is actually the *differentiability at each j* — and rw used `fun j hj => ...`? No.

Wait wait. Maybe the actual goal at that point is NOT what I think. Let me look at line 494-499 again:

```
have hbridge : fderiv ℝ (fun z : ChartPoint ι => ∑ j : ι, c.g z i j * Y z i * Z z j) x
    = fderiv ℝ (∑ j ∈ (Finset.univ : Finset ι),
        (fun z : ChartPoint ι => c.g z i j * Y z i * Z z j)) x := by
  congr 1
  funext z
  simp
```

Hmm — hbridge's LHS: `fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x` — the fderiv at x. RHS: `fderiv ℝ (∑ j ∈ univ, (fun z => c.g z i j * Y z i * Z z j)) x` — sum of functions, fderiv at x. 

The current goal (before hbridge): `fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x (X x) = ∑ j : ι, (...)` — hmm wait! Does the goal include `(X x)` application? Look at the calc structure at line 488-490: `_ = ∑ i : ι, ∑ j : ι, (c.g x i j * fderiv ℝ (fun z : ChartPoint ι => Y z i) x (X x) * Z x j + ...)` — the target. And the previous calc step at 478: `_ = (∑ i ∈ univ, fderiv ℝ (fun z => ∑ j : ι, ...) x) (X x)`. Hmm — line 478-479: the goal was `... (X x) = (∑ i ∈ univ, fderiv ℝ (fun z => ∑ j : ι, ...) x) (X x)` — the outer application to (X x) remains!

Then at line 488-491: target `∑ i, ∑ j, (c.g x i j * fderiv ℝ (fun z => Y z i) x (X x) * Z x j + ...)`. After `simp` (line 491): hmm — `simp` distributes the outer `(∑ i ∈ univ, fderiv...) (X x)`? `(∑ i, f_i) v = ∑ i, f_i v` — yes simp handles `sum_apply`-style: for continuous linear maps, `(∑ i, L i) v = ∑ i, L i v` — simp has `map_sum`... The simp then applies sum_congr and the goal becomes per-i: `fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x (X x) = ∑ j : ι, (c.g x i j * fderiv ℝ (fun z => Y z i) x (X x) * Z x j + ...)`. 

WAIT — so the current goal at line 500 DOES have `(X x)` applied to the fderiv! hbridge's LHS is `fderiv ... x` WITHOUT `(X x)`. So `rw [hbridge]` — hbridge's LHS pattern `fderiv ℝ (fun z => ∑ j : ι, ...) x` occurs as a subterm of the goal `fderiv ℝ (fun z => ∑ j : ι, ...) x (X x) = ...` — rw rewrites the subterm: goal becomes `fderiv ℝ (∑ j ∈ univ, (fun z => ...)) x (X x) = ∑ j : ι, (...)`. Fine.

Then `rw [fderiv_sum]` — matches `fderiv ℝ (∑ j ∈ univ, A j) x` subterm — side goal `∀ j ∈ univ, DifferentiableAt ℝ (A j) x` — should be first...

Hmm OK — actually, hmm, maybe the issue: `rw [fderiv_sum]` — the lemma's conclusion `fderiv 𝕜 (∑ i ∈ u, A i) x = ∑ i ∈ u, fderiv 𝕜 (A i) x` — rw also rewrites the RHS of the goal?? The goal RHS: `∑ j : ι, (c.g x i j * fderiv ℝ (fun z => Y z i) x (X x) * Z x j + ...)` — contains `fderiv ℝ (fun z => Y z i) x` — pattern `fderiv 𝕜 (∑ i ∈ u, A i) x` — no match. OK.

So why "no binders"?! Let me think about `rw [hbridge]` — it also rewrites the RHS?? hbridge LHS `fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x` — appears once.

Hmm, let me look at the bullet structure more carefully. After `rw [fderiv_sum]` there are `·` bullets at 502 and 504. The error is only at 502 (`intro`), and 504's bullet contents (`simp; apply sum_congr...`) come later. The compiler reports the FIRST error at 502 — so the first goal is the one with no binders.

What if rw did NOT create a side goal because... the differentiability hypothesis of `fderiv_sum` got *auto-synthesized*?? No — it's explicit.

What if the goal order: rw puts the main goal first?? Actually — hmm! I recall that `rw` (the `rewrite` tactic) puts *all* side goals generated by the rewrite FIRST, then the final goal. Hmm, but maybe for this specific rw, the side goal was generated and it's `∀ j, ...` — binders.

Hmm, hold on, let me reconsider: maybe the goal at 500 is different — maybe after `apply Finset.sum_congr rfl; intro i hi`, the goal is `fderiv ℝ (∑ j : ι, c.g z i j * Y z i * Z z j) x (X x) = ...`?? No wait — `simp` at 491 might have transformed the LHS: `(∑ i ∈ univ, fderiv ℝ (fun z => ∑ j : ι, ...) x) (X x)` → `∑ i ∈ univ, fderiv ℝ (fun z => ∑ j : ι, ...) x (X x)`. And then apply sum_congr on `∑ i ∈ univ, ...` vs `∑ i : ι, ∑ j : ι, ...` — hmm, the RHS is a double sum `∑ i : ι, ∑ j : ι, ...` and LHS is `∑ i ∈ univ, ...` — sum_congr with rfl unifies `∑ i ∈ univ, f i = ∑ i : ι, ...`? `∑ i : ι, ∑ j : ι, t i j` is `Finset.univ.sum (fun i => ∑ j, t i j)` — the sum_congr needs `s = t` and per-element equality. `rfl` for `univ = univ`. Then per-i: `fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x (X x) = ∑ j : ι, (...)`. OK so my analysis holds: goal has `(X x)`.

So at 500: `rw [hbridge]` → `fderiv ℝ (∑ j ∈ univ, (fun z => ...)) x (X x) = ∑ j : ι, (...)`.

501: `rw [fderiv_sum]` → side goal + main goal `(∑ j ∈ univ, fderiv ℝ (fun z => ...) x) (X x) = ∑ j : ι, (...)`.

Side goal first: `∀ j ∈ univ, DifferentiableAt ℝ (fun z => c.g z i j * Y z i * Z z j) x`. `· intro j hj` — binders `j` and `hj`...

UNLESS `rw [fderiv_sum]` — the h hypothesis — wait, maybe rw didn't add a side goal but instead the *entire* rewrite produced a goal `∀ j ...` *after* the main goal, and the FIRST goal is the main goal `(∑ j ∈ univ, fderiv ...) x (X x) = ...` which has no binders → intro fails! 

Is that possible? rw's goal ordering... I believe rw puts generated goals first. Hmm, but maybe not — let me check: in Lean 4, `rw` creates side goals for hypotheses, and the order... From experience: `rw [h]` where h : P → a = b generates goals `⊢ P` first? Actually I recall that rw puts the NEW goals BEFORE the main goal — yes, side goals first. Hmm.

But wait — there's another subtlety: the `·` bullets. If rw generated TWO goals [diff-goal, main-goal], then `· intro j hj` targets diff-goal — should work. The error says it doesn't have binders. So the first goal is NOT the diff-goal. So rw put main goal first?? Or... rw didn't generate a side goal at all because the unifier solved `h`?? No.

Hmm — actually WAIT. I bet I know what happened! `rw [fderiv_sum]` — the REWRITE: rw tries to close side goals automatically by `try solve | assumption ...`? No.

Alternatively — maybe `rw [fderiv_sum]` matched NOT the LHS fderiv but something else, e.g. via `fderiv_sum`'s conclusion being symmetric — no, rw uses LHS→RHS only.

OK here's the thing — I can't resolve this from armchair analysis. Best to just make all the trivial fixes and then interactively debug the remaining errors by iterating. The `lake env lean` cycle is fast enough. Let me now apply the fixes I'm confident about, and for the `fderiv_sum` bullet issue, I'll restructure the proof slightly: replace

```
      rw [hbridge]
      rw [fderiv_sum]
      · intro j hj
        exact ((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j)
      · simp
```
with
```
      rw [hbridge]
      rw [fderiv_sum (fun j hj => ((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j))]
      simp
```
Wait — that would require the side goal solved in the rewrite... `rw [fderiv_sum (fun j hj => ...)]` — no, rw's argument must be a lemma; can't apply a proof to it inline... Actually you CAN: `rw [fderiv_sum (fun j hj => ...)]` — no, rw takes terms of type `lhs = rhs`; `fderiv_sum (fun j hj => ...)` IS a term of the right equality type if the hypothesis elaborates. But the A i's must unify with the goal's functions — the expected type of the term is fixed by the lemma's type with metavariables for A and x — rw will unify. This can work: `rw [fderiv_sum (fun j hj => ((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j))]`.

Hmm, but is `.mul` with the associativity right? `((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j)` : `DifferentiableAt ℝ (fun z => c.g z i j * Y z i * Z z j) x` — yes as analyzed.

Actually — hold on, let me reconsider the "no binders" mystery once more, because if rw put the MAIN goal first, then `· intro j hj` hits `(∑ j ∈ univ, fderiv ...) x (X x) = ∑ j : ι, (...)` — no binders — EXACTLY the reported error. So apparently in this Lean version, `rw` puts the main goal first?? That contradicts my memory... Actually — hmm, I now recall: `rw` in Lean 4: when a rewrite needs a hypothesis, the tactic generates the side goals and then the result — and the ORDER in the goal list is: side goals first. I've seen `rw [foo]; · intro ...; · ...` patterns work...

Hmm, actually no wait — I recall there IS a subtlety: `rw` generates side goals from instances/hypotheses, and the order is side goals BEFORE the main goal, BUT when there are multiple side goals they're reversed?? For a single side goal, it should come first. Hmm.

Let me look at the first block that WORKED (lines 478-487):
```
      _ = (∑ i ∈ (Finset.univ : Finset ι),
              fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x) (X x) := by
            rw [fderiv_sum]
            intro i hi
            have hfun : ... := by funext z; simp
            rw [hfun]
            exact DifferentiableAt.sum (fun j hj => (hdiff_g i j).mul ((hdiff_Y i).mul (hdiff_Z j)))
```
Here after `rw [fderiv_sum]` the remaining goal is the main goal (with NO bullet), and `intro i hi` — WAIT. If the side goal `∀ i ∈ univ, DifferentiableAt ...` were the first goal, then `intro i hi` would apply to IT and then the rest (`have hfun; rw; exact DifferentiableAt.sum ...`) would be the DIFFERENTIABILITY proof — and then the main goal would remain UNSOLVED... but the error was at 487 (the DifferentiableAt.mul type mismatch) — inside this proof — so the goals after rw were: first goal = `∀ i ∈ univ, DifferentiableAt ...`, which `intro i hi`... hmm, and the `exact DifferentiableAt.sum (fun j hj => ...)` at 487 closes the DIFFERENTIABILITY goal. Then the main goal remains?! No — if the main goal remained unsolved, there'd be an "unsolved goals" error after the block. Errors listed: 487 type mismatch — maybe the main goal error comes after fixing 487.

Hmm wait, no. Let me recount: after `rw [fderiv_sum]` at 480, IF side-goal-first ordering, goals = [∀ i ∈ univ, DifferentiableAt ℝ (fun z => ∑ j : ι, ...) x; main]. Then `intro i hi` (goal 1), `have hfun...`, `rw [hfun]` — goal: `DifferentiableAt ℝ (∑ j ∈ univ, (fun z => c.g z i j * Y z i * Z z j)) x`, `exact DifferentiableAt.sum (fun j hj => ...)` — closes goal 1. Main goal: `(∑ i ∈ univ, fderiv ...) x (X x) = ...`? Wait no — the calc step's goal: LHS `fderiv ℝ (∑ i ∈ univ, (fun z => ∑ j : ι, ...)) x (X x)` = RHS `(∑ i ∈ univ, fderiv ℝ (fun z => ∑ j : ι, ...) x) (X x)`. The rw at 480 rewrites LHS (the fderiv of the sum of functions). Main goal after rw: `(∑ i ∈ univ, fderiv ℝ (fun z => ∑ j : ι, ...) x) (X x) = (∑ i ∈ univ, fderiv ℝ (fun z => ∑ j : ι, ...) x) (X x)` — rfl! So the main goal is closed by rfl automatically (rw's post-processing does `rfl`? No — the goal would be left as `(∑...) v = (∑...) v` — hmm, rw doesn't auto-close; but the tactic `rw` followed by nothing... the block ends after `exact DifferentiableAt.sum ...`. If the main goal `x = x` remains, the block fails with unsolved goals. Unless rw's `dsimp`/refl handling closed it. Hmm — actually `rw` leaves the goal `(∑ ...) (X x) = (∑ ...) (X x)` — that's closed by `rfl` — and in calc-style `by` blocks, unsolved rfl goals ARE auto-closed? No, they're not.

But the error list doesn't show an unsolved-goals error for the 478-487 block — only the type mismatch at 487. So maybe: rw ordered [main, side]? Then after rw: goal 1 = main = rfl-trivial? `intro i hi` on `(∑ i ∈ univ, ...) (X x) = (∑ i ∈ univ, ...) (X x)` — intro fails — no binders!! But there's no such error at 481... So NO.

OK so ordering must be side-goals-first at 480, with the main goal being rfl and auto-closed somehow... Hmm, actually — WAIT. I remember now! `rw` in Lean 4 uses `rewrite` which, at the end, applies... no. But there's the fact that the main goal after the rewrite is *definitionally* `x = x`, and `rw`'s default postprocessing doesn't close it. Hmm.

Let me just check: maybe the calc block at 478-487 compiled fine because the main goal WAS left and the unsolved-goal error wasn't reported in the tail I saw (I only looked at the head of the errors!). The build output showed errors from 150 to 502 — I saw them all listed including 382, 396, 415, 487, 502... The list I captured was from `head -80` — there could be MORE errors (unsolved goals) not shown. Right — my grep showed the first batch only.

OK. Enough. Empirical approach: fix everything I'm confident about, then iterate on the remaining via lake env lean. Let me now make all edits:

**Fix 1** — sum_add_split (line 148-150):
```lean
lemma sum_add_split (f g : ι → ι → ℝ) :
    (∑ i : ι, ∑ j : ι, (f i j + g i j)) = (∑ i : ι, ∑ j : ι, f i j) + ∑ i : ι, ∑ j : ι, g i j := by
  simp only [Finset.sum_add_distrib]
```

**Fix 2** — christoffelFamily_smooth (line 288-294): replace the `simpa using (ContDiff.sum ... (ContDiff.const_smul (1 / 2 : ℝ) ...))` with:
```lean
  simpa [one_div] using (ContDiff.sum (s := (Finset.univ : Finset ι)) (by
    intro l hl
    refine ContDiff.mul ?_ ?_
    · exact c.gInv_component_smooth k l
    · exact ContDiff.const_smul (1 / 2 : ℝ)
        (ContDiff.add (c.dFamily_smooth i j l)
          (ContDiff.sub (c.dFamily_smooth j i l) (c.dFamily_smooth l i j)))))
```
Wait — but the hΓ statement: `fun x => ∑ l, c.gInv x k l * ((1 / 2 : ℝ) * (dFamily + dFamily - dFamily))`. After `rw [hΓ]` goal: `ContDiff ℝ ∞ (fun x => ∑ l : ι, c.gInv x k l * ((1 / 2 : ℝ) * (...)))`. The const_smul term: `ContDiff ℝ ∞ (fun y => ∑ l ∈ univ, ...)` hmm — the sum: `ContDiff.sum` with explicit `s := univ` gives `ContDiff ℝ ∞ (∑ i ∈ univ, F i)`? Let me check `ContDiff.sum`: `theorem ContDiff.sum {ι : Type*} {F : ι → E → F} {s : Finset ι} (h : ∀ i ∈ s, ContDiff 𝕜 n (F i)) : ContDiff 𝕜 n (fun x => ∑ i ∈ s, F i x)`. So the term gives `ContDiff ℝ ∞ (fun x => ∑ l ∈ univ, c.gInv x k l * ((1/2) • (...)))`. Target after rw: `ContDiff ℝ ∞ (fun x => ∑ l : ι, c.gInv x k l * ((1/2) * (...)))`. `∑ l ∈ univ, t` vs `∑ l : ι, t` — defeq? `∑ l ∈ univ, t` = `Finset.sum univ (fun l => t)`, `∑ l : ι, t` = `Finset.univ.sum (fun l => t)` — same term (bigOperators notation `∑ x, f x` is `Finset.univ.sum fun x => f x`; `∑ x ∈ s, f x` is `Finset.sum s (fun x => f x)`). With s = univ — defeq. Good. And `(1/2) • t` vs `(1/2) * t` and `2⁻¹` — simpa [one_div] handles: `1/2` → `2⁻¹` via one_div, `•` → `*` via smul_eq_mul [simp]. In my scratch test this exact pattern worked (`have h' := ContDiff.const_smul (1 / 2 : ℝ) h; simpa [one_div] using h'`).

**Fix 3** — lieBracket (line 339-341): add explicit c:
```lean
def lieBracket (c : SmoothChartData ι) (X Y : VectorField ι) : VectorField ι :=
  fun x k => ...
```
Note: within namespace SmoothChartData, `variable (c : SmoothChartData ι)` — declaring `(c : ...)` explicitly in the def shadows. Fine. Then line 344-345 `c.lieBracket X Y` works, and `unfold lieBracket` in nabla_torsionFree still works.

**Fix 4** — nabla_const_apply (line 380-383):
```lean
theorem nabla_const_apply (X0 Y0 : ChartPoint ι) (x : ChartPoint ι) (k : ι) :
    c.nabla (fun _ : ChartPoint ι => X0) (fun _ : ChartPoint ι => Y0) x k =
      nablaOf (c.chartCoefficients x) X0 Y0 k := by
  simp only [nabla, nablaOf_apply, fderiv_const_apply, christoffelFamily]
```
Test whether `fderiv_const_apply` fires under simp only. The function in the goal: after `simp only [nabla, ...]`, the fderiv term: `fderiv ℝ (fun z : ChartPoint ι => (fun _ : ChartPoint ι => Y0) z k) x` — hmm! The `nabla` def has `Y z k` where `Y` is `(fun _ => Y0)` — the simplifier's beta-reduction: simp does reduce `(fun _ => Y0) z` to `Y0`? simp does beta reduction as part of its normalization — yes, simp performs whnf which includes beta. So `fderiv ℝ (fun z => Y0 k) x` should appear and `fderiv_const_apply` fires. But — wait, `Y0 k` — the pattern `fun _ => c` — matches with c := Y0 k. Then goal `0 X0 + sums = sums`... hmm — `(0 : ChartPoint ι →L[ℝ] ℝ) X0` — simp needs `zero_apply`-style: there's `ContinuousLinearMap.zero_apply` [simp]. If not in the only-set, it stays. Add to the simp only list? `zero_add` needed too. Let me include a fallback: after the simp, `simp` again? Or:
```lean
  simp only [nabla, nablaOf_apply, fderiv_const_apply, christoffelFamily]
  rfl
```
Hmm — after first simp, remaining goal: `0 X0 + ∑... = ∑...` — `0 X0` — is `(0 : ChartPoint ι →L[ℝ] ℝ) X0` reducible to 0 by rfl? `0 : ContinuousLinearMap` — defined as `0 := ⟨0, ...⟩`? ContinuousLinearMap.zero is `{ toLinearMap := 0, cont := ... }`? Hmm, `zero_apply` is a theorem not rfl? For ContinuousLinearMap, `instZero` := `⟨(0 : E →ₗ[𝕜] F), ...⟩` and application of `⟨f, c⟩` is `f` — so `(0 : E →L[𝕜] F) v` reduces to `(0 : E →ₗ[𝕜] F) v` — and LinearMap.zero_apply: `0 v = 0` — is `LinearMap.zero_apply` rfl? LinearMap.zero := `{ toFun := fun _ => 0, ... }` — so `(0 : E →ₗ F) v` = `(fun _ => 0) v` = 0 by beta. So actually `(0 : E →L[𝕜] F) X0` IS definitionally `0`. So after `fderiv_const_apply` fires, the goal is `0 + sums = sums` and `zero_add` — `0 + x` reduces to `x`? For ℝ, addition `0 + x` — `OfNat` — `Nat.cast`... `0 + x` for ℝ: `zero_add` is a theorem (addition on ℝ not recursive). So need `zero_add` — simp only with `zero_add`? Hmm, but rfl won't close `0 + x = x` if addition isn't definitional. But hmm — actually for ℝ addition, `0 + x = x` — is it defeq? ℝ is defined via Cauchy sequences; addition is a defined function; `0 + x` doesn't reduce. So need simp lemma `zero_add`. 

So: `simp only [nabla, nablaOf_apply, fderiv_const_apply, christoffelFamily, zero_add]` — hmm, will simp's `only`-set process the `0 X0`? After fderiv_const_apply rewrites `fderiv ... x` → `0`, the term is `(0 : ChartPoint ι →L[ℝ] ℝ) X0` — simp only [.., zero_add] — does simp reduce `0 X0` to 0 automatically? simp does whnf-ish normalization of the whole term — `(0) X0` — hmm, `0 X0` — with the only-set, simp first puts the term in whnf: `0 X0` where `0 : E →L[𝕜] ℝ` — whnf of `0` is the constructor `⟨(0 : E →ₗ[𝕜] ℝ), _⟩` — `⟨f, c⟩ X0` reduces to `f X0` = `(0 : E →ₗ[𝕜] ℝ) X0` — then whnf again: `0 : LinearMap` = `⟨fun _ => 0, ...⟩`? — reduces to `0`. So by pure whnf reduction (definitional), `(0 : E →L[ℝ] ℝ) X0` → 0. Yes — since both zero instances are structure-wrapped functions, application reduces definitionally. So after the rewrite, the term normalizes to `0 + ∑ ... = ∑ ...`, and `zero_add` [simp] closes. So `simp only [nabla, nablaOf_apply, fderiv_const_apply, christoffelFamily, zero_add]` should close. If not, add a second `simp`.

**Fix 5** — hfun in nabla_smul (line 394-396): 
```lean
  have hfun : (fun z : ChartPoint ι => (f • Y) z k) = fun z => f z * Y z k := by
    funext z
    simp
```
(test2 confirmed `simp` closes the pointwise equality and plain `simp` on the function equality... wait test2's second example used plain `simp` on the function equality and it passed. So `simp` alone suffices — but the linter might complain about unused... no. Let me use `simp` directly.)

Hmm wait — test 2 second example: `example ... : (fun z : ChartPoint ι => (f • Y) z k) = fun z => f z * Y z k := by simp` — passed. OK.

**Fix 6** — remove `trace_state` at 411 and 424.

**Fix 7** — hΓsmul (line 413-422):
```lean
  have hΓsmul : (∑ i : ι, ∑ j : ι, c.christoffelFamily x k i j * X x i * (f • Y) x j)
      = f x * (∑ i : ι, ∑ j : ι, c.christoffelFamily x k i j * X x i * Y x j) := by
    rw [Finset.mul_sum]
    apply Finset.sum_congr rfl
    intro i hi
    rw [Finset.mul_sum]
    apply Finset.sum_congr rfl
    intro j hj
    have hs : (f • Y) x j = f x * Y x j := by simp
    rw [hs]
    ring
```
Wait — the original `rw [Finset.mul_sum]` — `mul_sum : c * ∑ x ∈ s, f x = ∑ x ∈ s, c * f x`? The goal: LHS `∑ i, ∑ j, Γ * X x i * (f • Y) x j` = RHS `f x * (∑ i, ∑ j, Γ * X x i * Y x j)`. `rw [Finset.mul_sum]` on RHS: `f x * (∑ i, ...)` → `∑ i, f x * (∑ j, Γ * X x i * Y x j)`. Then sum_congr per i: goal `∑ j, Γ * X x i * (f • Y) x j = f x * ∑ j, Γ * X x i * Y x j`; `rw [Finset.mul_sum]` → `∑ j, f x * (Γ * X x i * Y x j)`; per j: `Γ * X x i * (f • Y) x j = f x * (Γ * X x i * Y x j)`; `rw [hs]; ring`. Good. Note `mul_sum`'s exact form: `mul_sum : a * ∑ x ∈ s, f x = ∑ x ∈ s, a * f x`? It might need the univ form... `∑ i, ...` = `∑ i ∈ univ, ...` — fine.

Hmm wait, careful: `rw [Finset.mul_sum]` on RHS `f x * (∑ i : ι, ∑ j : ι, ...)` — the inner is a nested sum; mul_sum matches `a * ∑ x ∈ s, f x` with a := f x, s := univ, f := fun i => ∑ j, ...`. Rewrites to `∑ i ∈ univ, f x * (∑ j, ...)`. Good.

**Fix 8** — line 487: `.mul ((hdiff_Y i).mul (hdiff_Z j))` → `.mul` nesting: `((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j)`.

**Fix 9** — line 502-503: restructure:
```lean
      rw [hbridge]
      rw [fderiv_sum (fun j hj => ((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j))]
      simp
      apply Finset.sum_congr rfl
      intro j hj
      rw [fderiv_mul_mul_apply (f := fun z : ChartPoint ι => c.g z i j)
        (g := fun z : ChartPoint ι => Y z i) (h := fun z : ChartPoint ι => Z z j)
        (hf := hdiff_g i j) (hg := hdiff_Y i) (hh := hdiff_Z j) (v := X x)]
      ring
```
Wait — but `rw [fderiv_sum (fun j hj => ...)]` — will rw accept a term with explicit hypothesis? `fderiv_sum (fun j hj => H j hj)` has type `fderiv 𝕜 (∑ i ∈ u, A i) x = ∑ i ∈ u, fderiv 𝕜 (A i) x` for the right A — rw unifies A with the goal's functions. Potential issue: rw's unification with the explicit `A` in the term — the term has metavariables for A, u, x — rw should unify fine.

Hmm, but actually why did the original `· intro j hj` fail with "no binders"? If rw puts main-goal-first... wait — that can't be, the first block's pattern `rw [fderiv_sum]; intro i hi; ...` worked with side-goal-first ordering. Unless... OH WAIT. I see it now!!! At line 501, `rw [fderiv_sum]` — the goal is `fderiv ℝ (∑ j ∈ univ, A j) x (X x) = ∑ j : ι, (...)`. But `fderiv_sum`'s LHS is `fderiv 𝕜 (∑ i ∈ u, A i) x` — the goal has an EXTRA application `(X x)`!! `rw` matches subterms, so it rewrites the subterm `fderiv ℝ (∑ j ∈ univ, A j) x` inside `fderiv ℝ (∑ j ∈ univ, A j) x (X x)` — that works. Side goal: `∀ j ∈ univ, DifferentiableAt ℝ (A j) x`. Fine...

Hmm, so really why? Let me just run the file and see the goal states. I'll use `set_option pp.all false` and put `trace_state` temporarily... Actually, let me use a cleaner approach: rather than rw, do:

```lean
      rw [hbridge]
      rw [fderiv_sum (fun j hj => ((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j))]
```

If rw with the term fails to unify A, alternative:
```lean
      calc
        fderiv ℝ (fun z : ChartPoint ι => ∑ j : ι, c.g z i j * Y z i * Z z j) x (X x)
            = fderiv ℝ (∑ j ∈ (Finset.univ : Finset ι),
                (fun z : ChartPoint ι => c.g z i j * Y z i * Z z j)) x (X x) := by
              rw [hbridge]
        _ = ∑ j : ι, fderiv ℝ (fun z : ChartPoint ι => c.g z i j * Y z i * Z z j) x (X x) := by
              rw [fderiv_sum (fun j hj => ((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j))]
        _ = ∑ j : ι, (c.g x i j * fderiv ℝ (fun z : ChartPoint ι => Y z i) x (X x) * Z x j
            + c.g x i j * Y x i * fderiv ℝ (fun z : ChartPoint ι => Z z j) x (X x)
            + fderiv ℝ (fun z : ChartPoint ι => c.g z i j) x (X x) * Y x i * Z x j) := by
              apply Finset.sum_congr rfl
              intro j hj
              rw [fderiv_mul_mul_apply (f := fun z : ChartPoint ι => c.g z i j)
                (g := fun z : ChartPoint ι => Y z i) (h := fun z : ChartPoint ι => Z z j)
                (hf := hdiff_g i j) (hg := hdiff_Y i) (hh := hdiff_Z j) (v := X x)]
              ring
```

This is cleaner. But it's inside the big calc at 488-510 — I'd restructure that whole step. Actually simpler: keep the structure but replace the failing bullet block. The whole block 490-510:

```lean
            simp
            apply Finset.sum_congr rfl
            intro i hi
            have hbridge : ... := by congr 1; funext z; simp
            rw [hbridge]
            rw [fderiv_sum (fun j hj => ((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j))]
            simp
            apply Finset.sum_congr rfl
            intro j hj
            rw [fderiv_mul_mul_apply ...]
            ring
```

Hmm — but after `rw [fderiv_sum ...]` the goal: `(∑ j ∈ univ, fderiv ℝ (A j) x) (X x) = ∑ j : ι, (...)`. Then `simp` pushes the application inside the sum → `∑ j ∈ univ, fderiv ℝ (A j) x (X x) = ∑ j : ι, (...)`. Then sum_congr + per-j + fderiv_mul_mul_apply + ring. This matches the original second bullet — good.

But WAIT — why did the original fail at `intro j hj`?? Let me think ONE more time... 

OH!!! I think I finally see it. `rw [hbridge]` — hbridge's LHS is `fderiv ℝ (fun z : ChartPoint ι => ∑ j : ι, c.g z i j * Y z i * Z z j) x`. But the GOAL contains `fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x (X x)`. Hmm — the goal's fderiv might not be exactly that... wait — hmm, does the goal contain `(X x)` at this point? Let me recheck: the block starts with `simp` at line 491 (in the error message version, line 491). Wait no — looking at the file again, lines 488-510:

```
      _ = ∑ i : ι, ∑ j : ι, (c.g x i j * fderiv ℝ (fun z : ChartPoint ι => Y z i) x (X x) * Z x j
            + c.g x i j * Y x i * fderiv ℝ (fun z : ChartPoint ι => Z z j) x (X x)
            + fderiv ℝ (fun z : ChartPoint ι => c.g z i j) x (X x) * Y x i * Z x j) := by
            simp
            apply Finset.sum_congr rfl
            intro i hi
            have hbridge : fderiv ℝ (fun z : ChartPoint ι => ∑ j : ι, c.g z i j * Y z i * Z z j) x
                = fderiv ℝ (∑ j ∈ (Finset.univ : Finset ι),
                    (fun z : ChartPoint ι => c.g z i j * Y z i * Z z j)) x := by
              congr 1
              funext z
              simp
            rw [hbridge]
            rw [fderiv_sum]
            · intro j hj
              exact (hdiff_g i j).mul ((hdiff_Y i).mul (hdiff_Z j))
            · simp
              apply Finset.sum_congr rfl
              intro j hj
              rw [fderiv_mul_mul_apply ...]
              ring
```

The goal entering: `(∑ i ∈ univ, fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x) (X x) = ∑ i : ι, ∑ j : ι, (...)`. `simp` → `∑ i ∈ univ, fderiv ℝ (fun z => ∑ j : ι, ...) x (X x) = ∑ i : ι, ∑ j : ι, (...)`. sum_congr → per i: `fderiv ℝ (fun z => ∑ j : ι, ...) x (X x) = ∑ j : ι, (...)`.

`rw [hbridge]` — matches LHS `fderiv ℝ (fun z => ∑ j : ι, ...) x` — the subterm in the goal IS `fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x (X x)` — hmm, is the subterm `fderiv ℝ (fun z => ∑ j : ι, ...) x` present? The goal term is `fderiv ℝ (fun z => ∑ j : ι, ...) x (X x)` = `((fderiv ℝ (fun z => ∑ j : ι, ...) x) (X x))` — so yes, the subterm `fderiv ℝ (fun z => ∑ j : ι, ...) x` is present. rw [hbridge] rewrites it → `fderiv ℝ (∑ j ∈ univ, (fun z => ...)) x (X x) = ∑ j : ι, (...)`. ONE rewrite, no side goals.

`rw [fderiv_sum]` — rewrites `fderiv ℝ (∑ j ∈ univ, A j) x` → `∑ j ∈ univ, fderiv ℝ (A j) x`, side goal `∀ j ∈ univ, DifferentiableAt ℝ (A j) x`. Goals after: side goal first (I believe), then main. `· intro j hj` — should work!

Hmm — UNLESS the side-goal ordering in rw is: NEW goals are added AFTER the main goal. Let me check Lean 4's rw behavior... In Lean 4, `rw` side goals: the tactic framework appends new goals and the main goal — I'm now fairly sure rw puts side goals first (I've written `rw [h]; · intro ...; · ...` many times... hmm, have I? I recall `rw [foo]; exact ...; rfl` patterns where the hypothesis goal comes first).

Honestly, maybe the ordering changed in recent Lean or rw with `only`. Whatever — my replacement `rw [fderiv_sum (fun j hj => ...)]` avoids bullets entirely (the side goal is discharged inline), then `simp` etc. That's robust regardless of ordering. Let me go with that.

Also `· simp` at 504 — if side-goal-first, `simp` would hit the diff-goal — but we've discharged inline now.

**Fix 10** — the `simpa using h` at line 303 (christoffelFamily_metricDerivative) — no error reported. Fine.

Now — one more consideration: line 382's `simp only [nabla, nablaOf_apply, fderiv_const, christoffelFamily]` — replace `fderiv_const` with `fderiv_const_apply` and add `zero_add`.

And at line 396/415, `Pi.smul_apply` — doesn't exist. Replace per plan.

Let me also double check `nabla_smul`'s later part (line 423-426):
```lean
  rw [hmulv, hΓsmul]
  trace_state  -- remove
  simp only [Pi.add_apply, Pi.smul_apply, smul_eq_mul]
  ring
```
Goal after ext k and unfold nabla: `fderiv ℝ (fun z => (f • Y) z k) x (X x) + ∑ i, ∑ j, Γ * X x i * (f • Y) x j = (f x • c.nabla X Y x + fderiv ℝ f x (X x) • Y x) k`. After `rw [hmulv, hΓsmul]`: LHS = `f x * fderiv (Y k) (X x) + Y x k * fderiv f x (X x) + f x * (∑ i, ∑ j, Γ * X x i * Y x j)`. RHS: `(f x • c.nabla X Y x + fderiv ℝ f x (X x) • Y x) k` — with `nabla` still unfolded in RHS: `(f x • (fun x k => fderiv ... + ∑...) + ...) k` — hmm, RHS contains `c.nabla X Y x` — after `unfold nabla` at line 393, ALL occurrences of `nabla` unfold? `unfold nabla` unfolds the RHS `c.nabla X Y x` too. So RHS = `(f x • (fun x k => fderiv ℝ (fun z => Y z k) x (X x) + ∑ i, ∑ j, Γ x k i j * X x i * Y x j) x + fderiv ℝ f x (X x) • Y x) k`. Hmm wait — `f x • (c.nabla X Y x)` where `c.nabla X Y x : ChartPoint ι` — Pi smul: `(f x • v) k = f x • v k`... The original final step: `simp only [Pi.add_apply, Pi.smul_apply, smul_eq_mul]` then `ring`. `Pi.add_apply` — exists? `Pi.add_apply : (f + g) x = f x + g x` — yes exists. `Pi.smul_apply` — does NOT exist in this revision. So replace with plain `simp` (which knows how to unfold Pi structures) then `ring`. Or `simp only [Pi.add_apply, smul_eq_mul]` + something for the Pi smul of the outer field. Let me think: RHS term: `((f x • (c.nabla X Y x)) + (fderiv ℝ f x (X x)) • Y x) k`. Need `(f x • v) k = f x * v k` — that's the same Pi smul pointwise equality as before — plain `simp` handles (test confirmed for the analogous `(f • Y) z k`). So final: `simp` then `ring`:
```lean
  rw [hmulv, hΓsmul]
  simp
  ring
```
Wait — careful: plain `simp` might over-normalize the LHS sums or mess with `∑`. LHS after rws: `f x * fderiv ... + Y x k * fderiv ... + f x * (∑ i, ∑ j, Γ * X x i * Y x j)`. RHS after `simp`: `f x * (fderiv ℝ (fun z => Y z k) x (X x) + ∑ i, ∑ j, Γ x k i j * X x i * Y x j) + fderiv ℝ f x (X x) * Y x k` — hmm, is the RHS's fderiv `fderiv ℝ (fun z => Y z k) x (X x)` — the `nabla`-unfolded RHS has `fderiv ℝ (fun z : ChartPoint ι => Y z k) x (X x)`. After simp: `f x * (fderiv ... (X x) + ∑...) + (fderiv f (X x)) * Y x k`. Then `ring` closes. Yes.

Hmm — but one concern: `simp` may also simplify `f x * (∑ i, ∑ j, ...)` by distributing?? simp doesn't distribute multiplication over sums automatically (mul_sum is not [simp] I think — it IS... hmm, `Finset.mul_sum` — is it tagged simp? `sum_mul`/`mul_sum` are NOT simp by default in recent mathlib I believe). If it did distribute, ring still closes. Fine.

Actually hold on — there's a subtlety about hΓsmul's RHS: `f x * (∑ i, ∑ j, Γ * X x i * Y x j)` — and the LHS after rw: `f x * fderiv (Y k) (X x) + Y x k * fderiv f x (X x) + f x * (∑...)`. The RHS target: `(f x • (nabla...x) + (fderiv ℝ f x (X x)) • Y x) k` — the `nabla X Y x k` term inside has `∑ i, ∑ j, Γ x k i j * X x i * Y x j` — matching. OK.

One more: the ext k at line 392 — `ext k` on Pi type — fine.

Now also fix the nabla_smul hdiff: `hf.contDiffAt.differentiableAt_one` — no error reported, fine.

Now the remaining errors after these fixes might surface (e.g., the unsolved-goal after the 478-487 block, if any). Let me apply the edits now.

Also — I noticed the file has `import Mathlib.LinearAlgebra.StdBasis` — probably used for `Pi.basisFun`. Fine.

Let me now make the edits.
dsh: reasoning:
Progress — down to 4 error clusters (plus warnings):

1. **Line 292** — `simpa [one_div]` didn't fix it: my scratch test had `have h' := ...; simpa [one_div] using h'` — that worked there, but here the target has `2⁻¹ * (a + b - c)` while term has `(1/2) • (a + (b - c))`. The associativity: `c.dFamily x i j l + c.dFamily x j i l - c.dFamily x l i j` vs `c.dFamily y i j l + (c.dFamily y j i l - c.dFamily y l i j)` — these are defeq? `a + b - c` parses as `(a + b) - c`, and `a + (b - c)` — NOT defeq (different association). In my scratch test the body was just `(1 : ℝ)` so no associativity issue. Hmm. So `simpa` can't close because the two function bodies differ by associativity. Options:
   - Restructure so the term's body matches the target exactly: use `ContDiff.add (ContDiff.add (dFamily_smooth i j l) (dFamily_smooth j i l)) (ContDiff.neg (dFamily_smooth l i j))`? That gives `fun y => (d y i j l + d y j i l) + -(d y l i j)` — hmm, and target `2⁻¹ * (a + b - c)` = `2⁻¹ * ((a+b) + -c)`... `(a + b) - c` = `(a+b) + -c`? Subtraction `a - b = a + -b` — so target body: `(d x i j l + d x j i l) + -d x l i j`? Hmm — `c.dFamily x i j l + c.dFamily x j i l - c.dFamily x l i j` — parses as `(c.dFamily x i j l + c.dFamily x j i l) - c.dFamily x l i j` — and `-` is `Sub.sub` — for ℝ, `a - b = a + -b` is a theorem (sub_eq_add_neg), but `Sub.sub` is a separate operation! For ℝ (a ring), `Sub.sub a b` — the instance is `sub_eq_add_neg` as definition? In mathlib, `Sub ℝ` instance: `instSubReal := ⟨fun a b => a + -b⟩`? For fields, `Sub` is defined via `addGroup`... In mathlib's hierarchy, `Sub.sub` for an `AddGroup` IS defined as `a + -b` (the `Sub` class instance `SubNegMonoid.toSub`? with `sub := fun a b => a + -b`). Actually in mathlib: `class Sub` with instance from `AddGroup`: `sub a b := a + -b` — definitional. So `a + b - c` = `(a+b) - c` = `(a+b) + -c` defeq. And `a + (b - c)` = `a + (b + -c)`. Not defeq.

   Cleanest fix: change `hΓ` to have the body match the const_smul construction: state hΓ with `((1 / 2 : ℝ) * (c.dFamily x i j l + (c.dFamily x j i l - c.dFamily x l i j)))`. Wait — hΓ's purpose: unfold christoffelFamily to the sum. The natural form from the def: `c.gInv x k l * (ChartMetricCoefficients.christoffelLower ...)`. Let me look at how christoffel unfolds. `ChartMetricCoefficients.christoffel k i j = ∑ l, gInv k l * christoffelLower l i j`? and `christoffelLower l i j = (1/2) * (d i j l + d j i l - d l i j)` or similar. The hΓ in the file: `∑ l, c.gInv x k l * ((1 / 2 : ℝ) * (c.dFamily x i j l + c.dFamily x j i l - c.dFamily x l i j))` — and its proof: `simp only [christoffelFamily, chartCoefficients, ChartMetricCoefficients.christoffel, ChartMetricCoefficients.christoffelLower]` — that proof worked (no error at 284-287). So the unfolded form is exactly that. To make the target body match the const_smul term, rewrite the target body's associativity: instead of trying to match `a + b - c` vs `a + (b - c)`, I can make the const_smul construction produce exactly `(a + b - c)`... `ContDiff.add (ContDiff.add A B) (ContDiff.neg C)` gives `fun y => (A y + B y) + -C y`? Hmm — `ContDiff.neg C : ContDiff ℝ ∞ (-C)` where `-C := fun y => -(C y)`. So `ContDiff.add (ContDiff.add A B) (ContDiff.neg C) : ContDiff ℝ ∞ ((A + B) + (-C))` = `fun y => (A y + B y) + -(C y)`. And target body `(a + b) + -c`? The target after hΓ: `2⁻¹ * (d x i j l + d x j i l - d x l i j)` — `a + b - c` = `(a+b) - c` = `(a+b) + -c` (defeq via Sub instance). So `ContDiff.add (ContDiff.add A B) (ContDiff.neg C)` yields `fun y => (A y + B y) + -C y` — matches `(a+b) + -c`! And `(1/2) • ((A y + B y) + -C y)` vs `2⁻¹ * ((a+b) + -c)` — smul_eq_mul + one_div. So:

   ```lean
   · exact ContDiff.const_smul (1 / 2 : ℝ)
       (ContDiff.add (ContDiff.add (c.dFamily_smooth i j l) (c.dFamily_smooth j i l))
         (ContDiff.neg (c.dFamily_smooth l i j)))
   ```
   and keep `simpa [one_div]`.

   Hmm wait — `ContDiff.neg` — name check: `ContDiff.neg : ContDiff 𝕜 n f → ContDiff 𝕜 n (fun x => -f x)`. I'll verify by compile.

2. **Line 382** — `0 X0 + sums = sums` remains; `zero_add` reported unused?? Interesting: `0 X0` — the goal shows `0 X0 + ∑... = ∑...`. The `zero_add` arg unused means the goal's `0 X0 + ...` — hmm, `0 X0` where `0 : ChartPoint ι →L[ℝ] ℝ` — wait the goal displays `0 X0` — and `zero_add` would rewrite `0 + a = a` — the goal HAS `0 X0 + ∑...` — so why is zero_add unused?! Unless `0 X0` didn't reduce and the goal is `(0 X0) + ...` — `zero_add`'s LHS is `0 + a` — the goal's LHS is `0 X0 + ∑` — pattern `0 + ?a` vs `0 X0 + ?a` — `0` alone must match `0 X0` — no! `zero_add` : `0 + a = a` where `0` is the zero of the additive monoid — the goal's `0 X0` is the zero MAP applied, not the zero scalar. So need `0 X0 = 0` first. As analyzed, `0 X0` should reduce by whnf... but the goal shows `0 X0` unreduced. simp's only-set doesn't whnf? simp does not fully whnf subterms before matching patterns in `simp only` mode? Hmm. `ContinuousLinearMap.zero_apply : (0 : E →L[𝕜] F) x = 0` — is it [simp]-tagged? If so, `simp only [ContinuousLinearMap.zero_apply, zero_add]`... but zero_apply might not be [simp]... it IS tagged simp in mathlib (`@[simp] theorem zero_apply`). Let me add `ContinuousLinearMap.zero_apply` to the only list, or simply change the proof:
   ```lean
   simp only [nabla, nablaOf_apply, fderiv_const_apply, christoffelFamily]
   simp
   ```
   The second plain `simp` closes `0 X0 + ... = ...` — hmm wait, but first line leaves goal `0 X0 + ∑... = ∑...`? The error shows fderiv got rewritten to `0` (goal shows `0 X0 + ∑`), so `fderiv_const_apply` FIRED. Then plain `simp` should handle `0 X0 → 0` and `0 + a → a` and close. Let me do:
   ```lean
   simp only [nabla, nablaOf_apply, fderiv_const_apply, christoffelFamily]
   simp
   ```
   Wait — but will plain simp after that close `0 X0 + A = A`? `0 X0` — simp whnf-reduces the goal term first: `(0 : ChartPoint ι →L[ℝ] ℝ) X0` — does simp's whnf reduce this to `0`? ContinuousLinearMap `0` is `⟨(0 : E →ₗ[𝕜] F), ...⟩` — wait, actually is `instZeroContinuousLinearMap` defined as a structure literal? `instance : Zero (E →SL[σ] F) := ⟨⟨0, map_zero' ...⟩⟩`-ish. Application: `⟨f, c⟩ x = f x` — defeq. And `(0 : E →ₗ[𝕜] F) x` — LinearMap zero — `instance : Zero (M →ₗ[R] M₂) := ⟨⟨fun _ => 0, ...⟩⟩`? If LinearMap's zero is also a structure literal, `(0 : E →ₗ F) x` reduces to `0`. In older mathlib, `LinearMap.instZero` := `⟨0, ...⟩` where the underlying function is `fun _ => 0`? It's `{ toFun := fun _ => 0, ... }`? Then application reduces. But actually — `ContinuousLinearMap.zero_apply` being a *theorem* (not rfl) suggests... hmm, in recent mathlib, `ContinuousLinearMap.instZero` is defined as `⟨(0 : E →ₗ[𝕜] F), (continuous_const)⟩`? If it's a structure literal, zero_apply would be rfl. The fact it's a theorem suggests the instance might be defined via `toLinearMap := 0` — still a structure literal though — `⟨0, proof⟩` — application `⟨0, p⟩ x` = `0 x` — reduces. Hmm.

   Simplest robust: 
   ```lean
   simp only [nabla, nablaOf_apply, fderiv_const_apply, christoffelFamily]
   simp only [ContinuousLinearMap.zero_apply, zero_add]
   ```
   Let me check if ContinuousLinearMap.zero_apply exists... I'll test. Or even simpler:
   ```lean
   have hz : (fderiv ℝ (fun z : ChartPoint ι => Y0 k) x) X0 = 0 := by
     simpa using congrFun (fderiv_const_apply (c := (Y0 k : ℝ)) (x := x)) X0
   ...
   ```
   Actually simplest: do the whole thing with a direct `simp` chain. Let me write:
   ```lean
   simp only [nabla, nablaOf_apply, fderiv_const_apply, christoffelFamily]
   rw [ContinuousLinearMap.zero_apply]
   ```
   then `zero_add` — the goal after rw: `0 + ∑ = ∑` — hmm `zero_add` — `rw [zero_add]`? Or just `simpa`. Let me try `simp only [ContinuousLinearMap.zero_apply]` first and see. Actually, `(0 : E →L[𝕜] F) X0 = 0` — hmm, in this mathlib revision, is `0 X0` defeq `0`? The goal after fderiv_const_apply displayed as `0 X0 + ∑... = ∑...` — if `0 X0` were defeq 0, the display would show `0 + ∑...`. The elaborator doesn't reduce for display... display shows the un-reduced term? Pretty printer shows the whnf? The pretty printer shows terms with some reduction (it shows `0 X0` — could be the unreduced form). Let me just test in scratch.

3. **Line 499** — `rw [fderiv_sum (fun j hj => ...)]` failed: pattern shows rw was looking for `fderiv ℝ (∑ i_1 ∈ ?m.864, ((fun z => c.g z i i_1) * fun z => Y z i) * fun z => Z z i_1) x` in target `(fderiv ℝ (∑ j, fun z => c.g z i j * Y z i * Z z j) x) (X x) = ...`. AH — I see: the target's sum is `∑ j, fun z => ...` — the LHS summand is `fun z => c.g z i j * Y z i * Z z j` (a lambda wrapping a product), whereas the `DifferentiableAt.mul`-composed term in my hypothesis-proof is `((fun z => c.g z i j) * fun z => Y z i) * fun z => Z z j` — a product of lambdas. These are NOT syntactically equal (rw requires the exact subterm match modulo unification of metavars; `fun z => c.g z i j * Y z i * Z z j` vs `((fun z => c.g z i j) * fun z => Y z i) * fun z => Z z j` — rw doesn't do eta/pointwise-expansion). So passing the hypothesis inline fails to unify.

   Original approach was right (rw then side goal with the summand as-is `fun z => c.g z i j * Y z i * Z z j`). The `.mul`-composed term has type `DifferentiableAt ℝ (((fun z => c.g z i j) * fun z => Y z i) * fun z => Z z j) x` and the goal needs `DifferentiableAt ℝ (fun z => c.g z i j * Y z i * Z z j) x`. These are defeq propositions?? `DifferentiableAt ℝ f x` where f = `((fun z => c.g z i j) * fun z => Y z i) * fun z => Z z j` — is f defeq `fun z => c.g z i j * Y z i * Z z j`? f = `(fun z => (fun z => c.g z i j) z * (fun z => Y z i) z) ...` hmm — `(A * B) z` where `A * B` is `Pi.mul` = pointwise: `(A * B) z = A z * B z` — the Pi mul instance: `instMulPi := ⟨fun f g i => f i * g i⟩` — so `(A * B) z` reduces to `A z * B z` = `c.g z i j * Y z i`. And `((A * B) * C) z` reduces to `(A z * B z) * C z` = `(c.g z i j * Y z i) * Z z j` — and the target lambda body `c.g z i j * Y z i * Z z j` = `(c.g z i j * Y z i) * Z z j` — SAME. So the terms ARE defeq! Then why did the original `.mul` version fail with a *type mismatch* at 487 (not a "failed to unify"?). The error at 487 was "Type mismatch" — because it was `exact` — hmm, `exact` requires... defeq should pass for exact. Unless the defeq fails somewhere. Hmm — the original 487 error:

   ```
   DifferentiableAt.mul (hdiff_g i j) (DifferentiableAt.mul (hdiff_Y i) (hdiff_Z j))
   has type
     DifferentiableAt ℝ ((fun z => c.g z i j) * ((fun z => Y z i) * fun z => Z z j)) x
   but is expected to have type
     DifferentiableAt ℝ (fun z => c.g z i j * Y z i * Z z j) x
   ```
   `(fun z => c.g z i j) * ((fun z => Y z i) * fun z => Z z j)` = `fun z => c.g z i j * (Y z i * Z z j)` — associativity: `a * (b * c)` vs `(a*b)*c` — NOT defeq. That's why. With my fix `((A).mul B).mul C` = `((fun z => c.g z i j) * fun z => Y z i) * fun z => Z z j` — hmm — `(A * B) * C` — this reduces pointwise to `fun z => (A z * B z) * C z` — defeq to target `fun z => c.g z i j * Y z i * Z z j` = `fun z => (c.g z i j * Y z i) * Z z j`. DEFEQ. But then why did rw at 499 fail to find the pattern?! The rw pattern shown: `fderiv ℝ (∑ i_1 ∈ ?m.864, ((fun z => c.g z i i_1) * fun z => Y z i) * fun z => Z z i_1) x` — rw elaborated `fderiv_sum (fun j hj => ((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j))` — the term's type unifies `A j` with `((fun z => c.g z i j) * fun z => Y z i) * fun z => Z z j` — then rw looks for the LHS `fderiv ℝ (∑ j ∈ univ, ((fun z => c.g z i j) * fun z => Y z i) * fun z => Z z j) x` in the target which has `fderiv ℝ (∑ j, fun z => c.g z i j * Y z i * Z z j) x`. rw does NOT do definitional matching (it uses syntactic matching with unification, and in newer Lean `rw` uses `withReducible`? rw uses semi-reducible matching? rw's matching does NOT unfold definitions). Since `fun z => ...` vs `(...) * (...)` are not syntactically identical (the Pi mul is a definitional wrapper), rw fails.

   So the inline-hypothesis approach fails due to rw's syntactic matching. Back to bullets: `rw [fderiv_sum]` then `· intro j hj` — and THAT had the mysterious "no binders" error. Hmm.

   Now I understand the binders mystery maybe: with `rw [fderiv_sum]`, side goal = `∀ j, j ∈ univ → DifferentiableAt ℝ (fun z => c.g z i j * Y z i * Z z j) x`. First goal after rw... let me just TEST this exact pattern in a scratch file to see the goal ordering. Also test whether `rw [hbridge]`-style chains work. Let me write a minimal repro.

4. **Line 551** — `rw [← Finset.sum_mul]` failed: target `∑ j_1, c.dFamily x j i j_1 * X x j * Y x i * Z x j_1 = (∑ k, X x k * c.dFamily x k i j) * Y x i * Z x j`. The `← Finset.sum_mul` pattern: `∑ i ∈ s, f i * a` — target LHS is `∑ j_1, c.dFamily x j i j_1 * X x j * Y x i * Z x j_1` — summand is `c.dFamily x j i j_1 * X x j * Y x i * Z x j_1` — a product of 4 — rw's pattern `f i * a` needs the summand to be *syntactically* `?f ?i * ?a` — the summand parses as `((d * X) * Y) * Z` — left-associated — the pattern `f i * a` matches `(d * X) * (Y * Z)`? The top node of the summand: `(d * X * Y) * Z` — is `Mul.mul ((d * X) * Y) Z` — pattern `?f ?i * ?a` = `Mul.mul (?f ?i) ?a` — matches with `?f ?i := (d*X)*Y` — but `?f ?i` requires the left side to be an *application* `?f ?i` — `(d*X)*Y` is a Mul node, not an application. No match. Hence failure.

   This is the `_ = ∑ i, ∑ j, (∑ k, X x k * d x k i j) * Y x i * Z x j` step (line 549-555). The goal per (i,j): `∑ k, d x k i j * X x k * Y x i * Z x j = (∑ k, X x k * d x k i j) * Y x i * Z x j`. Original code:
   ```
       _ = ∑ i : ι, ∑ j : ι, (∑ k : ι, X x k * c.dFamily x k i j) * Y x i * Z x j := by
             rw [Finset.sum_comm]
             apply Finset.sum_congr rfl
             intro i hi
             apply Finset.sum_congr rfl
             intro j hj
             rw [← Finset.sum_mul]
   ```
   Goal after the two sum_congr: `∑ k, c.dFamily x k i j * X x k * Y x i * Z x j = (∑ k, X x k * c.dFamily x k i j) * Y x i * Z x j`. Fix: rewrite the summand to `X x k * d x k i j * (Y x i * Z x j)`-ish form to match `f k * a`:
   ```lean
             intro j hj
             rw [← Finset.sum_mul]
             ring
   ```
   no wait. Let me handle per-k: `simp only [Finset.sum_mul]`? Use:
   ```lean
             apply Finset.sum_congr rfl  -- hmm no, we need the sum to distribute
   ```
   The cleanest: 
   ```lean
   _ = ∑ i : ι, ∑ j : ι, (∑ k : ι, X x k * c.dFamily x k i j) * Y x i * Z x j := by
     ...
     intro j hj
     rw [← Finset.sum_mul]  -- fails
   ```
   Fix by making the summand syntactically `(?f k) * ?a`:
   ```lean
     intro j hj
     have hm : (∑ k : ι, c.dFamily x k i j * X x k * Y x i * Z x j)
         = (∑ k : ι, (X x k * c.dFamily x k i j) * (Y x i * Z x j)) := by
       apply Finset.sum_congr rfl
       intro k hk
       ring
     rw [hm]
     rw [← Finset.sum_mul]
     ring
   ```
   After `rw [hm]`: goal `∑ k, (X x k * d x k i j) * (Y x i * Z x j) = (∑ k, X x k * d x k i j) * Y x i * Z x j`. `rw [← Finset.sum_mul]`: `sum_mul : (∑ x ∈ s, f x) * a = ∑ x ∈ s, f x * a` — rewriting RHS to `∑ k, (X x k * d x k i j) * (Y x i * Z x j)` — matches with f k = `X x k * d x k i j`, a = `Y x i * Z x j`. Then goal `∑ k, (X*k...) * (Y*Z) = ∑ k, (X*k...) * (Y*Z)` — closed by rw itself (rfl). So:
   ```lean
     intro j hj
     have hm : (∑ k : ι, c.dFamily x k i j * X x k * Y x i * Z x j)
         = (∑ k : ι, (X x k * c.dFamily x k i j) * (Y x i * Z x j)) := by
       apply Finset.sum_congr rfl
       intro k hk
       ring
     rw [hm, ← Finset.sum_mul]
   ```

5. **Line 586** — `rw [← hGamma]` failed: target has `(∑ i, ∑ j, (c.g x i j * ∑ a, ∑ b, Γ...i a b... * X x a * Y x b) * Z x j + ∑ i, ∑ j, c.g x i j * Y x i * ∑ a, ∑ b, Γ x j a b ... * X x a * Z x b)` while hGamma's LHS is `∑ i, ∑ j, c.g x i j * (∑ a, ∑ b, Γ x i a b * X x a * Y x b) * Z x j + (∑ i, ∑ j, c.g x i j * Y x i * (∑ a, ∑ b, Γ x j a b * X x a * Z x b))`. The target's first sum has `(c.g x i j * ∑ a, ∑ b, Γ ... * X x a * Y x b) * Z x j` — with the parens around `g * sums`! hGamma's LHS: `c.g x i j * (∑ a, ∑ b, Γ * X x a * Y x b) * Z x j` — `g * sums * Z` = `(g * sums) * Z` — hmm, actually `c.g x i j * (∑ a, ∑ b, ...) * Z x j` parses as `(c.g x i j * (∑ a, ∑ b, ...)) * Z x j`. And the target: `(c.g x i j * ∑ a, ∑ b, c.christoffelFamily x i a b * X x a * Y x b) * Z x j` — `∑ a, ∑ b, Γ...` vs `(∑ a, ∑ b, Γ...)` — syntactically the same? `c.g x i j * ∑ a, ∑ b, f a b` — bigOperator parsing: `∑ a, ∑ b, f a b` — hmm the target shows `c.g x i j * ∑ a, ∑ b, c.christoffelFamily x i a b * X x a * Y x b` — without parens around the double sum; hGamma shows `c.g x i j * (∑ a : ι, ∑ b : ι, ...) * Z x j`. The parse `c.g x i j * ∑ a, ∑ b, ...` — `∑ a, ∑ b, t` is `∑ a, (∑ b, t)` as one term — `g * (∑ a, ∑ b, t)` — precedence of `*` vs `∑` — `∑` binds... In Lean, the `∑ a,` binder has very low precedence? `c.g x i j * ∑ a, ∑ b, t` parses as `c.g x i j * (∑ a, ∑ b, t)`? Or `(c.g x i j * ∑ a, ∑ b) * t`?? The display shows `c.g x i j * ∑ a, ∑ b, c.christoffelFamily x i a b * X x a * Y x b` — if it parsed as `g * ((∑ a, ∑ b, Γ * X) * Y)`... hmm hard to tell. The mismatch: hGamma has explicit parens `c.g x i j * (∑ a, ∑ b, Γ x i a b * X x a * Y x b) * Z x j` and target `(c.g x i j * ∑ a, ∑ b, Γ x i a b * X x a * Y x b) * Z x j` — these differ in where the `* Z x j` associates: hGamma: `(g * S) * Z`, target: `(g * S) * Z` — same! Unless the sums differ: hGamma's S = `(∑ a, ∑ b, Γ x i a b * X x a * Y x b)` vs target's S = `∑ a, ∑ b, Γ x i a b * X x a * Y x b` — same. Hmm so why did rw fail? "Did not find an occurrence of the pattern dFormOf (c.chartCoefficients x) (X x) (Y x) (Z x)" — the rw was `rw [← hGamma]` where hGamma: LHS = dFormOf. `← hGamma` rewrites `dFormOf ...` → LHS-expression. The target (shown) doesn't contain `dFormOf` — it contains the christoffel sums. But `rw [← hGamma]` at 590 — the goal at that point (line 586-591):
   ```
   rw [hRsplit]
   rw [show (...) + (...) = (...) + (...) + (...) from by ring]
   rw [← hGamma]
   rw [hdForm]
   ```
   After the ring-rewrite, the goal: `(∑ g*dfirst... + ∑ g*second...) + ((∑ g * (∑a∑b Γ*X*Y) * Z) + (∑ g * Y * (∑a∑b Γ x j a b * X * Z))) = ∑ g * dfirst + ∑ g * second + ∑ fderiv-g * Y * Z`. Hmm wait — the target shown in the error:
   ```
   ∑ i, ∑ j, c.g x i j * (fderiv ℝ (fun z => Y z i) x) (X x) * Z x j +
         ∑ i, ∑ j, c.g x i j * Y x i * (fderiv ℝ (fun z => Z z j) x) (X x) +
       (∑ i, ∑ j, (c.g x i j * ∑ a, ∑ b, c.christoffelFamily x i a b * X x a * Y x b) * Z x j +
         ∑ i, ∑ j, c.g x i j * Y x i * ∑ a, ∑ b, c.christoffelFamily x j a b * X x a * Z x b)
     = ∑ i, ∑ j, c.g x i j * ... + ...
   ```
   The christoffel part: `(∑ i, ∑ j, (c.g x i j * ∑ a, ∑ b, Γ x i a b * X x a * Y x b) * Z x j + ∑ i, ∑ j, c.g x i j * Y x i * ∑ a, ∑ b, Γ x j a b * X x a * Z x b)` — hGamma's LHS:
   ```
   (∑ i : ι, ∑ j : ι, c.g x i j * (∑ a : ι, ∑ b : ι, Γ x i a b * X x a * Y x b) * Z x j)
     + (∑ i : ι, ∑ j : ι, c.g x i j * Y x i * (∑ a : ι, ∑ b : ι, Γ x j a b * X x a * Z x b))
   ```
   Differences: (1) parens around sums; (2) `c.g x i j * Y x i * ∑ a, ∑ b, ...` vs `c.g x i j * Y x i * (∑ a, ∑ b, ...)` — the target's is `g * Y * ∑a∑b...` = `(g*Y) * (∑...)`? And hGamma's is `g * Y * (∑...)` = `(g*Y) * (∑...)` — same association actually. The subtle one: `c.g x i j * ∑ a, ∑ b, ...` — the `∑` binder's parse — in hGamma with explicit parens, it's `g * (S)`. In the target, the display `c.g x i j * ∑ a, ∑ b, Γ x i a b * X x a * Y x b` — if `∑` binds LOOSER than `*`, this is `(c.g x i j * ∑ a, ∑ b, Γ x i a b * X x a) * Y x b` — no wait, that's ill-scoped (b free). `∑ a, ∑ b, t` — the `t` extends as far right as possible: `t` = `c.g x i j * ∑ a, ∑ b, Γ x i a b * X x a * Y x b`?? Hmm — big operator notation `∑ a, body` — body extends maximally. In `(c.g x i j * ∑ a, ∑ b, c.christoffelFamily x i a b * X x a * Y x b) * Z x j` — the inner term is `c.g x i j * (∑ a, (∑ b, Γ x i a b * X x a * Y x b))` — the body of the sum binders extends to the `)` before `* Z x j`. So it equals hGamma's `c.g x i j * (∑ a, ∑ b, Γ x i a b * X x a * Y x b)` — SAME. Hmm. So why the rw failure?

   Wait, actually re-read the error: the rw at 586 was `rw [← hGamma]`? Let me re-check what line 586 is now. After my edits the line numbers shifted. The error at "586:8: error: Tactic `rewrite` failed: Did not find an occurrence of the pattern dFormOf..." — hmm, the pattern it searched for: `dFormOf (c.chartCoefficients x) (X x) (Y x) (Z x)`. So the rw at that line was `rw [← hGamma]` — searching for dFormOf — not found in target. The target shown contains the christoffel sums — so the earlier `rw [show ... from by ring]` (line 581-589) FAILED to produce the expected form? No — that rw succeeded (it's before), and the resulting goal is the shown target. The shown target's christoffel part: `(∑ i, ∑ j, (c.g x i j * ∑ a, ∑ b, Γ x i a b * X x a * Y x b) * Z x j + ∑ i, ∑ j, c.g x i j * Y x i * ∑ a, ∑ b, Γ x j a b * X x a * Z x b)`.

   Hmm — compare with hGamma's LHS (which contains explicit `(...)`): the target term `(c.g x i j * ∑ a, ∑ b, Γ x i a b * X x a * Y x b) * Z x j`. Hmm — here's the thing: `c.g x i j * ∑ a, ∑ b, t` — actually, in Lean 4, `∑ a, ∑ b, t` — does the second binder's body extend over the `* X x a * Y x b`? Yes. So it's `g * (∑ a, ∑ b, (Γ * X x a) * Y x b)`. And hGamma: `c.g x i j * (∑ a : ι, ∑ b : ι, c.christoffelFamily x i a b * X x a * Y x b) * Z x j`. Wait — hGamma's LHS has the sums INSIDE parens followed by `* Z x j` — so hGamma LHS first summand = `(c.g x i j * (∑ a, ∑ b, Γ x i a b * X x a * Y x b)) * Z x j`. Target: `(c.g x i j * ∑ a, ∑ b, Γ x i a b * X x a * Y x b) * Z x j` — the same. Hmm hmm. So rw SHOULD find it...

   Unless — the second summand: target `c.g x i j * Y x i * ∑ a, ∑ b, c.christoffelFamily x j a b * X x a * Z x b` — hmm — `∑ a, ∑ b, Γ x j a b * X x a * Z x b` — wait, does the body of the outer `∑ a` here extend to include `* X x a * Z x b`?? The body of `∑ a` extends maximally to the right: `∑ a, (∑ b, Γ x j a b * X x a * Z x b)` — yes. So target second summand = `(g * Y) * (∑ a, ∑ b, Γ x j a b * X x a * Z x b)`? And hGamma's second: `c.g x i j * Y x i * (∑ a : ι, ∑ b : ι, c.christoffelFamily x j a b * X x a * Z x b)` = `(g * Y) * (∑...)`. SAME.

   So both summands match. Then the whole christoffel part should match hGamma's LHS. Unless the FIRST sum: `∑ i, ∑ j, (c.g x i j * ∑ a, ∑ b, ...) * Z x j` — hmm wait, in the TARGET display: `(∑ i, ∑ j, (c.g x i j * ∑ a, ∑ b, c.christoffelFamily x i a b * X x a * Y x b) * Z x j + ∑ i, ∑ j, c.g x i j * Y x i * ∑ a, ∑ b, ...)` — the paren structure: `(∑ i, ∑ j, ((c.g x i j * ∑ a, ∑ b, Γ x i a b * X x a * Y x b) * Z x j) + (∑ i, ∑ j, ...))`. Hmm — in hGamma, LHS = `(∑ i, ∑ j, c.g x i j * (∑ a, ∑ b, Γ x i a b * X x a * Y x b) * Z x j) + (∑ i, ∑ j, c.g x i j * Y x i * (∑ a, ∑ b, Γ x j a b * X x a * Z x b))`. First summand: `c.g x i j * (∑ a, ∑ b, ...) * Z x j` — here `* Z x j` — the sum body is `(∑ a, ∑ b, Γ x i a b * X x a * Y x b)` — the parens cut the body. Then `* Z x j`. So it's `(g * S) * Z`. Target: `(c.g x i j * ∑ a, ∑ b, Γ x i a b * X x a * Y x b) * Z x j` — the parens in target start before `c.g` and end after `Y x b` — so target's body = `c.g x i j * ∑ a, ∑ b, (Γ x i a b * X x a * Y x b)` — wait — the paren is `(c.g x i j * ∑ a, ∑ b, c.christoffelFamily x i a b * X x a * Y x b)` — inside: `c.g x i j * ∑ a, (∑ b, Γ x i a b * X x a * Y x b)` — the outer `∑ a`'s body extends to the closing paren: body = `∑ b, Γ x i a b * X x a * Y x b` — so inside = `g * (∑ a, ∑ b, Γ x i a b * X x a * Y x b)`. Then `* Z x j`: `(g * S) * Z`. IDENTICAL to hGamma. 

   So why did `rw [← hGamma]` fail?? Hmm — the error message says "Did not find an occurrence of the pattern dFormOf (c.chartCoefficients x) (X x) (Y x) (Z x)" — maybe the rw was actually `rw [← hGamma]` where hGamma — hmm wait, `← hGamma` — the LHS of `hGamma`'s conclusion is the dFormOf expression? hGamma: `(∑ i, ∑ j, c.g x i j * (∑ a, ∑ b, Γ x i a b * X x a * Y x b) * Z x j) + (∑ i, ∑ j, c.g x i j * Y x i * (∑ a, ∑ b, Γ x j a b * X x a * Z x b)) = dFormOf (c.chartCoefficients x) (X x) (Y x) (Z x)`. So `← hGamma` rewrites dFormOf → the sum expression. The failure says dFormOf not found in target — the target is the christoffel-summed expression — consistent: after the previous ring-rw, the goal no longer has dFormOf. So the previous `rw [show ... from by ring]` at 581-589 rewrote MORE than intended? The target displayed has the form `first + second + (gammaSum1 + gammaSum2) = first + second + dgSum` — hmm wait, it shows:

   ```
   ∑ i, ∑ j, c.g x i j * (fderiv ℝ (fun z => Y z i) x) (X x) * Z x j +
         ∑ i, ∑ j, c.g x i j * Y x i * (fderiv ℝ (fun z => Z z j) x) (X x) +
       (∑ i, ∑ j, (c.g x i j * ∑ a, ∑ b, c.christoffelFamily x i a b * X x a * Y x b) * Z x j +
         ∑ i, ∑ j, c.g x i j * Y x i * ∑ a, ∑ b, c.christoffelFamily x j a b * X x a * Z x b)
     = ∑ i, ∑ j, c.g x i j * (fderiv ℝ (fun z => Y z i) x) (X x) * Z x j +
         ∑ i, ∑ j, c.g x i j * Y x i * (fderiv ℝ (fun z => Z z j) x) (X x) +
       ∑ i, ∑ j, (fderiv ℝ (fun z => c.g z i j) x) (X x) * Y x i * Z x j
   ```
   So the goal is `first + second + (gamma1 + gamma2) = first + second + dg` — and `rw [← hGamma]` wants to rewrite `dFormOf` — not present; the correct rewrite here is `rw [← hGamma]` on the LHS's (gamma1 + gamma2) part — hGamma: gammaSum = dFormOf — so `rw [hGamma]` (forward) rewrites gamma1+gamma2 → dFormOf! The original code had `rw [← hGamma]` — WRONG direction. Then `rw [hdForm]` — hdForm: `dFormOf ... = ∑ i, ∑ j, fderiv...` — forward — rewrites dFormOf → dgSum. So the fix: line 590: `rw [hGamma]` instead of `rw [← hGamma]`.

   Wait, but with `rw [hGamma]`: hGamma's LHS must match the goal's `(gamma1 + gamma2)` — per the analysis above, the expressions match syntactically? The display shows target gamma1 = `∑ i, ∑ j, (c.g x i j * ∑ a, ∑ b, Γ x i a b * X x a * Y x b) * Z x j` — hmm, `∑ a, ∑ b, Γ x i a b * X x a * Y x b` — is the sum-body parse identical to hGamma's `(∑ a, ∑ b, Γ x i a b * X x a * Y x b)`? In the display, target shows `c.g x i j * ∑ a, ∑ b, c.christoffelFamily x i a b * X x a * Y x b` inside parens — so its structure is `c.g x i j * (∑ a, (∑ b, (Γ x i a b * X x a) * Y x b))`? The body of `∑ b`: `Γ x i a b * X x a * Y x b` — parse: `(Γ x i a b * X x a) * Y x b`. hGamma's: `c.christoffelFamily x i a b * X x a * Y x b` — same parse. So gamma1 = `∑ i, (∑ j, ((c.g x i j * (∑ a, (∑ b, ((Γ x i a b * X x a) * Y x b)))) * Z x j))` — hmm wait — target's paren: `(c.g x i j * ∑ a, ∑ b, Γ x i a b * X x a * Y x b) * Z x j` — inside parens: `c.g x i j * ∑ a, (∑ b, (Γ x i a b * X x a) * Y x b)`. So gamma1-summand = `(g * (∑ a, (∑ b, ((Γ*X)*Y)))) * Z`. hGamma's summand: `c.g x i j * (∑ a, ∑ b, Γ x i a b * X x a * Y x b) * Z x j` = `(g * (∑ a, (∑ b, ((Γ*X)*Y)))) * Z`. SAME.

   Hmm — so then why would `rw [← hGamma]` (dFormOf pattern) fail... because the goal doesn't contain dFormOf — that's expected for the WRONG direction! Right — the fix is the direction: use `rw [hGamma]`. But wait — the original author had it the other way: maybe originally `hGamma` was stated in the opposite orientation... The code says:
   ```
   have hGamma : (∑ i ... Γ...) + (∑ i ... Γ...) = dFormOf (c.chartCoefficients x) (X x) (Y x) (Z x) := ...
   ```
   and later `rw [← hGamma]` — hmm, `← hGamma` : `dFormOf ... = (gamma1 + gamma2)` — the goal after ring-rw is `first + second + (gamma1 + gamma2) = first + second + dg`. The RHS `first + second + dg` — no dFormOf present (dg is the expanded fderiv sum from hdForm's RHS — but hdForm hasn't been applied yet!). So dFormOf appears NOWHERE. The correct step: `rw [hGamma]` (forward: gamma1+gamma2 → dFormOf) then `rw [hdForm]` (forward: dFormOf → dg). So the fix: `rw [hGamma]`.

   Hold on — but that's what the code intended... Let me re-read the original block:
   ```
   rw [hRsplit]
   rw [show ((...) + (...)) + ((...) + (...)) = (first) + (second) + ((gamma1) + (gamma2)) from by ring]
   rw [← hGamma]
   rw [hdForm]
   ```
   Yes — `rw [← hGamma]` is a direction error; should be `rw [hGamma]`. (The previous invocation apparently left this unverified.)

6. Line 292's fix (ContDiff.neg) and the `simpa [one_div]` — hmm wait, but the goal's body is `2⁻¹ * (d x i j l + d x j i l - d x l i j)` = `2⁻¹ * ((a + b) - c)` = `2⁻¹ * ((a+b) + -c)` — with const_smul + add(add, neg): term body `(1/2) • ((a + b) + -c)` — wait `ContDiff.add A B : ContDiff 𝕜 n (A + B)` = `fun y => A y + B y`; `ContDiff.neg C : ContDiff 𝕜 n (-C)` = `fun y => -C y`. So `ContDiff.add (ContDiff.add A B) (ContDiff.neg C) : ContDiff ℝ ∞ (fun y => (A y + B y) + -C y)`. Good. And target body `a + b - c` = `Sub.sub (a+b) c` — hmm — is `Sub.sub (a+b) c` defeq `(a+b) + -c`? For ℝ, Sub instance — in mathlib, `Sub` for `AddGroup` is `SubNegMonoid.toSub`? The class: `class Sub (α) where sub : α → α → α` with instance `SubNegMonoid.toSub`? Hmm, actually in recent mathlib: `instance : Sub M := ⟨fun a b => a + -b⟩`? There's a coercion — in Lean 4 mathlib, `Sub` class instances are provided via `SubNegMonoid.toSub` — and `sub := fun a b => a + -b`? Let me check: mathlib `Mathlib/Algebra/Group/Operations.lean`: `instance SubNegMonoid.toSub {M} [SubNegMonoid M] : Sub M := ⟨fun a b => a + -b⟩` — hmm, or via `sub_eq_add_neg` being rfl? In any case, `x - y` for a group reduces to `x + -y` by definition (I'm fairly confident — `sub_eq_add_neg` is rfl or at least the instance is that). So target `(a+b) - c` ≡ `(a+b) + -c` defeq. And simpa [one_div] aligns `(1/2) • t` with `2⁻¹ * t`. Should work.

Also — the `hΓ` statement itself has `((1 / 2 : ℝ) * (c.dFamily x i j l + c.dFamily x j i l - c.dFamily x l i j))` — its proof: `simp only [christoffelFamily, chartCoefficients, ChartMetricCoefficients.christoffel, ChartMetricCoefficients.christoffelLower]` — that worked before (unfolded to exactly this? whatever — it passed). But now the body form `(a + b - c)` — after my const_smul change, the term body is `(a+b) + -c` — need `simpa [one_div]` to handle `2⁻¹ * ((a+b) - c)` vs `(1/2) • ((a+b) + -c)` — simp normalizes `x - y` → `x + -y` (sub_eq_add_neg [simp]) and `1/2 → 2⁻¹`, `• → *`. OK.

Let me also double check `fderiv_const_apply`'s firing: the goal at 382 now shows `0 X0 + ∑... = ∑...` — so fderiv_const_apply fired and left `0 X0`. Fix: replace `zero_add` with something that reduces `0 X0`: add `ContinuousLinearMap.zero_apply`? Hmm — but the `0` there: `(0 : ChartPoint ι →L[ℝ] ℝ) X0`. Let me test whether `simp` closes `0 X0 + A = A` — quick scratch test.

Let me write these fixes now.
dsh: reasoning:
Interesting:

1. `0 X0 + A = A` by `simp` — PASSED (no error at line 16-18).

2. ContDiff test FAILED: after simplification, term has `a y + b y + -c y` but expected `a x + b x - c x` — so `x - y` is NOT normalized by simp to `x + -y`?? The expected shows `a x + b x - c x` — simp didn't rewrite `sub` — hmm, `sub_eq_add_neg` is not a simp lemma? Wait, actually simp usually keeps `a - b`... The difference: expected `a x + b x - c x` vs term `a y + b y + -c y`. Need explicit rewrite: `rw [sub_eq_add_neg]` in the target? Or state hΓ differently.

   Simplest: make the `hΓ` statement use the `+ -` form: change hΓ to:
   ```lean
   have hΓ : (fun x : ChartPoint ι => c.christoffelFamily x k i j) = fun x =>
       ∑ l : ι, c.gInv x k l * ((1 / 2 : ℝ) *
         (c.dFamily x i j l + (c.dFamily x j i l + -c.dFamily x l i j))) := by
     funext x
     simp only [christoffelFamily, chartCoefficients, ChartMetricCoefficients.christoffel,
       ChartMetricCoefficients.christoffelLower, sub_eq_add_neg]
   ```
   Then after `rw [hΓ]` the target body is `2⁻¹ * (d i j l + (d j i l + -d l i j))` and the const_smul term body is `(1/2) • (d i j l + (d j i l + -d l i j))` — wait, `ContDiff.add (ContDiff.add A B) (ContDiff.neg C)` gives `fun y => (A y + B y) + -C y` — associativity: target `d i j l + (d j i l + -d l i j)` vs term `(d i j l + d j i l) + -d l i j` — MISMATCH again (a + (b + c) vs (a+b) + c)!! Ugh.

   Cleanest: make hΓ use the right-assoc form matching `add (add A B) (neg C)`: hΓ body: `d x i j l + d x j i l + -d x l i j`? Parses as `(a + b) + -c` — wait no: `a + b + -c` is left-assoc `(a+b) + -c`. Yes that's what I want: `(d x i j l + d x j i l) + -d x l i j`... but hmm, wait — `add (add A B) (neg C)` gives `(A + B) + (-C)` = `fun y => (A y + B y) + (-C) y` = `(A y + B y) + -(C y)`. So hΓ body should be `(c.dFamily x i j l + c.dFamily x j i l) + -c.dFamily x l i j` — i.e. write it as `c.dFamily x i j l + c.dFamily x j i l - c.dFamily x l i j` — which is `Sub.sub (a+b) c` — and target-after-simpa keeps `-`... and term has `+ -`. simp does NOT rewrite `a - b` to `a + -b` automatically?? In the failure message: "After simplification, term h' has type ... fun y => 2⁻¹ * (a y + b y + -c y) but is expected ... fun x => 2⁻¹ * (a x + b x - c x)" — so simp left BOTH sides as-is on the sub/add difference. So `sub_eq_add_neg` is not in the default simp set (it's `[simp]`-tagged? apparently not applied — or it IS but simp's normalization prefers... hmm, in mathlib, `sub_eq_add_neg` — hmm — actually I recall that for ℝ, `Sub.sub` is NOT defeq `a + -b`! In Lean 4 mathlib, `Sub` for additive groups: `instance SubNegMonoid.toSub`? Let me check the definition... In mathlib `Mathlib/Algebra/Group/Defs.lean` or `Operations.lean`: `class Sub (α) where sub : α → α → α` — and the instance for `SubNegMonoid`... I believe there's `instance : Sub α := ⟨fun a b => a + -b⟩`? If so, `a - b` IS defeq `a + -b` and simpa should have closed... but it didn't, so `Sub` must be a separate operation with `sub_eq_add_neg` a theorem, and it's not [simp] (simp wouldn't rewrite `a - b` → `a + -b` by default; actually there IS a simp lemma `sub_eq_add_neg`? hmm, but then it'd be applied...).

   Whatever the case — simplest robust fix: make the hΓ body use `-` in the same places as the goal will show. The goal after `rw [hΓ]` — hΓ's RHS is what I write. The `simpa [one_div] using (...)` — the term's body is `(a + b) + -c`. To match, write hΓ's RHS as `(1 / 2 : ℝ) * (c.dFamily x i j l + c.dFamily x j i l + -c.dFamily x l i j)` — with explicit `-` written as `+ -c...`: `c.dFamily x i j l + c.dFamily x j i l + -c.dFamily x l i j` — parse: `(a + b) + (-c)` — wait `+ -c.dFamily` — `a + b + -c` parses `(a+b) + -c`? Hmm: `a + b + -c` — `+` is left-assoc: `(a + b) + -c`. Yes. And hΓ's proof: `simp only [..., sub_eq_add_neg]` — need to unfold christoffelLower to see what form it produces — probably `(1/2) * (d i j l + d j i l - d l i j)`. Then simp with sub_eq_add_neg rewrites to `+ -`. Then the goal matches the const_smul term modulo `one_div` + `smul_eq_mul`. 

   Hmm wait — but there's another subtlety: after `rw [hΓ]`, the GOAL is `ContDiff ℝ ∞ (fun x => ∑ l, c.gInv x k l * ((1/2) * (a + b + -c)))` — the `(1/2)` appears as `(1 / 2 : ℝ)` in the goal — and simpa [one_div] rewrites to `2⁻¹`. And `(1/2) • ...` in term → `2⁻¹ • ...` → smul_eq_mul... Actually hmm, does simp rewrite `a • b` → `a * b` via smul_eq_mul? Earlier scratch test 4: `example (a b : ℝ) : a • b = a * b := by simp` — passed. Good.

   Actually — even simpler alternative: skip `simpa [one_div]` gymnastics; instead prove the smoothness via a different path: `ContDiff.const_mul`? Does `ContDiff.const_mul` exist in this revision? There's `contDiff_const_mul`? Hmm — I recall `ContDiff.const_mul (a : 𝕜) (h : ContDiff 𝕜 n f) : ContDiff 𝕜 n (fun x => a * f x)` — hmm not sure of the name. My const_smul approach with aligned bodies is fine. Let me also double-check `ContDiff.neg` exists — the test compiled the `have h'` line? The error was at simpa (line 25), so `ContDiff.const_smul (1/2) (ContDiff.add (ContDiff.add ha hb) (ContDiff.neg hc))` elaborated — yes, `ContDiff.neg` exists. 

3. rw [fderiv_sum] bullet test: "No goals to be solved" at line 35 — the second bullet `· rfl` had NO goal — meaning the side goal came FIRST, `intro j hj; exact hA j hj` closed it, and then... the main goal was closed by rw itself (rfl auto-discharge: rw does close goals that become rfl? Actually rw doesn't... hmm — "No goals to be solved" for the second bullet means after the first bullet, the remaining goal list was empty — so the MAIN goal (which was `(∑...) v = (∑...) v` — hmm wait — that IS rfl — and Lean's goal system: when a goal's target is defeq-rfl, does the tactic engine auto-close it? YES — actually the tactic framework closes goals that are definitionally `True`/rfl only via `rfl` tactic... Hmm, but here the second bullet had nothing to do. This means rw's main goal was auto-solved... Actually — `rw` has a post-step: after rewriting, it runs `try rfl`? I believe `rw` applies `close` — the `rw` tactic does attempt to close the goal with `rfl` at the end? Hmm, not sure, but empirically: the bullet test passed with only the diff goal remaining. 

   So: in the real file, `rw [fderiv_sum]` followed by `· intro j hj; exact ...` — the mysterious "no binders" error in the original code... hmm, now with this test, side-goal-first IS confirmed. So why did the original `· intro j hj` fail?! Maybe because in the original, the side goal had a different shape — the goal function `fun z => c.g z i j * Y z i * Z z j` — with `j` bound by the ∀ — `intro j hj` — should work... 

   OH WAIT. I bet I know! In the ORIGINAL code the error line 502 `· intro j hj` — but the `rw [fderiv_sum]` at 501 — hmm — maybe rw FAILED to match there because of the `(X x)` application? No — rw matches subterms...

   Hmm, hold on — actually, let me reconsider: maybe in the original file the rw at 501 DID match, side goal first, `intro j hj` — but the error message said "There are no additional binders or `let` bindings in the goal to introduce" — WAIT — maybe the goal was `∀ j : ι, j ∈ (Finset.univ : Finset ι) → ...` — `intro j hj` on `∀ j, j ∈ univ → ...` — that has TWO binders — introN should handle. Unless the goal was `DifferentiableAt ℝ (fun z => c.g z i j * Y z i * Z z j) x` with `j` ALREADY... no.

   You know what — maybe the original error was a CASCADE: the errors at 382 (unsolved goals in nabla_const_apply) caused subsequent elaboration to be confused? No — Lean elaborates declarations independently-ish, but `nabla_const_apply` failing doesn't break `nabla_metricCompatible`'s elaboration... Actually errors do cascade in the same file only for dependent declarations. `nabla_metricCompatible` doesn't depend on nabla_const_apply. Hmm.

   Actually — hmm, wait. Let me re-read the ORIGINAL error for 502: "error: Poincare/D12/ConnectionCurvature/ChartLeviCivitaSmooth.lean:502:20: Tactic `introN` failed". In the ORIGINAL file (before my edits), line 502 was `    · intro j hj`. The goals after `rw [fderiv_sum]` at 501: [side-diff-goal, main]. First bullet `· intro j hj` → side goal. Side goal: `∀ j, j ∈ univ → DifferentiableAt ℝ (fun z => c.g z i j * Y z i * Z z j) x` — hmm — actually WAIT. In the ORIGINAL code, the rewrite at 501 was `rw [fderiv_sum]` — with NO hypothesis term — the side goal. And the FIRST block (line 480) used exactly this pattern successfully... with goal `fderiv ℝ (∑ i ∈ univ, (fun z => ∑ j : ι, ...)) x (X x) = (∑ i ∈ univ, fderiv ...) (X x)` — hmm wait no, at 480 the rw target was `fderiv ℝ (∑ i ∈ (Finset.univ : Finset ι), (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j)) x` — hmm — but at 480 the goal was `fderiv ℝ (fun z => ∑ i : ι, ∑ j : ι, ...) x (X x) = (∑ i ∈ univ, fderiv ℝ (fun z => ∑ j : ι, ...) x) (X x)` — wait, where's the `∑ i ∈ univ, (fun z => ∑ j...)` on the LHS?? Looking at the original 471-480:

   ```
   calc
     fderiv ℝ (fun z => ∑ i : ι, ∑ j : ι, c.g z i j * Y z i * Z z j) x (X x)
         = fderiv ℝ (∑ i ∈ (Finset.univ : Finset ι),
             (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j)) x (X x) := by
           congr 2
           funext z
           simp
     _ = (∑ i ∈ (Finset.univ : Finset ι),
             fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x) (X x) := by
           rw [fderiv_sum]
           intro i hi
           ...
   ```
   OK so at 480, rw [fderiv_sum] — goal `fderiv ℝ (∑ i ∈ univ, (fun z => ∑ j : ι, ...)) x (X x) = (∑ i ∈ univ, fderiv ℝ (fun z => ∑ j : ι, ...) x) (X x)`. Side goal: `∀ i ∈ univ, DifferentiableAt ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x`. Then `intro i hi` — succeeded (error list didn't flag 481). Then `have hfun; rw [hfun]; exact DifferentiableAt.sum (fun j hj => ...)` — the sum of functions `∑ j ∈ univ, (fun z => c.g z i j * Y z i * Z z j)` — and the DifferentiableAt.mul error at 487. And then the MAIN goal `(∑...) v = (∑...) v` — rfl — auto-closed by rw (as the scratch test showed, the main goal is auto-discharged when it becomes rfl — the "No goals to be solved" at my second bullet confirms).

   So the SAME pattern at 501 SHOULD work. But the reported error says introN failed with no binders at 502... UNLESS the side goal at 501 had a different shape. What's the goal at 501? After `rw [hbridge]`, goal: `fderiv ℝ (∑ j ∈ (Finset.univ : Finset ι), (fun z : ChartPoint ι => c.g z i j * Y z i * Z z j)) x (X x) = ∑ j : ι, (...)`. rw [fderiv_sum]: rewrites the subterm `fderiv ℝ (∑ j ∈ univ, A j) x` — side goal `∀ j ∈ univ, DifferentiableAt ℝ (A j) x`. Main: `(∑ j ∈ univ, fderiv ℝ (A j) x) (X x) = ∑ j : ι, (...)` — NOT rfl — stays as the second goal. First bullet: side goal with binders j, hj — `intro j hj` SHOULD work.

   Hmm — so why the failure?? OH WAIT!!! I see it!!! `rw [hbridge]` — the hbridge rewrite — `hbridge`'s RHS contains `(fun z : ChartPoint ι => c.g z i j * Y z i * Z z j)` — with j FREE (it's the sum binder in hbridge's own type — no wait, hbridge is closed; the j in its RHS is bound by `∑ j ∈ univ`). Fine.

   Hmm, hmm. What if the problem is that at 500, the goal was NOT what I think — because the `simp` at 491 + `apply Finset.sum_congr rfl` + `intro i hi` — maybe `simp` transformed the goal differently, e.g. the goal became `fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x (X x) = ∑ j : ι, (c.g x i j * (fderiv ℝ (fun z => Y z i) x) (X x) * Z x j + ...)` — hmm — with the fderiv applied AT `(X x)` inside each summand — displayed as `(fderiv ℝ (fun z => Y z i) x) (X x)`. Then hbridge — hbridge's LHS: `fderiv ℝ (fun z : ChartPoint ι => ∑ j : ι, c.g z i j * Y z i * Z z j) x` — matches the LHS subterm `fderiv ℝ (fun z => ∑ j : ι, ...) x` — hmm — wait, is the LHS of the goal `fderiv ℝ (fun z => ∑ j : ι, c.g z i j * Y z i * Z z j) x (X x)` — after `simp`, maybe simp rewrote `(∑ i ∈ univ, fderiv ... x) (X x)` into `∑ i ∈ univ, fderiv ... x (X x)` — and the per-i goal's LHS is `fderiv ℝ (fun z => ∑ j : ι, ...) x (X x)`. hbridge rewrites it. OK.

   You know what — maybe the original failure was REAL and caused by goal-order differences when the main goal is NOT rfl. Hmm — "introN failed, no binders" — what if rw at 501 put the MAIN goal first this time?? Both are rw [fderiv_sum]... the only difference: at 480 the main goal after rw is rfl; at 501 it's not. If rw orders goals as [main, side] — at 480: [rfl-main, side] — hmm, but then `intro i hi` would hit the rfl-main with no binders — contradiction. If rw orders [side, main] and auto-closes rfl goals: at 480 — [side, rfl-main] — after intro i hi + proof, the rfl-main auto-closes (or the second goal... wait, the 480-block had NO bullets, so after `exact DifferentiableAt.sum ...` the block ends — the remaining rfl-main must have been auto-closed by the framework). At 501: [side, main] — `· intro j hj` on side — should work?!

   I give up on armchair diagnosis — empirically, my scratch test 5 confirms [side, main] ordering with rfl-main auto-closed. Let me now rewrite the 499-510 block to match the tested pattern exactly:
   ```lean
            rw [hbridge]
            rw [fderiv_sum]
            · intro j hj
              exact ((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j)
            · simp
              apply Finset.sum_congr rfl
              intro j hj
              rw [fderiv_mul_mul_apply ...]
              ring
   ```
   This is EXACTLY the original except the `.mul` associativity fix at the `exact`. The original error at 502 `introN failed` might have been a CASCADE from the error at 487!! YES — that's plausible: the 487 type-mismatch error made the elaboration of the surrounding block fail, and errors cascade within a single declaration's tactic script — the block at 488-510 is ONE `by` — the `exact DifferentiableAt.sum (fun j hj => (hdiff_g i j).mul (...))` at 487 errored, which corrupted subsequent goal processing in the same tactic script, producing the spurious "introN failed" at 502. Once 487's error is fixed, 502 might work as originally written!

   So: revert my inline-hypothesis change back to the bullet form with the fixed associativity. And keep `fderiv_mul_mul_apply` step.

4. The `sum_mul` fix as planned.

5. `rw [hGamma]` direction fix.

Let me apply these remaining fixes now.
dsh: reasoning:
Now I can see clearly:

1. **Line 292**: bodies now match except `(1/2) •` vs `2⁻¹ *` — the `simpa [one_div]` should have fixed... wait, the error says the const_smul term has type `ContDiff ℝ ∞ fun y => (1 / 2) • (...)` — and the `simpa [one_div] using (...)` — the error is a TYPE MISMATCH of the term itself, meaning `simpa` couldn't align. In my scratch test 5, the same pattern failed due to `+ -c` vs `- c` — now bodies align on `+ -c`. But `(1/2) • t` vs `2⁻¹ * t` — my scratch test 4 DID pass this pattern (`have h' := ContDiff.const_smul (1 / 2 : ℝ) h; simpa [one_div] using h'` with body `(1 : ℝ)`). Why not here? Hmm — maybe the difference: the term's type in the error display shows `(1 / 2) • (...)`. The `simpa [one_div]` — maybe `one_div` isn't rewriting because the expected type `2⁻¹ * (...)` — `2⁻¹` is `Inv.inv 2` — and `one_div` : `1 / a = a⁻¹` — so `1 / 2 = 2⁻¹` — simp should rewrite `(1/2) • t`: first `1/2 → 2⁻¹`, then `2⁻¹ • t → 2⁻¹ * t` (smul_eq_mul). And target `2⁻¹ * t` stays. So they should unify... unless `simpa` fails on the CONTEXT (the surrounding `fun x => ∑ l ∈ univ, ...` vs `fun x => ∑ l, ...`)? The error message shows only the const_smul subterm's type — the actual mismatch could be in the OUTER expression (the ContDiff.sum wrapper): term type `ContDiff ℝ ∞ (fun x => ∑ l ∈ univ, c.gInv x k l * ((1/2) • (...)))` vs expected `ContDiff ℝ ∞ (fun x => ∑ l : ι, c.gInv x k l * (2⁻¹ * (...)))`. Hmm — the error display picked the inner mismatch to show. `∑ l ∈ univ, t` vs `∑ l : ι, t` — defeq. `(1/2) •` vs `2⁻¹ *` — simpa [one_div] handles IF smul_eq_mul and one_div are in simp's reach... but wait — my scratch test 4 PASSED exactly this. The only difference: the body `(...)` is a sum `a + b + -c` — should not matter.

Hmm wait, actually — let me look again at the error: "has type ContDiff ℝ ∞ fun y => (1 / 2) • (c.dFamily y i j l + c.dFamily y j i l + -c.dFamily y l i j) but is expected to have type ContDiff ℝ ∞ fun x => 2⁻¹ * (...)". So simpa failed to align. Maybe `smul_eq_mul` is NOT a simp lemma in this revision (it exists as a theorem but not tagged simp?). In scratch test 4: `example (a b : ℝ) : a • b = a * b := by simp` — passed — so simp DOES close it, but via which lemma? Maybe via the module instance being `Semiring.toModule` — DEFEQ! `a • b` for ℝ over ℝ is defeq `a * b` — so `(1/2) • t` is defeq `(1/2) * t`. Then the remaining gap: `(1/2)` vs `2⁻¹`. `simpa [one_div]` — hmm — does `one_div` get applied to `(1/2 : ℝ)`? `1 / 2` — the display shows `(1 / 2)` — `one_div : 1 / a = a⁻¹` — should rewrite... UNLESS the type of `1/2` — hmm.

Hmm wait — but in scratch test 4, the exact same `simpa [one_div] using ContDiff.const_smul (1 / 2 : ℝ) h` PASSED against target `ContDiff ℝ ∞ (fun x => 2⁻¹ * (1 : ℝ))`. So why does it fail here with the sum body?! The term and target differ in... let me diff carefully:

Test 4 term: `ContDiff ℝ ∞ (fun y => (1/2) • (1 : ℝ))`, target `ContDiff ℝ ∞ (fun x => 2⁻¹ * (1 : ℝ))` — simpa [one_div] aligned. Here: term `fun y => (1/2) • (d y i j l + d y j i l + -d y l i j)`, target `fun x => 2⁻¹ * (d x i j l + d x j i l + -d x l i j)`. Same modulo the body. Hmm — but WAIT, the error is at line 292:6 — the error location is the whole `simpa` line? "292:6: error: Type mismatch" — 292:6 ≈ `simpa [one_div] using (ContDiff.sum ...` — the `simpa` is at col 3... Actually with my edit, line 292 is `  simpa [one_div] using (ContDiff.sum (s := (Finset.univ : Finset ι)) (by`. Hmm — the error says the term `ContDiff.const_smul (1 / 2) (ContDiff.add ...)` has the wrong type — this is reported when elaborating the argument of `ContDiff.mul` — at line 292 col 6 — the `refine ContDiff.mul ?_ ?_` region. So the error is: inside the `by` block, the expected type for the second `?_` is `ContDiff ℝ ∞ (fun x => c.gInv x k l * (2⁻¹ * (...)))` — wait no, `ContDiff.mul`'s second arg expectation: `ContDiff ℝ ∞ (fun x => 2⁻¹ * (...))` — and my term `ContDiff.const_smul ...` gives `(1/2) • (...)` — the error is at ELABORATION of the term inside the `by`, i.e. `exact`/elaboration requires the term to have the expected type (not just simpa-able). The `simpa` only helps at the outer level, but the inner `?_` placeholder must be filled with a term of type `ContDiff ℝ ∞ (fun x => 2⁻¹ * (...))` — type mismatch since `(1/2) • body` is defeq `(1/2) * body` — and `(1/2) * body` vs `2⁻¹ * body` — NOT defeq (1/2 = 1 * 2⁻¹... hmm — `1/2` reduces how? `(1/2 : ℝ)` = `HDiv.hDiv (1 : ℝ) (2 : ℝ)` — the Div instance for ℝ: `instDivOfField`? For a `Field`/`DivisionRing`, `Div` instance := `fun a b => a * b⁻¹` — so `(1/2)` = `(1 : ℝ) * (2 : ℝ)⁻¹` — and target `(2 : ℝ)⁻¹` — `1 * x` vs `x` — for ℝ, `one_mul` — NOT defeq. Hence mismatch. In scratch test 4, the outer simpa absorbed the mismatch because there the mismatch appeared only at the TOP level after elaboration — hmm, no — in test 4, the `have h' := ContDiff.const_smul (1 / 2 : ℝ) h` — elaborated fine (its type is its own), then `simpa [one_div] using h'` — closed the goal. HERE, the structure is different: the `?_` hole's expected type is FIXED by the goal, so the term must typecheck against it directly — `(1/2) •` vs `2⁻¹ *` mismatch.

Fix: make the hΓ target use `(1/2)` too: change hΓ's body from `((1 / 2 : ℝ) * (...))` — it already IS `(1 / 2 : ℝ) *`! Let me re-check hΓ: `∑ l : ι, c.gInv x k l * ((1 / 2 : ℝ) * (c.dFamily x i j l + ...))`. Hmm — and the error message says expected type `ContDiff ℝ ∞ fun x => 2⁻¹ * (...)`. So after `rw [hΓ]`, the goal is `ContDiff ℝ ∞ (fun x => ∑ l, c.gInv x k l * ((1/2) * (...)))` — and the expected type for the hole should be `ContDiff ℝ ∞ (fun x => c.gInv x k l * ((1/2) * (...)))`?? But the error says expected `2⁻¹ * (...)`. Hmm — the expected type in the error: `ContDiff ℝ ∞ fun x => 2⁻¹ * (c.dFamily x i j l + ...)`. So the goal after rw [hΓ] has `2⁻¹` — meaning hΓ's `rw` succeeded but the goal's `(1/2)` got normalized to `2⁻¹` somewhere?? OR — the hΓ statement I wrote has `((1 / 2 : ℝ) * ...)` and after rw the goal shows `2⁻¹ * ...` — because the pretty printer normalizes `1/2` to `2⁻¹`? The pretty printer for ℝ — `1 / 2` prints as `1 / 2`... hmm, but it might print `2⁻¹` — hmm, `1 * 2⁻¹` — the printer might simplify numerals. Whatever — the KEY: the goal's expression and the term's expression differ somewhere by non-defeq rewriting that simpa must close — but the hole's expected type is the GOAL's form, and my term has the different form — mismatch at elaboration.

Robust fix: fill the hole with an explicit term whose type matches the goal's form exactly. The goal (after rw [hΓ]): `ContDiff ℝ ∞ (fun x => ∑ l, c.gInv x k l * ((1 / 2 : ℝ) * (d i j l + d j i l + -d l i j)))`. Hmm wait, the expected-type display for the hole is `fun x => 2⁻¹ * (...)` — hmm — since hΓ's RHS wrote `((1 / 2 : ℝ) * ...)` — after `rw [hΓ]` the goal's body has `(1/2) * t` — the hole's expected type (under `ContDiff.mul`, the second argument): `ContDiff ℝ ∞ (fun x => (1 / 2 : ℝ) * (...))` — but the display says `2⁻¹ * (...)`. The pretty printer prints `1/2` as `2⁻¹`? Plausible (it prints `1 / 2` as... hmm). If the goal's body IS `(1/2) * t` (printer shows 2⁻¹), and my term's body is `(1/2) • t` = defeq `(1/2) * t` — these WOULD be defeq and elaboration succeeds... but it didn't. So the goal's body really is `2⁻¹ * t` — meaning the `rw [hΓ]` — hmm — the rw succeeded and the goal body is literally `2⁻¹ * ...`?? That means hΓ's RHS was elaborated with `(1 / 2 : ℝ)` NORMALIZED to `2⁻¹` — no wait — hΓ's RHS was written by me as `(1 / 2 : ℝ) *` — hmm — but maybe the ELABORATOR normalizes `1/2` literals?? The elaborator has numeral normalization: `(1 / 2 : ℝ)` — `ofScientific`/`norm_num` preprocessing — `1/2` might elaborate to `2⁻¹` via `norm_num`-style normalization (division by 2 → inverse)! In Lean 4, the elaborator runs `norm_num` on numerals: `1 / 2` — hmm, for ℝ — the numeral elaborator for `1/2` — hmm, `/` is not a numeral — `1/2` = `HDiv.hDiv 1 2` — but the elaborator might preprocess `ofScientific`... I don't think `1/2` gets auto-normalized.

Ugh — display: the expected type display `fun x => 2⁻¹ * (...)` — could be the pretty printer showing `(1/2)` as `2⁻¹`?? The delaborator for `HDiv.hDiv (1) (2)` with ℝ — might use `prettyNumeral`... I don't recall Lean printing `1/2` as `2⁻¹`. Hmm, but actually — maybe it does! The delaborator has special handling: `2⁻¹` is the "unnormalized" display of... no.

Empirical fix: align the hole term's body to the goal's form by using `ContDiff.const_mul`? Hmm, is there such a lemma? Let me check `ContDiff.const_mul` in this mathlib: grep. If `ContDiff.const_mul (a) h : ContDiff 𝕜 n (fun x => a * f x)` exists, then:

```lean
    · exact ContDiff.const_mul (2⁻¹ : ℝ) (ContDiff.add (ContDiff.add ...) (ContDiff.neg ...))
```
Hmm but wait — `ContDiff.const_mul` — let me check. Actually — hmm, better idea: avoid the `(1/2)` scalar entirely — reformulate hΓ with `2⁻¹`:
```lean
  have hΓ : (fun x : ChartPoint ι => c.christoffelFamily x k i j) = fun x =>
      ∑ l : ι, c.gInv x k l * (2⁻¹ * (...)) := by
    funext x
    simp only [christoffelFamily, chartCoefficients, ChartMetricCoefficients.christoffel,
      ChartMetricCoefficients.christoffelLower, sub_eq_add_neg, one_div]
```
The christoffelLower def presumably contains `(1 / 2 : ℝ) * (...)` — simp [one_div] rewrites `1/2 → 2⁻¹`. Then the goal body is `2⁻¹ * t` — and the hole term: `ContDiff.const_smul (2⁻¹ : ℝ) (...)` : `ContDiff ℝ ∞ (fun y => 2⁻¹ • t)` — `2⁻¹ • t` defeq `2⁻¹ * t` (ℝ smul = mul) — MATCHES. No simpa needed at the end (or keep simpa harmlessly).

Wait — is `2⁻¹ • t` defeq `2⁻¹ * t`? Earlier analysis: `Semiring.toModule` has smul := mul — but hmm, is that true? In scratch test 4, `example (a b : ℝ) : a • b = a * b := by simp` passed — via simp lemmas or defeq, either way `simp` closed it. For ELABORATION (defeq matching), I need DEFEQ, not simp. Hmm. Is `a • b` defeq `a * b` for ℝ? Let me check the Module instance for ℝ... `Mathlib/Data/Real/Basic.lean` — hmm. For fields, `Module ℝ ℝ` comes from... `Algebra ℝ ℝ` → `Algebra.toModule` with smul := `Algebra.smul`? For an Algebra over a CommSemiring where the algebra is the ring itself: `Algebra.smul_def`... The instance `instAlgebraOfSemiring`? Hmm — in mathlib: `instance (priority := 100) Algebra.id (R : Type*) [CommSemiring R] : Algebra R R` — its smul := `Semiring.toModule`'s? Hmm — `Algebra.toModule` takes the smul from... `Algebra` extends `SMul` with `toSMul : SMul R A` and `smul_def' : ∀ r x, r • x = algebraMap r x * x`. The instance `Algebra.id`: `toSMul := inferInstance` — which resolves to `Semiring.toModule`-based `SMul R R`? Hmm — is there a standalone `SMul R R` instance = `Semiring.toSMul`? `Semiring.toModule` provides the full Module which includes SMul with `smul := (· * ·)`. If `Algebra.id`'s toSMul := `Semiring.toModule`'s smul (the only candidate), then `a • b` = `a * b` DEFEQ. I believe this is right: in mathlib, `r • x` for r, x : ℝ is defeq `r * x`. (The standard fact: `smul_eq_mul` is `rfl`-provable for ℝ? Actually `smul_eq_mul` is stated as a theorem but its proof is `rfl`? In `Mathlib/Algebra/Algebra/Basic.lean`: `@[simp] theorem smul_eq_mul (a : A) (b : A) : a • b = a * b := by rw [Algebra.smul_def, algebraMap_eq_id, id_apply]`? Hmm — if Algebra.id's algebraMap is `id` and toSMul is the Semiring.toModule smul... then smul_eq_mul's proof is rw-based, meaning smul is NOT defeq mul... 

   OK — empirical again: test `example (a b : ℝ) : a • b = a * b := by rfl` in scratch. If rfl fails, then const_smul won't match and I need `ContDiff.const_mul`-style or a `.congr`-based proof. Alternative robust approach — bypass the smul entirely: prove the goal by `convert`/`exact (ContDiff.const_smul ...)` won't work if not defeq... 

   Alternative: since dFamily_smooth gives `ContDiff ℝ ∞ (fun x => dFamily x i j l)` etc., the whole expression `fun x => 2⁻¹ * (d x i j l + d x j i l + -d x l i j)` is smooth via the COMPOSITION route: `ContDiff.mul (contDiff_const) (ContDiff.add ...)` — `ContDiff.const_mul`! Let me check if `ContDiff.const_mul` exists: grep mathlib ContDiff/Operations for "const_mul".

Let me check these things: (a) `rfl` for a • b = a * b; (b) ContDiff.const_mul existence.
dsh: reasoning:
Both examples in scratch_test6 PASSED (no errors) — so `a • b = a * b` is DEFEQ for ℝ (rfl works). That means `ContDiff.const_smul (2⁻¹ : ℝ)` gives `fun y => 2⁻¹ • t` which is defeq `fun y => 2⁻¹ * t`. So the fix for line 292: use `(2⁻¹ : ℝ)` as the const_smul argument and make hΓ use `2⁻¹ *`.

`const_mul` doesn't exist (grep empty). OK.

Fix for christoffelFamily_smooth:
```lean
  have hΓ : (fun x : ChartPoint ι => c.christoffelFamily x k i j) = fun x =>
      ∑ l : ι, c.gInv x k l * ((2 : ℝ)⁻¹ *
        (c.dFamily x i j l + c.dFamily x j i l + -c.dFamily x l i j)) := by
    funext x
    simp only [christoffelFamily, chartCoefficients, ChartMetricCoefficients.christoffel,
      ChartMetricCoefficients.christoffelLower, sub_eq_add_neg, one_div]
  rw [hΓ]
  simpa using (ContDiff.sum (s := (Finset.univ : Finset ι)) (by
    intro l hl
    refine ContDiff.mul ?_ ?_
    · exact c.gInv_component_smooth k l
    · exact ContDiff.const_smul ((2 : ℝ)⁻¹)
        (ContDiff.add (ContDiff.add (c.dFamily_smooth i j l) (c.dFamily_smooth j i l))
          (ContDiff.neg (c.dFamily_smooth l i j)))))
```
Hmm — but wait: will `simp only [..., one_div]` in the hΓ proof rewrite `1/2` to `2⁻¹`? christoffelLower's definition — let me check ChartLeviCivita.lean to see how christoffelLower is defined — `(1/2) * (...)`. `one_div : 1 / a = a⁻¹` — hmm, `1/2 : ℝ` — the term is `HDiv.hDiv 1 2` — `one_div`'s LHS: `1 / a` — matches with a := 2. Rewrites to `(2)⁻¹`. But then the resulting expression must match my hΓ RHS `(2 : ℝ)⁻¹ * (...)` — `(2 : ℝ)⁻¹` vs `2⁻¹` — same. And the associativity: hΓ RHS body `c.dFamily x i j l + c.dFamily x j i l + -c.dFamily x l i j` = `(a + b) + -c` — and christoffelLower's body — need to check its exact form — `d i j l + d j i l - d l i j` = `(a+b) - c` — after sub_eq_add_neg: `(a+b) + -c`. MATCH.

But hmm — careful with `simp only [..., one_div]` — does the `-` in the def stay as `Sub.sub` after `sub_eq_add_neg` fires? sub_eq_add_neg : `a - b = a + -b` — [simp]-tagged? In mathlib, `sub_eq_add_neg` IS tagged simp? Hmm — actually — I recall `sub_eq_add_neg` is `@[simp]`... wait no. In mathlib: `theorem sub_eq_add_neg (a b : G) : a - b = a + -b` — is it simp? There's `[simp] theorem sub_neg_eq_add`... Let me not gamble: I'll write `simp only [..., sub_eq_add_neg, one_div]` — explicit, so it fires regardless of simp-tag.

Wait — but if `Sub.sub` for ℝ is NOT defeq `a + -b` — the earlier scratch test 5 failure showed simpa kept `a + b - c` distinct from `a + b + -c` — meaning either sub_eq_add_neg isn't in simp's default set or it's not defeq. With explicit `sub_eq_add_neg` in the only-list it will fire. Good.

Hmm — one more concern: `christoffelLower`'s def might be `(1 / 2 : ℝ) * (d i j l + d j i l - d l i j)` — and after simp the hΓ proof must close `∑ l, gInv k l * ((1/2) * (...)) = ∑ l, gInv k l * (2⁻¹ * (...))` — with one_div + sub_eq_add_neg — should be fine.

2. **Line 501 introN failure — NOW I can see the real cause!** The context shows: after `rw [hbridge]; rw [fderiv_sum]` — the current goal (from the 504 error) is `∀ (i_1 : ι), DifferentiableAt ℝ (fun z => c.g z i i_1 * Y z i * Z z i_1) x` — so the side goal DID come first — and it's `∀ i_1, DifferentiableAt ...` — ONLY ONE binder (no `∈ univ`)! Because `fderiv_sum`'s hypothesis is `∀ i ∈ u, ...` — hmm, but the goal shows `∀ (i_1 : ι), DifferentiableAt ... x` — the `∈ univ` part got... hmm — with `u = Finset.univ`, `i ∈ Finset.univ` is TRUE by `Finset.mem_univ` — and the elaborator/rw... The goal `∀ i_1 ∈ univ, ...` — hmm, the display shows `∀ (i_1 : ι), DifferentiableAt ...` — where's `i_1 ∈ Finset.univ`? OH — maybe rw/tactic simplifies `i ∈ univ → P` to `P`?? The `rw` tactic's side-goal creation — hmm — no. Wait, maybe the elaborator applied `Finset.mem_univ` automatically — no. Hmm, the display might just not show it — `∀ (i_1 : ι), DifferentiableAt ...` — hmm.

   And the 501 error says: `introN failed: no additional binders` — the goal has `∀ (i_1 : ι), ...` — that IS a binder!! introN should introduce `i_1`... The 501 error context (shown before 504) is cut off. Hmm — the 501:20 error: `· intro j hj` — "There are no additional binders or let bindings in the goal to introduce" — with the goal shown in the 504 error being `∀ (i_1 : ι), DifferentiableAt ℝ (fun z => c.g z i i_1 * Y z i * Z z i_1) x` — hmm, that HAS a binder. UNLESS at 501, the first goal was different — the errors are from ONE elaboration pass — the 501 error happened when processing `intro j hj` on the goal state AT THAT TIME. Hmm — maybe at 501 the first goal was the MAIN goal (not rfl), i.e. ordering [main, side]?! But then at 504 (second bullet), `simp` — and the 504 error says the goal there was `∀ i_1, DifferentiableAt ...` — so at 504 the remaining goal was the SIDE goal — consistent with ordering [main, side]?? If ordering were [main, side]: at 501, `· intro j hj` targets MAIN goal `(∑ j ∈ univ, fderiv ...) (X x) = ∑ j, (...)` — no binders → EXACTLY the 501 error! And at 504 `· simp` would target the SIDE goal — `simp` on `∀ i_1, DifferentiableAt ...` — simp does something (simp simplifies under ∀? it would try to simplify `DifferentiableAt ...` — no progress but no error either?) — then `apply Finset.sum_congr rfl` — error at 504:14 — "could not unify ... with the goal ∀ i_1, DifferentiableAt ..." — YES!! That matches PERFECTLY. So rw's goal ordering is [MAIN first, then side goals]?? But my scratch test 5 showed side-goal-first... wait — scratch test 5: `rw [fderiv_sum]` then `· intro j hj; exact hA j hj` then `· rfl` — and the error was "No goals to be solved" at the second bullet. With [main, side] ordering: first bullet `intro j hj` on main goal `(∑...) v = (∑...) v` — rfl-goal — intro fails?? But the test only errored at line 35 (the second bullet)... So in the test, the first bullet succeeded — `intro j hj` on the main rfl goal should have failed... unless main goal was auto-closed (it's rfl!) and the first bullet went to the side goal. AH — THAT's consistent: rw with a hypothesis: goals = [main-goal (after rewrite), side-goals]?? but when the main goal is rfl, the tactic framework auto-closes it (the `by` block's `·` handling: `rw` closes rfl main goal, leaving [side]). Hmm — but does `rw` auto-close rfl goals? — Actually the tactic framework: when a tactic script finishes and the current goal is definitionally rfl (`⊢ a = a`), Lean's `tactic` engine does NOT auto-close... but rw DOES have a final `try (rfl)`? I believe `rw` ends with `try rfl`-ish behavior? Hmm, no — actually I recall: the elaboration of `by ...` blocks — leftover goals cause errors. In scratch test 5, main-after-rw = `(∑ j ∈ univ, fderiv ℝ (A j) x) v = (∑ j ∈ univ, fderiv ℝ (A j) x) v` — if that were the first goal and `intro j hj` hit it — "introN failed" would be the first error at line ~33. The reported error was only at line 35 ("No goals to be solved") — meaning at line 33-34, `intro j hj; exact hA j hj` succeeded on SOME goal, and at line 35 `· rfl` found NO goal left. With [main, side] ordering and rw auto-closing the rfl main: remaining = [side]; `intro j hj; exact hA j hj` closes side; second bullet — no goals — "No goals to be solved" — MATCHES! With [side, main] ordering and main auto-closed: [side] then main closed — same observable behavior. Both orderings are consistent with the test.

   And in the real file: main-after-rw is NOT rfl, so: if ordering [side, main]: first bullet gets side (`∀ i_1, DifferentiableAt`) — `intro j hj` — hmm — the goal has ONE binder `i_1` — `intro j hj` = introN 2 — introN with 2 names on a goal with 1 binder — FAILS?? "There are no additional binders" — YES!!! `intro j hj` tries to introduce `j` then `hj` — after introducing `j` (= the `∀ i_1`), the goal `DifferentiableAt ...` has NO more binders → "no additional binders" error!! THAT'S IT. The side goal is `∀ i_1 : ι, DifferentiableAt ℝ (fun z => c.g z i i_1 * Y z i * Z z i_1) x` — only ONE binder (the `i_1 ∈ univ` was auto-reduced to True and eliminated — because `∀ i_1, i_1 ∈ univ → P i_1` — hmm — the display shows only `∀ (i_1 : ι), DifferentiableAt ...` — maybe the membership hypothesis was folded... whatever — empirically it has 1 binder).

   Hmm wait — in the FIRST fderiv_sum use (line ~480), the side goal was `∀ i ∈ univ, DifferentiableAt ℝ (fun z => ∑ j : ι, ...) x` — and `intro i hi` worked there — TWO binders. Why does the second side goal have only ONE binder?? Because at 480, u is... hmm — at 480, the sum is `∑ i ∈ (Finset.univ : Finset ι)` — with the membership hypothesis `i ∈ Finset.univ` — that's `Finset.Mem` — stays as a hypothesis. At 501, the sum `∑ j ∈ (Finset.univ : Finset ι)` too... Hmm, but the 504-error goal display shows `∀ (i_1 : ι), DifferentiableAt ℝ (fun z => c.g z i i_1 * Y z i * Z z i_1) x` — hmm — maybe the display DID drop the `∈ univ`... no, displays don't drop. Hmm — hold on, maybe rw's side-goal for fderiv_sum is `∀ i ∈ u, ...` and for u = univ, the elaborator simplifies `i ∈ univ` to `True` and the ∀-True is simplified away automatically by the tactic framework?? Actually YES — the tactic engine applies "simp-like" cleaning? No... Hmm — actually there IS such behavior: when rw creates side goals, it doesn't simplify them. But the DISPLAY in the error context at 504 shows the goal as-is. So the goal really is `∀ (i_1 : ι), DifferentiableAt ...` — one binder.

   Hmm — why the difference from the first use? OH WAIT — I see — at 501, the `rw [fderiv_sum]`'s matching: the target sum is `∑ j, fun z => ...` — hmm — no wait, after hbridge, the LHS is `fderiv ℝ (∑ j ∈ (Finset.univ : Finset ι), (fun z : ChartPoint ι => c.g z i j * Y z i * Z z j)) x` — hmm, the hbridge RHS display in the 504 context: `hbridge : fderiv ℝ (fun z => ∑ j, c.g z i j * Y z i * Z z j) x = fderiv ℝ (∑ j, fun z => c.g z i j * Y z i * Z z j) x` — hmm — `∑ j, ...` (no `∈ univ`)! The hbridge I have in the file was written with `(∑ j ∈ (Finset.univ : Finset ι), ...)` — but the display shows `∑ j, ...` — pretty printer for `∑ j ∈ univ` prints `∑ j, ...`? Yes — the delaborator simplifies `∑ x ∈ Finset.univ` to `∑ x`. Fine.

   The side goal: `∀ j ∈ (Finset.univ : Finset ι), DifferentiableAt ...` — the delaborator prints `∀ j ∈ univ, P j` as `∀ j, P j`?? No — delaborator prints `∀ x ∈ s, P` as `∀ x ∈ s, P` — hmm, but for s = univ it might simplify... The context display shows `⊢ ∀ (i_1 : ι), DifferentiableAt ℝ (fun z => c.g z i i_1 * Y z i * Z z i_1) x` — hmm — this could be the delaboration of `∀ i_1, i_1 ∈ univ → ...` with `i_1 ∈ univ` auto-simplified... I don't think so.

   Whatever the exact binder count — EMPIRICAL fix: replace `intro j hj` with `intro j` and then close:
   ```lean
            · intro j
              exact ((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j)
   ```
   If there are 2 binders (j and hj), `intro j` leaves `hj : j ∈ univ` in context and the exact still works (extra hypotheses are fine). If 1 binder, `intro j` works and exact works. ROBUST to both! 

   Hmm wait — but if the side goal is actually `∀ j ∈ univ, ...` (2 binders) — `intro j` leaves `⊢ j ∈ univ → DifferentiableAt ...` — `exact (...)` — has type `DifferentiableAt ...` — with an extra implication target — FAILS ("exact" doesn't auto-intro). Then I'd need `intro j hj`. Since the first use at 480 with `intro i hi` WORKED (its side goal had 2 binders), and the 501 one apparently has 1... why the difference?! Hmm — hmm. Let me look at the FIRST use's side goal again: `∀ i ∈ univ, DifferentiableAt ℝ (fun z => ∑ j : ι, ...) x` — `intro i hi` succeeded there (error list didn't complain) — 2 binders. The SECOND's side goal: `∀ (i_1 : ι), DifferentiableAt ℝ (fun z => c.g z i i_1 * Y z i * Z z i_1) x` — 1 binder?! 

   OH!!! I think I get it — the side goal's function in the second case: `fun z => c.g z i i_1 * Y z i * Z z i_1` — and rw's side-goal generation: for `fderiv_sum`'s hypothesis `h : ∀ i ∈ u, DifferentiableAt 𝕜 (A i) x` — when u = univ, the elaborated side goal is `∀ (j : ι), DifferentiableAt ...` — hmm — because `∀ j ∈ univ, P` — the `Membership` — `j ∈ (Finset.univ : Finset ι)` — when the goal is created via the tactic's `mkDecideProof`? No...

   Hmm — actually! Here's a real possibility: the difference is `i` vs `i_1` — the delaborator renamed. And in the first use the hypothesis came as `i ∈ Finset.univ` because the SUM there was written `∑ i ∈ (Finset.univ : Finset ι)` in the GOAL (explicit membership), whereas in the second the goal's sum (after hbridge, written with `∑ j ∈ (Finset.univ : Finset ι)` in hbridge) — hmm, both have univ membership...

   Whatever — simplest bulletproof: handle both cases:
   ```lean
            · intro j
              exact ((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j)
   ```
   If it turns out 2 binders, compile error will say "exact" type mismatch with implication — I'll adjust to `intro j; simpa using ...`? Hmm — no — `exact` against a goal `P → Q` with a term of type Q — "type mismatch". So iterate. But actually — WAIT. Let me reconsider — maybe the goal at 501's first bullet was NOT the side goal! Per the 504-error context: the goal at 504-bullet was `⊢ ∀ (i_1 : ι), DifferentiableAt ℝ (fun z => c.g z i i_1 * Y z i * Z z i_1) x` — that's the SIDE goal (the main goal would be `(∑ j ∈ univ, fderiv ...) (X x) = ∑ j, (...)`). The 504 bullet starts with `simp` — simp on `∀ i_1, DifferentiableAt ...` — simp does nothing (no progress — it might simplify the body but no error) — then `apply Finset.sum_congr rfl` — error "could not unify ... with the goal ∀ i_1, DifferentiableAt ..." — consistent: the 504 bullet hit the SIDE goal. So ordering = [main, side]! And the 501 bullet hit the MAIN goal — `(∑ j ∈ univ, fderiv ℝ (A j) x) (X x) = ∑ j, (...)`: `intro j hj` → no binders → ERROR. That's the real story: **rw puts the main goal FIRST when it's not rfl?** or ALWAYS main first, with rfl-main auto-closed. Consistent with everything:
   - Scratch test 5: main = rfl → auto-closed (by rw's post-processing — hmm, does rw auto-close? Apparently the first bullet went to the side goal and the second had nothing — YES consistent: [main(auto-closed), side]).
   - First use at 480: main after rw = rfl `(∑...) v = (∑...) v` → auto-closed; remaining [side]; `intro i hi` — 2 binders — worked.

   Hmm wait — but is the main-goal-after-rw at 480 really rfl? Goal: `fderiv ℝ (∑ i ∈ univ, A i) x (X x) = (∑ i ∈ univ, fderiv ℝ (A i) x) (X x)` — rw [fderiv_sum] rewrites LHS: `(∑ i ∈ univ, fderiv ℝ (A i) x) (X x) = (∑ i ∈ univ, fderiv ℝ (A i) x) (X x)` — rfl — auto-closed by rw (the `rw` tactic in Lean 4 does have a final `rfl` attempt — yes, I now recall `rw` tries `rfl` at the end).

   So the fix for the 501 block: reorder bullets: first bullet = main goal (simp/apply sum_congr/fderiv_mul_mul_apply/ring), second = side goal (intro j; exact ...). But wait — with auto-closed-rfl exceptions... at 501 the main goal is NOT rfl so it stays. New structure:
   ```lean
            rw [hbridge]
            rw [fderiv_sum]
            · simp
              apply Finset.sum_congr rfl
              intro j hj
              rw [fderiv_mul_mul_apply ...]
              ring
            · intro j
              exact ((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j)
   ```
   Hmm — but the side goal binder count (1 vs 2) remains uncertain. And ALSO the FIRST use at 480: `rw [fderiv_sum]` then `intro i hi; have hfun; rw [hfun]; exact DifferentiableAt.sum ...` — this block has NO bullets — it works on the FIRST goal only (main, rfl, auto-closed) — wait NO — if main is rfl and auto-closed by rw's final rfl, then the remaining goals are just [side], and the block's tactics run on the side goal: `intro i hi` (2 binders — works), then the diff proof. And after that block ends... main was already closed. GOOD — that explains why 480's block worked.

   For the side-goal binder question at 501: by symmetry with 480's side goal (which had 2 binders — `intro i hi` worked and its `exact DifferentiableAt.sum` needed hi? no it didn't use hi), the 501 side goal should also have 2 binders `∀ j, j ∈ univ → ...`. The display in the 504 context showed `⊢ ∀ (i_1 : ι), DifferentiableAt ℝ (fun z => c.g z i i_1 * Y z i * Z z i_1) x` — hmm — that shows ONE binder — but maybe the delaborator hides `∈ univ` since... hmm. Actually you know what — the `simp` in the 504 bullet ran FIRST and might have simplified the goal from `∀ i_1, i_1 ∈ univ → ...` to `∀ i_1, ...`?? simp DOES simplify under forall binders and uses `Finset.mem_univ` — wait — `i_1 ∈ univ` — simp [Finset.mem_univ]? `mem_univ` — is it [simp]? `@[simp] theorem Finset.mem_univ : x ∈ univ := True.intro` — hmm — it's `theorem mem_univ (x) : x ∈ univ := ...` — if [simp]-tagged, `simp` on `∀ i_1, i_1 ∈ univ → P i_1` — hmm, simp under forall: introduces i_1, goal `i_1 ∈ univ → P i_1` — simp on the implication premise `i_1 ∈ univ` → True — so goal becomes `True → P i_1` → `P i_1` — and the forall is re-abstracted → `∀ i_1, P i_1`. YES! That explains the 504 display. And the ERROR at 504 came from the `apply` after that simp. So the side goal DID have 2 binders originally, and `simp` reduced it to 1-binder form. 

   SO: the correct fix at 501: bullet ORDER [main, side] — main first:
   ```lean
            rw [hbridge]
            rw [fderiv_sum]
            · simp
              apply Finset.sum_congr rfl
              intro j hj
              rw [fderiv_mul_mul_apply (f := fun z : ChartPoint ι => c.g z i j)
                (g := fun z : ChartPoint ι => Y z i) (h := fun z : ChartPoint ι => Z z j)
                (hf := hdiff_g i j) (hg := hdiff_Y i) (hh := hdiff_Z j) (v := X x)]
              ring
            · intro j hj
              exact ((hdiff_g i j).mul (hdiff_Y i)).mul (hdiff_Z j)
   ```
   Hmm wait — but careful: the `simp` in the main goal — the main goal `(∑ j ∈ univ, fderiv ℝ (A j) x) (X x) = ∑ j : ι, (...)` — `simp` turns `(∑ j ∈ univ, f_i) v` into `∑ j ∈ univ, f_i v` — then `apply Finset.sum_congr rfl` — LHS sum over univ, RHS `∑ j : ι` = sum over univ — unifies — then per-j: `fderiv ℝ (A j) x (X x) = (c.g x i j * (fderiv (Y i)) (X x) * Z x j + ...)` — rw [fderiv_mul_mul_apply] + ring. Same as the original second bullet. 

   Hmm, but wait — why did the ORIGINAL author's structure work for the FIRST use at 480 (side goal first... no wait). Ugh, let me recompute the original's first use: goals after `rw [fderiv_sum]` at 480: rw's final rfl auto-closes the rfl-main → [side]. Block continues: `intro i hi` → side goal (2 binders — worked), `have hfun...`, `rw [hfun]`, `exact DifferentiableAt.sum ...` — closes side. Block done. Main was rfl — closed. The whole calc step closed. ✓.

   And at 501 original: rw → main NOT rfl → goals [main, side]. Original bullets: `· intro j hj` → MAIN goal → no binders → ERROR ✓ (matches!). Then `· simp...` → side goal → simp reduces to ∀-1-binder, apply fails ✓ (matches the 504 error).

   PERFECT understanding now. The fix: swap the bullets (main first, side second). 

3. **Line 559**: `rw [hm, ← Finset.sum_mul]` — pattern `∑ k, c.dFamily x k i j * X x k * Y x i * Z x j` not found — the goal shows `∑ j_1, c.dFamily x j i j_1 * X x j * Y x i * Z x j_1 = ...` — the sum binder is `j_1` — but hm's LHS has binder `k`! Alpha-renaming: `rw` matches binders by unification — the pattern's binder `k` vs goal's `j_1` — rw does handle alpha-renaming?? Hmm — "Did not find an occurrence of the pattern ∑ k, ..." — the target has `∑ j_1, ...` — alpha-equivalent terms SHOULD match... unless the binder has a TYPE ascription issue or the summand body differs: hm LHS summand: `c.dFamily x k i j * X x k * Y x i * Z x j` — target summand: `c.dFamily x j i j_1 * X x j * Y x i * Z x j_1` — alpha: k ↦ j_1 — should match?! Unless — hmm — the goal's sum: `∑ j_1, c.dFamily x j i j_1 * X x j * Y x i * Z x j_1` — wait, that has free `j` — where did `j` come from? The context: we're inside `intro j hj` — hmm, the goal at 559 — let me look at what the goal was: `∑ k, c.dFamily x k i j * X x k * Y x i * Z x j = (∑ k, X x k * c.dFamily x k i j) * Y x i * Z x j` — the LHS summand: `c.dFamily x k i j * X x k * Y x i * Z x j` — parses as `((d * X) * Y) * Z` — hm's LHS: `c.dFamily x k i j * X x k * Y x i * Z x j` — same. Binder k vs k — hmm, but the error shows the target `∑ j_1, ...` and pattern `∑ k, ...`. rw should alpha-match. Why fail?! 

   Hmm — maybe rw's binder matching: the pattern from `hm` is a *closed term* — `hm`'s LHS has `k` bound inside; when rw searches, it matches the target `∑ j_1, t[j_1]` against pattern `∑ k, t[k]` — rw uses `matchPattern` which does support binder alpha-equivalence (it unifies the lambdas)... Actually — rw's pattern matching uses `kabstract` + `isDefEq` with reduction... The sum `∑ j_1, f j_1` = `Finset.univ.sum (fun j_1 => f j_1)` — the pattern `∑ k, f k` = `Finset.univ.sum (fun k => f k)` — isDefEq between the fun-bodies with free var substitution — YES standard. So why fail...

   OH WAIT — look at the error target: `∑ j_1, c.dFamily x j i j_1 * X x j * Y x i * Z x j_1` — the `c.dFamily x j i j_1` — and pattern: `c.dFamily x k i j * X x k * Y x i * Z x j` — target has `c.dFamily x j i j_1 * X x j` — hmm — with `k ↦ j_1`: `c.dFamily x j_1 i j * X x j_1 * Y x i * Z x j` — vs target `c.dFamily x j i j_1 * X x j * ...` — the arguments are in DIFFERENT ORDER — target: d x j i j_1 (the k-arg is LAST in target, FIRST in pattern)! So the GOAL at that point was `∑ j_1, c.dFamily x j i j_1 * X x j * Y x i * Z x j_1 = (∑ k, X x k * c.dFamily x k i j) * Y x i * Z x j` — i.e. the goal's LHS is NOT my hm's LHS! The actual goal: after the `sum_three_cycle`-based calc step at line ~549 (`_ = ∑ k, ∑ i, ∑ j, c.dFamily x k i j * X x k * Y x i * Z x j`), then `rw [Finset.sum_comm]` — swaps outer sums: `∑ i, ∑ k, ∑ j, ...` — then two sum_congrs — goal per (i, j): `∑ k, c.dFamily x k i j * X x k * Y x i * Z x j = (∑ k, X x k * c.dFamily x k i j) * Y x i * Z x j` — hmm, but the error target shows `∑ j_1, c.dFamily x j i j_1 * X x j * Y x i * Z x j_1` — the sum binder is `j_1` and the k-slot appears as `j`! So the goal's LHS sum is over the LAST slot of d — the goal is `∑ j_1, c.dFamily x j i j_1 * X x j * Y x i * Z x j_1`?? That means the rw [Finset.sum_comm] + the two sum_congrs produced a goal where the outer binder pair is (j, j_1)-ish — hmm — wait: after `rw [Finset.sum_comm]` on `∑ k, ∑ i, ∑ j, d x k i j * X x k * Y x i * Z x j` — sum_comm swaps the FIRST two sums: `∑ i, ∑ k, ∑ j, ...`. Then sum_congr i, sum_congr j?? — no wait — the original code:
   ```
       _ = ∑ k : ι, ∑ i : ι, ∑ j : ι, c.dFamily x k i j * X x k * Y x i * Z x j := ...
       _ = ∑ i : ι, ∑ j : ι, (∑ k : ι, X x k * c.dFamily x k i j) * Y x i * Z x j := by
             rw [Finset.sum_comm]
             apply Finset.sum_congr rfl
             intro i hi
             apply Finset.sum_congr rfl
             intro j hj
             rw [← Finset.sum_mul]
   ```
   Hmm — the third sum is `∑ j, d x k i j * X x k * Y x i * Z x j` — after sum_comm (swaps k and i): `∑ i, ∑ k, ∑ j, ...`. Then sum_congr on the OUTER (i) — wait, `apply Finset.sum_congr rfl` targets `∑ i, (∑ k, ∑ j, ...)` vs `∑ i, ∑ j, (...)` — per-i: `∑ k, ∑ j, d x k i j * X x k * Y x i * Z x j = ∑ j, (∑ k, X x k * d x k i j) * Y x i * Z x j`. Then second sum_congr per-j: `∑ k, d x k i j * X x k * Y x i * Z x j = (∑ k, X x k * c.dFamily x k i j) * Y x i * Z x j`. Hmm — the error target shows `∑ j_1, c.dFamily x j i j_1 * X x j * Y x i * Z x j_1 = ...` — the LHS sum binder `j_1` with d-args `(j, i, j_1)` — that's the k-slot LAST — so the goal's LHS is `∑ j_1, d x j i j_1 * X x j * Y x i * Z x j_1` — this is the goal AFTER a further sum_comm?! Hmm — the goal's LHS `∑ j_1, c.dFamily x j i j_1 * X x j * Y x i * Z x j_1` — the index `j` (free, from intro j hj) fills the FIRST d-slot, sum over the THIRD slot — that means at some point the sums got reordered as `∑ k, ∑ j, d x k i j ...` → hmm.

   Wait — I misread the original: `rw [Finset.sum_comm]` — which sums does it swap? The current goal LHS at that point: `∑ k, ∑ i, ∑ j, c.dFamily x k i j * X x k * Y x i * Z x j`. `Finset.sum_comm` on this — the lemma `sum_comm : ∑ i, ∑ j, f i j = ∑ j, ∑ i, f i j` — rw finds the OUTERMOST `∑ i, (∑ j, ...)`: with i := k, j := i: swaps to `∑ i, ∑ k, ∑ j, ...`. THEN rw also might rewrite... only once. So after rw: LHS = `∑ i, ∑ k, ∑ j, ...`. Hmm — but the ERROR at 559 came AFTER my hm-edit — with my edit inserted `have hm : (∑ k : ι, c.dFamily x k i j * X x k * Y x i * Z x j) = (∑ k : ι, (X x k * c.dFamily x k i j) * (Y x i * Z x j)) := ...` — the error says the pattern `∑ k, c.dFamily x k i j * X x k * Y x i * Z x j` (hm's LHS) not found in target `∑ j_1, c.dFamily x j i j_1 * X x j * Y x i * Z x j_1 = ...`. So at that point the goal's LHS is `∑ j_1, c.dFamily x j i j_1 * X x j * Y x i * Z x j_1` — NOT `∑ k, c.dFamily x k i j * X x k * Y x i * Z x j`!! So my assumed goal shape was wrong — the ACTUAL per-(i,j) goal has LHS `∑ j_1, d x j i j_1 * X x j * Y x i * Z x j_1` — hmm — where the sum is over the third slot and the first slot = j (the free var). So the sums were reordered differently: after `rw [Finset.sum_comm]` — hmm — maybe the first rw (line 550-ish `rw [Finset.sum_comm]`) — the original target had `∑ k, ∑ i, ∑ j, ...` — sum_comm swaps k↔i giving `∑ i, ∑ k, ∑ j` — then `apply sum_congr; intro i` — per-i: `∑ k, ∑ j, ...` vs `∑ j, ...` — then `apply sum_congr; intro j` — per-j: `∑ k, d x k i j * X x k * Y x i * Z x j = (∑ k, X x k * d x k i j) * Y x i * Z x j` — hmm — that's what I assumed. But the error target LHS is `∑ j_1, d x j i j_1 * ...` — with `j` free and sum over third slot. So actually the goal went through another reordering — the `simp` in the calc... no wait — line 549's step: `_ = ∑ i : ι, ∑ j : ι, (∑ k : ι, X x k * c.dFamily x k i j) * Y x i * Z x j := by rw [Finset.sum_comm] ...` — the PREVIOUS step's RHS was `∑ k, ∑ i, ∑ j, c.dFamily x k i j * X x k * Y x i * Z x j` (from the sum_three_cycle step at 547-548: `∑ k : ι, ∑ i : ι, ∑ j : ι, c.dFamily x k i j * X x k * Y x i * Z x j`). Then `rw [Finset.sum_comm]` — hmm — the pattern `∑ i, ∑ j, f i j = ∑ j, ∑ i, f i j` — matches `∑ k, (∑ i, (∑ j, ...))` — swaps k and i: `∑ i, ∑ k, ∑ j, ...`. THEN — rw continues matching the new expression?! `rw` rewrites to fixpoint! It would match AGAIN: `∑ i, ∑ k, ∑ j, ...` — swaps i and k AGAIN — back to `∑ k, ∑ i, ∑ j` — infinite?? No — rw to fixpoint with a self-inverse lemma terminates (it stops when... hmm, it might actually loop or do it twice and stop because the result is syntactically equal? No — after two applications it returns to the original — rw's fixpoint check would stop after seeing no progress?). Hmm — actually `rw [Finset.sum_comm]` — default rw rewrites ALL occurrences ONCE (not fixpoint): occurrences in the goal of the pattern `∑ i, ∑ j, f i j` — the term `∑ k, ∑ i, ∑ j, t`: outermost occurrence = `∑ k, (∑ i, (∑ j, t))` — rewritten to `∑ i, ∑ k, ∑ j, t`. Then rw checks for OTHER occurrences — the new term `∑ i, ∑ k, ∑ j, t` contains `∑ i, (∑ k, ...)` — another occurrence! rw rewrites it → `∑ k, ∑ i, ∑ j, t` — back to start — rw detects no progress and stops. Net effect: no change?? But then the goal would be `∑ k, ∑ i, ∑ j, ...` — and per-(i,j)... hmm.

   Ugh, this is getting deep. Let me look at the ACTUAL goal at that point by reading the current file lines 545-565 and reasoning from the error target directly. The error target: `∑ j_1, c.dFamily x j i j_1 * X x j * Y x i * Z x j_1 = (∑ k, X x k * c.dFamily x k i j) * Y x i * Z x j`. So the LHS is `∑ j_1, d x j i j_1 * X x j * Y x i * Z x j_1` — the RHS `(∑ k, X x k * d x k i j) * Y x i * Z x j`. Hmm — so LHS = sum over THIRD slot with FIRST slot = free j; RHS = sum over FIRST slot. The RHS is the intended target. The LHS — from `∑ i, ∑ j, ∑ k ...`-ordering: `∑ i, (∑ j, (∑ k, d x k i j * X x k * Y x i * Z x j))` — per (i, j): `∑ k, d x k i j * X x k * Y x i * Z x j` — first slot = k. But error shows LHS `∑ j_1, d x j i j_1 * X x j * Y x i * Z x j_1` — first slot = j (free), sum slot = j_1 (third). So the goal's LHS came from `∑ i, ∑ k, ∑ j, d x k i j ...` after per-(i, k)... hmm — no wait — the two sum_congrs: `apply Finset.sum_congr rfl; intro i hi; apply Finset.sum_congr rfl; intro j hj` — on goal `∑ i, ∑ k, ∑ j, ... = ∑ i, ∑ j, (...)` — hmm — sum_congr rfl requires the same finset on both sides: LHS `∑ i, ...` RHS `∑ i, ...` — univ/univ ✓. Per-i: `∑ k, ∑ j, d x k i j * X x k * Y x i * Z x j = ∑ j, (∑ k, ...) * Y x i * Z x j`. Second sum_congr rfl: LHS sum `∑ k, ...` vs RHS `∑ j, ...` — hmm — sum_congr unifies s := univ on both — per-element: binder k vs j — the second intro is `intro j hj` — hmm — if the LHS's second sum binder is `k`, then `intro j hj` introduces the k-binder as `j`?? and the RHS's `∑ j` element binder is ALSO introduced by the same intro (both sides of the equality get the same binder name). So per-element goal: `∑ j, d x j i j * X x j * Y x i * Z x j = (∑ k, X x k * d x k i j) * Y x i * Z x j` — WAIT — with the LHS sum being the THIRD (originally) sum `∑ j, d x k i j * X x k * Y x i * Z x j` — renamed k ↦ j by intro?? — and RHS per-element: `(∑ k, X x k * d x k i j) * Y x i * Z x j`. Hmm — the error target LHS: `∑ j_1, c.dFamily x j i j_1 * X x j * Y x i * Z x j_1` — sum binder j_1, d-args (j, i, j_1) — this matches: the remaining sum is over the ORIGINAL third slot (j_1), with the first slot filled by the free `j`. So the goal really is: `∑ j_1, d x j i j_1 * X x j * Y x i * Z x j_1 = (∑ k, X x k * d x k i j) * Y x i * Z x j`. Hmm — that's not what the original author intended (`rw [← Finset.sum_mul]` would need summand `f k * a` — the LHS summand `d x j i j_1 * X x j * Y x i * Z x j_1` — hmm — `((d * X) * Y) * Z` — the `← sum_mul` pattern `∑ i ∈ s, f i * a` — summand's top node `Mul ((d*X*Y)) (Z x j_1)` — f j_1 := (d x j i j_1 * X x j * Y x i)? no — `f i * a` — the top node must be `*` with right = a (no index dependence) — matches a := Z x j_1?? — a must be a constant not containing the binder — but Z x j_1 CONTAINS j_1 — rw's pattern `?f ?i * ?a` — ?a could still depend on... in rw's matching, `?a` is a metavariable that must NOT depend on the binder — rw uses `kabstract`-style matching where the pattern's non-binder metavars get abstracted... The original `rw [← Finset.sum_mul]` FAILED per the original error at 551: "Did not find an occurrence of the pattern ∑ i ∈ ?s, ?f i * ?a in the target expression ∑ j_1, c.dFamily x j i j_1 * X x j * Y x i * Z x j_1 = ..." — consistent.

   So the goal structure is different from what I assumed — the sums after `rw [Finset.sum_comm]` came out as `∑ i, ∑ j, ∑ k ...`-ish — whatever — the ACTUAL goal: `∑ j_1, d x j i j_1 * X x j * Y x i * Z x j_1 = (∑ k, X x k * d x k i j) * Y x i * Z x j`.

   Hmm — hold on — the goal LHS sums over the third slot with d-args (j, i, j_1) — but the RHS sum is `∑ k, X x k * d x k i j` — sum over FIRST slot. So the equality `∑_{j_1} d(j, i, j_1) X(j) Y(i) Z(j_1) = (∑_k X(k) d(k, i, j)) Y(i) Z(j)` — this is NOT TRUE in general!! Wait — is it? LHS = `(∑_{j_1} d(j,i,j_1) Z(j_1)) * X(j) * Y(i)` — RHS = `(∑_k X(k) d(k,i,j)) * Y(i) * Z(j)` — these are different sums — NOT equal in general!!! So the goal is FALSE?! That can't be — the calc step must have been mis-stated. Hmm — wait — unless the goal got to a genuinely wrong state because `rw [Finset.sum_comm]` rewrote in a way the author didn't anticipate (double-rewrite), producing a WRONG target. Let me recompute: the step's target RHS: `∑ i, ∑ j, (∑ k, X x k * d x k i j) * Y x i * Z x j`. LHS (previous step): `∑ k, ∑ i, ∑ j, d x k i j * X x k * Y x i * Z x j`. Is LHS = RHS true? RHS = `∑ i, ∑ j, (∑ k, X k d kij) Y i Z j` = `∑ i,j,k X k d kij Y i Z j` ✓ = LHS. TRUE. But with the sum_comm double-rewrite the per-(i,j) subgoal became the FALSE statement above. So `rw [Finset.sum_comm]` DID rewrite twice (self-inverse, no net effect — wait, that would leave the original `∑ k, ∑ i, ∑ j` — then per-(i, j) — hmm.

   OK let me stop and think about what ACTUALLY happened: the error's per-(i,j) goal LHS: `∑ j_1, d x j i j_1 * X x j * Y x i * Z x j_1` — this is `∑ i, (∑ j, (∑ k, d x k i j * X x k * Y x i * Z x j))` — per-(i, j): the inner-most sum `∑ k, d x k i j * X x k * Y x i * Z x j` — but the error shows d-args `(j, i, j_1)` with sum over the THIRD — so the sum order became `∑ i, ∑ j, ∑ k` — with per-(i,j) picking the... hmm, if the goal LHS after the two sum_congrs is `∑ j_1, d x j i j_1 * X x j * Y x i * Z x j_1` — where `j` is the free var from `intro j hj` — then the LHS structure at that point is `∑ i, (∑ j, (∑ j_1, d x j i j_1 * X x j * Y x i * Z x j_1))`?? — the free `j` inside the inner sum — that's `∑ i, ∑ j, ∑ k, d x j i k * X x j * Y x i * Z x k` — the index `j` appears in d's FIRST slot and X's slot — this came from `∑ k, ∑ i, ∑ j, d x k i j ...` — hmm — rename: original `∑ k, ∑ i, ∑ j, d x k i j * X x k * Y x i * Z x j` — after ONE sum_comm (k↔i): `∑ i, ∑ k, ∑ j, d x k i j * X x k * Y x i * Z x j`. After rw rewrites a SECOND occurrence — the new term `∑ i, ∑ k, ∑ j, ...` — pattern `∑ i, ∑ j, f` — swaps i↔k: `∑ k, ∑ i, ∑ j, ...` — rw detects no progress (syntactically equal to pre-rewrite state? — it would stop when the rewrite produces a term already seen — modern rw stops to avoid loops and reports?? — or it just does 2 rewrites and stops because the second rewrite brings back the original and the "no progress" check fires). If rw did swap twice, net = original. Then per-(i,j) would be over k — but error shows otherwise.

   Whatever the mechanics — the subgoal as reported IS mathematically false, which means `rw [Finset.sum_comm]` corrupted the goal. The ROBUST fix: replace that whole calc step with a direct proof of the equality:
   `∑ k, ∑ i, ∑ j, d x k i j * X x k * Y x i * Z x j = ∑ i, ∑ j, (∑ k, X x k * d x k i j) * Y x i * Z x j`
   — via sum_three_cycle (or sum_product flattening) + sum_mul. Cleanest: use the flattening approach with explicit Finset.sum_product':

   Actually simpler: prove via `sum_three_cycle`-like flatten: both sides equal `∑ (i, j, k) : ι × ι × ι, X x k * d x k i j * Y x i * Z x j`. Hmm — that's a bit of work but robust. OR — use `Finset.sum_comm` carefully with explicit position control:

   ```lean
      _ = ∑ i : ι, ∑ j : ι, (∑ k : ι, X x k * c.dFamily x k i j) * Y x i * Z x j := by
            rw [Finset.sum_comm]  -- only once: force with occurrence? 
   ```
   Hmm. `Finset.sum_comm` has signature: `sum_comm : ∑ i, ∑ j, f i j = ∑ j, ∑ i, f i j`? In this mathlib: `theorem Finset.sum_comm {f : α → β → γ} : (∑ a, ∑ b, f a b) = ∑ b, ∑ a, f a b`. rw [Finset.sum_comm] on `∑ k, ∑ i, ∑ j, ...` — rewrites the outermost `∑ k, (∑ i, ...)` → `∑ i, ∑ k, ...` — then does rw try the newly created subterm `∑ i, (∑ k, ...)`? rw rewrites ALL occurrences OF THE ORIGINAL TERM (it re-scans the goal after each rewrite, but marks rewritten regions?). Modern rw: "rewrites all occurrences" — it repeatedly searches — and there's loop detection via syntactic equality with previous states? I believe rw does `repeat`-ish search with `occs` and stops when no NEW occurrence — it would loop 2 times and return to start... which would make the net change ZERO and then `apply sum_congr...` would produce the TRUE subgoal `∑ k, d x k i j ... = (∑ k, ...) * Y i * Z j` — hmm — but the error shows the FALSE subgoal, so net change was ONE swap: `∑ i, ∑ k, ∑ j, d x k i j ...`. Then: `apply Finset.sum_congr rfl` — LHS `∑ i, (∑ k, (∑ j, ...))` vs RHS `∑ i, (∑ j, (...))` — per-i: `∑ k, ∑ j, d x k i j * X x k * Y x i * Z x j = ∑ j, (∑ k, X x k * d x k i j) * Y x i * Z x j`. Second sum_congr rfl: LHS `∑ k, (...)` vs RHS `∑ j, (...)` — sum_congr requires same finset (univ ✓) — per-element with binder introduced as `j` (the intro name): LHS summand: `∑ j, d x j i j * X x j * Y x i * Z x j` — WAIT — no — the LHS at this point is `∑ k, (∑ j, d x k i j * X x k * Y x i * Z x j)` — sum_congr rfl on `∑ k, (∑ j, ...) = ∑ j, (...)` — hmm — sum_congr's `rfl` proves `univ = univ`, then per-element: `(∑ j, d x k i j * X x k * Y x i * Z x j) = (∑ k, X x k * d x k i j) * Y x i * Z x j` — with k := j (intro j hj renames the LHS's k-binder to j). So the per-element goal: `∑ j, d x j i j * X x j * Y x i * Z x j = (∑ k, X x k * d x k i j) * Y x i * Z x j` — the error shows `∑ j_1, d x j i j_1 * X x j * Y x i * Z x j_1` — the inner sum binder got renamed j_1 (avoiding the free j) ✓. So the goal is genuinely: `∑_{j_1} d(j, i, j_1) X(j) Y(i) Z(j_1) = (∑_k X(k) d(k, i, j)) Y(i) Z(j)` — FALSE in general. CONFIRMED: `rw [Finset.sum_comm]` swapped k↔i once, leaving `∑ i, ∑ k, ∑ j` — and the original author's subsequent sum_congrs were written assuming `∑ i, ∑ j, ∑ k` (i.e., assuming sum_comm swapped to give the RIGHT ordering for the target `∑ i, ∑ j, (∑ k, ...) * Y i * Z j` — which needs LHS `∑ i, ∑ j, ∑ k, d x k i j * X x k * Y x i * Z x j`).

   So the FIX: replace `rw [Finset.sum_comm]` with an explicit rewrite that gives `∑ i, ∑ j, ∑ k, ...`: apply `sum_three_cycle`! We have `sum_three_cycle (f : ι → ι → ι → ℝ) : ∑ i, ∑ j, ∑ k, f i j k = ∑ k, ∑ i, ∑ j, f k i j`. To get `∑ i, ∑ j, ∑ k, d x k i j * X x k * Y x i * Z x j` from `∑ k, ∑ i, ∑ j, d x k i j * X x k * Y x i * Z x j`: use sum_three_cycle TWICE? sum_three_cycle: LHS `∑ i, ∑ j, ∑ k, f i j k` = RHS `∑ k, ∑ i, ∑ j, f k i j`. Apply with f k i j := d x k i j * X x k * Y x i * Z x j: `∑ k, ∑ i, ∑ j, f k i j = ∑ j, ∑ k, ∑ i, f j k i`? Let me recompute: sum_three_cycle f: `∑ i, ∑ j, ∑ k, f i j k = ∑ k, ∑ i, ∑ j, f k i j`. Setting the RHS to match our term: `∑ k, ∑ i, ∑ j, d x k i j * X x k * Y x i * Z x j` = `∑ k, ∑ i, ∑ j, f k i j` with f a b c := d x a b c * X x a * Y x b * Z x c. Then LHS = `∑ i, ∑ j, ∑ k, f i j k` = `∑ i, ∑ j, ∑ k, d x i j k * X x i * Y x j * Z x k`. So `sum_three_cycle f` gives `∑ i, ∑ j, ∑ k, d x i j k * X x i * Y x j * Z x k = ∑ k, ∑ i, ∑ j, d x k i j * X x k * Y x i * Z x j`. We want the reverse direction: `rw [← sum_three_cycle (fun i j k => ...)]` — rewriting `∑ k, ∑ i, ∑ j, d x k i j * ...` → `∑ i, ∑ j, ∑ k, d x i j k * X x i * Y x j * Z x k`. Hmm — but then the per-(i,j) subgoal after two sum_congrs: `∑ k, d x i j k * X x i * Y x j * Z x k = (∑ k, X x k * d x k i j) * Y x i * Z x j` — sum over the THIRD slot with d(i,j,k) — and RHS sum over the FIRST slot with d(k,i,j) — STILL not matching! Because the target `(∑ k, X x k * c.dFamily x k i j) * Y x i * Z x j` has d with k in the FIRST slot, while the LHS summand after sum_three_cycle has d(i,j,k) with k in the THIRD slot. Need d's slots reindexed: d x k i j vs d x i j k — these are NOT equal (d is only symmetric in the LAST TWO slots: dFamily_symm: d x i j k = d x i k j).

   Hmm. So the natural way: get the LHS as `∑ i, ∑ j, ∑ k, d x k i j * X x k * Y x i * Z x j` — then per-(i,j): `∑ k, d x k i j * X x k * Y x i * Z x j = (∑ k, X x k * d x k i j) * Y x i * Z x j` — TRUE via sum_mul (summand = (X x k * d x k i j) * (Y x i * Z x j) after ring). So I need LHS `∑ i, ∑ j, ∑ k, d x k i j * X x k * Y x i * Z x j`. From `∑ k, ∑ i, ∑ j, d x k i j * X x k * Y x i * Z x j` — apply `Finset.sum_comm` to swap... the structure `∑ k, (∑ i, (∑ j, ...))` — I want `∑ i, (∑ j, (∑ k, ...))` — a 3-cycle, not a 2-swap. sum_three_cycle f with LHS `∑ i, ∑ j, ∑ k, f i j k` = RHS `∑ k, ∑ i, ∑ j, f k i j`. Take f i j k := d x j k i * X x j * Y x k * Z x i — then RHS = `∑ k, ∑ i, ∑ j, f k i j` = `∑ k, ∑ i, ∑ j, d x i j k * X x i * Y x j * Z x k` — nope. Try: want RHS = `∑ k, ∑ i, ∑ j, d x k i j * X x k * Y x i * Z x j` — so f k i j := d x k i j * X x k * Y x i * Z x j — i.e. f a b c := d x a b c * X x a * Y x b * Z x c. LHS = `∑ i, ∑ j, ∑ k, f i j k` = `∑ i, ∑ j, ∑ k, d x i j k * X x i * Y x j * Z x k` — again the wrong slot order.

   Apply sum_three_cycle twice: first `sum_three_cycle f1` with f1 k i j := d x k i j * X x k * Y x i * Z x j (RHS = our current term): gives LHS `∑ i, ∑ j, ∑ k, d x i j k * X x i * Y x j * Z x k`. Then apply `sum_three_cycle f2` to this: we want `∑ i, ∑ j, ∑ k, d x k i j * X x k * Y x i * Z x j`... hmm — sum_three_cycle can only cycle in one direction. Apply the cycle twice = cycle twice = inverse cycle. Let me define: the current term T = `∑ k, ∑ i, ∑ j, d x k i j * X x k * Y x i * Z x j`. Target T' = `∑ i, ∑ j, ∑ k, d x k i j * X x k * Y x i * Z x j`. T → (sum_three_cycle with f1 a b c = d x a b c * X x a * Y x b * Z x c; T = RHS of sum_three_cycle f1) → LHS = `∑ i, ∑ j, ∑ k, f1 i j k` = `∑ i, ∑ j, ∑ k, d x i j k * X x i * Y x j * Z x k` =: U. U → (sum_three_cycle with f2 a b c = d x b c a * X x b * Y x c * Z x a: U = LHS? U = `∑ i, ∑ j, ∑ k, d x i j k * X x i * Y x j * Z x k` — sum_three_cycle f2 LHS = `∑ i, ∑ j, ∑ k, f2 i j k` = `∑ i, ∑ j, ∑ k, d x j k i * X x j * Y x k * Z x i` — nope, U's summand has d(i,j,k) — set f2 i j k := d x i j k * X x i * Y x j * Z x k — then LHS = U ✓, RHS = `∑ k, ∑ i, ∑ j, f2 k i j` = `∑ k, ∑ i, ∑ j, d x k i j * X x k * Y x i * Z x j` = T!!! — so sum_three_cycle f2 applied to U returns T. Useless (it's the inverse).

   Hmm — so sum_three_cycle alone can't get from T to T'. What about `Finset.sum_comm` applied at the RIGHT place — I want to swap the k-sum with the (i,j)-pair: T = `∑ k, ∑ i, ∑ j, t k i j` → `∑ i, ∑ j, ∑ k, t k i j` — that's `Finset.sum_comm` with the outer double sum `∑ i, (∑ j, ...)` — apply sum_comm to the subterm `∑ k, (∑ i, (∑ j, ...))` — wait — sum_comm swaps TWO sums: `∑ a, ∑ b, f a b → ∑ b, ∑ a, f a b`. T's structure: `∑ k, ((∑ i, (∑ j, t)))` — the outermost pair is (k, i): swap → `∑ i, (∑ k, (∑ j, t))` — then the pair (k, j) inside: `∑ k, (∑ j, t)` — swap → `∑ j, (∑ k, t)` — result: `∑ i, ∑ j, ∑ k, t k i j` ✓!! That's the original author's intent with `rw [Finset.sum_comm]` — ONE rw call — but rw stopped after ONE swap (or double-swapped back). To do two swaps explicitly: `rw [Finset.sum_comm]` twice? First rw: `∑ i, ∑ k, ∑ j, t` — second `rw [Finset.sum_comm]`: the pattern now matches `∑ i, (∑ k, ...)` — hmm — outermost is (i, k) — swap → back to `∑ k, ∑ i, ∑ j` — NO. Need the SECOND swap to hit the INNER pair (k, j), not the outer (i, k). Use `conv` or rewrite under the outer binder:
   ```lean
   rw [Finset.sum_comm]
   conv_lhs =>
     enter [2]  -- hmm
   ```
   Simpler — use `Finset.sum_comm` as a FUNCTION with explicit composition, or use the flattening approach with `Finset.sum_product'`:
   ```lean
   have hT : (∑ k : ι, ∑ i : ι, ∑ j : ι, c.dFamily x k i j * X x k * Y x i * Z x j)
       = ∑ i : ι, ∑ j : ι, ∑ k : ι, c.dFamily x k i j * X x k * Y x i * Z x j := by
     calc
       (∑ k : ι, ∑ i : ι, ∑ j : ι, t k i j)
           = ∑ p : ι × ι × ι, t p.1 p.2.1 p.2.2 := by ... flatten
       _ = ∑ i : ι, ∑ j : ι, ∑ k : ι, t k i j := by ... unflatten
   ```
   That's the sum_three_cycle proof technique from earlier in the file. I can generalize: prove a lemma `sum_cycle_inv (f : ι → ι → ι → ℝ) : (∑ k, ∑ i, ∑ j, f k i j) = ∑ i, ∑ j, ∑ k, f k i j` via the same flattening. Then use it.

   Actually — cleanest: use `sum_comm` correctly by rewriting under binders:
   ```lean
   rw [show (∑ k : ι, ∑ i : ι, ∑ j : ι, c.dFamily x k i j * X x k * Y x i * Z x j)
       = ∑ i : ι, ∑ j : ι, ∑ k : ι, c.dFamily x k i j * X x k * Y x i * Z x j from by
     calc
       _ = ∑ k : ι, ∑ j : ι, ∑ i : ι, c.dFamily x k i j * X x k * Y x i * Z x j := by
             rw [Finset.sum_comm] -- hmm — this swaps (k,i) → (i,k)... 
   ```
   no wait — hmm — `rw [Finset.sum_comm]` on `∑ k, (∑ i, (∑ j, t))` — matches `∑ k, (∑ i, ...)` — wait, but which occurrence does rw pick — the OUTERMOST `∑ a, (∑ b, f a b)` — a := k, b := i — result `∑ i, ∑ k, ∑ j`. Then rw scans for more occurrences — the newly created `∑ i, (∑ k, (...))` — that's an occurrence — rewrites it back?? — the earlier analysis said the net result was `∑ i, ∑ k, ∑ j` (ONE swap) — so rw did NOT re-rewrite the new subterm (rw marks the rewritten region as "done"? In Lean 4, rw's loop: it rewrites occurrences; after rewriting, the new subterm could match again but rw tracks progress via syntactic equality of the whole goal — the second rewrite would produce the original goal — rw stops when the goal state repeats — hence ONE swap net). Empirically from the error: net = one swap. So a single `rw [Finset.sum_comm]` gives `∑ i, ∑ k, ∑ j, t`.

   To get the second swap (inner pair k↔j): after the first rw, the term is `∑ i, (∑ k, (∑ j, t))` — I want to rewrite INSIDE the outer binder: 
   ```lean
   rw [Finset.sum_comm]
   conv_lhs =>
     enter [2]
     rw [Finset.sum_comm]
   ```
   `conv_lhs => enter [2]` — hmm, `enter [2]` on `∑ i, (∑ k, (∑ j, ...))` — enters the argument position 2 of the outer sum (the inner double sum), then rw swaps (k,j) → `∑ j, (∑ k, t)`. Result: `∑ i, ∑ j, ∑ k, t` ✓. `conv` with `enter [2]` — need `conv_lhs => enter [2]; rw [Finset.sum_comm]` — hmm — does `enter [2]` work like that? `conv_lhs => enter [2]` — I think the syntax is `conv_lhs => enter [2]` then newline `rw [Finset.sum_comm]`. Hmm — actually within conv, `enter` then `rw` — but after entering, the goal inside conv is the subterm — rw [Finset.sum_comm] applies to it. OK.

   Alternatively — simplest of all — avoid the issue by changing the calc TARGET to match the actual one-swap result: target the calc step as `_ = ∑ i, ∑ j, (∑ k, X x k * d x k i j) * Y x i * Z x j` — no. The target is fixed (it's the RHS of the equality chain). The LHS side must be massaged to `∑ i, ∑ j, ∑ k, d x k i j * X x k * Y x i * Z x j`.

   Hmm — wait, actually — EVEN SIMPLER: swap the roles — apply `sum_three_cycle` to the RHS target instead? The RHS `∑ i, ∑ j, (∑ k, X x k * d x k i j) * Y x i * Z x j` — I could instead make the calc target `∑ k, ∑ i, ∑ j, ...` — no, the target is what it is (the dFormOf equality chain needs `∑ i, ∑ j, fderiv-g * Y i * Z j` form).

   Let me go with the explicit have + flattening lemma. Actually — cleanest and most robust: write a helper lemma at the top of the file:

   ```lean
   /-- Reindexing the triple sum by moving the first sum to the last position
   (flattening via `Finset.sum_product'`). -/
   lemma sum_cycle_first_to_last (f : ι → ι → ι → ℝ) :
       (∑ k : ι, ∑ i : ι, ∑ j : ι, f k i j) = ∑ i : ι, ∑ j : ι, ∑ k : ι, f k i j := by
     classical
     calc
       (∑ k : ι, ∑ i : ι, ∑ j : ι, f k i j)
           = ∑ q : ι × ι × ι, f q.1 q.2.1 q.2.2 := by
             calc
               (∑ k : ι, ∑ i : ι, ∑ j : ι, f k i j)
                   = ∑ k : ι, ∑ p : ι × ι, f k p.1 p.2 := by
                     apply Finset.sum_congr rfl
                     intro k hk
                     simpa using (Finset.sum_product' (s := (Finset.univ : Finset ι))
                       (t := (Finset.univ : Finset ι)) (f := fun i j => f k i j)).symm
               _ = ∑ q : ι × ι × ι, f q.1 q.2.1 q.2.2 := by
                     simpa using (Finset.sum_product' (s := (Finset.univ : Finset ι))
                       (t := (Finset.univ : Finset (ι × ι))) (f := fun k p => f k p.1 p.2)).symm
       _ = ∑ i : ι, ∑ j : ι, ∑ k : ι, f k i j := by
             calc
               (∑ q : ι × ι × ι, f q.1 q.2.1 q.2.2)
                   = ∑ p : ι × ι, ∑ k : ι, f k p.1 p.2 := by
                     simpa using (Finset.sum_product' (s := (Finset.univ : Finset ι))
                       (t := (Finset.univ : Finset (ι × ι))) (f := fun k p => f k p.1 p.2))
               _ = ∑ i : ι, ∑ j : ι, ∑ k : ι, f k i j := by
                     apply Finset.sum_congr rfl
                     intro p hp
                     simpa using (Finset.sum_product' (s := (Finset.univ : Finset ι))
                       (t := (Finset.univ : Finset ι)) (f := fun j k => f k p.1 j))
   ```
   Hmm wait — the last step: `∑ p : ι × ι, ∑ k, f k p.1 p.2 = ∑ i, ∑ j, ∑ k, f k i j` — the LHS: `∑ p : ι×ι, (∑ k, f k p.1 p.2)` = `∑ (i,j), ∑ k, f k i j` — RHS `∑ i, (∑ j, (∑ k, f k i j))` — product' flattens `∑ i, ∑ j, g i j = ∑ p, g p.1 p.2` — so RHS = `∑ p : ι×ι, ∑ k, f k p.1 p.2` ✓ via sum_product'. Good.

   Then in the metric-compat proof:
   ```lean
      _ = ∑ i : ι, ∑ j : ι, (∑ k : ι, X x k * c.dFamily x k i j) * Y x i * Z x j := by
            rw [sum_cycle_first_to_last (fun k i j => c.dFamily x k i j * X x k * Y x i * Z x j)]
            apply Finset.sum_congr rfl
            intro i hi
            apply Finset.sum_congr rfl
            intro j hj
            have hm : (∑ k : ι, c.dFamily x k i j * X x k * Y x i * Z x j)
                = (∑ k : ι, (X x k * c.dFamily x k i j) * (Y x i * Z x j)) := by
              apply Finset.sum_congr rfl
              intro k hk
              ring
            rw [hm, ← Finset.sum_mul]
   ```
   And the per-(i,j) goal after the two sum_congrs is now `∑ k, d x k i j * X x k * Y x i * Z x j = (∑ k, X x k * d x k i j) * Y x i * Z x j` ✓ TRUE, and `hm` + `← sum_mul` closes it.

   Wait — sum_congr after the rw: goal `∑ i, ∑ j, ∑ k, t = ∑ i, ∑ j, (∑ k, ...) * Y x i * Z x j` — first sum_congr per-i: `∑ j, ∑ k, t k i j = ∑ j, (∑ k, X x k * d x k i j) * Y x i * Z x j` — second per-j: `∑ k, t k i j = (∑ k, X x k * d x k i j) * Y x i * Z x j` ✓ where t k i j = d x k i j * X x k * Y x i * Z x j. 

Now — 559's fix is clear. Let me also double-check the earlier `_ = ∑ k, ∑ i, ∑ j, ...` step (line ~547-548): `exact sum_three_cycle (fun k i j => c.dFamily x k i j * X x k * Y x i * Z x j)` — sum_three_cycle f: `∑ i, ∑ j, ∑ k, f i j k = ∑ k, ∑ i, ∑ j, f k i j` — with f k i j := ...: RHS = `∑ k, ∑ i, ∑ j, d x k i j * X x k * Y x i * Z x j` ✓ and the step's LHS was `∑ i, ∑ j, ∑ k, c.dFamily x i j k * X x i * Y x j * Z x k` — hmm wait — the dFormOf expansion: `dFormOf (c.chartCoefficients x) (X x) (Y x) (Z x) = ∑ i, ∑ j, ∑ k, c.dFamily x i j k * X x i * Y x j * Z x k` — hmm, let me check the dFormOf def in ChartLeviCivitaForm — `dFormOf c X Y Z = ∑ i, ∑ j, ∑ k, c.d i j k * X i * Y j * Z k` — with the sum_three_cycle f: LHS `∑ i, ∑ j, ∑ k, f i j k` = `∑ i, ∑ j, ∑ k, d x i j k * X x i * Y x j * Z x k` ✓ — matches dFormOf's expansion — and RHS = `∑ k, ∑ i, ∑ j, d x k i j * X x k * Y x i * Z x j` — and the step's target was `∑ k : ι, ∑ i : ι, ∑ j : ι, c.dFamily x k i j * X x k * Y x i * Z x j` ✓. That step is fine.

Then the final alignment step at 556: `_ = ∑ i, ∑ j, fderiv ℝ (fun z => c.g z i j) x (X x) * Y x i * Z x j` — uses fderiv_apply_eq_sum_single — fine.

4. Now line 292's fix (const_smul (2⁻¹)) and hΓ with 2⁻¹ — apply.

Let me now write all these edits.
dsh: reasoning:
Wait — I need to check the dFormOf step that precedes the `_ = ∑ i, ∑ j, (∑ k, ...) * ...` step. Let me view the current state of that region of the file to make sure the steps line up (the previous step had `_ = ∑ k, ∑ i, ∑ j, c.dFamily x k i j * X x k * Y x i * Z x j` from `exact sum_three_cycle`). Then my `rw [sum_cycle_first_to_last]` takes it to `∑ i, ∑ j, ∑ k, ...`. Good.

Let me run the check again.
dsh: reasoning:
Progress. Two issues remain:

1. My new `sum_cycle_first_to_last` lemma has a bug in the last calc step (lines 206-209): the flattening direction. `sum_product'` gives `∑ p : α × β, f p.1 p.2 = ∑ x, ∑ y, f x y` (or the reverse?). The error shows `Finset.sum_product' Finset.univ Finset.univ fun k p => f k p.1 p.2` has type `∑ x, f x.1 x.2.1 x.2.2 = ∑ x ∈ univ, ∑ y, f x y.1 y.2` — so `sum_product' : (∑ p : α × β, f p.1 p.2) = ∑ x ∈ s, ∑ y ∈ t, f x y` — i.e. it flattens a product-sum INTO a double sum (direction: product → double). In the earlier `sum_three_cycle` the usage was `.symm` for double→product and direct for product→double. In my last step I wrote:
```
(∑ q : ι × ι × ι, f q.1 q.2.1 q.2.2)
    = ∑ p : ι × ι, ∑ k : ι, f k p.1 p.2 := by
      simpa using (Finset.sum_product' (s := univ) (t := univ (ι × ι)) (f := fun k p => f k p.1 p.2))
```
`sum_product'` with s : Finset ι, t : Finset (ι × ι): `∑ p : ι × (ι × ι), F p.1 p.2 = ∑ k ∈ univ, ∑ p ∈ univ, F k p` — hmm — the error says the term has type `∑ x, f x.1 x.2.1 x.2.2 = ∑ x ∈ univ, ∑ y, f x y.1 y.2` — so sum_product' flattens the product `ι × (ι × ι)` into `∑ k, ∑ p, f k p.1 p.2`?? — with q.1 k and q.2 = p = (q.2.1, q.2.2) — the LHS `∑ x, f x.1 x.2.1 x.2.2` ✓ (q: ι × (ι × ι)) — RHS `∑ x ∈ univ(ι), ∑ y ∈ univ(ι × ι), f x y.1 y.2` — expected type: `∑ q, f q.1 q.2.1 q.2.2 = ∑ p ∈ univ(ι × ι), ∑ k ∈ univ(ι), f k p.1 p.2` — my `simpa` tried to prove `∑ q... = ∑ p, ∑ k, f k p.1 p.2` from the term giving `∑ q... = ∑ x, ∑ y, f x y.1 y.2` — the RHS's differ by SUM ORDER (k outer vs p outer): `∑ k, ∑ p, f k p.1 p.2` vs `∑ p, ∑ k, f k p.1 p.2` — need sum_comm! So the last step: `∑ p : ι × ι, ∑ k : ι, f k p.1 p.2 = ∑ i, ∑ j, ∑ k, f k i j` — I wrote it backwards. Let me redo:

Goal: `(∑ q : ι × ι × ι, f q.1 q.2.1 q.2.2) = ∑ i : ι, ∑ j : ι, ∑ k : ι, f k i j`. 

Step A: `∑ q : ι × ι × ι, ... = ∑ p : ι × ι, ∑ k, f k p.1 p.2` — via sum_product' with s := univ (ι), t := univ (ι×ι), F k p := f k p.1 p.2: `∑ p : ι × (ι × ι), F p.1 p.2 = ∑ k ∈ univ, ∑ p ∈ univ, F k p` — LHS: `∑ q, f q.1 q.2.1 q.2.2` ✓ RHS: `∑ k, ∑ p, f k p.1 p.2` — that's `∑ k, ∑ p : ι × ι, ...` — NOT `∑ p, ∑ k` — order differs. So use sum_comm: `∑ k, ∑ p, g k p = ∑ p, ∑ k, g k p` — where g k p := f k p.1 p.2 — via `Finset.sum_comm` — hmm, single application on `∑ k, (∑ p, g k p)` — one swap — safe here (no double-sum nesting beyond two levels).

So:
```
    _ = ∑ i : ι, ∑ j : ι, ∑ k : ι, f k i j := by
          calc
            (∑ q : ι × ι × ι, f q.1 q.2.1 q.2.2)
                = ∑ k : ι, ∑ p : ι × ι, f k p.1 p.2 := by
                  simpa using (Finset.sum_product' (s := (Finset.univ : Finset ι))
                    (t := (Finset.univ : Finset (ι × ι))) (f := fun k p => f k p.1 p.2))
            _ = ∑ p : ι × ι, ∑ k : ι, f k p.1 p.2 := by
                  rw [Finset.sum_comm]
            _ = ∑ i : ι, ∑ j : ι, ∑ k : ι, f k i j := by
                  rw [Finset.sum_comm] -- hmm — careful: `∑ p : ι × ι, ∑ k, ...` → `∑ k, ∑ p, ...`?? NO — sum_comm swaps the pair: `∑ a, ∑ b, g a b = ∑ b, ∑ a, g a b` — on `∑ p, (∑ k, f k p.1 p.2)`: gives `∑ k, (∑ p, f k p.1 p.2)` — back! WRONG.
```
For the last step `∑ p : ι × ι, ∑ k, f k p.1 p.2 = ∑ i, ∑ j, ∑ k, f k i j` — use sum_product' (with s := univ (ι × ι) — no wait — product' flattens `∑ p, g p` where p : ι × ι into `∑ i, ∑ j, g (i, j)` — the goal: LHS `∑ p : ι×ι, (∑ k, f k p.1 p.2)` — set g p := ∑ k, f k p.1 p.2: `sum_product' : ∑ p : ι×ι, g p = ∑ i ∈ univ, ∑ j ∈ univ, g (i,j)` = `∑ i, ∑ j, (∑ k, f k i j)` ✓ — PERFECT — use sum_product' directly:
```
            _ = ∑ i : ι, ∑ j : ι, ∑ k : ι, f k i j := by
                  simpa using (Finset.sum_product' (s := (Finset.univ : Finset ι))
                    (t := (Finset.univ : Finset ι)) (f := fun i j => ∑ k : ι, f k i j))
```
Hmm — wait, sum_product' s t f: `∑ p : ι × ι, f p.1 p.2 = ∑ x ∈ s, ∑ y ∈ t, f x y` — with f i j := ∑ k, f k i j ✓.

And the middle step `∑ k, ∑ p : ι × ι, f k p.1 p.2 = ∑ p, ∑ k, f k p.1 p.2` — `rw [Finset.sum_comm]` — single swap on a two-level sum — fine (the earlier corruption was because there were THREE nested sums and rw swapped the outermost leaving a wrong two-level structure — here the term is exactly two levels: `∑ k, (∑ p, ...)` — sum_comm swaps → `∑ p, (∑ k, ...)` ✓ correct).

Also check my FIRST step's `.symm` usage for the middle flattening — `∑ k, ∑ p, f k p.1 p.2 = ∑ q, f q.1 q.2.1 q.2.2` — the simpa used `(sum_product' ...).symm` ✓ (as in sum_three_cycle's analogous step).

2. Line 580: unsolved goals in the `hL`/calc area — line 580:84 — need to see. It's likely the sum_congr + fderiv_mul_mul_apply step or the ring step in the new bullet ordering. Let me look at the actual goal state — the error output got truncated. Let me view lines 570-600 of the file and get the full error.
dsh: reasoning:
Down to one error: line 580:84 — unsolved goals. Line 580 is in the hL calc block — the `· simp; apply Finset.sum_congr rfl; intro j hj; rw [fderiv_mul_mul_apply...]; ring` bullet. Line 580:84 — let me see exactly what's at line 580. The output shows hL succeeded. Let me view lines 574-585.
dsh: reasoning:
Line 580:84 — `(g := fun z : ChartPoint ι => Y z i)` — col 84 is `(hf := hdiff_g i j) (hg := ...)` — hmm, line 580 corresponds to `(hf := hdiff_g i j) (hg := hdiff_Y i) (hh := hdiff_Z j) (v := X x)]`. The unsolved goal error at 580:84 — the goal state shown in the error: it's the hL context, and the goal... it got truncated. Let me get the full error with more context.
dsh: reasoning:
The output is being truncated oddly. Let me save the full output to a file and inspect it.
dsh: reasoning:
The unsolved goal is at line 580 — it's in the hdForm calc's `_ = ∑ i, ∑ j, (∑ k, ...) * ...` step — the goal:

`(∑ i_1, X x i_1 * c.dFamily x i_1 i j) * (Y x i * Z x j) = (∑ k, X x k * c.dFamily x k i j) * Y x i * Z x j`

after `rw [hm, ← Finset.sum_mul]` — the remaining goal: `(∑ i_1, X x i_1 * c.dFamily x i_1 i j) * (Y x i * Z x j) = (∑ k, X x k * c.dFamily x k i j) * (Y x i * Z x j)` — wait, the RHS shows `(∑ k, X x k * c.dFamily x k i j) * Y x i * Z x j` — and LHS `(∑ i_1, X x i_1 * c.dFamily x i_1 i j) * (Y x i * Z x j)` — these differ in `* (Y * Z)` vs `* Y * Z` — `a * (b * c)` vs `(a * b) * c` — a `ring` closes it! The `rw [hm, ← Finset.sum_mul]` rewrote the LHS via hm into `(∑ k, X x k * d x k i j * (Y x i * Z x j))` — hmm — hm's RHS: `∑ k, X x k * c.dFamily x k i j * (Y x i * Z x j)` — then `← Finset.sum_mul` rewrites to `(∑ k, X x k * c.dFamily x k i j) * (Y x i * Z x j)` — wait the goal display shows LHS = `(∑ i_1, X x i_1 * c.dFamily x i_1 i j) * (Y x i * Z x j)` — the summand in the sum-mul-rewritten form is `X x k * c.dFamily x k i j` — hmm — but the display shows the summand as `X x i_1 * c.dFamily x i_1 i j` — hmm, that's `X x i_1 * d x i_1 i j` — hm's RHS summand: `X x k * c.dFamily x k i j * (Y x i * Z x j)` — sum_mul's pattern: `(∑ x ∈ s, f x) * a = ∑ x ∈ s, f x * a` — `←` direction rewrites `∑ k, f k * a` → `(∑ k, f k) * a` with f k := `X x k * c.dFamily x k i j` and a := `Y x i * Z x j` — the result `(∑ k, X x k * c.dFamily x k i j) * (Y x i * Z x j)` ✓. And the target RHS: `(∑ k, X x k * c.dFamily x k i j) * Y x i * Z x j` — remaining difference: associativity `* (Y*Z)` vs `* Y * Z` — ring. So just add `ring` after the rw. The original author's code ended the step with `rw [hm, ← Finset.sum_mul]` — no ring — need to add it.
dsh: reasoning:
The file compiles now (only warnings remain). Let me address the warnings:
1. Line 371:16 warning — `c` unused in `lieBracket` — the def now has explicit `(c : SmoothChartData ι)` but doesn't use it. Fix: name it `_c`? No — field notation requires the name... Actually field notation works with `c.lieBracket X Y` regardless of the binder name. Rename the binder to `(_c : SmoothChartData ι)`? Hmm — underscore-prefixed names suppress the linter and still allow field notation? Field notation looks at the parameter TYPE, not name. Let me use `(_c : SmoothChartData ι)`.

2. The unusedSectionVars warnings (DecidableEq in various theorems) — these are pre-existing warnings in the original (sum_add_split etc. don't use [DecidableEq ι] or even [Fintype]?). To keep the build clean, I could add `set_option linter.unusedSectionVars false` at the top — but the original file had these warnings already and they're just warnings. The audit counts "0 sorry/axioms" — warnings are fine. But cleaner to silence. Actually — careful: modifying might not be worth it; warnings don't fail the build. However the task asks for clean evidence. The warnings existed in the previous invocation's file too. I'll leave the section-vars warnings but fix the `_c` one since it's directly caused by my edit.

Wait, actually — since I'm touching this file anyway, let me add `set_option linter.unusedSectionVars false` — hmm, no — that would hide real issues. The warnings are about section variables in scope but unused — harmless. Leave them.

Fix the `_c` warning:
dsh: reasoning:
`lake build Poincare.D12` succeeds. The smooth module now compiles with:
- `christoffelFamily_smooth` — smooth Christoffel family
- `nabla_smooth`, `lieBracket_smooth` — smoothness of connection/bracket
- `nabla_smul` — Leibniz rule
- `nabla_torsionFree` — torsion-free
- `nabla_metricCompatible` — metric compatibility with actual fderiv
- Plus the two new lemmas: `sum_cycle_first_to_last` (and the file's original `sum_three_cycle`).

Now, checkpoint at 30 min: I should update checkpoint.json and results. But first, let me continue the substantive work for this invocation:

The remaining major item per the task and checkpoint: **curvature from the chart connection**. The task's core asks: "Prove one substantive theorem constructing the smooth Levi-Civita connection or curvature from metric coefficients on a real chart" — connection is done. "with a correctly proved coordinate transformation/naturality or contraction statement" — done via RicciSymmetry (contraction) and the D7 bridge. "Derive one downstream Ricci/scalar identity with theorem provenance" — done (ricci_symm, scalarCurvature_eq_sum_ricci_basis, so3_ricci_e00).

The task also says "Inspect LeviCivita, covariant derivatives, D7 curvature and D9 tensor algebra." — D9 tensor algebra inspection — I should check what D9 is and connect. Let me check D9 sources.

For this invocation, priorities:
1. ✓ Fix ChartLeviCivitaSmooth (done) — this was the broken state.
2. Update checkpoint.json + results (at least every 30 min — I'm at ~30 min in).
3. Substantive addition: the **curvature of the smooth chart Levi-Civita connection** — the component formula and its properties, plus maybe a 2D non-flat model (sphere metric) — this would fully close "constructing the smooth Levi-Civita connection or curvature from metric coefficients on a real chart".

Wait — but curvature needs fderiv of the christoffelFamily (second derivatives of g appear via fderiv of fderiv — but there's the classical cancellation: in the curvature formula R(X,Y)Z = ∇X∇YZ - ∇Y∇XZ - ∇[X,Y]Z, the second-derivative terms of Z cancel, leaving fderiv of Γ terms — fderiv of christoffelFamily needs fderiv of (gInv * d) — d is fderiv g — so fderiv d = second fderiv of g — needed! So the curvature DOES involve second Frechet derivatives of g. That's heavier: need fderiv of the product gInv·d, and for the compatibility identity, the second derivative of g appears — with symmetry (Schwarz: fderiv of fderiv symmetric for C² — `second_derivative_symmetric`). Hmm — that's a substantial but doable lemma in this mathlib: `fderiv_fderiv_symm`? There's `second_derivative_symmetric` in Analysis.Calculus.ContDiff. The metric-compat of curvature (⟨R(X,Y)Z,W⟩ = -⟨R(X,Y)W,Z⟩) or the algebraic Bianchi identity would require Schwarz.

Scope decision for THIS 4-hour invocation: 
- The module is fixed and builds. 
- Run the full audit + negative control + axiom checks (the checkpoint says these were passing before; must re-run since I changed the file).
- Then add the curvature family module: `chartCurvatureFamily` computing R from Γ and its fderivs, proving at minimum: (a) the curvature vanishes identically when Γ ≡ 0? trivial. Better: (b) an explicit non-flat 2D chart model with nonzero curvature — the sphere stereographic metric g(x,y) = 4/(1+x²+y²)² δ — computing R(e₁,e₂)e₁ = 4e₂ ≠ 0 requires: explicit g, gInv, fderivs — on ℝ² with the given formulas — CONCRETE computation, verifiable by `norm_num`/ring with explicit second derivatives. This is a concrete model computation (non-vacuity), doable in finite time since everything is explicit.

Hmm — but honestly, given the invocation budget (4 hours, already ~45 min in), and the remaining mandatory items (audit rerun, checkpoint/results updates with fresh hashes, full build), the priority order:
1. Re-run full audit + axiom evidence (required for every new declaration).
2. Update checkpoint.json + results (required every 30 min).
3. Curvature family (if time permits — strong value-add).

Also inspect D9 to honor "Inspect ... D9 tensor algebra".

Let me first check the audit infrastructure: Audit/D12/D12Audit.lean and the logs dir, negcontrol. And D9. Then decide.
dsh: reasoning:
D9 doesn't exist yet as a module in the release (it's another worker's task — "DeTurck/ParabolicEstimates APIs" / "tensor algebra" mentioned in other workers' objectives). The task says "Inspect ... D9 tensor algebra" — D9's module doesn't exist in this snapshot (it's a parallel worker). I should record this in the checkpoint as a dependency note: D9 tensor algebra API absent from this snapshot. Let me double-check by searching the whole release tree for tensor-algebra-like modules (maybe under a different name).

Also check Stage1/CurvatureAlgebra (D7/Stage1 interface) which the checkpoint mentions as `CurvatureOperator` — that's the D7 tensor data bridge, already used.

Let me check what modules exist and then re-run the audit.
dsh: reasoning:
Audit PASSES: 228 project declarations, 172 theorems, 0 axioms/sorry/unsafe/native_decide/proof_wanted. Cones only {propext, Classical.choice, Quot.sound}, {propext}, {propext, Quot.sound}.

Note: D9 tensor algebra — no D9 module exists in this snapshot (it's a parallel worker's task). Stage1/CurvatureAlgebra is the D7 interface used. I'll record the D9 inspection finding in the checkpoint.

Now let me:
1. Update checkpoint.json + results (it's been ~35 min since start; the high-frequency rule says update every 30 min).
2. Then start the substantive curvature-family work.

For the curvature family module — plan:
`ChartCurvature.lean` (new module):
- `chartRiemann` or extend: `curvatureFamily c x k i j l := fderiv of Γ` — needs `fderiv ℝ (fun z => c.christoffelFamily z k j l) x (e_i)`-ish terms.
- Component formula: `Rᵏᵢⱼₗ = ∂ᵢΓᵏⱼₗ − ∂ⱼΓᵏᵢₗ + Σₘ (ΓᵏᵢₘΓᵐⱼₗ − ΓᵏⱼₘΓᵐᵢₗ)`.
- Prove the algebraic structure: skew in (i,j), and metric-compatible curvature (⟨R(X,Y)Z,W⟩ = −⟨R(X,Y)W,Z⟩)? The metric-compat skew needs the derivative identity ∇g = 0 for ∂Γ... it requires differentiating the ∇g=0 identity — second derivatives of g with Schwarz. HEAVY.

Simpler valuable target that avoids second-derivative-of-g-Symmetry pain: 
**Compute curvature for a CONCRETE nonconstant metric family on a 2D chart with explicit formulas** — e.g. the sphere stereographic metric: g(x,y) = (4/(1+x²+y²)²)·δ on ℝ². Then:
- Γ¹₁₁(x,y) = -2x/(1+x²+y²), Γ¹₁₂ = -2y/(1+r²), Γ¹₂₂ = 2x/(1+r²) etc. (classical for g = 4/(1+r²)²δ — hmm let me recompute. For the metric g_{ij} = (4/(1+r²)²) δ_{ij}, the Christoffel symbols: Γᵏᵢⱼ = (1/2) g^{kl} (g_{jl,i} + g_{il,j} − g_{ij,l}). With φ = ln(4/(1+r²)²) = ln 4 − 2 ln(1+r²), ∂ᵢφ = −4xᵢ/(1+r²). g_{ij} = e^φ δ_{ij}. ∂_i g_{jk} = φ_i e^φ δ_{jk}. So Γᵏᵢⱼ = (1/2) e^{-φ} δ^{kl} e^φ (φ_j δ_{kl}?? let me do it directly: Γ^k_{ij} = (1/2) (φ_j δ_{ki} + φ_i δ_{kj} − φ_k δ_{ij}).
  For k=i=j: Γ¹₁₁ = (1/2) φ₁ = −2x/(1+r²). ✓
  Γ¹₁₂ = (1/2)(φ₂ δ₁₁ + φ₁ δ₁₂ − φ₁ δ₁₂) = (1/2)φ₂ = −2y/(1+r²).
  Γ¹₂₂ = (1/2)(φ₂δ₁₂ + φ₂δ₁₂ − φ₁δ₂₂) = −(1/2)φ₁ = 2x/(1+r²). ✓
  Γ²₁₁ = −(1/2)φ₂ = 2y/(1+r²). Γ²₁₂ = (1/2)φ₁ = −2x/(1+r²). Γ²₂₂ = (1/2)φ₂ = −2y/(1+r²).
  Then R¹₂₁₂-type: R(X,Y)Z = ∇X∇YZ − ∇Y∇XZ − ∇[X,Y]Z; in coordinates R^l_{ijk} = ∂ᵢΓᵏ... standard: R(e₁,e₂)e₁ at x=0: For the sphere of radius 1/2 (curvature 4): R(e₁,e₂)e₁ = K·(⟨e₂,e₁⟩e₂ − ⟨e₁,e₁⟩e₂)... K = 4, g(e₁,e₁) = 4. R(e₁,e₂)e₁ = 4·(0·e₂ − 4·e₂)·(1/4) = −4·e₂·... let me compute directly with components at origin: R(e₁,e₂)e₁ = R^l_{112}·... R^k_{112} = ∂₁Γᵏ₂₁ − ∂₂Γᵏ₁₁ + Γᵏ₁ₘΓᵐ₂₁ − Γᵏ₂ₘΓᵐ₁₁.
  Γ¹₂₁ = Γ¹₁₂ = −2y/(1+r²); ∂₁Γ¹₂₁ = ∂ₓ(−2y/(1+r²)) = 4xy/(1+r²)² = 0 at 0.
  ∂₂Γ¹₁₁ = ∂_y(−2x/(1+r²)) = 4xy/(1+r²)² = 0 at 0.
  Γ¹₁ₘΓᵐ₂₁ = Γ¹₁₁Γ¹₂₁ + Γ¹₁₂Γ²₂₁ = (−2x/(1+r²))(−2y/(1+r²)) + (−2y/(1+r²))(−2x/(1+r²)) = 8xy/(1+r²)² = 0 at 0.
  Γ¹₂ₘΓᵐ₁₁ = Γ¹₂₁Γ¹₁₁ + Γ¹₂₂Γ²₁₁ = (−2y/(1+r²))(−2x/(1+r²)) + (2x/(1+r²))(2y/(1+r²)) = 0.
  So R¹₁₁₂ = 0 at 0?? That's wrong — for the sphere, R(e₁,e₂)e₂ should be nonzero: R^k_{212} = ∂₁Γᵏ₂₂ − ∂₂Γᵏ₁₂ + Γᵏ₁ₘΓᵐ₂₂ − Γᵏ₂ₘΓᵐ₁₂.
  R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₁₂ + Γ¹₁ₘΓᵐ₂₂ − Γ¹₂ₘΓᵐ₁₂
    ∂₁Γ¹₂₂ = ∂ₓ(2x/(1+r²)) = 2(1+r²−2x²)/(1+r²)² = 2 at 0.
    ∂₂Γ¹₁₂ = ∂_y(−2y/(1+r²)) = −2(1+r²−2y²)/(1+r²)² = −2 at 0.
    So R¹₂₁₂(0) = 2 − (−2) + 0 − 0 = 4. So R(e₂,e₁)e₂ = 4e₁ at origin ✓ (sphere with K=4, g(e₂,e₂)=4: R(e₂,e₁)e₂ = R^l_{212}e_l — hmm — the geodesic curvature formula: R(e₁,e₂)e₂ = K(⟨e₂,e₂⟩e₁ − ⟨e₁,e₂⟩e₂) = 4·4·e₁ = 16e₁?? Hmm — but R^l_{ijk} as computed: R(e_j, e_k) e_i = R^l_{ijk} e_l. R^1_{212} = 4 at 0 means R(e₁,e₂)e₂ = 4e₁ + R²₂₁₂e₂. R²₂₁₂ = ∂₁Γ²₂₂ − ∂₂Γ²₁₂ + ... = ∂ₓ(−2y/(1+r²)) − ∂_y(−2x/(1+r²)) = 4xy/(1+r²)² + 4xy/(1+r²)² = 0 at 0; Γ²₁ₘΓᵐ₂₂ − Γ²₂ₘΓᵐ₁₂: Γ²₁ₘΓᵐ₂₂ = Γ²₁₁Γ¹₂₂ + Γ²₁₂Γ²₂₂ = (2y/(1+r²))(2x/(1+r²)) + (−2x/(1+r²))(−2y/(1+r²)) = 0; Γ²₂ₘΓᵐ₁₂ = Γ²₂₁Γ¹₁₂ + Γ²₂₂Γ²₁₂ = (−2x/(1+r²))(−2y/(1+r²)) + (−2y/(1+r²))(−2x/(1+r²)) = 0. So R(e₁,e₂)e₂ = 4e₁ at 0. And ⟨R(e₁,e₂)e₂, e₁⟩ = g₁₁·4 = 4·4 = 16 = K·(g₁₁g₂₂ − g₁₂²) = 4·(4·4−0) = 64?? MISMATCH. Hmm — ⟨R(e₁,e₂)e₂,e₁⟩ = K(g₁₁g₂₂ − g₁₂²) = 4·16 = 64. So R(e₁,e₂)e₂ should be 16e₁ (in terms of ⟨·,e₁⟩: ⟨16e₁, e₁⟩ = 16·4 = 64 ✓). So R¹₂₁₂(0) should be 16, not 4?! Let me recompute ∂₁Γ¹₂₂: Γ¹₂₂ = 2x/(1+r²). ∂ₓ = 2(1+r²)−2x·2x over (1+r²)² = (2+2x²+2y²−4x²)/(1+r²)² = (2−2x²+2y²)/(1+r²)² = 2 at (0,0) ✓. ∂₂Γ¹₁₂: Γ¹₁₂ = −2y/(1+r²). ∂_y = −2(1+r²)+2y·2y over... = (−2−2x²−2y²+4y²)/(1+r²)² = (−2−2x²+2y²)/(1+r²)² = −2 at (0,0) ✓. So R¹₂₁₂ = 2 − (−2) = 4. Then ⟨R(e₁,e₂)e₂,e₁⟩ = g₁₁·R¹₂₁₂ = 4·4 = 16 ≠ 64?! Hmm — where's the discrepancy? The formula R^l_{ijk} = ∂ᵢΓˡⱼₖ − ∂ⱼΓˡᵢₖ + ΓˡᵢₘΓᵐⱼₖ − ΓˡⱼₘΓᵐᵢₖ — indices: R(e_i, e_j) e_k = R^l_{kij} e_l with R^l_{kij} = ∂ᵢΓˡⱼₖ... hold on — the standard: R(∂ᵢ, ∂ⱼ)∂ₖ = R^l_{kij} ∂ₗ where R^l_{kij} = ∂ᵢ Γ^l_{jk} − ∂ⱼ Γ^l_{ik} + Γ^l_{im} Γ^m_{jk} − Γ^l_{jm} Γ^m_{ik}. So R(e₂,e₁)e₂ = R^l_{212}e_l with R^l_{212} = ∂₂Γˡ₁₂ − ∂₁Γˡ₂₂ + Γˡ₂ₘΓᵐ₁₂ − Γˡ₁ₘΓᵐ₂₂.
   R¹₂₁₂ = ∂₂Γ¹₁₂ − ∂₁Γ¹₂₂ + ... = −2 − 2 = −4 at origin. So R(e₂,e₁)e₂ = −4e₁?? Then ⟨R(e₂,e₁)e₂,e₁⟩ = −16, but K-formula: ⟨R(e₂,e₁)e₂, e₁⟩ = K(⟨e₂,e₁⟩² − ⟨e₂,e₂⟩⟨e₁,e₁⟩) = 4(0 − 16) = −64. Hmm STILL mismatch (sign conventions!). Let me carefully redo with a known convention. Standard (Lee, do Carmo): R(X,Y)Z = ∇X∇YZ − ∇Y∇XZ − ∇[X,Y]Z. Riemann tensor components: Rm(X,Y,Z,W) = ⟨R(X,Y)Z, W⟩. Sectional curvature of plane span(e₁,e₂): K = Rm(e₁,e₂,e₂,e₁)/(g₁₁g₂₂ − g₁₂²). For the sphere of radius ρ with metric g = (4/(1+r²)²)δ — stereographic projection of the sphere of radius ρ: g = (4ρ⁴/(ρ²+r²)²)δ? Hmm — actually stereographic: for the unit sphere (radius 1), g_{ij} = 4δ_{ij}/(1+r²)² — K = 1. For radius ρ: g = 4ρ²δ/(1+r²/ρ²)²... let me just take the unit sphere metric g = 4δ/(1+r²)², K = 1.

   With K = 1: Rm(e₁,e₂,e₂,e₁) = K·(g₁₁g₂₂ − g₁₂²) = 1·(4·4 − 0) = 16 at origin. R(e₁,e₂)e₂ = Rm(·,e₂,e₂,e₁)... In components: ⟨R(e₁,e₂)e₂, e₁⟩ = 16. With R^l_{212} = ∂₂Γˡ₁₂ − ∂₁Γˡ₂₂ + ... : R¹₂₁₂ = ∂₂Γ¹₁₂ − ∂₁Γ¹₂₂ + Γ¹₂ₘΓᵐ₁₂ − Γ¹₁ₘΓᵐ₂₂. ∂₂Γ¹₁₂ = −2 (computed above), ∂₁Γ¹₂₂ = 2. So R¹₂₁₂ = −4, giving ⟨R(e₁,e₂)e₂,e₁⟩ = g₁₁R¹₂₁₂ = −16 — wrong sign. So the index convention I used for R^l_{kij} must be flipped: R^l_{kij} = ∂ᵢΓˡₖⱼ − ∂ⱼΓˡₖᵢ + ΓˡᵢₘΓᵐₖⱼ − ΓˡⱼₘΓᵐₖᵢ. Let me redo: R^l_{212} = ∂₁Γˡ₂₂ − ∂₂Γˡ₂₁ + Γˡ₁ₘΓᵐ₂₂ − Γˡ₂ₘΓᵐ₂₁. R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₂₁ + Γ¹₁ₘΓᵐ₂₂ − Γ¹₂ₘΓᵐ₂₁ = 2 − (−2) + 0 − 0 = 4. ⟨R(e₁,e₂)e₂,e₁⟩ = 4·4 = 16 ✓ K = 16/16 = 1 ✓✓. GREAT: so with the convention R^l_{kij} = ∂ᵢΓˡₖⱼ − ∂ⱼΓˡₖᵢ + ΓˡᵢₘΓᵐₖⱼ − ΓˡⱼₘΓᵐₖᵢ, the sphere metric gives R¹₂₁₂(0) = 4 ≠ 0.

   So a concrete 2D model: g(x,y) = 4/(1+x²+y²)²·δ, gInv = ((1+r²)²/4)·δ — g·gInv = 1 ✓ (pointwise — need inv_mul proof: (1+r²)²/4 · 4/(1+r²)² = 1 ✓ by field_simp/ring).

   Curvature at origin: R¹₂₁₂ = 4, R²₂₁₂ = 0, and by symmetry R(e₁,e₂)e₂ = 4e₁ ≠ 0. This gives a NONZERO-curvature chart model — computing R via explicit derivatives of Γ.

   Computing this in Lean requires:
   - ChartPoint (Fin 2) → ℝ — 2D chart; write x = (x₀, x₁).
   - Explicit g, gInv, dFamily (fderiv of explicit functions!) — the dFamily is fderiv of `fun z => c.g z j k` in direction e_i — for explicit polynomial/rational functions, fderiv computation in Lean is painful! The fderiv of 4/(1+z₀²+z₁²)² requires proving the derivative formula via hasFDerivAt lemmas — `hasFDerivAt_of_fun`? There are lemmas: HasFDerivAt.mul, HasFDerivAt.inv, HasFDerivAt.pow, HasFDerivAt.proj... — doable but tedious. Alternatively — since the SmoothChartData's dFamily is derived from fderiv, computing it means proving fderiv formulas. That's a chunk of work but standard: for functions on ℝ² like `fun z : Fin 2 → ℝ => 4/(1+z 0²+z 1²)²` — I can prove `fderiv ℝ g x = -16(1+r²)⁻³ · (x 0, x 1)`-style formulas via fderiv composition lemmas... it's genuinely doable but time-consuming (each derivative lemma needs HasFDerivAt reasoning).

   ALTERNATIVE — avoid the sphere (rational) and use a POLYNOMIAL metric! g = 1 + x²y²? Curvature nonzero somewhere? Hmm — the 1D model already exists (ChartModel1D) and 1D curvature = 0. For a 2D chart with a polynomial metric, the curvature formulas involve polynomial Γ and polynomial ∂Γ — computable by ring/norm_num without fderiv lemmas — BUT the dFamily derivation (fderiv of g coefficients) is the bridge: the whole SmoothChartData machinery uses actual fderiv. Hmm — wait — do I need the SmoothChartData machinery for the curvature model? The curvature component formula needs ∂ᵢΓ — derivatives of the Christoffel family — I can define a SEPARATE explicit curvature construction: define explicit smooth families G, GInv with explicit derivative data (not fderiv-derived but PROVED to equal fderiv via HasFDerivAt — or just define a self-contained structure with derivative fields).

   Hmm — actually — the honest boundary matters: the task wants "smooth Levi-Civita connection or curvature from metric coefficients on a real chart". The connection construction (SmoothChartData → nabla) is done and uses ACTUAL fderiv for the derivative data. For the curvature, the natural continuation is `fderiv` of the Christoffel family — which requires differentiating gInv·(d) — needs fderiv of gInv (data, fine — actually we'd need fderiv of gInv as new data) and fderiv of d (= second derivative of g — needs new smoothness and Schwarz for symmetry properties).

   Time-box decision: Given ~3 hours left in this invocation and the mandatory bookkeeping (checkpoint, results, audit, hashes), I'll scope the curvature work as follows:

   **New module `ChartCurvature.lean`**: 
   1. Extend the pointwise algebraic core: prove the pointwise curvature component formula has the right algebraic identities: `R^k_{jil} = -R^k_{ijl}` (skew-symmetry) and first Bianchi `R^k_{ijl} + R^k_{jli} + R^k_{lij} = 0` — pure algebra from Γ data (no derivatives) — these are TRUE for ANY torsion-free connection's curvature components... wait — the Bianchi identity needs ∂Γ symmetry (Schwarz) in general! For the ALGEBRAIC first Bianchi of the curvature TENSOR of an abstract connection: R(X,Y)Z + R(Y,Z)X + R(Z,X)Y = T-terms — for torsion-free connections, the algebraic Bianchi holds and its component version involves ∂ᵢ∂ⱼ − ∂ⱼ∂ᵢ cancellation = Schwarz. So pointwise algebraic-only Bianchi is FALSE in general (needs ∂-commutation). Skew R^k_{jil} = −R^k_{ijl} IS purely algebraic ✓.
   2. `chartRiemannComponent` as a field on a datum with second-derivative data: `curvatureFamily c x k i j l := ∂Γ-term` — with `dΓ` data: fderiv of christoffelFamily — hmm.

   Honest scope: define a NEW datum `CurvatureChartData` — smooth g with fderiv-derived d AND fderiv-derived second data? That's heavy.

   SIMPLER HIGH-VALUE: **the naturality statement** — the task allows "coordinate transformation/naturality OR contraction". We have contraction (RicciSymmetry). A "coordinate transformation" statement for the chart connection: under a LINEAR change of coordinates on the chart, the Christoffel family transforms by the classical law and nabla/curvature are natural. Hmm — also nontrivial.

   Given the remaining budget, the highest-value closure is actually: (a) keep the module fixed + audited; (b) add the **pointwise curvature algebra module**: define `chartRiemannComponent` from Christoffel + dΓ data at a point (as a *structure field* — honest: the ∂Γ data is an input at the algebraic level), prove skew-symmetry and the Bianchi-with-Schwarz... no — at the algebraic level with dΓ as data, prove: skew in (i,j), and the identity relating the curvature to (∇∇−∇∇−∇[,]) on constant coefficient fields (the "curvature operator" at the pointwise level) — i.e., prove that the chart curvature operator equals the commutator expression on pointwise data — that's provable purely algebraically from the formula. AND prove the so(3)/sphere-type nonzero witness.

   Hmm — actually, let me reconsider the sphere model more carefully, because a NONZERO CURVATURE chart model is the single most valuable remaining item (the 1D model has zero curvature; so(3) has nonzero curvature but it's the Lie-model, not the chart-model). The task says "Begin with a nonconstant smooth metric example if needed" — the chart metric with nonzero curvature = the substantive "curvature from metric coefficients on a real chart" piece.

   To make the sphere model tractable, I can bypass fderiv gymnastics by defining the model INSIDE the existing framework cleverly:
   - Define a `SmoothChartData (Fin 2)` with g = 4/(1+r²)²δ — but dFamily is fderiv-derived — I need fderiv proofs of the explicit functions. Doable with `hasFDerivAt` lemmas? For `fun z => 4 / (1 + z 0 ^ 2 + z 1 ^ 2) ^ 2` — derivative w.r.t. z0 at x: -16·x0/(1+r²)³. Proving `fderiv ℝ (fun z : Fin 2 → ℝ => (1 + z 0 ^ 2 + z 1 ^ 2) ^ 2) x = ...` — I'd need to build HasFDerivAt for proj functions: `HasFDerivAt.proj`? There are lemmas `hasFDerivAt_proj`/`fderiv_proj` — `Pi.single`-related: `hasFDerivAt_pi`, and for the components: fderiv of `fun z => z i` is `Pi.single i 1` — hmm, actually there's `fderiv_proj`? Let me search mathlib for `fderiv_proj` / `hasFDerivAt_proj`. I recall: `@[simp] theorem fderiv_proj {i : ι} : fderiv 𝕜 (fun x : ι → E => x i) x = proj i`? Something like that. And fderiv of pow: `fderiv_pow`? `HasFDerivAt.pow`, `fderiv_pow`. fderiv of inv: `fderiv_inv`. fderiv of div... Composition via `fderiv.comp` / chain rule `HasFDerivAt.comp`. This is all standard calculus but each step needs care in Lean. Time: maybe 1-2 hours of careful work. Risky within budget but valuable.

   Alternative pragmatic model: build the 2D model on the ALGEBRAIC pointwise datum (ChartMetricCoefficients) with EXPLICIT derivative fields (declared as data), and separately prove the smoothness of everything (all explicit functions are ContDiff) WITHOUT deriving d from fderiv. I.e., a parallel structure `ExplicitSmoothChartData` with fields: g, gInv, d, dΓ (derivative of christoffel), all ContDiff, with the algebraic hypotheses — and a theorem that if d agrees with the fderiv... no — that re-introduces the bridge.

   OR — use `ChartMetricCoefficients` pointwise at each point with explicit d and dΓ — and PROVE the curvature identity R¹₂₁₂ ≠ 0 at the origin with explicit values. The "from metric coefficients" requirement is satisfied at the algebraic level (ChartLeviCivita proves christoffel/∇g=0 identities from coefficients). For the model, the coefficient data is explicit numbers/rational functions — the proof computes R¹₂₁₂(0) = 4 ≠ 0 by direct evaluation. This is honest: it's a MODEL (recorded as such) at the pointwise level, and the smoothness of the explicit families (g, gInv, d, dΓ as ContDiff functions) is provable via ContDiff combinators (polynomial/rational — `ContDiff.inv` etc. — or even easier: polynomial in x0, x1 and (1+r²)⁻¹ factors — ContDiff since 1+r² > 0 — needs the denominator nonzero — ContDiff.inv requires ≠ 0 — fine, 1+r² ≥ 1).

   Hmm — but honestly the cleanest value-add with guaranteed completion in this window:

   **`ChartCurvature.lean` with:**
   1. Pointwise curvature component `Rcomp` from a datum with Christoffel + dΓ data (algebraic, honest field) — hmm, this adds a new structure with new hypotheses — more "algebraic core" — of limited new value.

   Let me instead do the **sphere model with fully explicit pointwise computation at the origin plus smoothness proofs**, and a **general lemma**: for the smooth chart datum, IF the Christoffel family is given as an explicit smooth function family agreeing pointwise with christoffelFamily (provable via the existing machinery), then the curvature component formula at a point is a polynomial in (gInv, d, dΓ) values — too abstract to matter.

   Hmm — wait. Actually, let me reconsider what gives the task real closure. Re-reading the objective: "Prove one substantive theorem constructing the smooth Levi-Civita connection or curvature from metric coefficients on a real chart, with a correctly proved coordinate transformation/naturality or contraction statement." — CONNECTION construction ✓ DONE (nabla_metricCompatible with actual fderiv). "Derive one downstream Ricci/scalar identity with theorem provenance" ✓ DONE (ricci_symm etc.). "Begin with a nonconstant smooth metric example if needed, but record that it is a model" ✓ DONE (ChartModel1D, so3). So the MILESTONE is already met by invocation 1's work + my fixes. The remaining opportunities are strengthening: nonzero curvature on a chart (2D sphere model) or the curvature component formula theorem.

   Given the 4-hour window and the requirement of "compile-checked checkpoint at least every 30 minutes" + final results/JSON with fresh hashes + full build + full audit + a final verification pass — I'll allocate:
   - ~1.5h: sphere 2D model with nonzero curvature (best value-add; strengthens "curvature from metric coefficients").
   - ~0.5h: bookkeeping (checkpoint, results, hashes, logs).
   - Rest: final build + audit + verification.

   Let me first update the checkpoint (30-min mark), then dive into the sphere model.

   For the sphere model, plan the module `SphereChartModel.lean`:
   - ι = Fin 2; ChartPoint (Fin 2) → ℝ. Write components via x 0, x 1. (Fin 2 has DecidableEq, Fintype — fine.)
   - r2 x := x 0^2 + x 1^2; denom x := 1 + r2 x.
   - g x i j := if i = j then 4 * (denom x)⁻² else 0 — i.e. 4/denom² · δ.
   - gInv x i j := if i = j then (denom x)² / 4 else 0 — hmm — (denom²)·4⁻¹. inv_mul: for k = j: Σ_l gInv k l · g l j = gInv k j · g j j (only l = j contributes since g l j = 0 for l ≠ j) = (denom²/4)·(4/denom²) = 1 ✓; for k ≠ j: gInv k l = 0 for l ≠ k; l = j ≠ k → gInv k j = 0 — so sum = 0 ✓.
   - ContDiff smoothness of g, gInv: ContDiff of `fun x => 4 * (1 + x 0 ^ 2 + x 1 ^ 2)⁻²` — via ContDiff combinators + `ContDiff.inv`? — for `(1+r²)⁻¹`-type: `ContDiff.inv` needs the function never zero: 1 + r² ≥ 1 — need a proof `1 + x 0^2 + x 1^2 ≠ 0` — via nlinarith (squares ≥ 0). Fine.
   - dFamily derived: fderiv of g components — THE HARD PART. For the explicit formula ∂ᵢ(4/denom²) = −16 xᵢ/denom³ — proving via HasFDerivAt chain. Hmm — in this mathlib, is there `fderiv_pow`, `fderiv_inv`? Let me check. There's `HasFDerivAt.inv : HasFDerivAt f f' x → f x ≠ 0 → HasFDerivAt (fun x => (f x)⁻¹) (-(f x)⁻² • f') x`? or `fderiv_inv`. And `fderiv_pow (n)`? For pow: `HasFDerivAt.pow (hf) : HasFDerivAt (fun x => f x ^ n) ((n • f x ^ (n-1)) • f') x`. And projections: `hasFDerivAt_proj`? hmm — there's `fderiv_proj`? Let me grep mathlib.
   - Christoffel at origin: Γ via christoffelFamily — then evaluate — the fderiv direction e_i = Pi.single i 1.
   - R¹₂₁₂ (origin) = ∂₁Γ¹₂₂ − ∂₂Γ¹₂₁ + Γ¹₁ₘΓᵐ₂₂ − Γ¹₂ₘΓᵐ₂₁ = 4 — needs ∂Γ = fderiv of christoffelFamily — fderiv of a sum of products of (gInv · (d-terms)) — d-terms are fderivs — second derivatives appear — fderiv of fderiv — OUCH. This is where it gets really heavy: fderiv of `fun x => fderiv ℝ (fun y => c.g y j k) x (single i 1)` — the second Frechet derivative — mathlib has `fderiv_fderiv`? There's `second_derivative` machinery — `fderiv_fderiv_apply`? This could sink hours.

   PRAGMATIC ALTERNATIVE for the model: skip the fderiv-derived d and use the **pointwise algebraic datum** `ChartMetricCoefficients (Fin 2)` at the ORIGIN with EXPLICIT d-values matching the true derivatives (declared as values with explicit formulas: d i j k = ∂ᵢg_jk(x) computed by hand at x=0 — for the sphere at origin: ∂ᵢg_{jk}(0) = 0! All first derivatives vanish at the origin — d ≡ 0 at origin — then Γ(0) = 0! and the curvature comes entirely from ∂Γ: R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₂₁ = ∂₁Γ¹₂₂ − 0...). But the pointwise datum only has d (first derivatives) — the curvature component formula needs dΓ (fderiv of Γ) — not part of ChartMetricCoefficients. So the pointwise-algebraic route can't express R.

   So expressing the curvature needs a datum with dΓ. Define a NEW algebraic structure in the model file (self-contained): `SphereOriginCurvatureDatum` — hmm, this drifts toward "computation with assumed derivative values" — the honest thing is to PROVE the fderiv values from the explicit formulas.

   Time-box decision: attempt the full sphere model with fderiv proofs for at least the FIRST derivative (dFamily) and see how far I get in ~60-75 min; if second-derivative (fderiv-of-fderiv) proofs prove too costly, fall back to: proving the explicit dΓ values via `HasFDerivAt` on the EXPLICIT Γ formulas (defined as explicit functions, then proved equal to christoffelFamily via the algebraic identities — a bridge theorem: `sphere_christoffel_formula : christoffelFamily c x k i j = explicit formula` — provable by the algebraic core if dFamily equals the explicit d — which needs fderiv of g... still needs first-derivative fderiv proofs).

   Hmm — the alternative fallback: define the sphere datum with the derivative data as DATA (an honest structure with explicit d and dΓ fields that I PROVE are the actual fderivs only for the first level — or not at all, recording "derivative fields declared as data, validated numerically... no — no native_decide.

   Let me look at what fderiv lemmas exist for concrete computation: check `fderiv_proj`, `HasFDerivAt.proj`, `fderiv_pow`, `fderiv_inv`, `fderiv_comp` in pinned mathlib. If good ones exist, the first-derivative computation is very doable: g = 4·(1+r2)⁻² where r2 = x0² + x1². 

   Actually — even better idea to cut complexity: take a metric with POLYNOMIAL coefficients so derivatives are polynomial: g(x,y) = 1 + x²·y²... its Γ involves 1/(1+x²y²)-type factors (inverse metric) — gInv = 1/g — fderiv of 1/g — inv lemma needed again. UNLESS g is constant determinant... any 2D diagonal metric g = diag(a(x), b(x)) has gInv = diag(1/a, 1/b) — invs needed.

   What about a metric where EVERYTHING is polynomial: g = I + ε·h with h small — gInv = (I+εh)⁻¹ — infinite series — no.

   OK — the inv/pow fderiv route with the sphere is the way. Let me check the available lemmas first, then commit ~75 minutes to the model.

   Also: maybe prove dFamily for the sphere via a SMARTER route: fderiv is linear in directions; dFamily x i j k = fderiv g_jk x e_i. For the sphere: g_jk(x) = 4·(1+r2(x))⁻²·δ_jk. The fderiv of `4·inv^2`: use `fderiv_mul`, `fderiv_inv`, `fderiv_pow`, `fderiv_const`, `fderiv_add`, `fderiv_proj`... The function `fun x => (1 + x 0^2 + x 1^2)` — fderiv = sum of const + two squares — `fderiv_pow (2)` on `fun x => x 0` — fderiv of `x 0` = proj: `fderiv_proj`-like — let me check names.

   Let me also decide the second-derivative issue for ∂Γ: ∂Γ = fderiv of christoffelFamily = fderiv of Σ gInv·d — needs fderiv of d = fderiv of fderiv. mathlib: `fderiv_fderiv`? There's `HasFDerivAt.fderiv` / `ContDiff.fderiv` machinery and `fderiv_fderiv_comm`-ish results under C² (Schwarz). For EXPLICIT computations at a point, one can use `second_derivative_symmetric` — hmm.

   HOLD ON — simpler idea for the model's curvature at the ORIGIN: compute R¹₂₁₂(0) = ∂₁Γ¹₂₂(0) − ∂₂Γ¹₂₁(0) + ΓΓ terms (which vanish since Γ(0)=0 — Γ(0) = 0 because d(0)=0 ✓). So R¹₂₁₂(0) = ∂₁Γ¹₂₂(0) − ∂₂Γ¹₂₁(0) — a DIRECTIONAL second-derivative-like object. Now Γ¹₂₂(x) = Σ_l gInv¹ˡ(x)·(1/2)(d₂₂ₗ + d₂₂ₗ − dₗ₂₂)(x) = (1/2)gInv¹¹(x)(2d₂₂₁(x) − d₁₂₂(x)) — for the sphere: Γ¹₂₂ = (1/2)·(denom²/4)·(2·(−16x₁·δ? hmm — dᵢg_{jk} = ∂ᵢ(4/denom²)δ_jk = −16xᵢ/denom³·δ_jk. d₂₂₁ = ∂₂g₂₁ = ∂₂(4/denom²·δ₂₁) = −16x₂/denom³·δ₂₁. d₁₂₂ = −16x₁/denom³·δ₂₂ = −16x₁/denom³. So Γ¹₂₂(x) = (1/2)(denom²/4)(2·(−16x₂/denom³·δ₂₁) − (−16x₁/denom³)) — δ₂₁ = 0 — = (1/2)(denom²/4)(16x₁/denom³) = 2x₁/denom ✓ (matches the hand computation). Then ∂₁Γ¹₂₂(0) = 2 at origin. And Γ¹₂₁ = (1/2)(denom²/4)(d₁₂₁ + d₂₁₁ − d₁₂₁) — d₁₂₁ = ∂₁g₂₁ = −16x₁/denom³δ₂₁ = 0; d₂₁₁ = ∂₂g₁₁ = −16x₂/denom³ — Γ¹₂₁ = (1/2)(denom²/4)(−16x₂/denom³) = −2x₂/denom ✓. ∂₂Γ¹₂₁(0) = −2. So R¹₂₁₂(0) = 2 − (−2) = 4 ✓.

   So to compute R¹₂₁₂(0), I need DIRECTIONAL fderivs of explicit functions like `x ↦ 2x₁/(1+x₀²+x₁²)` at 0 — that's fderiv of `2·proj₁·denom⁻¹` — first-derivative-level machinery only IF I work with the EXPLICIT Γ formulas rather than the fderiv-derived d. The bridge: prove `sphere_christoffel_formula` relating christoffelFamily to the explicit formula — which needs dFamily = explicit d — which needs fderiv of g (FIRST derivatives only — one level). Then ∂Γ via fderiv of the explicit formula (first derivative of explicit functions). NO second-derivative machinery needed!!! Because I compute ∂Γ by differentiating the EXPLICIT formula, not by differentiating through the fderiv-derived d.

   Plan for `SphereChartModel.lean`:
   1. 2D chart: ι = Fin 2. Explicit functions: r2, denom, gComp, gInvComp — all ContDiff (with ≠0 proof).
   2. dComp x i j k = explicit ∂ᵢg_{jk}(x) = -16·xᵢ·δ_{jk}·denom⁻³ — hmm wait — for the SmoothChartData the d is DERIVED from fderiv. Instead of using SmoothChartData, define the sphere as a SmoothChartData c — then prove `dFamily_eq : c.dFamily x i j k = explicit` via fderiv lemmas (first level).
   3. Prove `christoffelFamily_eq : c.christoffelFamily x k i j = explicit Γ formula` — via christoffel_symm-… using dFamily_eq + the definition (algebra).
   4. Prove the explicit Γ formula smooth.
   5. Compute fderiv of the explicit Γ¹₂₂ and Γ¹₂₁ at 0 along e₁, e₂: ∂₁Γ¹₂₂(0) = 2, ∂₂Γ¹₂₁(0) = −2 — via fderiv lemmas on explicit functions.
   6. Define curvature component `Rcomp` via the formula with ∂Γ := those fderivs, evaluate: R¹₂₁₂(0) = 4 ≠ 0 → **sphere_curvature_nonzero**.
   7. Also prove Γ(0) = 0, and full g·gInv = δ (inv_mul) for SmoothChartData.

   Step 2's fderiv proofs: g_comp = 4·denom⁻². fderiv = fderiv of constant·inv-pow — lemmas: fderiv_const (Function.const), fderiv_inv (needs differentiability + nonzero), fderiv_pow — for `(denom)⁻²` — hmm — write as `(denom⁻¹)^2` or `(denom^2)⁻¹` — either way fderiv_pow + fderiv_inv. And fderiv of denom = fderiv of (1 + proj0² + proj1²): fderiv_const_add? `fderiv_const_add`, `fderiv_add`, `fderiv_pow` on projections, and fderiv of projections: `fderiv_proj`? — let me check mathlib for these lemma names. This is the risk area; let me grep now.

   Actually — hmm, wait. There's ANOTHER simplification: instead of proving dFamily_eq via general fderiv lemmas for arbitrary x, prove it via `HasFDerivAt` ONLY where needed — but dFamily is used at every x for the christoffelFamily_eq proof — need the general formula. Fine — general fderiv lemmas exist; the computation is mechanical.

   Let me first do the checkpoint update (30 min mark), then grep the fderiv lemmas, then write the module.
dsh: reasoning:
Available: `fderiv_comp`, `fderiv_inv'`, `fderiv_pow`, `fderiv_mul`, `fderiv_const_mul`, `fderiv_sum`, `fderiv_const_apply`. No `fderiv_proj` — I'll need fderiv of projections. Let me check how to get fderiv of `fun x : Fin 2 → ℝ => x i`. There's likely a lemma for this: maybe `fderiv_apply` or in `FDeriv/Pi.lean`: `fderiv_pi`? Or `hasFDerivAt_proj`? Let me search for "proj" in FDeriv files.
dsh: reasoning:
No projection lemmas in FDeriv/Pi.lean under "proj". Let me check what's in Pi.lean and how to differentiate `fun x => x i` — maybe `fderiv_apply`? In Pi.lean, there might be `fderiv_pi`, `fderiv_single`, `hasFDerivAt_pi'`, or I can use `fderiv_eq` + `HasFDerivAt` with `hasFDerivAt_pi`... For `fun x : ι → ℝ => x i` — this is a continuous LINEAR map (the i-th projection) — so `fderiv` of a linear map is itself: `fderiv_linear`/`fderiv_id`... There's `fderiv_continuousLinearMap`? or `ContinuousLinearMap.fderiv`? Let me grep for those.
dsh: reasoning:
`fderiv_single` exists. For the i-th projection `fun x : ι → E => x i` — hmm — `fderiv_single {i : ι} (y : E i) : fderiv 𝕜 (Pi.single i) y = continuousMultilinearCurry... `? Let me look. Also there's the LinearMap route: `(fderiv 𝕜 (fun x => x i) x)` — the projection is the continuous linear map `Pi.evalLinearMap`-ish — `ContinuousLinearMap.proj`? In mathlib: `ContinuousLinearMap.proj (i : ι) : (∀ i, E i) →L[𝕜] E i := { toLinearMap := LinearMap.proj i, cont := ... }`. And fderiv of a continuous linear map L at any point is L itself: `fderiv_clm`? There's `ContinuousLinearMap.fderiv : fderiv 𝕜 L x = L` — let me check its name... `ContinuousLinearMap.fderiv` exists in Linear.lean or Basic. And then `(ContinuousLinearMap.proj i) x = x i` — `ContinuousLinearMap.proj_apply`?

Let me check both `fderiv_single`'s statement and the ContinuousLinearMap.proj lemmas.
dsh: reasoning:
OK — `ContinuousLinearMap.fderiv : fderiv 𝕜 f x = f` exists (Linear.lean:79). And for the projection, I can use `ContinuousLinearMap.proj` — it's in `Mathlib/Analysis/Normed/Module/Basic.lean`? Let me find it. Actually simpler: `fun x : ι → ℝ => x i` = `ContinuousLinearMap.proj i`? Hmm, hmm. In this mathlib, the Pi projection CLM: `ContinuousLinearMap.proj (i : ι) : (∀ i, E i) →L[𝕜] E i`. Grep "proj" in Module/Basic.
dsh: reasoning:
Hmm — `ContinuousLinearMap.proj` not in Basic.lean. Let me search where the Pi coordinate projection as a CLM lives.
dsh: reasoning:
No `ContinuousLinearMap.proj` found via grep "proj" (only Fredholm stuff). In this mathlib revision the Pi coordinate projection CLM might be defined differently. Let me check `fderiv_pi` in FDeriv/Pi.lean and how to write fderiv of `fun x => x i`.

Actually — the smart move: since `fun x : ι → ℝ => x i` is a continuous linear map, and there's `ContinuousLinearMap.fderiv : fderiv 𝕜 f x = f` — I need the CLM whose application gives `x i`. Is `fun x => x i` recognized as a CLM? There's `Pi.evalLinearMap`? or `LinearMap.proj`? In mathlib: `LinearMap.proj (i : ι) : (∀ i, M i) →ₗ[R] M i` — and the continuous version... Let me search for `Pi.eval`.
dsh: reasoning:
Pi.lean is only 40 lines. The note about Pi.evalLinearMap must be elsewhere. Let me search for `evalLinearMap` across mathlib.
dsh: reasoning:
`evalLinearMap` doesn't exist in this revision either. OK — different approach for the projection derivative. I recall the lemma in FDeriv/Pi.lean — wait — it was only 40 lines and I saw the content: hasFDerivAt_update, hasFDerivAt_single, fderiv_update, fderiv_single. Hmm.

For `fun x : ι → ℝ => x i` — write it as `LinearMap.proj i` restricted... Actually — simplest approach that definitely works: `fun x => x i` IS a continuous linear map, and I can prove its fderiv via `ContinuousLinearMap.fderiv` after `change fderiv ℝ (Pi.evalLinearMap...` hmm.

Alternative cleaner approach: prove the needed fderivs directly via `HasFDerivAt` construction and `fderiv_eq`-style uniqueness:
```lean
lemma fderiv_proj (i : ι) (x : ChartPoint ι) :
    fderiv ℝ (fun y : ChartPoint ι => y i) x = (ContinuousLinearMap.proj i) := by ...
```
— but proj doesn't exist...

Wait — maybe it does but under another name. Let me search for "toLinearMap := LinearMap.proj" or in ContinuousLinearMap files. Actually — hold on — there IS `ContinuousLinearMap.proj`? Let me grep the whole mathlib for `proj (i`.
dsh: reasoning:
`LinearMap.proj` exists. For a finite-dimensional Pi space, `LinearMap.proj i` is continuous — there's `LinearMap.continuous_of_finiteDimensional` or `ContinuousLinearMap.mk`... This is getting complicated.

PIVOT — simpler formulation of the model to avoid ALL fderiv-of-projection pain: **work in the 1D-trick**: no. Alternative: **define the 2D chart model where the metric is the sphere metric but the DERIVATIVE data is computed via the existing `fderiv_apply_eq_sum_single` + known directional values**? Still needs fderiv of g.

Actually — the cleanest available: there's the existing infrastructure in ChartLeviCivitaSmooth: `dFamily c x i j k = fderiv ℝ (fun y => c.g y j k) x (Pi.single i 1)`. For the sphere datum c, I need `fderiv ℝ (fun y : Fin 2 → ℝ => 4 * (1 + y 0 ^ 2 + y 1 ^ 2)⁻²) x`. The chain: this is `4 * (denom⁻²)` — constant_mul of (inv ∘ pow2 ∘ denom) — needs fderiv of `y ↦ (1 + y 0² + y 1²)` — sum of constant, proj0², proj1² — fderiv_pow needs DifferentiableAt of `fun y => y 0` — hmm, the fderiv VALUE of `fun y => y i`: I can avoid naming a CLM by proving directly with HasFDerivAt:
```lean
lemma hasFDerivAt_proj (i : ι) (x : ChartPoint ι) :
    HasFDerivAt (fun y : ChartPoint ι => y i) (ContinuousLinearMap.proj ...) 
```
still needs the CLM.

ALTERNATIVE: `fderiv_pi`! There IS `fderiv_pi` in Basic.lean? Let me check — `fderiv_pi : fderiv 𝕜 (fun x => ...)` hmm. Let me grep for "fderiv_pi".
dsh: reasoning:
`hasFDerivAt_pi''` references `proj i` (a CLM projection)! From Prod.lean:388: `hasFDerivAt_pi'' (hφ : ∀ i, HasFDerivAt (fun x => Φ x i) ((proj i).comp Φ') x)` — so there IS a `ContinuousLinearMap.proj`. It's likely in `Mathlib/Analysis/Normed/Operator/ContinuousLinearMap.lean` or in `Mathlib/Topology/Algebra/Module/...`. Let me find `def proj` with namespace ContinuousLinearMap.
dsh: reasoning:
`ContinuousLinearMap.proj (i : ι) : (∀ i, φ i) →L[R] φ i` exists in `ContinuousLinearMap/PiProd.lean` (line 234). So I can use:
- `ContinuousLinearMap.proj i` — the i-th coordinate projection CLM.
- `ContinuousLinearMap.fderiv : fderiv 𝕜 f x = f` for f a CLM (Linear.lean:79).
- `ContinuousLinearMap.proj_apply : proj i x = x i` — presumably exists (let me assume, verify at compile).

So `fderiv ℝ (fun y : ChartPoint ι => y i) x = ContinuousLinearMap.proj i` — via `ContinuousLinearMap.fderiv` after `change`.

Now — plan the sphere model module. Actually — WAIT. Before diving into the sphere with fderiv computations (potentially long), let me reconsider scope for this invocation. Time spent so far: ~50 min. Remaining: ~3h10m. The sphere module could take 1-2h of careful work with uncertain outcome. Given the task instructions — "hourly durable checkpoints" and the need to finish with clean results — I'll attempt it with a hard timebox: if it's not compiling by ~2h into the module work, fall back to a simpler but still valuable addition.

SIMPLER FALLBACK: a "coordinate transformation / naturality" lemma for the ALGEBRAIC chart core — e.g., the classical transformation law for Christoffel symbols under a linear coordinate change, proved algebraically from the coefficient identities. Hmm — that also needs d-transformation... 

Actually — hmm — let me reconsider. What about the 1D-trick again: the existing `ChartModel1D` has g(x) = 1 + x². For 1D, curvature = 0 (proved). For a NONZERO-curvature chart model, 2D is needed.

Let me think about what's the cheapest nonzero-curvature computation: the sphere at the origin, using the EXPLICIT formulas directly (not through SmoothChartData). I.e., a standalone model:

- ι = Fin 2. 
- Define g x i j = 4·(1+r2 x)⁻²·(if i=j then 1 else 0) — hmm — but the "from metric coefficients" provenance: the task's milestone is about constructing the connection (done in ChartLeviCivitaSmooth). For the curvature model, connecting to the general machinery would be ideal but a standalone explicit computation with the standard component formula is also a legitimate MODEL (recorded as model, non-vacuity witness).
- Define Γ explicit formulas.
- PROVE the explicit Γ equal the christoffelFamily of a SmoothChartData sphere datum — the bridge needs dFamily = explicit d → needs fderiv proofs of g — still needed.

Compromise: in the model module, define the sphere datum c : SmoothChartData (Fin 2) with g_smooth/gInv_smooth (ContDiff proofs — routine), and prove the fderiv-based dFamily equals the explicit formula — this requires:
(a) fderiv of `fun y => y i` = proj i (via CLM.fderiv + change).
(b) fderiv of pow2: `fderiv_pow 2 hdiff : fderiv 𝕜 (fun x => f x ^ 2) x = (2 • f x) • fderiv 𝕜 f x` — with smul on ℝ — `2 • f x` = `2 * f x`.
(c) fderiv of sum: `fderiv_add`, `fderiv_const_add`.
(d) fderiv of inv: `fderiv_inv' (hx : x ≠ 0) : fderiv 𝕜 Inv.inv x = -mulLeftRight 𝕜 R x⁻¹ x⁻¹` — hmm — that's for R with mulLeftRight... for ℝ this simplifies: `mulLeftRight 𝕜 ℝ a b = fun y => a * y * b` — the CLM. Applied: `fderiv ℝ (fun y : ℝ => y⁻¹) x v = -x⁻² * v`-ish. Hmm — `fderiv_inv'` gives the derivative as `-mulLeftRight ℝ ℝ x⁻¹ x⁻¹` — applying to v: `- (x⁻¹ * v * x⁻¹)` = `-(x⁻²)·v`. Workable with ring.
   Actually there might be a friendlier lemma: `fderiv_inv` for functions: in FDeriv/Mul.lean — `theorem fderiv_inv (hc : DifferentiableAt 𝕜 c x) (h0 : c x ≠ 0) : fderiv 𝕜 (fun x => (c x)⁻¹) x = -fderiv 𝕜 c x • (c x)⁻²`-style? Let me check. There's `HasFDerivAt.inv`? Let me grep "fderiv_inv " and "HasFDerivAt.inv".
(e) fderiv of pow of inv composition: chain via `fderiv_comp`.

Then dFamily = explicit. Then christoffelFamily_eq via simp/ring (the pointwise algebra). Then the curvature: define `dGamma k i j l x := fderiv ℝ (fun z => explicitΓ k i j z) x (single l 1)` — hmm — OR compute fderiv of the explicit Γ via the same lemmas. Then `Rcomp := ∂ᵢΓˡₖⱼ − ∂ⱼΓˡₖᵢ + Σₘ ΓˡᵢₘΓᵐₖⱼ − ΓˡⱼₘΓᵐₖᵢ`. Evaluate R¹₂₁₂(0) = 4.

That's a LOT of fderiv algebra. Realistically 2-3 hours with uncertain outcome. Hmm.

DIFFERENT IDEA — MUCH cheaper, still substantive and directly on-target for "curvature from metric coefficients": use the **so(3) chart trick**? No.

Cheaper: **perturbation metric with small polynomial correction where the inverse is computable exactly**: g = [[1 + εx₁², 0],[0, 1]] — gInv = [[1/(1+εx₁²), 0],[0,1]] — still has 1/(1+εx₁²) in Γ — fderiv_inv needed for ∂Γ. Hmm — but the fderiv of 1/(1+x²)-type single-variable-in-one-slot functions is simpler than the sphere (which couples x₀,x₁ in the denominator). Hmm — the sphere's denom = 1 + x₀² + x₁² — fderiv needs proj₀ and proj₁ both — fine, that's just (b)+(c) twice.

Actually — hmm — WAIT. Let me reconsider ONE more time. The metric g = [[f(x₁), 0],[0, 1]] on ℝ² (warped product): Γ¹₁₁(x) = f'(x₁)/(2f(x₁)), Γ²₁₁ = −f'(x₁)/2, others 0. Curvature: R(e₁,e₂)e₁ at generic point: R²₁₂₁ = ∂₂Γ²₁₁ − ∂₁Γ²₁₂ + ... = −f''/2 − (Γ²₁₁Γ¹₁₂ + ...)... For f(x₁) = 1/(1+x₁²)-ish... hmm — for the standard hyperbolic-plane-like metric f = e^{2x₁}... let me pick f(x₁) = (1+x₁²)⁻¹? Then f' = −2x₁(1+x₁²)⁻², Γ¹₁₁ = −x₁/(1+x₁²)·... hmm — Γ¹₁₁ = f'/(2f) = −x₁(1+x₁²)⁻¹. ∂₁Γ¹₁₁ = −(1+x₁²)⁻¹ + 2x₁²(1+x₁²)⁻² — at x₁=0: −1. Γ²₁₁ = −f'/2 = x₁(1+x₁²)⁻²·... = x₁(1+x₁²)⁻²·2/2 = x₁(1+x₁²)⁻². R(e₁,e₂)e₁: R¹₁₂₁... the components: R²₁₂₁ = ∂₂Γ²₁₁ − ∂₁Γ²₁₂ + Γ²₂ₘΓᵐ₁₁ − Γ²₁ₘΓᵐ₂₁ = −∂₁Γ²₁₂ − Γ²₁ₘΓᵐ₂₁ — Γ²₁₂ = 0 (for diagonal metric with f' only in slot 1: Γ²₁₂ = (1/2)g²²(g₂₁,₂ + g₂₂,₁ − g₁₂,₂)... = (1/2)(0 + 0 − 0) = 0) — Γ²₁₁ = x₁(1+x₁²)⁻². So R²₁₂₁ = −∂₁Γ²₁₂ − ... — wait — R^l_{kij} = ∂ᵢΓˡₖⱼ − ∂ⱼΓˡₖᵢ + ... — R²₁₂₁ = ∂₂Γ²₁₁ − ∂₁Γ²₁₂ + Γ²₂ₘΓᵐ₁₁ − Γ²₁ₘΓᵐ₂₁ = ∂₂Γ²₁₁ − 0 + 0 − 0 = ∂₂(x₁(1+x₁²)⁻²) = 0 (no x₂ dependence!). Hmm — R²₁₂₁ = 0?? For the warped metric g = f(x₁)dx₁² + dx₂² — the sectional curvature K = −f''/(2f) — nonzero — but R(e₁,e₂)e₁ = K(⟨e₂,e₂⟩e₁ − ⟨e₂,e₁⟩e₂)/... = K·1·e₁... in components: R¹₁₂₁ = K·1·1·(1/f)·... hmm — R(e₁,e₂)e₁ = K(⟨e₁,e₂⟩e₂ − ⟨e₂,e₂⟩e₁) = −K e₁ — so R¹₁₂₁ = −K = f''/(2f). Let me compute R¹₁₂₁ = ∂₂Γ¹₁₁ − ∂₁Γ¹₁₂ + Γ¹₂ₘΓᵐ₁₁ − Γ¹₁ₘΓᵐ₂₁ = 0 − ∂₁Γ¹₁₂ + Γ¹₂ₘΓᵐ₁₁ − Γ¹₁ₘΓᵐ₂₁. Γ¹₁₂ = (1/2)g¹¹(g₁₂,₁ + g₁₁,₂ − g₁₂,₁)... g₁₂ = 0 → Γ¹₁₂ = (1/2)g¹¹(g₁₁,₂) = 0 (f doesn't depend on x₂). Γ¹₂ₘΓᵐ₁₁ = Γ¹₂₁Γ¹₁₁ + Γ¹₂₂Γ²₁₁ = Γ¹₁₂Γ¹₁₁ + 0 = 0. Γ¹₁ₘΓᵐ₂₁ = Γ¹₁₁Γ¹₂₁ + Γ¹₁₂Γ²₂₁ = 0 + 0 = 0. So R¹₁₂₁ = 0?!?! That contradicts K ≠ 0. I must have the index convention wrong again. Ugh.

Standard check with formula: R^l_{kij} = ∂ᵢ Γ^l_{kj} − ∂ⱼ Γ^l_{ki} + Γ^l_{im} Γ^m_{kj} − Γ^l_{jm} Γ^m_{ki} — this is R(∂ᵢ, ∂ⱼ)∂ₖ = R^l_{kij} ∂ₗ — hmm — is it R(∂ᵢ,∂ⱼ)∂ₖ = R^l_{kij}∂ₗ? Let me verify with the sphere result I did by hand: I used R^l_{kij} = ∂ᵢΓˡₖⱼ − ∂ⱼΓˡₖᵢ + ΓˡᵢₘΓᵐₖⱼ − ΓˡⱼₘΓᵐₖᵢ and got R¹₂₁₂ = 4 with the correct K=1 answer (⟨R(e₁,e₂)e₂,e₁⟩ = g₁₁R¹₂₁₂ = 16 = K(g₁₁g₂₂−g₁₂²) ✓). Hmm wait — which slots: I computed R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₂₁ + Γ¹₁ₘΓᵐ₂₂ − Γ¹₂ₘΓᵐ₂₁ = 2 − (−2) = 4. With R^l_{kij} = ∂ᵢΓˡₖⱼ − ∂ⱼΓˡₖᵢ + ΓˡᵢₘΓᵐₖⱼ − ΓˡⱼₘΓᵐₖᵢ: R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₂₁ + Γ¹₁ₘΓᵐ₂₂ − Γ¹₂ₘΓᵐ₂₁ ✓ consistent.

Now the warped metric: R¹₁₂₁ = ∂₂Γ¹₁₁ − ∂₁Γ¹₁₂ + Γ¹₂ₘΓᵐ₁₁ − Γ¹₁ₘΓᵐ₂₁ — hmm with l=1,k=1,i=2,j=1: R¹₁₂₁ = ∂₂Γ¹₁₁ − ∂₁Γ¹₁₂ + Γ¹₂ₘΓᵐ₁₁ − Γ¹₁ₘΓᵐ₂₁. Hmm — that gave 0 above. Let me compute R(e₂,e₁)e₁ = R^l_{121}e_l = R¹₁₂₁e₁ + R²₁₂₁e₂. K(g₂₂g₁₁ − g₂₁²)... ⟨R(e₂,e₁)e₁, e₂⟩ = g₂₂R²₁₂₁·? — ⟨R^l_{121}e_l, e₂⟩ = g₂₂ R²₁₂₁ = K(g₁₁g₂₂ − 0)·(±1) = K f·1·(−1) (antisymmetry R(e₂,e₁) = −R(e₁,e₂)) — so R²₁₂₁ = −Kf. Let me recompute R²₁₂₁ = ∂₂Γ²₁₁ − ∂₁Γ²₁₂ + Γ²₂ₘΓᵐ₁₁ − Γ²₁ₘΓᵐ₂₁ = ∂₂Γ²₁₁ − 0 + 0 − 0. Γ²₁₁ = −f'(x₁)/2 — no x₂ → ∂₂ = 0 → R²₁₂₁ = 0 ≠ −Kf?!? Something's off — my "∂ᵢΓˡₖⱼ" convention must be wrong for the k-slot. Let me redo from the DEFINITION: R(X,Y)Z = ∇X∇YZ − ∇Y∇XZ − ∇[X,Y]Z. In coordinates with ∂ᵢ = eᵢ (commuting, [∂ᵢ,∂ⱼ]=0): R(∂ᵢ,∂ⱼ)∂ₖ = ∇ᵢ∇ⱼ∂ₖ − ∇ⱼ∇ᵢ∂ₖ = ∇ᵢ(Γᵐⱼₖ∂ₘ) − ∇ⱼ(Γᵐᵢₖ∂ₘ) = (∂ᵢΓᵐⱼₖ)∂ₘ + ΓᵐⱼₖΓⁿᵢₘ∂ₙ − (∂ⱼΓᵐᵢₖ)∂ₘ − ΓᵐᵢₖΓⁿⱼₘ∂ₙ. So R^l_{kij} = ∂ᵢΓˡⱼₖ − ∂ⱼΓˡᵢₖ + ΓᵐⱼₖΓˡᵢₘ − ΓᵐᵢₖΓˡⱼₘ. (Indices: R^l_{kij} = ∂ᵢΓˡⱼₖ − ∂ⱼΓˡᵢₖ + ΓˡᵢₘΓᵐⱼₖ − ΓˡⱼₘΓᵐᵢₘ... let me be careful: the last terms: ΓᵐⱼₖΓˡᵢₘ (from ∇ᵢ of Γᵐⱼₖ∂ₘ: the m-component of ∇ᵢ∂ₘ is Γˡᵢₘ) minus ΓᵐᵢₖΓˡⱼₘ.) So: **R^l_{kij} = ∂ᵢΓˡⱼₖ − ∂ⱼΓˡᵢₖ + ΓˡᵢₘΓᵐⱼₖ − ΓˡⱼₘΓᵐᵢₖ** — where Γᵐⱼₖ = Γˡₖⱼ = Γˡⱼₖ symmetric.

Check sphere: R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₁₂ + Γ¹₁ₘΓᵐ₂₂ − Γ¹₂ₘΓᵐ₁₂ — Γ¹₂₂ = 2x₁/denom → ∂₁ = 2 at 0; Γ¹₁₂ = −2x₂/denom → ∂₂ = −2 at 0; Γ(0)=0. R¹₂₁₂ = 2 − (−2) = 4 ✓ same as before (since Γ¹₂₁ = Γ¹₁₂, the formulas agree because my earlier "R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₂₁" used Γ¹₂₁ = Γ¹₁₂ ✓).

Warped metric check: R²₁₂₁ = ∂₂Γ²₁₁ − ∂₁Γ²₂₁ + Γ²₂ₘΓᵐ₁₁ − Γ²₁ₘΓᵐ₂₁ = 0 − ∂₁Γ²₂₁ + Γ²₂ₘΓᵐ₁₁ − Γ²₁ₘΓᵐ₂₁. Γ²₂₁ = (1/2)g²²(g₂₁,₂ + g₂₂,₁ − g₁₂,₂) = (1/2)·1·(0 + 0 − 0) = 0. Γ²₂ₘΓᵐ₁₁ = Γ²₂₁Γ¹₁₁ + Γ²₂₂Γ²₁₁ = 0·Γ¹₁₁ + 0 = 0. Γ²₁ₘΓᵐ₂₁ = Γ²₁₁Γ¹₂₁ + Γ²₁₂Γ²₂₁ = Γ²₁₁·0 + 0 = 0. So R²₁₂₁ = 0 — meaning ⟨R(e₂,e₁)e₁,e₂⟩ = 0?? For the metric g = f(x₁)dx₁² + dx₂²: the well-known K = −(∂₁²√f)/√f — for f = (1+x₁²)⁻¹... hmm — wait — is the warped metric g = f dx₁² + dx₂² flat?? Let me directly compute: is g = f(x₁)dx₁² + dx₂² flat for ANY f? The famous result: this metric is flat (it's just dx̃₁² + dx₂² with x̃₁ = ∫√f dx₁)! YES — g = f(x₁)dx₁² + dx₂² is ALWAYS flat (integrable 1-form √f dx₁). So R ≡ 0 ✓ — my computation was right and my intuition (K = −f''/2f) was for the metric f(dx₁² + dx₂²)?? No — for g = f·(dx₁²+dx₂²) (conformally flat, f = f(x₁)): K = −(f''·f − 2f'²)/(2f²)... hmm — for conformally flat g = e^{2φ}δ: K = −e^{−2φ}Δφ = −(φ''+φ''ₙ)/(e^{2φ}). For g = f·δ with f = f(x₁): φ = (1/2)ln f, K = −(1/(2f))·(f''/f − f'²/f²)·... let me just: K = −e^{−2φ}φ'' = −(1/f)((f''f − f'²)/(2f²)) = (f'² − ff'')/(2f³). For f(x₁) = (1+x₁²)⁻¹: f' = −2x₁(1+x₁²)⁻², f'' = −2(1+x₁²)⁻² + 8x₁²(1+x₁²)⁻³. At x₁ = 0: f = 1, f' = 0, f'' = −2. K(0) = (0 − 1·(−2))/(2·1) = 1. NONZERO ✓. 

So the metric g(x₀,x₁) = (1+x₁²)⁻¹·δ (conformal, depending only on x₁) has K(0) = 1 ≠ 0. Its Christoffels: g_{ij} = f δ_{ij}, g^{ij} = f⁻¹ δ^{ij}. Γ^k_{ij} = (1/2)(φ_j δ_{ki} + φ_i δ_{kj} − φ_k δ_{ij}) with φ = ln f, φ_i = f'/f δ_{i1}. Γ¹₁₁ = φ₁/2, Γ¹₁₂ = φ₂/2 = 0, Γ¹₂₂ = −φ₁/2, Γ²₁₁ = −φ₂/2 = 0, Γ²₁₂ = φ₁/2, Γ²₂₂ = φ₂/2 = 0. R(e₂,e₁)e₁ — hmm — R²₁₂₁ = ∂₂Γ²₁₁ − ∂₁Γ²₂₁ + Γ²₂ₘΓᵐ₁₁ − Γ²₁ₘΓᵐ₂₁ = 0 − ∂₁(0) + Γ²₂₂Γ²₁₁ − (Γ²₁₁Γ¹₂₁ + Γ²₁₂Γ²₂₁) = 0. R¹₁₂₁ = ∂₂Γ¹₁₁ − ∂₁Γ¹₁₂ + Γ¹₂ₘΓᵐ₁₁ − Γ¹₁ₘΓᵐ₂₁ = 0 − 0 + Γ¹₂₂Γ²₁₁ − Γ¹₁₁Γ¹₂₁ − Γ¹₁₂Γ²₂₁ = Γ¹₂₂Γ²₁₁ = (−φ₁/2)·0 = 0. Hmm — R(e₂,e₁)e₁ = 0?! But K = (f'²−ff'')/(2f³) ≠ 0 — contradiction?!? Wait — K from ⟨R(e₁,e₂)e₂,e₁⟩ = R¹₂₁₂·f... let me recompute R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₁₂ + Γ¹₁ₘΓᵐ₂₂ − Γ¹₂ₘΓᵐ₁₂ = ∂₁(−φ₁/2) − 0 + Γ¹₁₁Γ¹₂₂ + Γ¹₁₂Γ²₂₂ − Γ¹₂₁Γ¹₁₂ − Γ¹₂₂Γ²₁₂ = −φ''/2 + (φ₁/2)(−φ₁/2) − 0 = −φ''/2 − φ₁²/4. And K = Rm(e₁,e₂,e₂,e₁)/(g₁₁g₂₂−g₁₂²) = (f·R¹₂₁₂)/(f²) = R¹₂₁₂/f. R¹₂₁₂ = −φ''/2 − φ'²/4; φ = ln f: φ' = f'/f, φ'' = f''/f − f'²/f². R¹₂₁₂ = −(f''/f − f'²/f²)/2 − f'²/(4f²) = (−2ff'' + 2f'² − f'²)/(4f²) = (−2ff'' + f'²)/(4f²). K = R¹₂₁₂/f = (f'² − 2ff'')/(4f³). For f = (1+x₁²)⁻¹ at 0: (0 − 2·1·(−2))/(4·1) = 4/4 = 1 ✓. GREAT — so K = (f'² − 2ff'')/(4f³) ≠ 0 and the nonzero component is **R¹₂₁₂ = −φ''/2 − φ₁²/4** — which needs only x₁-derivatives of the EXPLICIT function φ₁(x₁) = f'(x₁)/f(x₁) = −2x₁/(1+x₁²) — a UNIVARIATE function!

This metric is MUCH better for Lean: everything depends only on x₁, and all derivatives collapse to univariate calculus. Even better — pick f = e^{−2x₁}? φ₁ = −1 const, Γ constant, curvature 0 (that's hyperbolic-like flat? no — f = e^{-2x} gives K = (f'²−2ff'')/(4f³) with f' = −2f, f'' = 4f: (4f² − 8f²)/(4f³) = −1 — the hyperbolic plane! g = e^{−2x₁}(dx₁²+dx₂²) is the hyperbolic plane (K = −1). With f = e^{−2x₁}: φ₁ = −1, Γ¹₁₁ = −1/2, Γ¹₂₂ = 1/2, Γ²₁₂ = −1/2 — CONSTANT Christoffels — curvature R¹₂₁₂ = −φ''/2 − φ'²/4 = 0 − 1/4 = −1/4·... hmm — R¹₂₁₂ = −1/4·? — with f = e^{−2x₁}: R¹₂₁₂ = −(0)/2 − (1)/4·? φ' = −1 → φ'² = 1 → R¹₂₁₂ = −1/4. K = R¹₂₁₂/f — f(0) = 1 → K = −1/4?? But the hyperbolic plane has K = −1. Hmm — let me recompute: φ = ln f = −2x₁. Γ¹₁₁ = φ₁/2 = −1, Γ¹₂₂ = −φ₁/2 = 1, Γ²₁₂ = φ₁/2 = −1. R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₁₂ + Γ¹₁ₘΓᵐ₂₂ − Γ¹₂ₘΓᵐ₁₂ = 0 − 0 + Γ¹₁₁Γ¹₂₂ + Γ¹₁₂Γ²₂₂ − Γ¹₂₁Γ¹₁₂ − Γ¹₂₂Γ²₁₂ = (−1)(1) − (1)(−1) = −1 + 1 = 0?!? That gives K = 0?? Hmm — that contradicts. Wait — Γ²₁₂ = −1, so Γ¹₂₂Γ²₁₂ = 1·(−1) = −1. R¹₂₁₂ = Γ¹₁₁Γ¹₂₂ − Γ¹₂₂Γ²₁₂ = (−1)(1) − (1)(−1) = −1 + 1 = 0. Hmm!! K = 0?? But g = e^{−2x₁}(dx₁²+dx₂²) — is that flat?? e^{−x₁}dx₁ is exact (−d(e^{−x₁})) and e^{−x₁}dx₂ — d(e^{−x₁}dx₂) = −e^{−x₁}dx₁∧dx₂ ≠ 0 — NOT integrable — the metric is NOT flat. So my component computation must be wrong somewhere. Let me redo R¹₂₁₂ with the DEFINITION once more, carefully: R(e₁,e₂)e₂ = ∇₁∇₂e₂ − ∇₂∇₁e₂ − ∇_{[e₁,e₂]}e₂ = ∇₁(Γᵐ₂₂eₘ) − ∇₂(Γᵐ₁₂eₘ) = ∇₁(Γ¹₂₂e₁ + Γ²₂₂e₂) − ∇₂(Γ¹₁₂e₁ + Γ²₁₂e₂) = [∂₁Γ¹₂₂·e₁ + Γ¹₂₂∇₁e₁ + ∂₁Γ²₂₂e₂ + Γ²₂₂∇₁e₂] − [∂₂Γ¹₁₂e₁ + Γ¹₁₂∇₂e₁ + ∂₂Γ²₁₂e₂ + Γ²₁₂∇₂e₂] = [∂₁Γ¹₂₂e₁ + Γ¹₂₂(Γ¹₁₁e₁+Γ²₁₁e₂)] − [0 + 0 + 0 + Γ²₁₂(Γ¹₂₂e₁+Γ²₂₂e₂)] (∂ terms vanish, Γ²₂₂ = Γ¹₁₂ = 0). = (Γ¹₂₂Γ¹₁₁ − Γ²₁₂Γ¹₂₂)e₁ + (Γ¹₂₂Γ²₁₁ − Γ²₁₂Γ²₂₂)e₂ = (1·(−1) − (−1)(1))e₁ = 0·e₁. So R(e₁,e₂)e₂ = 0?! Then ⟨R(e₁,e₂)e₂,e₁⟩ = 0 → K = 0??? That means g = e^{−2x₁}(dx₁² + dx₂²) IS flat!? Let me sanity check with a direct formula: conformal factor e^{2φ} with φ = −x₁: K = −e^{−2φ}Δφ = −e^{2x₁}·∂²φ/∂x₁² = −e^{2x₁}·0 = 0!!! YES — FLAT! Because φ is HARMONIC (linear in x₁)! e^{−2x₁}(dx₁²+dx₂²) — substitute u = e^{−x₁}: du = −e^{−x₁}dx₁ — g = du² + u²dx₂² — polar-like — u(dx₂) integrable? g = du² + u²dx₂² IS flat (it's the standard metric on ℝ²\{0} in "polar" form? g = dr² + r²dθ² is flat ✓ YES). Confirmed flat. My earlier "K = −1" was WRONG (I confused with f = 1/x₁²-type). Good thing I computed.

So use f = (1+x₁²)⁻¹: φ = −ln(1+x₁²), K = (f'² − 2ff'')/(4f³) — at 0: f(0) = 1, f'(0) = 0, f''(0) = −2 → K(0) = 1. And R¹₂₁₂(x) = −φ''/2 − φ'²/4 = −(f''f − f'²)/(2f²) − f'²/(4f²) = (−2ff'' + f'²)/(4f²). At 0: (−2·1·(−2) + 0)/4 = 1. So R¹₂₁₂(0) = 1 ≠ 0. 

Now in Lean: everything univariate in x₁. Define:
- f x := (1 + x 1 ^ 2)⁻¹ — hmm — or better directly: the metric g x i j = if i = j then (1 + x 1 ^ 2)⁻¹ else 0. Wait — actually — hmm, even simpler: is the FUNCTION 1/(1+x₁²) easy to differentiate in Lean? fderiv of `fun x : Fin 2 → ℝ => (1 + x 1^2)⁻¹` — chain: fderiv of inv composed with (1 + proj₁²) — same machinery as the sphere but only ONE projection and the result is a function of x₁ only. The sphere needed proj₀ and proj₁ — the same amount of work roughly. The sphere's advantage: symmetric in both variables (only one family g11 = g22). The conformal metric's advantage: g22 = g11 = f, same symmetry. Actually both are equally "one function" — f = 4(1+r²)⁻² for the sphere (2-variable) vs f = (1+x₁²)⁻¹ (1-variable). The 1-variable version has simpler fderiv (only ∂₁ nonzero).

Given the sphere gives a cleaner story (unit sphere via stereographic projection, K = 1, the model matches classical literature), but the warped-conformal metric g = (1+x₁²)⁻¹δ is simpler in Lean... The warped one has the nonzero K = 1 too, and it's still a "nonconstant smooth metric example on a real chart" with nonzero curvature. Hmm — but wait — for the warped one the curvature is NOT constant and the metric isn't the sphere — that's fine.

Hmm, hold on — actually — let me reconsider using g = (1+x₁²)⁻¹δ vs sphere g = 4(1+r²)⁻²δ. The DIFFERENCE in Lean work: both need fderiv of `x ↦ (1 + (x 1)^2)⁻¹` vs `x ↦ 4·(1 + (x 0)^2 + (x 1)^2)⁻²` — the latter is pow-2 of inv + TWO projections + a sum. The former is ONE inv + ONE projection + no pow. So the warped metric is genuinely simpler: d(x,i,j,k) = −2x₁(1+x₁²)⁻²·δ₁ᵢ·δⱼₖ... plus the fderiv of inv composed: fderiv of `(1+x₁²)⁻¹` = −(1+x₁²)⁻²·fderiv(1+x₁²) = −(1+x₁²)⁻²·2x₁·proj₁.

Even simpler!! — take f = (1 + x₁²)⁻¹ but actually — why not take f = e^{2x₁}-type? No — exp has fderiv_exp — fine but the metric g = e^{2x₁}δ is FLAT (φ = x₁ harmonic). OK.

Actually — hmm — the SIMPLEST possible: **g = ((1 + x₁²)⁻¹, 1) diagonal (warped)** — FLAT (proved above — any f(x₁)dx₁² + dx₂² is flat). NO. Need conformal f·δ with f'' ≠ 0 at 0.

Hmm — what about polynomial conformal factor? f = 1 + x₁² (instead of its inverse!): g = (1+x₁²)δ. φ = (1/2)ln(1+x₁²), K = (f'² − 2ff'')/(4f³) = (4x₁² − 2(1+x₁²)·2)/(4(1+x₁²)³) = (4x₁² − 4 − 4x₁²)/(4(1+x₁²)³) = −1/(1+x₁²)³ — at 0: K = −1 ≠ 0!! And g = (1+x₁²)δ is POLYNOMIAL — gInv = (1+x₁²)⁻¹δ — still has the inv in Γ... BUT — the inv appears in Γ via gInv·d — Γ^k_{ij} = (1/2)g^{kk}(...) = (1/2)(1+x₁²)⁻¹·(d-terms) — d-terms polynomial. ∂Γ involves fderiv of (1+x₁²)⁻¹ — ONE inv-derivative. Same amount of work as before. The inv is unavoidable (gInv = 1/g in 2D diagonal).

Hmm — OR — a metric with constant determinant AND polynomial entries: g = [[1 + x₁², 0],[0, (1+x₁²)⁻¹]]?? det = 1, but (1+x₁²)⁻¹ entry — inv again — and g is warped-ish? No — g₁₁g₂₂ = 1, non-flat? g = f dx₁² + f⁻¹ dx₂² — this is the Poincaré half-plane-like metric! K for g = f dx₁² + f⁻¹dx₂²: standard: this metric has K = −(1/2)(ln f)''·(1/f)... — for f = (1+x₁²): it's the metric of the... hmm — g₁₁·g₂₂ = 1 → det = 1. K = −(1/2)Δ(ln f)/det-ish. ln f = ln(1+x₁²), (ln f)'' = 2(1−x₁²)/(1+x₁²)². Hmm — I don't remember the formula. But the entries: g₂₂ = (1+x₁²)⁻¹ — has inv in the METRIC itself → d = fderiv of inv needed at the FIRST level (for Γ) — same total work.

OK — decision: go with **g = (1+x₁²)·δ** on the 2D chart (conformal polynomial metric, K(0) = −1 ≠ 0). Components:
- g x i j = (1 + x 1^2) * (if i = j then 1 else 0).
- gInv x i j = (1 + x 1^2)⁻¹ * (if i = j then 1 else 0). inv_mul: (1+x₁²)⁻¹·(1+x₁²) = 1 ✓ needs (1+x₁²) ≠ 0: nlinarith [sq_nonneg (x 1)].
- dFamily x i j k = fderiv ℝ (fun y => (1 + y 1^2) * (if j = k then 1 else 0)) x (single i 1) = 2·x 1·(if i=1 then 1 else 0)·(if j=k then 1 else 0).
- christoffelFamily x k i j = (1/2)·gInv k k·(d i j k + d j i k − d k i j) = explicit: for k=1: Γ¹₁₁ = x₁(1+x₁²)⁻¹, Γ¹₁₂ = 0, Γ¹₂₂ = −x₁(1+x₁²)⁻¹; k=2: Γ²₁₁ = 0, Γ²₁₂ = x₁(1+x₁²)⁻¹, Γ²₂₂ = 0.
  Check: Γ¹₁₁ = (1/2)(1+x₁²)⁻¹(d₁₁₁ + d₁₁₁ − d₁₁₁) = (1/2)(1+x₁²)⁻¹·2x₁ = x₁(1+x₁²)⁻¹ ✓. Γ¹₁₂ = (1/2)(1+x₁²)⁻¹(d₁₂₁ + d₂₁₁ − d₁₂₁) = (1/2)(1+x₁²)⁻¹(d₂₁₁) — d₂₁₁ = ∂₂g₁₁ = 0 → 0 ✓. Γ¹₂₂ = (1/2)(1+x₁²)⁻¹(d₂₂₁ + d₂₂₁ − d₁₂₂) = (1/2)(1+x₁²)⁻¹(0 + 0 − 2x₁) = −x₁(1+x₁²)⁻¹ ✓. Γ²₁₁ = (1/2)(1+x₁²)⁻¹(d₁₁₂ + d₁₁₂ − d₂₁₁) = (1/2)(1+x₁²)⁻¹(0+0−0) = 0 ✓. Γ²₁₂ = (1/2)(1+x₁²)⁻¹(d₁₂₂ + d₂₁₂ − d₂₁₂) = (1/2)(1+x₁²)⁻¹(d₁₂₂) = x₁(1+x₁²)⁻¹ ✓.
- R¹₂₁₂(0) = ∂₁Γ¹₂₂(0) − ∂₂Γ¹₁₂(0) + ΓΓ(0) — Γ(0) = 0 ✓ — = ∂₁(−x₁(1+x₁²)⁻¹)(0) − 0 = −1. K(0) = R¹₂₁₂(0)/g₁₁(0) = −1/1 = −1 ✓ matches K = −1/(1+x₁²)³ at 0 = −1 ✓.

So the target theorem: **R(e₁,e₂)e₂ at 0 = −e₁ ≠ 0** — i.e., R¹₂₁₂(0) = −1 — nonzero curvature. 

Lean work items:
1. SmoothChartData instance for this metric (ContDiff proofs: polynomial and inv — `ContDiff.mul`, `ContDiff.pow`, `ContDiff.inv` (needs ≠ 0), `contDiff_const`, `ContDiff.add`... — for `x ↦ x 1` smooth: `contDiff_proj`-ish — hmm — ContDiff of projections: `contDiff_apply`? For `fun x => x i` — there's `contDiff_pi.mp` reverse... To show ContDiff of a single coordinate: `ContDiff` of proj — there's `contDiff_proj`? Let me check — probably `contDiffAt_apply`? There's the CLM route: `(ContinuousLinearMap.proj i).contDiff` and then congr. Fine.
2. dFamily_eq: fderiv of `fun y => (1 + y 1^2) * δ` — fderiv_const_mul + fderiv_add + fderiv_pow + fderiv proj. Need `fderiv_proj`: prove `fderiv ℝ (fun y : ChartPoint ι => y i) x = ContinuousLinearMap.proj i` — via `ContinuousLinearMap.fderiv` after `change`. 
3. christoffelFamily_eq: unfold + dFamily_eq + algebra (by_cases on indices + norm_num/ring) — Fin 2 has 4 indices — manageable with fin_cases? Fin 2 — `fin_cases` tactic handles. The explicit formula with `if i = j` — use fin_cases and norm_num.
4. ∂Γ: fderiv of `fun x => -x 1 * (1 + x 1^2)⁻¹` at 0 in direction e₁ — fderiv_const_mul/mul/inv chain at the point 0 — need differentiability at 0 + the value. Result: ∂₁(−x₁(1+x₁²)⁻¹)(0) = −1. Directional: fderiv at 0 applied to single 1 1 (the e₁ vector) — proj₁(e₁) = 1.
5. Rcomp def + evaluation: R¹₂₁₂(0) = −1 ≠ 0.

This is still a solid chunk of work (fderiv chain computations are the risk). Timebox: 90 minutes. If stuck, fallback plan: keep the module minimal — e.g., prove the metric datum + Γ formulas (no curvature), and record the curvature-derivative step as remaining.

Hmm — wait, actually — let me reconsider ONE more simplification. Step 2 (dFamily_eq) requires proving fderiv of the metric via the general fderiv lemmas. There's an alternative: instead of proving dFamily_eq (which is an equality of fderivs), use `fderiv_eq`/`fderiv_unique`? Hmm — hmm — actually there's an even better tool: `fderiv_const_mul`, `fderiv_add`, `fderiv_pow`, plus for proj — maybe there's `fderiv_apply`-style lemma... Let me check what `ContinuousLinearMap.proj` gives and whether there's `fderiv_eval`/`fderiv_apply`:

Actually — I recall in this mathlib revision there IS a dedicated lemma: `fderiv_pi`? no. Hmm — `fderiv_single` I saw. For the projection — let me just use `ContinuousLinearMap.fderiv`:
```lean
lemma fderiv_proj (x : ChartPoint ι) (i : ι) :
    fderiv ℝ (fun y : ChartPoint ι => y i) x = ContinuousLinearMap.proj i := by
  change fderiv ℝ (ContinuousLinearMap.proj i) x = ContinuousLinearMap.proj i
  exact ContinuousLinearMap.fderiv
```
`ContinuousLinearMap.fderiv : fderiv 𝕜 f x = f` — for f : E →L[𝕜] F. And `proj` def: `ContinuousLinearMap.proj (i) : (∀ i, φ i) →L[R] φ i` — with `proj_apply`? Need `(ContinuousLinearMap.proj i) y = y i` — there's `ContinuousLinearMap.proj_apply` presumably — check at compile.

OK — let me also double check `fderiv_inv`'s exact form for functions — grep the Mul.lean around fderiv_inv.

Let me now write the module. First grep fderiv_inv / fderiv_pow exact statements.
dsh: reasoning:
All the lemmas I need are available:
- `fderiv_pow (n) (hf) : fderiv 𝕜 (f^n) x = (n • f x^(n-1)) • fderiv 𝕜 f x`
- `DifferentiableAt.inv (hf) (hz) : DifferentiableAt 𝕜 h⁻¹ z`
- `fderiv_inv' {x : R} (hx : x ≠ 0) : fderiv 𝕜 Inv.inv x = -mulLeftRight 𝕜 R x⁻¹ x⁻¹` — for the raw inv function — but for the COMPOSED `(fun y => (1 + y 1^2)⁻¹)` I should use `fderiv_comp` or... hmm — actually there's a cleaner lemma: `fderiv_inv` for composed functions? Let me check: grep showed `fderiv_inv'` (noncomm) — is there a `fderiv_inv (hc : DifferentiableAt 𝕜 c x) (h0 : c x ≠ 0) : fderiv 𝕜 (fun x => (c x)⁻¹) x = -fderiv 𝕜 c x • (c x)⁻²`? Let me grep "theorem fderiv_inv " precisely.

Also `ContinuousLinearMap.proj` with `proj_apply : (proj i) b = b i` — rfl — PERFECT.

For the directional evaluation, I'll need `ContinuousLinearMap.proj_apply`-style simplifications.

One more useful piece: `fderiv_comp`. And for univariate simplification: `mulLeftRight 𝕜 ℝ a b` — applying: `(mulLeftRight 𝕜 ℝ a b) v = a * v * b` — hopefully simp handles or I prove it. I'll use `fderiv_comp` with `fderiv_inv'`:
`fderiv ℝ (fun x => (denom x)⁻¹) x = (fderiv ℝ Inv.inv (denom x)).comp (fderiv ℝ denom x)` — then apply and simplify. Actually `fderiv_comp` requires DifferentiableAt inv at denom x (via DifferentiableAt.inv) and differentiability of denom. Hmm — actually there's an even more direct route: `HasFDerivAt.inv`? — `HasFDerivAt.inv (hf : HasFDerivAt f f' x) (h0 : f x ≠ 0) : HasFDerivAt (fun x => (f x)⁻¹) (-(f x)⁻² • f') x` — let me check. That's the cleanest: build HasFDerivAt for denom, then .inv, then .fderiv, then apply to v.

Let me grep HasFDerivAt.inv.
dsh: reasoning:
There's no plain `fderiv_inv` for composed functions or `HasFDerivAt.inv` in this revision (only `fderiv_inv'` on the raw `Inv.inv`). So I'll compose via `fderiv_comp`:

```lean
fderiv ℝ (fun y => (denom y)⁻¹) x 
  = (fderiv ℝ Inv.inv (denom x)).comp (fderiv ℝ denom x) := fderiv_comp hdiff_inv hdiff_denom
  = -(mulLeftRight ℝ ℝ (denom x)⁻¹ (denom x)⁻¹).comp (fderiv ℝ denom x) := by rw [fderiv_inv' hne]
```
Then applying to v and simplifying `mulLeftRight` — need `mulLeftRight_apply`? Let me check the definition: `mulLeftRight 𝕜 R a b : R →L[𝕜] R` — `mulLeftRight_apply`-style simp: probably `@[simp] theorem mulLeftRight_apply (a b x) : mulLeftRight 𝕜 R a b x = a * x * b`. Hmm — it might be `continuous_mulLeftRight_apply`. Let me check quickly. Actually — alternative that avoids mulLeftRight entirely: work with `HasFDerivAt` and `DifferentiableAt.inv` + the derivative value computed via `fderiv_comp`... hmm — the value IS `-mulLeftRight...` regardless.

OR — cleaner idea: avoid differentiating the composed inv at all by choosing the metric so that NO inv differentiation is needed at the level I care about. Recall what needs fderiv-of-inv:
1. dFamily = fderiv of g — g = (1+x₁²)·δ is POLYNOMIAL — NO inv! ✓
2. ∂Γ — fderiv of explicit Γ = fderiv of ±x₁·(1+x₁²)⁻¹ — inv IS needed here. Unavoidable for curvature (unless the Christoffel family is polynomial — which happens iff... Γ = x₁(1+x₁²)⁻¹ — no).

So I need ONE fderiv-of-inv computation: `fderiv ℝ (fun x : ChartPoint (Fin 2) => -x 1 * (1 + x 1^2)⁻¹) 0` applied to e₁ = single 1 1. Hmm — wait — but ALSO — hmm — the RHS of the curvature evaluation needs ∂₂Γ¹₁₂ — Γ¹₁₂ = 0 identically — fderiv of the constant-zero function — trivial. And ∂₁Γ¹₂₂(0) — the inv-derivative above. And the ΓΓ(0) terms: 0·Γ sums — trivial. So exactly ONE inv-fderiv computation. 

For that single computation, the cleanest proof strategy: use `HasFDerivAt` lemmas directly to build the derivative of `fun x => (1 + x 1^2)⁻¹`:
- `hasFDerivAt_proj`: `HasFDerivAt (fun x => x 1) (ContinuousLinearMap.proj 1) x` — from `ContinuousLinearMap.hasFDerivAt`? There's `ContinuousLinearMap.hasFDerivAt : HasFDerivAt f f x`? Let me check — in Linear.lean there's likely `protected theorem hasFDerivAt : HasFDerivAt f f x := f.isBoundedLinearMap.hasFDerivAt`. Yes probably.
- `.pow 2`, `.const_add 1`?, `.inv hne` — hmm — is there `HasFDerivAt.inv`? grep said no... — then for inv: `(hasFDerivAt_inv' hne).comp x hdenom` — `HasFDerivAt.comp` — `hasFDerivAt_inv' (hx : x ≠ 0) : HasFDerivAt 𝕜 Inv.inv (-mulLeftRight 𝕜 R x⁻¹ x⁻¹) x` — then `.comp hdenom`. Then the full derivative is `(-mulLeftRight ... (denom x)⁻¹ (denom x)⁻¹).comp (fderiv-ish of denom)`. Then `.fderiv`, then evaluate.

Simplification burden: `mulLeftRight 𝕜 ℝ a b v = a * v * b` — let me check the simp lemma exists: grep mulLeftRight_apply.

Then the value at v = e₁: `-(denom 0)⁻¹ * (fderiv denom 0 e₁) * (denom 0)⁻¹` — denom 0 = 1, fderiv denom 0 e₁ = 2·(proj₁ e₁)·(x₁=0) = 0... wait — ∂₁Γ¹₂₂(0) = fderiv of −x₁(1+x₁²)⁻¹ at 0 along e₁. By product rule: Γ¹₂₂ = −(proj₁)·denom⁻¹ — derivative at 0 = −(fderiv proj₁ e₁)·denom(0)⁻¹ − 0·(fderiv inv e₁) = −1·1 = −1. So via `fderiv_mul`: `fderiv ℝ (fun x => -x 1 * denom⁻¹) 0 e₁` = ... = −1. Using fderiv_const_mul/fderiv_mul + fderiv_inv' + fderiv_proj, then norm_num/ring.

Fine — let me now also handle the Fin-2 bookkeeping. ι = Fin 2. ChartPoint (Fin 2) = Fin 2 → ℝ. DecidableEq (Fin 2) ✓, Fintype ✓. The datum needs `[Fintype ι] [DecidableEq ι]` — instantiate with Fin 2.

For the explicit computations with `if i = j` on Fin 2: the `fin_cases` tactic + `norm_num` handles.

One important check: the SmoothChartData's inv_mul field: `∀ x k j, (∑ l : ι, gInv x k l * g x l j) = if k = j then 1 else 0`. For our diagonal data: the sum over l — only l = k and l = j contribute... prove by cases: if k = j: Σ_l gInv k l g l j = gInv k k·g k k (other l: gInv k l = 0 or g l j = 0) — hmm — l ≠ k → gInv k l = 0; l ≠ j → g l j = 0; if k = j, need Σ = (1+x₁²)⁻¹·(1+x₁²) = 1. Via `Finset.sum_eq_single k` — with the side conditions. Or simpler: since Fin 2, `fin_cases k; fin_cases j; norm_num [g, gInv]; field_simp/nlinarith`-style: the sum becomes explicit 2 terms — `norm_num` evaluates the sums to explicit expressions in x 1 — then `field_simp [sq_nonneg]`-ish — need (1+x₁²) ≠ 0 — `nlinarith [sq_nonneg (x 1)]`.

Now the metric datum fields g_smooth, gInv_smooth: ContDiff ℝ ∞ of `fun x => (1 + x 1 ^ 2) * (if i = j then 1 else 0)` — by cases on i=j: constant 1+x₁²... The if: `(if i = j then 1 else 0 : ℝ)` — ContDiff of `x ↦ (1+x₁²)·δ` — via ContDiff.mul (contDiff_const + pow of proj...). ContDiff of `fun x => x 1` — hmm — ContDiff of projections: is there `contDiff_proj`? Or use `ContinuousLinearMap.proj 1` — `ContDiff.continuousLinearMap`-ish: `(proj 1).contDiff` — there's `ContDiff.clm_apply` or `ContinuousLinearMap.contDiff`? In this mathlib: `ContDiff.continuousLinearMap`? Hmm — I'll write a helper: 
```lean
lemma contDiff_proj (i : ι) : ContDiff ℝ ∞ (fun x : ChartPoint ι => x i) :=
  (ContinuousLinearMap.proj i).contDiff
```
`ContinuousLinearMap.contDiff : ContDiff 𝕜 ∞ f` — exists? There's `ContinuousLinearMap.contDiff` in ContDiff/Linear? I think `ContDiff.continuousLinearMap`... I'll test. Otherwise: `contDiff_pi.mp (contDiff_id) i`-style: `ContDiff ℝ ∞ (id : ChartPoint ι → ChartPoint ι)` = contDiff_id; then `(contDiff_pi.mp contDiff_id) i : ContDiff ℝ ∞ (fun x => id x i)` = `fun x => x i` ✓. That works: `contDiff_pi.mp contDiff_id` — hmm — contDiff_pi : `ContDiff 𝕜 n F ↔ ∀ i, ContDiff 𝕜 n (fun x => F x i)` — `contDiff_pi.mp contDiff_id : ∀ i, ContDiff ℝ ∞ (fun x => id x i)` — and `id x i = x i` — defeq ✓.

Then `1 + x 1^2` — `ContDiff.add contDiff_const (ContDiff.pow (proj) 2)`. `ContDiff.pow` exists. And the `if`-δ: define g via `if i = j then (1 + x 1^2) else 0` — hmm — ContDiff of if-then-else functions — messy. ALTERNATIVE: define g without if: `g x i j := (1 + x 1 ^ 2) * (if i = j then 1 else 0)` — ContDiff of the product of a smooth function with a CONSTANT (the δ is x-independent): `ContDiff.mul hpoly (contDiff_const)` — the constant `if i=j then 1 else 0` — ContDiff of a constant function ✓ (contDiff_const). 

Similarly gInv x i j := (1 + x 1^2)⁻¹ * (if i=j then 1 else 0) — ContDiff.mul (ContDiff.inv? hmm — ContDiff of (1+x₁²)⁻¹: `ContDiff.inv`? — there's `ContDiff.inv` for functions into a field with h0: `ContDiff.inv (hf : ContDiff 𝕜 n f) (h0 : ∀ x, f x ≠ 0) : ContDiff 𝕜 n (fun x => (f x)⁻¹)` — probably exists. Let me verify at compile.

inv_mul proof: as above with fin_cases + field_simp/nlinarith.

dFamily_eq: the fderiv proof. g y j k = (1 + y 1^2)·δ(j,k). fderiv = δ(j,k)·fderiv(1+y 1^2) — via fderiv_const_mul? — `fderiv_const_mul (ha) (b) : fderiv 𝕜 (fun x => a x * b) x = fderiv 𝕜 a x * b`? Hmm — `fderiv_const_mul (ha : DifferentiableAt 𝕜 a x) (b : 𝔸) : fderiv 𝕜 (fun x => a x * b) x = (fderiv 𝕜 a x) * b` — hmm — `*` of a CLM by b — for ℝ-valued fderiv: `(fderiv 𝕜 a x) * b` = `fun v => fderiv a x v * b` — probably there's a nicer version... The lemma at Mul.lean:379: `fderiv_const_mul (ha) (b) : fderiv 𝕜 (fun x => a x * b) x = fderiv 𝕜 a x * b` — the codomain is 𝔸 (an algebra) — for 𝔸 = ℝ fine.

Then fderiv of `1 + y 1^2`: `fderiv_add`/`fderiv_const_add` — `fderiv_const_add (c : F) : fderiv 𝕜 (fun y => c + f y) x = fderiv 𝕜 f x` ✓ (Add.lean:384). Then `fderiv_pow 2 hdiff : fderiv ℝ (fun y => y 1 ^ 2) x = (2 • (x 1) ^ 1) • fderiv ℝ (fun y => y 1) x` — then fderiv_proj — then apply to `single i 1`: `(2 • x 1) • (ContinuousLinearMap.proj 1) (single i 1) = 2·x 1·(if i = 1 then 1 else 0)` — `proj_apply` + `Pi.single_eq_same/ne`.

So dFamily c x i j k = (if j = k then 1 else 0)·2·x 1·(if i = 1 then 1 else 0) — hmm wait — careful — `single i 1` at slot 1: `Pi.single (i) (1 : ℝ) 1 = if i = 1 then 1 else 0` ✓.

christoffelFamily_eq: christoffelFamily x k i j = Σ_l gInv x k l · (2⁻¹·(d i j l + d j i l − d l i j)) — with our explicit d — then the sum over l ∈ Fin 2 — `fin_cases k <;> fin_cases i <;> fin_cases j <;> norm_num [...]` — the sums become 2-term sums — `norm_num` can evaluate Finset sums over Fin 2? The Finset.univ over Fin 2 — `norm_num` can handle `∑ l : Fin 2, f l`? — `norm_num` knows `Fin.sum_univ_two`? There's `Fin.sum_univ_two` lemma. `fin_cases` + `simp` — maybe. I'll use explicit: `rw [Fin.sum_univ_two]` then norm_num. 

Curvature: define explicit Γ¹₂₂ and Γ¹₁₂ as functions: `Gamma122 x := -x 1 * (1 + x 1^2)⁻¹` etc. Then `R2120 := fderiv ℝ (fun x => Gamma122 x) 0 (single 1 1) − fderiv ℝ (fun x => Gamma112 x) 0 (single 2 1) + Σₘ (Γ¹₁ₘ(0)Γᵐ₂₂(0) − Γ¹₂ₘ(0)Γᵐ₁₂(0))` — the sums vanish since Γ(0) = 0 — and ∂₂Γ¹₁₂ = 0 since Γ¹₁₂ ≡ 0 — so R = fderiv of Gamma122 at 0 along e₁ = −1.

Hmm wait — I need to double check the index convention I'll use, and present it honestly: define `Rcomp x l k i j := fderiv ℝ (fun z => Γ l k j z) x (single i 1) − fderiv ℝ (fun z => Γ l k i z) x (single j 1) + Σₘ (Γ l i m x · Γ m k j x − Γ l j m x · Γ m k i x)` — the standard component formula R^l_{kij} = ∂ᵢΓˡₖⱼ − ∂ⱼΓˡₖᵢ + ΓˡᵢₘΓᵐₖⱼ − ΓˡⱼₘΓᵐₖᵢ. Hmm — wait — earlier I derived from the DEFINITION: R^l_{kij} = ∂ᵢΓˡⱼₖ − ∂ⱼΓˡᵢₖ + ΓˡᵢₘΓᵐⱼₖ − ΓˡⱼₘΓᵐᵢₖ. Hmm — these differ: ∂ᵢΓˡⱼₖ vs ∂ᵢΓˡₖⱼ — same by Γ-symmetry ✓. ΓˡᵢₘΓᵐⱼₖ vs ΓˡᵢₘΓᵐₖⱼ — same by symmetry of the second Γ ✓. So both are equal given christoffel_symm. For the module I'll use one and note the convention. Let me re-derive once more to be safe: R(eᵢ,eⱼ)eₖ = ∇ᵢ∇ⱼeₖ − ∇ⱼ∇ᵢeₖ = ∇ᵢ(Γᵐⱼₖ eₘ) − ∇ⱼ(Γᵐᵢₖ eₘ) = (∂ᵢΓᵐⱼₖ)eₘ + ΓᵐⱼₖΓˡᵢₘeₗ − (∂ⱼΓᵐᵢₖ)eₘ − ΓᵐᵢₖΓˡⱼₘeₗ → Rˡₖᵢⱼ = ∂ᵢΓˡⱼₖ − ∂ⱼΓˡᵢₖ + ΓˡᵢₘΓᵐⱼₖ − ΓˡⱼₘΓᵐᵢₖ. And with (l k i j) = (1 2 1 2): R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₁₂ + Γ¹₁ₘΓᵐ₂₂ − Γ¹₂ₘΓᵐ₁₂ ✓ — matches my sphere check (R¹₂₁₂ = 4 with K = 1·(g₁₁g₂₂−g₁₂²)/g₁₁·... — hmm — wait — earlier sphere check: ⟨R(e₁,e₂)e₂,e₁⟩ = g₁₁R¹₂₁₂ — and K = ⟨R(e₁,e₂)e₂,e₁⟩/(g₁₁g₂₂−g₁₂²) = 4·4/16 = 1 ✓).

For our conformal metric g = (1+x₁²)δ: R¹₂₁₂(0) = ∂₁Γ¹₂₂(0) − 0 + 0 = −1. K(0) = g₁₁(0)·R¹₂₁₂(0)/(g₁₁(0)g₂₂(0)) = 1·(−1)/1 = −1 ✓.

In the module, to keep it honest and minimal, I'll:
1. Define the datum (g, gInv + fields).
2. Prove dFamily_eq (fderiv computation — general x).
3. Prove christoffelFamily_eq (explicit formula, general x).
4. Define the component formula `riemannComp` (on ANY explicit Γ family: two directional fderivs + quadratic sums).
5. Prove `conformal_riemannComp_1212_origin : riemannComp ... 1 2 1 2 0 = -1` — evaluating to −1 ≠ 0 → `conformal_curvature_nonzero : ... ≠ 0`.
6. Smoothness notes + docstring classifying as MODEL.

Hmm — wait — for honesty, the "riemannComp" on explicit Γ — the curvature is computed from the metric-coefficient-derived Christoffel family (proved equal to the explicit Γ), so the provenance is: coefficients → (proved) Γ → curvature formula. The formula itself is the classical component formula applied to our connection — for a MODEL computation this is the standard approach; I'll state it as the standard Riemann tensor component formula with the derivation (∇∇−∇∇−∇[,]) in the docstring. The identity "Rcomp = the actual curvature operator of nabla" at the algebraic level would be the deep bridge — for the model, the component formula IS the definition of the model's curvature; recorded as such.

Hmm — actually — could I instead define the model's curvature honestly through the EXISTING general machinery? The general `chartCurvatureOperator` from ChartLeviCivitaForm is at the POINTWISE level: curvature operator from the pointwise connection (algebraic). At the origin with our datum: the pointwise ChartMetricCoefficients at 0 has d = 0 → christoffel = 0 → pointwise curvature operator = 0 — the pointwise datum CAN'T see the curvature (needs ∂Γ). So no — the component formula with ∂Γ is the honest route for the model.

Given the risk, let me now write the module and iterate. File: `release/Poincare/D12/ConnectionCurvature/ChartCurvatureModel.lean`. Name — maybe `ConformalModel2D.lean`. I'll go with `ConformalChartModel.lean`.

Note the module needs: imports Mathlib.Tactic, ContDiff bits, FDeriv bits, ChartLeviCivitaSmooth (for SmoothChartData, ChartPoint, VectorField...). 

Let me write it now, then iterate on errors. Time check: ~1h05m elapsed. Budget ~75 min for this module.
dsh: reasoning:
WAIT — I made an error in the last step. Let me check the index computation again:

`riemannComp Γ 0 (0) (1) (0) (1)` = with l=0, k=1, i=0, j=1:
`∂ᵢΓˡₖⱼ − ∂ⱼΓˡₖᵢ + Σₘ (ΓˡᵢₘΓᵐₖⱼ − ΓˡⱼₘΓᵐₖᵢ)`
= `∂₀Γ¹₁₁ − ∂₁Γ¹₁₀ + Σₘ (Γ¹₀ₘΓᵐ₁₁ − Γ¹₁ₘΓᵐ₁₀)`.

Hmm — with l=0 (Fin 2 index 0 = the 1-slot in math notation... careful: I'm writing Fin 2 where 0 ↔ index 1 (first coordinate) and 1 ↔ index 2 (second coordinate). So l=0 means the upper index 1, k=1 means lower index 2, i=0 means ∂₁, j=1 means ∂₂. So `riemannComp Γ 0 l=0 k=1 i=0 j=1` = R¹₂₁₂ = ∂₁Γ¹₁₂ − ∂₂Γ¹₁₁ + Σₘ(Γ¹₁ₘΓᵐ₁₂ − Γ¹₂ₘΓᵐ₁₁).

Hmm — that's R¹₂₁₂ = ∂₁Γ¹₁₂ − ∂₂Γ¹₁₁ + ... — but the component I WANT is R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₂₁ + ... — wait — R^l_{kij} with (l,k,i,j) = (1,2,1,2): R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₂₁ + Γ¹₁ₘΓᵐ₂₂ − Γ¹₂ₘΓᵐ₁₂. In Fin 2 encoding: l = 0, k = 1, i = 0, j = 1. My riemannComp def: `∂ᵢΓˡₖⱼ − ∂ⱼΓˡₖᵢ + Σₘ(ΓˡᵢₘΓᵐₖⱼ − ΓˡⱼₘΓᵐₖᵢ)` — with l=0,k=1,i=0,j=1: `∂₀Γ(0,1,1) − ∂₁Γ(0,1,0) + Σₘ(Γ(0,0,m)Γ(m,1,1) − Γ(0,1,m)Γ(m,1,0))` = `∂₁Γ¹₂₂ − ∂₂Γ¹₂₁ + Σₘ(Γ¹₁ₘΓᵐ₂₂ − Γ¹₂ₘΓᵐ₂₁)` ✓✓ — that's exactly R¹₂₁₂ (the math-convention). 

So R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₂₁ + ΓΓ. Γ¹₂₁ = Γ¹₁₂ = 0 ✓. Γ¹₂₂ = −x₁(1+x₁²)⁻¹. ∂₁Γ¹₂₂(0) = −1. So the surviving term is `fderiv ℝ (fun z => -z 1 * (1 + z 1^2)⁻¹) 0 (single 0 1)` — direction i = 0 = e₁ — and `single 0 1` applied to slot... the direction vector `single 0 1` has 1 in slot 0 and 0 in slot 1. And the function is `-z 1 * ...` — depends on slot 1! So the derivative in direction e₀ (slot 0) of a function depending only on slot 1 is... the fderiv is a linear map = (derivative w.r.t. slot-1 stuff) ∘ proj₁ — applied to e₀ = (1,0): proj₁(e₀) = 0 → derivative = 0!!! 

WAIT — CONVENTION ERROR. Let me recheck: In my math derivation, `∂₁` means derivative in the FIRST coordinate direction (x₁). In the Fin 2 encoding, the FIRST coordinate is slot 0 and the SECOND coordinate is slot 1. The metric g = (1 + x₁²)δ — I encoded x₁ as `x 1` (slot 1 = SECOND coordinate)! So "x₁" in my math = `x 1` in Lean = the second coordinate. Then ∂₁ = direction e₁ = `single 1 1` (slot 1), NOT `single 0 1`.

So the surviving term should be: direction `single 1 1` — the derivative along the coordinate that the metric depends on. Let me redo the component check with this convention:

g depends on slot 1 (x 1). dFamily: ∂ᵢgⱼₖ = 2·(x 1)·[i = 1]·[j=k] — matches my dFamily_conformal statement (uses `if i = 1` — Fin 2's index 1 — i.e., the second slot — consistent with the metric depending on slot 1) ✓.

Γ¹₁₁ = γ = x 1·(1+x 1²)⁻¹ — in Fin 2 encoding: k=0,i=0,j=0 → `conformalGamma (0) (0) (0) x = x 1 * (...)⁻¹` ✓ (my match clause `| ⟨0,_⟩,⟨0,_⟩,⟨0,_⟩ => x 1 * ...` ✓).
Γ¹₂₂ = −γ: k=0,i=1,j=1 → `| ⟨0,_⟩,⟨1,_⟩,⟨1,_⟩ => -x 1 * ...` ✓.
Γ¹₁₂ = Γ¹₂₁ = 0: k=0,i=0,j=1 and k=0,i=1,j=0 → 0 ✓.
Γ²₁₁ = 0: k=1,i=0,j=0 → 0 ✓.
Γ²₁₂ = Γ²₂₁ = γ: k=1,i=0,j=1 and k=1,i=1,j=0 → `x 1 * ...` ✓.
Γ²₂₂ = 0 ✓.

Now R¹₂₁₂ in Fin-encoding: l=0, k=1, i=0, j=1 — R¹₂₁₂ = ∂ᵢΓˡₖⱼ − ∂ⱼΓˡₖᵢ + ... = ∂₀Γ¹₂₂ − ∂₁Γ¹₂₁ + ΓΓ — where ∂₀ = derivative along slot 0, ∂₁ = along slot 1. Γ¹₂₂ = −x₁·denom⁻¹ depends on slot 1. So ∂₀Γ¹₂₂ = 0!!! Hmm — that gives R¹₂₁₂ = 0?!

Hold on — let me recompute the math with the correct identification. In MATH notation: coordinates (x₁, x₂) — the metric depends on x₁. In LEAN: slot 0 = x₁, slot 1 = x₂?? — I need to DECIDE: my g def uses `x 1` — so Lean-slot 1 is the coordinate the metric depends on. If Lean-slot 0 = math-x₁ and Lean-slot 1 = math-x₂, then the metric depends on math-x₂. Everything is consistent either way — just relabeling. But my `conformalGamma` clauses assumed math-index k=1 ↔ Fin 0 (upper index 1 = Fin 0). The "first coordinate" (math x₁) is Fin slot 0. But g depends on `x 1` = Fin slot 1 = math x₂. So the metric is g = (1 + x₂²)δ — depends on the SECOND coordinate. Then ∂₁ (math, derivative in x₁-direction) = Fin-direction `single 0 1` — and Γ¹₂₂ = −x₂·denom⁻¹ — ∂₁Γ¹₂₂ = ∂_{x₁}(−x₂·denom⁻¹) = 0. So R¹₂₁₂ (math convention (l,k,i,j)=(1,2,1,2)) = ∂₁Γ¹₂₂ − ∂₂Γ¹₂₁ + ... = 0 − 0 = 0?? But that contradicts K = −1/(1+x₁²)³... 

Wait — no — the math K formula I derived: K = (f'² − 2ff'')/(4f³) with f = (1+x₁²), derivatives in x₁. If the metric depends on x₂ instead, then K = (f'²−2ff'')/(4f³) with f = f(x₂) — and R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₂₁ — with the roles of 1,2 swapped: the nonzero component would be R²₁₂₁ = ∂₂Γ²₁₁... hmm — let me recompute with metric g = (1+x₂²)δ (depends on second coordinate): by symmetry (swap coordinates 1↔2), the nonzero curvature component is R²₁₂₁ = ∂₂Γ²₁₁ − ∂₁Γ²₂₁ + ... — wait — hmm — let me recompute directly. For g = f(x₂)δ: dᵢgⱼₖ = f'(x₂)[i=2]δⱼₖ — wait — using math indices 1,2 with the metric depending on coordinate 2: ∂ᵢgⱼₖ = f'·[i=2]·δⱼₖ. Γ^k_{ij} = (1/2)f⁻¹(∂ᵢgⱼₖ + ∂ⱼgᵢₖ − ∂ₖgᵢⱼ) = (1/2)f⁻¹f'([i=2]δⱼₖ + [j=2]δᵢₖ − [k=2]δᵢⱼ). Nonzero components: Γ¹₁₁ = (1/2)φ·(0+0−0) = 0; Γ¹₁₂ = (1/2)φ(0 + δ₁₁·... [j=2]δᵢₖ: i=1,k=1 → 0; [k=2]δᵢⱼ = 0) = 0; Γ¹₂₂ = (1/2)φ([i=2]δⱼₖ: j=2,k=1 → 0 + [j=2]δᵢₖ: i=1,k=1 → 0 − [k=2]δ₁₂ = 0)... hmm that gives all Γ¹** = 0?! Let me redo carefully with φ = f'/f: Γ^k_{ij} = (1/2)(φ_jδ_{ki} + φ_iδ_{kj} − φ_kδ_{ij}) where φ_i = φ·[i=2] — the derivative of ln f. Γ¹₂₂ = (1/2)(φ₂δ₂₁ + φ₂δ₂₁ − φ₁δ₂₂) = (1/2)(0 + 0 − 0) = 0?? That's wrong — earlier for f = f(x₁) I got Γ¹₂₂ = −φ₁/2. Let me redo: Γ^k_{ij} = (1/2)g^{kl}(g_{jl,i} + g_{il,j} − g_{ij,l}) = (1/2)δ^{kl}f⁻¹f'([i]δⱼₗ + [j]δᵢₗ − [l]δᵢⱼ) — hmm — my formula: ∂ᵢgⱼₗ = f'[i=2]δⱼₗ. So Γ^k_{ij} = (1/2)f⁻¹f'( [i=2]δⱼₖ + [j=2]δᵢₖ − [k=2]δᵢⱼ ). Γ¹₂₂: i=2,j=2,k=1: (1/2)φ([2=2]δ₂₁ + [2=2]δ₂₁ − [1=2]δ₂₂) = (1/2)φ(0+0−0) = 0?? But for f=f(x₁) (depending on coord 1): Γ¹₂₂ = (1/2)φ([1=1]δ₂₁ + [1=1]δ₂₁ − [1=1]δ₂₂) = (1/2)φ(0+0−1) = −φ₁/2 ✓. So for f = f(x₂): Γ¹₂₂ = (1/2)φ([2=2]δ₂₁·...) — hmm — δ₂₁ = 0 — wait — the formula Γ^k_{ij} = (1/2)(φ_jδ_{ki} + φ_iδ_{kj} − φ_kδ_{ij}) with φ_i = ∂ᵢln f = φ·[i=2]: Γ¹₂₂ = (1/2)(φ₂δ₁₂ + φ₂δ₁₂ − φ₁δ₂₂) = (1/2)(0 + 0 − 0) = 0. Hmm — and Γ²₂₂ = (1/2)(φ₂δ₂₂ + φ₂δ₂₂ − φ₂δ₂₂) = φ/2. Γ²₁₁ = (1/2)(φ₁δ₂₁ + φ₁δ₂₁ − φ₂δ₁₁) = −φ/2. Γ²₁₂ = (1/2)(φ₂δ₂₁ + φ₁δ₂₂ − φ₂δ₁₂) = φ/2. Γ¹₁₁ = 0. Γ¹₁₂ = (1/2)(φ₂δ₁₁ + φ₁δ₁₂ − φ₂δ₁₂) = φ/2 — hmm — Γ¹₁₂ = (1/2)(φ₂·1 + 0 − 0) = φ/2.

So with g = f(x₂)δ: nonzero Γ: Γ²₂₂ = φ/2, Γ²₁₁ = −φ/2, Γ²₁₂ = φ/2, Γ¹₁₂ = φ/2 (and symmetric slots Γ¹₂₁ = φ/2, Γ²₂₁ = φ/2). Hmm — wait — Γ¹₁₂ should equal Γ¹₂₁ (symmetry) — Γ¹₂₁ = (1/2)(φ₁δ₂₁·... = (1/2)(φ₁δ₁₂ + φ₂δ₁₁ − φ₁δ₂₁) = φ₂/2 = φ/2 ✓ consistent.

Then R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₂₁ + ΓΓ = 0 − ∂₂(φ/2) + ... — ∂₂(φ/2) = φ'/2 ≠ 0 → R¹₂₁₂ = −φ'/2 ≠ 0!! So R¹₂₁₂ = −(1/2)(ln f)'' at... hmm wait — but that contradicts my hand computation for f = f(x₁) where R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₂₁ = −φ₁'/2·... let me redo the f=f(x₁) case with the CORRECT formula R^l_{kij} = ∂ᵢΓˡⱼₖ − ∂ⱼΓˡᵢₖ + ΓˡᵢₘΓᵐⱼₖ − ΓˡⱼₘΓᵐᵢₖ: R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₁₂ + Γ¹₁ₘΓᵐ₂₂ − Γ¹₂ₘΓᵐ₁₂ — Γ¹₂₂ = −φ₁/2 → ∂₁ = −φ''/2; Γ¹₁₂ = φ₂/2 = 0 → ∂₂ = 0; ΓΓ: Γ¹₁₁Γ¹₂₂ + Γ¹₁₂Γ²₂₂ − Γ¹₂₁Γ¹₁₂ − Γ¹₂₂Γ²₁₂ = (φ₁/2)(−φ₁/2) + 0 − 0 − (−φ₁/2)(φ₁/2) = −φ₁²/4 + φ₁²/4 = 0. So R¹₂₁₂ = −φ''/2. With f = (1+x₁²): φ = −ln(1+x₁²), φ'' = −2(1−x₁²)/(1+x₁²)² → at 0: −2. R¹₂₁₂(0) = −(−2)/2 = 1. K = R¹₂₁₂/g₁₁ — hmm — wait — with the formula R^l_{kij} = ∂ᵢΓˡⱼₖ − ∂ⱼΓˡᵢₖ + ... — ⟨R(e₁,e₂)e₂,e₁⟩ = g₁₁R¹₂₁₂ = 1·1 = 1 and K = 1/(g₁₁g₂₂) = 1 — consistent with K = (f'²−2ff'')/(4f³) at 0 = (0−2·(−2))/4 = 1 ✓✓. GOOD.

Earlier I "verified" the formula with the sphere and got R¹₂₁₂ = 4 with the ∂ᵢΓˡⱼₖ-form — consistent.

So NOW for my Lean metric g = (1+x₁²)δ where the Lean `x 1` is the coordinate it depends on — treat math coordinates (x₁, x₂) := (Lean slot 0, Lean slot 1). The metric depends on math-x₂. By the f(x₂) computation: nonzero Γ: Γ¹₁₂ = Γ¹₂₁ = φ/2, Γ²₁₁ = −φ/2, Γ²₁₂ = Γ²₂₁ = φ/2, Γ²₂₂ = φ/2 — WAIT — hmm — my `conformalGamma` clauses in the Lean file have Γ¹₁₁ = γ, Γ¹₂₂ = −γ, Γ²₁₂ = γ — those were for f = f(x₁)!! (f depends on Lean slot... let me recompute for g = f(Lean-slot-1)·δ — i.e., f = f(x₂) in math notation.)

Hmm — actually — I should just make the metric depend on Lean slot 0 (x 0) so the Lean encoding matches the f(x₁) hand computation directly. g = (1 + (x 0)²)·δ — then Γ¹₁₁ = γ, Γ¹₂₂ = −γ, Γ²₁₂ = γ with γ = (x 0)·(1+(x 0)²)⁻¹, all others 0 — matching my `conformalGamma` clauses EXACTLY (they use `x 1`... I wrote `x 1` in the clauses — need to change to `x 0`).

Then dFamily_conformal: ∂ᵢgⱼₖ = 2·(x 0)·[i = 0]·[j=k] — change `x 1` → `x 0` and `if i = 1` → `if i = 0`.

And denom = 1 + x 0^2.

Then R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₂₁ + ΓΓ (math (1,2,1,2) = Lean (0,1,0,1)): riemannComp Γ 0 l=0 k=1 i=0 j=1 = ∂₀Γ(0,1,1) − ∂₁Γ(0,1,0) + Σₘ(Γ(0,0,m)Γ(m,1,1) − Γ(0,1,m)Γ(m,1,0)) = ∂₁Γ¹₂₂ − ∂₂Γ¹₂₁ + Γ¹₁ₘΓᵐ₂₂ − Γ¹₂ₘΓᵐ₁₂ ✓ — the surviving term: fderiv of `fun z => -z 0 * (1 + z 0^2)⁻¹` at 0 in direction `single 0 1` = −1 ✓. And Γ¹₂₁ = Γ¹₁₂ = 0 identically (my clause `| ⟨0,_⟩,⟨0,_⟩,⟨1,_⟩ => 0` ✓ — conformalGamma (0) (0) (1) = 0 ✓), the ΓΓ-sum vanishes at origin (all Γ(0) = 0 ✓ — conformalGamma_origin_zero ✓).

So changes needed:
1. denom: `1 + x 0 ^ 2` (and denom_pos/ne_zero accordingly).
2. g/gInv: `(1 + x 0 ^ 2)`.
3. dFamily_conformal: `2 * x 0 * (if i = 0 then 1 else 0) * ...`.
4. conformalGamma clauses: `x 1` → `x 0` everywhere.
5. The hmain: `fderiv ℝ (fun z => -z 0 * (1 + z 0 ^ 2)⁻¹) 0 (single 0 1) = -1` — and fill in the proof (currently `sorry` — MUST NOT leave sorry!). Note: I left a `sorry` placeholder in the file — that must be replaced before any build/audit.

Also `conformalGamma_112_zero` — the clause for (0,0,1) is 0 ✓.

Wait — also in the fderiv direction: `riemannComp` uses direction `single i 1` where i = 0 → e₀ = e₁(math) ✓ — the direction along the coordinate the metric depends on ✓.

Hmm — also the `hzero` rewrite: `conformalGamma (0) (0) (1) = fun _ => 0` — after `rw [hzero]` the fderiv term becomes `fderiv ℝ (fun z => 0) 0 (single 1 1)` — wait — j = 1 — the ∂ⱼΓˡₖᵢ term with j = 1: direction `single 1 1` — the function is now `fun z => 0` — fderiv_const_apply → 0 ✓.

But hmm — `conformalGamma_112_zero` says conformalGamma (0) (0) (1) x = 0 — and in riemannComp the ∂ⱼΓˡₖᵢ term is Γ l k i = Γ (0) (1) (0) — WAIT!! Let me recheck riemannComp's def: `fderiv ℝ (fun z => Γ l k j z) x (single i 1) − fderiv ℝ (fun z => Γ l k i z) x (single j 1) + Σₘ (Γ l i m · Γ m k j − Γ l j m · Γ m k i)`. With l=0, k=1, i=0, j=1: terms: `∂₀Γ(0,1,1)` − `∂₁Γ(0,1,0)` + `Σₘ(Γ(0,0,m)Γ(m,1,1) − Γ(0,1,m)Γ(m,1,0))`.

Math: R¹₂₁₂ = ∂₁Γ¹₂₂ − ∂₂Γ¹₂₁ + Σₘ(Γ¹₁ₘΓᵐ₂₂ − Γ¹₂ₘΓᵐ₂₁). Check riemannComp's formula against R^l_{kij} = ∂ᵢΓˡₖⱼ − ∂ⱼΓˡₖᵢ + ΓˡᵢₘΓᵐₖⱼ − ΓˡⱼₘΓᵐₖᵢ — hmm — the CORRECT form (from the derivation): R^l_{kij} = ∂ᵢΓˡⱼₖ − ∂ⱼΓˡᵢₖ + ΓˡᵢₘΓᵐⱼₖ − ΓˡⱼₘΓᵐᵢₖ. My def has `∂ᵢΓˡₖⱼ` — Γ l k j — vs correct Γ l j k — by symmetry Γˡₖⱼ = Γˡⱼₖ ✓ fine. Second term: def has `∂ⱼΓˡₖᵢ` = Γ l k i vs correct Γ l i k — symmetry ✓. Quadratic: def `ΓˡᵢₘΓᵐₖⱼ` vs correct ΓˡᵢₘΓᵐⱼₖ — Γᵐₖⱼ = Γᵐⱼₖ ✓. `ΓˡⱼₘΓᵐₖᵢ` vs ΓˡⱼₘΓᵐᵢₖ ✓. So the def is right (modulo symmetry, which holds — but NOTE: my riemannComp uses `Γ l k j` — with conformalGamma symmetric in (k,j) ✓ — `christoffelFamily_symm` ensures this for the real family ✓).

So in the proof of conformal_riemann_1212_origin, the ∂ⱼ term: Γ l k i = Γ(0,1,0) = conformalGamma (0) (1) (0) = 0 identically (clause `| ⟨0,_⟩,⟨1,_⟩,⟨0,_⟩ => 0` ✓) — so I should rewrite with `conformalGamma_110_zero`-style lemma: conformalGamma (0) (1) (0) x = 0. My proof currently wrote `christoffelFamily_conformal (k := 0) (i := 0) (j := 1)` for the SECOND term — WRONG — should be (k := 0) (i := 1) (j := 0) → Γ(0,1,0). Let me fix: the second christoffelFamily_conformal rewrite should target Γ l k i = Γ(0,1,0). And the "zero function" lemma should be for (0,1,0).

Hmm — wait — in my current file I wrote:
```
rw [christoffelFamily_conformal (k := 0) (i := 1) (j := 1)]  -- Γ(0,1,1) = Γ¹₂₂ ✓ first term
rw [christoffelFamily_conformal (k := 0) (i := 0) (j := 1)]  -- Γ(0,0,1) = Γ¹₁₂ — WRONG, should be Γ(0,1,0)
```
The riemannComp after unfold has `Γ l k j` = Γ(0,1,1) ✓ first and `Γ l k i` = Γ(0,1,0) second. So second rw should be (k := 0) (i := 1) (j := 0). And `conformalGamma_112_zero` should be about (0,1,0) — rename to `conformalGamma_101_zero`. Also the hquad sum: Γ l i m = Γ(0,0,m), Γ m k j = Γ(m,1,1), Γ l j m = Γ(0,1,m), Γ m k i = Γ(m,1,0) — matches my hquad (I wrote Γ(0,0,m)Γ(m,1,1) − Γ(0,1,m)Γ(m,1,0)) ✓.

And hmain — fill in the real proof of the fderiv. Also — the direction for the first term: `single i 1` with i = 0 ✓.

Also in `conformal_riemann_1212_origin`, I wrote the second rw with wrong indices and `hzero` for (0,0,1). Fix all.

NOW — the hmain proof — the crux. Need:

`fderiv ℝ (fun z : ChartPoint (Fin 2) => -z 0 * (1 + z 0 ^ 2)⁻¹) 0 (single 0 1) = -1`

Plan:
```lean
have hmain : fderiv ℝ (fun z : ChartPoint (Fin 2) => -z 0 * (1 + z 0 ^ 2)⁻¹)
    (0 : ChartPoint (Fin 2)) (Pi.single (M := fun _ : Fin 2 => ℝ) (0 : Fin 2) (1 : ℝ))
    = -1 := by
  -- write as fderiv of product: f z = -z 0, h z = (1 + z 0^2)⁻¹
  have hf : fderiv ℝ (fun z : ChartPoint (Fin 2) => -z 0) (0 : ChartPoint (Fin 2))
      = -(ContinuousLinearMap.proj (0 : Fin 2)) := by
    -- fderiv of -proj: fderiv_const_neg / fderiv_neg
    ...
  have hh : fderiv ℝ (fun z : ChartPoint (Fin 2) => (1 + z 0 ^ 2)⁻¹) (0 : ChartPoint (Fin 2))
      = 0 := by
    -- chain: inv ∘ (1 + sq ∘ proj0); at 0 the inner derivative has factor (0 0)=0
    ...
```
Hmm — for the product rule at the point 0: `fderiv_mul hfDiff hhDiff` gives
`fderiv ℝ (fun z => (-z 0) * (1 + z 0^2)⁻¹) 0 = (-0) • fderiv h 0 + ((1 + 0^2)⁻¹) • fderiv f 0`
= `0 • fderiv h 0 + 1 • (−proj 0)` = −proj 0. Applied to single 0 1: −(single 0 1 0) = −1 ✓. 

So I need:
(a) `fderiv ℝ (fun z => -z 0) 0 = -ContinuousLinearMap.proj 0` — via `fderiv_neg` + `fderiv_proj`. `fderiv_neg` — hmm — `fderiv_neg (h : DifferentiableAt 𝕜 f x) : fderiv 𝕜 (fun x => -f x) x = -fderiv 𝕜 f x` — exists in Add.lean presumably. Then `(fderiv_proj 0 0)` for the inner — wait — `fderiv_proj (x := 0) (i := 0) : fderiv ℝ (fun y => y 0) 0 = proj 0`.
(b) differentiability hypotheses: DifferentiableAt of `fun z => -z 0` at 0 — from contDiff_proj-derived: `(contDiff_proj (0 : Fin 2)).neg.contDiffAt.differentiableAt_one` or via `DifferentiableAt.neg`. And DifferentiableAt of `fun z => (1 + z 0^2)⁻¹` at 0: `(ContDiff.inv ... )`-derived or `DifferentiableAt.inv` — `((ContDiff.add contDiff_const (ContDiff.pow (contDiff_proj 0) 2)).inv (fun x => denom_ne_zero x)).contDiffAt.differentiableAt_one`.

Then evaluate: `fderiv_mul hf hh` — `fderiv_mul : fderiv 𝕜 (fun y => c y * d y) x = c x • fderiv 𝕜 d x + d x • fderiv 𝕜 c x` — with c = `fun z => -z 0`, d = `fun z => (1+z 0^2)⁻¹`. At x = 0: `(-0) • fderiv d 0 + (1+0²)⁻¹ • fderiv c 0` — applied to single 0 1: `(-0)•(fderiv d 0 v) + 1⁻¹·(fderiv c 0 v) = 0 + 1·(−(proj 0 v)) = −1` — norm_num + proj_apply + single_eq_same.

Careful with `(-z 0)` vs `-(z 0)`: `-z 0` parses as `-(z 0)` ✓. And `(-0 : ℝ) = 0` norm_num ✓.

Hmm — `fderiv_mul`'s exact statement: `fderiv_mul (hc : DifferentiableAt 𝕜 c x) (hd : DifferentiableAt 𝕜 d x) : fderiv 𝕜 (fun y => c y * d y) x = c x • fderiv 𝕜 d x + d x • fderiv 𝕜 c x` ✓.

Also the smul: `c x • fderiv d x` — c x : ℝ, fderiv d x : ChartPoint →L[ℝ] ℝ — smul on CLMs — applying to v: `(c x • L) v = c x • L v = c x * L v` — hmm — `c x • (L v)` — ℝ smul — = c x * L v ✓ (smul_eq_mul). simp should handle.

Let me now also double check `fderiv_const_add`'s statement: Add.lean:384: `fderiv_const_add (c : F) : fderiv 𝕜 (fun y => c + f y) x = fderiv 𝕜 f x` — wait — the goal function is `fun y => c + f y` — constant FIRST — my hfd in dFamily_conformal uses exactly that ✓.

Also in dFamily_conformal, the fderiv_pow usage:
```lean
have h := fderiv_pow (2 : ℕ) (contDiff_proj (1 : Fin 2)).contDiffAt.differentiableAt_one
-- h : fderiv ℝ (fun x => x 1 ^ 2) x = (2 • (x 1) ^ 1) • fderiv ℝ (fun x => x 1) x
simpa using h
```
Hmm — `fderiv_pow (n) (hf) : fderiv 𝕜 (f ^ n) x = (n • f x ^ (n - 1)) • fderiv 𝕜 f x` — here f = `fun y => y 1` — so `f ^ 2` = the pointwise power of the FUNCTION — `(f^2) y = (f y)^2` ✓. And `(2 • f x ^ (2-1)) • fderiv 𝕜 f x` — `2 • (x 1)^1` — `(x 1)^1 = x 1` (norm_num/simp). Target after my hfd statement: `(2 * x 1) • fderiv ...` — `2 • (x 1)^1` vs `2 * x 1` — smul_eq_mul + pow_one — simpa handles.

But WAIT — in dFamily_conformal I use `(contDiff_proj (1 : Fin 2))` — with the metric depending on `x 0` now, it should be `(0 : Fin 2)`. Also `fderiv_proj` — i = 0. And `Pi.single_eq_same` for slot i = 0, `if i = 0`.

ALSO — the direction in dFamily: `dFamily x i j k = fderiv ... (single i 1)` — the i-th coordinate direction ✓.

Let me also double check `fderiv_const_apply` usage in dFamily_conformal's off-diagonal case: the function `fun y => (1 + y 0^2) * 0 = 0` — hmm — after `simp [hjk]` the function becomes `fun y => 0 * something`? — `(if j = k then 1 else 0)` with hjk false → `if j=k...` = 0 — the function is `fun y => (1 + y 0^2) * 0` — simp rewrites to `fun y => 0`? Then fderiv_const_apply fires ✓ (as in nabla_const_apply, where it fired after the function became constant). Hmm — the `simp [hjk]` at the start of the off-diagonal case: after it, the goal might be `fderiv ℝ (fun y => (1 + y 0^2) * 0) x (single i 1) = 2*x0*... * 0` — the fderiv of `fun y => 0`-ish — fderiv_const_apply fires only if the function reduces to `fun _ => c` — `(1 + y 0^2) * 0` — simp normalizes to `0`? `mul_zero` — `(1+y0²)·0 = 0` — simp rewrites the body to 0 — the lambda `fun y => 0` — then fderiv_const_apply ✓. And RHS: `2 * x 0 * (if i=0 then 1 else 0) * 0 = 0` — simp/norm_num. Let me structure that case as: `simp [hjk]` then `rw [fderiv_const_apply]` then `simp`.

Hmm — wait — in the off-diagonal case the LHS function after simp[hjk]: does `(if j = k then 1 else 0 : ℝ)` inside g_conformal get rewritten? `simp [hjk]` — hjk : ¬ j = k — simp with the hypothesis hjk closes `if j=k then 1 else 0` → 0 — via `if_neg hjk`. But the dFamily def: `fderiv ℝ (fun y => c.g y j k) x (single i 1)` — with `rw [g_conformal]` FIRST: `fderiv ℝ (fun y => (1 + y 1^2) * (if j = k then 1 else 0)) x (single i 1)` — then `simp [hjk]` → `fderiv ℝ (fun y => 0) x (single i 1) = 2 * x 0 * (if i=0 then 1 else 0) * 0`. Then `rw [fderiv_const_apply]` → `0 = ...` → `simp`. OK.

Actually — simpler overall structure for dFamily_conformal: 
```lean
  unfold SmoothChartData.dFamily
  rw [g_conformal]
  by_cases hjk : j = k
  · have hg : (fun y : ChartPoint (Fin 2) => (1 + y 0 ^ 2) * (if j = k then 1 else 0 : ℝ))
        = fun y => 1 + y 0 ^ 2 := by funext y; simp [hjk]
    rw [hg]
    ...fderiv computation...
  · have hg : ... = fun _ => 0 := by funext y; simp [hjk]
    rw [hg]
    simp [hjk]
```
This is cleaner. The diagonal case: goal `fderiv ℝ (fun y => 1 + y 0^2) x (single i 1) = 2 * x 0 * (if i = 0 then 1 else 0) * 1`. Compute:
```lean
    have hfd : fderiv ℝ (fun y : ChartPoint (Fin 2) => 1 + y 0 ^ 2) x
        = (2 * x 0) • fderiv ℝ (fun y : ChartPoint (Fin 2) => y 0) x := by
      rw [fderiv_const_add]
      have h := fderiv_pow (2 : ℕ) (contDiff_proj (0 : Fin 2)).contDiffAt.differentiableAt_one
      simpa using h
    rw [hfd, fderiv_proj]
    simp only [ContinuousLinearMap.proj_apply, Pi.single_eq_same]
    by_cases hi : i = 0 <;> simp [hi]
    norm_num
```
Hmm — `fderiv_const_add : fderiv 𝕜 (fun y => c + f y) x = fderiv 𝕜 f x` — the goal function `fun y => 1 + y 0^2` ✓ — result: `fderiv ℝ (fun y => y 0^2) x` — hmm — the target of rw: rewrite the fderiv part of the whole goal — after `rw [fderiv_const_add]` the goal's LHS fderiv becomes `fderiv ℝ (fun y => y 0 ^ 2) x` — then h (from fderiv_pow) rewrites... Let me structure with calc on the fderiv-application:
```lean
    calc
      fderiv ℝ (fun y : ChartPoint (Fin 2) => 1 + y 0 ^ 2) x (Pi.single (M := fun _ : Fin 2 => ℝ) i (1 : ℝ))
          = fderiv ℝ (fun y : ChartPoint (Fin 2) => y 0 ^ 2) x (Pi.single ... i 1) := by
            rw [fderiv_const_add]
      _ = (2 * x 0) * (ContinuousLinearMap.proj (0 : Fin 2)) (Pi.single ... i 1) := by
            have h := fderiv_pow (2 : ℕ) (contDiff_proj (0 : Fin 2)).contDiffAt.differentiableAt_one
            rw [h, fderiv_proj]
            simp
      _ = 2 * x 0 * (if i = 0 then 1 else 0 : ℝ) := by
            simp only [ContinuousLinearMap.proj_apply, Pi.single_eq_same]
            by_cases hi : i = 0 <;> simp [hi]
```
Hmm — `rw [fderiv_const_add]` inside calc — the first step's RHS has fderiv of `y 0^2` — fine. Second step: `(2 • (x 0) ^ 1) • fderiv ℝ (fun y => y 0) x` — rw [h, fderiv_proj] → `(2 • (x 0)^1) • proj 0` — applying to v: `(2 • (x 0)^1) • ((proj 0) v)` — hmm — the smul on CLMs: `(a • L) v = a • L v` — simp handles → `(2 • (x 0)^1) * (proj 0 v)` — then `2 • (x 0)^1 = 2 * x 0` — simp [pow_one] — `(x 0)^1 = x 0` — norm_num-ish. I'll write `simp` and see.

Then the third step: `proj 0 (single i 1) = single i 1 0 = if i = 0 then 1 else 0` — proj_apply gives `(single i 1) 0` — `Pi.single_eq_same/ne` — simp with by_cases.

For the christoffelFamily_conformal proof: after `simp only [dFamily_conformal]` — hmm — wait — the d in the unfolded expression: `ChartMetricCoefficients.christoffelLower ... = (1/2) * (d i j l + d j i l − d l i j)` — the `dFamily_conformal` lemma rewrites `conformalChartData.dFamily x i j k` — the unfolded christoffel has `conformalChartData.dFamily x ...` — with the chartCoefficients field... `unfold SmoothChartData.chartCoefficients` replaces `(conformalChartData.chartCoefficients x).d` with `conformalChartData.dFamily x` ✓. Then `simp only [dFamily_conformal]` rewrites all dFamily occurrences ✓. Then `rw [Fin.sum_univ_two]` — the sum over Fin 2 becomes 2 explicit terms. Then fin_cases ×3 + norm_num — the `if i = 0` etc. in the d-expressions — fin_cases on k,i,j resolves them; then the terms are polynomials in `x 0`, `(1+x 0²)⁻¹` — and the RHS conformalGamma — the match unfolds — norm_num evaluates `(1/2 : ℝ)` arithmetic — hmm — `(1 / 2 : ℝ) * (2 * x 0 * 1 * 1 + ...)` — ring + field_simp — let me use `norm_num` then `ring`/`field_simp`? — the expressions have `(1 + x 0 ^ 2)⁻¹ * ...` — after norm_num the goal: `(1+x 0²)⁻¹ * (2⁻¹·(2·x 0 + ...))·...` hmm — messy — maybe `simp [dFamily_conformal]` then `fin_cases` then `norm_num` then `ring_nf`? — hmm — `ring_nf` handles inverses via field normalization if denominators provably nonzero? `ring_nf` can clear denominators using positivity? For `(1+x 0²)⁻¹ * (1+x 0²)`-type — ring_nf might leave inverses. Maybe `field_simp [denom_ne_zero]` then ring — let me write:
```
  fin_cases k <;> fin_cases i <;> fin_cases j <;> norm_num [denom_ne_zero]
  <;> field_simp <;> ring
```
Hmm — wait — the goal after norm_num: expressions like `(1 + x 0^2)⁻¹ * (2⁻¹ * (2 * x 0 + 2 * x 0 - 0))` hmm — `norm_num` evaluates `2⁻¹ * (2a + 2a − 0)`? norm_num computes on numerals but the `x 0` stays symbolic — it might reduce `2⁻¹ * (2·a + 2·a)` to... norm_num does ring-normalize numerals: `2⁻¹ * (2·a + 2·a − 0) = 2·a`? — norm_num can do this? It normalizes rational numerals and distributes? Probably it handles `(2⁻¹ : ℝ) * 2` = 1 and stays at `x 0 * 1 + x 0 * 1`? Hmm — I'll write `norm_num` then `ring` in the bullets and iterate on errors. Actually the structure: after `simp only [dFamily_conformal]` and `rw [Fin.sum_univ_two]`, then `fin_cases ...` — 8 cases — for each: the goal becomes a concrete equality with `if`s resolved (the d-expressions have `if i = 0` etc. — fin_cases + norm_num resolves the ifs since indices become numerals). Then the sums: `Fin.sum_univ_two` expands to `f ⟨0,_⟩ + f ⟨1,_⟩`. Then norm_num + ring + field_simp — I'll iterate.

For `conformal_riemann_1212_origin`: restructure:
```lean
lemma conformal_riemann_1212_origin :
    riemannComp (fun k i j x => conformalChartData.christoffelFamily x k i j)
      (0 : ChartPoint (Fin 2)) (0 : Fin 2) (1 : Fin 2) (0 : Fin 2) (1 : Fin 2) = -1 := by
  unfold riemannComp
  rw [christoffelFamily_conformal (k := (0 : Fin 2)) (i := (1 : Fin 2)) (j := (1 : Fin 2))]
  rw [christoffelFamily_conformal (k := (0 : Fin 2)) (i := (1 : Fin 2)) (j := (0 : Fin 2))]
  have hzero : conformalGamma (0 : Fin 2) (1 : Fin 2) (0 : Fin 2) = fun _ => 0 := by
    funext x
    exact conformalGamma_101_zero x
  rw [hzero]
  have hd2 : fderiv ℝ (fun z : ChartPoint (Fin 2) => (0 : ℝ)) (0 : ChartPoint (Fin 2))
      (Pi.single (M := fun _ : Fin 2 => ℝ) (1 : Fin 2) (1 : ℝ)) = 0 := by
    rw [fderiv_const_apply]
  rw [hd2]
  have hquad : (∑ m : Fin 2,
        (conformalGamma (0 : Fin 2) (0 : Fin 2) m 0 * conformalGamma m (1 : Fin 2) (1 : Fin 2) 0
          - conformalGamma (0 : Fin 2) (1 : Fin 2) m 0 * conformalGamma m (1 : Fin 2) (0 : Fin 2) 0))
      = 0 := by
    rw [Fin.sum_univ_two]
    norm_num [conformalGamma_origin_zero]
  rw [hquad]
  have hmain : ... = -1 := by ...  -- the fderiv computation
  rw [hmain]
  norm_num
```
Hmm — `conformalGamma_origin_zero : conformalGamma k i j 0 = 0` — in hquad, `norm_num [conformalGamma_origin_zero]` — the terms `conformalGamma (0) (0) ⟨0,_⟩ 0 * conformalGamma ⟨0,_⟩ (1) (1) 0 − ...` — rw conformalGamma_origin_zero on each → 0·0 − 0·0 = 0 — norm_num with the lemma — hmm — `norm_num [conformalGamma_origin_zero]` — norm_num uses the lemma as a simp-ish rewrite? norm_num's `[...]` args are simp lemmas — yes it discharges via simp [..] + norm_num. The Fin 2 sum: `Fin.sum_univ_two` — hmm — its statement: `Fin.sum_univ_two : ∑ i : Fin 2, f i = f 0 + f 1` ✓.

And hmain:
```lean
  have hmain : fderiv ℝ (fun z : ChartPoint (Fin 2) => -z 0 * (1 + z 0 ^ 2)⁻¹)
      (0 : ChartPoint (Fin 2)) (Pi.single (M := fun _ : Fin 2 => ℝ) (0 : Fin 2) (1 : ℝ))
      = -1 := by
    have hf : fderiv ℝ (fun z : ChartPoint (Fin 2) => -z 0) (0 : ChartPoint (Fin 2))
        = -ContinuousLinearMap.proj (0 : Fin 2) := by
      rw [fderiv_neg]
      rw [fderiv_proj]
```
Hmm — `fderiv_neg` — statement? `fderiv_neg (h : DifferentiableAt 𝕜 f x) : fderiv 𝕜 (fun y => -f y) x = -fderiv 𝕜 f x` — hmm — the `fun z => -z 0` — is that `-(z 0)` — f = `fun z => z 0` — `fun z => -f z` ✓. Need h : DifferentiableAt — `(contDiff_proj 0).contDiffAt.differentiableAt_one`.

Then hh: derivative of the inv part — I don't actually need its value! In the product rule at 0, the first term `c 0 • fderiv d 0` = `(-0) • fderiv d 0` = 0 — but I DO need DifferentiableAt d 0 for fderiv_mul. The fderiv VALUE of d at 0 gets multiplied by (−0) = 0 — so no need to compute it:
```lean
    have hd0 : DifferentiableAt ℝ (fun z : ChartPoint (Fin 2) => (1 + z 0 ^ 2)⁻¹) 0 :=
      ((ContDiff.inv (ContDiff.add contDiff_const (ContDiff.pow (contDiff_proj (0 : Fin 2)) 2))
        (fun x => denom_ne_zero x)).contDiffAt).differentiableAt_one
    have hf0 : DifferentiableAt ℝ (fun z : ChartPoint (Fin 2) => -z 0) 0 :=
      ((contDiff_proj (0 : Fin 2)).neg.contDiffAt).differentiableAt_one
    have hmul := fderiv_mul hf0 hd0
    -- hmul : fderiv ℝ (fun y => (-y 0) * (1 + y 0^2)⁻¹) 0
    --      = (-(0 : ℝ)) • fderiv ℝ (fun z => (1 + z 0^2)⁻¹) 0 + (1 + 0^2)⁻¹ • fderiv ℝ (fun z => -z 0) 0
    calc
      fderiv ℝ (fun z : ChartPoint (Fin 2) => -z 0 * (1 + z 0 ^ 2)⁻¹) 0 (single 0 1)
          = ((-(0 : ℝ)) • fderiv ℝ (fun z : ChartPoint (Fin 2) => (1 + z 0 ^ 2)⁻¹) 0
              + (1 + (0 : ℝ) ^ 2)⁻¹ • fderiv ℝ (fun z : ChartPoint (Fin 2) => -z 0) 0) (single 0 1) := by
            rw [hmul]
      _ = -1 := by
            rw [hf]
            simp only [ContinuousLinearMap.proj_apply, Pi.single_eq_same]
            norm_num
```
Hmm — wait — fderiv_mul's function: `fun y => c y * d y` with c = `fun z => -z 0` — `c y * d y` = `(-y 0) * (1 + y 0²)⁻¹` — and the goal's function `fun z => -z 0 * (1 + z 0 ^ 2)⁻¹` — parses as `(-z 0) * ((1 + z 0^2)⁻¹)` ✓ same.

Hmm — careful — `fderiv_mul hf0 hd0` — argument order: `fderiv_mul (hc) (hd)` — the result: `c x • fderiv d x + d x • fderiv c x` ✓ with c = the `-z 0` function.

In the last step: after `rw [hf]`: `(-0 • fderiv d 0 + (1+0²)⁻¹ • (-(proj 0))) (single 0 1)` — the CLM sum/smul applied: simp + proj_apply + single_eq_same + norm_num → `0 * ? + 1 * -(1)` — hmm — `(1 + (0 : ℝ)^2)⁻¹ = 1` — norm_num ✓. `(-0 : ℝ) • anything = 0` — hmm — `-0 = 0` norm_num ✓. `single 0 1 0 = 1` — Pi.single_eq_same ✓. Result: `0 * L v + 1 * -(1) = -1` — hmm — `1⁻¹ • (-(proj 0 v))` — the smul `a • L` on CLMs: `(a • L) v = a • (L v)` — simp [smul_eq_mul] → `a * L v` — with a = 1⁻¹ = 1, L v = -1 — `1 * -1 = -1` ✓. The `0 • L` term: `0 * L v = 0` ✓.

Let me also double check `conformalGamma_101_zero`'s clause: `| ⟨0,_⟩, ⟨1,_⟩, ⟨0,_⟩ => 0` ✓ exists.

And `christoffelFamily_conformal` — wait — there's an issue: in `christoffelFamily_conformal` I unfold `SmoothChartData.christoffelFamily` — its def: `(c.chartCoefficients x).christoffel k i j` — and `unfold SmoothChartData.chartCoefficients` — hmm — chartCoefficients is a def in namespace SmoothChartData — `unfold SmoothChartData.chartCoefficients` — the d field: `conformalChartData.dFamily x` — hmm — `unfold` should handle the projection `(conformalChartData.chartCoefficients x).d` — after unfolding chartCoefficients, `(⟨...⟩).d` reduces to `conformalChartData.dFamily x` ✓. Then `ChartMetricCoefficients.christoffel` unfold — its def is `∑ l, gInv k l * christoffelLower l i j` — and `christoffelLower l i j = (1/2) * (d i j l + d j i l - d l i j)` — hmm — let me check the actual defs in ChartLeviCivita.lean to get the unfold right. Let me read that file quickly.
dsh: reasoning:
`christoffel k i j = Σ_l gInv k l * christoffelLower l i j` and `christoffelLower l i j = (1/2)·(d i j l + d j i l − d l i j)`. Good — matches my assumptions.

Now let me rewrite the model file completely with the slot-0 convention and the real proofs.
dsh: reasoning:
Several issues to fix:

1. **Lines 84-89**: `fderiv_proj` and `contDiff_proj` fail — they're in the section before the module's own `variable {ι} [Fintype ι] [DecidableEq ι]` — I dropped the section variable declarations! The imported ChartLeviCivitaSmooth has its own `variable {ι : Type w} [Fintype ι] [DecidableEq ι]` in ITS namespace — but after `end` those are out of scope in my new file. I need to redeclare: `variable {ι : Type w} [Fintype ι] [DecidableEq ι]` for the fderiv_proj/contDiff_proj lemmas (generic ι), since ChartPoint needs [Fintype ι] for normed instances (Pi normed space needs Fintype).

2. **Line 112**: `ContDiff.mul ?_ contDiff_const` — the `?_` in the inv case: expected `ContDiff ℝ ∞ (fun x => (1+x 0²)⁻¹)` — I passed `ContDiff.inv (...)` — the mismatch "ContDiff.mul ?m.224 contDiff_const has type ContDiff ... fun x => ?m.222 x * ?m.234 but is expected ContDiff ℝ ∞ fun x => (1 + x 0 ^ 2)⁻¹" — hmm — the mul term: `?m.222 x * ?m.234` vs `(1+x 0²)⁻¹ * const`? The expected type should be `ContDiff ℝ ∞ (fun x => (1+x 0^2)⁻¹ * (if i=j then 1 else 0))` — after the `by_cases h; simp [h]` — the goal is `ContDiff ℝ ∞ (fun x => (1+x 0²)⁻¹ * (if i=j then 1 else 0 : ℝ))` — with h in context — hmm — the error says expected `fun x => (1 + x 0 ^ 2)⁻¹` — so `simp [h]` rewrote the if to... hmm — in the TRUE case (h : i = j): `if i=j then 1 else 0` → 1 → `(1+x 0²)⁻¹ * 1` → `(1+x 0²)⁻¹` — so expected = `ContDiff ℝ ∞ (fun x => (1+x 0^2)⁻¹)` — and my term `ContDiff.mul ?_ contDiff_const` has type `fun x => ?m x * const` — NOT defeq `(1+x 0²)⁻¹` (the *1 isn't defeq). Fix: in the h-true branch, the goal is exactly `ContDiff ℝ ∞ (fun x => (1+x 0²)⁻¹)` — just `exact ContDiff.inv ...` directly without mul-by-const! Restructure:
```lean
  gInv_smooth := by
    rw [contDiff_pi]
    intro i
    rw [contDiff_pi]
    intro j
    by_cases h : i = j
    · simpa [h] using (ContDiff.inv (ContDiff.add contDiff_const (ContDiff.pow (contDiff_proj (0 : Fin 2)) 2))
        (fun x => denom_ne_zero x))
    · simp [h]
```
Hmm — `simpa [h]` — the goal in the true case: `ContDiff ℝ ∞ (fun x => (1 + x 0 ^ 2)⁻¹ * (if i = j then 1 else 0 : ℝ))` — simpa [h] reduces the if → 1 and `*1` → the term ✓. In the false case: `simp [h]` — the goal becomes `ContDiff ℝ ∞ (fun x => (1+x 0²)⁻¹ * 0)` — simp rewrites `*0` → 0 → `ContDiff ℝ ∞ (fun _ => 0)` — contDiff_const — does simp have `ContDiff.const`-discharge? `ContDiff` of constant functions — simp can't prove ContDiff goals — `simp [h]` closes it only if the goal becomes `ContDiff ℝ ∞ (fun _ => 0 : ℝ)` and there's a simp lemma `contDiff_const` — `contDiff_const` is [simp]-tagged? Probably yes (`@[simp] theorem contDiff_const`). Same for g_smooth's false case — the error at 123/126 says "case neg: ⊢ j = i → 0 = 1 + x 0^2" — that's from `g_symm`! My `g_symm` proof: `by_cases h : i = j <;> simp [h]` — the neg case goal: `(1+x 0²)*(if i=j then 1 else 0) = (1+x 0²)*(if j=i then 1 else 0)` — simp [h] leaves `j = i → 0 = 1 + x 0²`?? — because simp rewrote `if j = i then 1 else 0` with... hmm — the goal after simp: `j = i → 0 = 1 + x 0 ^ 2` — that's simp trying to prove `if j=i then 1 else 0 = 0`... which needs ¬(j=i) — but we have h : ¬(i=j) — not ¬(j=i)! simp split the if j=i into cases and in the positive case needs `0 = 1 + x 0²` — fails. Fix: `have h' : ¬ j = i := fun hji => h hji.symm` then `simp [h, h']`. Or simply: `by_cases h : i = j <;> simp [h, eq_comm]` — hmm — `eq_comm` might make `j = i` match `i = j`... Actually `simp [h]` with `h : ¬ i = j` — for the `if j = i` — simp uses `decide`-style? No. Cleanest:
```lean
  g_symm := by
    intro x i j
    by_cases h : i = j
    · simp [h]
    · have h' : ¬ j = i := fun hji => h hji.symm
      simp [h, h']
```
Hmm — but wait — in the neg case, LHS `(1+x 0²)*(if i=j then 1 else 0)` — simp [h] → `(1+x 0²)*0` → 0. RHS — simp [h'] → 0. ✓.

3. **Line 131**: `simp [conformalChartData, denom_pos]` — "Invalid argument: Variable conformalChartData is not a proposition" — because `conformalChartData` is a def (a structure term), and in the inv_mul field, I'm INSIDE the definition of conformalChartData — the name `conformalChartData` refers to... hmm — in the `where` block, `simp [conformalChartData]` — the unfolding of the term being defined? The def isn't complete — `conformalChartData` is a local constant? "Variable conformalChartData is not a proposition or let-declaration" — simp can't unfold it. The fields g/gInv inside inv_mul: the goal `∑ l : Fin 2, conformalChartData.gInv x k l * conformalChartData.g x l j = if k = j then 1 else 0` — hmm — since we're in the structure literal, the goal's `conformalChartData.gInv`/`g` — are these the FIELD VALUES being defined? In Lean 4, while defining structure fields with `where`, later fields see the earlier fields via the structure... The goal elaborates with `conformalChartData.gInv` referring to the PROJECTION of the (recursive) name — hmm — the error suggests it's treated as a variable, not unfoldable. Fix: `rw [gInv_conformal, g_conformal]`-style manual rewrites? But those lemmas come after. Simplest: restructure inv_mul to use the explicit field definitions:
```lean
  inv_mul := by
    intro x k j
    rw [show conformalChartData.gInv x k l ... ]
```
Hmm — can't rw since not unfoldable. Instead — write the goal explicitly: since `simp` can't unfold, use `change`:
```lean
  inv_mul := by
    intro x k j
    change (∑ l : Fin 2, ((1 + x 0 ^ 2)⁻¹ * (if k = l then 1 else 0 : ℝ)) *
        ((1 + x 0 ^ 2) * (if l = j then 1 else 0 : ℝ))) = if k = j then 1 else 0
    rw [Fin.sum_univ_two]
    fin_cases k <;> fin_cases j <;> norm_num [denom_ne_zero]
    · field_simp
    ...
```
Hmm — `change` with the projection applied — `conformalChartData.gInv x k l` — is that defeq to the field value? During structure literal elaboration, the projection `conformalChartData.gInv` — hmm — the recursive name might not be reducible yet. Hmm — actually — the error "Variable conformalChartData is not a proposition or let-declaration" suggests Lean treats `conformalChartData` in that context as an opaque local constant. But `change` might still work if defeq... Actually — in Lean 4, within a `where` structure definition, the fields ARE accessible via the structure name and ARE definitionally the values (the elaborator inserts the structure as a "let" — hmm — the error says NOT a let-declaration...). 

   SAFER: avoid the issue by defining g/gInv as standalone defs first:
```lean
def conformalG (x : ChartPoint (Fin 2)) (i j : Fin 2) : ℝ := (1 + x 0 ^ 2) * (if i = j then 1 else 0 : ℝ)
def conformalGInv (x : ChartPoint (Fin 2)) (i j : Fin 2) : ℝ := (1 + x 0 ^ 2)⁻¹ * (if i = j then 1 else 0 : ℝ)
```
then `conformalChartData` fields: `g := conformalG`, `gInv := conformalGInv` — and in inv_mul: `simp [conformalG, conformalGInv, denom_ne_zero]` — the defs ARE unfoldable (defined before) ✓. And g_conformal/gInv_conformal lemmas become `rfl`-ish (`conformalChartData.g x i j = conformalG x i j` rfl). And dFamily_conformal's `rw [g_conformal]` → unfold conformalG.

   Also — hmm — why did the error at 131 happen — "Invalid argument: Variable conformalChartData is not a proposition or let-declaration" ×4 — yes — because simp [conformalChartData] — replace with the standalone-def approach.

4. **Line 134**: "No goals to be solved" — cascade from the 131 errors.

5. **Line 154**: `rw [g_conformal]` in dFamily_conformal failed — because g_conformal's RHS referenced `conformalChartData.g` — after my restructure it'll be conformalG. Also note the target shows `(2 * x 0 * if i = 0 then 1 else 0) * if j = k then 1 else 0` — my lemma statement wrote `2 * x 0 * (if i = 0 then 1 else 0 : ℝ) * (if j = k then 1 else 0 : ℝ)` — parses as `((2·x·if)·if)` ✓ consistent.

6. **Lines 212-214**: in christoffelFamily_conformal — after `norm_num`, goals like `conformalChartData.gInv x 0 0 * x 0 = x 0 * (1 + x 0 ^ 2)⁻¹` — because `conformalChartData.gInv` didn't unfold (same non-unfoldable issue — I unfold SmoothChartData.christoffelFamily etc. but the datum's gInv field remained as a projection!). With the standalone-def restructure, add `simp only [conformalGInv, conformalG, ...]`? — hmm — after unfolding christoffelFamily/chartCoefficients, the expression `conformalChartData.christoffelFamily x k i j` becomes `∑ l, (conformalChartData.chartCoefficients x).gInv k l * christoffelLower ...` — and chartCoefficients unfold gives `conformalChartData.gInv x k l` — then need to unfold the FIELD — with the standalone defs: `change`/`simp [conformalGInv]` — `conformalChartData.gInv x k l` — the projection applied — simp can unfold it IF the def conformalChartData is reducible — since conformalChartData is now a closed def (defined after conformalG/conformalGInv), `simp [conformalChartData]` unfolds projections ✓. So in christoffelFamily_conformal: `simp only [dFamily_conformal, conformalChartData, conformalG, conformalGInv]`-ish — hmm — order: unfold christoffel... then `simp only [dFamily_conformal]` then `rw [Fin.sum_univ_two]` then `simp only [conformalChartData]` to unfold the gInv fields → hmm — actually cleaner: `simp only [christoffelFamily, chartCoefficients, conformalChartData, christoffel, christoffelLower, dFamily_conformal]` in one go? — the christoffel/christoffelLower unfold into the sums, conformalChartData unfolds the datum projections, dFamily_conformal rewrites the d terms. Then `rw [Fin.sum_univ_two]`, then fin_cases×3 + norm_num + ring/field_simp.

   Also the goals at 212-214 show products like `conformalChartData.gInv x 0 0 * x 0 = x 0 * (1+x 0²)⁻¹` — the `ring` failed because `conformalChartData.gInv x 0 0` is opaque — after unfolding: `(1+x 0²)⁻¹ * 1 * x 0 = x 0 * (1+x 0²)⁻¹` — ring closes (commutative) ✓.

7. In g_smooth — same mul-by-const issue for the TRUE case: expected `ContDiff ℝ ∞ (fun x => 1 + x 0 ^ 2)` (after simp[h] the if → 1, *1 → gone? — `(1 + x 0^2) * 1` — simp rewrites `*1` ✓) — so expected = `ContDiff ℝ ∞ (fun x => 1 + x 0^2)` and my term `ContDiff.mul ?_ contDiff_const` gives `fun x => ?m x * c` — mismatch. Restructure like gInv_smooth: true case: `simpa [h] using (ContDiff.add contDiff_const (ContDiff.pow (contDiff_proj (0 : Fin 2)) 2))`; false case: `simp [h]`.

   Wait — hmm — but there's a subtlety: in g_smooth's true case, goal after `simp [h]`... — I wrote `by_cases h : i = j <;> simp [h]` — hmm — that leaves the goal as-is after simp — then `refine ContDiff.mul ...` — wrong structure. Let me restructure each field:
```lean
  g_smooth := by
    rw [contDiff_pi]
    intro i
    rw [contDiff_pi]
    intro j
    by_cases h : i = j
    · simpa [h, conformalG] using (ContDiff.add contDiff_const
        (ContDiff.pow (contDiff_proj (0 : Fin 2)) 2))
    · simp [conformalG, h]
```
The false case: goal `ContDiff ℝ ∞ (fun x => (1+x 0²) * 0)` — simp → `ContDiff ℝ ∞ (fun x => 0)` — needs `contDiff_const` [simp] — should be fine.

8. Also — the `hzero` funext and general stuff should be OK.

9. One more concern at line ~84-89: add the missing section variables. Also `fderiv_proj`/`contDiff_proj` need `[Fintype ι] [DecidableEq ι]`? ChartPoint ι = ι → ℝ — NormedAddCommGroup instance for Pi: needs Fintype ι (normed Pi). DecidableEq not needed for these. The error mentioned `ι → Module ?m.29 ℝ` at line 84 (fderiv_proj's `fderiv ℝ (fun y : ChartPoint ι => y i)` — the module instance on ℝ needs `Module ℝ ℝ` ✓ — but the STUCK one: `ι → Module ?m.29 ℝ` — the continuous linear map structure of proj... whatever — adding `variable {ι : Type w} [Fintype ι] [DecidableEq ι]` at the top should resolve. Actually the earlier errors said "failed to synthesize NormedAddCommGroup (ChartPoint ι)" — Pi normed needs [Fintype ι] ✓.

Also for the fin_cases in inv_mul — `fin_cases k` on k : Fin 2 ✓.

Let me now rewrite the file with the standalone defs and restructured fields. Also fix g_symm/gInv_symm with the eq_comm handling.
dsh: reasoning:
Progress. Remaining errors:

1. **Line 88**: `fderiv_proj` — "typeclass instance problem is stuck: ι → Module ?m.28 ℝ" — the `ContinuousLinearMap.proj i` — hmm — the change: `fderiv ℝ (ContinuousLinearMap.proj i) x = ContinuousLinearMap.proj i` — the issue: `ContinuousLinearMap.fderiv` needs `f : E →L[𝕜] F` with... the stuck instance `ι → Module ?m.28 ℝ` — the proj's type `(∀ i, φ i) →L[R] φ i` — with φ i = ℝ — the CLM needs `∀ i, φ i` normed — needs Fintype — we have it... The stuck metavariable — maybe the issue: `fderiv`'s `𝕜` — I passed ℝ — hmm — "ι → Module ?m.28 ℝ" — this is the NORMEDSPACE... Hmm — maybe the problem is that `fderiv ℝ f x` requires `NormedSpace ℝ (ChartPoint ι)` — for the codomain `ℝ` — `NormedSpace ℝ ℝ` — hmm. The message shows a metavariable `?m.28` in `Module ?m.28 ℝ` — the scalar field not determined — from `ContinuousLinearMap.proj i`'s `R` param? `proj (i : ι) : (∀ i, φ i) →L[R] φ i` — R implicit — with φ i = ℝ and the codomain ℝ — R could be anything... The elaboration of `fderiv ℝ (ContinuousLinearMap.proj i) x` — proj's ring R — inferred from... the SMul instance `Module R ℝ` — many rings act on ℝ — the metavariable can't be solved — need to pin: `ContinuousLinearMap.proj (R := ℝ) (i := i)` — with `φ := fun _ => ℝ`.

   Fix: `change fderiv ℝ (ContinuousLinearMap.proj (R := ℝ) (φ := fun _ : ι => ℝ) i) x = ContinuousLinearMap.proj (R := ℝ) (φ := fun _ : ι => ℝ) i`.

   Hmm — also `fderiv_proj`'s statement itself might have the same problem: `fderiv ℝ (fun y : ChartPoint ι => y i) x = ContinuousLinearMap.proj i` — the RHS proj — R metavariable — it elaborated OK apparently (no error at the statement, only at 88 which is the `change` line). Actually line 88 is `change fderiv ℝ (ContinuousLinearMap.proj i) x = ContinuousLinearMap.proj i` — the explicit proj application needs R. Use `(ContinuousLinearMap.proj (R := ℝ) i)`.

   Similarly in the model's uses: `ContinuousLinearMap.proj (0 : Fin 2)` — appear in dFamily_conformal calc and hmain — those elaborate with R := ℝ from context (the fderiv ℝ) — hmm — the errors at 186: `fderiv_pow 2 (contDiff_proj ...).contDiffAt.differentiableAt_one` — "Application type mismatch ... expected NormedCommRing ... NormedAlgebra.toNormedSpace" — hmm — `fderiv_pow (n : ℕ) (hf : DifferentiableAt 𝕜 f x)` with f : E → 𝔸 — 𝔸 a NormedAlgebra?? — `fderiv_pow`'s codomain must be a `NormedAlgebra 𝕜 𝔸` — the function `fun y => y 0 ^ 2` has codomain ℝ — `NormedAlgebra ℝ ℝ` ✓ exists — the mismatch shows different instance paths (`NormedSpace.toModule` vs `(NormedAlgebra.toNormedSpace).toModule`) — the instance for `DifferentiableAt` from contDiff_proj uses `Real.normedAddCommGroup/RCLike...` hmm — the mismatch is between `?m.344` etc. — the contDiff_proj's ContDiff uses instance `Pi.normedSpace` (module structure on ChartPoint) while fderiv_pow expects `NormedAlgebra` codomain... 

   Hmm — this is the classic normed-space instance mismatch (different `NormedSpace ℝ (ChartPoint ι)` instances? No — the codomain ℝ: `ContDiff ℝ ∞ (fun x => x 0)` — the DifferentiableAt from it has codomain ℝ with instance `NormedSpace.toModule`?? and fderiv_pow wants `NormedAlgebra.toNormedSpace`.toModule — these are different Module instances syntactically?? For ℝ over ℝ, `NormedSpace ℝ ℝ` vs `NormedAlgebra ℝ ℝ` — different typeclass paths — but they should be defeq... The error says "Application type mismatch" — the argument's type uses `NormedAddCommGroup.toAddCommGroup NormedSpace.toModule` while expected `NormedCommRing.toCommRing.toAddCommGroup (NormedAlgebra.toNormedSpace ?m.345).toModule` — so the DifferentiableAt instance found for `fun y => y 0` (via contDiff_proj's chain) uses the generic NormedSpace instance for ℝ (RCLike.toInnerProductSpaceReal.toNormedSpace), while fderiv_pow's signature demands the NormedAlgebra-based instance. Not defeq-unifiable at elaboration?? They ARE the same definitionally? `RCLike.toInnerProductSpaceReal.toNormedSpace` vs `NormedAlgebra.toNormedSpace` — hmm — for ℝ these should be definitionally equal (both from... actually no — the inner-product-based normed space on ℝ vs the algebra-based one — these are DIFFERENT instances possibly not defeq!! The norm equality: both give |x| — the InnerProductSpace instance for ℝ defines norm via the inner product — the NormedAlgebra one via `NormedAddCommGroup`... — if they're separate instances, the module structure is the same (NormedSpace.toModule → Semiring.toModule for ℝ?) — but the AddCommGroup/NormedAddCommGroup parents could differ → not defeq).

   Fix: provide the differentiability hypothesis with the RIGHT instance path. Simplest: avoid `fderiv_pow`'s DifferentiableAt argument entirely by using `hasFDerivAt_pow` (which only needs HasFDerivAt — hmm — also instance-dependent) — OR construct the DifferentiableAt via the algebra path: `(contDiff_proj (0 : Fin 2)).contDiffAt.differentiableAt_one` — gives the wrong instance. Alternative: use `DifferentiableAt.proj`-like direct: hmm.

   Cleaner alternative: use the `fderiv` of the POW via `fderiv_comp` + `fderiv_pow_ring`? — `fderiv_pow_ring {x : 𝔸} (n : ℕ) : fderiv 𝕜 (fun x : 𝔸 ↦ x ^ n) x = (n • x ^ (n - 1)) • .id _ _` — the pow FUNCTION on 𝔸 directly — comp with proj: `fderiv_comp (hg := differentiableAt_fun_pow?)` — hmm — needs differentiability of pow at (x 0)... 

   SIMPLEST ROBUST FIX: avoid fderiv_pow entirely. The function `y ↦ y 0 ^ 2` — prove its fderiv DIRECTLY from the product rule `fderiv_mul` on `y ↦ y 0 * y 0`:
   ```lean
   have hsq : fderiv ℝ (fun y : ChartPoint ι => y 0 ^ 2) x = (2 * x 0) • fderiv ℝ (fun y => y 0) x := by
     have h := fderiv_mul (contDiff_proj (0 : Fin 2)).contDiffAt.differentiableAt_one
       (contDiff_proj (0 : Fin 2)).contDiffAt.differentiableAt_one
     ...
   ```
   Hmm — fderiv_mul's codomain 𝔸 — also NormedAlgebra-demanding? `fderiv_mul (hc) (hd) : fderiv 𝕜 (fun y => c y * d y) x = c x • fderiv 𝕜 d x + d x • fderiv 𝕜 c x` — codomain 𝔸 `NormedCommRing`+... — the hc from contDiff_proj — same mismatch risk.

   Hmm — actually — wait — let me reconsider the error at 186 more carefully: "fderiv_pow 2 (ContDiffAt.differentiableAt_one ?m.384)" — the expected type: `@DifferentiableAt ?m.344 ?m.347 ?m.346 NormedAddCommGroup.toAddCommGroup NormedSpace.toModule ... NormedCommRing.toCommRing.toAddCommGroup (NormedAlgebra.toNormedSpace ?m.345).toModule ...` — the expected ADDCOMMGROUP for the codomain is `NormedCommRing.toCommRing.toAddCommGroup` while the provided has `NormedAddCommGroup.toAddCommGroup`. These differ — the instance for ℝ's `DifferentiableAt` uses the `NormedAddCommGroup ℝ` = `Real.normedAddCommGroup` — but `NormedCommRing.toNormedAddCommGroup ℝ`?? — hmm — wait — the error is about the argument's type: "has type @DifferentiableAt ... NormedAddCommGroup.toAddCommGroup NormedSpace.toModule ..." — the ARGUMENT has generic `NormedAddCommGroup.toAddCommGroup`/`NormedSpace.toModule` (with metavariables for the normed space) — because `contDiff_proj (0 : Fin 2)` — the ι in contDiff_proj is a metavariable?! — `(contDiff_proj (0 : Fin 2)).contDiffAt.differentiableAt_one` — the ι of contDiff_proj inferred as Fin 2 ✓... "?m.384" — the DifferentiableAt's 𝕜 and spaces are metavariables — because contDiff_proj's `ContDiff ℝ ∞ (...)` — hmm — elaborated fine...

   Honestly — the fastest path: test in scratch what works for the square's fderiv. Let me try variants:
   (a) `fderiv_pow 2 (differentiableAt_proj...)` with explicit instance pinning.
   (b) Direct: `fderiv ℝ (fun y : ChartPoint (Fin 2) => y 0 * y 0) x` via fderiv_mul.
   (c) The hasFDerivAt route: `(hasFDerivAt_proj...).mul ...` — hmm.

   Let me scratch-test.

2. **Lines 126/136**: `simp [conformalG, h]` in the NEG cases of g_smooth/gInv_smooth leave `ContDiff ℝ ∞ fun x => 0` unsolved — simp didn't discharge `contDiff_const` — because the goal after simp is `ContDiff ℝ ∞ (fun x => (1 + x 0²) * 0)` → `ContDiff ℝ ∞ (fun x => 0 : ℝ)` — simp needs to apply `contDiff_const` — hmm — `contDiff_const` — its statement: `ContDiff 𝕜 n fun _ : E => c` — is it [simp]? Maybe not tagged. Add explicit: `exact contDiff_const` — hmm — the goal `ContDiff ℝ ∞ fun x => 0` — `contDiff_const` gives `ContDiff ℝ ∞ (fun _ => 0)` — exact/simpa. Fix:
   ```lean
   · simp [conformalG, h]
     exact contDiff_const
   ```
   Hmm — wait — `simp [conformalG, h]` — the goal `ContDiff ℝ ∞ (fun x => (1 + x 0 ^ 2) * (if i = j then 1 else 0))` — simp [conformalG? — no — the goal ALREADY has conformalG unfolded? The goal in g_smooth's neg case: `ContDiff ℝ ∞ (fun x => conformalG x i j)` — simp [conformalG, h] unfolds conformalG → `(1+x 0²) * if...` → with h: if → 0 → `(1+x 0²) * 0` → mul_zero → `0` → goal `ContDiff ℝ ∞ (fun x => 0)` — then simp needs `contDiff_const` — apparently not [simp]. Add `exact contDiff_const` after (goal `ContDiff ℝ ∞ fun x => 0` — `contDiff_const : ContDiff 𝕜 n (fun _ : E => c)` — unify c := 0 ✓ — exact works).

   Actually — is `contDiff_const` [simp]? I recall it IS tagged simp... but the goal wasn't closed. Hmm — maybe the issue is the goal is `ContDiff ℝ ∞ fun x => 0` where the function's codomain ℝ — `contDiff_const (c := 0)` — hmm. Let me just add `exact contDiff_const` and see.

3. **Line 133**: `simpa [conformalGInv, h] using ContDiff.inv ...` — "After simplification, term has type ... ∞ (fun x => 1 + x 0 ^ 2)⁻¹ ... NonUnitalNormedRing.toNormedAddCommGroup (NormedAlgebra.toNormedSpace ℝ) ... but expected ... Real.normedAddCommGroup RCLike...∞ fun x => (1 + x 0 ^ 2)⁻¹" — SAME instance-path mismatch: the ContDiff.inv term's codomain ℝ uses the NormedAlgebra-path instance while the goal's uses the Real/RCLike path. `ContDiff.inv`'s codomain is `R` a `NormedField`? — it produces `ContDiff 𝕜 n (fun x => (f x)⁻¹)` where the codomain instance comes from the field's `NormedAlgebra 𝕜 R`-ish path. Mismatch.

   Fix: force the goal's instance... hmm — or build the smoothness of the INV differently: the function `x ↦ (1 + x 0²)⁻¹` — via `ContDiff.inv` but with the SAME instance the goal uses — the mismatch is within `ContDiff.inv`'s signature (its codomain typeclass params). Alternative: use `ContDiff.comp` with the univariate smooth function `Inv.inv` restricted... The univariate `Inv.inv : ℝ → ℝ` — `ContDiff ℝ ∞ (Inv.inv : ℝ → ℝ)`? — `contDiff_inv`? — there's `contDiff_inv : ContDiff ℝ n (Inv.inv : R → R)`-ish for n... hmm — `contDiffAt_inv`/`differentiableOn_inv`... For `Inv.inv` as a function on ℝ: `ContDiff ℝ ∞ (fun x : ℝ => x⁻¹)` — hmm — is there `contDiff_inv`? Let me grep. If yes: `ContDiff.comp (contDiff_inv ℝ-ish...) (ContDiff.add ...)` — with `contDiff_inv`'s codomain ℝ instance — might match the goal better... same risk.

   ALTERNATIVE ROBUST FIX: `convert`-based: prove the ContDiff goal via `simpa` with an explicit instance coercion... hmm — heavy.

   ALTERNATIVE: `ContDiff.congr`? Hmm.

   Hmm — actually — wait — look at the expected instance: `Real.normedAddCommGroup RCLike.toInnerProductSpaceReal.toNormedSpace` — the goal's ℝ is an INNER PRODUCT SPACE instance — that's because `SmoothChartData`'s field type `ContDiff ℝ ∞ gInv` where `gInv : ChartPoint ι → ι → ι → ℝ` — the codomain ℝ — hmm — why inner product space? Because... `ChartPoint ι = ι → ℝ` — Pi — the Pi instances... whatever. The mismatch: term's ℝ has `NonUnitalNormedRing.toNormedAddCommGroup (NormedAlgebra.toNormedSpace ℝ)` — from `ContDiff.inv`'s requirements.

   A `convert`/`exact` with defeq won't work if instances differ non-defeq.

   PRACTICAL FIX: make the two sides use the same instance by REWRITING the goal to the term's instance first? Can't control instances syntactically easily...

   BETTER: prove the inv-part's smoothness with the SAME lemma that the GOAL-side would use. The goal: `ContDiff ℝ ∞ (fun x => conformalGInv x i j)` — unfold conformalGInv → `(1 + x 0^2)⁻¹ * if...` — for the true case: `(1 + x 0^2)⁻¹`. The instance for this goal comes from the field type — hmm — both are `ContDiff ℝ ∞ (fun x => t : ℝ)` — the ℝ's instance is fixed by the ContDiff predicate's structure — the DIFFERENCE is only which instance the TERM's proof used... `ContDiff.inv`'s CONCLUSION: `ContDiff 𝕜 n (fun x => (f x)⁻¹)` — its codomain ℝ — the instance used in the CONCLUSION is determined by `ContDiff.inv`'s variables: `{R : Type} [NormedField R] [NormedAlgebra 𝕜 R]` — so the conclusion's ℝ uses `NormedAlgebra.toNormedSpace`-path — while the goal's ℝ (from SmoothChartData's field elaboration) uses the Real/RCLike path. Since ℝ has multiple normed-space instances not all defeq... hmm — but actually — ARE they defeq? The type error says "Type mismatch: After simplification" — simpa found the difference — so NOT defeq at the kernel level.

   Hmm — wait — actually — hmm. Let me check how ChartLeviCivitaSmooth handled ContDiff.inv-adjacent things... it didn't use ContDiff.inv. ChartModel1D — let me look at how it proved smoothness of 1/(1+x²)-type functions! ChartModel1D: g(x) = 1 + x², gInv = 1/g — its smoothness proofs must have solved exactly this. Let me look at ChartModel1D.lean.
dsh: reasoning:
ChartModel1D is POINTWISE (no ContDiff involved) — doesn't help with the instance mismatch.

Let me scratch-test the ContDiff.inv instance mismatch and the fderiv_pow mismatch to find working formulations.
dsh: reasoning:
Key results:
- Test 1 (`ContDiff.inv`) — NO error! So `ContDiff.inv` works standalone here — the earlier failure at line 133 must have been caused by the `simpa [conformalGInv, h]` wrapper comparing against a goal with a DIFFERENT instance... hmm — wait — in my file the goal is `ContDiff ℝ ∞ (fun x => conformalGInv x i j)` and the term `ContDiff.inv ...` — the simpa failed with the instance path difference — but the standalone `exact` worked?? The scratch example's goal: `ContDiff ℝ ∞ (fun x : P2 => (1 + x 0 ^ 2)⁻¹)` — EXACT worked — so the instances DO match when the goal is written directly. In my file, the goal after `simp [conformalGInv, h]`-reduction — hmm — wait — in the file the error at 133 was `simpa [conformalGInv, h] using (ContDiff.inv ...)` — "After simplification, term has type ... NonUnitalNormedRing.toNormedAddCommGroup (NormedAlgebra.toNormedSpace ℝ) ... but is expected ... Real.normedAddCommGroup RCLike.toInnerProductSpaceReal.toNormedSpace" — so the GOAL has the RCLike instance and the TERM has the NormedAlgebra instance — but the scratch test's `exact` worked because the goal there was elaborated FRESH with the same instance the term used... In my file, the goal comes from the `conformalChartData.gInv_smooth` field whose type is fixed by `SmoothChartData.gInv_smooth : ContDiff ℝ ∞ gInv` — with `gInv : ChartPoint (Fin 2) → Fin 2 → Fin 2 → ℝ` — the ℝ codomain instance — why RCLike-toInnerProductSpace?? Hmm — because `SmoothChartData`'s field type elaboration: `ContDiff ℝ ∞ gInv` — hmm — the Pi over Fin 2 — the ℝ there — hmm — RCLike.toInnerProductSpaceReal — this instance path gets picked by the Pi normed space elaboration (the Pi instance for `Fin 2 → ℝ` uses each component's NormedSpace — for ℝ the Pi machinery... the instance `Pi.normedSpace` — with `[∀ i, NormedSpace 𝕜 (β i)]` — for β i = ℝ — the instance search finds `RCLike.toInnerProductSpaceReal`? — priority? — whatever — the point: the goal's ℝ uses a different instance path than `ContDiff.inv`'s conclusion. BUT in scratch test 1, the standalone goal used the same path as ContDiff.inv — because the goal was elaborated independently and instance search picked the same thing? Odd — the goal in test 1: `ContDiff ℝ ∞ (fun x : P2 => (1 + x 0 ^ 2)⁻¹)` — P2 := Fin 2 → ℝ — same shape as the field type — and `exact ContDiff.inv ...` worked — meaning instance search for BOTH picked the same instance. In my file the field type was elaborated ONCE (in SmoothChartData's declaration — wait — no — SmoothChartData is generic over ι — the field `gInv_smooth : ContDiff ℝ ∞ gInv` where gInv's codomain ℝ — elaborated with generic ι : `ContDiff ℝ ∞ (ChartPoint ι → ι → ι → ℝ)` — hmm — `ι → ι → ι → ℝ` — the codomain ℝ instance at elaboration time — the ℕ→NormedAddCommGroup error at line 30 suggests something ELSE...

Wait — test 3's error: "failed to synthesize NormedAddCommGroup ℕ" — `example : ContDiff ℝ ∞ (fun x : P2 => 0) := by simp` — the `0` — elaborated as `0 : ℕ`?! — because ContDiff's codomain metavariable... simp couldn't handle. And test 2's error at line 30 is the same fderiv_pow mismatch as before.

So:
- The fderiv_pow mismatch is REAL (test 2 fails) — `fderiv_pow`'s codomain 𝔸 requires `NormedCommRing`/`NormedAlgebra`-path instances and `contDiff_proj`-derived DifferentiableAt uses the NormedSpace-path. Need another approach for the square's fderiv.
- The ContDiff.inv issue in my file: the field-type goal's instance vs ContDiff.inv's instance — need to test exactly. The difference from scratch test 1: in the file, the goal is `ContDiff ℝ ∞ (fun x => conformalGInv x i j)` — after `rw [contDiff_pi]` ×2 and by_cases — the goal's ℝ — hmm.

Hmm — WAIT — maybe the actual issue in the file is the DOUBLE Pi: the field is `ContDiff ℝ ∞ (fun x => conformalGInv x)` where conformalGInv x : Fin 2 → Fin 2 → ℝ — contDiff_pi.mp twice gives the component `fun x => conformalGInv x i j` — the elaboration of `contDiff_pi.mp` requires the codomain normed — the intermediate `Fin 2 → ℝ` uses Pi.normedSpace with ℝ's instance = `RCLike.toInnerProductSpaceReal` — while the final ℝ's ContDiff.inv conclusion — the same ℝ... The error says the EXPECTED (goal) has RCLike-path and term has NormedAlgebra-path — so at the innermost level the goal's ℝ uses the RCLike instance — likely inherited from the outer Pi elaboration.

Fix approaches for the field:
(a) Prove `gInv_smooth` via a DIFFERENT route that produces the RCLike-path instance: hmm — what produces that? `ContDiff.inv` produces NormedAlgebra-path. The mismatch is inherent. 
(b) Prove the goal with `convert`/`exact` + a change of instance — not possible if not defeq.
(c) Prove the goal by congr on the functions: `ContDiff.congr`? — hmm — no — the instance problem is in the ContDiff predicate itself.
(d) Prove the field using the GENERIC `gInv_component_smooth`-style: hmm.
(e) Prove `ContDiff ℝ ∞ (fun x : ChartPoint (Fin 2) => conformalGInv x i j)` with the RCLike instance via `ContDiff.comp` with univariate `Inv.inv`: `ContDiff ℝ ∞ (fun x : ℝ => x⁻¹)`?? — what instance does THAT use? — `contDiff_inv` — hmm.

(f) SIMPLEST: change the goal's instance by declaring the field proof to FIRST convert the goal: since both instances exist on ℝ and the ContDiff predicate is "the same function is smooth", maybe I can prove with `simpa only [...]` after `change`-ing... no.

(g) Use `ContDiff.inv` but instantiate the codomain instance to the RCLike one explicitly?? `ContDiff.inv` is generic over `R` a NormedField with `NormedAlgebra 𝕜 R` — the conclusion uses the typeclass instances from those variables — can't easily override.

(h) DIFFERENT STRATEGY — prove smoothness of `x ↦ (1 + x 0²)⁻¹` as the COMPOSITION of the smooth univariate inverse function with the smooth chart function. The univariate `Inv.inv : ℝ → ℝ`: `ContDiff ℝ ∞ (fun x : ℝ => x⁻¹)` — hmm — `contDiff_inv`? — there IS `contDiffAt_inv`/`contDiff_inv`? Hmm — hmm — in ContDiff there might be `contDiff_inv : ContDiff 𝕜 n Inv.inv`-ish... — and `ContDiff.comp` — `ContDiff.comp (hg) (hf) : ContDiff 𝕜 n (g ∘ f)` — the composition's codomain instance — from the goal's elaboration — should match the goal. Let me check if `contDiff_inv` exists... hmm — actually — for `Inv.inv` on a field as a smooth function — I'm not sure there's a lemma `ContDiff ℝ ∞ (Inv.inv : ℝ → ℝ)`. There's `contDiffOn_inv`? Let me grep.

(i) OR — avoid the inv in the METRIC entirely!! Use a metric with POLYNOMIAL inverse... g·gInv = δ — with polynomial g AND polynomial gInv — impossible unless g constant (det const and...). For a DIAGONAL metric g = diag(f, 1/f)-type... hmm — what about g = diag(a, b) with a·b = 1 polynomial — a = b = 1 only. So no.

(j) The field-smoothness requirement is only about the METRIC datum structure. What if I DON'T use SmoothChartData for the model, but instead work at the ChartMetricCoefficients pointwise level + separate ContDiff lemmas for the explicit functions (g, gInv, Γ, R) — the curvature computation doesn't actually NEED the SmoothChartData structure! The honest connection to the general machinery: `christoffelFamily_conformal` requires the SmoothChartData... but I could prove the analogous statement at the pointwise level: `ChartMetricCoefficients.christoffel (pointwise datum at x) k i j = conformalGamma k i j x` — with the pointwise datum's d = the explicit d (data field, NOT fderiv-derived — with d_symm etc. proved pointwise). Then the curvature statement is about conformalGamma (which equals the pointwise Christoffel construction from coefficients). The smoothness of g, gInv, Γ, R-functions as separate ContDiff lemmas — where I can write each goal DIRECTLY (like scratch test 1, where `exact ContDiff.inv ...` WORKED).

   But — the smoothness claims were a nice-to-have; the fderiv-based dFamily_eq bridge was the link to the smooth datum. Given time constraints (I'm ~1h40m into the invocation; checkpoint update due; results/audit/hashes still to do), let me simplify:

   **Revised plan for the model (timebox ~40 more min):**
   1. Pointwise datum `conformalPointwise (x) : ChartMetricCoefficients (Fin 2)` with explicit d = `2·x 0·[i=0]·[j=k]` and g/gInv as above; prove inv_mul, g_symm, gInv_symm, d_symm (algebra, no fderiv).
   2. `christoffel_pointwise_conformal : christoffel (conformalPointwise x) k i j = conformalGamma k i j x` — algebra + Fin computation.
   3. `conformal_riemann_1212_origin` and `conformal_curvature_nonzero` — same as before but with `fun k i j x => christoffel (conformalPointwise x) k i j` — the fderiv of `conformalGamma`-terms — the hmain fderiv computation — still needs the fderiv of `-z 0 * (1 + z 0²)⁻¹` — needs fderiv_mul with differentiability — DifferentiableAt of the inv-part via ContDiff.inv (works standalone per scratch) — and fderiv of `-z 0` via fderiv_neg + proj — `fderiv_neg (hf)` — the differentiability argument — `(contDiff_proj' 0).contDiffAt.differentiableAt_one` — same instance path as fderiv_mul expects?? — fderiv_mul's codomain 𝔸 with NormedCommRing-path — the hf argument from contDiff_proj — NormedSpace-path — MISMATCH again?? — hmm — scratch test 2 showed fderiv_pow's argument mismatch. Let me scratch-test fderiv_mul + fderiv_neg with contDiff_proj-derived differentiability before writing more code.
   4. Skip the SmoothChartData field entirely — record in the docstring that the model's derivative data is declared pointwise (data) with the fderiv bridge recorded as the dependency (the dFamily_eq lemma requires resolving the normed-space instance paths).

   Hmm — wait — actually — for step 3's hmain, I need `fderiv_mul` — let me test in scratch whether fderiv_mul accepts contDiff-derived DifferentiableAt for `-z 0` and `(1+z 0²)⁻¹`... And whether the RESULT's smul simplifies. Also test whether the direct standalone ContDiff.inv-derived DifferentiableAt works in fderiv_mul.

   Also for `fderiv_neg` — check its argument requirement.

Let me scratch-test these pieces.
dsh: reasoning:
Learning:

1. `fderiv_neg` doesn't match `fderiv ℝ (fun z => -z 0) 0` — its statement must be different: maybe `fderiv_neg : fderiv 𝕜 (-f) x = -fderiv 𝕜 f x` where `-f` is the negated FUNCTION (pointwise neg via Pi instance) — `fun z => -z 0` is NOT `-f` for f = `fun z => z 0` syntactically (it's `fun z => -(z 0)` vs `(fun z => z 0)⁻¹`-style pointwise-neg `-f` = `fun z => -(f z)` — hmm — `-f` for f : P2 → ℝ IS `fun z => -(f z)` = `fun z => -(z 0)` — syntactically that should match... unless `fderiv_neg`'s statement is `fderiv 𝕜 (fun x => -f x) x' = ...` with the LHS in lambda form `fun x => -f x` — hmm — the pattern error shows it searched for `fderiv ?m.30 (-?m.38) ?m.39` — so fderiv_neg's LHS is `fderiv 𝕜 (-f) x` — the pointwise negated function `-f` — and the goal has `fun z => -z 0` — `-f` with f = `fun z => z 0` = `fun z => -(z 0)` — hmm — is `-f` definitionally `fun z => -(f z)`? YES (Pi.neg instance: `(-f) x = -f x`) — so `fderiv ℝ (-(fun z => z 0)) 0` vs goal `fderiv ℝ (fun z => -z 0) 0` — the goal is a lambda, the pattern is an application `-f` — rw doesn't eta-reduce the goal's lambda to `-(fun z => z 0)`. FIX: `change fderiv ℝ (-(fun z : P2 => z 0)) 0 = ...` first, or avoid fderiv_neg: use `fderiv_const_mul` with (-1)? — `fderiv_const_mul (ha) (b) : fderiv 𝕜 (fun x => a x * b) x = fderiv 𝕜 a x * b` — hmm — with a = `fun z => z 0`, b = -1: `fun x => (x 0) * (-1)` — not `-z 0`... change: `-z 0 = z 0 * -1` — hmm — `change fderiv ℝ (fun z : P2 => z 0 * (-1 : ℝ)) 0 = ...`. Then fderiv_const_mul — hmm — its RHS: `fderiv 𝕜 a x * b` — a CLM times a scalar — `(fderiv a x) * b` — for CLM-valued... hmm — `fderiv_const_mul (ha : DifferentiableAt 𝕜 a x) (b : 𝔸) : fderiv 𝕜 (fun x => a x * b) x = fderiv 𝕜 a x * b` — codomain 𝔸 NormedCommRing-ish — `fderiv a x * b` = the CLM multiplied by b — hmm — CLM `*` scalar? `(fderiv ℝ a 0) * (-1 : ℝ)` — hmm — this is `ContinuousLinearMap.mul`-ish... For 𝔸 = ℝ as the codomain: `(L : P2 →L[ℝ] ℝ) * (-1 : ℝ)` — hmm — there's a `*` on CLMs into an algebra? Ugh.

   SIMPLEST: `change fderiv ℝ (-(fun z : P2 => z 0)) 0 = -(ContinuousLinearMap.proj (R := ℝ) 0)` then `rw [fderiv_neg]`... wait — need the hypothesis arg: `fderiv_neg (h : DifferentiableAt 𝕜 f x)` — hmm — actually let me check fderiv_neg's signature — it might take `[ContinuousNeg]`... whatever — the `rw [fderiv_neg]` failed on PATTERN — after `change` it should match.

   ALTERNATIVELY — hmm — do I even need the `-z 0` derivative as a lemma? In hmain I used hf to simplify. Alternative: define the whole `fun z => -z 0 * (1 + z 0^2)⁻¹` as `-(fun z => z 0 * (1+z 0²)⁻¹)` and use fderiv_neg on THAT:
   `fderiv ℝ (-(fun z => z 0 * (1 + z 0 ^ 2)⁻¹)) 0 v = -fderiv ℝ (fun z => z 0 * (1+z 0²)⁻¹) 0 v` — then fderiv_mul on the positive product: `= -(0 0 • fderiv d 0 + d 0 • fderiv proj 0) v` — with `0 0 = 0` and d 0 = 1: `-(0 + 1 • proj 0 v) = -1` ✓. This avoids the `-z 0`-derivative lemma entirely. 

2. `ContDiffAt.differentiableAt_one` — needs ContDiffAt of order **1** — my `ContDiff.contDiffAt` gives order ∞ — the error: "has type ContDiffAt ℝ ∞ (...) but is expected ContDiffAt ℝ 1 (...)" — `differentiableAt_one : ContDiffAt 𝕜 1 f x → DifferentiableAt 𝕜 f x` — need `ContDiffAt.of_le (h : ContDiffAt 𝕜 n f x) (hn : 1 ≤ n)` — in ChartLeviCivitaSmooth the original code used `(ContDiff.of_le h (WithTop.coe_le_coe.mpr le_top)).contDiffAt` then `.differentiableAt_one` — wait — the pattern used was `((ContDiff.of_le (c.g_component_smooth i j) (WithTop.coe_le_coe.mpr le_top)).contDiffAt).differentiableAt_one` — hmm — that's `ContDiff.of_le : ContDiff 𝕜 n f → m ≤ n → ContDiff 𝕜 m f` — from ∞ to 1 — `WithTop.coe_le_coe.mpr le_top : (1 : ℕ∞) ≤ ∞`-ish. For ContDiffAt: `ContDiffAt.of_le h le_one_top`-ish — or just use the ContDiff-level: `(ContDiff.of_le (contDiff_proj' 0) (by norm_num... hmm — le_top?))` — hmm — the earlier working code in ChartLeviCivitaSmooth line 451-452: `((ContDiff.of_le (c.g_component_smooth i j) (WithTop.coe_le_coe.mpr le_top)).contDiffAt).differentiableAt_one` — hmm — `WithTop.coe_le_coe.mpr le_top` — hmm — `(1 : ℕ∞) ≤ ∞` — `WithTop.coe_le_coe.mpr le_top : (1 : WithTop ℕ) ≤ ↑⊤`?? — hmm — the `∞` for ContDiff is `ENat`-based ℕ∞ω — `WithTop.coe_le_coe.mpr le_top` proves `(1 : ℕ∞) ≤ (∞ : ℕ∞)`? — hmm — `le_top`... whatever — I'll copy the exact pattern from the working file: `(ContDiff.of_le h (WithTop.coe_le_coe.mpr le_top)).contDiffAt` — hmm — wait — but is it `WithTop` here? The order type for ContDiff ℝ ∞ is ℕ∞ (WithTop ℕ)?? The docstring of ChartLeviCivitaSmooth says "the `∞` of the two `ℕ∞ω` tops" — and that code used `WithTop.coe_le_coe.mpr le_top` — copy that pattern exactly. Hmm — hmm — `WithTop.coe_le_coe.mpr le_top` — le_top : ⊤ ≤ ⊤?? — no — `coe_le_coe.mpr le_top` — hmm — the mpr direction: `coe_le_coe : ↑a ≤ ↑b ↔ a ≤ b` — so `coe_le_coe.mpr le_top : (1 : WithTop ℕ) ≤ ↑(⊤ : ℕ)`?? — that doesn't typecheck... Actually in that file's line 451: `(ContDiff.of_le (c.g_component_smooth i j) (WithTop.coe_le_coe.mpr le_top))` — hmm — hmm — whatever it is, it COMPILED there, so copy it verbatim. Hmm wait — let me actually check the exact line. Line 451-452 from my earlier read:
   ```
   exact ((ContDiff.of_le (c.g_component_smooth i j)
     (WithTop.coe_le_coe.mpr le_top)).contDiffAt).differentiableAt_one
   ```
   Hmm — hold on — that was for ContDiff ℝ ∞ → hmm — `ContDiff.of_le h hn : ContDiff 𝕜 m f` with hn : m ≤ n — `(WithTop.coe_le_coe.mpr le_top)` : `(1 : ℕ∞) ≤ ∞` — where ℕ∞ = WithTop ℕ and ∞ = ⊤ — `coe_le_coe : (a : WithTop α) ≤ (b : WithTop α) ↔ a ≤ b` — hmm — mpr with le_top : ⊤ ≤ ⊤?? — no... — hmm — actually maybe ℕ∞ = ℕ∞ω = WithTop (WithTop ℕ) — `∞` = the SECOND top = ↑⊤ (coe of the first top) — and `1 = ↑↑1` — so `coe_le_coe : (1 : ℕ∞) ≤ (∞ : ℕ∞) ↔ (↑1 : WithTop ℕ) ≤ (⊤ : WithTop ℕ)` and `le_top : ↑1 ≤ ⊤` ✓!! — so `(WithTop.coe_le_coe.mpr le_top) : (1 : ℕ∞) ≤ (∞ : ℕ∞)` ✓. Copy verbatim.

3. So the fixes:
   - hmain: restructure with the single `-` on the whole product (change + fderiv_neg), and use `(ContDiff.of_le ... (WithTop.coe_le_coe.mpr le_top)).contDiffAt.differentiableAt_one` for differentiability hypotheses.

   - The dFamily_conformal fderiv_pow issue (test 2 failed): the square's fderiv — same instance problem — need a different route: use `fderiv_mul` on `y ↦ y 0 * y 0`:
     `fderiv ℝ (fun y => y 0 * y 0) x = y... = (x 0) • fderiv proj 0 + (x 0) • fderiv proj 0` — then `= (2 * x 0) • proj 0` — needs the smul arithmetic `(x 0) • L + (x 0) • L = (2 * x 0) • L` — for CLMs: `a • L + b • L = (a+b) • L` — `add_smul` — simp/ring-ish. Let me test fderiv_mul for this (does fderiv_mul have the same instance mismatch? fderiv_mul's codomain 𝔸 with `NormedCommRing`... — the scratch test 8's hmain with fderiv_mul — the error list showed only errors at 21/28/35 — the hmain example itself wasn't reported — because compilation stopped? The errors shown are the LAST batch — let me re-run scratch8 with the fixes and see if fderiv_mul works with the contDiff-derived differentiability (with proper of_le).

   - The gInv_smooth field: ContDiff.inv's instance mismatch against the FIELD goal — hmm — scratch test 1 passed with a FRESH goal — but the field-goal has the RCLike-instance. To confirm the mismatch is real in the field context, I could test a mini-structure. Alternatively — AVOID the SmoothChartData entirely as planned (pointwise route). Given the timebox, GO POINTWISE. But then dFamily_conformal (the fderiv bridge) — with the pointwise route, the "d" is data — I'd drop the dFamily_conformal fderiv lemma... — hmm — but its fderiv_pow issue is now fixable via fderiv_mul — hmm — but the pointwise route doesn't need it at all. The bridge lemma `dFamily_conformal` connecting the DERIVED dFamily to the explicit d — valuable for provenance ("d is the actual derivative") — but optional for the model's core claim (curvature from coefficients). Given remaining time (~1h50m left in invocation, need ~30-40 min for bookkeeping+final build+audit), I'll:
     (a) Keep the SmoothChartData construction IF I can fix the instance issues quickly (try the of_le fix for gInv_smooth via a direct test of the field-goal instance mismatch — if ContDiff.inv fails against the field goal, switch to pointwise).
     (b) Regardless: restructure to make hmain work (change + fderiv_neg route) and dFamily via fderiv_mul.

   Let me test the field-instance mismatch quickly with a mini structure replicating SmoothChartData's field, plus the fderiv_mul-based square.
dsh: reasoning:
Confirmed:
1. The **field-instance mismatch is REAL** (line 33): the structure-field goal's ℝ uses `Real.normedAddCommGroup RCLike.toInnerProductSpaceReal.toNormedSpace` while `ContDiff.inv` produces `NonUnitalNormedRing.toNormedAddCommGroup (NormedAlgebra.toNormedSpace ℝ)`. These two `NormedSpace ℝ ℝ` instances are NOT defeq in this mathlib. So the SmoothChartData route is BLOCKED for the gInv_smooth field (this is a genuine mathlib instance-path obstacle; the same would hit `ContDiff.inv`-based proofs of any Pi-elaborated field).

   → DECISION: go POINTWISE for the model (ChartMetricCoefficients with explicit d data), with the pointwise algebra + explicit curvature computation. The SmoothChartData-style fderiv bridge gets recorded honestly as remaining work with this exact instance blocker. This is fine — the model's headline (nonzero curvature from metric coefficients) holds at the pointwise coefficient level, and `ChartModel1D` is the same pointwise-level model.

   Hmm — wait — one more idea for the field: could I avoid `ContDiff.inv` and prove `ContDiff ℝ ∞ (fun x => (1 + x 0²)⁻¹)` with the RCLike instance by COMPOSITION with the univariate smooth `Inv.inv`? — `ContDiff.comp` — hmm — is `Inv.inv : ℝ → ℝ` ContDiff-∞? — `contDiff_inv`? — hmm — the univariate inv IS smooth away from 0: there might be `contDiffAt_inv`/`ContDiff.inv` on functions — same instance issue for the outer ℝ... `ContDiff.comp (g : F → G) (f : E → F)` — g = `fun y : ℝ => y⁻¹` — the univariate `Inv.inv` smooth: hmm — `contDiff_inv`? Let me not go down this path — the instance problem likely persists (the codomain of the composition is ℝ with whatever instance ContDiff.comp's elaboration uses).

   → POINTWISE IT IS. But wait — the fderiv_bridge (`dFamily_conformal`) was the "actual derivative" provenance — with pointwise, I'll state d explicitly and record the fderiv-bridge as blocked-by-instances. Hmm — but hold on — the dFamily_conformal lemma itself (STANDALONE, not in a field) — scratch test showed `ContDiff.inv` works standalone... but dFamily_conformal needs fderiv of `(1 + y 0^2) * if...` — polynomial — via fderiv_mul (tested — works modulo the `(fun y => y 0) * fun y => y 0` vs `fun y => y 0 * y 0` syntactic gap at line 57 — fixable with `change`/congr on the fderiv argument).

   Actually — hmm — time check: I've spent ~2h05m. The pointwise restructure is quick. Let me decide the FINAL structure of the model module:

   - `conformalPointwise (x) : ChartMetricCoefficients (Fin 2)` — explicit g, gInv, d with all fields proved (algebra).
   - `christoffel_pointwise_conformal : christoffel (conformalPointwise x) k i j = conformalGamma k i j x` — Fin computation.
   - `conformal_riemann_1212_origin` / `conformal_curvature_nonzero` — same as planned, referencing christoffel (conformalPointwise 0) via the pointwise equality.
   - DROP: SmoothChartData instance, dFamily_conformal, fderiv_proj/contDiff_proj generic lemmas (keep the hmain-specific fderiv proofs inline, with `change` fixes and the of_le-differentiability pattern).
   - Docstring: honest statement that the derivative datum is declared (pointwise model), the smooth datum + fderiv-bridge is blocked by the normed-space instance-path mismatch (NormedAlgebra vs RCLike instances on ℝ in the Pi-elaborated ContDiff field) — recorded in remaining_blockers.

   And keep a STANDALONE smoothness lemma for the metric components? — optional; `ContDiff.inv` standalone works (scratch test 1) — I can add `conformalG_smooth`/`conformalGInv_smooth` as standalone lemmas (not structure fields) — proving the coefficient functions are smooth — that preserves most of the "smooth metric" claim: the metric coefficients are smooth functions on the chart (standalone ContDiff lemmas, which typecheck) — and the datum is pointwise (matching ChartModel1D's approach). Actually — hmm — a standalone lemma `conformalGInv_smooth : ContDiff ℝ ∞ conformalGInv` — the goal's ℝ instance — elaborated fresh — which instance does a fresh goal get? Scratch test 1's fresh goal got the NormedAlgebra-path (since `exact ContDiff.inv` worked). So a standalone lemma works with `exact ContDiff.inv ...`. Good — include those as standalone smoothness lemmas.

   Hmm — also — the `two_smul`-thing at line 59: the last step of the square calc — `x 0 • L + x 0 • L = (2 * x 0) • L` — need `(x 0) + (x 0) = 2 * x 0` — `ring` — and `(a+b) • L = a • L + b • L` — `add_smul`. Since I'm dropping dFamily_conformal, I don't need the square's fderiv AT ALL — hmain only needs the product `-z 0 * (1+z 0²)⁻¹` via fderiv_mul + fderiv of `-z 0` — the `-z 0` fderiv — via `change` + fderiv_neg + proj — from scratch8: `rw [fderiv_neg]` failed on pattern — fix with `change fderiv ℝ (-(fun z : P2 => z 0)) 0 = ...` then `rw [fderiv_neg]` — hmm — fderiv_neg's statement might be `fderiv 𝕜 (-f) x = -fderiv 𝕜 f x`? — hmm — wait — actually — SIMPLER: hmain via the `-(whole product)` route: `change fderiv ℝ (-(fun z : P2 => z 0 * (1 + z 0 ^ 2)⁻¹)) 0 v = -1` then `rw [fderiv_neg]` → `-fderiv ℝ (fun z => z 0 * (1+z 0²)⁻¹) 0 v = -1` → `rw [fderiv_mul ...]` → `-(0 0 • fderiv d 0 + (1+0²)⁻¹ • fderiv proj 0) v` → `-1`. fderiv_neg's arg: DifferentiableAt — via of_le-differentiability of the product — `(ContDiff.of_le ((ContDiff.mul ...)) ...)`-hmm — build DifferentiableAt of `fun z => z 0 * (1+z 0²)⁻¹` via `DifferentiableAt.mul (of_le-proj) (of_le-inv)` — the DifferentiableAt.mul — hmm — instance paths again?? — the earlier hmain scratch (scratch8's last example) — the hmul line — its errors weren't shown (compilation of scratch8 aborted at earlier errors? No — scratch8's errors were at 21/28/35 — the hmain example at 40+ wasn't in the error output — hmm — actually the errors shown were the LAST 3 — grep showed the tail — the hmain example may have compiled! Let me recheck — scratch8's error output showed errors at 21 (fderiv_neg rw), 28 (differentiableAt_one), 35 (differentiableAt_one) — and the hmain example (starting line 42) — its `have hf0 := ((contDiff_proj' 0).neg.contDiffAt).differentiableAt_one` — would fail with the same of_le issue... but no error was reported for lines 42-65 — hmm — maybe compilation stopped early? Or the errors at 28/35 are within the hmain example (line 28 is the standalone DifferentiableAt example, line 35 hf0...). Let me recount scratch8 lines: 28 = the standalone DifferentiableAt inv example ✓, 35 = hf0 in hmain ✓. So hmain's OTHER parts (hmul := fderiv_mul hf0 hd0 — wait — hf0 failed at 35 — so hmul didn't elaborate. But the fderiv_mul itself — hmm — in scratch9, `have hm := fderiv_mul hd hd` ELABORATED fine (line 54-56) with hd from of_le ✓ — so fderiv_mul accepts the of_le-differentiability ✓✓. And its RHS: `x 0 • fderiv ... + x 0 • fderiv ...` ✓.

   So the hmain plan (all pieces tested):
   ```lean
   have hmain : fderiv ℝ (fun z : P2 => -z 0 * (1 + z 0 ^ 2)⁻¹) (0 : P2) (single 0 1) = -1 := by
     change fderiv ℝ (-(fun z : P2 => z 0 * (1 + z 0 ^ 2)⁻¹)) (0 : P2) (single 0 1) = -1
     rw [fderiv_neg]
     · have hz0 : DifferentiableAt ℝ (fun z : P2 => z 0) (0 : P2) :=
         ((ContDiff.of_le (contDiff_proj' 0) (WithTop.coe_le_coe.mpr le_top)).contDiffAt).differentiableAt_one
       have hd0 : DifferentiableAt ℝ (fun z : P2 => (1 + z 0 ^ 2)⁻¹) (0 : P2) :=
         ((ContDiff.of_le (ContDiff.inv (ContDiff.add contDiff_const (ContDiff.pow (contDiff_proj' 0) 2))
           (fun x => denom_ne x)) (WithTop.coe_le_coe.mpr le_top)).contDiffAt).differentiableAt_one
       have hpd := hz0.mul hd0  -- hmm — DifferentiableAt.mul — instance OK? — test
       ...
     · (differentiability hypothesis of fderiv_neg)
   ```
   hmm — `fderiv_neg`'s hypothesis: `(h : DifferentiableAt 𝕜 f x)` — rw [fderiv_neg] generates it as a side goal — `· ...` first? — rw puts side goal FIRST (per our earlier finding: main-goal-first ordering?? — earlier we concluded rw puts the MAIN goal first — wait — no — the earlier analysis: rw [fderiv_sum] had goals [main, side] with main auto-closed... hmm — we concluded "rw puts main goal first" — hmm — but then in scratch test 5 the rw [fderiv_sum] test had first bullet hitting the SIDE... Ugh — the ordering conclusion was: scratch test 5 [side(main-auto-closed-rfl), ...] — consistent with EITHER ordering given rfl-autoclose. The 501 case showed bullets hitting [main, side] — so main FIRST. So `rw [fderiv_neg]` → goals [main, diff-side-goal] — main first, then the diff hypothesis. So:
   ```lean
     rw [fderiv_neg]
     · -- main: -(fderiv of product applied) = -1
       rw [fderiv_mul hz0 hd0]   -- inline the hypothesis to avoid ordering issues
       ...simp...
     · -- side: DifferentiableAt
       exact (hz0.mul hd0)
   ```
   Hmm — fderiv_mul with inline args: `rw [fderiv_mul hz0 hd0]` — rw needs to match the LHS `fderiv ℝ (fun y => c y * d y) x` — the goal's fderiv arg is `fun z => z 0 * (1 + z 0 ^ 2)⁻¹` — a LAMBDA — while fderiv_mul's LHS has `fun y => c y * d y` — matching c := `fun z => z 0`, d := `fun z => (1+z 0²)⁻¹` — lambda-body `c y * d y` vs `y 0 * (1 + y 0 ^ 2)⁻¹` — c y = y 0 ✓, d y = (1+y 0²)⁻¹ ✓ — syntactic match ✓ (this matches unlike the `(fun y => y 0) * fun y => y 0` function-mul form). GOOD.

   Then the evaluation:
   goal: `-(0 0 • fderiv d 0 + (1 + (0:ℝ)^2)⁻¹ • fderiv (fun z => z 0) 0) (single 0 1) = -1`
   → rw [fderiv_proj]-ish for `fderiv (fun z => z 0) 0` — hmm — inline: `change` or rw with a have:
   ```
   have hproj : fderiv ℝ (fun z : P2 => z 0) (0 : P2) = ContinuousLinearMap.proj (R := ℝ) (0 : Fin 2) := by
     change fderiv ℝ (ContinuousLinearMap.proj (R := ℝ) (0 : Fin 2)) (0 : P2) = _
     exact ContinuousLinearMap.fderiv
   rw [hproj]
   simp only [ContinuousLinearMap.proj_apply, Pi.single_eq_same]
   norm_num
   ```
   The `0 0 • fderiv d 0` term: `(0 : ℝ) • L` — smul of CLM — applied: `(0 • L) v = 0` — simp handles; the whole: `-(0 * ? + 1 * 1) = -1` — norm_num ✓ — but wait — `(1 + (0:ℝ)^2)⁻¹ = 1` — norm_num ✓; `(1 : ℝ) • (proj v) = proj v = single 0 1 0 = 1` — simp [smul_eq_mul?] — `1 • x = x` — `one_smul` [simp] ✓.

   One catch: `-z 0 * (1 + z 0^2)⁻¹` = `-(z 0 * (1 + z 0^2)⁻¹)` — the `change` — `-z 0 * t` parses as `(-z 0) * t` = `(-(z 0)) * t` — and `-(z 0 * t)` — `neg_mul` — NOT defeq — the `change` needs `neg_mul`-style rewriting — `change` works only if defeq... `-a * b` vs `-(a * b)` — for ℝ: `neg_mul : -a * b = -(a * b)` — a THEOREM, not defeq. So `change` fails. Instead: `rw [neg_mul]` first:
   ```
   rw [show -z 0 * (1 + z 0 ^ 2)⁻¹ = -(z 0 * (1 + z 0 ^ 2)⁻¹) from by ring]  -- under the fderiv? rw inside lambda?
   ```
   Hmm — rw under the lambda: `fderiv ℝ (fun z => -z 0 * ...) 0` — `rw [neg_mul]` — rewrites `-z 0 * t` → `-(z 0 * t)` — under the binder — rw handles (rewrites at any position including under lambdas? — rw rewrites under binders fine via its abstraction) ✓. So:
   ```
   rw [neg_mul]
   change fderiv ℝ (-(fun z : P2 => z 0 * (1 + z 0 ^ 2)⁻¹)) (0 : P2) (single 0 1) = -1
   ```
   Hmm — `-(fun z => ...)` — after neg_mul the fderiv arg is `fun z => -(z 0 * (1+z 0²)⁻¹)` — and `change` to `-(fun z => z 0 * (...))` — `fun z => -(t)` vs `-(fun z => t)` — Pi.neg — defeq ✓ (Pi.neg: `(-f) z = -f z`).

   OK. NOW — the fderiv_neg lemma name check — `fderiv_neg` exists? — Add.lean — let me trust and verify at compile.

   Let me also decide the module's final form. Since I'm going pointwise, I can SIMPLIFY: drop fderiv_proj/contDiff_proj generic lemmas (keep as local), drop conformalChartData/SmoothChartData, drop dFamily_conformal. Keep:
   - conformalG, conformalGInv (+ smoothness standalone lemmas — hmm — do I even keep those? The task's milestone is met by invocation-1's ChartLeviCivitaSmooth; for the model, the smoothness of coefficients is a nice claim — include the two standalone smoothness lemmas IF they compile quickly (they will, per scratch test 1 — the gInv one needs by_cases + mul-by-const — hmm — `ContDiff.mul (inv...) contDiff_const`-style — the mul on the diagonal... for the standalone lemma `ContDiff ℝ ∞ conformalGInv` — via contDiff_pi ×2 + by_cases + `simpa [conformalGInv, h] using ContDiff.inv ...` — fresh-goal instances — scratch test 1 passed exact — with by_cases/simpa should also work. Include; drop if they fight.)
   - conformalPointwise : ChartMetricCoefficients (Fin 2) — pointwise.
   - christoffel_pointwise_conformal.
   - conformalGamma, riemannComp, conformalGamma_origin_zero, conformalGamma_101_zero.
   - conformal_riemann_1212_origin, conformal_curvature_nonzero.
   - Docstring honest: pointwise datum (derivative data declared, matching ChartModel1D's approach); the smooth-datum fderiv bridge blocked by normed-space instance paths (recorded).

Let me now rewrite the file. ~50 min remaining before I must start final bookkeeping. GO.
dsh: reasoning:
`ChartPoint` is unknown! I imported `ChartLeviCivita` but NOT `ChartLeviCivitaSmooth` — and `ChartPoint` is defined in `ChartLeviCivitaSmooth`! (It's an abbrev there.) I removed that import when going pointwise. Fix: import `Poincare.D12.ConnectionCurvature.ChartLeviCivitaSmooth` (which itself imports the Form module). That also gives ContDiff machinery. Keep the FDeriv imports.
dsh: reasoning:
Progress! Remaining errors:

1. **Line 125** — the `conformalGInv_smooth` STANDALONE lemma ALSO hits the instance mismatch now! Interesting — because the goal `ContDiff ℝ ∞ conformalGInv` — after `rw [contDiff_pi]` ×2 — the elaborated goal's ℝ instance is RCLike-path (the `contDiff_pi` rewrite produces it?) — while scratch test 1's goal was written inline. The difference: after `rw [contDiff_pi]`, the subgoal's ℝ instance is FIXED by the outer elaboration. Fix options:
   (a) Prove the goal DIRECTLY without contDiff_pi-splitting: `ContDiff ℝ ∞ conformalGInv` — hmm — conformalGInv : ChartPoint (Fin 2) → Fin 2 → Fin 2 → ℝ — the goal ℝ instance — which path? — elaborated fresh — could be either... Let me test whether `exact ContDiff.comp ...`-style works. 
   (b) Write the smoothness for the SCALAR version only: `ContDiff ℝ ∞ (fun x => (1 + x 0^2)⁻¹)` — scratch test 1 proved this with `exact ContDiff.inv ...` ✓ — then lift to the full family via `contDiff_pi` FORWARD: `contDiff_pi.mpr` — construct `∀ i, ContDiff ℝ ∞ (fun x => conformalGInv x i)` — hmm — the component `fun x => conformalGInv x i` : ChartPoint → Fin 2 → ℝ — contDiff_pi again... and the innermost via by_cases + `simpa [conformalGInv, h] using (ContDiff.inv ...)` — the component goal's ℝ instance — same problem?? — but scratch test 1's goal was exactly the scalar form and worked... The difference between scratch-1 and here: here the goal arises from `contDiff_pi.mpr`'s subgoal elaboration — the instance could differ. Let me just test quickly.
   
   Hmm — actually — simplest: use `contDiff_pi.mpr` with the component proofs being scratch-test-1-style (they worked there). But the subgoals of mpr: `∀ i, ContDiff ℝ ∞ (fun x => conformalGInv x i)` — then another mpr for the second index — the component `fun x => (1 + x 0^2)⁻¹ * (if i = j then 1 else 0)` — the `if` — by_cases — diagonal: `simpa [conformalGInv, h] using ContDiff.inv ...` — instance path?? — test empirically. If it fails, another approach: keep conformalGInv_smooth as a scalar lemma: 
   ```lean
   lemma conformalGInv_smooth : ContDiff ℝ ∞ (fun x : ChartPoint (Fin 2) => (1 + x 0 ^ 2)⁻¹) := by
     exact ContDiff.inv (ContDiff.add contDiff_const (ContDiff.pow (conformal_contDiff_proj 0) 2))
       (fun x => conformal_denom_ne_zero x)
   ```
   — proving the SCALAR inverse factor is smooth (scratch-tested ✓) — and document that the full family follows by multiplying by the constant δ (trivially). That's honest and sufficient for the model's smoothness claim.

2. **Line 156** — "No goals to be solved" — the `d_symm` proof: `intro i j k; simp [conformalG]` — wait — the error at 156:4 — let me look — line 156 area: the `d_symm` field — `intro i j k` then `simp [conformalG]` — "No goals to be solved" at 156:4 — hmm — the d_symm goal: `d i j k = d i k j` — the d is DEFINED INLINE in the structure (`d := fun i j k => 2 * x 0 * (if i = 0 then 1 else 0) * (if j = k then 1 else 0)`) — the goal: `2 * x 0 * (if i = 0 then 1 else 0) * (if j = k then 1 else 0) = 2 * x 0 * (if i = 0 then 1 else 0) * (if k = j then 1 else 0)` — shown at 160:12 as unsolved: `(if j = k then if i = 0 then 2 * x 0 else 0 else 0) = if k = j then if i = 0 then 2 * x 0 else 0 else 0` — hmm — the if got re-associated by simp — and "No goals to be solved" at 156 — the first bullet?? — wait — my d_symm proof: `intro i j k; simp [conformalG]` — hmm — I wrote `simp [conformalG]` — conformalG unused (it's about d!) — the goal needs `if j = k then ... = if k = j then ...` — the if-commutation — `by_cases h : j = k <;> simp [h, h.symm]`-ish. The "No goals to be solved" — maybe simp [conformalG] closed the main goal but... confusing. Let me restructure d_symm:
   ```lean
   d_symm := by
     intro i j k
     by_cases h : j = k
     · simp [h]
     · have h' : ¬ k = j := fun hkj => h hkj.symm
       simp [h, h']
   ```

3. **Lines 187-190** — `christoffel_pointwise_conformal` — the summand has `conformalGInv x 0 0` un-unfolded — because `simp only [..., conformalPointwise, conformalGamma]` — `conformalPointwise` unfolds the STRUCTURE — but `conformalGInv x 0 0` — hmm — `(conformalPointwise x).gInv k l` → after unfolding conformalPointwise: `conformalGInv x k l` — then conformalGInv needs unfolding — add `conformalGInv` and `conformalG` to the simp list:
   ```lean
   simp only [ChartMetricCoefficients.christoffel, ChartMetricCoefficients.christoffelLower,
     conformalPointwise, conformalGamma, conformalGInv, conformalG]
   ```
   Hmm — wait — the goal at 187: `conformalGInv x 0 0 * x 0 = x 0 * (1 + x 0^2)⁻¹` — after unfolding conformalGInv: `(1+x 0²)⁻¹ * 1 * x 0 = x 0 * (1+x 0²)⁻¹` — ring ✓.

Also — the `ring` failures at 187-190 suggest ring couldn't normalize — with conformalGInv unfolded, ring handles commutativity ✓.

Let me apply these fixes. Also line 128 warning "tactic never executed" — cascade, will vanish.
dsh: reasoning:
Close. Remaining:

1. Line 153 "No goals to be solved" — in `d_symm` — my `by_cases h : j = k <;> simp [h]`... wait — line 153 — let me look at the current lines 145-160. The `d_symm` — hmm — "No goals to be solved" means a tactic had no goal — the `· simp [h]` after by_cases? Hmm — actually the error line 153:4 — let me check the file. Wait — the d_symm I wrote:
```lean
  d_symm := by
    intro i j k
    by_cases h : j = k
    · simp [h]
    · have h' : ¬ k = j := fun hkj => h hkj.symm
      simp [h, h']
```
Hmm — line 153 is `· have h' := ...`? or the `simp [h]`? — "No goals to be solved" at 153:4 — maybe the FIRST `· simp [h]` closed everything and the second bullet has no goal?? No — by_cases creates two goals... unless the d_symm goal is CLOSED by `intro i j k` + something — hmm — actually — wait — maybe `by_cases h : j = k` — with the goal `2 * x 0 * (if i = 0 then 1 else 0) * (if j = k then 1 else 0) = 2 * x 0 * (if i = 0 then 1 else 0) * (if k = j then 1 else 0)` — hmm — the ifs are `if j = k` vs `if k = j` — `simp [h]` in the true case — the ifs both reduce to 1 — closed ✓. False case: `simp [h, h']` — both ifs → 0 — ✓. Where's "No goals to be solved"?? — line 153 col 4 — let me just view the region.

2. Line 191 "No goals to be solved" — in christoffel_pointwise_conformal — after the `rw [Fin.sum_univ_two]` + `fin_cases...` + `norm_num` — hmm — the 8 `· ring` bullets — "No goals to be solved" at 191 — one bullet too many or norm_num closed everything?? — the warning says `conformalG` simp arg unused — and "No goals to be solved" — maybe norm_num closed ALL 8 goals (the sums became trivially equal after conformalGInv/G unfolding + norm_num evaluated everything, leaving `x 0 * (1+x 0²)⁻¹ = x 0 * (1+x 0²)⁻¹`-style — rfl-closed by norm_num?) — then the 8 `· ring` bullets have no goals. Fix: remove the ring bullets (or make them `all_goals ring`? no — just remove).

   Actually the `fin_cases k <;> fin_cases i <;> fin_cases j <;> norm_num` — norm_num with the previously-simp-unfolded goal — each of the 8 goals like `(1+x 0²)⁻¹ * x 0 = x 0 * (1+x 0²)⁻¹` — norm_num closes?? norm_num doesn't do commutativity — hmm — but maybe simp inside norm_num does. It closed 187-190's earlier failures — now with conformalGInv unfolded, `norm_num` — hmm — norm_num would leave `x 0 * (1 + x 0²)⁻¹ = x 0 * (1+x 0²)⁻¹`?? — and then... "No goals to be solved" at 191 = the first `· ring` — meaning norm_num closed everything — maybe via the `ring`-inside-norm_num (norm_num does do basic ring normalization for numerals but not full ring — hmm — but maybe simp-only inside norm_num closed the refl). Either way — remove the 8 ring bullets and see.

3. Line 236: the `rw [christoffel_pointwise_conformal ...]` in conformal_riemann_1212_origin fails — pattern `(conformalPointwise 0).christoffel 0 1 1` not found — the goal has `(fun k i j x => (conformalPointwise x).christoffel k i j) 0 1 1 z` — the Γ argument in riemannComp is a LAMBDA — the christoffel appears under `fun z => ... z` — rw's pattern is `(conformalPointwise 0).christoffel 0 1 1` — the goal has `(conformalPointwise z).christoffel 0 1 1` (with z bound) — NOT `(conformalPointwise 0)` — the x got unified... The riemannComp def: `fderiv ℝ (fun z => Γ l k j z) x ...` with Γ := `fun k i j x => christoffel (conformalPointwise x) k i j` — so the fderiv function is `fun z => (fun k i j x => christoffel (conformalPointwise x) k i j) l k j z` = `fun z => christoffel (conformalPointwise z) l k j` — I want to rewrite `christoffel (conformalPointwise z) k i j` → `conformalGamma k i j z` UNDER the lambda — rw [christoffel_pointwise_conformal] — the lemma: `christoffel (conformalPointwise x) k i j = conformalGamma k i j x` — with x unified to the bound z — rw CAN do this (rewrites under binders with unification of x := z) — the failure message shows the pattern searched was `(conformalPointwise 0).christoffel 0 1 1` — because I passed `(x := 0)` explicitly!!! — I pinned x := 0 — remove the x := 0:
   ```lean
   rw [christoffel_pointwise_conformal (k := (0 : Fin 2)) (i := (1 : Fin 2)) (j := (1 : Fin 2))]
   rw [christoffel_pointwise_conformal (k := (0 : Fin 2)) (i := (1 : Fin 2)) (j := (0 : Fin 2))]
   ```
   — x inferred as z ✓. Also the QUADRATIC sum terms: `(fun k i j x => ...) 0 0 m 0 * ...` — the lambdas get beta-reduced by rw/unfold? — the hquad rewriting — hmm — the hquad `have` is about `conformalGamma ...` terms — but the goal's sum has `christoffel (conformalPointwise 0) ...` — rw [christoffel_pointwise_conformal] — hmm — the Γ argument lambdas in the SUM: `(fun k i j x => christoffel (conformalPointwise x) k i j) 0 0 m 0` — beta-reduces to `christoffel (conformalPointwise 0) 0 0 m` — rw's pattern after beta? — rw does whnf the target — `(fun ...) 0 0 m 0` reduces — then the pattern `christoffel (conformalPointwise x) k i j` matches with x := 0 ✓. So with x unpinned, the rw should hit all four occurrences in the sum too. ✓.

   Also note — the fderiv functions after rw: `fun z => conformalGamma (0) (1) (1) z` — hmm — my subsequent `hzero` rewrite expects `conformalGamma (0 : Fin 2) (1 : Fin 2) (0 : Fin 2) = fun _ => 0` — and the second fderiv's function after rw: `fun z => conformalGamma (0) (1) (0) z` — rw [hzero] — hzero's LHS `conformalGamma (0) (1) (0)` — the goal has `fun z => conformalGamma (0) (1) (0) z` — hmm — `conformalGamma (0) (1) (0)` vs `fun z => conformalGamma (0) (1) (0) z` — hzero: `conformalGamma (0) (1) (0) = fun _ => 0` — rw [hzero] on `fun z => conformalGamma (0) (1) (0) z` — the subterm `conformalGamma (0) (1) (0)` appears INSIDE the lambda — rw [hzero] rewrites it to `fun _ => 0` — making the fderiv function `fun z => (fun _ => 0) z` — hmm — ugly but reducible — fderiv_const_apply needs `fun z => 0`-form — beta-reduction handles `(fun _ => 0) z` → 0 — whnf ✓. Should work.

Let me fix these three things.
dsh: reasoning:
Line 153 = `· simp [h]` in d_symm — "No goals to be solved" at 153:4 — hmm — the by_cases generated goals but... wait — d_symm's goal is `d i j k = d i k j` — the `d` is INLINE in the structure — hmm — the goal elaborated: `(conformalPointwise x).d i j k = (conformalPointwise x).d i k j` — hmm — line 153 is the FIRST bullet `· simp [h]` — "No goals to be solved" means when the `·` was processed, there were no goals — because... `by_cases h : j = k` — hmm — maybe the elaborator resolved the d-projection and the goal became defeq-trivial?? — e.g. `(if j = k then ...) = (if k = j then ...)` — hmm — no auto-closing... Hmm — "No goals to be solved" at the `·` of the FIRST bullet — this happens when by_cases produced ZERO goals?? — no — by_cases always produces 2 (unless the goal is closed by the hypothesis immediately — hmm — no).

OH WAIT — I bet the issue: `by_cases h : j = k` — hmm — no. Let me look — line 153:4 is `· simp [h]`?? From the sed output: line 153 = `    · simp [h]` — hmm — line 152 = `by_cases h : j = k`, 153 = `· simp [h]`. "No goals to be solved" at 153:4 — the bullet processing — the tactic state had no goals — meaning after `by_cases`, all goals were already closed?? — Impossible... UNLESS the previous field's proof left the elaborator in a weird state — no — each field is separate. Hmm — OR — the d_symm GOAL ITSELF: `d i j k = d i k j` — the projection `(conformalPointwise x).d` — hmm — wait — maybe the goal after `intro i j k` is... and `by_cases h : j = k` — hmm — the FIRST goal (positive case) — `· simp [h]` — hmm.

Hmm — hold on — maybe the real issue: the error line numbering is off by the edit (line 153 = the `· simp [h]` inside gInv_symm?? Let me recount the sed output: lines 140-155 shown: g_symm's bullets at 140-142, gInv_symm 143-148, inv_mul 149-156, d_symm 157+. So line 153 = `    · field_simp` (the third field_simp in inv_mul!) — "No goals to be solved" at 153:4 — the THIRD `· field_simp` — meaning `fin_cases k <;> fin_cases j <;> simp [...]` closed MORE than expected — the 4 goals from fin_cases×2 — `simp [conformalG, conformalGInv, conformal_denom_ne_zero]` — closed some fully, leaving fewer for the field_simp bullets! — e.g. the off-diagonal cases (k ≠ j) get closed by simp entirely (sum = 0 = 0), and diagonal cases need field_simp. So bullets mismatched: 2 goals remain (the two diagonal), but I have 4 bullets. Fix: replace the 4 field_simp bullets with `all_goals field_simp` — hmm — all_goals applies to ALL remaining — but field_simp on already-closed... all_goals runs on remaining goals only. If exactly 2 remain, `all_goals field_simp` handles both. But if the diagonal goals need MORE than field_simp (e.g. ring after)? — field_simp might close fully. Let me use `all_goals field_simp`.

Hmm — wait — but "No goals to be solved" at 153 means at bullet 3 there were no goals — so the first 2 bullets consumed... hmm — bullet 1 field_simp closed goal 1, bullet 2 closed goal 2, bullet 3 — no goals left. So simp closed 2 of the 4 fully, and field_simp closed the remaining 2. Fix: `· field_simp` ×2? — but which 2 remain depends on goal order — the remaining 2 = the diagonal cases k=0,j=0 and k=1,j=1 (goals 1 and 4) — bullets run in ORDER: goal1 = (0,0) diagonal ✓ field_simp; goal2 = (0,1) — closed by simp; goal3 = (1,0) closed; goal4 = (1,1) ✓ field_simp. So bullets: [field_simp, field_simp] — 2 bullets. But WAIT — the bullet structure: after `fin_cases...; simp...` — if simp closed goals 2,3 — the remaining goal LIST = [diag(0,0), diag(1,1)] — the `·` bullets: first → diag(0,0), second → diag(1,1). So use TWO field_simp bullets. Hmm — but is field_simp alone enough for the diagonal? Goal: `(1+x 0²)⁻¹ * (1+x 0²) = 1`-form — field_simp → `(1+x 0²)⁻¹ * (1+x 0²) * something = ...` — hmm — field_simp [conformal_denom_ne_zero]? — the goal after simp: let me not over-think: replace the 4 bullets with 2 × field_simp; if a goal needs extra, iterate. Actually — safer: `all_goals (try field_simp; ring)` — hmm — `try field_simp` — hmm — field_simp might leave `x⁻¹ * x = 1`-ish which field_simp closes. I'll write:
```lean
    · field_simp
    · field_simp
```
and iterate.

2. Line 191 "No goals to be solved" — the first `· ring` in christoffel_pointwise — remove all 8 ring bullets (norm_num closed everything).

3. The rw x-pinning fix.
dsh: reasoning:
Three remaining issues:

1. **Line 153** — still "No goals to be solved" — hmm — line 153 is now... after my edit the inv_mul has 2 field_simp bullets — line 153 might now be the SECOND field_simp — no wait — the error persists at 153:4 — let me count: the edit removed 2 lines, so line 153 could be... Let me view 145-160 again. Possibly the FIRST `· field_simp` closed everything (field_simp can close multiple?? no). OR the goal list is just ONE remaining goal. Let me check: fin_cases k × fin_cases j = 4 goals; simp closed 3 fully (off-diagonal ×2 and... maybe ONE diagonal too?), leaving 1 → first bullet field_simp closes it, second bullet → no goals → error. Fix: use ONE bullet + `all_goals field_simp`? — safest: replace the two bullets with `all_goals field_simp`:
   ```lean
   all_goals field_simp
   ```
   Hmm — but if the remaining diagonal goal isn't closed by field_simp alone (needs ring too?) — iterate. Actually the diagonal goal: `(1+x 0²)⁻¹ * (1+x 0²) = 1` — field_simp closes (it uses inv cancellation with nonzero denominators — hmm — field_simp needs the denominators nonzero — `conformal_denom_ne_zero` — is it in context? field_simp solves nonzero obligations via `norm_num`/assumption? — field_simp generates side goals for denominators ≠ 0 — for `(1+x 0²)` it needs `1 + x 0² ≠ 0` — solvable by `conformal_denom_ne_zero x`?? — hmm — the context has x — field_simp might use `norm_num`-style `denom_ne_zero`?? — earlier I passed it into simp — the simp [..., conformal_denom_ne_zero] — hmm — field_simp has its own denom handling. Let me just use: `all_goals field_simp [conformal_denom_ne_zero]`? — hmm — hmm. Or avoid: the diagonal goal after simp might already be `(1+x 0²)⁻¹ * (1+x 0²) = 1` — `exact inv_mul_cancel₀ (conformal_denom_ne_zero x)`-ish — hmm — but as bullets it's fragile. Try `all_goals field_simp`.

2. **Line 180** — christoffel_pointwise — norm_num left `(1 + x 0 ^ 2)⁻¹ * (1 / 2 * (2 * x 0)) = x 0 * (1 + x 0 ^ 2)⁻¹` — 4 cases — norm_num didn't finish (needs ring after norm_num: `1/2 * (2 * x 0) = x 0` — ring). Fix: `fin_cases k <;> fin_cases i <;> fin_cases j <;> norm_num <;> ring` — hmm — `norm_num <;> ring` — hmm — the `<;>` chain: `fin_cases k <;> fin_cases i <;> fin_cases j <;> norm_num` then `all_goals ring`? — simplest: append `all_goals ring`:
   ```
   fin_cases k <;> fin_cases i <;> fin_cases j <;> norm_num
   all_goals ring
   ```
   ring handles `(1+x 0²)⁻¹ * (1/2 * (2 * x 0)) = x 0 * (1+x 0²)⁻¹`? — ring with the inverse term — ring treats `(1+x 0²)⁻¹` as an ATOM (a variable) — LHS = atom * (1/2*(2*x 0)) = atom * x 0 (ring normalizes 1/2*2 = 1) — RHS = x 0 * atom — commutativity ✓ — ring CLOSES ✓.

3. **Line 226** — the rw STILL fails — pattern `(conformalPointwise ?x).christoffel 0 1 1` not found — the goal's fderiv function: `fun z => (fun k i j x => (conformalPointwise x).christoffel k i j) 0 1 1 z` — the inner lambda NOT beta-reduced by rw's matching (rw does not beta-reduce the target before matching?). The pattern `(conformalPointwise ?x).christoffel 0 1 1` requires the term `(conformalPointwise z).christoffel 0 1 1` to be present after whnf — `(fun k i j x => (conformalPointwise x).christoffel k i j) 0 1 1 z` — whnf beta-reduces: `(fun ...) 0` → ... — the application is a redex — whnf DOES reduce it: `((fun k i j x => t) 0) 1 1 z` → whnf reduces the outer app chain: first `(fun k i j x => t) 0` → `fun i j x => t[k:=0]`?? — NO — whnf reduces only the HEAD redex: `(fun k i j x => t) 0 1 1 z` — the head is `(fun k i j x => t) 0` — a beta-redex — whnf reduces it to `(fun i j x => t[0]) 1` — then continues?? — whnf only reduces the top-level: `(fun k ... => t) 0 1 1 z` — the top redex is `(fun k...) 0` applied to `1 1 z` — whnf: `((fun k i j x => t) 0) 1 1 z` — the head `((fun k...) 0)` is a beta redex — whnf reduces → `(fun i j x => t[0]) 1 1 z` — STILL a redex at the head — whnf continues until no head-redex: → `(fun j x => t[0,1]) 1 z` → `(fun x => t[0,1,1]) z` → `t[0,1,1,z]` = `(conformalPointwise z).christoffel 0 1 1` ✓ — whnf DOES reduce it. Hmm — so why didn't rw find it?? — rw's matching uses `withReducible`?? — rw default: does NOT unfold definitions but DOES beta — hmm — but maybe rw's `matchPattern` doesn't whnf the TARGET... rw uses `kabstract` on the goal — the abstraction — the pattern matching in `rw` — hmm — it does use `isDefEq` with default transparency? — which includes beta? — `isDefEq` DOES reduce beta (whnf) — hmm.

   Hmm — wait — look at the error pattern: it searched `(conformalPointwise ?x).christoffel 0 1 1` — and the target contains `(fun k i j x => (conformalPointwise x).christoffel k i j) 0 1 1 z` — UNREDUCED in the displayed target — rw's abstraction: `kabstract` makes the goal abstracted... — maybe the issue: `fun z => (fun k i j x => ...) 0 1 1 z` — the `z`-lambda — rw works under the lambda by abstracting `z` — hmm.

   PRAGMATIC FIX: `simp only [christoffel_pointwise_conformal]`? — simp does beta+whnf aggressively and applies the lemma under binders ✓. Or add a `dsimp`/`change` first. Try replacing the two rw's with:
   ```lean
   simp only [christoffel_pointwise_conformal]
   ```
   Hmm — but simp only with the lemma — the lemma `christoffel (conformalPointwise x) k i j = conformalGamma k i j x` — simp rewrites LHS→RHS — occurrences: the fderiv functions (under binders — simp handles ✓) and the quadratic sum terms ✓. But CAREFUL — simp only [christoffel_pointwise_conformal] — would also rewrite... there are no other christoffel occurrences. And the direction LHS→RHS — ✓ (the lemma is LHS=explicit). Also the `(fun k i j x => ...) 0 1 1 z` — simp beta-reduces ✓.

   Actually — hmm — even simpler and more robust: change riemannComp's call to avoid the lambda: define the Γ family directly: in conformal_riemann_1212_origin, pass `Γ := fun k i j x => conformalGamma k i j x`?? — NO — that would bypass the christoffel provenance! The whole point: curvature of the COEFFICIENT-BUILT connection. Keep the lambda + simp approach.

   Then the follow-up `hzero` rw and hquad — the goal after simp: fderiv functions become `fun z => conformalGamma (0) (1) (1) z` etc. — hzero: `conformalGamma (0) (1) (0) = fun _ => 0` — rw [hzero] on `fun z => conformalGamma (0) (1) (0) z` — the subterm `conformalGamma (0) (1) (0)` — hmm — `conformalGamma (0) (1) (0)` appears as a subterm inside the lambda — rw rewrites it to `fun _ => 0` → the fderiv function `fun z => (fun _ => 0) z` — then hd2's fderiv_const_apply — hmm — the function must match `fun _ => c` — `fun z => (fun _ => 0) z` — beta-redex — fderiv_const_apply's pattern `fun _ => c` — the fderiv function `fun z => (fun _ => 0) z` — hmm — rw/simp whnf — `(fun _ => 0) z` reduces to 0 under whnf — the lambda body is `0` after whnf — pattern `fun _ => ?c` — matches ✓. But the rw [hzero] with the function-form LHS `conformalGamma (0) (1) (0) = fun _ => 0` — rewriting `fun z => conformalGamma (0) (1) (0) z` — the LHS pattern `conformalGamma (0) (1) (0)` — as a subterm — YES it's a subterm (applied to z). rw produces `fun z => (fun _ => 0) z` — then `rw [hd2]`?? — hd2's LHS is the FULL fderiv term — hmm — wait — I wrote `rw [hd2]` where hd2 : `fderiv ℝ (fun z => (0 : ℝ)) 0 (single 1 1) = 0` — the goal's second fderiv term after the hzero-rewrite: `fderiv ℝ (fun z => (fun _ => 0) z) 0 (single 1 1)` — hd2's LHS: `fderiv ℝ (fun z => 0) 0 (single 1 1)` — the functions `fun z => (fun _ => 0) z` vs `fun z => 0` — NOT syntactically equal — rw fails?! — hmm — unless rw's matching whnfs (it should — isDefEq with beta). RISK. SAFER: `simp only [christoffel_pointwise_conformal, hzero]` for the second term — simp applies hzero (function equality — simp can rewrite with function-extensional lemmas? — hzero is `conformalGamma ... = fun _ => 0` — an equality of FUNCTIONS — simp uses it to rewrite `conformalGamma (0) (1) (0) z`?? — simp rewrites `conformalGamma ...` — hmm — a function applied to z — the lemma LHS is the bare function — simp's congruence... it might work via funext-style... hmm.

   SIMPLEST BULLETPROOF: restructure the second term: after the christoffel-simp, use `change` to replace the whole second fderiv term:
   ```lean
   -- the ∂₂Γ¹₂₁ term vanishes
   have hzero2 : fderiv ℝ (fun z : ChartPoint (Fin 2) => conformalGamma (0 : Fin 2) (1 : Fin 2) (0 : Fin 2) z)
       (0 : ChartPoint (Fin 2)) (Pi.single (M := fun _ : Fin 2 => ℝ) (1 : Fin 2) (1 : ℝ)) = 0 := by
     rw [conformalGamma_101_zero]   -- hmm — the function → 0
     rw [fderiv_const_apply]
   rw [hzero2]
   ```
   where conformalGamma_101_zero : `conformalGamma (0) (1) (0) x = 0` — rw [conformalGamma_101_zero] rewrites `conformalGamma (0) (1) (0) z` → 0 inside the lambda ✓ (pointwise lemma — rw under binder with x := z ✓). Then `rw [fderiv_const_apply]` — function `fun z => 0` ✓ matches exactly.

   Similarly the first term: keep as-is (conformalGamma (0) (1) (1) = explicit) — the surviving fderiv function `fun z => conformalGamma (0) (1) (1) z` = `fun z => -z 0 * (1+z 0²)⁻¹` — hmain's LHS matches this modulo unfold conformalGamma — hmain: `fderiv ℝ (fun z => -z 0 * (1+z 0²)⁻¹) 0 (single 0 1) = -1` — the goal term `fderiv ℝ (fun z => conformalGamma (0) (1) (1) z) 0 (single 0 1)` — rw [hmain] — hmain's LHS function `fun z => -z 0 * ...` vs goal's `fun z => conformalGamma (0) (1) (1) z` — conformalGamma (0) (1) (1) z — unfold needed — rw won't unfold. Fix: `unfold conformalGamma` before hmain — or `change`:
   ```lean
   change fderiv ℝ (fun z : ChartPoint (Fin 2) => conformalGamma (0 : Fin 2) (1 : Fin 2) (1 : Fin 2) z) ...
       ... = -1
   rw [show (fun z : ChartPoint (Fin 2) => conformalGamma (0 : Fin 2) (1 : Fin 2) (1 : Fin 2) z) = fun z => -z 0 * (1 + z 0 ^ 2)⁻¹ from by funext z; norm_num [conformalGamma]]
   rw [hmain]
   ```
   Hmm — wait — actually — hmain's statement can just BE about conformalGamma directly:
   ```lean
   have hmain : fderiv ℝ (fun z : ChartPoint (Fin 2) => conformalGamma (0 : Fin 2) (1 : Fin 2) (1 : Fin 2) z)
       (0 : ChartPoint (Fin 2)) (Pi.single ... (0 : Fin 2) (1 : ℝ)) = -1 := by
     change fderiv ℝ (fun z : ChartPoint (Fin 2) => -z 0 * (1 + z 0 ^ 2)⁻¹) 0 (single 0 1) = -1
     ... (as before)
   ```
   `change` — `conformalGamma (0) (1) (1) z` defeq `-z 0 * (1+z 0²)⁻¹`? — conformalGamma is a MATCH — `conformalGamma (0) (1) (1) z` — reduces via the match clause `| ⟨0,_⟩,⟨1,_⟩,⟨1,_⟩ => -x 0 * ...` — match reduction — `(0 : Fin 2)` is `⟨0, proof⟩` — the match on `⟨0,_⟩` — DEFEQ reduction ✓ (match on constructor with proof irrelevance — the `_` matches any proof) — `change` does whnf — should work. Similarly the `change` for the fderiv_neg part inside hmain — keep as before.

   And the quadratic sum hquad: after the christoffel-simp, the sum terms: `conformalGamma (0) (0) m 0 * conformalGamma m (1) (1) 0 - ...` ✓ matches hquad's LHS (modulo m-renaming ✓).

   Let me now restructure conformal_riemann_1212_origin:
   ```lean
   lemma conformal_riemann_1212_origin : riemannComp (...) (0) (0) (1) (0) (1) = -1 := by
     unfold riemannComp
     simp only [christoffel_pointwise_conformal]
     -- now goal: fderiv (Γ 0 1 1) 0 e0 - fderiv (Γ 0 1 0) 0 e1 + quad = -1
     have hzero2 : fderiv ℝ (fun z : ChartPoint (Fin 2) => conformalGamma (0 : Fin 2) (1 : Fin 2) (0 : Fin 2) z)
         (0 : ChartPoint (Fin 2)) (Pi.single (M := fun _ : Fin 2 => ℝ) (1 : Fin 2) (1 : ℝ)) = 0 := by
       rw [conformalGamma_101_zero]
       rw [fderiv_const_apply]
     rw [hzero2]
     have hquad : (∑ m : Fin 2, (...) ) = 0 := by
       rw [Fin.sum_univ_two]
       norm_num [conformalGamma_origin_zero]
     rw [hquad]
     have hmain : fderiv ℝ (fun z : ChartPoint (Fin 2) => conformalGamma (0 : Fin 2) (1 : Fin 2) (1 : Fin 2) z)
         (0 : ChartPoint (Fin 2)) (Pi.single (M := fun _ : Fin 2 => ℝ) (0 : Fin 2) (1 : ℝ)) = -1 := by
       change fderiv ℝ (fun z : ChartPoint (Fin 2) => -z 0 * (1 + z 0 ^ 2)⁻¹) (0 : ChartPoint (Fin 2))
         (Pi.single (M := fun _ : Fin 2 => ℝ) (0 : Fin 2) (1 : ℝ)) = -1
       ... (fderiv_neg etc as before)
     rw [hmain]
     norm_num
   ```
   Wait — the hzero2 `rw [conformalGamma_101_zero]` — the lemma: `conformalGamma (0) (1) (0) x = 0` — rewriting `conformalGamma (0) (1) (0) z` — pattern `conformalGamma (0) (1) (0) ?x` — matches with x := z ✓ (under the lambda — rw's abstraction handles — earlier `rw [christoffelFamily_conformal]` under lambda FAILED though!! — hmm — wait — that failure was with the pinned x := 0 — after unpinning it might work... but I switched to simp. For hzero2 — `rw [conformalGamma_101_zero]` under the lambda — same risk — hmm — actually — WHY did the unpinned rw at 226 fail?? — the error shows the pattern searched `(conformalPointwise ?x).christoffel 0 1 1` — and target has `(fun k i j x => (conformalPointwise x).christoffel k i j) 0 1 1 z` — with the lambda NOT beta-reduced — rw does NOT beta-reduce the target — so the subterm `(conformalPointwise z).christoffel 0 1 1` is not "visible". rw under a lambda — hmm — rw DOES go under lambdas (it abstracts them) — but the ISSUE is the β-redex inside — rw does not unfold/reduce applications of lambdas. So for hzero2 — the goal's function `fun z => conformalGamma (0) (1) (0) z` — NO β-redex — the pattern `conformalGamma (0) (1) (0) ?x` — rw under the lambda: rw abstracts the binder z... — hmm — does rw rewrite under binders at all? — rw CAN (via `kabstract` it rewrites the whole expression where the binder becomes a free var) — e.g. `rw [conformalGamma_101_zero]` on `fderiv ℝ (fun z => conformalGamma (0) (1) (0) z) 0 v` — the goal is an equality of two terms — rw finds the subterm `conformalGamma (0) (1) (0) z` — hmm — but `z` is bound in the surrounding context — rw handles dependent contexts via abstract — YES rw rewrites under lambdas (it abstracts the free variables of the match). This worked earlier: `rw [conformalG, hjk]`-ish... e.g. in dFamily_conformal I did `rw [hg]` where hg was a FUNCTION equality — different. And in the metric-compat module `rw [hfun]`-style function-eq rws — hmm.

   To be SAFE: use `simp only [conformalGamma_101_zero]`?? — hmm — simp definitely handles under binders + beta. OR — funext-style: replace the fderiv function via a function equality + congr:
   ```lean
   have hzero2 : fderiv ℝ (fun z : ChartPoint (Fin 2) => conformalGamma (0 : Fin 2) (1 : Fin 2) (0 : Fin 2) z)
       (0 : ChartPoint (Fin 2)) (Pi.single (M := fun _ : Fin 2 => ℝ) (1 : Fin 2) (1 : ℝ)) = 0 := by
     simp only [conformalGamma_101_zero]
     rw [fderiv_const_apply]
   ```
   `simp only [conformalGamma_101_zero]` — rewrites the pointwise occurrence under the lambda (simp's congruence handles binders ✓ — definitely) — then the fderiv function becomes `fun z => 0` — `rw [fderiv_const_apply]` ✓. Hmm — but wait — `conformalGamma_101_zero`'s LHS `conformalGamma (0) (1) (0) x` — simp uses it as a rewrite `conformalGamma (0) (1) (0) ?x → 0` ✓.

   Similarly for hquad's `rw [hquad]` — hquad is about the SUM as a whole — `rw [hquad]` — the goal's sum matches hquad's LHS syntactically (no binders) ✓ fine.

   And `rw [hmain]` — the goal's first fderiv term vs hmain's LHS — hmain's LHS: `fderiv ℝ (fun z => conformalGamma (0) (1) (1) z) 0 (single 0 1)` — goal term after simp: `fderiv ℝ (fun z => conformalGamma (0) (1) (1) z) 0 (single 0 1)` — identical modulo... the goal's Γ family from riemannComp: after `simp only [christoffel_pointwise_conformal]` — the fderiv function: `fun z => (fun k i j x => conformalGamma k i j x) 0 1 1 z`?? — hmm — simp rewrote `christoffel (conformalPointwise z) ...` → `conformalGamma ... z` — but the ENCLOSING lambda `(fun k i j x => ...) 0 1 1 z` — simp beta-reduces it → `conformalGamma 0 1 1 z` — hmm — does simp beta-reduce `(fun k i j x => conformalGamma k i j x) 0 1 1 z`? — simp reduces head β-redexes — hmm — the function is a partial application `(fun k i j x => t) 0 1 1` — not fully applied to z? — in the fderiv function `fun z => Γ l k j z` with Γ := the lambda — `Γ 0 1 1 z` — full application — simp's whnf beta-reduces — the result `conformalGamma 0 1 1 z` ✓. So after the simp, the fderiv functions are exactly hmain/hzero2's forms ✓.

   Then `rw [hmain]` — exact syntactic match ✓ (no binders in the way — the fderiv function lambda is a subterm of the goal; rw matches the WHOLE fderiv-application term — pattern `fderiv ℝ (fun z => conformalGamma (0) (1) (1) z) 0 (single 0 1)` — the goal has exactly this ✓).

   Hmm — one subtlety — in `change` inside hmain: `change fderiv ℝ (fun z => -z 0 * (1 + z 0 ^ 2)⁻¹) 0 (single 0 1) = -1` — the current goal is `fderiv ℝ (fun z => conformalGamma (0) (1) (1) z) 0 (single 0 1) = -1` — change needs DEFEQ — conformalGamma (0) (1) (1) z — match reduction to `-z 0 * (1 + z 0^2)⁻¹` ✓ defeq — change does whnf ✓ should work.

   OK let me now rewrite the conformal_riemann_1212_origin lemma accordingly. Also fix inv_mul bullets and christoffel's ring.
dsh: reasoning:
Two small errors left:

1. **Line 229**: `⊢ 0 (Pi.single 1 1) = 0` — hzero2's `rw [fderiv_const_apply]` — the goal became `0 v = 0` — `(0 : CLM) v` — the `0` applied — simp didn't reduce `0 v` → 0. Add `simp` at the end of hzero2 (like nabla_const_apply):
   ```lean
   have hzero2 : ... = 0 := by
     simp only [conformalGamma_101_zero]
     rw [fderiv_const_apply]
     simp
   ```

2. **Line 247**: `rw [neg_mul]` — pattern `-?a * ?b` not found in `fderiv ℝ (fun z => -z 0 * (1 + z 0 ^ 2)⁻¹) 0` — the goal is `(fderiv ℝ (fun z => -z 0 * ...) 0) (single 0 1) = -1` — `-z 0 * t` is under the LAMBDA and rw didn't go under it?? — hmm — rw rewrites `-z 0 * (1+z 0²)⁻¹` — under the binder `fun z =>` — rw should handle — the pattern `-?a * ?b` — hmm — `-z 0 * t` parses as `(-(z 0)) * t` — pattern `-?a * ?b` = `(-?a) * ?b` — with ?a := z 0, ?b := t ✓ — "did not find" — hmm — maybe because rw matched at a different level first... or the `neg_mul` lemma's LHS form is `(-a) * b` — let me check: `neg_mul : -a * b = -(a * b)` ✓. Hmm — why not found — because rw needs to go UNDER the fderiv-application lambda — `fderiv ℝ (fun z => ...) 0` — the subterm `-z 0 * ...` is inside the lambda body — rw DOES rewrite under binders normally (it abstracts). BUT — the lambda is an ARGUMENT of `fderiv` — hmm — that shouldn't matter.

   Hmm — maybe the issue: `z 0` — `-z 0` — the parser: `-z 0 * (1 + z 0 ^ 2)⁻¹` — is `(-(z 0)) * ((1 + z 0^2)⁻¹)` ✓. The pattern `-?a * ?b` — hmm — the negation — `-?a` — hmm — the `?a` must match `z 0` ✓...

   Wait — actually — maybe rw's failure: the goal's expression `(fderiv ℝ (fun z => -z 0 * (1 + z 0 ^ 2)⁻¹) 0) (Pi.single 0 1) = -1` — hmm — the fderiv is applied to `(single 0 1)` — the function term `fun z => -z 0 * ...` — rw under the fun-binder: Lean's rw does NOT rewrite under lambda binders directly — it abstracts dependencies via `kabstract` which handles bound variables by... hmm — actually rw CAN rewrite under binders in many cases (e.g., earlier `rw [fderiv_const_add]` on `fderiv ℝ (fun y => 1 + y 0 ^ 2) x` worked! — that pattern was `fderiv 𝕜 (fun y => c + f y) x` — the WHOLE fderiv — not under the binder). And `rw [conformalGamma_101_zero]`-style under-binder rewrites I replaced with simp... hmm — did `rw [hg]` under a lambda work in dFamily_conformal? — hg was a FUNCTION equality — `rw [hg]` rewrites `fun y => conformalG y j k` — the lambda itself — OK.

   The problem case: rewriting a subterm INSIDE a lambda body where the pattern contains no binder. rw should do it (it does — e.g., `rw [hΓsmul]`-ish...). Hmm — but empirically "did not find". Maybe rw in this Lean version does NOT rewrite under binders for arbitrary patterns?? — no wait — earlier in the original ChartLeviCivitaSmooth, `rw [c.christoffelFamily_symm x k j i]` — hmm — that was under a sum_congr context...

   PRAGMATIC: use `simp only [neg_mul]` — simp definitely handles under-binder rewriting:
   ```lean
   simp only [neg_mul]
   ```
   then the `change` to `-(fun z => ...)` — hmm — wait — after simp only [neg_mul]: the fderiv function becomes `fun z => -(z 0 * (1 + z 0²)⁻¹)` — then `change fderiv ℝ (-(fun z => z 0 * (1 + z 0 ^ 2)⁻¹)) 0 (single 0 1) = -1` — defeq via Pi.neg ✓ — change works.

   Also — the `simp only [neg_mul]` might rewrite other negations in the goal?? — the goal `(fderiv ...) (single 0 1) = -1` — RHS `-1` — `neg_mul` pattern `-?a * ?b` — `-1` alone doesn't match ✓.

Let me apply these two fixes.
dsh: reasoning:
Last two issues in hmain:

1. **Line 258**: `rw [fderiv_mul hz0 hd0]` — fderiv_mul's LHS pattern: `fderiv ℝ ((fun z => z 0) * fun z => (1 + z 0 ^ 2)⁻¹) 0` — the function-PRODUCT form (Pi.mul), not the lambda `fun z => z 0 * (1+z 0²)⁻¹`. The goal has the lambda form — rw doesn't eta/pointwise-expand. Fix: `change` the goal's function to the product form first:
   ```lean
   change (-(fderiv ℝ ((fun z : ChartPoint (Fin 2) => z 0) * fun z => (1 + z 0 ^ 2)⁻¹) 0)) (single 0 1) = -1
   rw [fderiv_mul hz0 hd0]
   ```
   `(fun z => z 0) * (fun z => (1+z 0²)⁻¹)` — Pi.mul — `((A * B) z) = A z * B z` defeq ✓ — change works.
   Then after rw [fderiv_mul]: `-(z 0 • fderiv d 0 + d 0 • fderiv (z 0) 0) v` — wait — fderiv_mul's RHS: `c x • fderiv 𝕜 d x + d x • fderiv 𝕜 c x` with c = `fun z => z 0`, d = `fun z => (1+z 0²)⁻¹`, x = 0 — `(0 0) • fderiv d 0 + (1+0²)⁻¹ • fderiv c 0` — then `rw [hproj]` — hproj : fderiv of `fun z => z 0` at 0 = proj — the goal has `fderiv ℝ (fun z => z 0) 0` ✓ — then proj_apply/single_eq_same/norm_num — the remaining: `-(0 • (fderiv d 0 v) + 1 • (single 0 1 0))` — hmm — `0 • L` where L : CLM — `(0 • L) v = 0 • L v = 0` — and `(1 : ℝ)⁻¹ = 1`, `1 • L = L` — norm_num + the CLM application — need `(0 • L) v = 0` — `zero_smul`/simp — and `1 • L v = L v` — `one_smul` ✓ — norm_num handles numerals — hmm — `norm_num` might not discharge `(0 : ℝ) • x = 0` — `zero_smul` is [simp] — norm_num calls simp? — norm_num uses its own engine + simp-ish normalization — safer: `simp only [ContinuousLinearMap.proj_apply, Pi.single_eq_same]` then `norm_num` — the smul-of-CLM application: `(0 • fderiv d 0) (single 0 1)` — simp: `(a • L) v = a • L v`?? — hmm — CLM smul: `(a • L) v` — the SMul on CLMs — `smul_apply`?? — `(a • L) v = a • (L v)` — is that simp-normalized? — there's `ContinuousLinearMap.smul_apply` — hmm — `coe_smul`/`smul_apply` [simp] for CLMs ✓ probably. And `a • (L v)` for ℝ = `a * L v` — smul_eq_mul [simp] — then `0 * x = 0` ✓, `1 * x = x` ✓. So `simp only [ContinuousLinearMap.proj_apply, Pi.single_eq_same]` then `norm_num` should close `-(0 * ? + 1 * 1) = -1`. ✓

2. **Line 266**: "No goals to be solved" — the `· exact (...).mul (...)` second bullet — because `rw [fderiv_neg]`'s side goal — after the rw the goals ordering [main, side] — main bullet first — hmm — wait — the error at 266 = the second bullet — "No goals" — hmm — maybe fderiv_neg's hypothesis was auto-discharged?? — fderiv_neg — let me check its signature — maybe `fderiv_neg` doesn't take a hypothesis (differentiability instance or it's for CLMs?). If `fderiv_neg : fderiv 𝕜 (-f) x = -fderiv 𝕜 f x` with NO hypothesis (the fderiv always exists — hmm — fderiv is a totalized function! — many fderiv lemmas in this revision state: `fderiv_neg (h : DifferentiableAt 𝕜 f x)` — hmm — but maybe the simp-tagged version doesn't need it. If rw [fderiv_neg] generated NO side goal and the main goal remains — then bullets: first bullet (my main-block) + second bullet → no goal. Let me check fderiv_neg's signature.
dsh: reasoning:
`fderiv_neg` takes NO hypothesis (it's unconditional — the totalized fderiv). So `rw [fderiv_neg]` generates no side goal — my bullet structure [main-block, diff-proof] is wrong: the diff-proof bullet has no goal. Fix: remove the second bullet entirely. Also note — with no differentiability needed for fderiv_neg, I can simplify hmain: drop the `.mul`-differentiability side bullet, keep hz0/hd0 for fderiv_mul.
dsh: reasoning:
Line 264: the `change` with the function-product: `(-(fderiv ℝ ((fun z => z 0) * fun z => (1 + z 0 ^ 2)⁻¹) 0)) (single 0 1) = -1` — "typeclass instance problem stuck: TopologicalSpace ?m.720" — the `(fun z => z 0) * (fun z => ...)` — the Pi.mul on functions — needs... hmm — the `*` on `ChartPoint (Fin 2) → ℝ` — Pi.instMul — shouldn't need TopologicalSpace... The stuck metavariable — maybe from `(fun z => z 0)` — the elided type — or `(1 + z 0^2)⁻¹`'s elaboration — hmm — the `fun z => (1 + z 0 ^ 2)⁻¹` — z's type not inferred?? — add explicit types: `((fun z : ChartPoint (Fin 2) => z 0) * (fun z : ChartPoint (Fin 2) => (1 + z 0 ^ 2)⁻¹))`.

Hmm — wait — the stuck TopologicalSpace — maybe from the fderiv_mul matching... no — line 264 is the `change` line — `fderiv ℝ ((fun z => z 0) * fun z => (1+z 0²)⁻¹) 0` — hmm — the `*` — `Pi.instMul` — hmm — should be fine. The `?m.720` — hmm — maybe the issue: the NEGATION — `-(fderiv ...)` — the CLM negation — fine...

Let me just add the type annotations and see.
dsh: reasoning:
Still stuck at line 264 col 12. Col 12 ≈ `change (-(fderiv ℝ ...` — hmm — the `change` tactic's pattern elaboration — the stuck TopologicalSpace metavariable — where does TopologicalSpace appear? — `ContinuousLinearMap` has `[TopologicalSpace]` requirements — `(fun z => z 0) * (fun z => ...)` — the PRODUCT of two functions `ChartPoint → ℝ` — Pi.mul — no topology. The `fderiv ℝ (that product) 0` — fderiv requires the function between normed spaces — fine. `-fderiv...` — CLM neg — fine. `(...) (single 0 1)` — fine.

Hmm — the metavariable is probably from `(1 + z 0 ^ 2)⁻¹` — `⁻¹` — Inv.inv — ℝ — fine...

OR — the stuck one: the `change`'s expected-type — the goal `(-fderiv ℝ (fun z => z 0 * (1 + z 0 ^ 2)⁻¹) 0) (single 0 1) = -1` — hmm — `change` compares the new target with the old — the OLD goal's fderiv function — hmm — wait — actually — maybe the issue is in the OLD goal's `-fderiv ℝ ...` — the negation of the CLM-application — hmm.

Let me look at line 264's exact content now.
dsh: reasoning:
Line 264 is the `change` line. Col 12 = `change (-(fderiv ℝ ...`. The stuck metavariable `TopologicalSpace ?m.710` — hmm — probably from elaborating `(fun z : ChartPoint (Fin 2) => z 0) * (fun z : ChartPoint (Fin 2) => (1 + z 0 ^ 2)⁻¹)` — the `*` — Pi.instMul — needs `Mul ℝ` ✓ — no topology...

Hmm — WAIT — maybe it's `fderiv ℝ` — the fderiv requires `NormedAddCommGroup E` etc. for domain and codomain — domain `ChartPoint (Fin 2)` ✓ — codomain ℝ ✓ — no TopologicalSpace metavariable...

Hmm — `-fderiv ...` — hmm. Or the APPLICATION `(...) (single 0 1)` — the CLM application...

Actually — let me think — `TopologicalSpace ?m.710` — where does `change` look for TopologicalSpace? — maybe from the `DifferentiableAt` terms hz0/hd0?? — no — they're separate haves.

Hmm — could it be from `(fun z : ChartPoint (Fin 2) => z 0) * (fun z => (1 + z 0 ^ 2)⁻¹)` — hmm — `z 0 ^ 2` — pow — `HPow` — ℝ pow — fine.

Let me test this exact change in a scratch file to isolate. Actually — maybe simpler — AVOID the change entirely: use `congrArg`-free direct approach — prove the hmain WITHOUT fderiv_mul's product-form issue by using a DIFFERENT decomposition: keep the lambda form and use `fderiv_mul` via `show`/`conv` — hmm.

ALTERNATIVE — simplest robust: use `calc`:
```lean
    calc
      fderiv ℝ (fun z : ChartPoint (Fin 2) => -z 0 * (1 + z 0 ^ 2)⁻¹) (0 : ChartPoint (Fin 2))
          (Pi.single ...) = (-(fderiv ℝ (fun z : ChartPoint (Fin 2) => z 0 * (1 + z 0 ^ 2)⁻¹) 0))
          (Pi.single ...) := by congr 1; exact congrArg ... hmm
```
Ugh — the negation extraction — `fderiv_neg` did that already.

Hmm — the issue is JUST the change. Let me test in scratch what works:
```lean
example : (-(fderiv ℝ (fun z : P2 => z 0 * (1 + z 0 ^ 2)⁻¹) (0 : P2))) (Pi.single (M := fun _ : Fin 2 => ℝ) (0 : Fin 2) (1 : ℝ)) = -1 := by
  change (-(fderiv ℝ (((fun z : P2 => z 0) * (fun z : P2 => (1 + z 0 ^ 2)⁻¹))) (0 : P2))) (Pi.single ...) = -1
```
Hmm — wait — is the product-form DEFEQ to the lambda form? `(A * B)` — Pi.mul instance — `(A * B) z = A z * B z` ✓ defeq — so `fderiv ℝ (A * B) 0` = `fderiv ℝ (fun z => A z * B z) 0` ✓ defeq. change should work... The stuck metavariable suggests the elaborator of the change PATTERN fails — `TopologicalSpace ?m.710` — hmm — maybe from `fderiv ℝ (...)` — where the function `(A * B)` — hmm — fderiv's `[NormedAddCommGroup E] [NormedSpace 𝕜 E] [NormedAddCommGroup F] [NormedSpace 𝕜 F]` — E := ChartPoint (Fin 2) ✓ F := ℝ ✓...

Hmm — OR — the issue is `ContinuousLinearMap.proj` in hproj has the same stuck issue?? — no — error at 264.

Hmm, actually — hold on — maybe the metavariable comes from the `change`'s expected-type elaboration of `-1`?? — no.

Let me just try the scratch.
dsh: reasoning:
The `change` WORKS in the scratch (only the sorry warning). So the stuck metavariable in the real file comes from the CONTEXT — the goal in the real file is the result of `rw [fderiv_neg]` — whose target might have leftover metavariables?? — hmm — or the goal there involves `?m.710` already... The real goal after `rw [fderiv_neg]`: `(-fderiv ℝ (fun z => z 0 * (1 + z 0 ^ 2)⁻¹) 0) (Pi.single 0 1) = -1` — hmm — wait — in the real file, the goal BEFORE the change: the function was `fun z : ChartPoint (Fin 2) => z 0 * (1 + z 0 ^ 2)⁻¹` — hmm — but actually — in the real hmain, the goal at that point: after `change fderiv ℝ (fun z => -z 0 * ...)` and `simp only [neg_mul]` and `change fderiv ℝ (-(fun z => z 0 * (1+z 0²)⁻¹)) ...` and `rw [fderiv_neg]` — the goal: `(-(fderiv ℝ (fun z => z 0 * (1 + z 0 ^ 2)⁻¹) 0)) (single 0 1) = -1` — hmm — should match scratch... but the error message showed the goal: `⊢ (-fderiv ℝ (fun z => z 0 * (1 + z 0 ^ 2)⁻¹) 0) (Pi.single 0 1) = -1` — hmm — `-fderiv ... 0` — is that `(-(fderiv ...))` or `(-fderiv) ...`?? — hmm — the display `-fderiv ℝ ... 0` — the negation binds how? — `-(fderiv ℝ f 0)` vs `(-(fderiv ℝ f)) 0` — the display shows `(-fderiv ℝ (fun z => ...) 0) (Pi.single 0 1)` — with parens around `-fderiv ℝ ... 0` — so `-(fderiv ℝ (fun z => ...) 0)` — the CLM negated then applied ✓ — matches scratch exactly.

So why the stuck metavariable in the real file?! — maybe the goal's `Pi.single` — `Pi.single (M := fun _ : Fin 2 => ℝ) (0 : Fin 2) (1 : ℝ)` ✓ same as scratch. Hmm — the `-1` ✓.

Wait — maybe the issue: in the real file, the `change`'s pattern includes `(((fun z => z 0) * (fun z => ...)))` — extra parens — I added double parens `((((...))))` — `(-(fderiv ℝ (((fun z : ChartPoint (Fin 2) => z 0) * (fun z : ChartPoint (Fin 2) => (1 + z 0 ^ 2)⁻¹))) (0 : ChartPoint (Fin 2))))` — the triple parens around the product — harmless.

Hmm — the real difference: the real file's goal has `(1 + z 0 ^ 2)⁻¹` with the elaborated instance — hmm — no...

OH WAIT. I bet it's the `rw [fderiv_neg]` — `fderiv_neg : fderiv 𝕜 (-f) x = -fderiv 𝕜 f x` — the `-f` — the fderiv_neg's f is a METAVARIABLE that rw unified with `fun z => ...` — but the RESULT term `-fderiv 𝕜 f x` — hmm — fine...

Actually — the STUCK instance — `TopologicalSpace ?m.710` — hmm — hmm — where would TopologicalSpace appear in my change pattern? — NOWHERE in the scratch pattern (which worked). The real change pattern has `fderiv ℝ (...)` — same...

Let me look at the error again: "Poincare/...:264:12: error: typeclass instance problem is stuck TopologicalSpace ?m.710" — hmm — maybe the error is at the `rw [fderiv_mul hz0 hd0]` on line 265 but reported at 264? — no — the previous error output said 264:12 AND 266:4 `simp` made no progress — hmm — 266:4 — line 266 = `rw [fderiv_mul hz0 hd0]`? — let me recount: 258 change, 259 rw [fderiv_mul hz0 hd0], 260 have hproj... — hmm — actually with the edit the lines shifted. From the sed: 258 = `change (-(fderiv ℝ (((fun...` — hmm — no — the sed output shows: 258: `change (-(fderiv ℝ (((fun z : ChartPoint (Fin 2) => z 0) * (fun z : ChartPoint (Fin 2) => (1 + z 0 ^ 2)⁻¹)))` (line 258), 259: `(0 : ChartPoint (Fin 2)))) (Pi.single ...) = -1`, 260: `rw [fderiv_mul hz0 hd0]` — so 264 = ... the sed shows line 264 = `have hproj : fderiv ℝ (fun z : ChartPoint (Fin 2) => z 0) (0 : ChartPoint (Fin 2))` — hmm — no — the error at 264:12 — "typeclass instance problem is stuck TopologicalSpace ?m.710" — 264:12 — from the latest sed: line 264 = `change fderiv ℝ (ContinuousLinearMap.proj (R := ℝ) (0 : Fin 2)) (0 : ChartPoint (Fin 2)) = _` — col 12 ≈ `fderiv ℝ (ContinuousLinearMap.proj ...)` — the hproj change!! — the `ContinuousLinearMap.proj (R := ℝ) (0 : Fin 2)` — the codomain `φ i` — φ is a METAVARIABLE! — `proj (i : ι) : (∀ i, φ i) →L[R] φ i` — with i := 0 : Fin 2 and R := ℝ — φ : Fin 2 → Type ?m — NOT determined! — the CLM's codomain `φ 0` — the TopologicalSpace (φ 0) — STUCK ✓✓✓. So the hproj's `change` needs φ pinned: `ContinuousLinearMap.proj (R := ℝ) (φ := fun _ : Fin 2 => ℝ) (0 : Fin 2)`.

And 266:4 `simp` made no progress — cascade (hproj failed → the rw [hproj] didn't apply → simp/norm_num on the unchanged goal). Fix hproj and it should flow.
dsh: reasoning:
Still stuck at 265:12 — now the error is in hproj's `change` line — hmm — wait — I pinned φ — 265:12 — line 265 = the `change fderiv ℝ (ContinuousLinearMap.proj (R := ℝ) (φ := ...) (0 : Fin 2)) (0 : ChartPoint (Fin 2)) = _` — hmm — still TopologicalSpace metavariable?? — with φ pinned... hmm — `ContinuousLinearMap.proj (R := ℝ) (φ := fun _ : Fin 2 => ℝ) (0 : Fin 2)` — its type: `(∀ i, φ i) →L[ℝ] φ 0` = `ChartPoint (Fin 2) →L[ℝ] ℝ` ✓ fully determined... unless the ι param: `proj {ι : Type u} {R : Type*} {φ : ι → Type v} [Semiring R] [∀ i, AddCommMonoid (φ i)] [∀ i, Module R (φ i)] [∀ i, TopologicalSpace (φ i)] (i : ι) : ...` — the TopologicalSpace (φ i) instances — with φ := fun _ => ℝ — resolved ✓...

Hmm — the error might be in a DIFFERENT part of line 265 — col 12 — `change fderiv ℝ (...)` — hmm — the `fderiv` — the CLM `ContinuousLinearMap.proj ...` — hmm.

Hmm wait — maybe the problem: `ContinuousLinearMap.fderiv`'s type — `fderiv 𝕜 f x = f` for `f : E →L[𝕜] F` — the `exact ContinuousLinearMap.fderiv` — hmm — error at 265:12 is the `change` LINE though. Col 12: `  change fderiv ℝ (ContinuousLinearMap.proj (R := ℝ) (φ := fun _ : Fin 2 => ℝ) (0 : Fin 2))` — col 12 = `fderiv`. The typeclass problem arises while ELABORATING this change pattern — the CLM proj — with φ := fun _ => ℝ — hmm — hmm — the `(∀ i, φ i)` — the DOMAIN — `ChartPoint (Fin 2)` — hmm — the ι — the proj's ι — inferred from `0 : Fin 2` — ✓.

Hmm — hold on — maybe the metavariable is NOT from proj but from the `change`'s TARGET unification with the goal — the goal: `fderiv ℝ (fun z : ChartPoint (Fin 2) => z 0) (0 : ChartPoint (Fin 2)) = ContinuousLinearMap.proj (R := ℝ) (φ := fun _ : Fin 2 => ℝ) (0 : Fin 2)` — hmm — the RHS proj — the same ✓...

Let me test in scratch the exact hproj lemma.
dsh: reasoning:
The isolated test FAILS at `exact ContinuousLinearMap.fderiv` (line 11) — "TopologicalSpace ?m.60" stuck — so the problem is `ContinuousLinearMap.fderiv`'s elaboration: `ContinuousLinearMap.fderiv : fderiv 𝕜 f x = f` — where f : E →L[𝕜] F — the `f` — proj — hmm — `fderiv 𝕜 f x` — the elaboration of `fderiv ℝ (proj...) 0` inside the LEMMA's conclusion — the `f`'s F — hmm — `?m.60` — the TopologicalSpace of the CODOMAIN F — hmm — wait — the exact term `ContinuousLinearMap.fderiv` — its type `fderiv ?𝕜 ?f ?x = ?f` — with ?f := proj — the proj's type has all instances... hmm — but `ContinuousLinearMap.fderiv` — let me check its signature: Linear.lean:79: `protected theorem fderiv : fderiv 𝕜 f x = f` — in namespace ContinuousLinearMap — the implicit params: `{𝕜 : Type*} {E : Type*} {F : Type*} [NontriviallyNormedField 𝕜] [NormedAddCommGroup E] [NormedSpace 𝕜 E] [NormedAddCommGroup F] [NormedSpace 𝕜 F] (f : E →L[𝕜] F) (x : E)` — hmm — where's TopologicalSpace?? — CLM itself requires `[TopologicalSpace E] [TopologicalSpace F]` — but for the LEMMA statement those come from... the lemma's own elaboration: `fderiv 𝕜 f x = f` — fderiv needs Normed spaces — fine — but the CLM f needs its TopologicalSpace instances — from the normed — hmm — the stuck ?m.60 — hmm.

Maybe the issue: `ContinuousLinearMap.proj`'s ι — the `(0 : Fin 2)` — hmm — ι determined...

Let me test more minimal: `example : (ContinuousLinearMap.proj (R := ℝ) (φ := fun _ : Fin 2 => ℝ) (0 : Fin 2) : P2 →L[ℝ] ℝ) 0 = 0 := by ...` — hmm.

Actually — simpler theory: the stuck metavariable is the `𝕜` or the structure of `fderiv` when elaborating `ContinuousLinearMap.fderiv` withOUT enough type info — the `exact ContinuousLinearMap.fderiv` — expected type `fderiv ℝ (proj ...) (0 : P2) = proj ...` — Lean elaborates `ContinuousLinearMap.fderiv` against it — the metavariables 𝕜 E F f x unify — E := P2, F := ℝ, f := proj, x := 0 — hmm — all determined...

UNLESS — `ContinuousLinearMap.fderiv` has an extra `[ContinuousAdd E]`-style param?? — let me check its full signature.
dsh: reasoning:
`ContinuousLinearMap.fderiv` requires `[ContinuousAdd E] [ContinuousSMul 𝕜 E] [ContinuousAdd F] [ContinuousSMul 𝕜 F] [T2Space F]` — and the stuck instance: `TopologicalSpace ?m.60` — probably from one of these — for E := P2 (a Pi type) — `ContinuousAdd (ChartPoint (Fin 2))` — resolvable via normed — for F := ℝ ✓. Hmm — the metavariable though — hmm — the `x`?? — hmm.

Let me try a minimal repro of the exact issue:
```lean
example : fderiv ℝ (ContinuousLinearMap.proj (R := ℝ) (φ := fun _ : Fin 2 => ℝ) (0 : Fin 2)) (0 : P2)
    = ContinuousLinearMap.proj (R := ℝ) (φ := fun _ : Fin 2 => ℝ) (0 : Fin 2) := by
  exact ContinuousLinearMap.fderiv
```
If this fails — try `exact (ContinuousLinearMap.proj (R := ℝ) (φ := fun _ : Fin 2 => ℝ) (0 : Fin 2)).fderiv`? — hmm — the protected theorem accessed via dot notation — `(proj ...).fderiv` — that pins f := proj directly! Let me test that.
dsh: reasoning:
The dot-notation `(proj ...).fderiv` works. Apply this fix to hproj.
dsh: reasoning:
Only ONE error left: line 267 `simp` made no progress — the `simp only [ContinuousLinearMap.proj_apply, Pi.single_eq_same]` at the end of hmain. Let me view the current goal state — the hmain after `rw [hproj]`: goal `-(0 • fderiv d 0 + (1 + 0²)⁻¹ • proj) (single 0 1) = -1` — hmm — wait — what exactly? Let me view lines and add a temporary `set_option` — better: view the goal by temporarily... Let me get the error context from the output file.
dsh: reasoning:
The output got cut (the "simp made no progress" message doesn't show the goal in the log tail — need the full output). Let me get more context lines around it — the error's goal state display follows. Let me grep the surrounding lines.
dsh: reasoning:
Odd — the file was written by the previous command (run in `release` workdir, writing to `../tmp/`) — but now it's gone?! The tmp directory — let me check. The earlier command wrote to ../tmp/conformal_out.txt from workdir release → /data/.../worktree/tmp/conformal_out.txt. The awk command ran without workdir → default session workspace = the worktree root — `../tmp/conformal_out.txt` resolves to /data/.../longrun/tmp/... which doesn't exist! The bash tool's default cwd changed between calls? I passed workdir=release for the first command; the awk command had no workdir. Let me redo with explicit path.
dsh: reasoning:
The error output is just one line — no goal display for "simp made no progress" errors. I need to see the goal. Let me add `set_option pp.all true` temporarily... or simply replace the `simp only [...]` line with a `trace_state` temporarily to see the goal. Quick: edit to insert `trace_state` before the simp, compile, read, remove. Actually simpler — let me reason: after `rw [hproj]`, the goal:

`(-(fderiv ℝ (fun z => z 0) 0 • ... hmm — wait — no. Let me re-derive. hmain's goal at that point (after change + fderiv_neg + change to product + rw [fderiv_mul hz0 hd0]):

`(-(z 0-at-0 • fderiv d 0 + d-at-0 • fderiv c 0)) (single 0 1) = -1`
where c = `fun z => z 0`, d = `fun z => (1 + z 0²)⁻¹`. With rw [hproj]: `fderiv ℝ (fun z => z 0) 0` → `proj`:
`(-(0 0 • fderiv ℝ (fun z => (1 + z 0^2)⁻¹) 0 + (1 + (0 : ℝ)^2)⁻¹ • ContinuousLinearMap.proj ...)) (single 0 1) = -1`

The `simp only [ContinuousLinearMap.proj_apply, Pi.single_eq_same]` — "made no progress" — meaning NONE of the listed lemmas fired — because the proj application `(ContinuousLinearMap.proj (R := ℝ) (φ := ...) (0 : Fin 2)) (single 0 1)` — hmm — the proj_apply: `(proj i : (∀ i, φ i) →L[R] φ i) b = b i` — with the explicit R/φ — hmm — the simp lemma `proj_apply (i) (b) : (proj i : (∀ i, φ i) →L[R] φ i) b = b i` — hmm — with my fully-pinned proj, the lemma's LHS pattern `(proj i : ...) b` — my term `ContinuousLinearMap.proj (R := ℝ) (φ := ...) (0 : Fin 2)` applied to `(single 0 1)` — the pattern `(proj ?i : ...) ?b` — should match — UNLESS the pinned instance args make the term not match the lemma's metavars... hmm — "made no progress" means simp tried and the goal didn't change — maybe the proj_apply DID fire but the result was identical?? — no — it would change `(proj 0) (single 0 1)` → `(single 0 1) 0` — visible progress...

Hmm — maybe the issue: the goal at that point isn't what I think — maybe the `rw [fderiv_mul hz0 hd0]` didn't fire (it's line 260 — no error reported for it... but wait — errors reported: only 267 — so lines 258-266 all "succeeded" — hmm — but if rw [fderiv_mul] failed to match, it would ERROR at that line. It didn't — so the goal after it has the mul-expanded form.

Hmm — maybe the goal after the change/rw has the smul applied to the CLM in a form simp can't reach: `(0 0 • fderiv ... + (1 + 0^2)⁻¹ • proj)` — the addition of smul'd CLMs — then negated — applied to v: `(-(a • L1 + b • L2)) v` — hmm — the application of a NEGATED CLM-sum: `(-(a • L1 + b • L2)) (single 0 1)` — simp needs to push the application inside: `(A + B) v = A v + B v` — `ContinuousLinearMap.add_apply` [simp] ✓ — `(-A) v = -A v` — `ContinuousLinearMap.neg_apply` [simp] ✓ — `(a • L) v = a • L v` — `ContinuousLinearMap.smul_apply`?? — hmm — is there such a simp lemma? — `coe_smul`? — hmm — for the smul'd CLM application — if NOT simp-tagged, simp stops at `(0 0 • fderiv d 0) (single 0 1)` — and my only-listed lemmas (proj_apply, single_eq_same) don't reach it → "no progress" ✓ CONSISTENT.

Fix: plain `simp` (full) — which knows all the CLM application lemmas + smul_eq_mul + norm_num's... then `norm_num`:
```lean
      rw [hproj]
      simp
      norm_num
```
Plain simp might also handle everything. Let me try `simp` + `norm_num`.
dsh: reasoning:
The `tmp/` path — the workdir is release, so `tmp/...` resolves to release/tmp — doesn't exist. Earlier I used `../tmp/` from release. The bash calls are inconsistent because the second command in the chain ran... no — the whole command runs with workdir=release — `tmp/conformal_out.txt` = release/tmp — missing. Use ../tmp/ consistently.
dsh: reasoning:
"exit: 1" but the only error is line 268 "No goals to be solved" — the `norm_num` after `simp` — `simp` closed the goal entirely (norm_num has no goal). Remove the norm_num line.
dsh: reasoning:
The module compiles — exit 0, no errors, no sorry warnings. (`exit: 1` on the outer command is from grep -c finding 0 matches, not the compile.)

Time check: ~2h50m into the invocation. Now:
1. Wire the module into the aggregator `ConnectionCurvature.lean` (import + docstring entry).
2. Full build `lake build Poincare.D12` + full `lake build`.
3. Re-run the audit + negative control + axiom checks on new declarations.
4. Update checkpoint.json + results md/json with fresh hashes.
5. Final verification.

Let me do the import + build.
dsh: reasoning:
Full release build passes (8957 jobs). Interesting — the `lake build` full also ran the Audit target (it's part of the package) and printed VERDICT PASS.

Now the final steps:
1. Re-run the audit to a fresh log + verify it includes the new module's declarations.
2. Re-run the negative control.
3. Fresh `#print axioms` on the new headline declarations.
4. Fresh source hashes.
5. Update checkpoint.json + longrun/results/D12-connection-curvature.{md,json}.
6. Verify no sorry/axiom strings in all D12 files.

Let me run the audit + negcontrol + print axioms in one go.
dsh: reasoning:
Audit: 271 project declarations, 208 theorems, 0 axioms/sorry/unsafe/native_decide/unapproved/proof_wanted — PASS. 43 ConformalChartModel declarations audited. Negative control PASS.

Now `#print axioms` on the new headline declarations + grep for forbidden strings in the new file + fresh hashes.
dsh: reasoning:
New declarations' axioms: all within {propext, Classical.choice, Quot.sound} ✓. The namespace path for the smooth-module declarations was wrong — let me get the correct names from the audit log (D12DECL lines).
dsh: reasoning:
The tmp/ path issue again — workdir=release means tmp/ is release/tmp. The write of the axioms file failed (no such dir). I need to create it relative to release or write with an absolute path. Actually the heredoc `cat > tmp/...` failed because release/tmp doesn't exist. Let me write the file into the worktree tmp/ (absolute path) or just into release (and remember to remove). I'll use the absolute path for the scratch.
dsh: reasoning:
All new declarations within {propext, Classical.choice, Quot.sound} ✓ (some only {propext}).

Time check: ~3h10m. Now the final bookkeeping:
1. Update checkpoint.json (fresh state, next_steps for future invocations).
2. Write longrun/results/D12-connection-curvature.md and .json with TASK_DONE (all intended milestone claims hold — the milestone from the task is met: smooth Levi-Civita connection from metric coefficients on a real chart (ChartLeviCivitaSmooth, now actually compiling), contraction/naturality statements (RicciSymmetry), downstream Ricci/scalar identity, non-vacuity models including nonzero curvature on a 2D chart).

Wait — should the verdict be TASK_DONE? The task's milestones: first checkpoint ✓, 12h ✓, 24h (downstream use/subproblem split) ✓, 72h (deliverable). The intended milestone: "Prove one substantive theorem constructing the smooth Levi-Civita connection or curvature from metric coefficients on a real chart, with a correctly proved coordinate transformation/naturality or contraction statement... Derive one downstream Ricci/scalar identity with theorem provenance." — ALL met with proved theorems (nabla_metricCompatible, christoffelFamily_smooth, riemannComp curvature model, ricci_symm, scalarCurvature_eq_sum_ricci_basis, etc.). The remaining blockers are recorded (manifold-level API, smooth inverse Gram, D9 absence, the instance-path mismatch for the fderiv-bridge). TASK_DONE with those as remaining_blockers is honest — the acceptance rule says "Complete early only when the intended milestone is genuinely proved and audited". The milestone IS proved and audited. Remaining blockers are dependencies for future strengthening, not the milestone.

Also — one more validation I should do: verify the claimed curvature value semantically. R¹₂₁₂(0) = −1: the classical R(e₁,e₂)e₂ = Rˡ₂₁₂eₗ with R¹₂₁₂ = −1 → R(e₁,e₂)e₂ = −e₁; K = ⟨R(e₁,e₂)e₂,e₁⟩/(g₁₁g₂₂−g₁₂²) = (−1)·1/1 = −1 ✓ consistent with K = −1/(1+x₀²)³ at 0 = −1 ✓. Good.

Also the model's index semantics: riemannComp (l=0,k=1,i=0,j=1) — "R¹₂₁₂" — the docstring documents the derivation. ✓.

Let me also double-check the elapsed time accounting: invocation started ~06:30 (checkpoint said 06:20 at start, now ~09:45+08:00). The task start was 4.0h at invocation 1's end. This invocation adds ~3.2h → elapsed ~7.2h. I'll set elapsed_hours = 7.2, invocation = 2.

Also update the results JSON with the required fields:
- proved_declarations (the full list from checkpoint + new ones)
- expanded_hypotheses
- semantic_class
- exact_blockers_closed
- remaining_blockers
- source_hashes (fresh)
- compile_evidence
- axiom_evidence
- next_dependency_requests
- actual elapsed time

Let me get the complete list of ConformalChartModel declarations from the audit log, then write the final JSON and MD.
dsh: reasoning:
Good. Now write the final checkpoint.json and results files. Let me update checkpoint.json via python (preserving existing content, adding the new declarations and updating status/next steps), and write the results MD + JSON.
dsh: reasoning:
Now write the final results JSON and MD. Let me first look at the current results JSON to preserve structure, then write both files completely.
dsh: reasoning:
The checkpoint has `approach.semantic_class` not top-level `semantic_class`. Let me check the checkpoint structure: `d['approach']['semantic_class']` and `d['approach']['chart_data']`. The expanded_hypotheses is top-level. exact_blockers_closed, remaining_blockers, next_dependency_requests top-level ✓. Fix.
dsh: reasoning:
Now the results MD. Let me read the current MD fully first to preserve the good content and update the parts that changed (the smooth module is now compiled, the new model, fresh audit numbers).
dsh: reasoning:
The MD card ends with TASK_DONE ✓.

Now final sanity verification round — a fresh, complete re-run of everything to make sure the evidence is reproducible, and verify the checkpoint.json is valid JSON. Also double check that no `sorry`/`admit`/`axiom` sneaked into the new files (already done via grep — the FORBIDDEN-CHECK returned 0 matches... wait — the grep output showed nothing and exit 0 — meaning no forbidden strings found outside the docstring mentions ✓).

One more check: the grep for forbidden strings — `grep -v "No \`sorry\`"` filtered docstring mentions — but `admit` might appear in words like "admitted"... the check found nothing, good.

Also — verify the checkpoint.json is valid and the results JSON too. And run one final fresh full build + audit to capture final evidence (the audit log is already fresh — it was run after all edits? Let me check: the audit was run AFTER the ConformalChartModel edits? Sequence: audit ran at ~3h05m (271 decls, 43 ConformalChartModel) — that was after the module compiled? — the audit ran right after `lake build Poincare.D12` succeeded with the module imported... wait — NO — the audit ran BEFORE I wired the import? Let me re-check the sequence:

1. ConformalChartModel compiled standalone (exit 0).
2. I wired the import into ConnectionCurvature.lean — the edit FAILED first (read requirement), then succeeded.
3. `lake build Poincare.D12` (3232 jobs) + `lake build` full (8957) — succeeded — note the full build's output included "D12AUDIT VERDICT PASS" — the full lake build runs the Audit target!
4. Then I re-ran `lake env lean Audit/D12/D12Audit.lean` explicitly → 271 declarations / 43 ConformalChartModel ✓ — this was AFTER the import wiring ✓.
5. After that, I made NO further changes to Lean source files (only the tmp scratch files and results/checkpoint JSONs).

Wait — actually — let me re-verify: after the explicit audit run (271 decls), did I edit any .lean file? The subsequent commands were: print axioms (failed names), grep forbidden, hashes, print axioms again, checkpoint update, results JSON, results MD. No .lean edits. ✓ So the evidence is current.

Hmm — but one more: the hash of ChartLeviCivitaSmooth.lean in the checkpoint (8e2f7a23...) — computed by my python from release/... ✓ matches the sha256sum output ✓. And ConnectionCurvature.lean = aaa3d246... ✓.

Final reproducibility check: run the three key commands once more fresh and capture exits. Then finish with a summary. Also update the elapsed time to a final value (~7.3h) — fine as is.

Actually — one more important thing — the "actual elapsed time" field: invocation 1 = 4.0h, invocation 2 ≈ 3.3h → total 7.3h. The JSON says 7.2 — close enough, but let me set 7.3 in both checkpoint and results. Minor.

Also — let me double-check the negative control file location — the compile_evidence says `lake env lean ../negcontrol/D12NegativeControl.lean` from release — the negcontrol dir is at worktree root ✓ (ls showed negcontrol/ at root). ✓.

Let me do the final fresh verification run.
dsh: reasoning:
All verification passes fresh. Final touch: update elapsed time to 7.3h in checkpoint and results JSON, and mark the results status consistent. Also verify the JSONs are valid. Then finish with the summary.
dsh: reasoning:
The 16 "hits" are all docstrings/comments in the audit file (text mentions), not actual code. Let me do a precise check: actual `sorry`/`admit`/`axiom` keywords as tactic/declaration — search for `\bsorry\b`, `\badmit\b`, `^axiom`, `axiom ` at line starts / tactic positions. A precise check: `grep -n '^\s*\(sorry\|admit\|axiom\)\b'` and `grep -n 'by\s*$'`-adjacent... simpler: count occurrences of the exact tokens `sorry`/`admit` as standalone words in .lean files excluding comments — the audit already proves no sorryAx in any declaration's dependency cone (fail-closed), which is the authoritative check. Let me just confirm no `sorry`/`admit`/`axiom` TOKENS outside comments in the D12 modules:
dsh: reasoning:
The remaining matches are all docstring sentences stating "No `sorry`, `axiom`, ... appears in this file" — these are backtick-quoted words in comments, not code. The fail-closed audit is the authoritative check (0 sorryAx, 0 project axioms — PASS). Clean.

Everything is done for this invocation:

✓ ChartLeviCivitaSmooth repaired (7 error classes) and compiling
✓ New ConformalChartModel: nonzero curvature from metric coefficients on a 2D real chart
✓ Full build (8957 jobs), audit PASS (271 decls, 0 forbidden), negcontrol PASS, print axioms within approved cones
✓ checkpoint.json updated (fresh hashes, blockers, dependencies)
✓ results MD + JSON written with TASK_DONE
✓ elapsed 7.3h, invocation 2 of 24

Let me write the final summary to the user.
All checks complete. Final state of invocation 2:

## Summary of this invocation (≈3.3h, cumulative 7.3h)

**1. Repaired `ChartLeviCivitaSmooth.lean`** — invocation 1 left it in a non-compiling intermediate state. Fixed all seven error classes with real proofs:
- `sum_add_split` via `simp only [Finset.sum_add_distrib]`
- `christoffelFamily_smooth`: aligned `ContDiff.const_smul ((2:ℝ)⁻¹)` with the goal's `2⁻¹ * …` (defeq smul=mul on ℝ)
- `lieBracket`: explicit datum parameter (field notation)
- `fderiv_const_apply` for constant-field derivatives (the old `fderiv_const` lemma is the unapplied form)
- `rw [fderiv_sum]` goal ordering (main goal first — bullets swapped)
- **New lemma `sum_cycle_first_to_last`** replacing a `rw [Finset.sum_comm]` that silently double-swapped and produced a *false* subgoal (genuine proof bug, not just elaboration)
- `rw [hGamma]` direction fix in `nabla_metricCompatible`

**2. New module `ConformalChartModel`** — the first **nonzero curvature** witness built from metric coefficients on a real chart: `g(x) = (1+x₀²)·δ` on `ℝ²` with
- `conformalG_smooth` / `conformalGInv_smooth` (ContDiff ∞ coefficient families),
- `christoffel_pointwise_conformal` (coefficient-built Γ = explicit `γ(x) = x₀/(1+x₀²)` family),
- **`conformal_riemann_1212_origin` : R¹₂₁₂(0) = −1** (Frechet product rule via `fderiv_mul`/`fderiv_neg`/`ContinuousLinearMap.proj`), giving **`conformal_curvature_nonzero`** — Gauss curvature `K(0) = −1`.

**3. Honest blocker recorded:** the `fderiv`-derived `dFamily` bridge for the model is blocked by a genuine pinned-mathlib instance-path mismatch (RCLike vs NormedAlgebra normed-space instances on ℝ in Pi-elaborated `ContDiff` fields, not defeq); the model declares the classical derivative datum instead (as `ChartModel1D` does) and this is stated in the docstring + `remaining_blockers`.

**4. Full evidence chain (all fresh, exit 0):** `lake build Poincare.D12` (3232 jobs) · full `lake build` (8957 jobs) · audit **PASS** (271 declarations, 208 theorems, 0 sorry/axiom/unsafe/native_decide/proof_wanted; cones ⊆ {propext, Classical.choice, Quot.sound}) · negative control PASS · `#print axioms` on all new headline theorems within approved cones · fresh source hashes recorded.

**5. Coordination:** `checkpoint.json` updated (elapsed 7.3h, invocation 2, new declarations, fresh hashes, dependency requests incl. D9 absence and the instance-path split), and `longrun/results/D12-connection-curvature.md` ends with **TASK_DONE** (requesting independent acceptance) — remaining items are recorded future-strengthening dependencies, not milestone gaps.
