dsh: reasoning:
Let me explore more. The checkpoint is basically empty. Let me read README.md, docs/UPSTREAM-INTEGRATION.md, longrun/DELIVERY.md, and look at third_party.
dsh: reasoning:
This worktree is based on the D6-weekly-release, but my task is D13-cross-audit-360-cards: independently re-verify 360-produced D12 result cards. Let me read docs/UPSTREAM-INTEGRATION.md, third_party/frenzymath/IMPORT.md, SOURCE.json, and explore the Poincare-Conjecture snapshot.

Also I need to find the D12 result cards (connection-curvature, volume-ibp, spectral-sobolev, comparison-geodesics, geometric-compactness, surgery-recognition, triangulation-topology, tensor-maximum-bochner, semantic-ledger). These might be in the pinned snapshot.
dsh: reasoning:
Now I need to find the D12 result cards produced by "360" (likely task 360 or agent named "360"). The cards are: connection-curvature, volume-ibp, spectral-sobolev, comparison-geodesics, geometric-compactness, surgery-recognition, triangulation-topology, tensor-maximum-bochner, semantic-ledger.

These may be in the upstream snapshot (PoincareConjecture/blueprint?) or they may exist elsewhere in the repository (parent worktree / longrun directory). Let me search the broader repo for D12 result cards. The task says "Independently re-verify the 360-produced D12 result cards ... rebuild from source hashes, re-run axiom audits, confirm/refute exact_blockers_closed".

Let me look at other worktrees - maybe a D12 worktree exists. But we're told to work only in our isolated worktree. However, reading files from the parent repo (input) may be needed. Let me check what's available: maybe there's a sibling worktree with D12 results. Let me look at the broader longrun directory (read-only).

Let me first check git status and the queue to understand task D13.
dsh: reasoning:
There are sibling D12 worktrees. The task says "Independently re-verify the 360-produced D12 result cards". The "360" likely refers to worktree(s) that produced those. Let me check D12 worktrees for result cards, particularly D12-connection-curvature, D12-volume-ibp, D12-spectral-sobolev, D12-comparison-geodesics, D12-geometric-compactness, D12-surgery-recognition, and triangulation-topology (probably D10-triangulation-low-dim or D11?), tensor-maximum-bochner (D11-maximum-principle-tensor), semantic-ledger (D12-semantic-ledger).

Wait — the task says work only in my isolated worktree. But reading sibling worktrees is presumably acceptable for auditing (we need source hashes). Actually "Work only in your isolated worktree" means writing only there. Reading is fine, but is it allowed by sandbox? workspace-write means modifications are limited to the session workspace; reads might be allowed anywhere. Let's test reading a sibling worktree.

Also, I should check if there are copies of D12 result cards in my worktree's input. Let me search for the card file names.

Let me also check the queue and DELIVERY for the D13 task description.
dsh: reasoning:
Good. Sibling worktrees are readable. Now let me check the D12 cards in the sibling worktrees. The 9 cards:

1. connection-curvature → D12-connection-curvature
2. volume-ibp → D12-volume-ibp
3. spectral-sobolev → D12-spectral-sobolev
4. comparison-geodesics → D12-comparison-geodesics
5. geometric-compactness → D12-geometric-compactness
6. surgery-recognition → D12-surgery-recognition
7. triangulation-topology → ? (maybe D11? or D10-triangulation-low-dim). Let me search.
8. tensor-maximum-bochner → D11-maximum-principle-tensor or D11-bochner-manifold? "tensor-maximum-bochner" maybe a card name in one of these.
9. semantic-ledger → D12-semantic-ledger

Let me check the queue.json in the parent longrun dir and my worktree's queue.updated.json for the D13 task definition. The parent longrun/queue.json may not be readable... let me test.

Actually the queue.updated.json in my worktree is from D6. The task description in the prompt is the authoritative one. Let me look for "D13-cross-audit-360-cards" in the parent dir.

Let me first check what result cards exist in each D12 worktree.
dsh: reasoning:
The D12 worktrees each have one result card. "triangulation-topology" likely refers to D10-triangulation-low-dim; "tensor-maximum-bochner" to D11-maximum-principle-tensor. The task mentions 9 cards:
1. connection-curvature → D12-connection-curvature
2. volume-ibp → D12-volume-ibp
3. spectral-sobolev → D12-spectral-sobolev
4. comparison-geodesics → D12-comparison-geodesics
5. geometric-compactness → D12-geometric-compactness
6. surgery-recognition → D12-surgery-recognition
7. triangulation-topology → D10-triangulation-low-dim (card produced by 360? maybe)
8. tensor-maximum-bochner → D11-maximum-principle-tensor
9. semantic-ledger → D12-semantic-ledger

Hmm, "360-produced" might mean a group of worktrees numbered 360-something. Let me look for a queue.json or a task list in the parent longrun directory mentioning D13-cross-audit-360-cards. Let me check the parent queue.

Actually, maybe "360" refers to a batch id. Let me search for the string "360" in the parent longrun dir.

Let me also check whether these D12 worktrees are complete (checkpoint.json) and whether they have release/ packages that I can rebuild. My audit plan:
- For each card: read the card (.md/.json), collect source_hashes and release package, copy to my worktree (or reference in place), verify hashes, rebuild the Lean package (compile), re-run axiom audit with #print axioms, and confirm/refute exact_blockers_closed claims.

Key question: what Lean toolchain and mathlib are available locally? The D12 worktrees likely use Lean 4.33.0 and a pinned mathlib prebuild. Let me check what's available: `lake`, `lean` binaries, elan home.

Let me look at one D12 card to understand the structure.
dsh: reasoning:
Let me read the queue entry for D13-cross-audit-360-cards to understand exactly which cards are meant, and check the prompt file (which is probably the same as the task description). Let me get the queue entry.

Also let me look at the "360" designation. Maybe there's a directory of D12 "360" tasks. Let me grep the queue for "360".
dsh: reasoning:
Now let me find the list of D12 tasks with lane 360-1/360-2 to identify the 9 cards. Let me print all tasks with stage D12 and their lanes, and also cross-audit tasks.
dsh: reasoning:
Now I have the exact list of 9 D12 cards produced by lane 360-1/360-2:
1. D12-connection-curvature (360-1)
2. D12-volume-ibp (360-1)
3. D12-tensor-maximum-bochner (360-1)
4. D12-spectral-sobolev (360-1)
5. D12-semantic-ledger (360-1)
6. D12-comparison-geodesics (360-2)
7. D12-geometric-compactness (360-2)
8. D12-triangulation-topology (360-2)
9. D12-surgery-recognition (360-2)

That matches the task list exactly. Now, where are the worktrees? We saw D12-connection-curvature, D12-volume-ibp, D12-spectral-sobolev, D12-comparison-geodesics, D12-geometric-compactness, D12-surgery-recognition, D12-semantic-ledger. But not D12-tensor-maximum-bochner or D12-triangulation-topology. Let me check.

Interesting: the sibling worktree list included D11-maximum-principle-tensor (which contains cards from D10/D11), and D10-triangulation-low-dim. But the D12 objects might be elsewhere. Let me search for directories matching "*tensor-maximum*" and "*triangulation*".
dsh: reasoning:
D12-tensor-maximum-bochner and D12-triangulation-topology worktrees don't exist as separate dirs. But "remote_owned" status for those means the 360 host owns them (remote machine). Hmm, but the task says "360-produced D12 result cards". Perhaps the cards themselves are in the longrun/results directory of the parent (promoted), or in some sync location. Let me check:
- /data3/guoshaoyang/workdir/lean_poincare/longrun/results/ (parent promoted results)
- /data3/guoshaoyang/workdir/lean_poincare/longrun/state/D12-*/

Let me look.
dsh: reasoning:
The remote-owned 360 cards (connection-curvature, volume-ibp, tensor-maximum-bochner, comparison-geodesics, geometric-compactness, triangulation-topology, surgery-recognition) may be synced somewhere. Let me check:
- D12-plan.json in longrun
- integrated_overlay
- inbox
- decisions

Let me look for card files matching "D12-tensor-maximum-bochner" and "D12-triangulation-topology" anywhere under /data3/guoshaoyang/workdir/lean_poincare/longrun.
dsh: reasoning:
Now I understand the setup. I need to audit the 9 D12 cards from lanes 360-1/360-2. Two worktrees don't exist locally (D12-tensor-maximum-bochner, D12-triangulation-topology) — those tasks are "remote_owned" on host 360-1/360-2. But the prompt says "Independently re-verify the 360-produced D12 result cards". So maybe the remote host synced the worktrees under a different name or the cards are missing. Let me check all worktrees for those module names — maybe D12-tensor-maximum-bochner's release is inside D11-maximum-principle-tensor? Let me grep the release dirs for "TensorMaximumBochner".

Let me check the log dir /data3/guoshaoyang/workdir/lean_poincare/longrun/logs too.

Let me also check the hostname: I am on ophis-gpu? The prompt says host ophis-gpu for my task. The 360 lanes are remote machines; maybe their worktrees are synced via rsync to a shared path. Let's search the entire /data3/guoshaoyang/workdir/lean_poincare for "TensorMaximumBochner" (bounded depth to avoid huge scan).
dsh: reasoning:
Important: the sibling D12 worktrees are still "in_progress"/"awaiting_acceptance" — they may be still running or synced. My audit needs to be independent: rebuild from source hashes, re-run axiom audits, confirm/refute exact_blockers_closed.

Two cards (tensor-maximum-bochner, triangulation-topology) are not present locally. I need to determine whether their source packages exist anywhere. Since they are "remote_owned" on 360-1/360-2 hosts, maybe their worktrees are synced onto this host under a different name. Let me search more broadly for the module names in the whole poincare dir (bounded depth, exclude third_party huge lib).

Let me check:
- `/data3/guoshaoyang/workdir/lean_poincare/tasks/`
- `/data3/guoshaoyang/workdir/lean_poincare/external/`
- `/data3/guoshaoyang/workdir/lean_poincare/logs/`
- `longrun/control-*.tar.gz` — maybe these contain remote snapshots!

Let me check the tarballs and other paths.
dsh: reasoning:
The search timed out (huge tree). Let me be targeted. The missing cards are D12-tensor-maximum-bochner and D12-triangulation-topology, both remote_owned by 360-1/360-2. Since this host is ophis-gpu (zp-nc71), remote worktrees may not be synced. But the task explicitly lists 9 cards. Let me check the D13-cross-audit-ophis-cards task (the mirror) — it audits ophis cards from 360 host. And maybe there is a sync directory.

Let me check the supervisor log for my task and for the 360 tasks. Actually let me check if there's a mechanism where remote card results get pulled. Check `poincare_next_wave.py` or the dispatcher. Alternatively, check if the results of remote tasks are in `longrun/state/<task>`.

Let me look at the state dir for D12-semantic-ledger, D12-spectral-sobolev etc. And the supervisor logs list — no D12-connection-curvature log (remote). So remote tasks' logs are absent.

The key question: what artifacts can I audit? Available locally:
- D12-connection-curvature worktree (360-1) — yes! It has checkpoint + release.
- D12-volume-ibp — yes
- D12-spectral-sobolev — yes
- D12-semantic-ledger — yes
- D12-comparison-geodesics — yes
- D12-geometric-compactness — yes
- D12-surgery-recognition — yes
- D12-tensor-maximum-bochner — NOT found locally
- D12-triangulation-topology — NOT found locally

Wait, maybe the remote worktrees are synced into the same worktrees dir but named differently. Let me grep the state dir or check for directories modified around Sep 11. Let's list worktrees sorted by mtime.
dsh: reasoning:
The supervisor log is empty. So tasks that were assigned to host 360-1/360-2 were actually run locally on other machines and their worktrees were synced back (D12-connection-curvature mtime Sep 11 02:18 etc.). Since the whole fleet seems to run on the same filesystem paths, maybe there are separate "control" tarballs containing remote files. But D12-tensor-maximum-bochner and D12-triangulation-topology worktrees are missing.

Wait: maybe those two were assigned "remote_owned" and are still running on the remote hosts, or failed and never synced. Let me check the queue status: D12-tensor-maximum-bochner remote_owned, D12-triangulation-topology remote_owned — same as D12-connection-curvature which IS present locally. Hmm.

Let me search the filesystem with a targeted, fast scan: find directories at depth <= 3 under /data3/guoshaoyang/workdir/lean_poincare named *tensor-max* or *triangulation-top*, excluding our longrun/worktrees. Also check other roots: /data/home/guoshaoyang/workdir/... (the prompt's worktree path is a symlink?). Let me check both roots; maybe /data/home/... is a symlink to /data3/....

Also check for tarballs in longrun/state or elsewhere.

Actually, the prompt says "Worktree: /data/home/guoshaoyang/workdir/lean_poincare/longrun/worktrees/D13-cross-audit-360-cards". Let's see if /data/home/guoshaoyang is the same as /data3/guoshaoyang (probably a symlink or bind mount).
dsh: reasoning:
The two missing worktrees are not present. But maybe the D12 cards for tensor-maximum-bochner and triangulation-topology were landed in the release/ of other worktrees? Let me search the release dirs for a module named D12/TensorMaximumBochner.lean or similar. Quick targeted find at depth 4 on worktrees only, filename patterns.

Actually earlier I ran `grep -rl "TensorMaximumBochner" .../worktrees/*/release/` which returned nothing quickly (no output). So the D12 TensorMaximumBochner module isn't in any local release dir. Let me check `module` names in queue: D12-tensor-maximum-bochner module "TensorMaximumBochner", D12-triangulation-topology module "TriangulationTopology". Search for files with those names in worktrees (fast find by name).

Also the input directory of my worktree may contain a bundle of the cards to audit ("input/" with d5-manifest etc. from D6). Let me list my input dir.

Actually my worktree is a copy of D6 weekly release, and input/ contains D5 artifacts. Not the D12 cards.

Let me check the longrun/results in the parent: no D12 cards promoted yet.

Hmm — maybe the remote sync puts remote worktrees under a different root, e.g. /data3/guoshaoyang/workdir/lean_poincare/longrun/worktrees_360/. Let me list /data3/guoshaoyang/workdir/lean_poincare/longrun/ fully (top-level only) - done: bin, control*.tar.gz, d11-heat-input.tar.gz, D12-plan.json, decisions, dispatcher.lock, inbox, integrated_overlay, logs, poincare_*.py, prompts, prompts_v2, queue.json, README.md, results, state, undici6.tgz, worktrees.

Let me check `integrated_overlay` — it has longrun and release subdirs. Let me look inside.
dsh: reasoning:
The find command with -o didn't work well (the maxdepth applies only to first expression, and no results). Let me redo with proper grouping.

But wait: the real issue is that the two worktrees for tensor-maximum-bochner and triangulation-topology don't exist locally. They are "remote_owned". Yet my task says to audit those cards too. Since this task has "max_rounds 4" and 24h max but invocation limited to 4h, maybe some cards are simply not available, and I should audit what exists locally and document the missing ones as unverifiable (a genuine blocker for those two).

Actually wait. Maybe the remote 360 hosts have their worktrees in a different local path on the same shared filesystem, e.g. /data3/guoshaoyang/workdir/lean_poincare/longrun/worktrees is the ophis host's view; remote hosts may write to a shared mount under a different name. Let me check if there is an rsync/sync directory. Check the dispatcher script `poincare_next_wave.py` for paths.

Actually, let me check for other "root" directories: `ls /data3/guoshaoyang/workdir/` and `ls /data/home/guoshaoyang/workdir/lean_poincare/`.

Also check /data3/guoshaoyang for other lean_poincare dirs.

Let me look at the queue.json statuses: D12-tensor-maximum-bochner remote_owned, D12-triangulation-topology remote_owned. But D12-connection-curvature also remote_owned and present. So "remote_owned" doesn't mean not synced; it means the task is owned by a remote lane. The worktrees were created by the dispatcher for all tasks? Not necessarily — remote lanes create their own worktrees on the remote host, which might be a different filesystem, and then the results are copied back. But we see connection-curvature locally... maybe because these worktrees get created by the dispatcher in a shared directory.

Let me check timestamps: D12-connection-curvature mtime 02:18; its checkpoint updated_at 11:00 (Sep 11?), invocation 2 7.3h. Hmm, the checkpoint says updated_at 2026-09-11T11:00 but mtime of the dir is 02:18. That's strange. Let me check the mtimes of files inside: maybe the content was copied at 11:00 but dir mtime is off. Let me check the card file mtime.

Actually more useful: check which D12 worktrees have a complete release package and compile logs. And check the queue statuses: D12-connection-curvature remote_owned (not verified), D12-volume-ibp remote_owned, D12-tensor-maximum-bochner remote_owned, D12-spectral-sobolev verified, D12-semantic-ledger gate_failed, D12-comparison-geodesics remote_owned, D12-geometric-compactness remote_owned, D12-triangulation-topology remote_owned, D12-surgery-recognition remote_owned.

So "gate_failed" for semantic-ledger! And the others remote_owned.

Given the two missing cards, one plausible explanation: they're still running on remote hosts (not finished), so no artifacts. But the remote tasks D12-connection-curvature etc. finished and were synced.

Let me check the longrun/logs for the queue's "verified" tasks to see whether cards were promoted: `longrun/results/` only has up to D6. So the 360-produced cards are not in the promoted results dir; they live in the worktrees.

Now, my audit must be independent:
For each available card:
1. Read card .md/.json: source_hashes, proved_declarations, exact_blockers_closed, semantic_class.
2. Verify source hashes in the corresponding worktree release dir.
3. Rebuild the package in a *fresh* copy in my worktree (independent build dir), with the pinned toolchain/mathlib.
4. Re-run axiom audit: #print axioms of every audited declaration, check for forbidden axioms (sorryAx, native_decide, etc.), and forbidden syntax (sorry, admit, axiom, unsafe).
5. Semantically review the key claims and the exact_blockers_closed entries: check the blocker was a real named blocker, the constructor is real, downstream use exists.
6. Confirm/refute.

For missing cards (tensor-maximum-bochner, triangulation-topology), attempt to locate artifacts; if absent, record as "not auditable / refuted as claimed present" — no wait, the task says re-verify the 9 cards. If sources are absent, my finding is "card source unavailable in this workspace; claim unverified" and that is a blocker for those two. But maybe I should search harder: maybe there is a tarball from remote lanes, or the remote cards were imported into some other worktree. Let me check the D13-cross-audit-ophis-cards task's prompt - since it audits ophis cards by 360-1, maybe the workflow is symmetric; maybe there's an input bundle.

Let me also check /data3/guoshaoyang/workdir/lean_poincare/longrun/prompts/D13-cross-audit-360-cards.md (probably same prompt).

Let me look at the state dir for D12-spectral-sobolev and D12-semantic-ledger (ophis tasks' state). For remote tasks there's no state dir (D12-connection-curvature isn't in state list). OK.

So the missing two worktrees: let me search whole /data3/guoshaoyang for directories named "*tensor-maximum*" quickly with maxdepth 6.

Actually, hold on: maybe the remote results were placed in worktrees named by task ID but with module name "D12-tensor-maximum-bochner" — we listed /worktrees and there is no such dir. Confirmed via ls -d earlier.

Maybe the tasks are at a *different* stage naming: D11-maximum-principle-tensor (which produced D11 card) and D10-triangulation-low-dim. The D12 cards for these might be *updates* that were intended but not done. The queue says remote_owned, meaning the remote lane owns them; the remote lane may have failed or is still working. But the checkpoint.json updates in D12-connection-curvature show invocation 2 complete. Hmm.

Let me check the supervisor logs for D12 tasks on this host — only ophis-host tasks have logs. For remote tasks, maybe there's a `logs/<task>.log` in the worktree's own logs/ dir. Let's check D12-connection-curvature/logs/.

Also, let me check the `.supervisor.log` for D12-comparison-geodesics (present locally?) — no, the ls of logs didn't show D12-comparison-geodesics.supervisor.log. So the worktrees present locally for remote tasks were synced, but their supervisor logs are absent.

OK here's my plan: audit the 7 available cards; for the 2 missing, do a thorough search and document absence; treat them as "unverifiable/absent" and record a blocker. That's an honest audit result: 7/9 verifiable, 2/9 source-absent.

Wait — maybe D12-tensor-maximum-bochner and D12-triangulation-topology *were* produced but as D11/D10 modules. Let me check whether there are any D12 modules in the D11-maximum-principle-tensor and D10-triangulation-low-dim worktrees (e.g. release/Poincare/D12/...). Let me check.

Let me now proceed systematically. First, set up the environment: Lean toolchain. Check elan, lake availability, the pinned mathlib prebuild.

Let me check the available toolchains and the D12 release lakefiles.
dsh: reasoning:
Elan has no default toolchain but likely has v4.34.0-rc2 installed. Let me check `elan toolchain list`. And check the lakefile of D12-connection-curvature: toolchain 4.34.0-rc2 (different from upstream v4.32.1). The task says read the pinned Frenzymath snapshot and reuse upstream packages instead of recreating foundations. But building upstream requires v4.32.1 which may not be installed. Let me check available toolchains and mathlib caches.

Let me check the elan toolchains dir, and .lake dirs in the D12 worktrees (mathlib prebuild symlinks).
dsh: reasoning:
Good: v4.34.0-rc2 toolchain is installed, mathlib pinned at 7974e751 with a shared prebuilt .lake/packages. So rebuilding local D12 packages should be feasible.

Now let me plan the audit.

Time budget: 4 hours. Let me be efficient. Steps:
1. Snapshot/hash inventory: for each of the 9 cards, locate card file and worktree. For the 7 present, record card hashes and source hashes.
2. Create an audit workspace in my worktree: `audit360/` with a fresh copy of each card's release package (source only, no .lake build), plus a per-card audit driver that:
   - verifies source hashes against card's source_hashes
   - builds with lake (using symlinked shared packages)
   - runs `#print axioms` for every declaration in proved_declarations (generated probe), checking allowed axioms
   - greps for forbidden constructs (sorry, admit, axiom, unsafe, native_decide, proof_wanted)
   - compiles an audit module
3. Semantic checks: read card claims and adversarially inspect the Lean statements for the headline theorems and exact_blockers_closed: check statement strength (e.g., vacuity, hypothesis smuggling, conclusion weakening), check the "closed blocker" is genuinely closed (constructor exists, not an alias), and check downstream use is real.
4. Produce results .md/.json + checkpoint updates every 30 min.

Let me be careful about disk/time: compiling mathlib-dependent packages can be slow, but mathlib prebuild is shared (oleans), so building a small D12 package should be quick-ish. Also, I can share the `.lake/packages` symlink.

Critical decision: the cards' release packages may be large. For independent verification, I'll copy the *sources* into my worktree and build there with a fresh .lake build dir but shared packages (that's what the D6 verification did). That gives independent compile evidence.

Let me first check the size of each worktree release and how the D12 packages are structured. Also check if there is an existing audit script in the D12 worktrees (e.g., Audit/D12/D12Audit.lean) which I can reuse for the axiom audit — but I must re-run it independently, and the audit module itself could be wrong, so I should generate my own probe from the card's proved_declarations.

Let's look at one D12 release structure in detail and check compile logs.
dsh: reasoning:
Interesting: D12-volume-ibp source_hashes use relative paths like "Audit.lean", "Basic.lean" — these look like hashes within `release/Poincare/D12/VolumeIBP/`? Let me examine the release tree for D12-volume-ibp.

Wait, the source_hashes keys are odd — "Audit.lean", "Basic.lean", "Blocked.lean", "ChangeOfVariables.lean", "Compat.lean", "Divergence.lean", "Example.lean", "IBP.lean", "Regularity.lean", plus "audit.py" and "ProbeScratch.lean". Those are probably files under release/Poincare/D12/VolumeIBP/ — with keys being basenames (ambiguous). This is a hash-provenance weakness I should note. Let me verify by computing hashes.

Let me look at the release layout for D12-volume-ibp.
dsh: reasoning:
Note the source_hashes omit `Poincare/D12.lean`, `Poincare/D12/VolumeIBP.lean`. The relative "Audit.lean" is `Poincare/D12/VolumeIBP/Audit.lean`. So hashes are keyed by basename — I can verify by matching content hashes. This is a provenance weakness (ambiguous key names) worth reporting.

Let me now:
1. Build an inventory script that, for each of the 7 available cards, extracts:
   - worktree path
   - card .json fields
   - source_hashes
   - verifies each hash against the actual file (searching by basename under release/)
2. Copy the release sources into my worktree under `audit360/<card>/` (excluding .lake) and build.

But careful: the release packages are derived from a common D6 base (~58 modules) and may be large. Copying 7 × maybe 100 files is fine. Building each package fully may take time but mathlib oleans are shared. Actually, the .lake/packages symlink points to /data3/.../poincare-lab/.lake/packages (shared). If I copy the release dir without .lake, then create .lake/packages symlink, lake should reuse mathlib oleans. Build of each package's own modules might take a few minutes each.

Alternative cheaper approach: build in place in the sibling worktrees with a *fresh* build dir. But that would modify sibling worktrees (writing to their .lake) — "work only in your isolated worktree" suggests not modifying others. Copying to my worktree is safer and more independent.

Total time budget 4h; 7 packages. Let me first check sizes.

Also important: mathlib revision used by D12 cards is 7974e751..., while the upstream snapshot requires 520045ab and Lean v4.32.1. The local cards use v4.34.0-rc2 + mathlib 7974e751. I can't rebuild upstream packages with v4.32.1 unless mathlib 520045ab is downloaded (no network? maybe there's a cache). Let me check if there's another packages dir for v4.32.1. The `toolchain-cache/extracted` may contain something. Let me check.

Actually my audit targets the D12 cards, which are local packages. Reusing upstream packages: the cards say they adapted from upstream (DoCarmo) but built self-contained. I'll note whether upstream sources are consistent.

Let me check disk space too, since copying might be heavy. Let me measure release sizes.
dsh: reasoning:
Good. Source-only copies are small (<1.5MB each). Disk is fine.

D12-spectral-sobolev has a 16G .lake build (its own build cache). I'll copy only sources.

Now, let me check whether each card's release package has a proper lakefile.toml, lean-toolchain, lake-manifest.json. And check the Audit modules.
I should set up an audit directory in my worktree: `audit360/`.
For each card:
- `audit360/<card>/src/` (copy of release sources: lakefile.toml, lean-toolchain, lake-manifest.json, *.lean, subdirs, excluding .lake)
- `audit360/<card>/.lake/packages` symlink to shared packages
- audit driver: generate Probe file with `#check` and `#print axioms` for each proved declaration; run `lake env lean`.

But wait: building the full package requires compiling all modules including imports from the D6 base. Let me check if the base modules are in the release trees (yes, ~58 files). Fine.

Potential issue: compiling 7 packages × ~60 modules with mathlib imports. The D12 worktrees already have build caches; copying sources means full recompile. With shared mathlib oleans, each package might take 3-10 min. Let's test with one package first (volume-ibp) to measure.

Actually, a faster and still independent approach: build only the D12 module subtree + audit probe using `lake env lean` per file? That still requires compiling dependencies. `lake build Poincare.D12.<X>` will compile only needed modules. The D6 verification approach was `lake build Poincare.D12` (their library glob). Let me build the specific D12 target rather than full package.

Let me look at the lakefile of a D12 package to see library globs.

Also — axiom audit: I'll write a generator that reads the card's proved_declarations and emits:
```
import Poincare.D12.<...>
#print axioms Foo.bar
```
But importing just the top module may not import all submodules. Better: import the card's top-level module(s) that contain the declarations, or import all D12 modules. Let me use `import Poincare.D12` if exists in each package (D12.lean aggregator).

For robustness, I'll import every `.lean` module in the package's D12 dir via a generated probe that imports each module individually? An `import` list can include all. Then `#check`/`#print axioms` each declaration.

Note: `#print axioms` of a declaration that doesn't exist errors out — which is good (fail-closed). I also need to handle names that are definitions vs theorems: `#print axioms` works for both (shows axioms used, possibly none for definitions? Actually for definitions it prints the axioms of the definition's value).

Additionally, an independent axiom audit should include:
- forbidden token scan in sources: `sorry`, `admit`, `axiom`, `unsafe`, `native_decide`, `proof_wanted`, `implemented_by`, `extern`, `@[implemented_by]`, `set_option maxHeartbeats 0`? (maxHeartbeats is fine but note).
- `#print axioms` cones.
- Check for `Classical.choice` etc. allowed only.
- Negative control: my own file with `sorry` and `native_decide`, run the audit predicate on it to prove it catches violations (fail-closed).

Also semantic audit: I want to check the *statements* of headline theorems for vacuity/weakening. Let me read the cards and the Lean statements.

Time is limited (4h). Let me prioritize:
1. Inventory + hashes (30 min)
2. Builds + axiom probes for all 7 (bulk of time; run in background parallel? Lake builds in parallel could contend; but 7 packages × small size — I can run maybe 3-4 concurrently. CPU count? Let me check nproc.)
3. Semantic review of headline claims + exact_blockers_closed (1h)
4. For missing 2 cards, document absence.
5. Write results.

Let me check nproc and memory.
dsh: reasoning:
192 cores, 2TB RAM. Great — parallel builds feasible.

Interesting: D12-volume-ibp release has no `Poincare/D12.lean`? The `ls $w/release/Poincare/D12*.lean` only printed for connection-curvature. Let me check each package's D12 top module names.

Let me set up the audit. First, create the todo list and checkpoint. Then write an inventory/verification script.

Plan:
- `audit360/` in my worktree.
- `audit360/inventory.json` — card inventory with hashes.
- For each card: `audit360/pkgs/<card>/` — copied sources.
- `audit360/hash_check.py` — verify source_hashes against files (by basename search + exact relpath).
- `audit360/regen_probe.py` — generate Probe file per card.
- `audit360/audit_axioms.py` — parse `#print axioms` output; fail-closed.

Let me first do the inventory + hash verification (fast, no build), then kick off builds in background.

Also for the two missing cards, let me do a final targeted search on the whole /data3 filesystem for filenames (fast with find on limited dirs), plus check the `logs/` of my fleet and `prompts_v2`. Maybe remote cards are in `longrun/prompts_v2`? Let me look at that dir.
dsh: reasoning:
Now let me start the inventory. I'll write a Python script to:
1. For each of the 9 card names, map to worktree dir if it exists.
2. Read card .json.
3. Verify source_hashes: for each key, find candidate files under the worktree release/ (search by basename/relative path) and compare sha256.
4. Output inventory.

Let me write this script in my worktree. First check my worktree's writability and the presence of tools/python.

I'll create the directory structure audit360/.

Let me write the script.
dsh: reasoning:
Some packages lack a `Poincare/D12.lean` aggregator. I'll generate imports for all module files in the D12 subtree.

Now let me write the inventory script.

Key design: for each card dir (worktree), copy the release sources to audit360/pkgs/<card>. But first verify hashes in the original.

source_hashes keys: basenames or relative paths. Strategy: for each key, search all files under release (excluding .lake) whose path endswith key; compare sha256; record matches and mismatches. Fail if no match; note ambiguity if multiple matches with same content (fine) or one matches + others don't.

Let me also hash the card files themselves (for provenance in my results).

Let me write `audit360/inventory.py`.
dsh: reasoning:
Interesting! Several cards have "bad" hashes (mismatch), meaning recorded source hashes do NOT match the files on disk. This is a major audit finding — but I must check whether the mismatch is because the producers updated sources after writing the card (cards frozen earlier), or because the hash keys are ambiguous/mis-keyed. Let me inspect the details.

Let me look at the inventory JSON details for each mismatch.
dsh: reasoning:
The spectral-sobolev card's source_hashes has a key "frenzymath" with value "not used by D12 ..." — a string, not a hash. My check treats it as hash. Similar probably for others (comparison-geodesics etc. may use different structures). Let me dump the source_hashes for each card to see. Also fix the script's error (want is a dict maybe).
dsh: reasoning:
The source_hashes format differs per card:
- D12-connection-curvature: flat relative to release/ root? Keys "Poincare/D12.lean", etc. Wait no, they are "Poincare/D12.lean" — relative to release/ (there is release/Poincare/D12.lean). And "negcontrol/D12NegativeControl.lean" — relative to the worktree root, not release! So mixed. That one is missing because my search was under release/ only. Since key "negcontrol/..." is worktree-relative. I should search under the whole worktree.
- D12-volume-ibp: flat basenames under release/Poincare/D12/VolumeIBP? We saw 11 ok.
- D12-spectral-sobolev: nested {"sha256": {...}, "mathlib": ..., "frenzymath": ...} with release/-relative paths. My script compared the top-level keys ("sha256", "mathlib", "frenzymath") as hashes. Need to handle nesting.
- D12-semantic-ledger: keys nested under "package_lean_files" with release-relative paths.
- D12-comparison-geodesics: flat release-relative paths.
- D12-geometric-compactness: flat release-relative.
- D12-surgery-recognition: flat release-relative.

Let me update the inventory script to normalize: detect nested dicts with "sha256"/"package_lean_files"/flat; treat values that are dicts/strings non-hex as metadata.

Then re-run to see real mismatch counts.

Also note: D12-spectral-sobolev and D12-geometric-compactness include `tools/d12_axiom_audit.sh|py` which is outside release (worktree-relative). So search scope should be the worktree root but excluding release/.lake and third_party. Let me search worktree root excluding .lake dirs and third_party (huge). Keys containing "release/" will match under release/.

Let me rewrite the script.
dsh: reasoning:
All recorded source hashes for the 7 present cards verify against on-disk bytes. Good: hash provenance intact.

The "ambiguous: 1" for volume-ibp: one key has multiple candidates (e.g. basename "Audit.lean" matches both release/Audit.lean and Poincare/D12/VolumeIBP/Audit.lean?). Let me check. That's a provenance weakness (key ambiguity) but hashes ok.

Now let me handle the missing two cards. They don't exist locally. Let me do a final search over plausible roots:
- `/data3/guoshaoyang/workdir/lean_poincare/longrun/**` for filenames containing "TensorMaximumBochner" — earlier grep -rl timed out due to worktrees size; let me do a name-based find (fast) with -maxdepth limits.
- Also check for other user's dirs? Probably not.

Let me run a bounded find by name across /data3/guoshaoyang/workdir/lean_poincare with -maxdepth 6 and exclude worktrees/third_party (or include; find by name is fast if the fs is local, but 2352 upstream files × 85 worktrees... still fine).

Actually the number of files: each worktree release has ~100-400 files, plus .lake dirs (huge: 16G spectral). Excluding .lake is essential. Let me use find with -name pattern and -prune for .lake.

Let me time-box to 120s.
dsh: reasoning:
The two missing cards (D12-tensor-maximum-bochner, D12-triangulation-topology) have no artifacts anywhere in the local workspace: no module files, no card, no worktree. Only referenced in plan JSONs.

So my audit will cover 7/9 cards; 2/9 are source-absent. That's a real finding: the claim "360-produced D12 result cards for 9 modules" cannot be verified for 2 of them because the producer artifacts were never synced/created on this host. Given the acceptance criteria ("compiled_only_until_independent_semantic_review"), I'll record:
- 7 cards: audited (with per-card verdict)
- 2 cards: NOT AUDITABLE — no source, no card, no worktree. This is a blocker A3 sub-item (artifact availability), not a mathematical closure/refutation.

Hmm wait. Maybe D12-tensor-maximum-bochner and D12-triangulation-topology cards could exist on the remote hosts and never synced. The task says "Independently re-verify the 360-produced D12 result cards (...)" and lists them. If artifacts are absent, I report exactly that. That's an honest audit.

Also possible: those two tasks are still running remotely (queue says remote_owned). Since the D13 mirror task (D13-cross-audit-ophis-cards on 360-1) is "queued", the 360 hosts seem active. But my 4-hour limit means I proceed with what's available.

Now let me start the builds. Plan:
1. Copy sources for the 7 present cards into audit360/pkgs/<card>/.
2. Create .lake/packages symlink to shared packages.
3. Add a probe file generated from the card's proved_declarations (with the imports).
4. Build the D12 subtree and run the probe.

Actually, for independence, I should not rely on the producer's audit script (`Poincare/D12/.../AxiomAudit.lean` or `tools/d12_axiom_audit.py`). I'll generate my own probe + parse `#print axioms` output.

But `#print axioms` in Lean 4 prints e.g. `'Foo.bar' depends on axioms: [propext, Classical.choice, Quot.sound]` or `'Foo.bar' does not depend on any axioms`. For definitions/theorems with no axioms: "does not depend on any axioms".

The probe file needs to import modules that declare the names. Since all declarations of the card are in the D12 subtree plus possibly base modules (e.g., D12-volume-ibp includes "ChartMetric.matrix" etc. from Poincare/D12/VolumeIBP/Basic.lean), importing the D12 aggregator or all D12 modules should cover them.

Let me check the module names and whether each card's `proved_declarations` entries are all in the D12 subtree. E.g., D12-semantic-ledger's proved_declarations may reference Poincare.Longrun... base modules. I'll import all modules in the package (all .lean files) — that's simplest and guarantees the names are in scope. Importing all files requires compiling everything anyway.

But careful with files like `ProbeScratch.lean`, `ReleaseCheck.lean`, `D6AuditReport.lean` which may have `#eval` or heavy computation. Importing them is fine (they're modules; but `#eval` at import time? `#eval` executes at elaboration of that module once, then it's in the olean; importing doesn't re-run. Fine.)

Generating one probe importing all modules could be expensive but fine.

Approach per card:
- `audit360/pkgs/<card>/` ← copy of release (sources only)
- `.lake/packages` symlink → /data3/.../poincare-lab/.lake/packages
- `A3Probe.lean` at package root: imports of all modules in the package (relative paths converted to module names, excluding the probe itself), then `#check`/`#print axioms` for each proved_declarations entry.
- Build: `cd audit360/pkgs/<card> && lake build Poincare.D12...`? Or simply `lake env lean A3Probe.lean` which will compile dependencies? `lake env lean` doesn't build imports automatically — it requires oleans present. Actually `lean` will look for olean files; if missing, it errors. So I need `lake build` first, or use `lake build A3Probe` after adding it as a target? A file at package root not in a lean_lib glob won't be built by `lake build`, but `lake build A3Probe` might work if lake can infer the module? In Lean 4 lake, you can do `lake env lean A3Probe.lean` — Lean does search for imports' oleans and will error if not built. Hmm, actually `lake env lean file.lean` does NOT build dependencies.

Simplest: `lake build` with the existing defaultTargets, then `lake env lean A3Probe.lean`. Full `lake build` includes ReleaseCheck, ReleaseAudit, D6AuditReport drivers — which might be heavy/slow but they were built before.

Alternatively, `lake build Poincare` builds all Poincare modules (the D12 subtree plus base). That's the bulk. Then the probe.

Let me test with D12-volume-ibp first to measure time. Actually, let me do all 7 in parallel in background — 192 cores, but each lake build uses many cores by default (lake uses all cores?). Running 7 concurrent full builds could thrash. Better: run 2-3 at a time. Since D12 packages are small (~70-110 modules), each build maybe 2-5 min with mathlib imported (mathlib oleans shared). Let me test one first.

Also: copying sources — need to include `Poincare/`, `Audit/`, `Ledger/`, `Probe/`, root .lean files, lakefile.toml, lean-toolchain, lake-manifest.json, and any `tools/` scripts referenced (not needed for build).

Let me write a setup script `audit360/setup_pkg.sh` or Python that copies and prepares each package, generating the probe.

Probe generation: parse card JSON `proved_declarations` (list of names). For some cards (e.g. semantic-ledger), the JSON may have other structures. Let me inspect the card JSONs for their declaration lists.

Let me check keys of each card JSON.
dsh: reasoning:
Schemas differ. I'll write a normalizer to extract declaration names from:
- list of str
- list of dicts with 'name'
- dict of category → list (of str or dict with 'name')

Also handle short names (e.g., "ChartMetric.matrix", "doubleBallHomeoSphere") that need namespace resolution. `#check ChartMetric.matrix` may fail if the namespace is open in a module but not globally. For those, I need to find the full names. Approach: use `#check` on the short name in the probe after importing everything (namespaces may be `Poincare.D12.VolumeIBP.ChartMetric`). Short names won't resolve unless opened. Hmm.

Options: for names without a dot-prefix matching a known root (`Poincare.`), I can search the sources for `theorem <name>` / `def <name>` / `lemma <name>` and construct the fully-qualified name by tracking namespaces per file. That's doable but error-prone.

Alternative: `#check` with `open` declarations? No — Lean doesn't allow wildcard open of all namespaces (only `open Foo` for specific).

Better approach: parse the sources to build a map from short name → fully qualified name:
- Track `namespace X` / `end X` and `section`/`end` per file.
- Find `theorem|lemma|def|abbrev|structure|instance|noncomputable def ...` declarations; record name and current namespace.
- Fully qualified name = namespace path + name (unless name already contains dots relative to namespace, e.g., `ChartMetric.matrix` inside namespace `Poincare.D12.VolumeIBP` → `Poincare.D12.VolumeIBP.ChartMetric.matrix`).

That's a reasonable static approach. But Lean's namespace resolution is more complex (nested namespaces, `open scoped`, etc.). Since all these are project-local declarations, tracking `namespace`/`end` blocks and `section` should be adequate.

Actually a simpler robust approach: use Lean itself to search. Generate a probe that does:
```
import <all modules>
#check Poincare.D12.VolumeIBP.ChartMetric.matrix   -- if guess right
```
Failing that, I can run the probe and for unresolved names, fall back to a source-derived map, or use `#find`? There's `#check` only. Lean 4 has `#find` in some versions? Not standard.

Alternative robust approach: emit `#print axioms` for names as given, but for short names, first try `#check name`, and if it fails, try candidate names generated from the source scan. I can implement this two-pass:
1. Generate probe with the best-guess qualified names.
2. Compile; collect "unknown identifier" errors.
3. For failures, use a Lean script `#check` alternatives? Or source scan.

Actually, the cleanest: use a source-level scan to find declarations with matching short names and compute fully-qualified names. Then generate a probe that:
- first `#check` the FQ name; if it fails → the declaration doesn't exist (fail).
- `#print axioms` FQ name.

For cards whose names are already fully qualified (start with "Poincare."), use them directly.

Let me write the source scanner. Let me look at a typical source file to see namespace structure.

Also note: D12-semantic-ledger's proved_declarations include 'file' and 'sha256' fields — that's nice, it tells me exactly which file. But the sha256 might be a file hash. Let me check a source file.

Let me look at a couple of files to confirm namespace patterns.
dsh: reasoning:
Namespace tracking is straightforward. Let me now write the audit harness:

`audit360/prepare.py`:
- For each present card: 
  - copy release sources (exclude .lake) to `audit360/pkgs/<card>/`
  - symlink `.lake/packages` → shared
  - build module import list (all .lean files in the copy, converting path to module name)
  - resolve declared names via source scan
  - write `A3Probe.lean` with imports + `#check` + `#print axioms`
  - write `A3Meta.json` with resolution results

Then `audit360/run_builds.sh` builds each package: `lake build` (default targets) then `lake env lean A3Probe.lean`, capturing logs.

Concern: full `lake build` includes the D6 drivers and `Audit/D12/D12Audit.lean` modules which may be slow, but should be OK.

Wait: careful with `Poincare/D12/.../AxiomAudit.lean` files: importing them might have side effects? They're just `#print axioms` commands; importing a module with `#print axioms` doesn't re-run the command on import (commands run at elaboration, and elaboration happens once when building that module; the olean stores declarations). Fine.

But: some modules may contain `#eval` heavy computation — building them will run it. The producer already built them, so it should be tolerable. Actually D12-spectral-sobolev's 16G .lake suggests heavy builds. Let me check its files for heavy tactics. We'll see.

Another concern: total build time. Let me first prepare and build ONE package (connection-curvature) to validate the harness, then run the rest in parallel groups.

Also: the independent negative control. I'll write `A3NegativeControl.lean` with a `sorry` theorem and a `native_decide` theorem, and verify my audit predicate flags them. The negative control can live in one package (needs an import? can be standalone with `import Mathlib`? `sorry` works with just `import Mathlib`). I'll create it at `audit360/negcontrol/A3NegativeControl.lean` and run `lake env lean` inside one of the built packages (using its .lake env) — that's how D12 did it.

Let me now write the prepare script. Key details:

Module name from path: strip `.lean`, replace `/` with `.`. Skip files that are not modules? All .lean files are modules.

Import list: all modules in the package. But some might fail to import due to name clashes (e.g., `Poincare.D12.lean` and `Poincare/D12/...` both exist — fine). Also `ProbeScratch.lean` may exist — fine. But `A3Probe.lean` itself excluded.

Careful: if I add A3Probe.lean to the package root, lake's `Poincare` glob etc. won't include it. `lake env lean A3Probe.lean` works. But to build it as a module with imports, dependencies must be built first — so run `lake build` first.

Resolution of short names: I'll implement a scanner:
- Walk lines; track namespace stack via `^namespace\s+(\S+)` and `^end(\s+(\S+))?$`. `end X` pops if X matches the top; `end` pops one.
- Detect declarations: regex `^(?:@\[[^\]]*\]\s*)?(?:private\s+|protected\s+|noncomputable\s+|partial\s+|unsafe\s+)*?(theorem|lemma|def|abbrev|structure|class|instance|inductive|opaque)\s+([A-Za-z_][A-Za-z0-9_'\.]*)`. For instances, name may be anonymous or `instFoo`; proved_declarations are unlikely to be instances. Also `example` no name.
- Build map short name → set of FQ names. FQ = current namespace stack (joined with '.') + '.' + name if namespace nonempty else name.

Then resolution:
- If name starts with "Poincare." → trust as FQ; verify exists in scan.
- Else look up name in map: candidates = FQ names whose (a) full name == name, or (b) full name endswith "." + name. Ambiguity → record.

For D12-semantic-ledger, entries have "name" with FQ.

The probe will then `#check` and `#print axioms` each FQ name. If a name fails to resolve, the probe errors — I'll parse the output and mark that declaration as unresolved. To keep one failure from killing the whole probe, I can put each `#print axioms` in a `section`? Errors don't stop Lean; it continues after an error (unless fatal). Lean reports errors but continues elaboration of subsequent commands. So the probe will produce a list of lines; I'll parse `'Name' depends on axioms: [...]` or `'Name' does not depend on any axioms`.

Actually with `#print axioms Foo`, if Foo is unknown, the error message includes "unknown identifier 'Foo'" or "unknown constant". I'll parse errors and map them.

Better: use `#check` first? `#print axioms` alone is enough; successful lines give the cone.

Let me also add `#print axioms` for the *declared* name; but note that for a `def`, `#print axioms` returns axioms used in the definition body (usually none). For a theorem, gives axioms. Some declarations may be structures/inductives: `#print axioms` works.

Now the "fail-closed" criterion:
- Every declared name must resolve (else audit FAIL for that card).
- All cones ⊆ {propext, Classical.choice, Quot.sound} (else FAIL).
- At least N declarations audited (match count).
- Forbidden token scan in the D12 sources (excluding producer audit files? No — scan all): patterns: `sorry`, `admit`, `axiom `, `unsafe`, `native_decide`, `proof_wanted`, `implemented_by`, `extern`, `sorryAx`. But beware false positives in comments/docstrings. I'll implement a scanner that strips comments (naive `/- -/` and `--`) and strings before matching. Also allow `axiom` inside comments. Let me write a careful-ish tokenizer: remove `/-! ... -/`, `/- ... -/` (nested), `-- ...`, then match word boundaries.

Also check for `set_option` tricks like `set_option autoImplicit` (fine), `set_option maxHeartbeats 0` (suspicious but not unsound), `set_option pp.universes` etc. And `@[implemented_by]`/`@[extern]` (unsafe-ish). Also `Classical.choice` allowed.

Another key adversarial check: "no assumption equivalent to the conclusion" / no vacuous statements. E.g., a theorem with hypothesis `h : P` and conclusion `P`. I'll inspect headline statements manually. For a systematic check, I can look for hypotheses among the theorem's binders that are structurally identical to the conclusion — hard to automate. I'll do targeted manual review of headline declarations for each card, especially those cited in exact_blockers_closed and semantic_class.

Also need to check: the card's `exact_blockers_closed` claims. For each, verify:
- There is a real constructor declaration in the sources (existence + type).
- Not an alias/Prop certificate.
- Downstream checked use: a theorem that consumes it.

Let me now write prepare.py.

One more thing: cards may reference "release/Poincare/..." paths; the copy will preserve the release structure.

I'll also record the sha256 of every source file in the copy (my own manifest) so my results have source_hashes for the audited snapshot.

Let me write it.
dsh: reasoning:
Some unresolved names — likely due to declarations generated in `section` with `variable` and names inside `namespace ... where` or `mutual` blocks, or names in `instance` with generated names, or the declaration spans multiple lines. Let me inspect unresolved entries.

Also connection-curvature: 74 unresolved out of 119 — likely because names like "Poincare.D12.ConnectionCurvature.ChartLeviCivita.ChartMetricCoefficients" are structure *fields* or names declared via `where`? Or because my regex requires the keyword at line start (after optional attributes), but in that card the declarations might be inside a `namespace` with commands indented? Let me check.

Let me look at the unresolved lists for connection-curvature and semantic-ledger.
dsh: reasoning:
Two issues:
1. connection-curvature: names not found — because the card lists `Poincare.D12.ConnectionCurvature.ChartLeviCivita.ChartMetricCoefficients.christoffel` but the actual declaration might be `ChartMetricCoefficients.christoffel` under namespace `Poincare.D12.ConnectionCurvature`. My scanner records short and FQ; if the namespace in the file is `Poincare.D12.ConnectionCurvature.ChartLeviCivita` and then `namespace ChartMetricCoefficients`, then the FQ would match exactly. "no source declaration found" means neither FQ nor suffix match. Possibly the card's module layout changed (card from checkpoint vs current sources) or the declarations are in files with different structure. Let me check the actual source.

2. comparison-geodesics: the card groups several names into one string with " / " separators — I need to split on "/" when the string contains " / ".

3. semantic-ledger: two names missing from source, e.g. `real_initialCondition_specializes`. Need to check if they exist in a different module or were renamed.

Let me inspect the connection-curvature source for "ChartMetricCoefficients".
dsh: reasoning:
Ah, the namespaces are declared one per line: `namespace Poincare`, `namespace D12`, `namespace ConnectionCurvature`. My scanner pushes each, giving FQ `Poincare.D12.ConnectionCurvature.ChartMetricCoefficients.christoffel_symm` — that should match the card's declared name... unless the regex for declaration lines failed for indented declarations. Looking at line 102: `theorem christoffel_symm (k i j : ι) : ...` starts at column 0 (grep shows no leading spaces). Hmm.

Wait — the scan is done over the *copied package*. Did the copy include these files? Yes, we grepped the copy. Let me debug the scanner: maybe `end ChartMetricCoefficients` popped correctly... Actually the issue: my `END_RE` matches `end ChartMetricCoefficients`; ns[-1] is 'ChartMetricCoefficients' → pop. Good.

Hmm, but the card declared name is `Poincare.D12.ConnectionCurvature.ChartLeviCivita.ChartMetricCoefficients` — with `ChartLeviCivita` in the path. But the actual module declares `Poincare.D12.ConnectionCurvature.ChartMetricCoefficients` (no `ChartLeviCivita` namespace). So the card's names are wrong/stale! The `.lean` file is `ChartLeviCivita.lean` but the namespace is `Poincare.D12.ConnectionCurvature`.

That's a significant finding: the D12-connection-curvature card's proved_declarations list 74 names with a `ChartLeviCivita` namespace component that doesn't exist in the sources. Actually wait — let me check whether some file declares `namespace Poincare.D12.ConnectionCurvature.ChartLeviCivita`. Let me grep for "ChartLeviCivita" in namespaces.

Let me check which declarations DO exist: 45 resolved. The other 74 include names with `ChartLeviCivita`, `ChartLeviCivitaSmooth`, etc.

This is exactly the kind of provenance defect an adversarial audit should find: the card's declaration inventory does not correspond to the compiled sources (stale namespace). But careful: maybe the card intended the file-level names and the checkpoint is newer/older. The card's source_hashes do match the files on disk. So the card was generated from these sources but with incorrect namespace qualification in proved_declarations.

Let me verify by grepping the namespace lines in ChartLeviCivita*.lean and compare with the card names.

For semantic-ledger, the two missing names might genuinely not exist (card overclaims) or may be in a "notes" section. Let me search the whole semantic-ledger package for "real_initialCondition". Nothing found. So those two names don't exist in the sources → card lists declarations that are not in the compiled sources. Hmm, but maybe they're `example`s or the card lists them under a different section ("proved_declarations" only 9 entries). Let me read the semantic-ledger card in full later.

Let me now check the scanner correctness by listing all FQ names found in the connection-curvature copy that contain "Chart".
dsh: reasoning:
Confirmed: the D12-connection-curvature card's `proved_declarations` uses a `ChartLeviCivita`/`ChartLeviCivitaForm`/`ChartLeviCivitaSmooth` namespace segment that does not exist in the compiled sources. E.g. card says `Poincare.D12.ConnectionCurvature.ChartLeviCivita.ChartMetricCoefficients`; the source declares `Poincare.D12.ConnectionCurvature.ChartMetricCoefficients`. The file names are ChartLeviCivita.lean etc., so the card author probably used file-derived names. This is a *provenance/claim defect* — not necessarily unsoundness, but the card's declaration inventory does not resolve. My audit must report this precisely: for that card, X/Y declared names resolve, Z don't.

But careful: maybe some of those 74 do exist under the shorter namespace (e.g. `chartAbstractConnection` may be `Poincare.D12.ConnectionCurvature.chartAbstractConnection`). Let me check by mapping card names to their shorter forms (drop the file-namespace segment) and see if they resolve. That's the fair reading: the declaration exists; the card mis-qualified it.

Let me write a second-pass resolution: for unresolved names, try removing the segment right after `ConnectionCurvature.` (i.e., any namespace component that matches a filename in the imports). More generally, try all prefixes removal after a fixed root, and suffix matching. Simplest: for an unresolved FQ name, take the last component (short name) and check if exactly one declaration matches that short name in the sources; if found, record as "resolved-by-shortname (card namespace overstated)".

Similarly for comparison-geodesics: split the grouped strings on " / ".

For semantic-ledger, the two missing names truly don't exist. Let me read that card carefully.

Let me update prepare.py to:
1. Split declared names on " / " and "," when they look grouped.
2. For unresolved, try short-name resolution with a flag.

Then re-run.

Also I should double check the semantic-ledger names: `Poincare.D12.SemanticLedger.real_initialCondition_specializes` and `realField_unsatisfiable_by_gaussian`. Grep found nothing. Maybe they're in the card under "proved_declarations" but the sources have them under a different name. Let me read the card and the sources.
dsh: reasoning:
The two unresolved semantic-ledger declarations live in `audit_probes/D12RealModuleProbe.lean` — a probe file outside the release package (not in the copied release tree). Let me check if that file exists in the worktree.

So for semantic-ledger, the declaration list includes audit-probe declarations. I need to include that file in my audit (copy it or reference). Let me check the worktree.

Now let me update prepare.py:
1. Split grouped names on " / ".
2. For audit_probes files: also copy them into the audit package (e.g., under `A3Probes/`)? They may import from the release package; I can copy the file and run it with `lake env lean` if it's self-contained (imports Poincare...). Simpler: include the probe file's own declarations in the axiom probe if I copy the file into the package and import it. But its module name/path may matter. Let me look at it.

Also for connection-curvature: implement short-name fallback resolution and record `namespace_note`.

Let me check the audit_probes dir.
dsh: reasoning:
`audit_probes/D12RealModuleProbe.lean` is not part of the release package (explicitly). It imports real modules and declares theorems in namespace `Poincare.D12.SemanticLedger`. To audit those two declarations, I need to compile this probe file with the release package env (it's a standalone file). I can copy it into the audit package as e.g. `A3SemanticLedgerProbe.lean` at the package root and run `lake env lean A3SemanticLedgerProbe.lean` after building. But my generated A3Probe.lean imports modules; the two probe declarations won't be visible unless I also import the probe file. Since I run `A3Probe` with `lake env lean`, I can add an `import` of the probe module if I place it inside the package and build it. Placing `D12RealModuleProbe.lean` at package root gives module name `D12RealModuleProbe`; `lake env lean A3Probe.lean` will look for `D12RealModuleProbe.olean` — I'd need `lake build D12RealModuleProbe` first (allowed? `lake build` with a module name works if lake can find it as a target; for a root file not in a lean_lib glob, `lake build D12RealModuleProbe` may fail with "unknown target"). Alternative: concatenate: put the probe file's content into A3Probe before the `#print axioms` section. Since the probe file ends with audit code that runs at elaboration (prints and maybe throws), that would pollute my probe output.

Simplest: create a separate probe file `A3Probe2.lean` that imports nothing but is the *module* `D12RealModuleProbe`? No.

Option: Copy `audit_probes/D12RealModuleProbe.lean` to `audit360/pkgs/D12-semantic-ledger/A3SLProbe.lean`, then run `lake env lean A3SLProbe.lean` — Lean will compile the file in memory and process its commands. If it defines the theorems and then my `#print axioms` commands were appended at the end, all in one file, they'd be in scope. So: create a combined file = original probe content (minus nothing; its final audit code will run too, which is fine — I can compare its own audit output with mine) + appended `#print axioms` for the two declarations. That's the cleanest.

I'll handle this as a special case in prepare.py: for semantic-ledger, append the audit probe content + the two `#print axioms` lines into a separate file `A3ProbeSL.lean`.

Wait, does the D12RealModuleProbe import `Lean.Util.CollectAxioms` and use `#eval`? Let me see the rest of the file. It's fine either way.

Now, also for connection-curvature, I need the short-name fallback. Let me update the prepare script and rerun. Also let me split grouped names on " / ".

Additionally, for cards where dedup matters, ensure no duplicates.

Let me update prepare.py.
dsh: reasoning:
Also handle the semantic-ledger audit_probes special case. Let me add: after probe generation, if the worktree has `audit_probes/*.lean`, copy them and create a combined probe file for names that live there. More generally: for unresolved names, check if the card entry has a "file" field pointing to a source outside release (e.g. audit_probes/) — then compile that file with appended #print axioms.

Let me implement: for each card, after resolving, gather the set of `file` fields from card declarations that were unresolved; copy those files into the package under `A3Extra/`; generate `A3ProbeExtra.lean` = file content + appended `#print axioms` lines. And a build command `lake env lean A3ProbeExtra.lean`.

Module names for files under A3Extra/: `A3Extra.D12RealModuleProbe`. They import release modules; with `lake env lean A3Extra/D12RealModuleProbe.lean` (path invocation), the module name is derived from the path but imports resolve from the package root. Since the file itself imports existing modules, and defines declarations, running `lean` on it directly works (no need to have olean of itself).

Wait, but the semantic-ledger probe file might be huge/heavy. It's 7KB. Fine.

Let me add this to prepare.py. I'll collect `file` fields for the unresolved names (only in semantic-ledger: audit_probes/D12RealModuleProbe.lean).

Actually simpler and more general: for each card, collect all `file` values in the card JSON declarations; if a file path is outside `release/` and exists in the worktree as `.lean`, copy it and append probes for the declared names found in it. Let me implement targeted handling for the semantic-ledger case with a generic fallback.

Let me add code after probe generation.
dsh: reasoning:
There's a leftover buggy line `names_here = [...]` followed by reassignment; the first comprehension references `r["declared"]` correctly but it's dead code overwritten. Let me clean it up. Also `hashes` is defined before this block? Let me check order in the file: hashes computed before probe generation — yes, `hashes` is defined before "probe" section. Good.

Let me remove the dead comprehension.
dsh: reasoning:
Hmm, connection-curvature still 74 unresolved — the short-name fallback didn't work. Why? Because the card names are like `Poincare.D12.ConnectionCurvature.ChartLeviCivita.ChartMetricCoefficients.christoffel_symm`. Short name = `christoffel_symm`. The scan map should contain `Poincare.D12.ConnectionCurvature.ChartMetricCoefficients.christoffel_symm`. `short_hits` = fq names ending with ".christoffel_symm" → exactly 1. So it should have resolved...

Unless the scan doesn't include those declarations at all. Maybe my DECL_RE fails because declarations are indented with two spaces? Looking at grep output: "102:theorem christoffel_symm ..." — grep output has no leading spaces shown, but that's grep's format `102:theorem`. Let me directly check the file for leading whitespace.

Wait, earlier `grep -n "^theorem\|^lemma..."` matched line 102, so no leading whitespace. Hmm.

Maybe the issue is that the copy `dst` uses `Poincare/D12/ConnectionCurvature/ChartLeviCivita.lean` — but did the scan walk see it? Yes.

Let me debug: check `scan` for 'christoffel_symm'. Let me run the scanner standalone on the package copy.
dsh: reasoning:
The scan does contain `Poincare.D12.ConnectionCurvature.ChartMetricCoefficients.christoffel_symm`. So short-name fallback should have matched. Unless... `prepare.py` ran with the OLD edited version? I edited the file, then ran. Hmm, wait: in my edit, the fallback uses `short_hits = sorted({fq for fq in scan if fq == short or fq.endswith("." + short)})` where `short = name.split(".")[-1]`. For `Poincare.D12.ConnectionCurvature.ChartLeviCivita.ChartMetricCoefficients.christoffel_symm`, short = `christoffel_symm`. short_hits = 1 → resolved with note. But the output still says 74 unresolved.

Oh! I see: the `edit` to add fallback might have been applied to a different branch — let me check the current file content around that section. Maybe my earlier edit created `cands` check but I wrote `if len(cands) == 1: ... continue` then fallback. Let me read the file.
dsh: reasoning:
The code is correct. So why 74 unresolved? Maybe short_hits > 1 because `scan` contains duplicates: e.g., `christoffel_symm` might exist in multiple namespaces? Let me check: `sum_three_cycle` etc. might be declared in both ConnectionCurvature and elsewhere. For `ChartMetricCoefficients.christoffel_symm`, only one. But for `ChartLeviCivitaSmooth.SmoothChartData.sum_add_split`, short `sum_add_split` might exist in multiple files.

But the summary said resolved 45, unresolved 74 — exactly the same as before. Suspicious: identical counts. Maybe the script run used a cached .pyc? No. Or maybe the unresolved names have trailing characters like `metricDerivative_christoffel'` (apostrophe) — for that one short = `metricDerivative_christoffel'`; scan should contain it (regex allows `'`).

Hold on — maybe the run I did was before the edit? No: I edited at 12:xx then ran. Let me just rerun and print details for one name.

Actually, wait. Look at the order of my commands: I made edit #1 (split_grouped), edit #2 (short fallback), then edit #3 (extra probes) and edit #4 (remove dead code). Then ran prepare.py. The counts for comparison-geodesics changed (16→34 declared, 33 resolved), so the new script DID run (split worked). For connection-curvature, declared stayed 119 and unresolved stayed 74. So the fallback isn't matching.

Let me debug directly with a small script that mimics the resolution for a specific name.
dsh: reasoning:
Found it: `scan` includes both the FQ and the bare short name as separate keys (I add `names[fq] = fq` and `names.setdefault(short, fq)`). So `short_hits` has 2 entries (the FQ and the bare short) → len != 1 → falls through to "ambiguous".

Fix: in scan_decls, don't add bare short keys; instead keep a separate list of short names, or in resolution filter hits to those containing a "." (FQ). Simplest: change scan_decls to only record FQ names (drop `names.setdefault(short, fq)`), and build a separate short→fq map for lookups when needed. Since `name in scan` checks FQ names and card names may be short (e.g., "ChartMetric.matrix"), I need short resolution too.

Let me restructure: scan_decls returns dict fq→fq only. Then build `short_map`: for each fq, short = fq.split('.')[-1]; map to list of fq. Resolution:
1. name in scan (exact FQ) → resolved.
2. candidates: fq == name or fq.endswith("."+name) → these handle partial qualification.
3. short fallback: short_map.get(short) — unique → resolved with note.

Note: `fq.endswith("." + name)` for a short name like "ChartMetric.matrix" gives `Poincare.D12.VolumeIBP.ChartMetric.matrix` — good.

Let me edit.
dsh: reasoning:
Much better. Remaining unresolved: connection-curvature 1, semantic-ledger 2 (handled via extra probe), comparison-geodesics 1. Let me check those.

Then start builds. Let me look at the unresolved entries.
dsh: reasoning:
Good:
- connection-curvature: one ambiguous name `RicciSymmetry.curvatureEndo` — actual is `Poincare.D12.ConnectionCurvature.curvatureEndo` (in RicciSymmetry.lean file but namespace ConnectionCurvature). The other candidate is `Poincare.Longrun.Geometry.AbstractConnection.curvatureEndo` (base module). The intended one is the D12 local. I'll resolve it manually with a note (the card's `RicciSymmetry` prefix is file-derived). I can add a tie-break: prefer candidates in the same module subtree as the card (e.g., prefix Poincare.D12.<Module>). Let me add manual overrides: for card connection-curvature, map `Poincare.D12.ConnectionCurvature.RicciSymmetry.curvatureEndo` → `Poincare.D12.ConnectionCurvature.curvatureEndo`.
- comparison-geodesics `_ge` is an artifact from splitting "euclidModel_singular_comparison / _ge / euclidModel_bishopGromov / euclidModel_volume" — the original text probably was "euclidModel_singular_comparison / _ge" meaning `euclidModel_singular_comparison_ge`. I'll handle by a special-case join or ignore with a note. Better: implement a heuristic — if a split part starts with "_", join it with the previous part. Let me add that in split_grouped.

Now, the connection-curvature card's namespace discrepancy is itself a finding: 73 of 119 declared names in the card do not resolve as written. I'll report it as a "card inventory defect (namespace over-qualification)" — confirmed by unique short-name matches in the compiled sources.

Next: builds. Let me write the run script:
- For each package: `lake build` (full default targets) with log; then `lake env lean A3Probe.lean` with log; for semantic-ledger also `lake env lean A3Extra/D12RealModuleProbe.lean`.
- Use `set -o pipefail`; record exit codes.
- Run packages in parallel (7 packages; each lake build spawns many processes; with 192 cores, run all 7 concurrently is OK but lake may use up to nproc each → heavy oversubscription. Lake uses `-j` default = number of cores? Let me set `lake build -j 8` per package to keep total ~56. Hmm, but some packages (spectral with 16G cache) may be heavy.

Let me first test a single build to see timing and any issues (connection-curvature, the smallest one). Run in background and monitor.

Note: `lake build` with defaultTargets includes "D6AuditReport" etc. Also the release package may have `Audit/D12/D12Audit.lean` compiled via `lake build Audit.D12.D12Audit`. `lake build` builds defaultTargets only (Poincare, Probe, Ledger, Audit, ReleaseCheck, ReleaseAudit, D6AuditReport). The `Audit` lib covers Audit/**. Fine.

For the probe, I need all modules built. `lake build` builds the default targets; A3Probe imports ALL .lean files including `ProbeScratch.lean` (root) and `A3Probe`-adjacent files. Root files like `ProbeScratch.lean` may not be part of any target and thus not built! `#check`/`#print axioms` need their oleans. `lake env lean A3Probe.lean` searches for imports; missing oleans → error "object file ... does not exist".

Solution: use `lake env lean A3Probe.lean` but first build each imported module explicitly with `lake build <module>` for all modules. `lake build` accepts module names for any module in the package? For files not covered by a lean_lib glob, lake may still resolve module names via `LEAN_PATH`. Lake builds targets by name if it can find the module in the workspace (default facet). I think `lake build ProbeScratch` works if the file is at the root and the package has `lean_lib` globs not covering it — lake might complain "unknown target". Let me test.

Alternative: remove the import of unbuildable root files from the probe. Let me check which root .lean files exist per package: D12-connection-curvature had `D6AuditReport.lean`, `D6LedgerProbe.lean`, `ReleaseAudit.lean`, `ReleaseCheck.lean`, `ReleaseClaims.lean`, `ProbeScratch.lean`(volume-ibp). Their modules are covered by lean_libs except ProbeScratch. So I can drop imports of modules not covered by any lean_lib glob. Simplest: exclude root-level modules that aren't in a lean_lib, i.e. exclude files at package root except those matching `lean_lib` names. Actually easier: attempt `lake build <all module names>`; if that works, great.

Let me test with one package. I'll write the build script and run for volume-ibp first (it has ProbeScratch).

Actually let me just try: `cd pkg && lake build Poincare Probe Ledger Audit ReleaseCheck ReleaseAudit D6AuditReport` then `lake env lean A3Probe.lean`. If the probe complains about missing olean for `ProbeScratch`, I'll handle.

Let me check the D12-volume-ibp A3Probe imports to see which root modules are included.

Let me write the runner script now.
dsh: reasoning:
Before launching, fix the two remaining resolution issues:
1. connection-curvature `RicciSymmetry.curvatureEndo` → `Poincare.D12.ConnectionCurvature.curvatureEndo`.
2. comparison-geodesics split: join parts starting with "_" to previous.

Let me patch prepare.py: in split_grouped, if a part starts with "_", append it to the previous part. And add an explicit override map for known file-derived namespace prefixes: after short-name resolution is ambiguous, prefer a candidate that starts with "Poincare.D12." (same stage as card). Actually generic tie-break: prefer candidates whose prefix matches the card's D12 module root, e.g. `Poincare.D12.<CamelName>`. For card "D12-connection-curvature" → module `Poincare.D12.ConnectionCurvature`. I can derive the module root from the card's name: D12-connection-curvature → Poincare.D12.ConnectionCurvature. Let me add parameter `card_root` and in ambiguous case prefer candidate starting with card_root.

Let me patch.
dsh: reasoning:
All resolved except semantic-ledger's 2 (which are covered by the extra probe). Now, launch builds. Let me start with a quick single-card test (volume-ibp) in the background, and meanwhile continue semantic review of cards.

Actually, to save wall-clock time, launch all 7 in parallel with `-j 16` each. 7×16=112 processes < 192 cores. Start them as background jobs via bash `run_in_background: true`. But I can also use nohup and poll. The `bash` tool with `run_in_background: true` returns a job id; I can collect later.

Let me launch all 7 now. Also start the semantic reading (cards' md files) in the meantime.

One concern: `lake build` may try to re-resolve packages or update mathlib (network). The lake-manifest pins mathlib rev; `.lake/packages/mathlib` already exists; lake should not fetch. If it tries, it may hang. Let's see.

Let me launch.
dsh: reasoning:
The logs dir doesn't exist yet (run_card.sh creates it after redirect). The nohup redirect failed, but did the scripts start? The `&` backgrounded `nohup bash ... > missing-file` — bash failed to open the file, so the command likely didn't start, yet "started ... pid" printed. Let me check whether the processes are running.

Let me mkdir logs first and relaunch.
dsh: reasoning:
Hmm, `ps aux | grep run_card.sh | wc -l` = 0 after 10s — the scripts may have exited quickly. Let me check the driver logs. Also the first `ps aux | grep -c` showed 2 (maybe from the earlier failed ones? or grep itself).

Let me look at logs.
dsh: reasoning:
Builds failed fast (exit 1 in 3 seconds). Let me see the errors.
dsh: reasoning:
Two issues:
1. `lake build -j 16` — this lake version doesn't accept `-j` (it's `lake build -j`? apparently not; maybe `lake -j 16 build` or `lake build -- -j`?). Let me check `lake build --help`. I'll just use `lake build` (uses all cores by default) or set `LAKE_JOBS`. Let me check help.
2. Probe failed because build didn't run.

Let me fix run_card.sh: use `lake build` without -j, and for parallelism control use env `LAKE_JOBS=16`? Let me check lake --help quickly.
dsh: reasoning:
No `-j` option; lake uses `LEAN_NUM_THREADS`/`LAKE_JOBS`? In Lake, parallelism is controlled by `lake` itself using a job count from `-j`? Not in this version. Lake 5.x has `lake --jobs`? The help shows only targets and `-o`. There might be a `--no-build` etc. Let me just use `lake build` and rely on the OS scheduler; run fewer packages concurrently (e.g., 3 at a time).

Also, `lake build` may limit itself to `core count / 2`? Not sure. Let's set `LEAN_NUM_THREADS=4` to cap per-process threading? Not needed.

Let me fix the script: remove `-j 16`. Then run all 7 but in two batches (4 + 3) to avoid contention. Actually mathlib oleans are prebuilt — the only compilation is the package's own ~70-110 modules plus dependencies in Poincare base (which are part of the same package, ~50-100 files). Each package is similar; let's just launch all 7 at once and see. The machine has 192 cores and 2TB RAM. Lake will spawn up to N jobs where N = number of targets? It probably caps at `System.Platform.numCores` (192). Seven packages × 192 = oversubscription, but total work is only ~700 modules; the OS will time-slice. Could slow things but not fatally. Safer: launch 3 now, then 4 after.

Actually, let me reconsider: since packages share the same base modules but each has its own .lake/build, there's no sharing. Each package needs the full base compiled (~50-100 modules). With mathlib oleans available, compile times should be modest.

Let me fix and launch 4 packages now (connection-curvature, volume-ibp, spectral-sobolev, semantic-ledger), then the remaining 3 after. Actually let me launch all 7 with a `nice` and see; total CPU is abundant.

One more consideration: `lake build` may attempt to fetch/update mathlib if the manifest isn't satisfied. `.lake/packages` symlink exists with mathlib matching rev. Should be fine.

Fix script and relaunch.
dsh: reasoning:
Builds are running (16 lake processes). While they run, let me do the semantic review of the cards. Let me read each card's md and the key Lean statements.

Priority order for semantic review:
1. exact_blockers_closed claims (confirm/refute) — the core of my task.
2. semantic_class / headline claims — check for weakening/vacuity.

Let me first extract exact_blockers_closed from all 7 cards.
dsh: reasoning:
Now I have the blocker closure claims:
- connection-curvature: 1 closure (LeviCivitaExistenceStatement).
- volume-ibp: none.
- spectral-sobolev: none.
- semantic-ledger: none (and explicitly says "no blocker claimed closed").
- comparison-geodesics: none.
- geometric-compactness: none.
- surgery-recognition: 3 closures (SR-5 sphere_of_spheres, covering-space recognition, coveringTrivial).

Task says "confirm/refute exact_blockers_closed". So I need to verify 4 blocker-closure claims (1 + 3).

Also named blocker A3 in my task queue. What is A3? Let me find the blocker definitions in the queue/state. The D12-semantic-ledger blockers were ["A1","A2","A3","P1","P5"]. My task's named blocker is A3. Let me find what A3 means — probably in a blockers manifest or the queue adapter. Let me grep for '"A3"' in manifests.

While builds run, let me investigate:
1. What A3 is (so I can report on whether my audit addresses/closes it).
2. The LeviCivitaExistenceStatement closure — verify in sources.
3. The surgery-recognition closures — verify constructors and downstream uses.
4. Headline statements for each card.

Let me grep for A3 definitions.
dsh: reasoning:
A3 = "audit-residual-finding: No false statement was found, but the adversarial audit only covers the D4 evolution cluster; earlier clusters (D2/D3) have axiom audits but no counterexample search."

So my task (auditor, blocker A3) is a cross-audit of the 360-produced D12 cards; A3 is the residual audit-coverage blocker. My audit must be adversarial: search for false, vacuous or overstrong statements in the D12 cards, and report coverage. My independent semantic review should therefore look for false/vacuous/overstrong statements, not just axiom cones.

The D12-semantic-ledger card also claims to address A1/A2/A3 by auditing D1-D11. Since it's a 360-produced card that I'm auditing, I should assess whether its claims about A3 coverage hold, and whether it actually performed the counterexample search for D2/D3. Interesting.

Let me now check build progress, then read cards.
dsh: reasoning:
Builds are progressing (8921/8957 jobs). Interesting: some builds already show `#print axioms` output in their build logs — because the package's own audit modules run `#print axioms` during compilation. Good.

Note D12-surgery-recognition includes a `Poincare/VKPort/HatcherLib` module — ported upstream Hatcher library? That's relevant to "reuse upstream packages" and licensing. I should check provenance of VKPort (maybe adapted from the frenzymath snapshot's Hatcher formalization). I'll examine later.

While builds run, let me read the cards' md files. Start with connection-curvature (already partially read), then volume-ibp, surgery-recognition (blocker closures), and the others.

Let me extract key statement texts from the cards. Rather than reading whole md files (long), let me grep the "What is proved" sections. But for a genuine semantic audit I need the actual Lean statements. Let me pull the #check types from the probe outputs once built — or directly grep the Lean source for `theorem <name>` headers.

Priority: verify the 4 blocker-closure claims in the Lean sources.

1. `leviCivitaExists` in MilnorLeviCivita.lean — check statement and witness.
2. surgery: `ConnectedSumDecomposition.mkV2`, `stage6Target_of_v2decomposition`, `finiteFreeOrbit_isQuotientCoveringMap`, `deckTrivial_of_simplyConnected_quotient`, `sphericalPieceRecognition_of_spaceForm`, `stage6Target_of_v3hypotheses`.

Let me read those source files now.
dsh: reasoning:
Good. `leviCivitaExists` is a real theorem `leviCivitaExists (m) (b) : LeviCivitaExistenceStatement m b := ⟨milnorConnection m b, milnorConnection_isLeviCivita m b⟩`. I need to check `LeviCivitaExistenceStatement` in `Poincare/Longrun/Geometry/LeviCivitaBlocked.lean` — the original named blocker — and confirm the closure is not a weakening (e.g., changed definition). Important: the builder may have *modified* the definition of `LeviCivitaExistenceStatement` to make it provable — that would be a fake closure. Let me check:

1. The base module in the D12 release (`Poincare/Longrun/Geometry/LeviCivitaBlocked.lean`) — is it the same as the accepted D6/D7 version? I can compare with the D6 worktree base or the D7-levi-civita-smooth worktree.
2. Whether `LeviCivitaExistenceStatement` still says what the original blocker said.

Let me look at the definition and compare across worktrees.

Let me also check `MetricData.raiseIndex` existence (used by adTranspose) and `IsLeviCivita` definition.

Note the card's claim: "Historical docstring conflated mean-connection compatibility with existence". This is a semantic claim — plausible.

Key adversarial check: is the base file `LeviCivitaBlocked.lean` in the D12 worktree byte-identical to the D6 accepted release? The D12 card's source_hashes only cover D12 files plus Audit/D12 (and negcontrol). The base files are inherited. I should verify the base files were not modified relative to the D6 release. Let me diff the base modules across the D12 release and the D6 release.

Actually the D12 release package is a copy of D6 release plus new D12 modules. If they changed `LeviCivitaBlocked.lean` (e.g., weakened the statement), the hash check of D12 sources wouldn't catch it. That's an important audit check! Let me diff the entire base (non-D12) part of each D12 package against the D6 release package. The D6 worktree is available at ../D6_weekly_release/release.

Let me do that now while builds run.
dsh: reasoning:
Interesting. The diff shows:
- connection-curvature, spectral-sobolev, semantic-ledger, geometric-compactness: base identical to D6 release (only D12 additions).
- volume-ibp: adds ProbeScratch.lean.
- comparison-geodesics: adds Poincare/D10 directory and a `scratch` dir.
- surgery-recognition: adds Poincare/D7 and Poincare/VKPort directories (overriding/extending base!).

But wait — `diff -rq` with `--exclude="D12*"` excludes top-level D12 files but not files inside Poincare/D12 (the exclude pattern applies to basenames during traversal? GNU diff `--exclude=PATTERN` matches basenames). "Poincare/D12" dir contents have basenames like "Basic.lean" — not excluded. Hmm, but no differences were reported for D12 files because diff -rq reports "Only in ..." for D12 dirs (which I filtered with `grep -v "^Only in $D6"`). Ugh, my grep filtered out all "Only in D6..." lines, which includes "Only in D6/release/Poincare: D12" — but not "Only in <w>/release/..." lines. So the output shows only additions in D12 worktrees, and files that differ would show as "Files ... differ". Let me redo more carefully: compare only files that exist in both, ignoring D12-specific dirs, and list differing files.

Key check: are any *base* files modified? Let me list "Files X and Y differ" lines.
dsh: reasoning:
No base file differs from the D6 release in any D12 package. Good: the builders did not modify accepted base modules (the D7 HeatKernel etc.). But wait — surgery-recognition added `Poincare/D7` files. Are those new files or overrides of existing D6 files? Since diff reports no "Files ... differ", any file in both trees is identical. "Only in <w>/release/Poincare: D7" means the D7 directory exists only in the D12 tree (D6 doesn't have Poincare/D7 at all? That seems odd — D6 release has 58 modules; maybe D7 modules weren't in D6). Let me check what's in surgery's Poincare/D7.

Also for comparison-geodesics: Poincare/D10 added and `scratch/` dir. And VKPort in surgery.

Let me list those additions and check whether they are upstream-adapted (Hatcher) code. Also check the `scratch` dir (could contain dead code, not compiled — fine but note).

More importantly: are there *other* base modules outside the D6 release that the D12 cards import (e.g., D7 modules)? The D6 release presumably contains D7? Let me check D6 release dirs.
dsh: reasoning:
So the D12 packages add base modules from other stages (D7 Recognition, D10 Jacobi, VKPort) as vendored copies. Those are not in the D6 release; the "source_hashes" of the cards may not cover them! E.g., D12-surgery-recognition's source_hashes cover only the D12/SurgeryRecognition files. The D7 modules it depends on are copied in, and if they were modified relative to the accepted originals, that could weaken things. I should verify those vendored modules against their origin worktrees (D7-* worktrees, D10-jacobi-constant-curvature, etc.).

Key check: `Poincare/D7/Recognition/*` in D12-surgery-recognition vs the accepted D7-sphere-recognition worktree (or whatever produced it). And `Poincare/D10/JacobiConstantCurvature/*` vs D10-jacobi-constant-curvature worktree. And VKPort/HatcherLib vs upstream frenzymath snapshot (formalized-sources/Hatcher).

This is a strong adversarial audit angle: cards may vendor modified dependencies. Let me compare.

Note: the builds are running; I can do file comparisons concurrently.

Let me find the origin worktrees:
- D7-recognition modules: search worktrees for Poincare/D7/Recognition/Basic.lean.
- D10 Jacobi: D10-jacobi-constant-curvature.
- VKPort HatcherLib: search worktrees; also upstream snapshot has formalized-sources/Hatcher.
dsh: reasoning:
There are many worktrees with these modules; to determine "the accepted origin" I'd need the promotion chain. A robust approach: compare the vendored copies across all worktrees — if they're all identical, provenance is consistent. If differences exist, identify which.

Let me write a quick script to hash and compare a few key files across worktrees:
1. `Poincare/D7/Recognition/*.lean` across the D12-surgery package and some D12/D11 worktrees.
2. `Poincare/D10/JacobiConstantCurvature/*.lean`.
3. `Poincare/VKPort/**` between D12-surgery and D13-vankampen.

Let me do that now.
dsh: reasoning:
No mismatches: the vendored copies (D7/Recognition, D10/JacobiConstantCurvature, VKPort) are byte-identical across all worktrees that contain them. Good provenance consistency.

Now let me check the VKPort source against the upstream frenzymath snapshot (Hatcher formalization) — licensing/adaptation. Also check whether VKPort files contain the allowed axioms. The build log showed `HatcherLib.vanKampenMap_surjective depends on axioms: [propext, Classical.choice, Quot.sound]` — fine.

Now let's check build progress.
dsh: reasoning:
Wait, the .rc files show 1 for connection-curvature, spectral-sobolev, volume-ibp — but those might be from the earlier failed run (the rc files are overwritten by the new run only when it completes). Actually the new run writes rc after build completes. The build logs show active progress for those three, so the rc=1 files are stale from the first failed attempt. Fine.

Interesting: connection-curvature's own audit module output appears in the build log and PASSes. Good, but my independent probe is what counts.

While waiting, let me review the semantic content. Let me look at the key claims of each card. Start with surgery-recognition (3 blocker closures) since that's the most consequential.

Let me read the surgery card md and its key Lean statements.
dsh: reasoning:
The surgery card is thorough. Key claims to verify independently:

1. `ConnectedSumDecomposition.mkV2` produces a genuine D7 `ConnectedSumDecomposition` whose `sphere_of_spheres` field is *proved*. Need to check: is `mkV2` just packaging `ConnectedSumDecompositionV2` data (which itself has `sphere_of_spheres` as a field) into V1, i.e., is the SR-5 blocker actually closed or just *relabeled*? The card says V2 carries actual gluing data. Let me read `SphereOfSpheres.lean` around lines 150-242 to see the definitions of ConnectedSumDecompositionV2 and mkV2, and what `sphere_of_spheres` requires.

The D7 `ConnectedSumDecomposition` structure (vendored in Poincare/D7/Recognition/Basic.lean line 169) has fields; `sphere_of_spheres` is presumably a field requiring X ≃ₜ S³ if all pieces are spheres. In V2, the structure might take the decomposition data with a *proof* field. The card claims `mkV2` builds V1 from V2 with the field proved. But if V2 itself has `sphere_of_spheres` as a hypothesis/field, then mkV2 is just projection — a relabeling, not a closure. Need to check.

2. `finiteFreeOrbit_isQuotientCoveringMap` — a real theorem with constructed evenly covered neighborhoods. Need to verify statement isn't vacuous: requires `ContinuousSMul Γ S3` or `ContinuousConstSMul`, Free action, finite group. Should be genuine.

3. `deckTrivial_of_simplyConnected_quotient`: `Subsingleton M.Γ` from `SimplyConnectedSpace M.quotient.Carrier`. Need to check that M.quotient is the orbit quotient and that `Subsingleton Γ` follows (i.e., the theorem isn't trivially true because `SphericalSpaceFormModel` requires Subsingleton as a field!). That's a critical adversarial check: if `SphericalSpaceFormModel` has a field asserting triviality or has `[Subsingleton Γ]`, the theorem is vacuous. Let me inspect.

Let me read the relevant Lean code.
dsh: reasoning:
Let's compare with the D7 `ConnectedSumDecomposition` structure to see whether `mkV2` genuinely closes the SR-5 field or merely relocates it.

Let me read Poincare/D7/Recognition/Basic.lean around line 169.
dsh: reasoning:
Now the adversarial question for SR-5: The original D7 `sphere_of_spheres` field was a *missing input* — a function `(∀P∈pieces, P ≃ₜ S³) → X ≃ₜ S³`. `mkV2` produces this function from V2 data, but V2's `sumHomeo` field is `Nonempty (X ≃ₜ iteratedSphereSum pieces pieceSphere)`. This is a *construction*-level assumption: it says "X is homeomorphic to the iterated connected sum of the pieces" as data. In the D7 structure, that geometric fact was hidden in the opaque `relation : Prop` field (which had no content). So mkV2 replaces the opaque relation with actual data (`sumHomeo`), and derives `sphere_of_spheres` from `iteratedSphereSum_homeo_sphere` (a real theorem: connected sum of spheres ≃ S³).

Is this a genuine closure? Per the D12-plan acceptance: "An existing named missing input is constructed under the original intended geometric assumptions, consumed by a downstream theorem, and independently rebuilt. A new alias, Prop, projection or stronger hypothesis does not close a blocker."

Assessment:
- The missing input `sphere_of_spheres` in D7 was: given the decomposition, prove X ≃ S³ from pieces ≃ S³. The new V2 data supplies `sumHomeo : X ≃ iteratedSum` (the connected-sum identity, now actual data rather than an opaque relation) and `pieceSphere`. Then `sphere_of_spheres` follows from the proved `iteratedSphereSum_homeo_sphere` (which itself rests on `sphereConnectSum_homeo_sphere : S³#S³ ≃ S³`, a genuinely proved topological theorem in BallGluing/ConnectedSumTopology, using `doubleBallHomeoSphere` — need to verify these are genuine, not trivially true).
- However: the closure depends on V2 data that must be constructed for a given X. `mkV2` requires `sumHomeo` — X is homeomorphic to the iterated sum. Is that a "stronger hypothesis" than the original blocker? The original D7 `relation` was opaque (no content). V2's `sumHomeo` is a real geometric identification. In the intended application (X is the terminal configuration of the flow, which by the surgery analysis is the connected sum of terminal pieces), `sumHomeo` is exactly the geometric input that must come from the surgery construction — it is NOT proved here; it's carried as data. So SR-5 ("connected sum of sphere summands is S³") IS proved (via iteratedSphereSum_homeo_sphere), while the identification "X is the connected sum" remains data (as it must be). That's a fair closure of SR-5 specifically, with the caveat that the closure is conditional on V2 data being constructible in the end-game setting — the card says "The van Kampen field simplyConnected_pieces remains an explicit hypothesis".

Wait, one subtlety: `mkV2`'s `sphere_of_spheres` requires `Nonempty (X ≃ iteratedSum)`. But `sphere_of_spheres`'s premise is `∀P∈pieces, Nonempty (P ≃ S³)`. In `mkV2`, the proof uses `D.pieceSphere`, ignoring the premise (`intro _hpieces`). That's fine (stronger).

But is `SphereThree` the same as `S3`? In D7 Recognition/Basic, `SphereThree` is probably defined as `Metric.sphere 0 1` in ℝ⁴ or the `TopSpace` wrapper. In D12 files they use `S3`; let me check they're defeq. The build passes, so it typechecks.

Another subtlety: does `iteratedSphereSum_homeo_sphere` genuinely prove the iterated connected sum? The `SphericalSum` structure's `sumHomeo` is built by recursion using `sphereConnectSum_transported`, which uses `connectSumCongr` and `sphereConnectSum_homeo_sphere`. Need to verify `sphereConnectSum_homeo_sphere` is a real theorem (S³#S³ ≃ S³) and not vacuous. The card says it uses `southPunctureHomeo` and `doubleBallHomeoSphere`. Let me check `connectedSum` definition — is it a genuine quotient of punctured spheres? If `connectedSum` were defined to always be S³ by fiat, that would be cheating. Let me read ConnectedSumTopology.lean and BallGluing.lean key definitions.

Let me check:
- `def connectedSum` (quotient construction)
- `doubleBallHomeoSphere`
- `sphereConnectSum_homeo_sphere` statement/proof.

Also verify the axiom audit covers these.

Let me read those.
dsh: reasoning:
`connectedSum` is a genuine quotient of X ⊔ Y by a relation. Let me check `connectSumRel` and the sphere cases. It's plausible.

Now let me check `sphereConnectSum_homeo_sphere` and `doubleBallHomeoSphere` statements.
dsh: reasoning:
`sphereConnectSum_homeo_sphere` isn't in ConnectedSumTopology.lean; it's probably in SphereOfSpheres.lean. Let me grep all files.
dsh: reasoning:
Note: `sphereConnectSum_homeo_sphere` is a `def` (a term of type `Nonempty (...)`), not a `theorem` — still a proved term. `doubleBallHomeoSphere : DoubleBall ≃ₜ S3` is a def building an explicit homeomorphism. That's fine.

Now the deeper adversarial check on the homeomorphism defs: Could `DoubleBall` be a trivial quotient making the homeomorphism easy? `DoubleBall := Quot glueRel` gluing two closed balls along boundary. The homeomorphism is explicit via hemisphere maps. Building a `Homeomorph` requires `toFun`, `invFun`, `left_inv`, `right_inv`, `continuous_toFun`, `continuous_invFun` — so it's genuine.

However, a subtle issue: `Ball3` might be defined as `Metric.closedBall 0 1` in ℝ³ and `S3` as unit sphere in ℝ⁴. The gluing relation identifies boundary points via some homeomorphism S²→S². Fine.

OK, SR-5 closure looks genuine but conditional on the V2 data (sumHomeo). The card's claim "constructor mkV2 ... sphere_of_spheres field is proved" is accurate in the sense that the field's *proof* no longer is an input; but the geometric fact "X is the connected sum" is V2 data rather than proved. The card explicitly says this. So: CONFIRMED with qualification.

Now the third closure: `deckTrivial_of_simplyConnected_quotient`. Let me read `SphericalSpaceFormModel` in CoveringRecognition.lean and DeckTrivial.lean.
dsh: reasoning:
`SphericalSpaceFormModel` has no triviality field — good, non-vacuous. `deckTrivial_of_simplyConnected_quotient (M) [SimplyConnectedSpace M.quotient.Carrier] : Subsingleton M.Γ`. That's a real statement (for the antipodal model, the quotient ℝℙ³ is not simply connected, so no contradiction; the theorem is not vacuously true since e.g. Γ finite acting freely with simply connected quotient forces trivial deck group — true since π₁(quotient) = Γ for free actions... actually for a free finite action of Γ on a simply connected space, Γ ≅ π₁(quotient), so simply connected quotient ⇒ Γ trivial. Correct math.)

Wait: is it true that for a free finite group action on a simply connected space, the quotient being simply connected implies Γ trivial? Yes: the quotient map is a covering with deck group Γ; the monodromy action gives π₁(Q) acting on the fiber Γ, and since Γ acts transitively on the fiber, if π₁(Q) is trivial, the fiber is a single point, so Γ is trivial. Correct. Note: `SimplyConnectedSpace` in mathlib means path-connected + subsingleton π₁? Actually `SimplyConnectedSpace X` = `PathConnectedSpace X` ∧ `Subsingleton (FundamentalGroup X x)`. The theorem requires `[SimplyConnectedSpace M.quotient.Carrier]`.

Now, is `M.quotient.Carrier` reducible to `MulAction.orbitRel.Quotient M.Γ S3`? Yes by def. Fine.

Next closure: covering-space recognition `finiteFreeOrbit_isQuotientCoveringMap (Γ) [Group Γ] [Fintype Γ] [MulAction Γ S3] (free : ...) : IsQuotientCoveringMap (Quotient.mk ...) Γ`? Let me check the exact signature (does it require ContinuousConstSMul?).

Let me read the statement.
dsh: reasoning:
These look like genuine theorems. `finiteFreeOrbit_isQuotientCoveringMap` requires `[ContinuousConstSMul Γ S3]` and the free hypothesis; the proof uses `IsQuotientCoveringMap` fields including disjointness. Note: does `IsQuotientCoveringMap` require `toContinuousConstSMul : ContinuousConstSMul Γ S3` — the structure is presumably the "quotient covering map" with deck group Γ. This is a real definition in some worktree (maybe locally defined in CoveringRecognition.lean). I should check `IsQuotientCoveringMap` is a genuine covering notion and not a bespoke Prop with weak fields. Let me check its definition.

Also `spaceFormFiberEquivGroup` etc.

Let me look at IsQuotientCoveringMap definition.
dsh: reasoning:
`IsQuotientCoveringMap` isn't defined in the package — it must come from mathlib? Let me grep the mathlib package. Actually mathlib has `IsQuotientCoveringMap` in `Mathlib.Topology.Covering`? Let me check.
dsh: reasoning:
`IsQuotientCoveringMap` is mathlib's genuine structure. Good.

Now: the card claims `finiteFreeOrbit_isQuotientCoveringMap` has a complete proof. My build will verify compilation; the axiom probe will verify no sorry.

Next, check the base file `LeviCivitaBlocked.lean` statement and closure (D12-connection-curvature). Then check the other cards' headline claims quickly.

Let me look at the build status first — it's been a few minutes.
dsh: reasoning:
All 7 builds succeeded (exit 0) in ~2 minutes each. Excellent. Now the probes are running (`lake env lean A3Probe.lean`). Let me wait and check probe results. Meanwhile, continue semantic review.

Let me check probe logs now.
dsh: reasoning:
All probes failed with rc=1 quickly (17 lines). Let me see the error — probably the same "unknown module prefix" issue or maxHeartbeats / import problem.
dsh: reasoning:
As anticipated: some root-level modules (D6LedgerProbe, ProbeScratch, ReleaseClaims, etc.) aren't built by default targets, so their oleans don't exist.

Fix: exclude modules that aren't covered by the package's lean_lib globs from the import list. The lean_libs are: Poincare (globs Poincare.+), Probe (Probe.+), Ledger (Ledger.+), Audit (Audit.+), ReleaseCheck, ReleaseAudit, D6AuditReport. So root modules like D6LedgerProbe, ReleaseClaims, ProbeScratch are not covered.

Simplest: build the missing modules explicitly. `lake build D6LedgerProbe` — but lake may not know the target. Alternative: exclude them from imports. Since proved_declarations live in D12 modules (and Audit), excluding non-target root drivers is safe — but I must check none of the card's declarations are declared in those files. For volume-ibp, `ProbeScratch.lean` likely contains no card declarations (it's a scratch probe). My resolution step already only maps declarations to source files; I can restrict the probe imports to modules that contain at least one resolved declaration OR are dependencies. But dependencies are handled automatically by Lean imports.

Better approach: for each resolved declaration, I know its FQ name, but not its file. I can find the file by scanning: map fq → file during scan. Then import only the files declaring the needed names (plus transitively their imports — Lean handles that automatically through the `import` statements in those files). This is cleaner and faster.

So modify prepare.py:
- scan_decls returns fq→fq; change to also record file: return dict fq → relpath (module name).
- The probe imports the modules declaring the resolved names + module of any extra declaration.
- Keep module list as fallback.

Also the declarations might be in modules that import others; fine.

Let me update: `scan_decls(path)` returns dict fq → module name. Then when resolving, record `module`. Probe imports = sorted set of modules for all resolved declarations. If a declaration is a structure field or instance elsewhere... the scanner should find the declaration line.

For semantic-ledger, the two extra declarations are in A3Extra/D12RealModuleProbe.lean which imports many modules — the extra probe file will compile them.

Let me patch prepare.py and re-run (rebuild not needed since sources are built; probe only). I need to regenerate A3Probe.lean then run `lake env lean A3Probe.lean` for each card.

Let me patch.
dsh: reasoning:
Wait — prepare.py re-copies sources (deleting dst and re-copying), which wipes the compiled `.lake/build`? Let me check: `copy_sources` does `shutil.rmtree(dst)` — that deletes the whole package including `.lake/build`! So the builds I just did are gone. Damn.

I need to avoid re-copying on re-run. Let me modify copy_sources to preserve `.lake` (or only copy files that changed). Simpler: add a flag `--no-copy` or check if dst exists and skip copying (but I want fresh copies conceptually — the first run already made them; the second overwrote and then re-copied, losing build artifacts).

Let me check whether .lake/build survived: `ls audit360/pkgs/D12-volume-ibp/.lake/`. The second prepare run (the one just now) deleted and re-copied, so .lake/build is likely gone (only the packages symlink is recreated by the script). We'd need to rebuild (2 min each, fine). But better: stop re-copying. Let me patch copy_sources to not delete `.lake`, and then rerun only the probe (no recopy) — but the damage is done for this run.

Plan:
1. Patch prepare.py: preserve `.lake` (skip rmtree of `.lake`; remove individual files instead).
2. Re-run prepare (re-copies sources but keeps .lake/build? The build artifacts would be stale/invalid if sources changed — they didn't change, so oleans remain valid; lake will rebuild only if hashes differ).
3. Also verify that the copy still contains identical sources to the originals (hash check again) — prepare does record copy hashes.
4. Re-run run_card.sh for all 7 (it will rebuild quickly since oleans exist, then run probes).

Actually simpler: since sources are byte-identical (the re-copy produces identical bytes), the .olean files from the previous build are still valid. But they were deleted by rmtree. Let me check if .lake survived at all — no, rmtree removed it. Ugh.

Let me just re-run builds after fixing the script; 2-3 minutes each is acceptable.

Let me patch copy_sources: 
```
def copy_sources(src, dst):
    # remove everything except .lake
    if os.path.exists(dst):
        for entry in os.listdir(dst):
            if entry == '.lake': continue
            p=os.path.join(dst,entry)
            shutil.rmtree(p) if os.path.isdir(p) else os.remove(p)
    else: os.makedirs(dst)
    ...
```
Then re-run prepare, then run_card.sh for all 7 again.

Actually, since the present source copies are already correct and only A3Probe.lean changed, I could simply run the probes without re-copying. But `run_card.sh` runs `lake build` first, which will rebuild the missing oleans. So: patch script, re-run prepare (which now preserves .lake — but .lake/build is already gone; prepare will just recreate the symlink), then run_card.sh.

Let me do it.
dsh: reasoning:
Builds are running again. Meanwhile, let me continue the semantic audit.

Check the LeviCivitaBlocked statement — the named blocker being "closed". Let me read it in the D12-connection-curvature package and compare with the original in a pre-D12 worktree (e.g., D7-levi-civita-smooth release) to ensure it's unchanged. Earlier diff vs D6 showed no base file differences, so it's the same as D6. But is D6's version the "original blocker"? The card says blocked in `Poincare.Longrun.Geometry.LeviCivitaBlocked`. Let me read it.
dsh: reasoning:
Now let me see the actual `LeviCivitaExistenceStatement` definition:

It's likely:
```
def LeviCivitaExistenceStatement (m : MetricData V ι) (b : LieBracketData ℝ V) : Prop :=
  ∃ nabla : V →ₗ[ℝ] V →ₗ[ℝ] V, IsLeviCivita m b nabla
```

Let me check. Also check the claim in the docstring "it is false without extra hypotheses such as invariance" — the D12 card claims this docstring claim is wrong: existence always holds (Milnor connection). Is that mathematically correct? For a finite-dimensional real vector space with a positive-definite inner product and any Lie bracket, does there always exist a torsion-free metric-compatible connection? The Milnor formula gives exactly that. Yes: define ∇_X Y = ½([X,Y] − (ad_X)^T Y − (ad_Y)^T X). This is torsion-free and metric-compatible. This is classical (Milnor 1976). So the original docstring's claim is indeed incorrect (though the docstring hedged "false without extra hypotheses"). Fine.

So the closure is mathematically genuine IF the definitions are as expected. Let me verify.
dsh: reasoning:
The Levi-Civita closure looks genuine:
- `LeviCivitaExistenceStatement m b := ∃ nabla, IsLeviCivita m b nabla` — unchanged (base module identical to D6).
- `leviCivitaExists` proves it unconditionally via Milnor's formula.
- The proof of metric compatibility is a bit terse (`rw ... ring`), but it compiles and the axioms will be checked.

One adversarial check: `adTranspose_apply_form` and `adTranspose_apply_form_swap` must correctly characterize the transpose, and `raiseIndex` must be a genuine dual (requires nondegenerate form). The proofs of those lemmas were resolved and audited. Since the final statement is exactly the blocker and the witness is explicit, this closure seems CONFIRMED, assuming no axiom cheats (probe will verify).

Downstream use: card claims `so3MeanLeviCivita -> ricci_symm and so3_ricci_e00`. Let's verify: `so3MeanLeviCivita` exists (resolved) and `so3_ricci_e00 : ricci K e0 e0 = 1/2`. Also `ricci_symm` consumes `milnorLeviCivitaData → toCurvatureOperator`. Let me check `ricci_symm`'s statement and how it obtains the connection: `ricci_symm` presumably takes a `LeviCivitaData` and proves Ricci symmetry using first Bianchi + skew-adjointness. The "downstream checked use" is the so3 instance.

Is that enough for "downstream checked use"? The blocker closure criterion says "consumed by a downstream theorem". `so3MeanLeviCivita` uses the Milnor construction; `so3_ricci_e00` computes a nonzero Ricci value. Yes.

Now, let me check the build/probe progress.
dsh: reasoning:
These are stale from the earlier run; the current builds are running (compiling ~8900 jobs each). Let me wait for the builds to finish and check. Meanwhile, continue semantic review of other cards.

Let me look at the other two cards with interesting claims:
- D12-spectral-sobolev (TASK_DONE, 36 declarations, "general within the 1-torus model").
- D12-comparison-geodesics (Sturm comparison etc.).
- D12-volume-ibp (chart IBP, no blocker closed).
- D12-geometric-compactness (in_progress status but has 39 declarations).
- D12-semantic-ledger (audit card, claims validating D11 defect and recounting blockers).

The adversarial question for A3 (residual audit finding): are any statements false/vacuous/overstrong? Let me focus on headline theorems of each card and check for vacuity:
1. A theorem whose hypotheses are unsatisfiable (vacuous).
2. A theorem whose conclusion is trivially true.
3. A theorem that assumes the conclusion.

Let me examine each card's md "what is proved" section and key statements. Let me start with volume-ibp because it's the one whose semantic class is "model".

Also the task says "rebuild from source hashes, re-run axiom audits, confirm/refute exact_blockers_closed". The core deliverable is the audit card. Semantic review depth: check the main claims.

Let me read the D12-volume-ibp card md sections and headline statements.
dsh: reasoning:
The volume-ibp card is chart-level (model/conditional) with no blocker closures claimed. Its scope is honest. Key theorems to verify are real (chart IBP). My probe will confirm axioms. A semantic check: `chart_ibp` hypotheses (C², compact support) and conclusion — and whether `riemannianMeasure` is actually built (not assumed). The card describes it accurately.

Now let me look at the comparison-geodesics card and spectral card briefly, and check for overclaiming. Then check probes.

Actually, time management: it's been ~35 min since start (12:04 → ~12:40). I need to update checkpoint.json every 30 min. Let me write an initial checkpoint now with the audit status, then continue.

Let me first check probe progress.
dsh: reasoning:
Builds are near completion (~8945/8957). Let me write a checkpoint.json now (per instructions every 30 min), then check probes.

Checkpoint content: task id, status, approach, progress, findings so far, next steps.

Let me write checkpoint.json in the worktree root.
dsh: reasoning:
Wait, the timestamps say 12:34 but current time should be ~12:45. Are these the new probe runs? The probe logs from the previous failed run had timestamps 12:31. Now 12:34 with rc=0. But I relaunched at ~12:38? Hmm, the driver was launched after the rebuild started... Let me check the full probe log content. Actually the sequence: I relaunched run_card.sh at ~12:33 (after prepare), builds took ~2 min... but the rc=0 at 12:34 — that seems too fast for a full rebuild after rmtree deleted .lake/build. Unless the rebuild reused... hmm, `.lake/build` was deleted by the first prepare re-run, then the second prepare run preserved `.lake` (which had only `packages`). Wait no: timeline:
- 12:29 first builds ran and completed (12:31) — .lake/build created.
- 12:31 probes failed.
- ~12:36 I patched prepare (preserve .lake) and re-ran prepare → this deleted all non-.lake entries and re-copied, .lake/build preserved? No! The FIRST prepare re-run (before the preserve patch) already rmtree'd everything including .lake/build at ~12:35. So .lake/build was gone.
- Then I relaunched run_card.sh at ~12:37. Builds take ~2 min... but the probe log says 12:34, which is before.

Hmm, maybe the timestamp is from the old logs (not overwritten because the new run is still in build phase). The `.probe.rc` file still contains old values (rc=0? no, old was rc=1). Confusing.

Let me look at the actual log contents and mtimes.
dsh: reasoning:
The probes ran at 12:34 and all exited 0. (Current time 12:35 — so the rebuild after the rmtree must have been fast because `lake build` recompiled using the shared oleans... wait, the build logs need checking; regardless, builds were rc=0 and probes rc=0.)

Hmm wait: the rebuild at ~12:33 completed by 12:34 — only 1-2 minutes for 8957 jobs? That's plausible with 192 cores and mathlib prebuilt; the earlier builds took ~100 seconds. Fine.

Now let me analyze the probe outputs:
1. For each card: count `#print axioms` lines, extract cones, check ⊆ allowed.
2. Check `#check` output for errors (rc=0 means no errors, but let me verify no "error" lines).
3. Cross-check counts vs declared counts.

Let me write the analyzer `audit360/analyze.py`. It should produce per-card:
- probe rc, error count
- declarations checked (from #check output)
- axiom lines parsed: name → cone
- violations: cone not subset of {propext, Classical.choice, Quot.sound}; names missing from probe output
- forbidden-token scan over copied sources (comment/string-aware)
- sha256 of the probe log for evidence
- also parse extra probe logs (semantic-ledger)

Then produce an `audit360/audit_result.json`.

For the forbidden scan, I'll write a Python scanner that removes comments and string literals (approximately) then searches for tokens: `sorry`, `admit`, `axiom`, `unsafe`, `native_decide`, `proof_wanted`, `implemented_by`, `extern`, `set_option` (report separately). I must be careful about false positives in names like `sorryAx` (that's what we want to catch) and words inside identifiers. Use word-boundary regex.

Note the producers' own audit modules contain prose in docstrings mentioning these tokens; comment stripping handles that.

Let me also implement a negative control: create a small Lean file with `sorry` and `native_decide`, compile it in one of the package environments, and verify:
- the forbidden-token scanner flags it, and
- the axiom probe on those declarations shows sorryAx / native_decideDecide axioms.

Let me write the analyzer + negative control now.

Also, my axiom probe `#print axioms` lines: for `#check` output, each declaration prints its type. The `#print axioms` output format:
- `'Foo' depends on axioms: [propext, Classical.choice, Quot.sound]`
- `'Foo' does not depend on any axioms`

Let me parse.

Let me look at a sample of the probe output tail first.
dsh: reasoning:
The probe output is good: 78 axiom lines for 78 declared names. Cones: 59 with the standard cone, and the rest are `#check` type annotations (instance brackets) not axiom cones.

Now let me write the analyzer. It will:
- For each card: parse probe log, extract decl→cone.
- Check all resolved names present.
- Check cones ⊆ allowed.
- Check no error lines.
- Forbidden token scan of package sources (only card-authored files? I'll scan all .lean in the package, but exclude vendored/base or flag separately). Better: scan the *card's own modules* (those under the D12 module dir, plus the vendored modules separately). Actually forbidden tokens anywhere in the compiled package would be a finding. Let me scan all .lean files in the audit copy, and report per-file.
- Also detect `set_option` overrides and `@[implemented_by]`/`@[extern]`.

Also: does `#print axioms` catch `sorryAx`? Yes, it lists `sorryAx` in the cone. The negative control will confirm.

Let me write analyze.py with:
1. `parse_probe(log)` → dict.
2. `scan_forbidden(path)` → list of (file, line, token).
3. Negative control compile + probe.

For the negative control, create `audit360/negcontrol/A3NegativeControl.lean`:
```lean
import Mathlib

theorem a3_neg_sorry : (1:ℕ) = 1 := by sorry
theorem a3_neg_native : (List.range 3).length = 3 := by native_decide
```
Then run `lake env lean ../negcontrol/A3NegativeControl.lean` inside the volume-ibp package (needs to be in the package dir but can reference a path outside; `lake env lean` accepts a file path). Its imports resolve.

Then `#print axioms a3_neg_sorry` and `a3_neg_native` should show sorryAx / Lean.ofReduceBool? Let me append those print commands to the file itself so the output includes the cones.

I'll create the negative control with appended #print axioms lines.

Then the analyzer checks that the audit predicate (cone ⊆ allowed) FAILS for these two, proving fail-closed behavior. Note: `native_decide` produces axiom `Lean.ofReduceBool` (and `Lean.trustCompiler`), which is not in the allowed set.

Let me write these.
dsh: reasoning:
Results:
- Negative control works: `sorry` → sorryAx; native_decide → private axiom. Good, fail-closed predicate validated.
- All cards FAIL my strict predicate due to:
  1. `audited < declared` — many declared names missing from probe output. Why? Because `#print axioms` for declarations that are *structures/inductives/classes* may print nothing? Or because some names are duplicates/shadowed? Let me inspect.
  2. `forbidden hits` (3-5 per card) — likely false positives from my comment stripper (docstrings with `/-!` nesting? or tokens inside identifiers like `sorry` in `AxiomAudit.lean` prose that I didn't strip because it's in a `/- ... -/` block that my stripper handles... hmm). Let me inspect the hits.

Let me look at both issues.
dsh: reasoning:
Two problems to fix in my analyzer:

1. Missing declarations: `#print axioms` prints nothing for `def`s? No — volume-ibp's `matrix` (a def) printed a cone. Wait, the missing ones for surgery include `sphereConnectSum_homeo_sphere` which is a `def` (Nonempty ...), `mkV2` (def), `stage6Target_*` (theorems). Why missing?

Hypothesis: My probe prints `#check` for all names then `#print axioms` lines. But `#print axioms` output might be suppressed when the declaration's axioms are... no. Let me grep the actual probe log for one missing name.

Actually — I bet the issue is that these are *later* in the probe file and the log got truncated? No, rc=0.

Another hypothesis: `#print axioms` for declarations that are *opaque/def with no proof*? Let me check one: `Poincare.D12.VolumeIBP.ChartMetric.integral_riemannianMeasure_eq`. Let me grep the probe log.

2. Forbidden hits: 
   - `unsafe` in D6AuditReport.lean / ReleaseAudit.lean / D12Audit.lean at specific lines — these are likely strings in audit code like `"unsafe"` (the audit script checks for the token) or field names. My string-literal stripper should remove `"unsafe"` strings... unless they're in identifiers like `unsafeDecls` or `hasUnsafe`. Let me check the lines.
   - `axiom` in Poincare/D12/VolumeIBP/Audit.lean line 12 — likely a docstring line mentioning "axiom" that my stripper missed because it's inside a `/-! ... -/` that starts at the file top; my stripper handles it... unless line 12 is inside a `--` comment? It handles `--`. Let me check the line.

Let me examine.
dsh: reasoning:
Two findings:

1. Missing declarations were a parsing artifact: `#print axioms` output is *line-wrapped* (e.g., `'Name' depends on axioms: [propext,` then continuation ` Classical.choice, ...]`). My regex requires the closing bracket on the same line. I need to join continuation lines. Fix: accumulate lines — when a line matches `^'...' depends on axioms: [` without closing `]`, keep appending until `]`.

2. Forbidden hits are false positives:
   - `unsafe` at D6AuditReport.lean:43? Line 43 shown is `def isProjectModule...` — hmm, the line numbers in the stripped text don't correspond to the original file because stripping removes comment chunks but keeps newlines? My stripper counts lines in the *stripped* code, and block comments are removed including their newlines (I `i += 2` for delimiters but keep scanning inside the comment without appending, so newlines inside comments are dropped!). That shifts line numbers. I should preserve newlines when stripping. Also the token `unsafe` appeared somewhere after line ~43 in stripped coordinates. Let me fix the stripper to preserve newlines for all removed text.
   - The `axiom` hit in volume Audit.lean line 12 is inside a `/-! ... -/` docstring — my stripper should have removed it. Wait, after stripping, line 12 doesn't correspond. The token `axiom` likely appears later in a genuine comment or in a string; with line preservation I can check.

Let me fix:
(a) stripper preserves `\n` in comments and strings.
(b) probe parsing joins wrapped lines.
(c) Also handle the `--` comment removal preserving newline.
(d) For docstring text like "`#print axioms` cone" inside `/-! -/`, stripping removes it. Good.

Let me also double check: `AxiomAudit.lean` in spectral has `#print axioms` lines (intended, producing cones) — those are commands, not the token "axiom" as a declaration. My forbidden scan looks for exact word `axiom` — `#print axioms` contains "axioms" which won't match `axiom` due to the negative lookahead `(?![A-Za-z0-9_'])` — "axioms" has 's' after, so no match. Good.

But the producer's own Audit.lean files contain `#print axioms` commands — fine.

Also, my scan flags `unsafe` in the *base* D6 audit modules (D6AuditReport.lean, ReleaseAudit.lean) which might genuinely contain the identifier `unsafe`? Let me check after fixing the stripper.

Let me patch analyze.py.
dsh: reasoning:
Excellent progress! Now:
- 6/7 cards PASS the axiom probe fully (all declared names audited, cones within allowed, negative control validates the predicate).
- connection-curvature: 118/119 audited — one name missing. Likely the `metricDerivative_christoffel'` with apostrophe (parsing issue) or the ambiguous `curvatureEndo` (resolved to a candidate; maybe the probe printed it but parse failed due to apostrophe in name). Let me check.
- forbidden hits (3-5 per card) still present. Let me inspect them now with fixed line numbers.

Let me check both.
dsh: reasoning:
Findings on forbidden tokens:
1. `.«unsafe»` in D6AuditReport.lean / ReleaseAudit.lean / D12Audit.lean — these are *audit code* matching on the constructor name `DefinitionSafety.unsafe`. The token appears as `.«unsafe»` (guillemet-quoted identifier), not an unsafe declaration. This is a false positive of my scanner. I should treat `.«unsafe»` as a quoted identifier reference (not a declaration/attribute). Also `unsafeDefs` identifier — my word-boundary regex prevents matching inside `unsafeDefs` (followed by 'D', so no match). The match is on `.«unsafe»`, where the char before is `«` (not alnum) and after is `»` (not alnum) → matches. I'll refine: exclude matches immediately surrounded by `.«` and `»`.

2. `axiom negativeControl : False` in D12-volume-ibp's `Poincare/D12/VolumeIBP/Audit.lean` line 40 — this IS a real `axiom` declaration inside the package! The comment says it's an intentional negative control ("Do not use in any proof"). It's in the audit module, not used by any theorem. This is legitimate as a negative control, but I must verify:
   - The axiom is not used by any audited declaration (axiom cones show no `negativeControl`, and the axiom's name would appear in cones if used).
   - It's explicitly documented as a negative control.
   My audit result should record it as an intentional negative control, verified unused.

So I should refine the forbidden scanner:
- Exclude `.«unsafe»` and `.«partial»` guillemet identifiers (audit metaprogramming).
- Record `axiom` hits but classify by context: if the declaration name is in a known negative-control list... better: verify no audited cone contains it. Since my probe cones are all ⊆ allowed, the axiom is unused. I'll classify "axiom negativeControl : False" as an intentional, unused negative control and not a violation.

Also, the missing name `metricDerivative_christoffel'` — apostrophe in the name. Let me check the probe log for it. The parser might fail because the `#check` output or `#print axioms` line wraps. Let me grep.

Let me check.
dsh: reasoning:
The issue: Lean's `#print axioms` output for the primed name uses `''` (doubled apostrophe) as an escape: `'...christoffel'' depends on axioms:` — the actual output has the name ending with `'` and the quote delimiter, giving `''`. My regex `^'([^']+)'` captures up to the first `'`, giving the name without the prime, then expects ` depends` but sees `' depends` → no match. So the primed declaration's cone isn't captured.

Fix: match the name more flexibly. The format is `'NAME' depends on axioms: [...]` where NAME may contain `'`. Since the delimiter is `' ` (apostrophe followed by space then "depends"), I can regex: `^'(.+?)' (depends on axioms:|does not depend)`. With `.+?` non-greedy and requiring `' ` before "depends", it should capture `christoffel'` correctly. Actually for the primed name, the raw is `'...christoffel'' depends on axioms:` — the regex `^'(.+?)' (depends...)` would match NAME = `...christoffel'` because the second-to-last `'` is followed by `'` not space; the regex requires `' ` (apostrophe+space) so it continues to the final `'` before space. Good.

Let me update AXIOM_RE to `^'(.+?)' (depends on axioms: \[([^\]]*)\]|does not depend on any axioms)`.

Hmm, careful: `(.+?)` non-greedy with the alternative requiring either `' depends on axioms: [...]` or `' does not depend...`. Fine.

Also update the forbidden scanner to:
- ignore `.«unsafe»`/`.«partial»` quoted idents.
- classify `axiom` declarations: collect their names and check they're unused (appear in no cone). I'll add a `negative_control_axioms` field with the declared axiom names and verify unused.

Let me patch analyze.py.
dsh: reasoning:
All 7 cards now pass the independent axiom audit:
- 119+78+36+7+33+39+30 = 342 declared names, all audited.
- 0 cone violations.
- Negative control flagged (sorryAx and native-decide private axiom).
- volume-ibp's 1 forbidden hit is the intentional `axiom negativeControl : False` in its Audit.lean, verified unused (unused_in_cones) — but wait, the ok predicate requires `not blocking_forbidden`, where blocking_forbidden = axiom hits with not nc_axioms_unused. Since it's used-only... let me verify the field `axiom_declarations_unused_in_cones` contains negativeControl. Let me check.

Hmm, careful: `used_names` is built from `all_cones.values()` — the *axiom names* appearing in cones. `negativeControl` doesn't appear (no cone contains it). So nc_axioms_unused = ['negativeControl'], and blocking_forbidden = [] → PASS. Good. But I should double check that `negativeControl` is genuinely not used anywhere: my probe only audits the *declared* names, not the whole package. A builder theorem could use `negativeControl` without being listed in proved_declarations. However, if any of the listed theorems used it, its cone would show. To be thorough, I should check the axiom is referenced nowhere outside its declaration — grep the sources for `negativeControl` and confirm only Audit.lean. Let me do that.

Also, connection-curvature cone for the primed name now parsed (119/119).

Next steps:
1. Verify the `negativeControl` axiom usage.
2. Semantic review / vacuity checks.
3. exact_blockers_closed verify (already partially done).
4. Write results.

Let me check negativeControl usage and also get a summary of cones per card (how many declarations depend on no axioms etc.).
dsh: reasoning:
All cones are exactly `{propext, Classical.choice, Quot.sound}` — within allowed. `negativeControl` is only declared and printed in Audit.lean, never used elsewhere. 

Note: all cards' declarations depend on the full standard cone; that's typical for mathlib-based proofs. Fine.

Now let me do the semantic review. Key areas:

A. exact_blockers_closed confirmation/refutation (4 claims):
   1. connection-curvature LeviCivitaExistenceStatement → CONFIRMED (statement unchanged, real witness, downstream use). Need to verify downstream use `so3_ricci_e00` etc.
   2. surgery SR-5 sphere_of_spheres → CONFIRMED with qualification (V2 data).
   3. surgery covering-space recognition → CONFIRMED (real theorem, mathlib's IsQuotientCoveringMap).
   4. surgery coveringTrivial → CONFIRMED (real monodromy proof; not vacuous since model has no triviality field).

B. Vacuity/overclaim checks on headline theorems. Let me identify per card the "headline" declarations and check:
   - Are hypotheses satisfiable? (non-vacuity witnesses)
   - Is the conclusion weaker than claimed?
   - Does a hypothesis embed the conclusion?

Let me do a systematic vacuity screen: for each declared theorem, extract its type from the `#check` output in the probe log, and look for hypotheses that are structurally identical to the conclusion (naive string similarity) or hypotheses of the form `P → P`. Also look for `True` conclusions or hypotheses like `(h : ... )` where the same proposition appears in the conclusion.

A simpler robust approach: use Lean itself. For each card, write a probe that uses `Lean.Util.CollectAxioms`? No — vacuity detection is heuristic. 

Practical approach: extract the types from `#check` output (I have them in probe logs), then:
- Flag theorem types with a hypothesis whose pretty-printed string appears verbatim in the conclusion (after stripping binder names).
- Flag conclusions that are `True`, `Nonempty ...` built from hypotheses, etc.
- Flag hypotheses of type `Nonempty X` where X is exactly the conclusion.

Given time, let me focus on the headline theorems per card (as listed in the cards). I'll extract types via `#check` from the probe logs and manually review the top ones.

Let me write a script to extract declaration type text from the probe log (the `#check` section, which is everything before the first `'...' depends` line). Then print types for a curated headline list per card.

Headline lists (from cards):
- connection-curvature: leviCivitaExists, milnorConnection_eq_mean_iff, ricci_symm, chartMetricCompatible_form, chartTorsionFree_form, nabla_torsionFree, nabla_metricCompatible, christoffelFamily_smooth, conformal_riemann_1212_origin, conformal_curvature_nonzero, so3_ricci_e00.
- volume-ibp: chart_ibp, chart_weighted_ibp, divergence_integral_eq_zero_density, pullback density law, measure naturality.
- spectral-sobolev: poincare_wirtinger, heatSeries_hasSum, heatEvolve_tendsto_self, heatEvolve_add.
- semantic-ledger: not_initialCondition_gaussian, integrand_lintegral_eq_top, realField_unsatisfiable_by_gaussian.
- comparison-geodesics: sturm_zero_comparison, logDeriv_le_of_le, riccati comparison, bishopGromovVolumeRatio.
- geometric-compactness: criterion equivalence, compactness assembly, subsequence witnesses (need names from the card, they're in dict categories).
- surgery-recognition: the ones read already.

Let me extract the #check types for each card into a per-card text file and review the key ones. Since the probe logs contain the types, let me write an extractor that saves `types/<card>.txt`.

Then I'll read the relevant ones.
dsh: reasoning:
Now, a systematic vacuity screen. Let me implement a heuristic on the type text:
- Split each declaration's pretty-printed type into a "hypotheses" part and "conclusion" part. Pretty-printed types use `→` or `(h : P) →` binders. Hard to parse generally.
- Heuristic: check if some hypothesis string (normalized whitespace) equals the conclusion string (normalized). Also check whether the conclusion appears verbatim among hypotheses.
- Also flag `True` conclusions or hypotheses `(h : True)`.
- Also flag hypotheses that are `Nonempty X` where the conclusion is `Nonempty X`.

Let me write it and see if anything pops. Given the types are pretty-printed across lines, I'll normalize whitespace.

Additionally, a stronger check for "assumption equivalent to the conclusion": use `#check` types and look for any hypothesis whose set of tokens is a superset of the conclusion's tokens (with same length ratio). That's noisy; let me first try exact match and simple patterns.

Let me write the screen.
dsh: reasoning:
No exact hypothesis-equals-conclusion flags. But note the declaration splitting is imperfect (connection-curvature 109 vs 119; surgery 19 vs 30) because of multi-line headers. The heuristic screen is weak. Let me manually review headline types.

Let me print the key headline declarations' types per card from the probe logs (using grep with context). Start with the cards I haven't reviewed: spectral-sobolev, comparison-geodesics, geometric-compactness, semantic-ledger, volume-ibp.

Let me extract the full type text for selected names.
dsh: reasoning:
The entries dict keys are the first token of the header line, which may be a partial name (e.g. `Poincare.D12.SpectralSobolev.heatWeight`), and matching by `k.endswith(n)` should work... but nothing found. Perhaps the splitter breaks because types contain lines starting at column 0? Let me just grep the probe logs directly for the names with context.
dsh: reasoning:
This is a substantive theorem set (genuine infinite Fourier series heat evolution on the torus, L²-convergence, contraction, tendsto self). Good.

Let me look at poincare_wirtinger's full type (line 66 onwards) and the comparison-geodesics headlines.
dsh: reasoning:
These are genuine ODE comparison theorems with nonconstant coefficients, no division at zero (logDeriv defined with positivity hypotheses). The Sturm zero comparison statement is the classical one; the "or k₁ = k₂" disjunct handles the equality case. Non-vacuous: EuclideanModel instances exist.

Now let me check the semantic-ledger card more carefully. It's an audit card claiming:
- 36 main-chain claims classified.
- D11 heat test-function defect VALIDATED by kernel-checked counterexample.
- D7 recognition deliverable classified as CONDITIONAL.
- Blockers recounted: 31 entries (23 open, 1 documented, 1 resolved-in-worktree, 3 environment, 3 informational).
- "No builder source modified; no blocker claimed closed".

Interesting: the ledger card says 23 open blockers, but my task's named blocker A3 is "audit covers D4 only; earlier clusters D2/D3 have no counterexample search". Does the semantic-ledger card address A3? Its blockers were ["A1","A2","A3","P1","P5"]. The card claims to enumerate/classify D1-D11 but does it do a counterexample search for D2/D3? Let me read its card and ledger.

Let me read the semantic-ledger card md.
dsh: reasoning:
The semantic-ledger card is thorough and honest: it explicitly states A3 remains open (first counterexample audit outside D4, D2/D3 audits still absent). Good.

Note: the card claims `realField_unsatisfiable_by_gaussian` proved against the real D7 structure rebuilt from pristine sources. My audit will verify this (the extra probe). Since I ran `lake env lean A3Extra/D12RealModuleProbe.lean` in the copied package, let me confirm rc and cone output. Earlier the analyzer found 7/7 audited for semantic-ledger, including the two extra declarations (they're in the extra log). Wait: `audited=7/7` — declared = 7 resolved in-package, and extra declarations aren't in `declared`; the extra cones are added to all_cones so they'd count only if in declared. The 2 extra names are in `unresolved`, so not counted in `audited`. Let me verify the extra probe log's cones: `real_initialCondition_specializes`, `realField_unsatisfiable_by_gaussian` with allowed cones, and that its own `#eval` audit passed.

Let me check the extra logs.
dsh: reasoning:
The extra probe failed because `Poincare.D7.HeatKernel.Basic` isn't built in the semantic-ledger package (D12-semantic-ledger's release only has the D6 base with D7? Let me check: earlier the semantic-ledger worktree's release had Poincare/Longrun only... The semantic-ledger card said it recompiled 37 D7/D10/D11 modules from pristine sources into task-local oleans — likely in a separate location (`snapshot_rebuild/` or similar), not in release/.

So to audit those two declarations, I need to replicate the rebuild of the D7/D10 modules from their pristine sources. The semantic-ledger worktree has `manifest/d12-rebuild-manifest.json` with the module list and hashes. Let me find where those oleans were built (maybe deleted) and how the probe was compiled by the producer.

Let me inspect the semantic-ledger worktree: logs, manifest, scratch dirs.
dsh: reasoning:
The producer's rebuild took sources from `D11-bochner-manifold/release` and compiled them into task-local oleans. To audit `realField_unsatisfiable_by_gaussian`, I need those D7 modules available in my audit package.

Options:
A. Use the D11-bochner-manifold release tree (which contains D7 modules) — I can build the semantic-ledger extra probe inside a *different* package that has all needed modules. E.g., copy the D11-bochner-manifold release sources into an audit package, add the D12 semantic-ledger Defect.lean + audit probe, and build. The D11-bochner-manifold release has D7/D10/D11 modules plus D12/SemanticLedger? Let me check: earlier `ls D11-bochner-manifold/release/Poincare/` showed D10, D11, D7, D8, D9, Longrun, Stage1, Stage6. It also had `D12`? The list showed: Basic.lean, D10, D11, D7, D8, D9, Longrun, Stage1, Stage6 — no D12. But the semantic-ledger probe imports `Poincare.D12.SemanticLedger.Defect`, which is in the semantic-ledger release only.

B. Simplest: build the semantic-ledger package's release with the D7 modules added (copy Poincare/D7 from the D11-bochner-manifold release, plus D10/HeatKernelEuclidean), then compile the extra probe. That effectively recreates the producer's rebuild inside my audit package. It's a bigger build but fine.

Actually, do I need the full D7 tree? The extra probe imports:
- Poincare.D12.SemanticLedger.Defect
- Poincare.D7.HeatKernel.{Basic,Blocked,Grid,Instance,Example}
- Poincare.D7.Recognition.{Basic,Assembly,Homeomorphism}
- Poincare.D10.HeatKernelEuclidean.{Basic,Mass,HeatEquation,Semigroup}

I can copy the needed module files plus their dependencies from D11-bochner-manifold/release. Easiest: copy the entire `Poincare/D7`, `Poincare/D10`, `Poincare/D11`, `Poincare/D8`, `Poincare/D9`, `Poincare/Stage1`, `Poincare/Stage6`, `Poincare/Longrun` from D11-bochner's release? That may conflict with the semantic-ledger release's own versions (D6 base). Since the producer compiled these D7/D10 modules against the same base, and the semantic-ledger release already has Poincare/Longrun etc. from D6, the D7 modules may import Longrun modules — if the D6 versions differ from D11-bochner's versions, compilation could break.

Alternative C: Use the D11-bochner-manifold worktree's release as the base (it's a superset with D7/D10/D11/D12?), then add the semantic-ledger's Defect.lean and the audit probe. Let me check whether D11-bochner-manifold/release has Poincare/D12/SemanticLedger — earlier list showed no D12 dir at top level. Let me confirm, and check if D11-bochner has the D7 modules with the same hashes.

Actually, the simplest robust approach: copy the whole D11-bochner-manifold release (sources) into a new audit package `D12-semantic-ledger-snapshot`, then copy in the semantic-ledger's `Poincare/D12/SemanticLedger/` directory and the audit probe, add lakefile lean_lib for D12 if needed (the lakefile globs `Poincare.+` so it's automatic). Then `lake build` and run the probe. This is essentially replaying the producer's rebuild independently, with source hashes verified against the manifest.

But wait: the producer's manifest says sources came from D11-bochner-manifold/release with specific hashes. I can verify those hashes against the D11-bochner release and against my copy.

Let me first check the sizes and whether D11-bochner's release builds (it has its own .lake maybe). Rather than a full separate build, maybe I can build just the needed modules in the semantic-ledger audit package by copying the D7/D10 dirs from D11-bochner and hoping base compat. Let me check whether D11-bochner's release base modules (Poincare/Longrun) are identical to semantic-ledger's (D6-based). Earlier the D12 worktrees were all D6-based; D11-bochner is also presumably D6-based plus D7-D11. Let me diff the two base trees.

Let me check.
dsh: reasoning:
D11-bochner-manifold's release has identical Longrun/Stage1/Stage6/Basic to the semantic-ledger release. So I can create an audit package that is:
- base = D11-bochner-manifold release sources (which include D7, D10, D11)
- plus D12/SemanticLedger from semantic-ledger release
- plus the audit probe.

Then build and run the probe. The lakefile from D11-bochner should have the same libs.

Let me check D11-bochner's lean-toolchain/mathlib rev match (should be same). And whether its release has a .lake build cache (probably).

Let me set this up as an extra package `D12-semantic-ledger-snapshot` and add it to the audit. Given the 4-hour budget (used ~50 min), this is worth doing for completeness (it's the card's headline claim).

Let me create it:
1. Copy D11-bochner-manifold/release sources (exclude .lake) to audit360/pkgs/D12-semantic-ledger-snapshot/.
2. Copy semantic-ledger/release/Poincare/D12/SemanticLedger → same path in the snapshot package; also D12.lean aggregator? The semantic-ledger release has Poincare/D12.lean? Let me check. Its module files: Defect.lean, LedgerProbe.lean, AxiomAudit.lean.
3. Copy audit_probes/D12RealModuleProbe.lean → A3Extra/.
4. Verify the source hashes of the D7/D10 modules against the producer's manifest (from the D11-bochner sources).
5. Symlink .lake/packages, build, run probe.

Let me also verify the producer's claimed source hash for `Poincare/D7/HeatKernel/Basic.lean` matches the D11-bochner release file.

Let me do it.
dsh: reasoning:
All 37 modules in the semantic-ledger rebuild manifest hash-match the D11-bochner-manifold pristine sources. Good provenance.

Now build the snapshot audit package. Let me write a small script for it.
dsh: reasoning:
Now I need to append the `#print axioms` lines for the two declarations to A3Extra/D12RealModuleProbe.lean (the producer's file already has its own audit code; appending my probe lines is fine). Actually let me check if the file itself calls `#print axioms` on those two already — likely yes via `realAuditedDeclarations`. Appending duplicates is harmless; parsing picks them up.

Let me append:
```
#print axioms Poincare.D12.SemanticLedger.real_initialCondition_specializes
#print axioms Poincare.D12.SemanticLedger.realField_unsatisfiable_by_gaussian
```

Then create a run script for this package: `lake build` + `lake env lean A3Extra/D12RealModuleProbe.lean`.

Let me write a specific runner.
dsh: reasoning:
While that builds (the D11-bochner release may take longer — it has D7/D9/D10/D11 modules), let me continue semantic review.

Let me now review the remaining cards' semantic claims:

1. D12-geometric-compactness (status in_progress but has a card?). Its card JSON had no status; checkpoint status in_progress, elapsed 3.8h. The card md exists. It claims 31 declarations: general/model/statement_only. Since it's in_progress, not TASK_DONE, the audit should note the card is not final. Let me check its card md and JSON status.

2. D12-comparison-geodesics: status "deliverable_complete_gate_repaired".

3. D12-spectral-sobolev: complete.

4. D12-volume-ibp: in_progress checkpoint but card says task_done.

The task says "Independently re-verify the 360-produced D12 result cards ... confirm/refute exact_blockers_closed". I should report each card's producer status and whether the card is frozen/final.

Let me check the exact statuses and card mtimes vs checkpoint mtimes to detect cards that were updated after build (staleness). Important audit check: does the built source correspond to the card's recorded hashes? Yes (hashes verified). But are the compiled sources the *latest*? Yes, they're the current worktree state.

Another check: does the card's `compile_evidence` match an actual independent rebuild? I did my own rebuild — fine.

Let me look at the geometric-compactness and semantic-ledger "status" fields and check the two in_progress cards' claims. Also check the D12-volume-ibp/connection-curvature cards whose checkpoints are in_progress but cards claim TASK_DONE.

Let me pull status/elapsed for all 7 cards.
dsh: reasoning:
All cards were written Sep 11 between 02:02 and 04:50. The queue statuses show several as remote_owned/in_progress (still running on remote lanes?). My audit snapshot is the state as of now (12:00+). Since the worktrees may still be updated by the remote producers, my audit freezes the current hashes.

Now, are the worktrees' sources still being modified? The mtimes: D12-connection-curvature release mtime 02:18, checkpoint 04:38/04:50. So not being actively modified since ~05:00. Fine.

Now let me do the semantic review of the two "gate_failed" aspects:
- D12-semantic-ledger queue status is gate_failed (blockers A1,A2,A3,P1,P5). Its own card says TASK_DONE. The queue's gate_failed likely refers to the acceptance gate (the semantic ledger was supposed to close A1-A3 but they remain open). My audit should note the discrepancy: card verdict TASK_DONE vs queue gate_failed; and the card itself admits A3 remains open. That's coherent (the task was to produce an independent ledger, not to close A3).
- D12-connection-curvature queue: remote_owned (not verified).

Let me now examine the remaining semantic claims:
1. geometric-compactness headline claims.
2. connection-curvature chart theorems (already looked at some).
3. volume-ibp chart_ibp statement.
4. Any overstrong/false statements.

Given the A3 blocker is about counterexample search for D2/D3, my audit should attempt adversarial falsification of the D12 cards' key statements. A practical approach: look for hypotheses that are impossible (vacuous), or conclusions that are trivially satisfied, or definitions that encode the conclusion.

Let me check a few specific risk areas:

(a) D12-connection-curvature `chartMetricCompatible_form`/`chartTorsionFree_form` — are they stated with the coefficient hypotheses (g, gInv, d) such that they're just algebraic identities? The card labels them "GENERAL at the algebraic level with expanded coefficient hypotheses". The risk: the `d` coefficient data is arbitrary, so the statement `∂ₓg(Y,Z) = g(∇ₓY,Z)+g(Y,∇ₓZ)` with `∂ₓg` replaced by an arbitrary `d` datum could be a trivial identity by construction of `christoffel` — i.e., the "metric compatibility" is definitionally true given `d` is arbitrary rather than the actual derivative. But the card's honest boundary mentions the x-dependent smoothness bridge. In `ChartLeviCivitaSmooth`, `dFamily` is derived via fderiv and `nabla_metricCompatible` is against the actual Frechet derivative. That's the substantive version.

(b) D12-volume-ibp `chart_ibp`: check the statement's hypotheses (C², compact support) and that the conclusion isn't vacuous (e.g., if both sides are zero by triviality). `∫ u Δv ρ dx = -∫ ⟨∇u,∇v⟩ ρ dx` for u compactly supported C². Non-vacuity: `chart_ibp_euclidean_one` instantiates in dim 1. Need to check it's a nontrivial instance (u, v chosen nonzero). Let me look.

(c) D12-spectral-sobolev `poincare_wirtinger`: genuine inequality with optimal constant. Non-vacuity: sine saturates (poincare_wirtinger_sine_saturates). Good.

(d) D12-comparison-geodesics `bishopGromovVolumeRatio`: check the statement — analytic hypotheses on A, Ā. The card says it becomes BG when A is identified with sphere volume. Need to check the statement isn't trivially true (e.g., ratio ≤ 1 by assumption).

(e) D12-surgery-recognition: already reviewed; the deckTrivial/covering theorems are genuine.

(f) D12-geometric-compactness: check the compactness assembly isn't assuming a convergent subsequence. The card says "Do not prove compactness by assuming a record containing a convergent subsequence". Let me verify the main theorem's hypotheses.

Let me look at these one by one. Start with volume-ibp chart_ibp and geometric-compactness main theorem.
dsh: reasoning:
`chart_ibp` is a genuine identity: u compactly supported C², v C², integral over ℝⁿ⁺¹ with density. Non-vacuity: `chart_ibp_euclidean_one` instantiates it for Euclidean metric in dim 1 with arbitrary u,v (not a specific nonzero instance, but the theorem itself is not vacuous since such u exist, e.g. bump functions). Acceptable.

Now geometric-compactness. Let me check its card and main theorem.
dsh: reasoning:
This card is honest: general metric theorems + model + statement-only frontier, no blockers closed. The converse of Gromov's criterion and subsequence witnesses are genuine contributions. Non-vacuity: grid family with unbounded cardinality.

Now, one adversarial check: `gromovCriterion` claims "closed family compact ⟺ uniform diameter + covering bounds" — in `GHSpace`, compactness is sequential compactness (GHSpace is a metric space? mathlib's GHSpace has a pseudo-metric `ghDist` and is complete/second countable). If the criterion uses `IsCompact` on the family of GHSpace points, fine. Let me check the statement quickly.

Actually, the key risk with these cards is not fraud but overstatement in the cards. My audit needs to assess whether the cards' claims match the Lean statements. Given time constraints (about 1h used), I'll do targeted checks on:
- geometric-compactness `gromovCriterion` and `gh_subseq_*`.
- comparison-geodesics `bishopGromovVolumeRatio`.
- connection-curvature headline statements.
- spectral heat series.

Let me look at the extracted types for geometric-compactness and comparison-geodesics.
dsh: reasoning:
These are genuine, non-vacuous statements: the criterion is a real equivalence in mathlib's GHSpace, the subsequence theorem produces an explicit strictly monotone φ and limit (not assumed data). Good.

Now let me check `bishopGromovVolumeRatio` in comparison-geodesics.
dsh: reasoning:
`bishopGromovVolumeRatio` is a genuine analytic theorem with fully expanded hypotheses; conclusion `V R / V̄ R ≤ V r / V̄ r`. Not vacuous (Euclidean model instance exists, `euclidModel_bishopGromov`). The card honestly labels the geometric identification as missing.

Now, let me focus on remaining audit checks:
1. Verify no `set_option` cheats (e.g., `set_option maxHeartbeats 0`, `set_option autoImplicit`), plus check for `@[implemented_by]`, `extern`, `partial`.
2. Check the semantic-ledger's claim about "no builder source modified" — I verified base modules unchanged vs D6 and vendored modules identical across worktrees. Also the ledger's recompiled snapshot hashes match D11-bochner sources. Good.
3. Check `exact_blockers_closed` for all cards (4 closures) — done semantically for 4/4.
4. Verify the card claims about declaration counts (e.g., connection-curvature claims 119 proved declarations; card JSON has 119; all resolve).
5. Check for weakened conclusions in the *cards'* headline claims vs actual statements. E.g., spectral card says "constant ((b-a)/2π)² proved optimal" — the statement shows the inequality with that constant; optimality via `poincare_wirtinger_sine_saturates`. Let me verify that saturates means equality holds for sine (not just that sine satisfies). Let me check.

6. A3-specific: "search for false, vacuous or overstrong statements". I should attempt at least one *substantive* falsification attempt. Candidate: the D12-connection-curvature card's correction of the historical blocker docstring ("LeviCivitaExistenceStatement is false without invariance"). The card claims it's actually unconditionally true. I verified the Math: Milnor's formula gives a torsion-free, metric-compatible connection for any bracket. So the historical docstring is indeed wrong; the closure is sound.

Another candidate for falsification: the semantic-ledger's counterexample to the D7 heat kernel initial condition. Let me verify the math: HeatKernelData.initialCondition says for ALL continuous f: Tendsto (fun t => ∫ y, kernel x y t * f y) (𝓝[>] 0) (𝓝 (f x)). Take f(y) = exp(y⁴), kernel Gaussian; the integrand for t>0 is (4πt)^(-1/2) exp(-(x-y)²/(4t) + y⁴). For large |y|, y⁴ dominates the quadratic, so the integral diverges (∞). In mathlib's Bochner integral convention, non-integrable → integral = 0. So the limit is 0 ≠ f(0)=1. Hence the field is false. Correct.

But: is the statement `¬ ∃ D : HeatKernelData ℝ, D.volume = volume ∧ D.kernel = gaussianKernelXY` correct? It says no HeatKernelData on ℝ with standard Lebesgue volume and Gaussian kernel. Since `initialCondition` is a field, and it's false for those choices, yes. But note: `HeatKernelData` may have more fields (e.g., symmetry, smoothness) that could be inconsistent anyway; the theorem is still valid.

Let me verify the extra probe compiles (snapshot build running). Let me check on it.

Also, I should check the negative control for the *forbidden token scanner*: my scanner should flag the A3NegativeControl.lean file (it has `sorry` and `native_decide`). Let me add that check to the analyzer or verify manually. Quick manual check via the scanner function.

Let me check snapshot build progress and run the scanner on the negative control.
dsh: reasoning:
The forbidden scanner correctly flags the negative control (sorry, native_decide). Good.

The snapshot build is at 9032/9176.

While waiting, let me check:
1. `set_option` scan for suspicious options.
2. Check declaration-count claims vs my probe counts.
3. Check the spectral optimality claim.

Let me also review the connection-curvature card's remaining claim: "chartMetricCompatible_form — for all coefficient vectors, from the coefficient identity A_{jkl} + A_{lkj} = d_{klj}". Let me read the statement.
dsh: reasoning:
Interesting observations:

1. `chartTorsionFree_form` states `∇_X Y − ∇_Y X = 0` (not `= [X,Y]`)! Because at the *coefficient/algebraic* level, `nablaOf` uses the chart Christoffel symbols which are symmetric, so torsion-freeness is expressed as symmetry of the connection (`∇_X Y = ∇_Y X`), i.e., zero Lie bracket in the coordinate frame. Hmm. That's the coordinate-frame formulation: for the coordinate vector fields, [∂_i, ∂_j] = 0, so torsion-freeness is `Γ^k_{ij} = Γ^k_{ji}`. The statement here is a *pointwise algebraic* statement about coefficient vectors, not about actual vector fields. The card calls it `chartTorsionFree_form` and claims "coordinate torsion-freeness" — the Lean statement is `nablaOf c X Y - nablaOf c Y X = 0` for arbitrary coefficient vectors X,Y. If it were claiming full torsion-freeness of the field-level connection, it would need `= lieBracket`. In the smooth module, `nabla_torsionFree` states `c.nabla X Y x k - c.nabla Y X x k = c.lieBracket X Y x k` — the real torsion-freeness with the actual Lie bracket of vector fields. So the packed-card description is accurate: `chartTorsionFree_form` is the algebraic coordinate-level statement; `nabla_torsionFree` is the field-level one.

But here's the thing: the card's exact wording says "`chartTorsionFree_form`, and the bridge..." and in the md: "* `chartMetricCompatible_form` — `∂ₓg(Y,Z) = g(∇ₓY,Z) + g(Y,∇ₓZ)`". In the Lean, the RH of chartMetricCompatible_form is `dFormOf c X Y Z`, where `dFormOf` is an arbitrary coefficient datum (from ChartMetricCoefficients.d), *not* an actual derivative. So at the algebraic level, it's a compatibility identity between the constructed `nablaOf` and the supplied `d` datum. That's what the card says ("from the coefficient identity A_{jkl} + A_{lkj} = d_{klj}"), and the real derivative statement is `nabla_metricCompatible` in the smooth module. So the card is honest, though a reader could over-read the algebraic statement. I'll note this in the audit as a "scope nuance": the algebraic form's `dFormOf` is coefficient data, and the genuine derivative statement is `nabla_metricCompatible` (which I verified).

2. `set_option maxHeartbeats 4000000` in one place — check where; it's a performance option, not soundness. Also `autoImplicit false` is good practice.

Let me check where maxHeartbeats 4000000 is (probably the heavy chart proof). Not a problem per se, but worth noting for reproducibility. Also check no `set_option maxHeartbeats 0` (unbounded).

Now let me check the snapshot build, then write up. Also, I should verify the `nabla` definition uses `dFamily` (derived) — yes from the card.

One more adversarial check on the connection-curvature headline: `nabla_smooth` claims smooth fields map to smooth fields. Let me check the definition of `SmoothChartData` to ensure `gInv` smoothness is a hypothesis (the card admits "smooth gInv taken as coefficient data" in remaining blockers). Fine.

Let me check snapshot build progress.
dsh: reasoning:
Snapshot build still going (9066/9176). Let me continue with the semantic review of remaining items while it finishes.

Let me now verify the exact_blockers_closed claims in a structured way and check downstream uses:

1. connection-curvature: `leviCivitaExists` downstream use: so3MeanLeviCivita → ricci_symm → so3_ricci_e00. Let me verify these declarations exist and that so3_ricci_e00's proof actually depends on the Milnor construction (not just a numeric computation on so(3) independent of the general theorem). The card says "Downstream checked use: so3MeanLeviCivita -> ricci_symm and so3_ricci_e00". Let me look at SoThreeModel.lean to see how so3_ricci_e00 is proved.

2. surgery: `ConnectedSumDecomposition.mkV2` → `stage6Target_of_v2decomposition`; `finiteFreeOrbit_isQuotientCoveringMap` → `stage6Target_of_v2hypotheses`; `deckTrivial...` → `stage6Target_of_v3hypotheses`. Let me verify those theorems actually consume the constructed objects (type-checked use).

Let me read the relevant code.
dsh: reasoning:
The so3 model genuinely uses the Milnor/mean Levi-Civita data and computes nonzero curvature/Ricci. Downstream use confirmed.

Now, one caveat: `so3MeanLeviCivita` is built from `meanLeviCivitaData` (the mean connection), which itself relies on `bracketInvariant` — not directly from `leviCivitaExists`. But the closure path is `milnorConnection_eq_mean_iff` + mean data. The general `ricci_symm` theorem is applied via `so3_ricci_symm`. Good enough: the constructed witness feeds the D7 interface and there is a nontrivial instance.

Let me now check the surgery downstream uses: `stage6Target_of_v2decomposition` and `stage6Target_of_v3hypotheses` — verify they consume `mkV2` / `sphericalPieceRecognition_of_spaceForm` in their proofs. Let me read ExpandedInterfaces.lean around those lines.
dsh: reasoning:
The downstream uses are real: `stage6Target_of_v2decomposition` rebuilds the extinction certificate with `mkV2`, and `stage6Target_of_v3hypotheses` uses `H.toRemainingV2`. The chain is checked.

Assessment of SR-5 closure: The named D7 blocker field `sphere_of_spheres` is now a *proved function* in `mkV2`; the geometric input (X ≃ iterated sum) is carried as V2 data. So the closure is genuine at the level: "connected sum of sphere summands ≃ S³" is proved, and the end-game assembly consumes it. The remaining assumption (X is the connected sum) is not the blocker — it's the decomposition data. I'll mark CONFIRMED (with the caveat recorded).

Now let me check the snapshot build status and then start writing results. Also I need to check the `axiom_evidence` claims in each card vs my findings (e.g., connection-curvature card claims "172 D12 declarations"; my probe covers 119 declared names — the card's `axiom_evidence` mentions a different count from its own audit module; not necessarily contradictory since their audit covers more declarations than proved_declarations).

Let me now also spot-check that the producers' claimed counts match:
- volume-ibp: card says "63 declared cones" in their audit; my probe: 78 declared names (card's proved_declarations list has 78). Their audit may cover a different set (63). Not a contradiction per se, but worth noting: my independent audit covers 78.

Let me check the snapshot build.
dsh: reasoning:
Still building (9091/9176). Meanwhile, let me prepare the results document structure and gather all the evidence into a summary JSON. Let me also do a few more checks:

1. Verify the card counts and my counts.
2. Check the semantic-ledger's blockers recount claim (31 entries) by reading its blockers.json count.
3. Check the "expanded_hypotheses" fields of each card for honesty (these are card claims; I'll summarize).

Also I should verify the producers' `axiom_evidence` negative controls are consistent with mine (I ran my own).

Let me gather per-card summary data programmatically into a JSON:
- card status, elapsed, semantics class, exact_blockers_closed count, remaining_blockers count, proved_declarations count
- my hash check summary
- my build/probe results
- forbidden scan
- notes/findings

Then write the md.

Before that, let me check the two missing cards' queue status one more time (maybe the worktrees appeared). Also check whether any new worktree dir appeared since 12:04.
dsh: reasoning:
Good summary. Two cards missing permanently (within this host/session).

Now let me verify the semantic-ledger's blocker recount: 31 entries. Let me check manifest/blockers.json in the semantic-ledger worktree, and see if A3 is among them with correct status.

Also, I should assess whether the semantic-ledger's audit of A3 is accurate: "this task adds the first counterexample audit outside D4 (the D7 heat field), but D2/D3 counterexample audits remain absent." My cross-audit adds D12 card audits; I should note whether A3 is closed or remains open. A3 is "adversarial audit only covers D4 evolution cluster; earlier clusters D2/D3 have axiom audits but no counterexample search". My task audits D12 360 cards, not D2/D3 — so A3 remains open, though I can report partial coverage. However, my audit does find concrete defects (card inventory namespaces, missing cards). Those are process/claim defects, not false mathematical statements.

Let me check the semantic-ledger blockers.json A3 entry and the count.
dsh: reasoning:
The semantic-ledger's blocker recount checks out (31 entries, 23 open, A3 open).

Now let me wait for the snapshot build, then run the analyzer to include the snapshot extra probe (need to extend CARDS/analysis for it, or handle manually). Let me extend analyze.py to also parse `D12-semantic-ledger-snapshot.extra.D12RealModuleProbe.log` and include the two declarations in the semantic-ledger card record.

Actually simpler: after the build completes, parse that log separately and record in the results. Let me extend analyze.py with a dedicated section for the snapshot probe.

Let me check build status.
dsh: reasoning:
The snapshot rebuild succeeded (9176 jobs, exit 0) and the extra probe compiled (rc 0) with both declarations showing the standard cone.

Let me look at the full extra probe log to see:
- the two declarations' cones
- the producer's own audit output (`realAuditedDeclarations` check) — verify PASS
- no errors

Then extend analyze.py to include the snapshot probe result in the audit result, and re-run analyze (it takes a while for the negative control recompile — ~10s).

Let me view the log.
dsh: reasoning:
The semantic-ledger's real-module probe compiles against a fresh rebuild of the D7/D10 sources (hashes verified against the producer manifest) and both declarations have the standard cone. Its own audit check PASSes.

Now let me extend analyze.py to include the snapshot probe, re-run, and then write the results.

Also, let me do a couple more semantic checks on the two cards I haven't deeply reviewed: spectral-sobolev optimality and comparison-geodesics Sturm non-vacuity; plus review exact_blockers_closed statements in the cards vs Lean (done).

Let me check `poincare_wirtinger_sine_saturates` (equality) quickly.
dsh: reasoning:
Findings:
- `poincare_wirtinger_sine_saturates` genuinely proves equality for sine → constant is optimal (with `poincare_constant_sharp` proving any valid C ≥ (b-a/2π)² presumably). Good.
- `euclidModel_singular_comparison_ge` is `m ≤ m` (trivially true!) — it's an instance where k₁ = k₂ = euclid model, so the comparison is reflexive. Similarly `euclidModel_bishopGromov` compares the same model to itself: `V R / V R ≤ V r / V r` i.e. `1 ≤ 1`. These are *non-vacuity witnesses* only in the weak sense that the hypotheses are satisfiable — they are trivially true instances, not demonstrations of a strict inequality. The card labels them "models / non-vacuity witnesses — Euclidean model instances of the singular comparisons and of ...". Hmm, technically they show the hypotheses are satisfiable (non-vacuity), which is their stated purpose. But they're not strong evidence of content. I'll note this as a minor "model instances are reflexive/trivial" observation — the card's claim is "non-vacuity witnesses", which is accurate but weak. Not a defect per se.

Wait, actually is `euclidModel_singular_comparison_ge` `m ≤ m`? Yes: `euclidModelM d t ≤ euclidModelM d t`. Trivial. And `euclidModel_bishopGromov` compares A with A: ratio ≤ same ratio. Trivial. So the "model" instances are tautological. That IS worth flagging as an adversarial finding (A3-style residual): the non-vacuity witnesses in D12-comparison-geodesics for the volume-ratio and singular comparisons are reflexive instances, so they do not independently exhibit a nontrivial comparison. The genuine non-vacuity is the Sturm/Riccati theorems with k₂ ≤ k₁ and different solutions; does the card provide a nontrivial instance? Let me check the ProvedDeclarations list for a witness with distinct k's. Let me look at all 33 declarations and their types.

Let me print the full types file for comparison-geodesics to review.
dsh: reasoning:
The comparison-geodesics declarations are substantive. The model instances are reflexive (`m ≤ m`, ratio ≤ ratio) — weak non-vacuity but the card calls them "non-vacuity witnesses" which is technically what they do (hypotheses satisfiable + conclusion holds trivially). I'll record this as a minor finding: model witnesses for the singular comparison and BG ratio are reflexive, so they do not exercise a strict comparison. Not a false claim, but the "non-vacuity" is minimal. It's the kind of A3 residual finding worth recording.

Now let me finish up:
1. Add snapshot probe to analyze.py results.
2. Compose the results .md and .json.
3. Update checkpoint.

Let me extend analyze.py for the snapshot, then re-run.

Actually, I can add the snapshot results manually into the final JSON. To keep the evidence generation automatic, let me add a small block to analyze.py that parses `D12-semantic-ledger-snapshot.extra.D12RealModuleProbe.log` and includes it in the semantic-ledger record as `snapshot_rebuild_audit`.

Let me patch analyze.py quickly.
dsh: reasoning:
All evidence gathered. Now let me review the remaining semantic question: whether the cards contain any *false* statements (A3's goal). Let me spend a bit more effort on adversarial falsification of the most substantive claims:

1. `leviCivitaExists` — the math is right.
2. `deckTrivial_of_simplyConnected_quotient` — math right.
3. `finiteFreeOrbit_isQuotientCoveringMap` — requires the action be free and continuous, finite group; proof constructs disjoint translates. Math right (finite group acting freely on a compact space is properly discontinuous).
4. `sphereConnectSum_homeo_sphere` — S³#S³ ≅ S³; math right.
5. `poincare_wirtinger` — the optimal constant for mean-zero periodic functions; right.
6. `heatSeries_hasSum` — Fourier series converges in L² to heat evolve; right.
7. `bishopGromovVolumeRatio` — analytic core of BG; the hypotheses include Euclidean normalization (standard for singular comparison); right.
8. `gromovCriterion` — real equivalence.
9. `not_initialCondition_gaussian` — right.
10. `chart_ibp` — right.

Any false statement? One candidate: `euclideanNormalizedOn_not_continuousOn_zero` — claims a function satisfying |m t − d/t| ≤ C on (0,t₀] is not continuous on [0,t₀]. Since d/t → ∞ as t→0⁺, yes, if d>0 then m is unbounded near 0, so not continuous on [0,t₀]. Correct.

Another: `chart1D_curvature_zero` — 1D curvature vanishes; right.

Another: connection-curvature card's claim that the historical docstring is wrong. Right.

Another candidate: the card's `semantic_class` claims "chartMetricCompatible_form / chartTorsionFree_form: GENERAL at the algebraic level with expanded coefficient hypotheses". As discussed, valid but the "dFormOf" is data, so the algebraic form is a definitional identity. The card discloses this. Fine.

Another: D12-geometric-compactness card says "gromovCriterion (closed family compact ⟺ uniform diameter + covering bounds)". Is `IsCompact t` for a set in GHSpace equivalent to sequential compactness? mathlib's GHSpace is a pseudo-metric space (`ghDist` may be zero for isometric spaces; `GHSpace` quotient?). Actually `GHSpace` in mathlib has `ghDist` as a pseudo-metric? It's a `PseudoMetricSpace`? If it's a pseudo-metric space, `IsCompact` and sequential compactness coincide (pseudo-metric spaces are first countable). Fine.

Let me check one potential issue: `totallyBounded_iff_uniformCovers` — for arbitrary `t : Set GHSpace`, totally bounded ⟺ uniform diameter + covering bounds. Is the forward direction true in a pseudo-metric space? Totally bounded means for each ε, finitely many balls cover t. Uniform bound K over all p ∈ t: yes, by total boundedness, there's a finite set s covering t, same s works for all p. And diameter bound: totally bounded sets are bounded. Fine. The converse is also true. Genuine.

I'm fairly confident there are no false statements among the audited declarations. My findings are:
(F1) D12-connection-curvature card's `proved_declarations` inventory is namespace-over-qualified for 73/119 entries (file-derived namespaces) — claim-resolution defect, not soundness.
(F2) D12-semantic-ledger attributes 2/9 declarations to a non-release audit probe file — provenance nuance (verified by rebuilding the snapshot; both are genuine).
(F3) D12-tensor-maximum-bochner and D12-triangulation-topology cards are entirely absent from this host — cannot be audited (2/9 unverified).
(F4) D12-comparison-geodesics groups multiple names into single strings and its model "non-vacuity" witnesses for the singular comparison and BG ratio are reflexive (m ≤ m).
(F5) D12-volume-ibp package contains an intentional `axiom negativeControl : False` in its Audit module (documented, unused in any cone) — legitimate but a forbidden-token hit in a naive scan.
(F6) Several cards are in_progress/gate_failed in the queue despite TASK_DONE card verdicts; cards were frozen at various times. Also the queue status remote_owned/in_progress doesn't match card TASK_DONE. This is process state, relevant to acceptance.

Also positive confirmations:
(C1) All source hashes verified for 7 cards.
(C2) All 7 packages rebuild from scratch (exit 0).
(C3) 342 declared names independently re-audited, all cones ⊆ {propext, Classical.choice, Quot.sound}; negative control flags sorryAx/native_decide.
(C4) 4 exact_blockers_closed claims (1 connection-curvature, 3 surgery-recognition) confirmed by inspecting the definitions/constructors/downstream uses.
(C5) No base or vendored module was modified relative to D6/other worktrees; semantic-ledger's 37-module rebuild manifest hashes match D11-bochner sources.
(C6) Forbidden-token scan clean except documented negative controls.

Now: do I mark TASK_DONE? The task: "Independently re-verify the 360-produced D12 result cards (9 of them): rebuild from source hashes, re-run axiom audits, confirm/refute exact_blockers_closed." I have done this for 7/9; 2/9 don't exist anywhere accessible. The milestone "fully checked" requires all 9. Since 2 cards are source-absent, I cannot fully complete. Per instructions: "End TASK_DONE only if the milestone is fully checked; otherwise TASK_BLOCKED or checkpoint and TASK_IN_PROGRESS."

Hmm. The acceptance criteria in the queue for this task: "compiled_only_until_independent_semantic_review". The named blocker A3 remains relevant. My honest verdict: TASK_IN_PROGRESS/partial — the audit is complete for all available artifacts (7/9), with the 2 missing cards recorded as a concrete blocker (producer artifacts unavailable on this host; remote-owned). But is it "blocked"? The two missing cards may be completed later by the remote lanes; a re-invocation could audit them. Given the 4-hour limit and the fact that the milestone isn't fully checked, I should end with a checkpoint and TASK_IN_PROGRESS, or TASK_BLOCKED if the blocker (missing artifacts) is genuine and persistent.

The instructions say: "Report a genuine mathematical blocker rather than claiming completion with a weaker theorem." Here the blocker is not mathematical — it's artifact availability. I've verified 7/9 cards; the remaining two need the remote producers' outputs. I'll mark the result as TASK_IN_PROGRESS with the explicit blocker recorded (and note that if the supervisor confirms those two tasks are not part of the deliverable, 7/7 available cards verified). Actually, wait — maybe I should consider whether the task's "*the 360-produced D12 result cards*" list is exactly what I audited. The prompt names 9. Two are absent. Since the task explicitly says "End TASK_DONE only if the milestone is fully checked; otherwise TASK_BLOCKED or checkpoint and TASK_IN_PROGRESS", I'll end TASK_IN_PROGRESS (still within the 4h invocation; but I'm nearly done). Hmm, but TASK_IN_PROGRESS is odd for a final answer. Given the choice, TASK_BLOCKED might be more accurate for the two-missing-cards portion, but the overall audit did deliver substantial verified results; the supervisor can decide.

Let me think about what's most useful and honest:
- The main deliverable (audit report with per-card verdicts) is complete for 7/9.
- 2/9 cannot be checked because the producer artifacts don't exist on this host. This is a real, concrete blocker for full completion of the named milestone.
- A3 (the named blocker) remains open: my audit covers D12 cards (not D2/D3 counterexample search), though it does add adversarial coverage.

I'll end with status "TASK_IN_PROGRESS" in the results (explicitly: partial audit, 7/9 cards fully re-verified, 2/9 source-absent; named blocker A3 remains open; no claim of completion). Given the instruction ordering ("otherwise TASK_BLOCKED or checkpoint and TASK_IN_PROGRESS"), and since the missing artifacts are an external condition that I cannot resolve within this worktree (they may arrive later), I think the most accurate terminal state is TASK_BLOCKED for the full 9-card milestone, with the reason: two producer packages absent from the shared filesystem. But "blocked" usually means I can't proceed at all. I did proceed and delivered 7/9. Hmm.

The prompt says "End TASK_DONE only if the milestone is fully checked; otherwise TASK_BLOCKED or checkpoint and TASK_IN_PROGRESS." I'll use TASK_IN_PROGRESS and record the missing-artifact blocker + remaining verification steps (re-audit the 2 cards when synced; deeper D2/D3 counterexample search for A3). This is honest and actionable. Actually, let me reconsider: the supervisor may interpret TASK_IN_PROGRESS as "needs another invocation" which is correct: a re-invocation after the remote cards are synced would complete the audit.

Wait — but there's a subtlety: my invocation is limited to 4 hours. I've used ~1h. I have time to do more. What else can I do to make the audit more complete?

Options:
1. Search for the missing cards on the remote hosts via the fleet's tarballs (control-*.tar.gz)? Let me check those tarballs — maybe they contain remote worktrees. The tarballs are small (7-15KB), likely configs. Let me check quickly.
2. Audit the *predecessor* cards for tensor-maximum-bochner and triangulation-topology (D11-maximum-principle-tensor and D10-triangulation-low-dim) — but the task explicitly asks for the D12 cards with those names. Since the D12 modules don't exist, I could audit the D11/D10 cards that presumably are the basis. But that would not be the named cards. I could mention the closest available artifacts.
3. Deeper A3 counterexample search: attempt to falsify one or two D2/D3 claims? That's beyond the task scope (D12 cards), and the semantic-ledger says D2/D3 counterexample audits remain absent. My task is cross-audit of 360 cards, not D2/D3.
4. Do a stronger independent verification of the axiom audits: e.g., verify that the compiled oleans actually correspond to the sources I hashed (lake would rebuild if changed; my rebuild was from copied sources — good).
5. Check for "fake propositions" / weakened conclusions in each card more systematically, especially comparing card claims to Lean statements. I've done the headline checks.

Let me check the tarballs quickly, then write up.

Also, let me double check the `input/` dir of my worktree — maybe it contains a bundle of the D12 cards to audit (the D13 task may have been provisioned with inputs). My worktree was cloned from D6, so input/ has D5 artifacts. Let me check anyway for any D12 content.
dsh: reasoning:
No D12 card bundles in my worktree input. The control tarballs are fleet scripts.

Let me check if the fleet has a relay/inbox mechanism where remote results get pushed — maybe there's an `undici6.tgz` or a relay dir. Not worth more time. I've done an exhaustive name search on the whole `/data3/guoshaoyang/workdir/lean_poincare` tree, which is the shared filesystem. The two cards simply aren't there.

Now let me write the results. Before that, let me collect a few more evidence items:

1. The compile evidence: build logs exit 0, job counts, timings. Let me extract job counts per card from the build logs.
2. The source hash evidence: my own sha256 of the copied sources (in A3Meta.json), plus verification of the cards' recorded hashes (inventory.json).
3. `axiom_evidence`: per-card cones + negative control + forbidden scan.

Let me extract build job counts and timings.
dsh: reasoning:
Interesting: the builds all started at 12:32:56 and finished ~90 seconds later — meaning the `.lake/build` was NOT actually deleted (the earlier prepare run at 12:35 preserved... wait, timeline confusion). Anyway: the fresh copies plus lake's build system rebuilt what was needed and completed successfully. Actually, it seems the builds reused the earlier .lake/build artifacts? No: the first prepare run rmtree'd the dst, and then build ran at 12:31. Then the second prepare (preserve patch) at ~12:32 deleted non-.lake entries and re-copied sources, keeping .lake/build from the 12:31 build. Since the copied bytes are identical, the oleans are valid and lake skipped recompilation. That's fine — the compiled oleans correspond to identical source bytes (hash-verified). Hmm, but for maximal independence, would a skeptic say the oleans are stale? Lake verifies source hashes via its trace files: it checks the source file hash and would rebuild if changed. Since I re-copied identical bytes, the trace is valid. And `lake build` did run and reported success. Good enough, but let me make the evidence stronger: run a truly fresh build of ONE card in a pristine copy with no .lake/build, to demonstrate from-scratch reproducibility. Let me do that for one representative card (e.g., D12-connection-curvature or surgery-recognition). Cost ~2-3 minutes. This gives an unambiguous "fresh .lake/build" evidence.

Let me do that now: create `audit360/pkgs-fresh/D12-surgery-recognition` (full copy without .lake/build), build, and record. Also could do all 7 quickly in parallel since each took ~90s with warm caches; a cold build might take longer (job counts ~9000 include mathlib? No — mathlib is prebuilt; the 8957 jobs include the package's ~70 files plus... hmm, 8957 jobs for ~70 source files? That's the "jobs" count of the build plan including all imports? The numbers 8956/8957 etc. differ per package, suggesting they're counting build steps for the whole dependency closure (mathlib files built from cache? Lake plans all transitively required modules, mostly already built).

A cold build of one package would recompile its own ~70 modules (~1-3 min). Let me do a fresh build for 2 packages to strengthen evidence: connection-curvature and surgery-recognition (the two with blocker closures).

Let me set them up: copy from audit360/pkgs (excluding .lake) to audit360/pkgs-fresh/<card>, symlink packages, then `lake build` + probe. Run in background.
dsh: reasoning:
While the cold builds run (~few minutes), let me start drafting the results markdown. I'll structure it as:

# D13-cross-audit-360-cards — adversarial cross-audit result card

- Task id, verdict, elapsed, scope
- Method (independent rebuild pipeline)
- Per-card verdict table
- Detailed findings
  - hash provenance
  - compile evidence
  - axiom evidence
  - exact_blockers_closed confirmations/refutations
  - semantic findings (defects)
  - missing cards
- A3 assessment
- Remaining blockers / next dependency requests
- Evidence index (files, hashes)

And a JSON with the required fields: proved_declarations, expanded_hypotheses, semantic_class, exact_blockers_closed, remaining_blockers, source_hashes, compile_evidence, axiom_evidence, next_dependency_requests, elapsed.

Let me gather the per-card data for the JSON:
- For each card: audited declarations (list of fq names), hash summary, build rc, probe rc, cones histogram, exact_blockers_closed (from producer card, with audit verdict), remaining blockers count, card status.

Let me write a script that composes the results JSON from the collected artifacts, then write the md manually.

First, let me decide the overall verdict/status:
- status: "TASK_IN_PROGRESS" (7/9 cards fully re-verified; 2/9 source-absent; A3 remains open).
- Actually the deliverable is the audit; the audit itself is complete for what exists. The milestone "re-verify the 9 cards" is not fully checked → TASK_IN_PROGRESS with a concrete blocker item.

Let me now write the composer script.
dsh: reasoning:
The elapsed computation uses a hardcoded start epoch (1757500000) which is wrong. Let me record the actual start time: the task started at 2026-09-11T12:23:11+08:00 (from the queue started_at). Current time ~13:05. Let me compute elapsed = (now - start)/3600 with start parsed from a fixed ISO string. Let me fix.

Also check the cold builds.
dsh: reasoning:
Cold builds are progressing (they're rebuilding the base modules from scratch — no .lake/build). These will take a few minutes.

Meanwhile, let me write the results markdown. It should be comprehensive but readable. I'll write it after the cold builds finish so I can include their evidence. Actually, I can write it now and update the cold-build evidence after.

Let me draft the markdown now.
dsh: reasoning:
Cold builds still running. Meanwhile, let me run compose.py to generate the JSON (it will include what exists now; I'll rerun after cold builds and add fresh evidence). Actually, let me first check what fields might fail: `out["remaining_blockers"][card]` etc.

Let me run compose.py now.
dsh: reasoning:
Bug: WT in compose.py is `os.path.dirname(HERE)` = the worktree (D13...), but producer worktrees are siblings. I need `SIB = dirname(WT)` = worktrees dir. Currently WT = .../worktrees/D13-cross-audit-360-cards; producer paths need `os.path.join(os.path.dirname(WT), card, ...)`.

Also results output path uses WT/longrun/results → correct.

Let me fix: add `SIB = os.path.dirname(WT)` and use SIB for producer worktrees.
dsh: reasoning:
Results JSON written. Elapsed 0.3h? The start time was 12:23 and now ~12:40? Let me check current time. Actually the elapsed says 0.3h = 18 min, so current time ~12:41. Hmm, earlier logs said 12:34... so we're at ~12:41. OK, time flies. I have plenty of budget.

Wait, the clock: earlier `date -Is` said 12:35 at around the analyze step. Now ~12:41. Fine.

Let me verify the JSON contents look right, then wait for cold builds, update the md compile evidence, and do a final checkpoint.

Also, I should consider doing more adversarial work within the remaining budget. Options:
- Deeper vacuity screen using Lean itself: for each headline theorem, check hypotheses are satisfiable by constructing instances (already done for several).
- Attempt to independently verify the D12-connection-curvature `conformal_curvature_nonzero` claim by computing the curvature? It's a Lean theorem; kernel-checked. Not needed.
- Cross-check the D12-semantic-ledger's classification of the D7 recognition deliverable (conditional). Already consistent with the surgery card.
- Check the queue's gate_failed status for semantic-ledger.
- Check the producers' negative controls (they have their own; my independent one suffices).

I think the audit is substantively complete. Let me also double check the JSON's `remaining_blockers` for the semantic-ledger (23 items — likely long). Fine.

Let me inspect the JSON size and a sample.
dsh: reasoning:
The producer_lanes mapping in scope is slightly wrong: CARDS[:5] = connection-curvature, volume-ibp, spectral-sobolev, semantic-ledger, comparison-geodesics — but comparison-geodesics is 360-2, and the missing tensor-maximum-bochner is 360-1. Let me fix the lane mapping:
- 360-1: connection-curvature, volume-ibp, tensor-maximum-bochner, spectral-sobolev, semantic-ledger
- 360-2: comparison-geodesics, geometric-compactness, triangulation-topology, surgery-recognition

Let me patch compose.py.

Also, I realize the `proved_declarations` for semantic-ledger only includes 7 resolved in-package names; the 2 snapshot ones are recorded in the extra probe. I should add them to the semantic-ledger proved_declarations list in the JSON (with a note). Let me include them: append the two names to the list if they're in the snapshot audit cones.

Also, for completeness, `cards` entries should include the two missing cards with present=false. Let me add them.

Let me patch compose.py and regenerate.
dsh: reasoning:
JSON is complete. Note: `audited=7/7` for semantic-ledger in the cards entry but proved_declarations has 9 — I should reflect the +2 in the card entry. Let me add "audited_extra_snapshot": 2 to the semantic-ledger card record. Minor; let me patch for accuracy.

Also, the card record's `audited` should perhaps be 9 for semantic-ledger. Let me set audited = 9 with a note field.

Let me also check the cold builds and then update the md with results.
dsh: reasoning:
Cold builds are near done (8946/8957). Wait — that's suspiciously fast for a cold build (started ~12:38, now ~12:45). It seems lake found the oleans? The pkgs-fresh copy excluded `.lake`, so there should be no build cache... but the build shows [8946/8957] with ~4s per module — that's just the last few. Hmm, cold build of ~70 modules at a few seconds each ≈ 3-5 min. Plausible.

Let me wait for them to finish. Meanwhile, let me deepen the semantic audit slightly: check whether any of the cards' theorem statements are *overstrong* in a way the producer claims are general. Specifically:

1. D12-volume-ibp card claims "chart change of variables ... `fderiv_injective`" etc. — fine.
2. D12-spectral-sobolev card claims "arbitrary f in L² of the probability Haar measure" — check `AddCircle.haarAddCircle` is a probability measure. In mathlib, `AddCircle.haarAddCircle` is the Haar measure on AddCircle T with total mass T? Let me check: `AddCircle.haarAddCircle` is a probability measure? In mathlib, `MeasureTheory.Measure.AddCircle.haarAddCircle` is the Haar measure with `haarAddCircle univ = T`? Hmm. The card says "probability Haar measure". Let me verify quickly — this is a "semantic_class" claim; if the measure isn't a probability measure, the claim is wrong but the math still fine. Let me check mathlib.

Actually let me grep mathlib for `haarAddCircle` definition.
dsh: reasoning:
Confirmed: `haarAddCircle` is a probability measure in mathlib. The spectral card's claim is accurate.

Let me now check the cold builds again; then finalize. Also, I want to double-check the `D12-connection-curvature` card's claim in exact_blockers_closed about downstream use `so3_ricci_e00`; I verified `so3_curvature_nonzero` and `so3_ricci_symm` context. Let me look at `so3_ricci_e00` itself.
dsh: reasoning:
`so3_ricci_e00` genuinely uses the general contraction formula `ricci_contraction_eq_sum_basis` (the downstream theorem) applied to the Milnor/mean Levi-Civita data, computing 1/2. Good confirmation.

Cold builds: connection-curvature fresh build log's last line is an `info:` from Audit/EvolutionAudit — meaning it's building audit modules; surgery at 8970/8993. Let me wait a bit.

Let me use the time to also verify the "expanded_hypotheses" claims of a couple of cards against the Lean definitions — e.g., volume-ibp's ChartMetric structure vs the card's description. Quick check.

Actually, more valuable: let me run a final consistency check that my results' claims about counts are accurate, and prepare the final checkpoint.

Let me wait for the cold builds to finish, then run their probes, then update the MD §4 with the fresh evidence, regenerate JSON (adding cold build evidence), and write the checkpoint.

Let me poll the cold builds.
dsh: reasoning:
Cold builds (no `.lake/build` at all) succeeded for both connection-curvature and surgery-recognition, and their probes also exited 0. Let me verify the fresh probe logs have the full declaration sets (119 and 30) and no errors.
dsh: reasoning:
Cold rebuild evidence confirmed:
- connection-curvature: 72 build steps (its own modules), 119/119 declarations probed, 0 errors.
- surgery-recognition: 108 build steps, 30/30 declarations probed, 0 errors.

Now let me update the compose script to include cold-build evidence in compile_evidence, regenerate JSON, and update the MD §4 accordingly.

Let me patch compose.py to add a `cold_rebuild` entry for these two cards.
dsh: reasoning:
Hmm, the fresh "jobs" values (8956/8957, 8992/8993) are the same as the warm builds. That's because the fresh build reached the same final job index (the plan's total). Fine.

Wait, but the cold probe log only had 119 lines matching "depends on axioms|does not depend" — good.

Note the jobs field is a list because re.findall captured groups. Let me fix to take the last tuple: `fjobs[-1]` is `("8956","8957")`. It's fine but let me store just the total. Minor cosmetic; acceptable.

Now update the MD §4 table with the cold results, and adjust the claim "≈90 s each" — the cold builds took ~3-4 min. Let me check actual cold durations: started ~12:38:30 (launch), done 12:42:01 and 12:42:17 → ~3.5 min.

Let me update MD §4.
dsh: reasoning:
Now let me add evidence pointers to the MD (log hashes are in the JSON; the MD mentions files). Also fix the reproduction section to mention `pkgs-fresh`.

Then update the checkpoint with the completed audit and TASK_IN_PROGRESS status.

But before finalizing: let me reconsider whether there's more substantive verification I should do with the remaining budget (I've used ~40 min of 4h). The task asks for an adversarial audit. I've done:
- hash provenance
- rebuild
- axiom audits with negative control
- forbidden scans
- semantic review of headlines and closures
- missing cards documented

Additional high-value work:
1. **Independent check of the producers' own audit claims** (e.g., their `AxiomAudit.lean` modules and `d12_axiom_audit.py` scripts): verify their audit scripts actually fail closed (negative control). I ran my own, so this is secondary.
2. **Statement-level falsification attempts**: try to construct counterexamples to a couple of headline theorems. E.g., test `sturm_zero_comparison` with concrete k₁,k₂ via Lean? That requires proving an instance — time-consuming.
3. **Check the D12-spectral-sobolev heat series convergence more carefully**: the statement `HasSum (fun n => heatWeight • fourierCoeff • fourierLp 2 n) (heatEvolve ...)` — is `fourierLp 2 n` normalized? If not normalized, the series wouldn't converge to heatEvolve. But it's kernel-checked, so fine.
4. **Verify the semantic-ledger's classification claims** (14 genuine-general, 7 conditional, 13 model, 2 statement-only) — its manifest lists 36 entries; I could spot-check the classification of a few. Let me do a quick check of the ledger JSON: count classes and check a couple of entries against the referenced declarations' actual axioms/statements. This is part of auditing the semantic-ledger card (whose deliverable is the ledger itself).
5. **Check the D12-geometric-compactness card is in_progress** — its card may be incomplete; my audit verified what exists.

Let me do #4 quickly (the semantic ledger's classification counts) and also validate that its ledger entries reference declarations that exist (spot-check a sample via my probes? The ledger covers D1-D11 declarations, not in my packages). I can at least check the JSON's internal consistency: the class counts and the declared lists.

Let me look at the ledger JSON summary.
dsh: reasoning:
The semantic ledger's main chain has 36 entries with class counts 14/7/13/2 exactly as the card claims. Good. Its `proved_declarations` has 7 (the in-package ones — matching my 7 resolved + 2 extra = 9 audited).

Hmm, the ledger's `proved_declarations` (7) may not include the 2 audit probe declarations; the card's `proved_declarations` had 9. Fine.

Let me now finalize:
1. Update the MD with a note that the ledger classification counts were cross-checked (14/7/13/2) and the card's own counts match.
2. Update checkpoint.json.
3. Final JSON/MD consistency.

Also, I should decide the final status once more. The task instruction says "End TASK_DONE only if the milestone is fully checked; otherwise TASK_BLOCKED or checkpoint and TASK_IN_PROGRESS." I'm at a natural stopping point with 7/9 verified. The invocation has 4 hours but the audit is complete for the available artifacts. Options:
(a) End session with TASK_IN_PROGRESS and a checkpoint (the supervisor will re-invoke later after remote cards sync).
(b) Use remaining time to attempt to locate the remote artifacts further (e.g., check if hosts 360-1/360-2 have ssh/network access — no, and I shouldn't).
(c) Do additional independent verification (more semantic checks) to strengthen the audit of the 7 available cards.

Given the 4-hour budget and the value of deeper verification, let me spend a bit more on (c) but wrap up soon. Specifically:
- Verify the axiom evidence of the *producers'* headline claims that I haven't explicitly probed: all 342 covered.
- Check the D12-geometric-compactness "statement-only" props are not consumed by proved theorems (card claim). I can check imports/uses by grepping for the Prop names in the module files.
- Check the D12-volume-ibp "Blocked.lean state-only Props never used" claim.
- Check the D12-connection-curvature "Manifold-level CovariantDerivativeCurvatureStatement still missing" claim.

These are card claims I can verify with grep. Let me do a quick batch.
dsh: reasoning:
The geometric-compactness statement-only props have non-definition references (4, 1, 10, ...) — but these could be references in docstrings/comments or in `*_iff` shape lemmas, and possibly in the definitions of each other (e.g., cheegerGromovCompactness might be referenced in ancientKappaCompactnessFrontier's definition). The card claims "No inhabitant is constructed, none is assumed, and no proved theorem consumes them." I should verify that no *proved theorem* (the 39 declarations) has one of these props as a hypothesis. My probe's `#check` types include all 39 — I can grep the types file for those prop names.

Let me check.
dsh: reasoning:
The occurrences are the definitions themselves in the proved-declarations list (they're listed as `def`s, so `#check` prints them). Let me confirm that no *theorem* has them as a hypothesis. The occurrences at lines 113, 116 etc. are their own declarations (defs). The card lists them under "statement_only_explicitly_excluded" not in the proved set (per its JSON categories: general 17, model 14, statement_only 8 — the 39 in my probe include the 8 statement-only because the card's JSON `proved_declarations` is a dict with categories and I collected all names including statement-only!).

Wait: my `collect_decl_names` collects all lists in the dict, including `statement_only_explicitly_excluded` (8 names). So my 39 audited names include the 8 statement-only Props. That's actually good (they're audited too), but the card's "proved declarations (39 total; classes)" table in the md says 39 total including the statement-only ones under a separate heading. Let me recount: general 17 + model 14 + statement_only 8 = 39. So the card itself counts 39 including statement-only. Fine — my count matches, and I should note that the 39 include 8 statement-only Props with axiom cones (defs of Prop have cones too). No issue.

Let me verify that the `curvatureBoundImpliesUniformCovers` occurrences in types are all self-references and not hypotheses of other theorems. Let me grep the context.
dsh: reasoning:
Confirmed: the frontier Props appear only in their own declarations and `_iff` shape lemmas; no theorem consumes them as hypotheses. The card's claim holds.

Now let me finalize. I'll:
1. Update the MD §2 and §5 with the cross-check of the semantic ledger (36 entries 14/7/13/2) and the statement-only props check.
2. Update checkpoint.json with the final audit state.
3. Run one final compose + verify deliverables exist.

Let me also include in the MD the elapsed time (about 0.6h so far) and the final status.

Let me add a small "cross-checks" paragraph.
dsh: reasoning:
Now, I still have time. Let me consider whether I can add more independent verification value:

1. **Re-verify the producers' own `AxiomAudit`/`d12_axiom_audit.py` claims** — specifically that their scripts fail closed. E.g., run their `tools/d12_axiom_audit.py` in my package copy and see if it passes/fails as they claim. But my own audit supersedes it. Still, checking their tools might reveal that their audits are weaker (e.g., not fail-closed), which is a useful audit finding. Let me quickly run the producers' audit scripts in my copies:
   - D12-volume-ibp: `Poincare/D12/VolumeIBP/audit.py`
   - D12-comparison-geodesics: `tools/d12_axiom_audit.py` (but tools/ wasn't copied — it's outside release; I can copy it)
   - D12-geometric-compactness: `tools/d12_axiom_audit.py`
   - D12-spectral-sobolev: `tools/d12_axiom_audit.sh`
   - D12-surgery-recognition: `tools/d12_axiom_audit.py`
   - D12-connection-curvature: uses `Audit/D12/D12Audit.lean` + maybe print_axioms.lean
   
   Running them takes time but is quick. The value: verify the producers' claims that their audits PASS and are fail-closed (e.g., they throw on violation). I can also plant a violation to test fail-closed behavior — but that modifies my copy only (fine).

2. **Statement-level semantic diff**: compare each card's prose claims against the Lean statements for overstatement. I did the headlines. There might be specific claims I should verify:
   - volume-ibp: "no smoothness assumed" for density — the card says density smoothness is proved from adjugate/det. My probe confirms `density_contDiff` exists with cone. Let me check its statement/hypotheses to ensure smoothness isn't assumed in ChartMetric. ChartMetric has `smooth : ∀ i j, ContDiff ℝ 2 (g i j)` in the VolumeIBP card? Earlier Basic.lean showed `smooth` field. The card says "smooth/continuous/measurable (inverse-metric entries smooth entrywise through adjugate/det — no smoothness assumed)". Let me verify the ChartMetric structure fields to see if `smooth` is a field (it is: line 63 structure with `smooth`). So smoothness of g IS assumed (C²), and gInv smoothness is derived. The claim "no smoothness assumed" refers to gInv. Fine.

3. **Check the D12-spectral-sobolev proof of `heatSeries_hasSum` for a subtle issue**: `HasSum (fun n => heatWeight • fourierCoeff • fourierLp 2 n) (heatEvolve ...)`. Is `fourierLp 2 n` an orthonormal basis? In mathlib, `fourierLp 2 n` for AddCircle is `(fourier n).toLp`? The `HasSum` in L² with `•` scalars. It's kernel-checked; fine.

Let me do #1 (run producers' audits) as an extra layer, with a fail-closed probe. Actually, running their scripts may require their environment/venv; they're Python scripts that shell out to lake. Let me try one or two in the background.

Actually, a more valuable use of time: **verify that no producer audit tool is used as evidence in my results** (I don't rely on them) and check whether the producers' claims about their own audit coverage match mine (e.g., connection-curvature claims 172 declarations audited; my probe covers 119 declared names + the base 53 others? The card's `axiom_evidence` says "172 D12 declarations"). This discrepancy is worth quantifying: their audit covers 172 declarations in the D12 namespace, while the card's `proved_declarations` lists 119. Both can be true. My audit covers the 119 declared; the extra 53 are likely lemmas not in the card inventory. Should I audit those too for completeness? That would catch a hidden `sorry` in a declaration not listed in the card. Hmm — that's a real gap in my audit: a producer could list only "clean" theorems while a helper lemma uses `sorryAx`, and the headline theorem's cone would show sorryAx (since #print axioms is transitive). Wait — #print axioms is transitive: if a listed theorem depends on an unlisted lemma that uses sorryAx, the cone includes sorryAx. So auditing the listed declarations transitively covers all dependencies. But a `sorry` in an unused lemma wouldn't be caught. To be thorough, I could audit *all* declarations in the D12 namespace, not just the card's list. My forbidden scan already covers `sorry` tokens in sources (comment-aware), which catches any literal `sorry`. Combined with the transitive cones, coverage is good.

Still, let me strengthen: run an audit over *all* declarations in each rebuilt package (not just card-listed ones) by parsing the environment? Lean can't easily enumerate declarations without metaprogramming. But the producers' D12Audit.lean files do exactly that for connection-curvature (172 declarations). Let me run the producers' own audit modules as a *secondary* check while noting they are producer artifacts. Actually, my forbidden scan + transitive cones is sufficient; and running producer tools is lower value.

Better use of time: **check for `sorry` in dependencies outside the release tree** — e.g., the vendored VKPort or D7 modules. My forbidden scan only covered the copied release package (all files, including vendored D7/VKPort). Yes, scan_forbidden walks the whole package. Good — but wait, in the analysis I scanned `pkg` (the copy) which includes vendored modules. The reported hits were only the documented ones. So no `sorry` anywhere in the compiled sources of the 7 packages. 

Let me verify that claim: the scan found hits only in D6AuditReport/ReleaseAudit/«unsafe» (filtered) and the negativeControl axiom. So yes, no sorry/axiom/admit/unsafe/native_decide/proof_wanted in any of the 7 packages' compiled Lean sources. That's a strong result.

Wait — VKPort (10 files) was in surgery-recognition's package and got scanned; its README may mention tokens but those are comments. Good.

Now, one more check: the D12-spectral-sobolev package's build was 8953 jobs but its .lake was 16GB in the producer worktree — the producer may have used heavy computation. My rebuild was fine.

I think the audit is complete. Let me do a final verification pass of the deliverables:
1. Re-run compose to ensure JSON matches the final MD.
2. Verify MD/JSON exist and are non-trivial.
3. Ensure no `sorry`/axiom in MY deliverables (the prompt forbids them in proofs; my deliverables are docs/scripts).
4. Double check the JSON has all required fields: proved_declarations, expanded_hypotheses, semantic_class, exact_blockers_closed, remaining_blockers, source_hashes, compile_evidence, axiom_evidence, next_dependency_requests, elapsed.

Let me also make sure my results MD mentions elapsed time explicitly. Let me add it to the header. Then final compose/verify.
dsh: reasoning:
Wait, elapsed is 0.36h — that means the current time is ~12:45?? Let me check the actual time. The start was 12:23:11. If elapsed is 0.36h = 21.6 min, then now is ~12:45. But I've done a lot since 12:35... Let me check `date`. Maybe the system clock differs from my perception; earlier `date -Is` showed 12:35 at one point, and now compose says 0.36h → 12:45. OK, so everything happened fast (the tool calls are quick). Fine. I have ~3h15m left in the budget, but the work is essentially done.

I could use some of the remaining time to deepen the audit. Valuable additions:

1. **Audit all declarations in the D12 namespaces, not just card-listed ones.** I can do this using Lean metaprogramming: write a Lean file that imports the package's modules and uses `Lean.Environment` to enumerate constants whose name starts with the D12 root, then prints axioms for each. But `#print axioms` at elaboration can't be generated dynamically without `run_cmd`/`#eval` metaprogramming. However, I can write a Lean metaprogram:

```lean
import Lean
open Lean Elab Command
run_cmd do
  let env ← getEnv
  let names := env.constants.toList.filter (fun (n,_) => n.toString.startsWith "Poincare.D12.")
  for (n,_) in names do
    logInfo m!"CHECK {n}"
```

Then to get axiom cones programmatically, use `Lean.Util.CollectAxioms.collect`? There's `CollectAxioms.collect (env) (constName)`. Let me check the API in the installed Lean. In Lean 4, `Lean.Util.CollectAxioms` has `collect : Environment → Name → NameSet` maybe. The semantic-ledger probe imported `Lean.Util.CollectAxioms` and `Lean.Elab.Command`, suggesting they used it.

I could write a metaprogram that:
- enumerates all constants in the D12 module root,
- computes their axiom sets,
- prints `AXIOM <name> [..]`,
- and fails (throws) if any axiom is outside the allowed set.

That would give a *complete* audit of every declaration in the D12 namespace, catching unlisted declarations. This is a stronger, more independent verification than the producer's. Let me do it — it's high value for an adversarial audit.

Let me look at how the semantic-ledger probe used CollectAxioms to copy the API.
dsh: reasoning:
The producer's metaprogram uses `Lean.collectAxioms d` and `throwError` for fail-closed behavior. I can write my own complete-enumeration audit:

```lean
import Lean
import <all card modules>
open Lean Elab Command

run_cmd do
  let env ← getEnv
  let root := `Poincare.D12
  let allowed : List Name := [``propext, ``Classical.choice, ``Quot.sound]
  let mut count := 0
  let mut bad : Array (Name × List Name) := #[]
  for (n, ci) in env.constants.toList do
    if (`Poincare.D12).isPrefixOf n then
      -- skip the audit's own declarations and the negative control axiom
      count := count + 1
      let axs ← Lean.collectAxioms n
      let unapproved := axs.toList.filter (fun a => !allowed.contains a)
      if !unapproved.isEmpty then bad := bad.push (n, unapproved)
  logInfo m!"A3ALL: {count} declarations under Poincare.D12"
  for (n, u) in bad do logError m!"A3ALL-BAD: {n} -> {u.map Name.toString}"
  if !bad.isEmpty then throwError "A3ALL: FAIL"
  logInfo m!"A3ALL: PASS"
```

Concerns:
- `env.constants` includes definitions/theorems/axioms in the modules imported. Iterating ~10k constants is fine.
- `Lean.collectAxioms` may be expensive per constant (it traverses the value). For a few thousand D12 constants, could take a while but is doable. Let me limit to `Poincare.D12` prefix (each package has ~100-300).
- For declarations with no value (axioms/opaque), collectAxioms returns the name itself? For an axiom, its axioms = {itself}. The volume-ibp `negativeControl` axiom would be flagged (expected; I'll whitelist it).
- Efficiency: collectAxioms on the same constant repeatedly is fine.
- I need to import all modules of the package: the A3Probe already imports the declaring modules only. For the full audit, I should import all modules in the package (including ones not in the card's list). The package's `lake build` built the default targets, which may exclude some root modules (e.g. ProbeScratch). I'll import all modules that are part of built targets: Poincare.*, Audit.*, Ledger.*, Probe.*, plus ReleaseCheck/ReleaseAudit/D6AuditReport if built. Then run.

Let me write `A3FullAudit.lean` per card (generated by a script) with imports = all modules (excluding the unbuildable root ones — actually I can include them if they were built... `lake build` default targets: Poincare, Probe, Ledger, Audit, ReleaseCheck, ReleaseAudit, D6AuditReport. Root files not in these (D6LedgerProbe, ReleaseClaims, ProbeScratch, D12RealModuleProbe) aren't built. So import all modules under those libs.

Simpler: for each package, import all modules that have an .olean present in `.lake/build/lib/lean`. I can generate the import list from the build output directory! That guarantees imports resolve.

Let me implement:
- For each package, walk `.lake/build/lib/lean/**/*.olean`, derive module names, exclude `A3Probe`/`A3Extra`.
- Generate `A3FullAudit.lean` with those imports + the run_cmd enumeration restricted to `Poincare.D12` (and maybe also `Poincare.D7`/`D10`/`VKPort` for the surg/semantic packages? The task is about the D12 cards; the D12 namespace root covers their contributions. For vendored modules, the cards' claims rest on them; my probe audited the listed declarations which transitively include their dependencies. Full-namespace audit for D12 is the key addition.)
- Run `lake env lean A3FullAudit.lean`, capture output, check for PASS and count.

Then update results with "complete namespace audit: N declarations, 0 outside allowed cones (plus documented negative control)".

This is a strong addition. Let me do it — time budget is ample.

Note: `env.constants` includes private declarations with mangled names (`_private...`), and the D12 namespace may include constructors/recursors. All should have allowed cones. Some might have `sorryAx` if the producer hid a sorry in an unlisted lemma — this will catch it!

One nuance: `Lean.collectAxioms` on constants defined in terms of `Classical.choice` etc. returns the standard set. For structures/inductives, returns ∅. Fine.

Let me write the generator.
dsh: reasoning:
All full audits failed fast (rc=1) — probably a compile error in my metaprogram. Let me look at a log.
dsh: reasoning:
Metaprogramming syntax issues in the m!"..." interpolation: `{count}` where count is a `Nat` should work... The error says OfNat MessageData 0 at line 83:19 — that's `let mut count := 0`? In `run_cmd do`, `let mut count := 0` needs a type annotation because of metavariable issues? The error is `OfNat MessageData 0` which suggests `count` was inferred as MessageData due to later use in m!"{count}". Lean's `let mut count := 0` with no other constraint should default to Nat... but the interpolation `m!"A3FULL: {count} declarations"` should coerce Nat to MessageData via `ToMessageData`. Hmm, maybe the issue is that `count` is used both as Nat and in interpolation, and `OfNat MessageData 0` arises from `let mut count := 0` being elaborated after the m!"..." use? No, order is top-down.

Fix: annotate types explicitly: `let mut count : Nat := 0`, `let mut bad : Array (Name × List Name) := #[]`, `let mut skipped : Array Name := #[]`. Also the m!"..." with `{bad.size}` may need `Nat` — fine.

Actually the specific failing line numbers 83 and 91 in the generated file: those are `logInfo m!"A3FULL: {count} declarations..."` (83) and `logInfo m!"A3FULL: PASS"`? Let me just annotate everything and avoid interpolation pitfalls by using `MessageData.ofNat count` etc. Simpler: use `logInfo m!"A3FULL: {count} declarations"` with count : Nat should be fine once annotated.

Let me also double check `root.isPrefixOf n` — Name.isPrefixOf exists. `env.constants.toList` — env.constants is a NameMap; `.toList` exists. `Lean.collectAxioms n` returns `NameSet`? In the producer's code: `let axs ← Lean.collectAxioms d; let bad := axs.toList.filter ...` — so it's a monadic function returning something with toList. Good.

Let me patch the generator with explicit type annotations.
dsh: reasoning:
Excellent! The complete-namespace audits PASS for all 7 packages:
- volume-ibp: 159 declarations under Poincare.D12, all cones ⊆ allowed, with the documented negative control skipped.

This is a much stronger result than the card-listed probes: it enumerates *every* constant in the D12 namespace in the compiled environment, so no hidden `sorry` in unlisted declarations.

Let me collect the counts per card and add this to the results (JSON + MD). Let me extract the counts.
dsh: reasoning:
Full-namespace audit results:
- connection-curvature: 260 declarations, 0 bad
- volume-ibp: 159, 0
- spectral-sobolev: 68, 0
- semantic-ledger: 28, 0
- comparison-geodesics: 139, 0
- geometric-compactness: 71, 0
- surgery-recognition: 378, 0
Total: 1103 declarations under Poincare.D12 across the 7 packages, all with cones ⊆ allowed (plus the documented negative control skipped).

Wait — the D12 namespace counts include the same names across packages? No, each package is separate; 260 for connection-curvature includes its own D12 modules. The sum 1103 is the union of per-package counts (some overlap because the D6 base has no D12, so no overlap at all except... each package has exactly one D12 module tree, so no overlap).

This is a strong result to add. Let me update the analysis: add a `full_namespace_audit` section to the results JSON and MD. Let me patch compose.py to read these logs, then regenerate.
dsh: reasoning:
Now update the per-card table note about the "342 declared names" to also mention the 1103 full-namespace audit, and update the reproduction section to include gen_full_audit/run_full_audit. Also update the "Elapsed" line (~0.55h).

Then finalize the checkpoint and end.

Let me make these small edits.
dsh: reasoning:
Now let me finalize. A few remaining tasks:
1. Validate the MD and JSON are consistent and complete.
2. Check the required JSON fields: proved_declarations ✔, expanded_hypotheses ✔, semantic_class ✔, exact_blockers_closed ✔, remaining_blockers ✔, source_hashes ✔, compile_evidence ✔, axiom_evidence ✔, next_dependency_requests ✔, elapsed ✔.
3. Update checkpoint.json final.
4. Confirm no forbidden tokens in my own Lean files (A3Probe etc. contain none since generated; A3FullAudit contains no sorry).

Also, I should double check my own scripts don't contain `sorry` etc. (they're Python/Bash; A3NegativeControl intentionally does).

Let me do a final integrity check of the deliverables and update the checkpoint with the full-namespace audit result.
dsh: reasoning:
All required fields present. `audited total: 344` (342 + 2 snapshot for semantic-ledger).

Now let me update the checkpoint one final time (with the full-namespace audit results and final deliverable status), and do a last review of the results MD for accuracy.

One correction: in the MD header I say "Elapsed: ≈0.6 h" — let me verify actual elapsed from compose: 0.4h. Let me set it to ≈0.5 h to be safe, or better, remove the specific number and reference the JSON. Let me update to "≈0.5 h of the 4 h invocation".

Also the MD says "342 declared names ... re-audited" — plus 2 snapshot = 344. Let me adjust to "342 in-package + 2 snapshot declarations".

Let me make these edits.
dsh: reasoning:
Now let me do a final read of the results MD to check for errors and coherence (full read), then regenerate the JSON one last time (to reflect the MD changes? compose doesn't read the MD, so no need). Actually the JSON's `elapsed_hours` was computed at compose time; let me re-run compose once more to refresh elapsed and ensure everything is current. Then final verification.

Let me read the MD fully to check it.
dsh: reasoning:
The MD looks good. One correction: §2 says "D12-geometric-compactness | in_progress" — the card JSON has no status field and the checkpoint is in_progress. Fine.

Note "7/9 cards fully re-verified" and "independent probe" column. Fine.

One thing: the F5 reference "documented neg. control F5" is correct.

Let me refresh the JSON (compose) one last time, then do a final validation and end with a summary. Also, should I mark the checkpoint status as "in_progress" or something else? The checkpoint schema allows status values; the deliverable results say TASK_IN_PROGRESS. Keep consistent.

Let me run compose once more.
dsh: reasoning:
Everything is in place. Let me do a final sanity check on the worktree: ensure no other tasks' files were modified (I only wrote in my worktree and read siblings). Also confirm I didn't write into sibling worktrees: I only read them. My builds were in my worktree's audit360/pkgs copies. Good.

One last consideration: the `longrun/results/D13-cross-audit-360-cards.md` is the only file that can complete the task. Done.

Now, final answer summary to the user. Since this is a long-running task with checkpoint semantics, I should report:
- Status: TASK_IN_PROGRESS (not TASK_DONE) with the concrete reason.
- What was verified (7/9 cards, 344 declared names, 1103 full-namespace declarations, 4/4 closures confirmed).
- Findings (F1-F7).
- A3 remains open.
- Deliverables and where.
- Next steps.

I should keep it reasonably concise but complete. Let me also mention elapsed ~0.4-0.5h of the 4h budget, and that the remaining budget wasn't needed because all available artifacts were audited.

Wait — one thing to double check before finishing: the task says "Continue from checkpoint.json" — the initial checkpoint was a stub (status queued). I updated it. Good.

Also, the task says "update checkpoint.json at least every 30 minutes" — I updated at ~12:42 and ~12:52. Good.

Let me make sure the final checkpoint mentions the full-namespace audit in the cards section (I updated the verdict, but the per-card section may not). It's fine.

One more check: my results .md is mode 0600 (rw-------) — readable by owner, that's fine (produced by write tool with default umask?). The JSON is 0644. The deliverable needs to be readable; 0600 is fine for the same user.

Let me finalize with the summary.
The audit is complete for every artifact available on this host. Final state:

## D13-cross-audit-360-cards — result

**Status: `TASK_IN_PROGRESS`** (not TASK_DONE — the 9-card milestone is not fully checkable here). Deliverables written:
`longrun/results/D13-cross-audit-360-cards.md` / `.json`, plus evidence under `audit360/` and an updated `checkpoint.json`.

### What was independently re-verified (7/9 cards)
| card | hashes | rebuild | probe | closures |
|---|---|---|---|---|
| connection-curvature | 12/12 | exit 0 + **cold rebuild** | 119/119 | `LeviCivitaExistenceStatement` **CONFIRMED** |
| volume-ibp | 11/11 | exit 0 | 78/78 | none claimed |
| spectral-sobolev | 8/8 | exit 0 | 36/36 | none claimed |
| semantic-ledger | 66/66 | exit 0 + 37-module snapshot replay | 7/7 + 2/2 | none claimed |
| comparison-geodesics | 8/8 | exit 0 | 33/33 | none claimed |
| geometric-compactness | 6/6 | exit 0 | 39/39 | none claimed |
| surgery-recognition | 8/8 | exit 0 + **cold rebuild** | 30/30 | SR-5, covering recognition, `coveringTrivial` all **CONFIRMED** |

- **344 card-declared/snapshot declarations** re-audited; all cones exactly `{propext, Classical.choice, Quot.sound}`.
- **Complete-namespace metaprogram audit** (every constant under `Poincare.D12`, not just listed names): **1103 declarations, 0 unapproved axioms** — this closes the hidden-unlisted-`sorry` gap.
- Negative control compiled and correctly flagged (`sorryAx`, `native_decide`); forbidden-token scan clean except one documented, unused `axiom negativeControl : False` and two audit metaprogram `.«unsafe»` matches.
- No accepted base module was modified (byte-identical to D6); vendored D7/D10/VKPort copies identical across worktrees.

### Key findings
- **F1** connection-curvature card over-qualifies 73/119 declaration names with nonexistent file-derived namespaces (all declarations exist and compile).
- **F2** semantic-ledger attributes 2/9 declarations to a probe outside the release package (replayed and verified).
- **F3** `D12-tensor-maximum-bochner` and `D12-triangulation-topology` have **no worktree, release, card or module anywhere on this host** (remote_owned only) → **not auditable, not refuted**; this is why the milestone is not marked done.
- **F4–F7** reflexive model witnesses in comparison-geodesics, the documented negative-control axiom, queue/card status mismatches, and the algebraic-vs-fderiv scope nuance in connection-curvature.
- **No false statement found** in the 7 audited cards.
- **Named blocker A3 stays OPEN**: this audit adds D12-card adversarial coverage but does not perform the D2/D3 counterexample search A3 asks for.

The only way to complete the 9-card milestone is a re-invocation after the two missing producer packages are synced or re-run on this filesystem; `audit360/run_card.sh <card>` audits them in ~4 minutes each.
