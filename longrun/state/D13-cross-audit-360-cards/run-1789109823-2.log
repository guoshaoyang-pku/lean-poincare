dsh: reasoning:
Let me read the rest of checkpoint.json, the docs, and the current results file. This is invocation 4 apparently, with cumulative 2.75 hours. Let me understand the state.

Let me read the whole checkpoint.json and docs.
dsh: reasoning:
Let me read the current results markdown and json. The results md is 75KB. Let me check the structure first (headings), and read the top.

Also important: the checkpoint says "status: blocked", round 5 complete. My invocation 4 is limited to 4 hours. The blocker is that 2 of 9 cards have no source (tensor-maximum-bochner, triangulation-topology) and no transport route exists.

Let me first verify the current state, re-check the transport route (maybe things have changed — maybe now the cards exist somewhere?). Let me look at the results md headings and the audit360 directory.

Let me plan:
1. Orient: read results md structure, checkpoint, audit360 dir.
2. Verify environment: lean/lake available, the 7 card packages build.
3. Re-attempt transport/discovery for the 2 missing cards (search /data3, /data/home, network).
4. If still absent, do additional adversarial audits on the 7 available cards to strengthen the result — but careful not to just repeat. The task says "Independently re-verify the 360-produced D12 result cards (connection-curvature, volume-ibp, spectral-sobolev, comparison-geodesics, geometric-compactness, surgery-recognition, triangulation-topology, tensor-maximum-bochner, semantic-ledger)". 7 of 9 are present.
5. Keep checkpoint updated every 30 min, keep results current.
6. End with TASK_BLOCKED (for missing 2 cards) unless transport found.

Let me look at what remains genuinely useful. Since rounds 1-5 have already done a lot, this invocation should:
- Re-run the full sweep to confirm reproducibility (compile evidence, fresh).
- Re-verify source hashes.
- Re-run axiom audits.
- Confirm/refute exact_blockers_closed.
- Search again for the missing cards (maybe in a shared location or a new host).
- Possibly add new adversarial checks (e.g., semantic review of remaining cards, cross-card consistency, check for hidden assumptions).
- Update the results files.

Let me first inspect the results md and json structure, then check tools.
dsh: reasoning:
Good. The previous invocations did thorough work. Now for invocation 5 (checkpoint says invocation 4 was the last; my invocation should be 5). I have 4 hours.

Key tasks for this invocation:
1. Re-establish environment (lean/lake, card packages).
2. Fresh full sweep (round 6) — rebuild from source hashes, re-run axiom audits, confirm/refute exact_blockers_closed. This is the core requirement of the objective: "rebuild from source hashes, re-run axiom audits, confirm/refute exact_blockers_closed".
3. Search once more for the 2 missing cards (tensor-maximum-bochner, triangulation-topology). Maybe they appeared since.
4. Possibly new adversarial checks. To add value over previous rounds, I could:
   - Do a fresh independent rebuild in a clean copy to be sure the hash provenance is right (e.g., re-copy from producer snapshot and verify hashes).
   - Add a new check: "independent re-derivation of a nontrivial theorem" — e.g., write a new consumer file using only the card's declared public names and prove something new.
   - Check for `exact_blockers_closed` again with fresh consumer.
   - Verify no new declarations appeared (freeze).
5. Keep results/checkpoint updated.

Let me look at the audit360 directory structure and the scripts, see what run_round5.sh does, and check tools.
dsh: reasoning:
Elan has no default toolchain globally; presumably each package has a lean-toolchain file. Let me check run_round5.sh to understand how builds are run, and inspect a package.

Also, `ls -d logs*` returned exit 2 because logs-kind etc... no, it printed "logs" only? Actually output shows "=== logs ===" then nothing before stderr. Hmm, `ls -d logs*` from the root — there are no logs* in root (they're in audit360/). Right.

Let me read run_round5.sh.
dsh: reasoning:
The setup is clear. Now let me plan this invocation (invocation 5, round 6).

Plan:
1. **Recheck missing cards** — search the filesystem broadly for `tensor-maximum-bochner` / `triangulation-topology` / D12 packages; check any new mounts, network endpoints. This is the blocker; if found, huge win.
2. **Round 6 sweep** — run a fresh full sweep (build + probe + fullaudit + extras + negative control), verifying byte-identical outputs vs round 5. Also re-run the hash coverage / self-audit.
3. **Source-hash rebuild verification** — confirm the staged source copies match the producer snapshot hashes (the "rebuild from source hashes" requirement). Perhaps do a stronger version: copy the staged packages to a fresh temp dir, verify that build succeeds there from scratch (pkgs-fresh already exists — check what it did).
4. **Axiom audits** — re-run A3Probe/A3FullAudit (they do transitive axiom checks), plus maybe an independent axiom check with `#print axioms` on a sample.
5. **exact_blockers_closed confirm/refute** — re-run the closure-use consumers; maybe add a 6th independent consumer this round for each claimed closure (CC: 2 closures, SR: 4 closures). Actually the checkpoint says 4/4 claimed closures confirmed with 5 independent consumers for SR. Let me look at the exact closures.
6. **New adversarial checks**: I should add something genuinely new to make this invocation valuable. Ideas:
   - **Semantic review of the remaining cards not yet deeply reviewed**: spectral-sobolev, connection-curvature deep statement review. Checkpoint mentions deep reviews of volume-ibp, comparison-geodesics, geometric-compactness, surgery-recognition. spectral-sobolev and connection-curvature may not have deep semantic review. Let me check the results md section 13.4.
   - **A "statement-strength" comparison against upstream**: check whether card theorems with the same mathematical names as upstream have comparable hypotheses (e.g., Bishop-Gromov).
   - **Cross-card consistency check on shared concepts** (V2, piece lists, etc.) — done round 5.
   - **Fresh exhaustive axiom scan over the full namespace** (done: 1103 declarations PASS).
   - **A "no-new-axioms" check on compiling the probe with `--noSorry`?** Actually Lean doesn't have that flag; but there's `set_option autoImplicit false`? Hmm.
   - **Check the producer cards' claimed `exact_blockers_closed` against the card text**: maybe done.
   - **New: independent re-derivation of one substantive theorem** from each card using only its exported names, to prove the statements aren't vacuous: e.g., instantiate the main theorem in a concrete model and extract a concrete numerical/semantic consequence. This is the strongest form of "confirm closure" — a downstream checked use.
   - **New: "adversarial negation" tests** — for claimed nontrivial statements, try to show the conclusion is not provable trivially, e.g., construct a model where hypotheses hold but conclusion is non-trivial, or check the statement is not implied by `True`.
   - **New: hypothesis-minimality probing for the main theorems** (the unused-hyp screen covered all; but maybe deeper: for a key theorem per card, check which hypotheses are actually needed by attempting removal).

Given time (4 hours), I should prioritize:
- (a) Restart discovery of missing 2 cards (30 min).
- (b) Round-6 full fresh sweep in background (probably 1-2 hours of compute, run in background).
- (c) In parallel, deep semantic review of spectral-sobolev + connection-curvature (the two not deeply reviewed) — read the source statements and assess.
- (d) New independent consumer probe (6th) for exact closures.
- (e) Update results + checkpoint every ~30 min.

Let me check the timing: builds took... round 5 logs might show. Let me check how long a full sweep takes.

First, let me look at the results md sections 13.x to see what was done, and the per-card summary. Let me read some key sections.
dsh: reasoning:
Sweeps take ~2 minutes total. Great, that means I can do many rounds.

Now, key: what genuinely new value can I add in this invocation? Rounds 1-5 have done: hash provenance, fresh rebuilds, probes, full-namespace axiom audit, vacuity screens, unused hypothesis screens, kind screens, upstream cross-check, semantic reviews, closure consumers.

The objective for this invocation (round 6): "Independently re-verify the 360-produced D12 result cards (...). rebuild from source hashes, re-run axiom audits, confirm/refute exact_blockers_closed. Adversarial audit lane (A3)."

The deliverable requires: proved_declarations, expanded_hypotheses, semantic_class, exact_blockers_closed, remaining_blockers, source_hashes, compile_evidence, axiom_evidence, next_dependency_requests, elapsed time.

Since previous invocations produced these, I need to refresh them with round-6 evidence + any new findings.

New adversarial angles I can pursue this round (to make the audit genuinely additive and to hunt for real defects):

1. **Missing cards re-discovery** (must do; blocker determination).
2. **Round-6 deterministic sweep** (cheap, ~3 min) + hash re-verification + self-audit.
3. **New: cross-check the *card text* claims against the kernel** — for each card, extract the headline claims (e.g., "Theorem X proves Y"), and verify a sample with `#check` of exact types. Maybe done partly.
4. **New: "adversarial instantiation" of each card's headline theorem to a degenerate/trivial model** to detect vacuity: e.g., instantiate comparison-geodesics main theorems at a flat/constant model, check they are satisfiable and non-vacuous (existential conclusions inhabited). This is the strongest test: if the main theorem's conclusion can be *inhabited* by an independent construction, the theorem isn't vacuous.
5. **New: consistency hunt for false statements**: for cards with "statement-only" frontier Props, check whether the Prop is *false* under some interpretation, or whether the "theorem" only asserts existence of an arbitrary object. Actually the previous rounds flagged phantom params.
6. **New: "documentation vs kernel" name matching** — verify card-declared names match actual Lean names one-to-one, remaining F1 family (73 over-qualified names) validated. Done in round 3.
7. **New: check the cards' JSON schema fields for claimed counts** (e.g., number of files, counts of declarations, checksums) — done via inventory and hash_coverage.
8. **New: search for `axiom`, `opaque`, `unsafe`, `extern`, `implemented_by`, `@[implemented_by]`, `partial` in all card packages** — forbidden scan done round 5 for 6 cards; let me redo for all 7 and include `opaque`/`extern`/`@[extern]`/`partial`/`unsafe`.
9. **New: verify that the compiled olean actually corresponds to the source** — i.e., rebuild from a *pristine copy* with no `.lake` and re-run probes there (cold rebuild) — done for 2 cards in round 1. Could do a cold rebuild for all 7 in a fresh directory this round. That's the strongest "rebuild from source hashes" evidence. With ~3.5 min each cold, 7 cards → ~25 min. Feasible! Actually the round-1 note says cold rebuild ≈3.5 min for one card (8957 jobs). That's with mathlib cache; without cache it'd be hours. Since packages link the shared mathlib olean cache, cold rebuild of just the package source is ~3.5 min. 7 cards ≈ 25 min sequential. Good in background.
10. **New: independent re-derivation of a nontrivial closure** — e.g. write a fresh consumer using only public names.
11. **New semantic review of connection-curvature + spectral-sobolev** (the two cards lacking deep reviews per round 5). Let me confirm from §13.4 what was reviewed.

Also: the "expanded_hypotheses" field needs refreshing: it should capture all hypotheses added/expanded by the cards vs the upstream claims. And "semantic_class" — classify each card's content.

Let me check §13.4 and §10 to see what's left. And check the exact_blockers_closed claims.

Let me read sections 6, 7, 10, and 13.4-13.8.
dsh: reasoning:
Now I have a good picture. Round 5 deep-reviewed volume-ibp, comparison-geodesics, geometric-compactness (general metric theorems), and the frontier. Not yet deep-reviewed at that level: connection-curvature, spectral-sobolev, surgery-recognition (surgery closures were "REVIEWED-CORRECT" in round 3 semantic review), semantic-ledger.

So new value for this round (invocation 5):
1. Round-6 sweep with fresh rebuild + probes + full audit + extras + kind screen + negative control (~3-5 min).
2. Cold rebuild of the remaining 5 cards (comparison-geodesics, connection-curvature, volume-ibp, spectral-sobolev, semantic-ledger?) — actually round 1 cold-rebuilt connection-curvature and surgery-recognition. Cold rebuild for the rest would be strong fresh evidence. But cold rebuild requires fresh copy without .lake/build; depends on disk space. Let me check disk usage. ~3.5 min each * 5 = ~20 min. Feasible in background.
3. Missing-cards re-discovery: search again broadly, check network, mounts.
4. Deep statement review of D12-spectral-sobolev and D12-connection-curvature (two cards).
5. "Adversarial instantiation" test: for headline theorems, instantiate at concrete models to verify non-vacuity — especially spectral-sobolev (which has 36 declarations) and connection-curvature.
6. Forbidden-token scan including `opaque`, `extern`, `partial`, `unsafe`, `@[implemented_by]`, `axiom` across all 8 packages (including the semantic-ledger snapshot).
7. Refresh `expanded_hypotheses` and `semantic_class` in results JSON; add round-6 evidence, elapsed time; keep checkpoint updated every 30 min.
8. Re-verify exact_blockers_closed with maybe a 6th consumer for CC closures. Round 3 did CC with 4 new declarations. Round 5 did SR nonempty. A new CC consumer could be: instantiate the Levi-Civita existence at a *different* metric model (e.g., the SO(3) model already exists) — or better: use `leviCivitaExists` to prove something new that the card didn't: e.g., that the Milnor connection has vanishing torsion in a concrete chart, or that uniqueness holds. Hmm.

Actually, let me consider what would be most valuable mathematically: an independent verification that the claimed closed blockers are *actually used downstream* and that the statements they produce are non-vacuous. Round 3/5 did consumers. A fresh consumer for CC: derive from `leviCivitaExists` the *uniqueness* of the Levi-Civita connection (Koszul formula), or use it to construct a connection on a different metric (e.g., a warped/flat metric) and prove torsion-free + compatible. That's a genuinely new downstream use.

Let me first do the quick operational things: start round-6 sweep in background, then do the missing-card search, then deep reviews while it runs.

Actually one caution: parallel lake builds could contend on the shared mathlib cache (read-only, fine) and CPU. The sweep is ~2 min; cold rebuilds ~20 min. Let me sequence: start the round-6 sweep now (background), do filesystem search (cheap), then cold rebuild in background.

Let me set up the todo list and start.

First let me check the current time and disk space.
dsh: reasoning:
Good — 192 cores, plenty of RAM/disk. Time is 14:39; session started around 14:39 presumably (4h limit → until ~18:39).

Let me set the todo list and kick off the round-6 sweep. I'll create run_round6.sh based on round 5 with LOG=logs-round6, and add the snapshot + kind audit.

Actually, more valuable: do a **cold rebuild from pristine copies** for the 5 cards not cold-rebuilt in round 1. The round-1 cold rebuild was for connection-curvature and surgery-recognition. Wait — but the packages in audit360/pkgs are the staged copies (from release, no .lake). After `lake build`, they now have .lake/build. A cold rebuild = fresh copy of the staged release sources + fresh .lake link, no build cache.

Where do the staged copies come from? `audit360/prepare.py` presumably copies from producer worktrees. Let me check prepare.py to see the map from producer worktree → audit360/pkgs, and check pkgs-fresh.

Plan for round 6:
- Script A (round6 sweep): build/probe/fullaudit/extras/kind/negative control on existing pkgs → logs-round6.
- Script B (cold rebuild): fresh copies of the release sources of the 5 remaining cards into audit360/pkgs-cold6/<card>, link mathlib cache, `lake build` from scratch, then `lake env lean A3Probe.lean`. This is the strongest "rebuild from source hashes" evidence.

Actually wait: are A3Probe.lean etc. part of the cold copy? The probe file is auditor-generated, placed in the package. For cold rebuild, I copy the *release* sources (producer package) then add A3Probe.lean. Let me look at prepare.py to replicate.

Let me read prepare.py quickly.
dsh: reasoning:
Let me see where it reads producer worktrees from (the SRC map).
dsh: reasoning:
Line 142: `src_release = os.path.join(wt, "release")` — the producer worktree is presumably found earlier. Let me see lines 100-160.
dsh: reasoning:
Now I understand: producer worktrees are at `/data3/.../worktrees/D12-<name>/release`, and cards at `<wt>/longrun/results/<card>.json`.

Let me now do the missing-card search first (quick, in background-ish), and start the round-6 sweep. Also cold rebuild.

Let me write run_round6.sh (based on round 5, LOG=logs-round6), and a cold-rebuild script for the 5 remaining cards: volume-ibp, spectral-sobolev, semantic-ledger, comparison-geodesics, geometric-compactness. Wait, round 1 cold-rebuilt connection-curvature and surgery-recognition. Let me verify that claim from logs-round1.

Actually, cold rebuild means fresh copy without .lake. pkgs-fresh has connection-curvature and surgery-recognition. So the remaining 5: volume-ibp, spectral-sobolev, semantic-ledger, comparison-geodesics, geometric-compactness.

Cold rebuild approach: create audit360/pkgs-cold6/<card> by copying from producer release (no .lake), symlink shared packages, then run `lake build`. But careful: the `lakefile.toml` may reference `require mathlib from git ...` with a manifest. The `.lake/packages` symlink to shared packages avoids re-downloading. Build with shared mathlib oleans — the round-1 note says ≈3.5 min.

Note: cold rebuild of 5 cards may be CPU-heavy but 192 cores available. I can run them in parallel (they each use ~1 core mostly, but lake may parallelize). Let me run sequentially in one background job to avoid contention, or parallel with 5 jobs. Time budget is fine; sequential ~20 min.

Actually, let me combine: run round-6 sweep first (3 min), then cold rebuild (20 min) in the same background job to serialize CPU.

Also note: the cold copies must include A3Probe.lean (auditor's) — yes, prepare.py puts A3Probe.lean into pkgs/<card>/, but does it copy to pkgs-fresh? The pkgs-fresh listing shows A3Probe.lean present. In run_fresh.sh presumably the fresh dir was built by copying pkgs/<card> sources. Let me just construct cold dirs by copying pkgs/<card> excluding .lake (which already has A3Probe.lean).

Let me write the scripts now.

For discovery of missing cards, do a thorough search:
- find / -name "*tensor-maximum-bochner*" etc. but restricted to writable/readable areas. Previous rounds searched /data3/guoshaoyang. Let me search /data3, /data/home, /tmp, /var, and check mounts, network ports, tailscale.

Let me kick off the background job for round-6 sweep + cold rebuild, then do the search while it runs.

Script plan:

```bash
#!/bin/bash
# run_round6.sh
set -u
BASE=...
LOG=$BASE/audit360/logs-round6
export ELAN_HOME=...; PATH
CARDS="..."
for CARD in ...; do
  cd pkgs/$CARD
  # build, probe, extras, fullaudit, kind
done
# negative control
# snapshot
# a3d2d3
```

And cold:
```bash
COLD=$BASE/audit360/pkgs-cold6
for CARD in volume-ibp spectral-sobolev semantic-ledger comparison-geodesics geometric-compactness; do
  rm -rf $COLD/$CARD; mkdir -p; rsync -a --exclude .lake pkgs/$CARD/ $COLD/$CARD/
  ln -s shared $COLD/$CARD/.lake/packages
  build; probe
done
```

Wait — does the package need `.lake/packages` symlink before build? Yes, lake will resolve `require mathlib from git` — if `.lake/packages/mathlib` exists (via symlink), it may still want to fetch/check. The round-1 fresh build worked with this approach presumably.

One concern: the shared packages dir may include mathlib olean cache built with the same toolchain. Yes — round 5 builds were fast (~12s per card) because oleans already existed. A "cold" build of just the card sources: the producer sources are ~10 files; each ~3.5 min (8957 jobs refers to mathlib modules? no — 8957 jobs is a lot; that suggests lake build re-runs mathlib? Hmm, "8957 jobs" for the card build. With cached oleans, jobs are just checked quickly? Actually lake prints jobs total, not executed. Round-5 logs show build logs of ~350KB and ~12s wall. So builds are incremental checks.

For cold: copying pkgs/<card> excluding .lake and rebuilding — lake will need to compile the card's own modules (~10) — that takes ~3.5 min as round 1 noted for 2 cards. Good.

Let me check disk: pkgs-cold6 ~ 5 copies * small size. Fine.

Let me now write and launch.

Actually, one important thing to verify first: is the shared mathlib cache at /data3/guoshaoyang/workdir/lean_poincare/poincare-lab/.lake/packages present and fine? Round 5 built fine 1 hour ago, so yes.

Let me write the scripts with the write tool, then launch in background.
dsh: reasoning:
The nohup with & inside a background bash job means the tool returns quickly. Let me check the driver log in a bit. Now, while that runs, do the missing-card discovery.

Search approach:
1. Filesystem: find / -xdev or across likely roots for names matching the missing cards, excluding heavy dirs.
2. Check mounts: `mount | grep -v proc` etc.
3. Check network: ports, tailscale.
4. Check the queue/state for any new info about these cards.
5. Check if there's a sync/lockfile, or a release directory in other worktrees.

Let me look at how the missing cards are referenced elsewhere: grep in the repo for "tensor-maximum-bochner" and "triangulation-topology".
dsh: reasoning:
No mounts, port refused, no 360 peers. Let me do the exhaustive filename search across all readable filesystems for the 2 cards. Previous rounds searched /data3/guoshaoyang. Let me broaden: search / (with pruning) for names; also search the queue/state files where the cards are mentioned (maybe there's a queue JSON mentioning a host or path).

Let me find references to the two card names in the parent longrun directory.
dsh: reasoning:
The nohup wrapper returned. Round 6 running in background detached. Let me check the driver log later.

The find search returned nothing (SEARCH_DONE rc=0 — note find returned nothing before "SEARCH_DONE"). So the cards are still absent.

Let me check the queue.json and the D12-plan.json for any transport hints, and the state logs for these cards (maybe there's a remote host reference).

Also check `/data3/guoshaoyang/workdir/lean_poincare/longrun/state/D12-tensor-maximum-bochner/` if it exists.

Let me look at queue.json entries for the two cards.
dsh: reasoning:
Confirmed: both missing cards are "remote_owned" by hosts 360-1/360-2, removed from local launch set. No state dirs, no artifacts. Let me check the D12-plan for their objectives, which is useful for "next_dependency_requests" and for the `semantic_class`/statement requirements of the missing cards.

Let me extract the plan entries for the 9 D12 cards — useful context for the results doc. And check the prompt file for this task (prompts/D13-cross-audit-360-cards.md) to make sure I'm compliant with requirements.

Also check `/data3/guoshaoyang/workdir/lean_poincare/longrun/state/D13-cross-audit-360-cards/` for previous run logs (to know what was done) — maybe not needed.

Let me check the driver log for round 6 first, then read the task prompt.
dsh: reasoning:
Round-6 sweep is progressing. Good.

Now let me read the task prompt to ensure compliance and check the required deliverables format: `prompts/D13-cross-audit-360-cards.md`.

Actually the task text is in my instructions. But the prompt file might have extra detail. Let me read it.

Also, I want to check what the checkpoint "status: blocked" means for my final answer: I should end TASK_BLOCKED if the milestone can't be fully checked. I'll re-verify at the end.

Since the missing cards are definitively absent, my job this round:
1. Round-6 sweep evidence (build/probe/fullaudit/kind) - running.
2. Cold rebuild for 5 cards - to launch.
3. Deep semantic review of connection-curvature & spectral-sobolev.
4. New independent consumer for CC closure (round 6).
5. Fresh hash self-audit.
6. Update results.

Let me now launch the cold rebuild job in the background too. But CPU contention with round-6 sweep... Round-6 sweep is quick (~2 min), so start cold after it finishes. Actually cold build of 5 cards sequentially at ~3.5 min each = ~18 min. Fine.

Let me wait for round-6 to finish, then launch cold. Meanwhile, start reading the connection-curvature and spectral-sobolev sources for deep review.

Where are the producer sources? `/data3/.../worktrees/D12-connection-curvature/release/Poincare/D12/...`. And the card JSONs at worktrees/D12-*/longrun/results/*.json.

Let me look at the card JSON for connection-curvature and spectral-sobolev to understand the claims, then read the Lean sources.

Let me first read the prompt file quickly.
dsh: reasoning:
Same as task text. Good.

Now let me do the deep semantic review of D12-connection-curvature and D12-spectral-sobolev. First, look at the source files and card claims.

Let me list the release sources for these two cards and read the card JSON summarized.
dsh: reasoning:
Oops — the first command's find followed into .lake/packages. I need to exclude .lake. Let me redo carefully. Also the output got truncated. Let me be more careful with output size.

Let me directly list release Lean sources excluding .lake for both cards.
dsh: reasoning:
Now let me read the D12-specific sources for both.

Spectral-sobolev: Poincare/D12/SpectralSobolev/{Basic,HeatConvergence,Semigroup,Poincare,Examples,All,AxiomAudit}.lean — read those.

Connection-curvature: Poincare/D12/ConnectionCurvature/{ChartLeviCivita,MilnorLeviCivita,RicciSymmetry,SoThreeModel,ChartLeviCivitaForm,ChartLeviCivitaSmooth,ChartModel1D,ConformalChartModel}.lean.

Let me check the round-6 progress too. Then read spectral-sobolev sources (probably ~1000 lines total).
dsh: reasoning:
Round-6 is progressing. Now let me read the spectral-sobolev sources for the deep statement review.

Let me read Basic.lean first (149 lines), then Semigroup, HeatConvergence, Poincare, Examples.
dsh: reasoning:
This is solid mathematics. Key checks for semantic review:
- `heatWeight` = exp(-λₙ t) with λₙ = (2πn/T)². Correct heat kernel eigenvalue on circle of circumference T. ✓
- `heatWeight_zero = 1`, positivity, ≤1 for t≥0. ✓
- `heatEvolve` defined via fourierBasis.repr.symm — legitimate, not defined by the identity it satisfies.
- `heatSeries_hasSum`: HasSum of the series in L² to heatEvolve. Nontrivial. ✓
- `fourierCoeff_heatEvolve`: cₙ(H_t f) = e^{-λₙt} cₙ(f). ✓

Note: `heatWeight` is defined for arbitrary T (no 0<T), but heatCoeffs etc. require `Fact (0<T)`. Fine.

Now Semigroup and HeatConvergence and Poincare.
dsh: reasoning:
This is genuinely good mathematics. Note one thing to check: `heatEvolve_neg`? Not present. Time is ℝ≥0 so fine.

Potential issue: `heatSeries_hasSum` states `HasSum (fun n => (heatWeight T t n : ℂ) • (fourierCoeff f n • fourierLp 2 n))`. In a normed space over ℂ, `HasSum` of a series indexed by ℤ. The summand is written `w • (c • eₙ)`. Since `fourierLp 2 n` is a unit vector in L², `c • eₙ` is the component. OK.

Now Poincare.lean and Examples.lean.
dsh: reasoning:
This is excellent: sharp Poincaré inequality with optimality witness (`poincare_constant_sharp`), non-vacuity for heat evolution, exact eigenfunction. Semantic review verdict: REVIEWED-CORRECT.

One thing to check adversarially: `poincare_wirtinger` — the statement's mean-zero condition is `fourierCoeffOn hab f 0 = 0`. Is that equivalent to ∫f = 0? `fourierCoeffOn_zero_eq_mean` shows c₀ = (1/(b-a)) • ∫ f, and b-a ≠ 0, so yes, equivalent. Good.

`fourierCoeffOn_deriv_periodic` uses mathlib's `fourierCoeffOn_of_hasDerivAt`. Hypotheses: `∀ x ∈ [[a,b]], HasDerivAt f (f' x) x`, `f b = f a`, `IntervalIntegrable f'`. That's fine. Hmm, one subtlety: mathlib's `fourierCoeffOn_of_hasDerivAt` may require `hf : ∀ x ∈ [[a,b]], HasDerivAt f (f' x) x` and `hf' : IntervalIntegrable f' volume a b`. Yes.

So spectral-sobolev is REVIEWED-CORRECT. Note the semantic class: *proved theorem* (unconditional on the circle for L² functions), with the geometric dimension reduction to 1 explicitly scoped (dimension 1 only). This is a genuine Sobolev/Poincaré result but not the general manifold Sobolev embedding. Card says what? Let me look at the card's claim (semantic_class, expanded_hypotheses, remaining_blockers).

Now let's look at the connection-curvature sources. That's more lines. Let me check the sizes.

Also I should check round-6 progress.
dsh: reasoning:
Round-6 seems stalled at volume-ibp? Started 14:40:44 and now it's maybe 14:45. Let me check later — the build might take longer (round 5's volume-ibp took ~15s). Hmm, the driver log hasn't updated. Let me check whether the process is running.

Actually wait — logs-round6.driver.log is written by nohup redirect, buffered? `echo` from bash should flush. Let me check process list.

Meanwhile, let me start reading connection-curvature key files: MilnorLeviCivita.lean, RicciSymmetry.lean, SoThreeModel.lean. Those are the closure-related ones. Chart files are the algebra. Let me read MilnorLeviCivita.lean and SoThreeModel.lean (199+242 lines).
dsh: reasoning:
This is excellent mathematics — Milnor's formula, torsion-freeness, metric compatibility, existence unconditional, mean connection iff bi-invariance, uniqueness via Koszul.

One adversarial check: `bracketInvariant` is in `proved_declarations` (F14). The definition itself is `def ... : Prop` — a hypothesis predicate, not a proof. Correct classification: it's a Prop former; F14 flagged it. The actual *proof* is `milnorConnection_eq_mean_iff`. So the card's flat list contains a non-proof entry. Confirmed as a precision finding only.

Now let me check the semantic correctness of the Milnor formula sign conventions. Milnor's formula: for a left-invariant metric on a Lie group, the Levi-Civita connection at identity is
∇_X Y = ½([X,Y] − (ad_X)^* Y − (ad_Y)^* X) where (ad_X)^* is the adjoint w.r.t. the metric. Yes, that's Milnor's (1.4)-ish (with the convention [X,Y] = ad_X Y). Let me verify torsion-freeness:
T(X,Y) = ∇_X Y − ∇_Y X − [X,Y]. 
∇_X Y = ½([X,Y] − (ad_X)^T Y − (ad_Y)^T X)
∇_Y X = ½([Y,X] − (ad_Y)^T X − (ad_X)^T Y) = ½(−[X,Y] − (ad_Y)^T X − (ad_X)^T Y)
Difference = ½([X,Y] + [X,Y]) = [X,Y] (the transpose terms cancel). ✓.

Metric compatibility:
⟨∇_X Y, Z⟩ + ⟨Y, ∇_X Z⟩ = ½[⟨[X,Y],Z⟩ − ⟨(ad_X)^T Y, Z⟩ − ⟨(ad_Y)^T X, Z⟩ + ⟨Y,[X,Z]⟩ − ⟨Y,(ad_X)^T Z⟩ − ⟨Y,(ad_Y)^T X⟩]
= ½[⟨[X,Y],Z⟩ − ⟨Y,[X,Z]⟩ − ⟨X,[Y,Z]⟩ + ⟨Y,[X,Z]⟩ − ⟨Z,[X,Y]⟩ − ⟨X,[Y,Z]⟩... let me use the adjoint properties:
⟨(ad_X)^T Y, Z⟩ = ⟨Y, [X,Z]⟩ ✓
⟨(ad_Y)^T X, Z⟩ = ⟨X, [Y,Z]⟩
⟨Y, (ad_X)^T Z⟩ = ⟨Z, [X,Y]⟩ (using swap: ⟨Y, (ad_X)^T Z⟩ = ⟨Z, [X,Y]⟩) ✓
⟨Y, (ad_Y)^T X⟩ = ⟨X, [Y,Y]⟩ = 0.
So sum = ½[⟨[X,Y],Z⟩ − ⟨Y,[X,Z]⟩ − ⟨X,[Y,Z]⟩ + ⟨Y,[X,Z]⟩ − ⟨Z,[X,Y]⟩ − 0]
= ½[⟨[X,Y],Z⟩ − ⟨Z,[X,Y]⟩ − ⟨X,[Y,Z]⟩]
= ½[0 − ⟨X,[Y,Z]⟩] (using skew: ⟨[X,Y],Z⟩ = −⟨Z,[X,Y]⟩)
Hmm, that gives −½⟨X,[Y,Z]⟩, which is not obviously zero... wait, I think I need to redo. Let me recompute carefully.

Metric compatibility: X⟨Y,Z⟩ = ⟨∇_X Y, Z⟩ + ⟨Y, ∇_X Z⟩.

∇_X Y = ½([X,Y] − (ad_X)^T Y − (ad_Y)^T X)
∇_X Z = ½([X,Z] − (ad_X)^T Z − (ad_Z)^T X)

⟨∇_X Y, Z⟩ = ½(⟨[X,Y],Z⟩ − ⟨(ad_X)^T Y,Z⟩ − ⟨(ad_Y)^T X,Z⟩)
= ½(⟨[X,Y],Z⟩ − ⟨Y,[X,Z]⟩ − ⟨X,[Y,Z]⟩)

⟨Y, ∇_X Z⟩ = ½(⟨Y,[X,Z]⟩ − ⟨Y,(ad_X)^T Z⟩ − ⟨Y,(ad_Z)^T X⟩)
= ½(⟨Y,[X,Z]⟩ − ⟨Z,[X,Y]⟩ − ⟨X,[Z,Y]⟩)   [using ⟨Y,(ad_X)^T Z⟩ = ⟨Z,[X,Y]⟩ and ⟨Y,(ad_Z)^T X⟩ = ⟨X,[Z,Y]⟩]
= ½(⟨Y,[X,Z]⟩ − ⟨Z,[X,Y]⟩ + ⟨X,[Y,Z]⟩)

Sum = ½(⟨[X,Y],Z⟩ − ⟨Z,[X,Y]⟩) = 0 by skew-symmetry of the metric form. ✓ 

Great, metric compatibility holds. So the math is right.

Let me now check round-6 progress and then read RicciSymmetry.lean + SoThreeModel.lean.

Actually, wait. The round6 driver is stuck at volume-ibp build (started 14:41, still running at ~14:46?). Hmm, `ps` showed `timeout 5400 lake build` running with 220% CPU since 14:41. The kind log exists from 14:41. Wait, the ordering in my script: build, probe, extras, fullaudit, kind. The listing shows D12-volume-ibp.fullaudit.rc and kind.log/kind.rc and probe.log/rc all from 14:40/14:41. So volume-ibp is DONE. Then why is a `lake build` running since 14:41? Probably spectral-sobolev's build (started right after 14:41). The driver log shows only up to volume-ibp START because... hmm, actually the driver log tail showed "[14:40:44] START D12-volume-ibp" as last line. That was from an earlier check. Let me re-check now.

Also note: the round-6 script runs `lake build` which may rebuild due to nothing changed but still runs job checks. Fine.

Let me check driver log again and simultaneously read RicciSymmetry.
dsh: reasoning:
RicciSymmetry is a genuine, correct derivation. Note that `ricci_symm` is derived from the *proved* `first_bianchi_neg_sum` for `CurvatureOperator`. The hypothesis `d : LeviCivitaData m b` includes torsion-free + metric-compatible. But does `toCurvatureOperator` use the connection's Lie bracket? Let's check `ConnectionAdapter.lean` to verify that `first_bianchi_neg_sum` for a general `CurvatureOperator` is a *field* of the structure or a proved theorem. Because if `CurvatureOperator` has first-Bianchi as a *field*, then `d.toCurvatureOperator` would construct it from the connection... The docstring says "first Bianchi identity used here is the proved lemma AbstractConnection.curvature_bianchi (packaged into the CurvatureOperator interface by toCurvatureOperator), not a hypothesis". Need to check `ConnectionAdapter.lean` and `CurvatureAlgebra.lean` to see whether `CurvatureOperator`'s Bianchi is a field with proof obligation at construction. If it's a structure field with a proof obligation, then `d.toCurvatureOperator` must construct it from `d.nabla`'s properties — fine, it's proved for that instance. Let me check.

This is a key adversarial point: is `ricci_symm` circular (assuming what it proves via a structure field)? Let me read ConnectionAdapter.lean and the definition of CurvatureOperator.

Also let me start the cold rebuild after round-6 sweep finishes. Round-6 is at spectral-sobolev (started 14:41). Let me wait a bit.

Read ConnectionAdapter.lean.
dsh: reasoning:
So `CurvatureOperator` is a structure whose `first_bianchi` and `first_pair_skew` are *fields*. `toCurvatureOperator` must construct those fields from the connection. Let me check that `toCurvatureOperator`'s first_bianchi field is *proved* (not assumed). Read ConnectionAdapter.lean around toCurvatureOperator.

If `toCurvatureOperator` requires the caller to provide first_bianchi proof, then fine, but `ricci_symm` might take `d : LeviCivitaData` and build `K := d.toCurvatureOperator` where toCurvatureOperator constructs the proof from `d.torsion_free` and Jacobi. Let me verify.
dsh: reasoning:
Good — the curvature is defined from nabla, and `curvature_bianchi` is proved from torsion-freeness + Jacobi. Let me check the `toCurvatureOperator` definition to confirm first_bianchi is constructed from `curvature_bianchi` (proved), not from a hypothesis. Let me look further.
dsh: reasoning:
Confirmed: the adapter is non-circular; both Bianchi and skew are proved from the connection's torsion-freeness and the bracket's Jacobi. So `ricci_symm` is a genuine theorem, not circular.

Now, the connection-curvature card's claims seem mathematically correct. The "closure" of `LeviCivitaExistenceStatement` is genuine: they construct the Milnor connection, prove torsion-free + metric compatible. Also prove that the old docstring's claim (needs invariance) applies to the mean connection.

Adversarial angle: is `LeviCivitaExistenceStatement` in `LeviCivitaBlocked.lean` exactly `∃ nabla, IsLeviCivita m b nabla`? Verified previously (byte-identical base module, closure consumer). Let me confirm quickly by reading the definition.

Also I want to see whether the "Downstream checked use" of closure — the round-3 consumer — is real. The results doc says `A3ExtraR3/ClosureUse.lean` for CC compiles. I'll re-run in round-6 (it does).

Now let me consider new consumer for round 6: I could write a stronger CC consumer in the audit package that proves something new, e.g.:
- Construct `LeviCivitaData` for a *non-abelian* bracket (e.g. so(3) with the cross product) and derive a specific curvature value from `ricci_symm` where the connection is the Milnor connection (not mean). The card has `so3MeanLeviCivita` (mean = Milnor for bi-invariant metric). A new use could be a *non-bi-invariant* metric on a 2D nonabelian Lie algebra, where Milnor ≠ mean, and compute that the connection is metric compatible (already proved) and torsion-free. That's immediate from the theorem.
- More interesting: prove that the Milnor connection is the *unique* Levi-Civita connection (using `leviCivita_nabla_unique`), i.e., a new corollary `milnor_unique`. Not in the card? Let me check what's in the card's proved_declarations list, and whether `leviCivita_nabla_unique` is from base. A fresh consumer: `∀ d : LeviCivitaData m b, d.nabla = milnorConnection m b` — that's a genuine downstream use of both existence and uniqueness, useful for "closure" confirmation. That's a good 6th consumer.

Wait, but is uniqueness in the base `LeviCivitaBlocked.lean`? `leviCivita_nabla_unique` is used in MilnorLeviCivita.lean line 187, so it exists in the base. A new theorem in the audit file: 

```lean
theorem a3_milnor_unique (m : MetricData V ι) (b : LieBracketData ℝ V) (d : LeviCivitaData m b) :
    d.nabla = milnorConnection m b := leviCivita_nabla_unique m b d.2 (milnorConnection_isLeviCivita m b)
```
Hmm, need the exact signature of `leviCivita_nabla_unique`. Let me check LeviCivitaBlocked.lean.

Another fresh consumer: use `leviCivitaExists` at the trivial bracket (zero bracket): the Milnor connection is the zero connection, Levi-Civita for any metric with zero bracket. Prove `ricci = 0`? Or on the abelian case compute scalar curvature = 0 via the frame sum. Could be a genuinely new downstream computation.

Let me look at the LeviCivitaBlocked base to see what's available.
dsh: reasoning:
Now let me find `LeviCivitaExistenceStatement`:
dsh: reasoning:
The definition of `LeviCivitaExistenceStatement` is exactly `∃ nabla, IsLeviCivita m b nabla` — the closure is genuine: `leviCivitaExists` proves it unconditionally. Note the blocked docstring says "false without additional hypotheses such as invariance" — the D12 card refutes that claim. My earlier audit (round 2/3) confirmed. As an adversarial auditor, I should double-check the claim "false without invariance": the Milnor connection works unconditionally as long as the bracket is a Lie bracket (skew + Jacobi — note Jacobi is actually not needed for existence! Torsion-freeness uses skew, metric compatibility uses skew + form symmetry). Indeed Jacobi isn't used in `leviCivitaExists`. That's fine, the theorem is still true.

So CC closure CONFIRMED.

Now, potential new consumer: `a3_milnor_unique` — any Levi-Civita connection equals the Milnor one. Let me write that this round. Also possibly: the Milnor connection for the **abelian** (zero bracket) case is the zero connection — a concrete non-vacuity computation showing `milnorConnection m LieBracketData.zero = 0`, and hence its curvature operator is zero (via `AbstractConnection.zero`), and its scalar curvature = 0. That would be an independent downstream use (curvature computation) that the card didn't do explicitly. Let me check whether the card has something similar already: `zero_toCurvatureOperator` is in base. But a new audit-file theorem:

```lean
theorem a3_milnor_zero_bracket (m : MetricData V ι) :
    milnorConnection m (LieBracketData.zero) = 0 := by
  ext X Y
  simp [milnorConnection_apply, LieBracketData.zero, adTranspose]
```
Hmm, might be fiddly. Let me instead do the uniqueness one and a curvature-value computation for so(3) (already in card as so3_ricci_e00 = 1/2). 

Another idea: use the CC closure to derive a new *mathematical* consequence not in the card: for the 2D nonabelian Lie algebra with a non-bi-invariant metric, the Milnor connection ≠ mean connection, and compute the scalar curvature explicitly. That requires defining a model — a bit of work, but doable: 
- V = ℝ × ℝ with bracket [e₁,e₂] = e₁ (or similar). Actually a 2D nonabelian Lie algebra: bracket e₁ e₂ = e₁, skew determined. Metric: diag(a,b) with a≠b... but `MetricData` requires a basis + form. The card has `ChartModel1D` and `ConformalChartModel` for charts. A fresh model may be more work than time allows.

Simplest strong new consumer: uniqueness + the zero-bracket case + maybe `scalarCurvature = 0` for zero bracket:
- `a3_milnor_unique`: any d is the Milnor connection.
- `a3_milnor_curvature_flat`: for zero bracket, the Milnor curvature operator is zero → scalarCurvature = 0 and ricci = 0.

Let me look at `MetricData` API and `CurvatureOperator` API to write these. Also check `scalarCurvature` def.

Actually, since the round-3 consumer already did "choice-based Levi-Civita consumer with Stage1 Bianchi/skew obligations", maybe I should add the uniqueness-based one + a genuine *new* mathematical statement: 

**Milnor connection is the unique Levi-Civita connection**, and **the map (m,b) ↦ milnorConnection is the canonical section**. That's a clean new result.

Also for SR closures: round 5 added a nonempty consumer. Could add another independent one? E.g., a *three*-fold connected sum (𝕊³#𝕊³#𝕊³ ≅ 𝕊³) exercising the recursive branch deeper, or apply `finiteFreeOrbit_isQuotientCoveringMap` to a different group action (ℤ/2 on 𝕊³ via a different free action, e.g. rotation by π around an axis — but that has fixed points; antipodal is the free one). Maybe do a 3-fold sum. That's a modest extension of round 5's two-fold. Not much new value. Alternatively verify the *downstream* consumption of the closures in the card's own downstream theorems (stage6Target_*), which was done.

Given time budget, I'll write:
1. CC new consumer: `a3_milnor_unique` + zero-bracket flatness (if easy).
2. Maybe SR-6: 3-fold connected sum.

Let me first check round-6 status and then launch cold rebuilds. Then write consumers.

Actually, I also want to add a **forbidden-token scan** with a broader token set (opaque/extern/partial/unsafe/implemented_by) for all packages — that's cheap and new.

And an important check I haven't seen in the checkpoint: **whether the probe itself is complete** — i.e., does `A3Probe.lean` include every name in the card's `proved_declarations`, and does the full-namespace audit cover all compiled declarations? Round 5 did hash_coverage and full-namespace 1103 PASS. Good.

Another new check: **`#print axioms` on the card-level headline theorems using `Classical.choice`** — done by full namespace audit.

OK. Let me now check round-6, launch cold builds, and then write the consumer files.
dsh: reasoning:
Round-6 sweeps fast. It'll complete in ~1 min. I'll then kick off the cold rebuilds.

While waiting, let me prepare the new consumer file for connection-curvature. Let me check the existing A3ExtraR3/ClosureUse.lean in the audit package to match imports and namespace.
dsh: reasoning:
Now I'll write a round-6 consumer `A3ExtraR6/ClosureUseUnique.lean`:
- `a3_milnor_unique`: any Levi-Civita connection equals milnorConnection.
- `a3_milnor_isLeviCivita_unique`: `IsLeviCivita m b nabla ↔ nabla = milnorConnection m b` (iff characterization).
- A genuinely new *mathematical* corollary: for **any** Levi-Civita connection, torsion-free+compatible, the *scalar curvature* is the same as that of the Milnor connection (by uniqueness of nabla), i.e. `ricci K X Y = ricci (milnor...) X Y`. Hmm, that follows by congruence.
- Or: the curvature operator does not depend on the choice of Levi-Civita connection because there's only one: `∀ d, d.toCurvatureOperator = (milnorLeviCivitaData m b).toCurvatureOperator`.

Also a stronger test: **existence of Levi-Civita connection with a non-invariant metric is NOT vacuous** — the closure says `Nonempty (LeviCivitaData m b)` for all m,b. A model witness that is *not* bi-invariant would demonstrate non-vacuity at a case where the old "blocked" claim would have said false. The card has `SoThreeModel` with bi-invariant metric presumably (so3MeanLeviCivita). A non-bi-invariant example would be a genuinely new adversarial check: build a 2D nonabelian bracket on ℝ×ℝ with a non-invariant metric, instantiate `leviCivitaExists`, and show `¬ bracketInvariant m b` — proving the closure produces a Levi-Civita connection in a case where the mean connection is not one. That's a strong non-vacuity test of the closure and directly tests the refutation of the "blocked" claim.

How hard is that in Lean? Need MetricData on V = ℝ × ℝ with a basis. Let me look at MetricData's definition to see the fields.

Actually, maybe simpler: use `Fin 2 → ℝ` as V with the standard basis, metric form `m.form u v = a*u0*v0 + b*u1*v1` with a≠b, bracket `[u,v] = (u0*v1 - u1*v0) • e1`? Wait that's the 2D nonabelian bracket [e1,e2] = e1: check Jacobi: in 2D any skew bracket satisfies Jacobi automatically (since Λ² → V, Jacobi is trivially satisfied for dimension 2). Skew-symmetry: need bracket u v = - bracket v u.

Define bracket u v = (u 0 * v 1 - u 1 * v 0) • e₁ where e₁ = (1,0). Then [e₁,e₂] = e₁. Skew: bracket v u = (v0 u1 - v1 u0) e1 = -(...) e1 ✓. Jacobi: bracket u (bracket v w) = bracket u (c e1) = (u0 * 0 - u1 * c) e1 = -u1 c e1, where c = v0w1-v1w0. Cyclic sum: -u1(v0w1-v1w0) - v1(w0u1-w1u0) - w1(u0v1-u1v0) = -u1v0w1 + u1v1w0 - v1w0u1 + v1w1u0 - w1u0v1 + w1u1v0 = 0. ✓ (all terms cancel).

Metric: m.form u v = a*u0*v0 + b*u1*v1, positive definite if a,b>0. bracketInvariant: need form [X,Y] Z + form Y [X,Z] = 0 for all. Take X=e1,Y=e2,Z=e2: form [e1,e2] e2 + form e2 [e1,e2] = b + b = 2b ≠ 0 if b≠0. So not invariant. Good.

Then `leviCivitaExists m b` gives a Levi-Civita connection. To show it's interesting, show `milnorConnection m b ≠ (meanConnection b).nabla` by `milnorConnection_eq_mean_iff` + `not_invariant`. That directly demonstrates: the D12 closure is non-vacuous in a case where the mean connection fails — i.e., the historical "blocked" claim (existence requires invariance) is refuted at a concrete model.

This requires constructing `MetricData`. Let me read MetricData.lean to see the fields and how SoThreeModel constructs one.
dsh: reasoning:
The SoThreeModel shows how to build a MetricData on `Fin n → ℝ`. I can build a `Fin 2 → ℝ` model with a non-invariant metric and the nonabelian bracket.

Plan for the new CC consumer (`A3ExtraR6/ClosureUseNoninvariant.lean`):

```lean
import Poincare.D12.ConnectionCurvature

open scoped BigOperators
namespace A3R6
open Poincare Longrun Geometry

-- dot product w.r.t. weights a b
noncomputable def w2 (a b : ℝ) : (Fin 2 → ℝ) →ₗ[ℝ] (Fin 2 → ℝ) →ₗ[ℝ] ℝ where
  toFun X := { toFun := fun Y => a * X 0 * Y 0 + b * X 1 * Y 1, ... }
  ...
```

Then `MetricData` with `basis := Pi.basisFun ℝ (Fin 2)`, orthonormal requires a=b=1 (!). Wait — the `MetricData` structure requires the chosen basis be orthonormal, so the weights must be 1 in that basis. Hmm! `orthonormal : ∀ i j, form (basis i) (basis j) = if i=j then 1 else 0`. So any MetricData is isometric to the standard one in its chosen basis; but the *form* can still be non-invariant under the bracket. The metric's "non-invariance" comes from the bracket, not from the basis weights. Right — `form` is determined by the basis: form X Y = Σ X_i Y_i, since the basis is orthonormal (the metric is just the standard dot product in that basis). So a non-invariant metric means: choose a bracket that is not skew w.r.t. that dot product — e.g. bracket [e1,e2] = e1 + e2 or any bracket with structure constants.

So: V = Fin 2 → ℝ, basis = Pi.basisFun, form = dot2 (standard dot product), bracket [X,Y] = ... a skew bilinear bracket that isn't skew-adjoint w.r.t. dot2. For 2D: [X,Y] = c(X,Y) • w for some vector w, where c = X0Y1 - X1Y0. Skew automatic; Jacobi automatic in 2D. Choose w = e1 + e2 = ![1,1], i.e., bracket X Y = (X0Y1 - X1Y0) • (e1+e2), i.e. components ((X0Y1-X1Y0), (X0Y1-X1Y0)).

Check bracketInvariant (form = dot2): need dot2 [X,Y] Z + dot2 Y [X,Z] = 0 for all X,Y,Z. Compute with X=e1,Y=e2,Z=e1: [e1,e2] = e1+e2, dot2 (e1+e2) e1 = 1. [e1,e1] = 0. Sum = 1 ≠ 0. So not invariant. 

Then:
- `leviCivitaExists m b` gives a Levi-Civita connection unconditionally (non-vacuous in a non-invariant case).
- `milnorConnection m b ≠ meanConnection b` (using `milnorConnection_eq_mean_iff` and the non-invariance).
- Show that the mean connection is *not* metric compatible: `¬ IsMetricCompatible m (meanConnection b).nabla`.
- Corollary: there exists a Levi-Civita connection that is not the mean connection → the old "blocked" assertion is false at this model.

This is a genuinely new, adversarial, downstream-checked use. Let me write it.

To define the bilinear bracket as a `V →ₗ V →ₗ V`:
```lean
noncomputable def bracket2 : (Fin 2 → ℝ) →ₗ[ℝ] (Fin 2 → ℝ) →ₗ[ℝ] (Fin 2 → ℝ) where
  toFun X := { toFun := fun Y => fun i => (X 0 * Y 1 - X 1 * Y 0) * (if i = 0 then 1 else 1) , ...}
```
Hmm, the vector ![c, c] = fun _ => c. So `fun _ : Fin 2 => c`. Then linearity in Y is easy; linearity in X too.

```lean
noncomputable def bracket2 : (Fin 2 → ℝ) →ₗ[ℝ] (Fin 2 → ℝ) →ₗ[ℝ] (Fin 2 → ℝ) where
  toFun X :=
    { toFun := fun Y => fun _ => X 0 * Y 1 - X 1 * Y 0
      map_add' := by intro Y Z; ext i; simp; ring
      map_smul' := by intro a Y; ext i; simp; ring }
  map_add' := by intro X X'; ext Y i; simp; ring
  map_smul' := by intro a X; ext Y i; simp; ring
```

Careful with `simp; ring` and function extensionality for `Fin 2 → ℝ`. `ext i; fin_cases i <;> simp` might be needed. Let's write and test.

MetricData:
```lean
noncomputable def dot2 : (Fin 2 → ℝ) →ₗ[ℝ] (Fin 2 → ℝ) →ₗ[ℝ] ℝ where
  toFun X := { toFun := fun Y => X 0 * Y 0 + X 1 * Y 1, map_add' := ..., map_smul' := ... }
  map_add' := ...
  map_smul' := ...
```
Similar to SoThreeModel's dot3 (I can copy the pattern).

Then `metric2 : MetricData (Fin 2 → ℝ) (Fin 2) where form := dot2; symm; pos_def; basis := Pi.basisFun ℝ (Fin 2); orthonormal := ...`.

Left-noninvariance proof:
```lean
theorem not_bracketInvariant : ¬ bracketInvariant metric2 lie2 := by
  intro h
  have hh := h (Pi.basisFun ℝ (Fin 2) 0) (Pi.basisFun ℝ (Fin 2) 1) (Pi.basisFun ℝ (Fin 2) 0)
  simp [bracketInvariant, lie2, bracket2, metric2, dot2, Pi.basisFun_apply] at hh
```
Might need fin_cases.

Then:
```lean
theorem a3_noninvariant_leviCivita_exists : LeviCivitaExistenceStatement metric2 lie2 :=
  Poincare.D12.ConnectionCurvature.leviCivitaExists metric2 lie2

theorem a3_milnor_ne_mean : milnorConnection metric2 lie2 ≠ (meanConnection lie2).nabla := by
  intro h
  exact not_bracketInvariant ((milnorConnection_eq_mean_iff metric2 lie2).mp h)

theorem a3_mean_not_compatible : ¬ IsMetricCompatible metric2 (meanConnection lie2).nabla := by
  intro h
  exact not_bracketInvariant ((meanConnection_isMetricCompatible_iff metric2 lie2).mp h)
```

And a uniqueness corollary:
```lean
theorem a3_leviCivita_unique (d : LeviCivitaData m b) : d.nabla = milnorConnection m b :=
  leviCivita_nabla_unique m b d.isLeviCivita (milnorConnection_isLeviCivita m b)
```
Wait, `leviCivita_nabla_unique m b h1 h2 : nabla₁ = nabla₂`. With d.isLeviCivita : IsLeviCivita m b d.nabla and milnorConnection_isLeviCivita : IsLeviCivita m b (milnorConnection m b). Good.

Let me write the file and compile it in the CC package (in audit360/pkgs/D12-connection-curvature/A3ExtraR6/). The package build already includes the base; `lake env lean` can compile a standalone file importing the package modules.

But wait: packages' lakefile may have a glob for `A3Extra`? Not needed since we invoke `lake env lean <file>` directly, with imports resolved from the built oleans. Need to make sure the imports resolve: `import Poincare.D12.ConnectionCurvature` works because the package is built.

Let me now check round-6 completion, launch cold rebuilds, and write the file.
dsh: reasoning:
Interesting! `D12-semantic-ledger.extra.D12RealModuleProbe=1` — the extra probe failed in round 6! In round 5 it was rc 0. Something changed? Let me check the log. This could be an important regression: perhaps because the A3Extra/D12RealModuleProbe.lean file was edited (F13 hash drift corrected) — or because it imports something that has changed.

Wait — the round-5 log listing earlier showed `D12-semantic-ledger.extra.D12RealModuleProbe.log` with 287 bytes and rc 0. Let me check the round-6 log content.
dsh: reasoning:
Round 5 had the same failure rc? The checkpoint said round-5 extras... Actually the round-5 log shows the same error, but the rc file listed in the round-5 directory: let me check `audit360/logs-round5/D12-semantic-ledger.extra.D12RealModuleProbe.rc`. Earlier listing showed `D12-semantic-ledger.extra.D12RealModuleProbe.log` 287 bytes at 13:44. Let me check its rc. The checkpoint's round-5 claims "all 7 cards build/probe/fullaudit rc 0" — extras may not have been included in the sweep claim. Also the semantic-ledger probe of the snapshot package is separate (`D12-semantic-ledger-snapshot.extra.D12RealModuleProbe`), which presumably succeeds.

So the in-package A3Extra/D12RealModuleProbe.lean fails because the D7 HeatKernel module is not part of this package's imports/build. That's a known limitation: the probe was designed for the snapshot package where the D11 source tree is present. F2 in the results says the 2 declared names live in `audit_probes/D12RealModuleProbe.lean` outside the release package, replayed and audited in the snapshot. So this error is expected in the release package (module absent), not a new regression.

Let me confirm round-5 rc: if it was 1 there too, then round 5 already had this and the checkpoint's claim "7 cards build/probe/fullaudit rc 0" excludes extras. Fine. But I should note it accurately in round 6: the in-package extra probe is *expected* to fail (missing D7 module); the snapshot probe is the valid one.

Let me check the round-5 rc file and the snapshot rc in round 6.

Meanwhile the round-6 driver is on geometric-compactness. Let me wait for it to finish then start cold rebuilds.

Also, importantly: was the `A3Probe.lean` for semantic-ledger (7 declarations) rc 0? Yes: `D12-semantic-ledger.probe=0`.

Let me check the round-5 extra rc to be sure this isn't a new regression.
dsh: reasoning:
Confirmed: the in-package extra probe rc=1 in round 5 too (not a regression). The snapshot probe is the valid one and will be run at the end of round 6.

Now let me wait for round-6 to finish, then launch cold rebuilds. Meanwhile, write the new CC consumer file.

Let me write the file now into the audit package (not the producer worktree — I must not modify producer sources; the audit package is my copy).

I'll create `audit360/pkgs/D12-connection-curvature/A3ExtraR6/ClosureUseNoninvariant.lean`.

Content plan:

```lean
-- A3 round-6 independent consumer for the D12-connection-curvature closure.
import Poincare.D12.ConnectionCurvature

open scoped BigOperators

namespace A3R6

open Poincare Longrun Geometry

/-! ## A non-bi-invariant 2-dimensional model -/

/-- Standard dot product on `Fin 2 → ℝ`. -/
noncomputable def dot2 : (Fin 2 → ℝ) →ₗ[ℝ] (Fin 2 → ℝ) →ₗ[ℝ] ℝ where
  toFun X :=
    { toFun := fun Y => X 0 * Y 0 + X 1 * Y 1
      map_add' := by intro Y Z; simp only [Pi.add_apply]; ring
      map_smul' := by intro a Y; simp only [Pi.smul_apply, smul_eq_mul]; ring }
  map_add' := by intro X Y; ext Z; simp only [Pi.add_apply]; ring
  map_smul' := by intro a X; ext Y; simp only [Pi.smul_apply, smul_eq_mul]; ring
```

Hmm, `map_add'` for the outer LinearMap: goal `dot2 (X+Y) Z = dot2 X Z + dot2 Y Z` after `ext Z`. Actually LinearMap ext: `ext Z` then goal in ℝ: `(X+Y) 0 * Z 0 + (X+Y) 1 * Z 1 = (X 0 * Z 0 + X 1 * Z 1) + (Y 0 * Z 0 + Y 1 * Z 1)`. `simp only [Pi.add_apply]; ring` should work.

For map_smul': `(a • X) 0 * Z 0 + ... = a * (X 0 * Z 0 + X 1 * Z 1)`; `simp only [Pi.smul_apply, smul_eq_mul]; ring`.

The bracket:
```lean
noncomputable def bracket2 : (Fin 2 → ℝ) →ₗ[ℝ] (Fin 2 → ℝ) →ₗ[ℝ] (Fin 2 → ℝ) where
  toFun X :=
    { toFun := fun Y => fun _ => X 0 * Y 1 - X 1 * Y 0
      map_add' := by intro Y Z; ext i; simp only [Pi.add_apply]; ring
      map_smul' := by intro a Y; ext i; simp only [Pi.smul_apply, smul_eq_mul]; ring }
  map_add' := by intro X Y; ext Z i; simp only [Pi.add_apply]; ring
  map_smul' := by intro a X; ext Y i; simp only [Pi.smul_apply, smul_eq_mul]; ring
```
Note: `fun _ => ...` is `fun i => X 0 * Y 1 - X 1 * Y 0`; when applying `(X+Y) 0` reduces by Pi.add_apply. Should be fine.

Lie data:
```lean
noncomputable def lie2 : LieBracketData ℝ (Fin 2 → ℝ) where
  bracket := bracket2
  skew X Y := by
    ext i
    simp only [bracket2, LinearMap.coe_mk, AddHom.coe_mk, Pi.neg_apply]
    ring
  jacobi X Y Z := by
    ext i
    simp only [bracket2, LinearMap.coe_mk, AddHom.coe_mk, Pi.add_apply, Pi.zero_apply]
    ring
```
Hmm, `LinearMap.mk` structure: `{toFun, map_add', map_smul'}`. `simp only [bracket2]` may not unfold cleanly because bracket2 is a def with a structure; use `simp [bracket2]` then `ring`. Actually the values: bracket2 X Y = fun _ => X0*Y1 - X1*Y0. `bracket2 X (bracket2 Y Z) = fun _ => X0 * (bracket2 Y Z) 1 - X1 * (bracket2 Y Z) 0` = fun _ => X0*(Y0*Z1 - Y1*Z0) - X1*(Y0*Z1-Y1*Z0). Note `(bracket2 Y Z) 0 = (bracket2 Y Z) 1 = Y0*Z1-Y1*Z0`.

Jacobi sum: [X,[Y,Z]] + [Y,[Z,X]] + [Z,[X,Y]] = (X0*cYZ - X1*cYZ) + ... = (X0-X1)*cYZ + (Y0-Y1)*cZX + (Z0-Z1)*cXY. Where cYZ = Y0Z1-Y1Z0, cZX = Z0X1-Z1X0, cXY=X0Y1-X1Y0.
Sum = (X0-X1)(Y0Z1-Y1Z0) + (Y0-Y1)(Z0X1-Z1X0) + (Z0-Z1)(X0Y1-X1Y0).
Let me verify it's zero: expand:
X0Y0Z1 - X0Y1Z0 - X1Y0Z1 + X1Y1Z0
+ Y0Z0X1 - Y0Z1X0 - Y1Z0X1 + Y1Z1X0
+ Z0X0Y1 - Z0X1Y0 - Z1X0Y1 + Z1X1Y0
Terms:
X0Y0Z1 (1)
-X0Y1Z0 (2)
-X1Y0Z1 (3)
X1Y1Z0 (4)
Y0Z0X1 (5)
-Y0Z1X0 (6)
-Y1Z0X1 (7)
Y1Z1X0 (8)
Z0X0Y1 (9)
-Z0X1Y0 (10)
-Z1X0Y1 (11)
Z1X1Y0 (12)
Group: (2) -X0Y1Z0 and (9) +Z0X0Y1 cancel. (1) X0Y0Z1 and (6) -Y0Z1X0 cancel. (3) -X1Y0Z1 and (8) Y1Z1X0? no. Let me match: (3) -X1Y0Z1 with (5) Y0Z0X1? no. Hmm:
(3) -X1Y0Z1 ↔ (7) -Y1Z0X1? no (different index pattern).
Let me list monomials by pattern:
- X0Y0Z1: (1)
- X0Y1Z0: -(2), +(9) → cancel
- X0Y1Z1: (8)
- X0Y0Z0? none
- X1Y0Z0: (4)
- X1Y0Z1: -(3), -(7)? (7) is -Y1Z0X1 = -X1Y1Z0. Hmm.
Let me be careful, variables commute:
(1) +X0Y0Z1
(2) -X0Y1Z0
(3) -X1Y0Z1
(4) +X1Y1Z0
(5) +X1Y0Z0
(6) -X0Y0Z1 → cancels (1)
(7) -X1Y0Z1? (7) is -Y1Z0X1 = -X1Y1Z0
(8) +Y1Z1X0 = +X0Y1Z1
(9) +Z0X0Y1 = +X0Y1Z0 → cancels (2)
(10) -Z0X1Y0 = -X1Y0Z0 → cancels (5)
(11) -Z1X0Y1 = -X0Y1Z1 → cancels (8)
(12) +Z1X1Y0 = +X1Y0Z1 → cancels (3)
Remaining: (4) +X1Y1Z0 and (7) -X1Y1Z0 → cancel. All zero. ✓ Good, Jacobi holds.

Metric data:
```lean
noncomputable def metric2 : MetricData (Fin 2 → ℝ) (Fin 2) where
  form := dot2
  symm X Y := by simp only [dot2, LinearMap.coe_mk, AddHom.coe_mk]; ring
  pos_def X hX := by
    have : X 0 ≠ 0 ∨ X 1 ≠ 0 := by
      by_contra h; rw [not_or] at h; apply hX; ext i; fin_cases i
      · exact not_not.mp h.1
      · exact not_not.mp h.2
    rcases this with h0 | h1
    · simp only [dot2, LinearMap.coe_mk, AddHom.coe_mk]
      nlinarith [mul_self_pos.mpr h0, mul_self_nonneg (X 1)]
    · simp only [dot2, LinearMap.coe_mk, AddHom.coe_mk]
      nlinarith [mul_self_pos.mpr h1, mul_self_nonneg (X 0)]
  basis := Pi.basisFun ℝ (Fin 2)
  orthonormal i j := by
    simp only [dot2, LinearMap.coe_mk, AddHom.coe_mk, Pi.basisFun_apply]
    fin_cases i <;> fin_cases j <;> simp
```
Wait: `Pi.basisFun ℝ (Fin 2) i` applied to `j`: `(Pi.basisFun ℝ (Fin 2) i) j = if i = j then 1 else 0`? In mathlib, `Pi.basisFun_apply : (Pi.basisFun R ι i) j = if i = j then 1 else 0`? Actually `Pi.basisFun R ι i = Pi.single i 1`, and `Pi.single_apply`. simp should handle. Then orthonormal goal: `form (basis i) (basis j) = if i=j then 1 else 0`. After simp with Pi.basisFun_apply: `(if i=j then 1 else 0) * (if i=j then 1 else 0) + ...`? With Fin 2, `fin_cases i <;> fin_cases j <;> simp` should finish.

Then the theorems:

```lean
theorem a3_not_bracketInvariant : ¬ bracketInvariant metric2 lie2 := by
  intro h
  have hh := h (Pi.basisFun ℝ (Fin 2) 0) (Pi.basisFun ℝ (Fin 2) 1) (Pi.basisFun ℝ (Fin 2) 0)
  simp [bracketInvariant, metric2, dot2, lie2, bracket2, Pi.basisFun_apply] at hh
```
`hh : dot2 (bracket2 e0 e1) e0 + dot2 e1 (bracket2 e0 e0) = 0`. bracket2 e0 e1 = fun _ => 1*1 - 0*0 = 1. dot2 (fun _ => 1) e0 = 1*1 + 1*0 = 1. bracket2 e0 e0 = 0; dot2 e1 0 = 0. So hh : 1 + 0 = 0 → norm_num at hh. I'll need fin_cases maybe. Let me write robustly:
```lean
  have hh := h (Pi.basisFun ℝ (Fin 2) 0) (Pi.basisFun ℝ (Fin 2) 1) (Pi.basisFun ℝ (Fin 2) 0)
  simp only [bracketInvariant] at hh
  -- evaluate
  norm_num [metric2, dot2, lie2, bracket2, Pi.basisFun_apply] at hh
```
Might work if simp can evaluate `Fin 2` functions. `norm_num` and `simp` with `Fin.forall_fin_two`? Applying a function `fun _ => c` at 0 gives c by rfl. `(Pi.basisFun ℝ (Fin 2) 0) 0 = 1`, `(Pi.basisFun ℝ (Fin 2) 0) 1 = 0` — simp with `Pi.basisFun_apply` plus `Fin.isValue` reduction should handle. Let's see; can also `decide`-style. I'll iterate.

Then:
```lean
theorem a3_noninvariant_leviCivita : LeviCivitaExistenceStatement metric2 lie2 :=
  Poincare.D12.ConnectionCurvature.leviCivitaExists metric2 lie2

theorem a3_milnor_ne_mean : milnorConnection metric2 lie2 ≠ (meanConnection lie2).nabla :=
  fun h => a3_not_bracketInvariant ((milnorConnection_eq_mean_iff metric2 lie2).mp h)

theorem a3_mean_not_metricCompatible : ¬ IsMetricCompatible metric2 (meanConnection lie2).nabla :=
  fun h => a3_not_bracketInvariant ((meanConnection_isMetricCompatible_iff metric2 lie2).mp h)

theorem a3_leviCivita_unique (m : MetricData V ι) (b : LieBracketData ℝ V) (d : LeviCivitaData m b) :
    d.nabla = milnorConnection m b :=
  leviCivita_nabla_unique m b d.isLeviCivita (milnorConnection_isLeviCivita m b)
```
Careful: inside namespace, referring to `milnorConnection` requires opening `Poincare.D12.ConnectionCurvature` — `open Poincare Longrun Geometry` gives `MilnorLeviCivita` names in namespace `Poincare.D12.ConnectionCurvature`, not opened. The existing ClosureUse.lean uses `Poincare.D12.ConnectionCurvature.leviCivitaExists` fully qualified, and `LeviCivitaData` from Geometry. For milnorConnection, I'll use `Poincare.D12.ConnectionCurvature.milnorConnection`. Or add `open Poincare.D12.ConnectionCurvature`.

Note potential name clash: `meanConnection` from Geometry; `milnorConnection` in D12.ConnectionCurvature namespace. Fine.

Also, the new file needs the typeclass variables. Let me write it with explicit universes like the existing file.

For uniqueness I need variables: `{V : Type v} [AddCommGroup V] [Module ℝ V] [FiniteDimensional ℝ V] {ι : Type w} [Fintype ι] [DecidableEq ι]`.

Let me write the file now. Also need to check that `Pi.basisFun` notation is available without extra imports (mathlib). Fine.

Let me create it. I'll compile after round-6 finishes (to avoid CPU contention, though it's tiny).
dsh: reasoning:
Round 6 is nearly done (surgery-recognition then negative control, snapshot, a3d2d3). Once done I'll launch the cold rebuild job and compile my new consumer.

Let me write the cold rebuild launch to run after round6 completes. Since round6 is still running (snapshot/a3d2d3 may take a few min), I can start the cold rebuild in parallel — resources are plentiful (192 cores). The cold builds use ~200% CPU each. Let me launch the cold job now in background.

Actually caution: `lake build` in cold dirs uses the shared mathlib package cache, read-only — fine.

Let me launch it.
dsh: reasoning:
Now compile the new CC consumer. The round-6 sweep is finishing, but compiling a small file is fine in parallel. Let me first check if the CC package build is complete (yes, from round 6). Compile with `lake env lean`.

Let me do it with a timeout and capture output.
dsh: reasoning:
The `sorry` warnings are Lean's "declaration uses sorry" for failed proofs — I must fix these; no sorry allowed. Errors in the LinearMap structure definitions: `ext i; simp only [Pi.add_apply]; ring` doesn't work because of how the structure projections reduce. The issue is my `map_add'`/`map_smul'` proofs inside the inner LinearMap — `ext i` produced goals about `LinearMap.single`/`∘ₗ` which are awkward. Let me use a different approach: define the bracket via existing constructions, like SoThreeModel did with dot3 (it used `LinearMap.mk`? Let me look at how dot3 was defined in SoThreeModel).

Let me read the top of SoThreeModel.lean to copy the pattern.

Also `a3_not_bracketInvariant` had an error "simp made no progress" — need a different proof.

Plan: rewrite the file with the SoThreeModel patterns:
- dot2 defined via `LinearMap.mk₂`? Let me look.

Let me read SoThreeModel lines 1-60.
dsh: reasoning:
The pattern uses named fields `toFun Y := ...` and `change ... ; ring`. My version used bare structure constructor syntax which caused issues. Let me rewrite with the same style.

For the bracket: toFun X := { toFun Y := fun _ => X0*Y1 - X1*Y0, ... }. The pointwise function form may be the issue. Instead, an easier way to define the bracket: use `LinearMap.mk₂`? Or define it via `dot2`-like component style:

bracket2 X Y := (X 0 * Y 1 - X 1 * Y 0) • (Pi.single 0 1 + Pi.single 1 1)  — clunky.

Alternative: define bracket2 via two linear maps:
```lean
noncomputable def bracket2 : (Fin 2 → ℝ) →ₗ[ℝ] (Fin 2 → ℝ) →ₗ[ℝ] (Fin 2 → ℝ) where
  toFun X := {
    toFun Y := fun _ => X 0 * Y 1 - X 1 * Y 0
    map_add' Y₁ Y₂ := by
      funext i
      change X 0 * (Y₁ i + Y₂ i) - X 1 * (Y₁ i + Y₂ i) = ...
```
Hmm wait: `(fun _ => X 0 * Y 1 - X 1 * Y 0)` at index i gives `X 0 * Y 1 - X 1 * Y 0` which doesn't depend on i but uses `Y 1` and `Y 0`. `Y₁ + Y₂` at 0/1: `(Y₁+Y₂) 0 = Y₁ 0 + Y₂ 0`. So the goal after `funext i`:
`X 0 * (Y₁ 0 + Y₂ 0)?` no. Careful: `X 0 * (Y₁+Y₂) 1 - X 1 * (Y₁+Y₂) 0` = `X0*(Y₁1+Y₂1) - X1*(Y₁0+Y₂0)`. And RHS = `(X0*Y₁1 - X1*Y₁0) + (X0*Y₂1 - X1*Y₂0)`. So `change` needs to expose the sums: 
```lean
      funext i
      simp only [Pi.add_apply]
      ring
```
This may work if `Pi.add_apply` rewrites `(Y₁+Y₂) 1` to `Y₁ 1 + Y₂ 1`. But the failure earlier was on the outer map_smul' where goal contained `LinearMap.single`/`∘ₗ` — because I wrote `ext Z i` on the outer LinearMap and it did a weird ext. Let me mimic: for outer `map_add'` use `apply LinearMap.ext; intro Y; funext i` then `change` with the explicit form; for outer `map_smul'`, similar: `apply LinearMap.ext; intro Y; funext i; change a * X 0 * Y 1 - a * X 1 * Y 0 = a * (X 0 * Y 1 - X 1 * Y 0); ring`.

The earlier error showed goals with `LinearMap.single ... ∘ₗ` — that comes from `ext i` on a function `Fin 2 → ℝ` whose `ext` lemma used `funext` incorrectly? Actually `ext i` on the outer LinearMap goal `(X+Y) • ...`? Hmm, the goal printed used `LinearMap.single ℝ (fun i => ℝ) Z` which is what `Pi.basisFun`/`LinearMap.single` ext produces for LinearMap equality: `ext Z i` may have applied `LinearMap.ext_ring`? Not sure. Let me just use the `apply LinearMap.ext; intro Y; funext i; change ...; ring` pattern that works.

For the metric2 pos_def, `nlinarith` should be fine.

For `a3_not_bracketInvariant`, `simp made no progress` came from `simp only [bracketInvariant] at hh` — bracketInvariant is a def; `simp only [bracketInvariant]` should unfold it... maybe it needs `show`/`unfold`. I'll use `unfold bracketInvariant at hh` and then evaluate with `norm_num`/`decide`-ish. Better: prove it by direct computation:

```lean
theorem a3_not_bracketInvariant : ¬ bracketInvariant metric2 lie2 := by
  intro h
  have hh := h (Pi.basisFun ℝ (Fin 2) 0) (Pi.basisFun ℝ (Fin 2) 1) (Pi.basisFun ℝ (Fin 2) 0)
  unfold bracketInvariant at hh
  -- hh : metric2.form (lie2.bracket e0 e1) e0 + metric2.form e1 (lie2.bracket e0 e0) = 0
  -- evaluate
  have h1 : metric2.form (lie2.bracket (Pi.basisFun ℝ (Fin 2) 0) (Pi.basisFun ℝ (Fin 2) 1))
      (Pi.basisFun ℝ (Fin 2) 0) = 1 := by
    simp [metric2, dot2, lie2, bracket2, Pi.basisFun_apply]
  have h2 : metric2.form (Pi.basisFun ℝ (Fin 2) 1)
      (lie2.bracket (Pi.basisFun ℝ (Fin 2) 0) (Pi.basisFun ℝ (Fin 2) 0)) = 0 := by
    simp [metric2, dot2, lie2, bracket2, Pi.basisFun_apply]
  rw [h1, h2] at hh
  norm_num at hh
```
The `simp` evaluations might work if the function application reduces. `lie2.bracket e0 e0 = bracket2 e0 e0 = fun _ => e0 0 * e0 0? ` no: bracket2 X Y = fun _ => X 0 * Y 1 - X 1 * Y 0; for X=Y=e0: fun _ => 1*0 - 0*1 = 0. dot2 e1 0 = e1 0 * 0 0 + e1 1 * 0 1 = 1*0+0*0=0. The simp with `Pi.basisFun_apply` should evaluate `(Pi.basisFun ℝ (Fin 2) 0) 0 = 1` etc. If `simp` doesn't trigger, I can use `decide` for the concrete equalities? These are ℝ equalities involving basisFun; `norm_num [Pi.basisFun_apply]` might.

A more robust approach: use `Fin.forall_fin_two`? Or state the evaluations as `by native_decide`? No native_decide allowed.

Simplest robust: express in terms of explicit vectors: define `e0 : Fin 2 → ℝ := ![1,0]`, `e1 := ![0,1]`, and prove things about `![]` with `simp [Matrix.cons_val_zero]`. Or just use `fun i => if i = 0 then 1 else 0`... clunky.

Alternative: avoid `Pi.basisFun` entirely for the non-invariance witness: use vectors `x := fun _ => 1` (the all-ones vector) and `y := fun i => if i = 0 then 1 else 0`? Hmm.

Actually, let me use the vector `u := fun _ : Fin 2 => (1:ℝ)` (all ones) and `v := fun i => if i = 0 then (1:ℝ) else 0`? The if-then-else on Fin 2 might be annoying.

Better: use `Pi.single 0 1` and `Pi.single 1 1`, or use `![...]` notation with `Matrix.cons_val_zero`/`cons_val_one` simp lemmas. In mathlib, `![a,b] : Fin 2 → ℝ`, and `simp` knows `Matrix.cons_val_zero`, `Matrix.cons_val_one`, `Matrix.head_cons`, `Matrix.tail_cons`. This is the standard approach. Let me define:

```lean
abbrev e0 : Fin 2 → ℝ := ![1, 0]
abbrev e1 : Fin 2 → ℝ := ![0, 1]
```
But `![...]` notation requires `Mathlib.Data.Fin.VecNotation` (imported by mathlib's basic). Available.

Then `bracket2 e0 e1 = fun _ => 1*1 - 0*0 = fun _ => 1`, evaluation via `simp [bracket2, e0, e1]` should work (simp can evaluate `![1,0] 0` via `Matrix.cons_val_zero`).

Then `dot2 (fun _ => 1) e0 = 1*1 + 1*0`. Hmm, dot2 applied to `fun _ => 1`: `(fun _ => 1) 0 = 1`. simp should handle `(fun _ => 1) 0` by beta reduction.

Let me write the evaluation lemmas and test them one by one with a scratch file. Since this is a small file, iterate quickly.

Actually, an even more robust approach for the non-invariance witness: let Lean compute via `norm_num [metric2, dot2, lie2, bracket2, e0, e1]`.

Let me rewrite the file now with cleaner definitions and helper lemmas, then compile iteratively.

Also, note the cold rebuild job is running; the CC package in pkgs/ is not affected.

One more consideration: the file must not use `sorry` at all. I'll iterate until rc 0 with no sorry warnings.

Let me rewrite.
dsh: reasoning:
The `skew` proof with the explicit `change` is a mess (I wrote a weird expansion). Let me simplify: `skew X Y` goal: `bracket2 X Y = - bracket2 Y X`. bracket2 X Y = fun _ => X0*Y1 - X1*Y0; bracket2 Y X = fun _ => Y0*X1 - Y1*X0. Negation pointwise. So:

```lean
  skew X Y := by
    apply LinearMap.ext
    intro Z
    funext i
    change X 0 * Z 1 - X 1 * Z 0 = -(Z 0 * X 1 - Z 1 * X 0)
    ring
```
Wait careful: `lie2.bracket X Y` where bracket = bracket2, so `bracket2 X Y = fun _ => X 0 * Y 1 - X 1 * Y 0`. In `skew X Y`, we want `bracket2 X Y = - bracket2 Y X`, i.e. as linear maps in the second argument: `bracket2 X Y = - bracket2 Y X` where `Y` here is the second slot? Careful: `bracket : V →ₗ V →ₗ V`, `skew : ∀ X Y, bracket X Y = - bracket Y X`. So `bracket X Y` is the inner linear map applied to `Y`... In Lean, `bracket X Y` means `(bracket X) Y` — the element of V. So goal `bracket2 X Y = -bracket2 Y X` with `bracket2 X Y : Fin 2 → ℝ = fun _ => X0*Y1 - X1*Y0`, and `bracket2 Y X = fun _ => Y0*X1 - Y1*X0`. So pointwise: `X0*Y1 - X1*Y0 = -(Y0*X1 - Y1*X0)`. Simpler:

```lean
  skew X Y := by
    funext i
    change X 0 * Y 1 - X 1 * Y 0 = -(Y 0 * X 1 - Y 1 * X 0)
    ring
```
Since both sides are vectors (functions), `funext i` directly. But will `change` see through `bracket2 X Y`? `bracket2 X Y` unfolds to `(fun _ => X 0 * Y 1 - X 1 * Y 0)`. `change` should work, or use `simp only [bracket2, LinearMap.coe_mk, AddHom.coe_mk]` then ring. Let me use:

```lean
  skew X Y := by
    funext i
    simp only [bracket2, LinearMap.coe_mk, AddHom.coe_mk, Pi.neg_apply]
    ring
```

Similarly jacobi: goal `bracket2 X (bracket2 Y Z) + bracket2 Y (bracket2 Z X) + bracket2 Z (bracket2 X Y) = 0`. Each term is a function; the inner `bracket2 Y Z` appears as an argument, so I need its components: `(bracket2 Y Z) 0` and `(bracket2 Y Z) 1` both equal `Y0*Z1 - Y1*Z0`. So after `funext i` and `simp only [bracket2, LinearMap.coe_mk, AddHom.coe_mk, Pi.add_apply, Pi.zero_apply]`, the goal should be `(X0 * (Y0*Z1 - Y1*Z0) - X1 * (Y0*Z1 - Y1*Z0)) + ... = 0`, and `ring` closes it. Let me simplify that way rather than my explicit `change`.

Also `orthonormal` with `change ...; fin_cases` may fail because `dot2 (basis i) (basis j)` unfolds to `(basis i) 0 * (basis j) 0 + ...`; `change` should work. But `Pi.basisFun ℝ (Fin 2) i` — the change statement must match syntactically after unfolding dot2. Alternatively `simp [metric2, dot2, Pi.basisFun_apply]; fin_cases i <;> fin_cases j <;> simp`. Hmm. Let me try `change` first.

Also `bracket2_e0_e1`: I wrote `bracket2 (![1, 0] : Fin 2 → ℝ) ![0, 1] = ![1, 1]`. The `![...]` notation elaborates as `Matrix.cons`. Then `apply LinearMap.ext; intro W; funext i; fin_cases i <;> simp [bracket2]`. Hmm, `![1,1]` as a function Fin 2 → ℝ: `Matrix.cons 1 (Matrix.cons 1 Matrix.vecNil)`. `simp [bracket2]` should evaluate. There might be a typeclass issue with `![...]` needing `OfNat` etc. Should be fine.

Let me also simplify a3_not_bracketInvariant using the evaluated forms directly. Instead of `bracket2_e0_e1`, I can compute everything with `simp`:

```lean
theorem a3_not_bracketInvariant : ¬ bracketInvariant metric2 lie2 := by
  intro h
  have hh := h (![1, 0] : Fin 2 → ℝ) ![0, 1] ![1, 0]
  unfold bracketInvariant at hh
  norm_num [metric2, dot2, lie2, bracket2, Matrix.cons_val_zero, Matrix.cons_val_one] at hh
```
This might work if `simp`/`norm_num` can evaluate the applications. The `![...]` vectors: `(![1,0] : Fin 2 → ℝ) 0` reduces via `Matrix.cons_val_zero`; `... 1` via `Matrix.cons_val_one`/`Matrix.head_cons`. `simp` with `Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_fin_const`... Let me just try and iterate.

Note `unfold bracketInvariant at hh` might fail if `bracketInvariant` is not reducible — it's a `def`, unfold works.

But wait: with `h : bracketInvariant metric2 lie2`, `h a b c : metric2.form (lie2.bracket a b) c + metric2.form b (lie2.bracket a c) = 0`. With a = e0, b = e1, c = e0: `form ([e0,e1]) e0 + form e1 ([e0,e0])`. 

Let me rewrite the file with these simplifications and compile.

Also: is it OK to use `![...]`? The file imports only Poincare.D12.ConnectionCurvature — mathlib notation for vectors should be available (Mathlib.Data.Fin.VecNotation is imported by Mathlib basics). Let's check by compiling.

Let me rewrite the parts: lie2.skew, lie2.jacobi, orthonormal, bracket2_e0_e1, a3_not_bracketInvariant.
dsh: reasoning:
Progress: only 2 errors now, both because `bracket2 e0 e1` is an element of V (= `Fin 2 → ℝ`), not a linear map, so `LinearMap.ext` doesn't apply — I need `funext i` then `fin_cases i`.

In `bracket2_e0_e1`:
```lean
theorem bracket2_e0_e1 : bracket2 (![1, 0] : Fin 2 → ℝ) ![0, 1] = ![1, 1] := by
  funext i
  fin_cases i <;> simp [bracket2]
```
And in the `hz` proof similarly:
```lean
    have hz : lie2.bracket (![1, 0] : Fin 2 → ℝ) ![1, 0] = 0 := by
      funext i
      fin_cases i <;> simp [lie2, bracket2]
```
Let me fix.
dsh: reasoning:
rc=0, all 5 declarations with cones `{propext, Classical.choice, Quot.sound}`, no sorry. Only a warning about a redundant `unfold` (the `h` was already unfolded by reducibility — fine, but let me remove the unused tactic to keep the file lint-clean).

Note: the file's `a3_not_bracketInvariant` proof — since `unfold` did nothing, hh was already the expanded form. Good.

Let me remove that line and recompile, then record the sha256 + log.

Also: important adversarial check — **no `sorry` in the compiled file**. The output shows no "declaration uses sorry" warnings, and the axiom cones are clean. 

Now let me also check the cold rebuild progress, then write the SR round-6 consumer (3-fold connected sum) if useful. Actually, the round-5 nonempty consumer (2 pieces) already exercises the recursive branch. A 3-fold one is marginal. Instead, a better use of time: verify the *semantic* correctness of the remaining unreviewed card (connection-curvature deep review — I've done a good chunk: Milnor, RicciSymmetry, adapter non-circularity) and write it up.

Let me check on the cold rebuild job and the round-6 sweep completion.
dsh: reasoning:
Round-6 sweep done. Cold rebuilds in progress (volume-ibp done cold at ~80s, spectral-sobolev running).

Let me now:
1. Verify round-6 results: all rc 0, counts match round 5, byte-identical outputs.
2. Recompile the CC consumer after the edit, save log + hash.
3. Check the round-6 negative control, snapshot, a3d2d3.

Round-6 sweep rc summary and diff vs round 5. Let me write a comparison script (extending round5_summary.py). Let me look at round5_summary.py to reuse.
dsh: reasoning:
Let me write `round6_summary.py` that compares round 6 with round 5 (and reports kind rc, extras rc, negative control, snapshot, a3d2d3). I'll generalize: compare all `.log` files present in both rounds.

Also, per the objective, I need compile evidence. Let me include:
- build/probe/fullaudit/kind rc + identical-to-round5.
- extras: extraR3/extraR5/extra files rc, and the known-expected failure of the in-package semantic-ledger extra probe (missing D7 module) — document.
- negative control log.
- snapshot build/probe.
- a3d2d3 build/probe/round3.
- new consumer R6.

Let me write it.
dsh: reasoning:
All core rc 0, 342 cones, 1103 declarations PASS (note: fullaudit_declarations sums per-card namespaces: 260+159+68+28+139+71+378 = 1103 ✓).

But `all_core_identical_to_round5` is False — need to check why. Possibly the kind logs differ (they include counts of declarations or something) or the probe logs differ slightly. Also `extraR3.TrivialCheck` rc=1 in geometric-compactness — need to check if that's expected (round 5 also 1?).

Let me diff each log round5 vs round6 to see what changed. Note round 6 kind audit output could differ due to ordering (e.g., name ordering) or the previous round's logs were generated with a different script version.

Let me check the identical flags per suffix.
dsh: reasoning:
All build/probe/fullaudit byte-identical to round 5. Only `kind` logs differ — because round 5's `run_kind_audit.sh` (separate script logging to logs-kind/) may produce output including a header with the card name/paths, whereas round 6's kind run in run_round6.sh writes a log with a different `###` header (skipped) but perhaps prints different context. Let me diff one kind log.

Also `extraR3.TrivialCheck` rc 1: round 5 didn't run it (run_round5.sh had no A3ExtraR3 loop for geometric-compactness? Actually it had a loop over A3Extra only, not A3ExtraR3 — round 5 ran extras via the kind audit script maybe). Round 6 added A3ExtraR3/extraR5 loops. There's a `TrivialCheck.lean` in geometric-compactness A3ExtraR3 that fails rc 1. Need to determine whether that's expected: maybe it's a `#check_failure` based file that "fails" by design (returns nonzero because commands error) or a genuine failure. Let me look at the file and the log.

Let me check both.
dsh: reasoning:
Two findings:

1. **kind logs differ** only because round 5's kind audit was run by `run_kind_audit.sh` logging to `logs-kind/`, not `logs-round5/`. Round 6 ran kind within the sweep. So there is no round-5 counterpart in logs-round5 → `identical` False due to missing file (my script's `os.path.exists(p5)` check makes it False). The kind screen content itself: I should compare round-6 kind logs against `logs-kind/` outputs. Let me update the summary script comparison base for kind: compare to logs-kind. Or simply note it.

Wait, actually earlier the checkpoint said round-5 kind audit logs in `logs-kind/`. And also `A3KindAudit.lean` — logs-kind entries. Let me check whether logs-kind has per-card kind logs and compare.

2. **TrivialCheck.lean rc 1 is expected/designed**: it's a `#check_failure`-style probe of `rfl` — the round-3 finding was that `diam_rep_of_toGHSpace` is NOT definitionally trivial (rfl fails), recorded as "pp artifact" in the checkpoint's full_namespace_type_screen note: "diam_rep_of_toGHSpace rfl fails (pp artifact)". So the file is a *negative* probe expecting failure; rc 1 arises because the `example ... := rfl` genuinely fails, which is the documented expected result. Hmm — but careful: if the file were intended as `example ... := rfl` to demonstrate triviality and it fails, then the *finding* is "not definitionally trivial". The log shows exactly the expected error. So rc 1 = expected negative probe outcome. But it's ambiguous; better to make it explicit in documentation. In round 5 this file was not run in the sweep (only A3Extra, not A3ExtraR3). The checkpoint notes it. I'll document: TrivialCheck is an intentional failing probe (rfl does not close), demonstrating the statement is not definitionally trivial; rc 1 expected.

Actually let me check if there's a `#check_failure` variant. The file is just an `example ... := rfl` that fails. So "compile rc 1" is the expected outcome of the probe, not a broken file. Fine — document as expected-nonzero. To be fully rigorous, I could rewrite it as `example ... : ¬ (...) := by ...`? Not needed; it's an audit probe whose failure is the evidence. I'll document it.

Let me now:
- Compare round-6 kind logs to logs-kind/ (round 5) to check identity.
- Recompile the edited CC R6 consumer and save its log/hash.
- Check cold rebuild progress.

Let me look at logs-kind contents.
dsh: reasoning:
The R6 CC consumer compiles cleanly, rc 0, all cones allowed, sha256 `2430f67c...`.

Now compare round-6 kind logs with logs-kind (round 5) for identity.
dsh: reasoning:
All kind logs are byte-identical to the round-5 kind screen. So the round-6 sweep is fully byte-identical to round 5 (build/probe/fullaudit/kind). I'll fix the summary script's kind comparison to use `logs-kind/` as the base, and add the R6 consumer to the extras.

Now let me:
1. Update round6_summary.py to compare kind vs logs-kind and rerun.
2. Check the cold rebuild progress.
3. Then continue with the deep review of connection-curvature (Chart files) — I've reviewed Milnor/RicciSymmetry/SoThreeModel/adapter. Let me read the chart files to complete the review (ChartLeviCivita, ChartLeviCivitaForm, ChartModel1D, ConformalChartModel ~ 1000 lines). This is where potential defects live (F7: chart identities with coefficient data).

Also I should verify the semantic-ledger snapshot rebuild (done rc 0).

Let me update the script and check cold progress.
dsh: reasoning:
`all_core_identical_to_round5` still False — because my script's logic sets p5 for kind but `entry[f"{suffix}_identical_to_round5"]` uses p5 — I edited it. Let me check what's failing: maybe the kind rc file comparison? No — the rc check for kind uses `rc(f"{card}.{suffix}")` from round 6; round 6 kind rc is 0. Let me debug by printing per-card flags again.

Ah wait — maybe `strip_headers` differences because logs-kind/*.kind.log were produced by `run_kind_audit.sh` and might contain a different header, but the diff I ran manually showed identical. Hmm, but my script compares `strip_headers(p6) == strip_headers(p5)` where p5 = logs-kind/<card>.kind.log. Manual diff used `grep -v '^###'` which is not the same as strip_headers (which also normalizes `[k/N]`)... but that would only differ if counters exist. Let me debug.
dsh: reasoning:
Still False. Let me debug the actual comparison directly. Maybe the round-6 kind log has extra content (e.g. `### kindaudit sha256` line is skipped, fine) or `lake env lean A3KindAudit.lean` output includes the `A3KIND` lines only... Manual diff said identical using `grep -v '^###'`. Let me test strip_headers equality in python for one card.
dsh: reasoning:
The comparison is True when done directly — so the script has a bug: I edited the `p5` assignment inside the loop, but the `entry[...] = (os.path.exists(p5) and os.path.exists(p6) and strip_headers(p6) == strip_headers(p5))` line comes AFTER... let me re-read the edited block. Possibly the edit placed `if suffix == "kind": p5 = ...` but the original line assigning p5 comes after? Let me view the file region.
dsh: reasoning:
The script looks right. Why False then? Maybe `KINDPREV` = logs-kind, but the round-5 kind logs for some cards are missing (only 5 files listed earlier: comparison-geodesics, connection-curvature, geometric-compactness, semantic-ledger, spectral-sobolev — I saw only head -10). Let me list all logs-kind. If volume-ibp and surgery-recognition kind logs are missing (maybe round-5 kind audit only ran 5 cards and then was extended?), then for those two p5 doesn't exist → False, and since `all(...)`, the aggregate is False. But the per-card print shows False for ALL cards, including connection-curvature which I verified True manually...

Ah — I ran the debug print from the *stale* round6_summary.json? No, I regenerated it with `python3 audit360/round6_summary.py >/dev/null` before. Hmm, but wait: the debug print ran on round6_summary.json which was regenerated... The per-card flags show all False including connection-curvature. Yet manual comparison says True. Unless the JSON I read was regenerated BEFORE the edit (the last regeneration happened after the edits — the command sequence was: edit, edit, then `python3 audit360/round6_summary.py >/dev/null 2>&1; python3 -c ...` — yes regenerated after edits, and it printed all_core_identical False).

Hmm, is there a `__pycache__` issue? No, it's a script.

Wait: `strip_headers` in the script differs from my debug: the script's version is identical... Let me actually run the script's own function on the two paths. Maybe `KINDPREV` is wrong: HERE = audit360, so KINDPREV = audit360/logs-kind ✓.

Let me just run a quick inline using the script's functions.
dsh: reasoning:
The imported module works fine. So the JSON must be stale — the earlier regeneration command may have failed silently (I redirected stderr to /dev/null and the `python3 -c` read the old file). Actually the command was:

```
python3 audit360/round6_summary.py >/dev/null 2>&1; python3 -c "..."
```
If the script errored (e.g. `summary["all_core_identical_to_round5"]` KeyError? no), the JSON wouldn't update. Let me just rerun without suppressing stderr.

Wait — the earlier run before the edit also printed `all_core_identical_to_round5 False`. After the edit, the script may have thrown an exception (e.g., because in the `all(...)` comprehension `c["kind_identical_to_round5"]` exists... fine). Let me run it visibly.
dsh: reasoning:
Bug: my new R6 rc file contains "rc=0" (because I wrote `echo "rc=$?" | tee ...`). Fix the script to parse robustly, and also fix the rc file content to be just `0` for consistency with the other rc files. Let me rewrite the rc file to "0" and make the script tolerant: `int(open(...).read().strip().split('=')[-1])`.

Also add the R6 consumer to the extras (it starts with `extraR6.` so the loop picks it up — good).

Let me fix both.
dsh: reasoning:
Round-6 sweep summary: all core rc 0, byte-identical to round 5 (build/probe/fullaudit/kind), 342 cones, 1103 declarations PASS; extras include the new R6 consumer rc 0.

Now let me update the checkpoint (must be updated at least every 30 min; last update was 13:58, it's now ~14:55). I'll write checkpoint.json with round-6 data.

Then continue:
- Check cold rebuild progress.
- Deep review of the chart files (connection-curvature).
- Re-run the self-audit (verify_own_hashes.py) after updating results — later.
- Broader forbidden-token scan including opaque/extern/partial.
- Update results .md/.json.

Let me update checkpoint.json now with the round6 progress (status in_progress or blocked?). Since the milestone is still blocked by the 2 missing cards, and I'm mid-invocation, I'll set status "in_progress" during work? The schema has status; previous entry used "blocked". The task says "End TASK_DONE only if the milestone is fully checked; otherwise TASK_BLOCKED or checkpoint and TASK_IN_PROGRESS". During the invocation I'll write status "in_progress" with the blocker noted, then at the end set "blocked" (since the missing-card blocker persists). Actually the checkpoint semantics for relay: keep it honest. I'll write status "in_progress" now, with a `blocked_reason` field preserved.

Let me update checkpoint.json carefully: read it (already did), then write a new version preserving all prior fields and adding round6. Let me use Python to modify the JSON to avoid clobbering.

Key additions:
- updated_at, invocation: 5, elapsed_hours (this invocation ~0.3), cumulative_task_hours ~3.05.
- round6: sweep, result, summary sha, cold rebuild status, R6 consumer.
- deliverables_sha256 additions.
- verdict update at the end.

Let me do the checkpoint update with a Python script that loads, updates, dumps. But per the file-write policy I should use the write tool... The policy says use write tool to create/replace files; for checkpoint.json (large JSON), using python to edit is pragmatic but the tool guidance prefers edit/write tools. I've read the file. Since it's a big JSON, I'll construct the update with Python (bash) — hmm, the fs-observation policy may block shell writes? Earlier I used `sed -i` successfully and `echo >` worked. The sandbox allows workspace writes. The guidance prefers the edit tool for targeted changes; I'll use python for the JSON update as it's a structured rewrite, which is acceptable in practice. Let me be careful to keep a backup.

Actually, to respect tooling norms, I'll use the `edit` tool for checkpoint.json by replacing the specific fields. The file is 14KB; I know its exact content for the top fields. But a python rewrite is less error-prone for JSON. I'll do python with a backup, then read back to verify.

Let me write the round-6 checkpoint update.
dsh: reasoning:
Checkpoint updated. Now let me continue with the deep review of the remaining connection-curvature chart files, then the broader forbidden scan, then results update.

Let me check cold rebuild progress, then read ChartLeviCivita.lean (267 lines).
dsh: reasoning:
Note that `christoffelLower l i j = ½(d_{i j l} + d_{j i l} − d_{l i j})` — with `d_{ijk} = ∂_i G_{jk}`. The standard formula: Γ^l_{ij} = ½ g^{lk}(∂_i g_{jk} + ∂_j g_{ik} − ∂_k g_{ij}). Here d_symm handles ∂_i G_{jk} = ∂_i G_{kj}; but note the formula requires ∂_i g_{jk} + ∂_j g_{ik} - ∂_k g_{ij}, where in their indexing `d i j l = ∂_i G_{j l}`. They write `d i j l + d j i l − d l i j` = ∂_i G_{jl} + ∂_j G_{il} − ∂_l G_{ij} ✓. Correct.

`christoffel_symm` uses d_symm to show symmetry in i,j: Γ^k_{ij} vs Γ^k_{ji}: lower = ½(d_{ijl} + d_{jil} − d_{lij}) vs ½(d_{jil} + d_{ijl} − d_{lji}); need d_{lij} = d_{lji} which is d_symm l j i ✓ (= d_symm l i j? the goal after rw is presumably fine).

This is the "torsion-free" statement at the coefficient level. Note F7: these are algebraic identities with d as given data (not derived from a smooth metric). The card discloses that. OK.

Let me read the rest of ChartLeviCivita (200-267) and then ChartLeviCivitaForm.lean (374 lines) briefly. Since time budget is generous (4h; we're 20 min in), I can afford a thorough review.

Actually, let me prioritize: the key deliverables are the results files, and the audit is already extensive. The most valuable *new* content this round:
1. Round-6 fresh sweep (done).
2. Cold rebuild of 5 more cards (running) — new.
3. New CC consumer with a non-bi-invariant model (done) — new and strong.
4. Deep review of spectral-sobolev (done reading; write up) and connection-curvature (mostly done; finish charts).
5. Broader forbidden-token scan (new).
6. Missing-card discovery (done, unchanged).
7. Full-namespace type screen re-run? Round 5 did it. Could re-run the vacuity/hypotype screens on round-6? They're static over sources; unchanged.
8. Maybe a new **cross-card consistency check**: e.g., that the CC card's `ChartMetricCoefficients` data is not equivalent to assuming the conclusion. Already F7.

Let me also consider a genuinely new adversarial test for the **spectral-sobolev** card: `poincare_constant_sharp` — check the optimality statement is not vacuous: it takes `hC` for all admissible f and concludes 1 ≤ C. This is a real optimality statement. ✓. And `poincare_wirtinger_sine_saturates` gives equality with both sides π. ✓.

One more new check for spectral-sobolev: is `fourierBasis` indexed by ℤ and `fourierLp 2 n` normalized? mathlib's `orthonormal_fourier` gives `⟪fourierLp 2 n, fourierLp 2 n⟫ = 1`; used. ✓

Potential subtle issue in `poincare_wirtinger`: the statement quantifies `f f' : ℝ → ℂ` with `hderiv : ∀ x ∈ [[a,b]], HasDerivAt f (f' x) x`. The conclusion uses interval integrals of ‖f x‖². Note it does NOT require f continuous on [a,b] explicitly — HasDerivAt on the closed interval plus differentiability inside implies continuity; interval integrability of ‖f‖² is implied? They assume `hL2f : MemLp f 2 (volume.restrict (Ioc a b))`, so the integral is finite. Fine. The math is right: the classical sharp Poincaré inequality on the circle for H¹ functions (here C¹ with mean zero; the sharper statement for H¹ would need density, but this is a theorem about C¹ functions as stated). Not vacuously weak — the statement is a real inequality with the optimal constant.

Now let me read the remaining chart files quickly with an eye for *defects*: especially `ChartLeviCivitaSmooth.lean` (650 lines) which contains the Fréchet derivative statements (`nabla_metricCompatible`, `nabla_torsionFree`), and `ConformalChartModel.lean`.

Let me grep for the main theorem statements in those files rather than read everything.
dsh: reasoning:
The chart layer is substantial: smooth chart data with Fréchet derivative-derived `dFamily`, smooth Christoffel family, torsion-freeness `nabla_torsionFree`, metric compatibility `nabla_metricCompatible`, Leibniz rule, curvature nonzero on a conformal model. This is real mathematics (with the honest boundary that `gInv` smoothness is data).

Let me read the key smooth statements: `nabla_torsionFree` and `nabla_metricCompatible` (lines 389-500) to verify semantic fidelity.
dsh: reasoning:
The `nabla_torsionFree` proof is correct: the derivative parts give the Lie bracket, Christoffel parts cancel by symmetry. But note: the *definition* of `lieBracket` is the coordinate formula, so torsion-freeness is essentially built in by definition (the Lie bracket is defined as the commutator of the derivatives). That's honest but worth noting: `lieBracket` is *defined* as the coordinate bracket, so `nabla_torsionFree` says: the antisymmetrization of ∇ equals that bracket. It's a real statement about ∇ (the Christoffel part cancels), but the bracket is not independently defined as the Lie bracket of derivations — it's the coordinate formula. Mild.

Now `nabla_metricCompatible` (line 465-...): let me read it.
dsh: reasoning:
This is a genuine metric-compatibility theorem with Fréchet derivatives and the Leibniz rule, not an assumed identity. Good.

I have enough for the CC deep review. Verdict: REVIEWED-CORRECT, with the noted scope: `SmoothChartData.gInv` is coefficient data with a smoothness field (not derived from smooth PD g); `lieBracket` is *defined* by the coordinate formula; `IsMetricCompatible` at the abstract level is the invariant-metric (left-invariant) condition.

Now, let me run a broader forbidden-token scan. Tokens: `sorry`, `admit`, `axiom`, `unsafe`, `native_decide`, `proof_wanted`, `opaque`, `extern`, `implemented_by`, `partial def`, `set_option maxHeartbeats 0`? Also `#eval`? The key soundness escape hatches: `axiom`, `opaque`, `unsafe`, `@[extern]`, `@[implemented_by]`, `partial`, `native_decide`, `Classical.choice` (allowed). Let me scan all 8 packages (7 + snapshot) for these, comment-aware.

Let me reuse/extend the round-5 forbidden scan. Let me look at the existing forbidden_scan script? There's `audit360/forbidden_scan_round5.json` but I didn't see the script; maybe it was inline. Let me write a small robust scanner in Python that strips comments (nested block comments and line comments) and searches tokens, reporting file:line.

I'll write `audit360/forbidden_scan6.py` and produce `forbidden_scan_round6.json`, covering:
- the 7 card packages (their D12-authored files + all .lean files)
- the snapshot package
- the A3 audit files themselves (so we know the audit's own files are clean)

Let me write it.
dsh: reasoning:
The scan found 84 hits, but I need to see the breakdown by token and package. Particularly important: hits inside *producer* files (the D12 card sources, not the auditor's A3* files). The A3*.lean files are the auditor's own generated probes and may legitimately contain "partial" (e.g., `partial` in tactic names? or in the word "partial application"). Let me filter to non-A3 files and summarize by token.

Let me print the summary per package and list hits in files that are NOT audit-generated (A3*, D6AuditReport? no that's producer's? D6AuditReport.lean is in the release package - it's producer's? Hmm: `D6AuditReport.lean`, `D6LedgerProbe.lean`, `ReleaseAudit.lean`, `ReleaseCheck.lean`, `ReleaseClaims.lean`, `Probe/*` are part of the release package. Are they producer-authored or audit? They came from the release copy created by the producer; D6AuditReport etc. are likely D6-era artifacts. Let me just list hits grouped.

Key question: does any hit indicate a real escape hatch in the *proved* content? E.g., `axiom negativeControl : False` in volume-ibp's AxiomAudit (documented, F5). And `native_decide` in a producer file would be a serious finding — the negative control uses native_decide in the *audit* directory (negcontrol), not in packages.

Let me summarize tokens per file (top hits).
dsh: reasoning:
Findings:
- `partial` hits are all in `A3UnusedHyp.lean`/`A3UnusedHypFull.lean` — the auditor's own generated files. Let me check what "partial" is there: likely `partial` in a docstring? No, comments are stripped. Maybe `Lean.Meta.mkAppM`... Actually the regex matches `partial def|theorem|lemma|instance`. Let me check the lines. It might be in meta code: `partial def collect` in the generated unused-hypothesis script. That's auditor code, not producer. Fine, but I should verify it's the auditor's meta-program (not used in proofs). Since these files are compiled probes, `partial def` in a meta program is harmless for soundness (partial defs are not used in proofs; they compile). But is `partial` allowed by the task rules? The rules ban sorry/axiom/admit/unsafe/native_decide/proof_wanted — `partial` is not banned but can be an escape hatch in `def`s that produce proofs? A `partial def` cannot produce a proof of an arbitrary Prop soundly (it would be `unsafe`? Actually `partial def` in Lean 4 requires the function to be in a `noncomputable`/`unsafe`? No: `partial def` is allowed for arbitrary types using `partial` fixpoint and compiles to a possibly non-terminating function; it is not `unsafe` per se but its reduction is opaque (`implemented_by`?). For proofs, `partial` definitions used at the proof level could introduce unsoundness? Lean's kernel treats `partial` definitions as opaque constants with `implemented_by`; they are not unfolded during kernel checking, so they can't prove False unless their type is inhabited... Actually a `partial def loop : False := loop` would be rejected? Lean 4 allows `partial def` only for types with `Inhabited`? Let me check: `partial def` requires the type to be inhabited? In Lean 4, `partial def f : α := ...` is allowed and uses `unsafe` internally? I recall `partial` definitions are compiled with `unsafe` and are marked as `implemented_by`; they are allowed in noncomputable code. Kernel-wise, `partial` creates a definition whose value is opaque (`_sorry`? no). Hmm, in Lean 4 `partial def` uses `partial` fixpoint and is not sound for proofs in the sense that `partial def f : False := f` — I believe Lean rejects `partial` on Prop? Not sure. Regardless, it's in the auditor's own probe file, not in the producer's audited content, so it doesn't affect the verdict on the cards. But I should inspect to be sure it's not in the audited package's proofs.

- `unsafe` hits: `D6AuditReport.lean`, `ReleaseAudit.lean`, `Audit/D12/D12Audit.lean` — these are audit/report files in the release package (likely `unsafe` used for `#eval` printing). Are these compiled into the package and do they affect axiom cones? The `#print axioms` probe covers the card's `proved_declarations`, and the full-namespace audit covers `Poincare.D12` roots. `unsafe` definitions cannot be used in theorem statements/proofs of safe definitions; if used only for IO/#eval, harmless. But note: `unsafe` code could still be in the package. The rules ban `unsafe`. Let me inspect those lines to see context and classify (auditor-file vs producer proof content).

- `axiom` hit: `Poincare/D12/VolumeIBP/Audit.lean` — the documented negative control `axiom negativeControl : False` (F5). Confirmed as expected.

Let me look at the actual lines for `unsafe` and `partial`.
dsh: reasoning:
Everything checks out:
- `partial` hits: auditor's own metaprogram (`A3UnusedHyp*.lean`) — `partial def` used for structural recursion over `Expr`; not producer proof content. Should be noted as audit-tool code (not in any axiom cone; metaprogram only). Actually, is `partial` in the auditor's file acceptable under my own constraints ("No sorry/axiom/admit/unsafe/native_decide/proof_wanted")? `partial` is not in the banned list, and it's only in a metaprogram used to inspect proof terms, not to prove anything. But to be safe I should note it and possibly avoid it in future. It doesn't produce any theorem. I'll document this as an audit-tool detail.
- `unsafe` hits: all inside *audit report* modules that scan other declarations (the word `unsafe` appears in strings/patterns `.«unsafe»` and in a local variable name `unsafeDefs`, plus docstring text — wait, comments are stripped, but `let mut unsafeDefs` is an identifier containing "unsafe", and `.«unsafe»` is a Name literal, and `logInfo m!"...unsafe..."` is a string literal that should have been stripped... my string stripping covers `"` so the m!"..." string was stripped. The hits are identifiers/Name literals → false positives from the token `\bunsafe\b`. Only `ReleaseAudit.lean`'s `.«unsafe»` pattern usage. No actual `unsafe def`. Let me verify there's no real `unsafe def` anywhere: grep for `unsafe def|unsafe theorem|unsafe instance|unsafe abbrev`. Let me do a targeted grep over producer files.
- `axiom` hit: the documented negative control (F5), never used.

Let me run a precise check for actual escape-hatch declarations: `unsafe def`, `opaque`, `axiom`, `partial def` outside A3 files, `@[extern]`, `implemented_by`. My scan already covers `opaque`, `extern`, `implemented_by`, `partial def` — zero hits for opaque/extern/implemented_by, and partial only in A3 files. And `unsafe` regex `\bunsafe\b` was too loose; refine to `^\s*unsafe\s+(def|theorem|lemma|instance|abbrev)`.

Let me update the scan to use precise patterns for unsafe and partial, then rerun and record. I'll make `unsafe` regex `^\s*(private\s+|protected\s+)?unsafe\s+(def|theorem|lemma|instance|abbrev|axiom|opaque)\b` and `partial` similar (already). Then hits should be zero except the axiom negative control.

Let me patch and rerun.
dsh: reasoning:
Now 57 hits, all `partial` in the auditor's A3 files plus the 1 axiom negative control. Let me verify by summarizing again, and also produce a filtered "producer-only" summary (excluding A3*.lean and the documented negative control).

Let me update the report to include a `producer_scope` classification: files whose names don't start with A3 and aren't the negative control. Actually the D6AuditReport/ReleaseAudit are producer-era audit reports; hits there were false positives from the loose `unsafe` pattern, now gone. Let me re-summarize.
dsh: reasoning:
Clean: across all 8 packages, the only non-audit-tool hit is the documented negative control `axiom negativeControl : False` in volume-ibp's Audit.lean (line 36), which is never used in any cone (verified by the full-namespace audit and previously by the producer's own audit). Zero `sorry`, `admit`, `native_decide`, `opaque`, `extern`, `implemented_by`, `unsafe def`, `proof_wanted` in producer files. The 56 `partial` hits are all in the auditor's own metaprogram `A3UnusedHyp*.lean` (structural recursion over `Expr`), which proves nothing.

Wait — one important nuance: `A3UnusedHyp.lean` and `A3UnusedHypFull.lean` are *inside* the staged packages, so when we run `lake build`, are they compiled as part of the package? If the lakefile globs all .lean files, they'd be built. They contain `partial def`s in the `Lean` meta namespace. That's auditor code, not producer. Fine.

However: the full-namespace audit (1103 declarations under Poincare.D12 roots) covers only `Poincare.D12` roots, right? So Auditor A3 files aren't included in the cone audit. But the `A3Probe.lean` covers the card's declared names. The A3UnusedHyp files prove nothing (they're metaprograms), so no risk.

Now let me check the cold rebuild progress and then run the self-audit + update results.

Let me also think about whether to do anything else new:
1. A **full-namespace axiom audit** is re-run each sweep: 1103 declarations PASS. ✓
2. **Vacuity screen** — round 3. Could I add a new adversarial check for round 6? Idea: **"definitional-triviality screen"**: for each card's main theorems, check whether the conclusion is provable by `rfl`/`trivial`/`simp` alone with hypotheses — i.e., detect theorems whose proof could be closed by `trivial`. The round-3 screen had T1–T11 criteria. A cheap new check: for each declaration in the probe list, attempt `example : <type> := by trivial`? That's not directly possible without re-elaborating types.

   Alternative new check: **hypothesis-satisfiability at a concrete model** for each card's headline theorems — i.e., non-vacuity. Round 5 reviewed this for several cards; the cards have Examples/witnesses. Might be overkill.

3. A **new check on the full-namespace audit coverage**: ensure the 1103 count matches the actual number of `Poincare.D12.*` constants and none are excluded. Round 5 did hash_coverage. Could re-run hash_coverage.py (round-5 version) — it records audited-copy hashes. Let me re-run it for round 6 to refresh.

4. **Update results .md/.json** — the main deliverable. I'll add a round-6 section (§14), refresh the header status/elapsed, update the JSON's round fields, proved_declarations (add the 5 new A3R6 declarations? The JSON's `proved_declarations` is a dict per card of card-declared names; the A3-D2D3 key has audit declarations. I could add a new key `A3-round6` with the new consumer's declarations. Better: add a `round6` object in the JSON, and add the 5 R6 declarations under a new key in `proved_declarations`.)

Also required deliverables fields: proved_declarations, expanded_hypotheses, semantic_class, exact_blockers_closed, remaining_blockers, source_hashes, compile_evidence, axiom_evidence, next_dependency_requests, elapsed time. These exist in the JSON per card. I'll update:
- `status`, `generated_at`, `verdict`, `elapsed`.
- `round6` evidence block.
- `compile_evidence`: add round-6 sweep + cold rebuilds.
- `axiom_evidence`: add round-6 full-namespace audit results.
- `exact_blockers_closed`: confirm again (CC + SR) with the new consumer.
- `remaining_blockers`: update CC (F1 inventory etc.), keep.
- `source_hashes`: re-verify hashes (self-audit) and refresh.
- `next_dependency_requests`: keep + maybe add the two missing cards' transport request.

Let me first check cold rebuild status, run hash verification (inventory.py re-run over producer trees + verify_own_hashes.py), then write the results updates.

Note the results JSON is 250KB — I must edit it programmatically. Let me inspect its top-level structure more completely first (I saw the key list). Then I'll write a Python updater.

Also need to keep results .md current — I'll append a round-6 section and update header lines via edit tool.

Let me check cold progress + run inventory hash verification.
dsh: reasoning:
Hash provenance re-verified round 6: all recorded hashes still ok (12+11+8+66+8+6+8 = 119 ok, 0 bad). Missing cards still missing.

Now let me run the self-audit (`verify_own_hashes.py`) — but note it verifies hashes recorded in the *results JSON*, which I haven't updated yet for round 6. I'll run it after updating the JSON.

Cold rebuilds continue (2 done + semantic-ledger + comparison-geodesics running).

Let me now write the results .md round-6 section and update the JSON. First, I need to see the results JSON structure in more detail for the fields I'll update. Let me dump the relevant top-level keys: status, verdict, compile_evidence, axiom_evidence, exact_blockers_closed, remaining_blockers, method, source_hashes, etc.

Let me inspect the JSON keys and a sample of compile_evidence/axiom_evidence.
dsh: reasoning:
Good. Round 5 already recorded the cold rebuild for connection-curvature. My round-6 cold rebuilds cover 5 more cards. I'll add a `reverification_round6` block + `cold_rebuilds_round6` + `new_independent_closure_consumers_round6` + `forbidden_scan_round6` + `semantic_review_round6` + `missing_cards_recheck_round6`, update `status`, `verdict`, `generated_at`, `elapsed_hours`, `cumulative_task_hours`, `self_audit_round6`, and `deliverables_sha256`-like info (the JSON has `round5_artifacts`).

Also `proved_declarations` should get the new R6 declarations under a new key (e.g., "A3-round6-consumer-connection-curvature"). Let me add a top-level key `a3_round6_declarations` with the 5 new names and their cones.

Before writing, let me wait for cold rebuilds to finish so I can include full evidence. Meanwhile, write the .md round-6 section.

For the .md, I'll add:
- Header status update: rounds 1–6 now (six sweeps), round 6 evidence, elapsed.
- New section §14 "Round-6 re-verification (invocation 5)" with subsections:
  - 14.1 Round-6 sweep — sixth consecutive byte-identical sweep (342 cones, 1103 decls PASS, kind screen identical).
  - 14.2 Cold rebuilds for the remaining 5 cards (from sources, no .lake/build).
  - 14.3 Sixth independent closure consumer: non-bi-invariant model (CC).
  - 14.4 Deep statement reviews round 6: spectral-sobolev REVIEWED-CORRECT; connection-curvature REVIEWED-CORRECT.
  - 14.5 Wider forbidden-token scan (round 6).
  - 14.6 Missing-card re-check (round 6) — unchanged.
  - 14.7 A3 status after round 6.
- Also update §2 table? Not necessary; add a note in header and the new section.
- Update §13.5 references? Fine.

Also update the top "Elapsed" line.

Let me draft the markdown section after the cold rebuilds finish. Meanwhile, let me check the .md tail (lines 989-1000) to see where to append.
dsh: reasoning:
Cold rebuild of comparison-geodesics is still going (started 14:46:26, it's 14:47:36). Fine.

Let me use the wait time to look at the remaining items:
1. The `expanded_hypotheses` and `semantic_class` fields in the results JSON need to be current — they are per-card from the previous rounds. I should verify they're still accurate and add round-6 review results. I'll add to `semantic_review_round6`.
2. Check the round-5 `hash_coverage.py` to re-run for round 6 (provenance coverage; F15). Let me run it and record if it's cheap.

Let me check hash_coverage.py usage.
dsh: reasoning:
Fine — hash_coverage_round5.json exists from round 5. For round 6, the situation is unchanged (producer files frozen). I can re-run it with a tweak to output round6 name, but it's essentially static; the round-6 sweep verified card freeze (hash inventory ok=119, bad=0). I'll note the coverage result from round 5 as still valid because card files are unchanged (verified by card_freeze_round4 + inventory round 6).

Actually, let me verify the producer card files are still frozen (mtimes/hashes) — `card_freeze_round4.json` recorded hashes; let me re-verify those. Let me check what's inside and verify.

Meanwhile, let me also prepare the "proved_declarations" addition for the JSON with the 5 R6 declarations.

Let me now write the results .md §14 draft. I'll do it with the edit tool appending after line 1000.

Content:

```
## 14. Round-6 re-verification and new evidence (invocation 5)

### 14.1 Round-6 sweep — sixth consecutive byte-identical sweep

`audit360/run_round6.sh` → `audit360/logs-round6/`, summarised in
`audit360/round6_summary.json` (sha256 `c4004cfd…`). All seven available cards:
`lake build` rc 0, `A3Probe.lean` rc 0, `A3FullAudit.lean` rc 0 PASS,
`A3KindAudit.lean` rc 0; **342 cones**, **1103 declarations PASS**; the
build/probe/full-audit outputs are byte-identical to round 5 and the kind audit
output is byte-identical to the round-5 `logs-kind/` screen. Negative control:
`sorryAx` flagged for `sorry`, and the private `native_decide` axiom flagged.
Semantic-ledger snapshot: build rc 0 + probe rc 0. A3 D2/D3: build rc 0,
`A3D2D3.lean` rc 0, `A3D2D3Round3.lean` rc 0.

Two rc-1 probes are expected and documented (not regressions):
- `A3Extra/D12RealModuleProbe.lean` in the D12-semantic-ledger release package
  (module `Poincare.D7.HeatKernel.Basic` is not in that package; also rc 1 in
  round 5); the valid replay is the snapshot package probe (rc 0).
- `A3ExtraR3/TrivialCheck.lean` in D12-geometric-compactness: the file *is* the
  `rfl` attempt on `diam_rep_of_toGHSpace`, so its failure is the recorded
  evidence that that statement is not definitionally trivial (round-3 finding).

### 14.2 Cold rebuilds of the five remaining cards

Round 1 cold-rebuilt only connection-curvature and surgery-recognition. Round 6
adds cold rebuilds (fresh copy with **no `.lake/build`**, shared pinned mathlib
package cache only) of the other five cards, `audit360/run_cold_round6.sh` →
`audit360/logs-cold6/`:

| card | cold `lake build` | cold probe | wall |
...

### 14.3 Sixth independent closure consumer — non-bi-invariant model (CC)

...

### 14.4 Deep statement reviews (round 6)

...
### 14.5 Wider forbidden-token scan (round 6)
...
### 14.6 Missing-card re-check (round 6)
...
### 14.7 A3 status after round 6
...
```

Let me fill in the cold rebuild numbers after they finish. Let me check progress and in the meantime verify the card freeze.
dsh: reasoning:
Cold rebuilds progressing well (about 80s each). Comparison-geodesics done. Geometric-compactness running, then done.

Let me write a round-6 card freeze check script to re-verify the producer card files are unchanged (using card_freeze_round4.json hashes). Then I'll know the verdicts are not stale.

Let me write and run it now.
dsh: reasoning:
All 14 producer card files (7 cards × md/json) are unchanged since the round-4 freeze → round-6 verdicts are not stale.

Now let me check the cold rebuilds finish, then write the results.

While waiting, let me also do a final missing-card transport re-check with a couple of new angles:
- Check whether any new worktrees appeared under /data3/.../worktrees (e.g., D12-tensor-maximum-bochner created since).
- Check the queue status of those cards (maybe changed to verified/complete and artifacts got published somewhere).
- Check for any `.tar`/`.zip` archives or shared "inbox" directories.

Let me do that now.
dsh: reasoning:
There's an `inbox` directory! Let me check it for artifacts from the 360 hosts. Also `bin/dispatch360.py` and `bin/resume_transport.py` may reveal the transport mechanism — maybe there's a way to pull artifacts.

Let me look at the inbox and the transport scripts.
dsh: reasoning:
The inbox contains only plan imports. The transport route is via `dispatch_loop.py` over some channel (probably SSH to remote hosts). Let me check dispatch360.py / dispatch_loop.py for the transport mechanism (host addresses) — maybe there's an SSH route that isn't 127.0.0.1:10022. Previous rounds concluded no route. Let me check quickly for the host config.
dsh: reasoning:
No SSH route in dispatch_loop; the dispatch is local (each host runs its own dispatcher with DSH_HOST env). The remote hosts 360-1/360-2 are separate machines with their own filesystems. Conclusion unchanged: no transport route from this host.

There's `relay_push.sh` — let me check it (it might push results to a relay). If there's a relay that could pull, maybe artifacts are available. Let me look.
dsh: reasoning:
This confirms the transport mechanism: the 360-X machines push via a reverse tunnel to 127.0.0.1:10022 on ophis-gpu, using rsync/ssh with `guoshaoyang@127.0.0.1`. Currently port 10022 is refused (verified multiple times), so no push is happening. The two cards have never been pushed (no state dirs, no PUSHED markers, no worktrees). Nothing more can be done from this host — the blocker is genuinely external (remote hosts haven't delivered).

One thing to check: maybe a newer push happened since the last check into the `state/` dir or `worktrees/` — we already listed worktrees (no D12-tensor-maximum-bochner or D12-triangulation-topology). Confirmed.

Now let me finish the cold rebuilds and then write results. Let me check cold progress.
dsh: reasoning:
Almost done (geometric-compactness running). Let me prepare the markdown section while waiting, then insert it once the cold logs are complete.

Actually, I can write the section now and fill in the cold table afterwards. Let me wait ~1 min and check again. In the meantime, let me collect the exact round-6 evidence hashes I'll need:

- round6_summary.json sha256
- forbidden_scan_round6.json sha256
- card_freeze_round6.json sha256
- cold logs per card (rc + log hashes)
- R6 consumer sha256 + log sha

Let me compute hashes for these artifacts once cold is done.

Also, for the JSON update, I want the per-card round-6 the "cold_rebuild" evidence merged into `compile_evidence`. Let me write a Python updater that:
1. Loads results JSON.
2. Sets `generated_at`, `status` (TASK_BLOCKED), `elapsed_hours` (this invocation), `cumulative_task_hours`.
3. Updates `verdict` string (append round-6 summary).
4. Adds `reverification_round6` dict (like round5).
5. Adds `cold_rebuilds_round6` dict per card.
6. Adds `a3_round6_declarations` (the 5 new names + cones + file hash).
7. Adds `forbidden_scan_round6`, `card_freeze_round6`, `missing_cards_recheck_round6`, `semantic_review_round6`.
8. Merges cold rebuild rc into `compile_evidence[card]['cold_rebuild_no_lake_cache_round6']`.
9. Updates `exact_blockers_closed` for CC with the new consumer reference (it's a dict of lists per card; CC has the D12 closure entry — I'll append a note to the A3-D2D3 or add a top-level `exact_blockers_closed_audit` update).
10. Keeps everything else.

Then run verify_own_hashes.py (which verifies recorded hashes in the JSON against disk — it may need updating for the new artifacts if it only checks known keys; it has a fixed list of trees and result-JSON-derived maps; new top-level keys like `round6_artifacts` may not be checked unless the script walks them. Let me read the rest of verify_own_hashes.py to see what it covers, and extend it for round-6 artifacts).

Let me read verify_own_hashes.py fully.
dsh: reasoning:
The verifier covers source hashes, audited-copy hashes, round3 artifacts, round5 artifacts and consumers. I'll extend it for round-6 artifacts: `round6_artifacts` dict of script/artifact names → hashes, plus the R6 consumer.

Note: `a.missing` count includes "unresolved"; the checkpoint said 758 verified 0 mismatches 0 unresolved in round 5. But we saw `A3Extra/D12RealModuleProbe.lean` etc. are part of audited_copy hashes... fine, they exist.

Now let me check the cold rebuild done, then compute hashes and write the update script.

Let me check the cold status.
dsh: reasoning:
Still running. Let me wait a little while doing something useful: review the D12-spectral-sobolev card's `AxiomAudit.lean` and confirm the 36 names are the card's declared ones (already audited). Or check the semantic-ledger card's ledger notes that were flagged F8/F9 (unchanged).

Actually, let me spend the time on an additional new verification that adds value: **an independent check that the round-6 `A3Probe.lean` names are exactly the card's declared names, with the F1 over-qualification mapping** — i.e., re-run the F1 validation (it's part of round 6 extras: `extraR3.F1Validation` rc 0 ✓).

Another idea: **verify the `round6` sweep used the exact same staged bytes as round 5** — i.e., the staged package sources are byte-identical between rounds. Since round 5 and round 6 both build the same `pkgs/` directories (no re-staging in between), and probe logs are byte-identical (they include the probe sha256 in the build log header — but headers are skipped in the comparison...). The build log's first lines include `### probe sha256`, which my strip ignores. Let me confirm the recorded probe hashes match across rounds: round 5 build log header vs round 6. Let me check that the A3Probe.lean hashes are unchanged and record them in the results.

Let me extract from logs-round5 and logs-round6 build logs the `### probe sha256`/`### fullaudit sha256`/`### kindaudit sha256` header values and compare.
dsh: reasoning:
Probe and fullaudit generator hashes identical between round 5 and 6 for all cards → the same audited declaration lists.

Let me check cold rebuild status again (should be done soon), then compile everything into the results.
dsh: reasoning:
Geometric-compactness cold build has been running since 14:47:45 (now 14:49:04) — about 80s, normal. Let me check the process; maybe it's slower because the package has more files. Let me wait a bit more while doing something else.

Let me prepare the results-JSON update script now (I'll run it after cold finishes). And draft the markdown section.

Actually, let me first check on the cold job's process state.
dsh: reasoning:
The cold job has finished (no processes). Let me check the driver log — it probably completed the last card.
dsh: reasoning:
All 5 cold rebuilds: build rc 0, probe rc 0. Excellent — now all 7 cards have been cold-rebuilt from source at least once (2 in round 1, 5 in round 6).

Let me collect the per-card cold probe declaration counts and log hashes for the results, then write the JSON + MD updates.

Let me gather:
- cold build rc, cold probe rc, probe cone counts from cold probe logs
- wall times (from driver log timestamps)
- log sha256s

Let me compute a `cold_rebuilds_round6.json` artifact with all details, then use it in the results.
dsh: reasoning:
All cold rebuilds clean: 78+36+7+33+39 = 193 declarations cold-probed (plus the 119+30 cold from round 1 = 342 total — every card-declared declaration has now been probed in a cold rebuild).

Now let me write the results updates. First the JSON updater, then the MD section.

JSON updater plan (script `audit360/update_results_round6.py`):
- Load results JSON.
- Update generated_at, status (TASK_BLOCKED), elapsed_hours (this invocation ≈ 0.35), cumulative_task_hours ≈ 3.1.
- verdict: append round-6 sentence.
- Add:
  - `reverification_round6`: sweep, summary sha, cones, decls, identical to round5, kind identical, negative control, snapshot, a3d2d3, probe/fullaudit generator hashes.
  - `cold_rebuilds_round6`: read cold_rebuilds_round6.json; merge per-card into `compile_evidence[card]['cold_rebuild_no_lake_cache_round6']`.
  - `new_independent_closure_consumers_round6`: CC entry with file + sha + declarations + cones.
  - `semantic_review_round6`: spectral-sobolev + connection-curvature verdicts with details.
  - `forbidden_scan_round6`: summary (tokens, total hits, non-A3 hits = the documented negative control; partial hits are audit metaprogram).
  - `card_freeze_round6`: all 14 files unchanged.
  - `missing_cards_recheck_round6`: same conclusion + transport route detail (relay_push.sh requires reverse tunnel 127.0.0.1:10022, refused).
  - `self_audit_round6` (filled after running verify script).
  - `round6_artifacts`: name → sha map (for the self-audit).
- Add `a3_round6_declarations` listing the 5 new theorem names with cone.
- Keep everything else.

Then extend `verify_own_hashes.py` to check `round6_artifacts` and `new_independent_closure_consumers_round6`, run it, and record the result back into the JSON (`self_audit_round6`). Since the verifier hashes the JSON? No — it verifies hashes recorded in the JSON, not the JSON itself. But `round5_artifacts` included the results files? Let me check what round5_artifacts contains — it lists artifact names with hashes. If it includes the results md/json, then updating results would invalidate those. Let me check.
dsh: reasoning:
Important: `round5_artifacts` includes `verify_own_hashes.py` with a hash. If I edit `verify_own_hashes.py` to add round-6 coverage, that recorded hash becomes stale — the same trap as round 5 (self-referential). Solution: after editing the verifier, update the recorded hash in `round5_artifacts` (or add the new hash under round6_artifacts and update round5_artifacts entry). Round 5 handled this by "refreshing the hash in both recorded locations".

Also note `round5_artifacts` has `forbidden_scan_round5.json` etc. — unchanged.

Plan:
1. Write `update_results_round6.py` that adds the round-6 blocks, including `round6_artifacts` with hashes of: run_round6.sh, round6_summary.py, round6_summary.json, cold_rebuilds_round6.py, cold_rebuilds_round6.json, run_cold_round6.sh, forbidden_scan6.py, forbidden_scan_round6.json, card_freeze_round6.py, card_freeze_round6.json, ClosureUseNoninvariant.lean (consumer, also under consumers_round6), update_results_round6.py, verify_own_hashes.py (to be refreshed after editing).
2. Extend `verify_own_hashes.py` to check `round6_artifacts` + `new_independent_closure_consumers_round6` + also verify `round5_artifacts['verify_own_hashes.py']` after refresh.
3. Run the updater, then the verifier, then write `self_audit_round6` back into the JSON (which doesn't change any hashed artifact since the JSON isn't self-hashed... wait, is the results JSON hashed in round3/5 artifacts? `round3_artifacts`/`round5_artifacts` don't include the results files. Good. But hold on — the task deliverable requires the .md and .json; the previous rounds' `deliverables_sha256` in checkpoint.json include them, which I'll refresh at the end.)

One subtlety: the self-audit script hashes `verify_own_hashes.py` itself as recorded in `round5_artifacts`. If I edit it, I must update that recorded value to the new hash before running. But then the JSON content changes, which is fine (not self-hashed). The new script computes the hash of the file on disk = new hash, and compares against the recorded value = new hash → OK.

Order of operations:
a. Edit verify_own_hashes.py to add round-6 coverage (reading `round6_artifacts` and `new_independent_closure_consumers_round6`).
b. Compute its new sha256.
c. Run update_results_round6.py, which:
   - updates round5_artifacts['verify_own_hashes.py'] = new hash
   - adds round6_artifacts (including verify_own_hashes.py = new hash)
   - adds other round-6 blocks.
d. Run verify_own_hashes.py → expect rc 0.
e. Write self_audit_round6 into JSON.
f. Update checkpoint + results md.

Note: `update_results_round6.py` itself is hashed in round6_artifacts — its own hash must be computed before it runs (it doesn't modify itself). Fine.

Let me now write the verifier extension.
dsh: reasoning:
Now write the update_results_round6.py script. It must be careful and idempotent-ish. Let me design it:

```python
import hashlib, json, os, datetime

WT = worktree root
RES = longrun/results/D13-cross-audit-360-cards.json
d = json.load(...)

def sha(p): ...

# 1. header
d['generated_at'] = now
d['status'] = 'TASK_BLOCKED'
d['elapsed_hours'] = 0.37
d['cumulative_task_hours'] = 3.1
d['verdict'] = (old first paragraph? better: compose new)
```

The verdict is a long string. I'll append a new paragraph rather than replace: `d['verdict'] = d['verdict'].rstrip() + ' Round 6 (invocation 5): ...'`. But the JSON I need to keep readable. I'll set a fresh verdict string that summarizes all rounds (with round 6 first). Let me write:

"Round 6 (invocation 5): 7/9 cards re-verified a fifth consecutive time with byte-identical outputs (342 cones, 1103 declarations PASS; build/probe/full-audit identical to round 5, kind screen identical to the round-5 screen); ALL SEVEN available cards now additionally cold-rebuilt from source with no .lake/build (round 1: connection-curvature, surgery-recognition; round 6: the other five) with probes rc 0; sixth independent closure consumer added for the connection-curvature closure at a concrete NON-bi-invariant 2D model (where the Milnor connection differs from the mean connection and the mean connection is not metric-compatible), plus a new uniqueness corollary; producer card files still frozen (14/14 unchanged); wider forbidden-token scan clean (only the documented volume-ibp negative-control axiom; no sorry/admit/native_decide/opaque/extern/implemented_by/unsafe def anywhere in producer files); deep reviews of spectral-sobolev and connection-curvature REVIEWED-CORRECT. 2/9 cards remain source-absent with no transport route (reverse tunnel 127.0.0.1:10022 refused; relay_push.sh cannot deliver) -> milestone still blocked."

Then add blocks. Let me write the script.

For `a3_round6_declarations`: names + cones:
A3R6.a3_not_bracketInvariant, a3_noninvariant_leviCivita, a3_milnor_ne_mean, a3_mean_not_metricCompatible, a3_leviCivita_unique — all cones {propext, Classical.choice, Quot.sound}.

For `reverification_round6`, read round6_summary.json and include key fields.

For `cold_rebuilds_round6`, read the artifact and merge `compile_evidence[card]['cold_rebuild_no_lake_cache_round6']`.

For `semantic_review_round6`: structured text.

For `forbidden_scan_round6`: summary from the JSON.

For `round6_artifacts`: hashes of the listed files.

Let me write it.
dsh: reasoning:
Wait: `update_results_round6.py` hashed `verify_own_hashes.py` while running — the hash is computed at runtime after I edited the verifier, so it's the current one. Good. But `update_results_round6.py` hashes *itself* too, which is fine (content unchanged during run).

Now run the self-audit verifier and record its result. Note the verifier will hash all recorded values including `round6_artifacts` — which includes `update_results_round6.py` (self-hash of the updater script, unchanged) and `verify_own_hashes.py` (current). Let's run.
dsh: reasoning:
As anticipated (the round-5 self-audit note described exactly this): a mismatch for `verify_own_hashes.py` because it is also listed in `round3_artifacts`/some other recorded location with the old hash. Let me find which recorded key still has `c5d3dcb9...`:
- `round5_artifacts` was refreshed by the updater.
- `round3_artifacts` doesn't include it (checked earlier: round3_artifacts has unused_hyp_screen.json, PhantomParams.lean, HypRemoval.lean, fulltype_screen.json/py, TrivialCheck.lean, A3AllTypesD6.lean...). Hmm, actually the verifier walks `round3_artifacts` by *name* over the whole worktree — if `verify_own_hashes.py` is listed there... let me check. The mismatch label is `('verify_own_hashes.py', ...)` without a prefix, which comes from the `round3_artifacts`/`round5_artifacts` loop (`a.missing.append((name,...))` / `a.check(name, hit, h)`). Round-5 artifacts would label "round5:verify_own_hashes.py" (I added prefix "round5:"). Round-3 loop uses `a.check(name, hit, h)` → label `verify_own_hashes.py`. So `round3_artifacts` must contain it. Let me check and refresh it.

Actually from the earlier dump, round3_artifacts was truncated; it may include `verify_own_hashes.py` too. Let me look and update both recorded locations.
dsh: reasoning:
Yes — `round3_artifacts['verify_own_hashes.py']` is stale (from round 3). The verifier walks round3 artifacts and checks each by name; it found the round-3 recorded hash and mismatched. This is exactly the self-referential drift the round-5 note described. Fix: set `round3_artifacts['verify_own_hashes.py']` to the current hash too (it's the same file, so all three records should carry the current hash).

Let me update it and re-run the verifier. Then write `self_audit_round6` into the JSON.
dsh: reasoning:
Self-audit: 773 hashes verified, 0 mismatches, 0 unresolved, rc 0.

Now record `self_audit_round6` in the JSON. I'll write it via a small Python snippet (file already read/edited by me in this session).

Then write the markdown round-6 section.

Let me add self_audit_round6 with:
- script, rc 0, hashes_verified 773, mismatches 0, unresolved 0
- note about refreshing the round-3 recorded verifier hash (the same self-reference issue as round 5).
- script_sha256.

Also add round6_artifacts note. Let me do it.
dsh: reasoning:
Wait: adding `self_audit_round6` to the JSON does not invalidate any recorded hash (the results JSON isn't self-hashed). ✓

But careful — the verifier checks `round6_artifacts` including `update_results_round6.py`; unchanged. ✓ And `round6_artifacts['verify_own_hashes.py']` = current hash ✓.

Now, the markdown. I'll append §14. Let me write it with the actual numbers:

Cold rebuild table:
| card | files | cache before | cold build rc | cold probe rc | probe decls | wall |
| D12-volume-ibp | 80 | absent | 0 | 0 | 78 | ~80 s |
| D12-spectral-sobolev | 76 | absent | 0 | 0 | 36 | ~80 s |
| D12-semantic-ledger | 73 | absent | 0 | 0 | 7 | ~81 s |
| D12-comparison-geodesics | 90 | absent | 0 | 0 | 33 | ~79 s |
| D12-geometric-compactness | 76 | absent | 0 | 0 | 39 | ~80 s |

Note the `files=80` counts include mathlib? No — the tar excluded `.lake`, so 80 .lean files in the staged package copy (including the shared Poincare/Longrun etc.). Fine.

Round-6 sweep table: all 7 cards rc 0, cones 119/78/36/7/33/39/30.

Now write the section. Also update the header lines (status/elapsed) and the §13.5 heading reference? I'll update the header `Status:` bullet and `Elapsed:` bullet via edit.

Let me first append the new section using the write... the file is 1000 lines; I'll use bash `cat >>` (allowed) or the edit tool with old_string = the last lines. The edit tool is preferred. The last line is:
"piece lists)."
Let me append after that.

Let me compose the text carefully.

Also should I add a note in §2's per-card table about round 6? I'll add a line under the table: "Round 6 re-ran the whole sweep a fifth time ... " Let me edit the paragraph after the table.

Let me do the header edits first.
dsh: reasoning:
Now add a note after the §2 table about round 6, and append §14. Let me find the exact text after the table: lines 76-83:

"**342 card-declared entries plus 2 snapshot-probe declarations** across the 7 available cards
were independently re-audited in this session; all cones are exactly
`{propext, Classical.choice, Quot.sound}`; zero violations. ..."

I'll add a sentence: "Round 6 re-ran the entire sweep a fifth time ..." Let me edit that paragraph.
dsh: reasoning:
Now append §14. Let me write the section content and append it to the file. I'll use the edit tool with the last line as anchor ("piece lists).") — I need to include enough context.

Let me draft the full §14 text.

---

## 14. Round-6 re-verification and new evidence (invocation 5)

### 14.1 Round-6 sweep — sixth consecutive byte-identical sweep

`audit360/run_round6.sh` → `audit360/logs-round6/`, summarised in
`audit360/round6_summary.json` (sha256 `c4004cfd660ea0d7…`):

| card | build rc | probe rc | full audit rc | kind rc | probe decls | full-audit decls | byte-identical to round 5 |
|---|---|---|---|---|---|---|---|
| D12-connection-curvature | 0 | 0 | 0 | 0 | 119 | 260 PASS | build/probe/full/kind ✔ |
...

All seven cards: **342 probe cones**, **1103 declarations PASS**, zero cone violations.
The `A3Probe.lean` / `A3FullAudit.lean` generator hashes are unchanged from round 5 for every
card (e.g. connection-curvature probe `f260ff8fe9b6…`, full audit `2ff5d905b816…`), so the
same declaration lists were audited. Negative control: `sorryAx` flagged for the `sorry`
declaration and the private `native_decide` axiom flagged; fail-closed predicate rc 0.
Semantic-ledger snapshot: build rc 0, out-of-package probe rc 0. A3 D2/D3: build rc 0,
`A3D2D3.lean` rc 0, `A3D2D3Round3.lean` rc 0.

Two non-zero probes are expected and documented (not regressions):
- ... (as above)

### 14.2 Cold rebuilds of the five remaining cards — every card now has a from-source cold build

(table)

All five: `confirmed-absent` `.lake/build` before the build, `lake build` rc 0, probe rc 0,
zero unapproved-axiom markers. Combined with round 1 (connection-curvature, surgery-recognition),
**all seven available cards have now been rebuilt cold from source** and their per-declaration
axiom probes re-run there: 119 + 78 + 36 + 7 + 33 + 39 + 30 = 342 declarations, all cones
`{propext, Classical.choice, Quot.sound}`. Artifact: `audit360/cold_rebuilds_round6.json`.

### 14.3 Sixth independent closure consumer — non-bi-invariant model (CC)

...describe the model, theorems, cones, and why it is adversarial: the historical BLOCKED docstring
claimed existence required invariance; here the metric is provably not invariant (1 ≠ 0) and the
closure still produces a Levi-Civita connection, while the mean connection is not one. Also the
uniqueness corollary is new (the card never states uniqueness of the Milnor connection as such).

### 14.4 Deep statement reviews (round 6)

spectral-sobolev: REVIEWED-CORRECT (details)
connection-curvature: REVIEWED-CORRECT (details, non-circularity of ricci_symm)

### 14.5 Wider forbidden-token scan (round 6)

tokens, files, hits; only the documented negative control; partial in audit metaprogram.

### 14.6 Producer-card freeze and self-audit (round 6)

14/14 unchanged; self-audit 773 hashes.

### 14.7 Missing-card re-check (round 6)

unchanged; transport evidence.

### 14.8 A3 status after round 6

unchanged; no new D2/D3 defect; new negative assurance.

---

Let me write it now. I'll be careful with the table content and accuracy of numbers:

Round-6 summary per card (from round6_summary.json):
- connection-curvature: build 0, probe 0, full 0, kind 0, probe cones 119, full decls 260 PASS, extras: ClosureUse 0, F1Validation 0, ClosureUseNoninvariant 0
- volume-ibp: 78, 159 PASS
- spectral-sobolev: 36, 68 PASS
- semantic-ledger: 7, 28 PASS
- comparison-geodesics: 33, 139 PASS
- geometric-compactness: 39, 71 PASS
- surgery-recognition: 30, 378 PASS
Sum full decls: 260+159+68+28+139+71+378 = 1103 ✓

Cold: 5 cards as above.

Let me now append. I'll use `edit` with old_string being the final lines:

"All three `#print axioms` lines are `{propext, Classical.choice, Quot.sound}`. This makes
the SR-5 closure confirmed by two structurally different consumers (empty and nonempty
piece lists)."

and new_string = that + "\n\n## 14. ...".
dsh: reasoning:
The results md is updated. Let me verify the section renders and the file ends properly. Then update the checkpoint with round-6 final state and the deliverable hashes.

Also, per the required deliverables in the .json: "proved_declarations, expanded_hypotheses, semantic_class, exact_blockers_closed, remaining_blockers, source_hashes, compile_evidence, axiom_evidence, next_dependency_requests, elapsed time". All exist. Let me make sure the round-6 additions didn't break anything, and that `exact_blockers_closed` for CC reflects the round-6 confirmation. The per-card `exact_blockers_closed` dict has CC list of 2 entries (the closure + maybe another). Round 5/6 evidence is recorded in `new_independent_closure_consumers_round6` and §6/§14.3. Good enough; but let me also append a note to `exact_blockers_closed['D12-connection-curvature']`? It's a list of dicts; I could add a `round6_confirmation` key to the first entry. Let me check the structure and add a small note.

Also `exact_blockers_closed_audit` top-level dict — let me see what it holds and update its round-6 status.

Let me inspect these.
dsh: reasoning:
I'll add the round-6 confirmation entries to both `exact_blockers_closed['D12-connection-curvature']` (a new dict) and `exact_blockers_closed_audit['D12-connection-curvature']` (append a round-6 evidence item). Let me do that via Python.

Also add SR-6? Round 6 didn't add a new SR consumer, so the SR confirmations stand as in rounds 3/5; I'll add a note that they were re-verified in the round-6 sweep (extras rc 0) and remain confirmed.

Then finalize:
- Checkpoint update with round6 final + deliverables hashes.
- Final check of results md tail.
- Then final verification pass: run the full sweep once more? Not needed — round 6 already done and byte-identical. But note that I modified files *inside the staged packages* (A3ExtraR6/ClosureUseNoninvariant.lean added after the sweep). Does that affect the package's build/probe outputs? Adding a new file doesn't change existing builds (unless the lakefile globs it into the build; `lake build` in the package might now include A3ExtraR6 if the default target globs all lean files. The next sweep would build it too. But we already compiled it standalone rc 0.)

Hmm — one consideration: `A3ExtraR6/ClosureUseNoninvariant.lean` is inside `pkgs/D12-connection-curvature/`. If a future `lake build` picks it up via the lakefile's glob, it will compile fine (rc 0 standalone). No issue.

Another consideration: does adding the file change the "audited_copy_sha256" recorded hashes? The recorded `audited_copy_sha256` maps specific files; the new file isn't in it, so no invalidation. But `hash_coverage` (round 5) checked coverage of D12-authored files — the new A3 file isn't D12-authored, so no impact.

Now let me add the closure notes and then update checkpoint.
dsh: reasoning:
Now let me update the checkpoint.json to the final round-6 state:
- status: "blocked" (milestone not fully checked)
- verdict: round-6 summary
- blocked_reason: keep (still accurate) + mention transport evidence
- next_steps: update with round-6 requests
- deliverables_sha256: refresh for results md/json + new artifacts
- round6 block: fill in cold rebuild results, self-audit, forbidden scan, etc.

Also the `cards` dict entries for the 2 missing cards remain.

Let me write the checkpoint update.

Note the checkpoint `elapsed_hours` should reflect this invocation (~0.5h so far, will grow). I'll set 0.6 and cumulative 3.35. At the end I'll refresh.

Let me write it now (and update again at the end if time).
dsh: reasoning:
Checkpoint updated. Note: I set status "in_progress" — I should set it to "blocked" at the end. Let me keep working a bit more; the invocation has plenty of time left (~4h limit, we're at ~25 min of work). But careful: the checkpoint delivers hashes of the results md/json which I just wrote; if I edit the results files again, those hashes go stale. I'll refresh at the end.

Now, is there anything else of real value to add? The task says the invocation is limited to 4 hours; I could wrap up now with a solid round 6, or add more. Given the instruction "Report a genuine mathematical blocker rather than claiming completion with a weaker theorem" and "End TASK_DONE only if the milestone is fully checked; otherwise TASK_BLOCKED", the milestone remains blocked by the two missing cards. The best remaining use of time:

1. **Double-check my new CC consumer doesn't contain any unsoundness** — the axioms show clean cones. Also ensure the file is included in the package's final state and the recorded hash is right.

2. **Verify the results .md content** for consistency (numbers correct). Let me re-read the new §14 for errors.

3. **Consider a final round-7 mini-sweep** after all my edits to ensure nothing broke: the only change to packages is the new R6 file. A final `lake build` + probe on connection-curvature would confirm the package still builds with the new file present (in case the lakefile globs it). Let me run `lake build` there once more and re-run the probe/fullaudit to be safe. Actually that would produce new logs; the round-6 recorded evidence remains valid. I can run a quick "round 6b" verification: `lake build` in CC package, then rerun A3Probe + fullaudit and compare to round-6 logs (should be identical). Let me do that as a final integration check.

4. **Add the missing-card transport request** to `next_dependency_requests` in the results JSON (the field is per-card; the top-level `next_dependency_requests` list exists). Let me check whether the top-level list mentions transport. It has 8 entries; let me add one about the missing cards.

5. Also maybe record the **upstream crosscheck** again? Round 3 did it; sources unchanged.

Let me do (3), (4), then finalize.

Actually, one more important verification: my new file uses `import Poincare.D12.ConnectionCurvature` which ensures the whole card compiles; fine.

Let me run the final CC package check.
dsh: reasoning:
After adding the R6 file, the CC package still builds rc 0, probe identical, fullaudit identical PASS. The build log differs (it now compiles the new A3ExtraR6 module, so extra lines), which is expected.

Wait — the build log DIFFERS: let me confirm it's only because of the new module being built (not an error). Let me diff briefly.
dsh: reasoning:
The only difference is the job counter `[k/3350]` → `[k/3766]` — because the package now includes the new A3ExtraR6 module in its build graph (lakefile globs all `*.lean`). Everything else identical. Probe and fullaudit identical, rc 0.

This is worth recording as a round-6b integration note: the new consumer is compiled as part of the package build too, and it doesn't perturb the audited declaration set (probe/fullaudit byte-identical).

Now add the transport request to `next_dependency_requests` (top-level list) and record the round-6b check. Then finalize the results md (add a line in §14.3 or 14.6 about the package-glob rebuild), update checkpoints/deliverable hashes.

Actually — adding the R6 file to the package means the *staged package* now differs from the producer release. That's fine (the audit package always contained audit-only A3 files). But the recorded `audited_copy_sha256` maps don't include it.

Let me add:
- `next_dependency_requests`: append transport request.
- `round6b_integration_check` block.

Then refresh results md with a sentence in §14.3, and finally update checkpoint deliverable hashes.
dsh: reasoning:
Now let me add a sentence about 6b to the results md §14.3 or 14.6, then do a final consistency read of the §14 section, then finalize.

Also I should double check that the results .md still has the required elements and that no numbers are inconsistent. Let me read the new section.

Also: the md is a deliverable; its hash is recorded in checkpoint. I'll refresh hashes at the end.

Let me add the 6b note to §14.3 (end).
dsh: reasoning:
Oops! I introduced a typo: "Leзvi-Civita" (Cyrillic з). Let me fix that immediately.
dsh: reasoning:
Only legitimate non-ASCII (ellipsis, é, ¹). Good.

Now finalize:
1. Refresh checkpoint deliverable hashes (results md/json changed after the last checkpoint write).
2. Set checkpoint status to "blocked" for the end of invocation? The task says end TASK_DONE only if fully checked; otherwise TASK_BLOCKED or checkpoint and TASK_IN_PROGRESS. Since my invocation is ending (I'll produce the final answer now-ish), the honest final status is TASK_BLOCKED (the milestone is not fully checked due to the 2 missing cards). I'll set checkpoint status "blocked" and reflect TASK_BLOCKED in the results files (already status TASK_BLOCKED in JSON; md header says TASK_BLOCKED ✓).

3. Run the self-audit one more time after the results JSON edits? The verifier only verifies hashes *recorded in* the JSON against files on disk; my last edits to the JSON (closure notes, next_dependency_requests, round6b) didn't invalidate recorded hashes (the md isn't hashed in the JSON). But the checkpoint hashes of the results md/json need refresh. Let me re-run the verifier quickly to confirm 773 still OK (cheap), then refresh checkpoint.

Wait — actually, one of the round-6 artifacts hashed in the JSON is `update_results_round6.py`, `round6_summary.json`, etc. — unchanged. And the md isn't recorded inside the JSON. So the verifier should still pass. Let me run it to be sure.

4. Do a final read of the checkpoints and results for correctness.

Let me also reconsider: should the invocation end now, or should I continue with more audit work? Time used: started ~14:39, now ~15:05 → 26 minutes. The 4-hour budget allows much more. The instructions emphasize "Continue from checkpoint.json; update checkpoint.json at least every 30 minutes and keep longrun/results/D13-cross-audit-360-cards.md current" and "Save a compile-checked checkpoint at least every 30 minutes". With so much budget left, ending after 30 minutes seems premature — but the work is genuinely complete for this round: the only blocker is external (missing cards).

However, the task says "This invocation is limited to four hours" — it doesn't require using all four. The goal is to maximize audited value. Is there more valuable adversarial work I can do?

Options for further rounds:
(a) More independent consumers for the SR closures (e.g., a 3-fold connected sum; or a different quotient model for the covering-space closure: apply `finiteFreeOrbit_isQuotientCoveringMap` to a free ℤ/3 action on 𝕊³? Constructing a free ℤ/3 action requires a model of S³ with a free action — the antipodal is ℤ/2. A ℤ/3 action on S³ exists (lens space L(3,1)) via rotation on ℂ² restricted to S³: (z₁,z₂) ↦ (ωz₁, ωz₂) — free because if ωz = z then z=0. Could be formalized using `Metric.sphere (0:ℂ×ℂ) 1` with the diagonal action — moderate effort. It would exercise the covering-space closure on a *non-free-antipodal* action, testing it at a different group. That's a genuinely new and strong consumer.
(b) More degenerate-model tests for other cards, e.g., volume-ibp's IBP identity on a concrete metric (Euclidean 1D) — the card has `chart_ibp_euclidean_one` example. Already.
(c) Comparison-geodesics: instantiate the Sturm comparison at a non-reflexive strict case (F4 noted the model witnesses are reflexive). A strict instance would strengthen non-vacuity: e.g., k₁ = 0, k₂ = 1 on [0, π] with u₁ = 1 (or t), u₂ = sin t; the Sturm alternative conclusion `(∃ c ∈ Ioo 0 π, u₁ c = 0) ∨ ∀ t ∈ Ioo 0 π, k₁ t = k₂ t` — with k₁ ≠ k₂, the first disjunct must hold, but u₁ = 1 has no zero... So the theorem requires hypotheses that fail. A strict satisfiable instance: u₁ = sin(t) with k₁ = 1, u₂ = sin(2t)? Hmm, the theorem's hypotheses include u₂'' + k₂u₂ = 0 and the *initial conditions* ordering. Constructing a strict non-reflexive witness would need care.
(d) A **new vacuity/triviality screen** different from round 3: e.g., check for each theorem whether its statement is *provable in the empty theory* — i.e., whether the theorem type is a tautology independent of the definitions. Hard to automate.
(e) **Verify the upstream cross-check claims** mechanically for a couple of items: e.g., confirm that `third_party/frenzymath/Poincare-Conjecture` commit bb91a091 and the files cited exist and contain the claimed theorems (DoCarmoLib Ch2 Levi-Civita etc.). Round 3 did `upstream_crosscheck.json`. Could spot-check one upstream Lean proof builds? Building upstream requires Lean v4.32.1 which we don't have (docs say that). Could verify the statement text matches the card's claims — a semantic check. Moderate value.
(f) **Independent re-derivation of a headline theorem from scratch** in the audit package, not using the card's proof: e.g., re-prove `chart1D_curvature_zero` independently? Or re-prove `milnorConnection_torsionFree` by a different route? That's genuine but time-consuming.
(g) Expand the audit to **D12-heat-domain-repair, D12-heat-semigroup-analysis, D12-parabolic-local-existence, D12-entropy-variation, D12-kappa-variational** — these are other D12 cards produced by the same lanes (360-1/360-2? no — they're local ophis worktrees). The task scope is the 9 listed cards. Not needed, but "360-produced D12 result cards" are the 9. The others are local (ophis) D12 tasks. Out of scope.

Given the objective and the explicit scope (9 cards; 7 audited; 2 missing), the milestone is blocked regardless of further work. The most valuable additions would be (a) or (f). But there's a risk/benefit: more code = more chance of errors, but also stronger verification.

I think a good use of remaining time is:
1. Add the ℤ/3 lens-space consumer for the covering-space closure (if feasible within ~30 min).
2. Alternatively, a 3-fold connected sum for SR-5 (simpler).

Actually, let me reconsider the audit value: the SR covering-space closure `finiteFreeOrbit_isQuotientCoveringMap` was reviewed as correct. A new consumer at a different free action would test generality. The antipodal action is ℤ/2; a ℤ/3 action on 𝕊³ ⊂ ℂ² is a classical lens space. In Lean, I'd need:
- The sphere 𝕊³ as a metric sphere in ℂ × ℂ (or Euclidean space ℝ⁴).
- The action of `ZMod 3` (or `Multiplicative (ZMod 3)`) by (z₁,z₂) ↦ (ζz₁, ζz₂) where ζ = exp(2πi/3).
- Prove it's a free continuous action.
- Apply `finiteFreeOrbit_isQuotientCoveringMap`.

That's a decent chunk of work, and the existing consumer already gives a nondegenerate instance. Perhaps a simpler high-value target: **use the CC uniqueness corollary to derive a new statement about curvature**: since every Levi-Civita connection is the Milnor one, for the non-bi-invariant model the curvature operator of *any* Levi-Civita connection equals that of the Milnor connection, hence the scalar curvature is the same for all. A new declaration:
```lean
theorem a3_curvature_independent_of_choice (m b) (d : LeviCivitaData m b) (X Y Z) :
    d.toCurvatureOperator X Y Z = (milnorLeviCivitaData m b).toCurvatureOperator X Y Z
```
This follows from `a3_leviCivita_unique` by congruence. It's a new corollary but a bit trivial.

Better: use `ricci_symm` + uniqueness to prove that for the non-bi-invariant model, the Ricci tensor is symmetric — but `ricci_symm` already gives that for *any* Levi-Civita connection.

Hmm. What about a genuinely new mathematical check: **compute the scalar curvature of the Milnor connection in the non-bi-invariant 2D model and show it's nonzero** — that would show the closure produces a nontrivial connection with nontrivial curvature (not a degenerate/flat object). This is a real computation: for V = ℝ² with bracket [X,Y] = c(X,Y)(e₁+e₂) and standard metric, compute ∇ and R.

Let me compute by hand. Let e₁=(1,0), e₂=(0,1), u = e₁+e₂. bracket(X,Y) = (X₁Y₂−X₂Y₁)u (using coordinates (x1,x2)).
Milnor: ∇_X Y = ½([X,Y] − (ad_X)^T Y − (ad_Y)^T X).
Metric transpose: ⟨(ad_X)^T Y, Z⟩ = ⟨Y, [X,Z]⟩.
Compute ad_{e₁}: [e₁,Y] = (1·Y₂ − 0·Y₁)u = Y₂ u. As a linear map: Y ↦ Y₂ u.
Its transpose T₁: ⟨T₁Y, Z⟩ = ⟨Y, [e₁,Z]⟩ = ⟨Y, Z₂ u⟩ = Z₂⟨Y,u⟩. So T₁Y = ⟨Y,u⟩ e₂? Because ⟨T₁Y, Z⟩ = Z₂ ⟨Y,u⟩ = ⟨⟨Y,u⟩e₂, Z⟩. Yes T₁Y = ⟨Y,u⟩ e₂ = (Y₁+Y₂)e₂.
Similarly ad_{e₂}: [e₂,Y] = (0·Y₂ − 1·Y₁)u = −Y₁ u. T₂: ⟨T₂Y,Z⟩ = ⟨Y,[e₂,Z]⟩ = ⟨Y, −Z₁u⟩ = −Z₁⟨Y,u⟩ ⇒ T₂Y = −(Y₁+Y₂)e₁.

At X = Y = e₁: [e₁,e₁]=0; T₁e₁ = (1+0)e₂ = e₂; T₁e₁ = e₂. ∇_{e₁}e₁ = ½(0 − e₂ − e₂) = −e₂.
∇_{e₁}e₂ = ½([e₁,e₂] − T₁e₂ − T₂e₁) = ½(u − (0+1)e₂ − (−(1+0))e₁) = ½(e₁+e₂ − e₂ + e₁) = ½(2e₁) = e₁.
∇_{e₂}e₁ = ½([e₂,e₁] − T₂e₁ − T₁e₂) = ½(−u − (−e₁) − e₂) = ½(−e₁−e₂+e₁−e₂) = −e₂.
∇_{e₂}e₂ = ½(0 − T₂e₂ − T₂e₂) = ½(0 − (−e₁) − (−e₁)) = e₁.

Curvature R(X,Y)Z = ∇_X∇_Y Z − ∇_Y∇_X Z − ∇_{[X,Y]} Z.
R(e₁,e₂)e₁ = ∇_{e₁}(∇_{e₂}e₁) − ∇_{e₂}(∇_{e₁}e₁) − ∇_{[e₁,e₂]}e₁
= ∇_{e₁}(−e₂) − ∇_{e₂}(−e₂) − ∇_{u}e₁.
Need ∇_X for general X: ∇_X Y = ½([X,Y] − (ad_X)^T Y − (ad_Y)^T X).
[X,Y] = (X₁Y₂−X₂Y₁)u.
(ad_X)^T Y: transpose of ad_X. Compute for general X = a e₁ + b e₂: ad_X = a ad_{e₁} + b ad_{e₂}; transpose = a T₁ + b T₂. So (ad_X)^T Y = a(Y₁+Y₂)e₂ − b(Y₁+Y₂)e₁ = (Y₁+Y₂)(a e₂ − b e₁).
(ad_Y)^T X = (X₁+X₂)(Y₁ e₂ − Y₂ e₁).

∇_{e₁}(−e₂) = −∇_{e₁}e₂ = −e₁.
∇_{e₂}(−e₂) = −∇_{e₂}e₂ = −e₁.
∇_{u}e₁ = ∇_{e₁}e₁ + ∇_{e₂}e₁ = −e₂ + (−e₂) = −2e₂.
Thus R(e₁,e₂)e₁ = −e₁ − (−e₁) − (−2e₂) = 2e₂.
So the curvature is nonzero. Then ricci(e₁,e₁) = ⟨R(e₁,e₁)e₁,e₁⟩+⟨R(e₂,e₁)e₁,e₂⟩ (sum over orthonormal frame; with the D7 definition ricci K X Y = tr(Z ↦ R(Z,X)Y) = Σᵢ ⟨R(fᵢ,X)Y, fᵢ⟩ for orthonormal f).
- R(e₁,e₁)e₁ = 0.
- R(e₂,e₁)e₁ = −R(e₁,e₂)e₁ = −2e₂.
ricci(e₁,e₁) = ⟨0,e₁⟩ + ⟨−2e₂,e₂⟩ = −2.
Scalar curvature = ricci(e₁,e₁)+ricci(e₂,e₂).
ricci(e₂,e₂): R(e₁,e₂)e₂ and R(e₂,e₂)e₂=0.
R(e₁,e₂)e₂ = ∇_{e₁}(∇_{e₂}e₂) − ∇_{e₂}(∇_{e₁}e₂) − ∇_{u}e₂
= ∇_{e₁}(e₁) − ∇_{e₂}(e₁) − (∇_{e₁}e₂ + ∇_{e₂}e₂)
= (−e₂) − (−e₂) − (e₁ + e₁) = −2e₁.
So ricci(e₂,e₂) = ⟨R(e₁,e₂)e₂, e₁⟩ + 0 = ⟨−2e₁,e₁⟩ = −2.
Scalar curvature = −4 ≠ 0. Nice — the curvature is nonzero, so the closure yields a genuinely curved connection on the non-invariant model.

This would be a strong new downstream computation: it proves the closure produces a connection with nonzero curvature in the non-invariant regime. I can formalize it using the card's `ricci_contraction_eq_sum_basis` and `scalarCurvature_eq_sum_ricci_basis` (F14 said scalarCurvature uses no metric fields — basis-only identity!). That's a nice use: compute `ricci` and `scalarCurvature` for the Milnor connection in my model, showing nonzero values, and use the card's frame-independence theorems.

But the computation in Lean requires: compute `milnorConnection metric2 lie2` on basis vectors, then compute the curvature operator values, then the ricci via the D7 `ricci` definition (which is a trace). This could take a while to formalize (the D7 definitions: `ricci K X Y = LinearMap.trace ℝ V (endoRicci K X Y)`; `endoRicci` from CurvatureAlgebra). Computing traces concretely requires `trace_eq_sum_diag` with the standard basis and evaluating `m.basis.repr` — doable but fiddly.

Alternative: prove non-vanishing curvature via a simpler route: exhibit X,Y,Z with `d.toCurvatureOperator X Y Z ≠ 0`? For the Milnor connection:
R(e₁,e₂)e₁ = 2e₂ ≠ 0 (by my computation). To prove that in Lean, I need to evaluate `milnorConnection` on basis vectors and then `toCurvatureOperator_apply`. That's a straightforward `simp [milnorConnection_apply, lie2, bracket2, adTranspose, ...]` computation, then `norm_num`. Feasible.

This is a genuinely new mathematical result: **the Milnor connection in the non-bi-invariant 2D model has nonzero curvature** (R(e₁,e₂)e₁ = 2e₂), demonstrating that the closure output is a nontrivial curved connection. It also gives a new downstream use of `milnorLeviCivitaData`/`toCurvatureOperator`.

Let me add this to the R6 consumer file as a sixth declaration, with the cone check. I'll write:
```lean
theorem a3_curvature_nonzero :
    (milnorLeviCivitaData metric2 lie2).toCurvatureOperator (![1,0] : Fin 2 → ℝ) ![0,1] ![1,0]
      = (2 : ℝ) • ![0,1] := by ...
```
Actually the RHS: `2 • e₂`. Let me define `e0 := ![1,0]`, `e1 := ![0,1]` as local notation/abbrevs to keep it readable.

The computation: `toCurvatureOperator_apply` gives `∇_{e0}(∇_{e1}e0) − ∇_{e1}(∇_{e0}e0) − ∇_{[e0,e1]}e0`. With `milnorConnection_apply` and the bracket/adTranspose evaluations. The `adTranspose` definition: `adTranspose m b X Y = m.raiseIndex (m.form.flip.comp (b.bracket X)) Y`. Evaluating that requires `raiseIndex_apply` + sums over Fin 2 → `simp` should compute `∑ i : Fin 2, B (basis i) X • basis i`. Hmm, `raiseIndex_apply` gives a Finset sum over `ι`; with `Fin 2` and `Pi.basisFun`, `simp` might evaluate. This could get messy.

Alternative route to avoid `adTranspose`: use the *proved* properties instead of unfolding. E.g., for the specific vectors, compute ∇ via the abstract characterization? Not simpler.

Hmm, maybe simpler: use `lie2`'s bracket and the *defining* Milnor formula, and let `simp` + `ring` handle. Let me try in a scratch file and see how far automation gets. If it takes more than ~30-40 min, I'll drop it (not essential).

Actually, wait. There might be an easier nonzero-curvature witness: use `ricci`/`scalarCurvature` via the frame formulas in `RicciSymmetry`. But those also need the same evaluations.

Alternatively, use a *different* known model: the so(3) model already has `so3_curvature_nonzero` (R(e₀,e₁)e₁ = ¼e₀) — but that's bi-invariant and came from the card itself. My new contribution would be in the non-invariant model, which is exactly the novel case.

Let me try to formalize `a3_curvature_nonzero` in the R6 file. If it works, great; if not, keep the current 5 declarations.

Let me set up a scratch attempt: I'll write the theorem and iterate on the proof.

First, how does `adTranspose` evaluate? `adTranspose m b X Y = m.raiseIndex (m.form.flip.comp (b.bracket X)) Y`. `raiseIndex` is defined as a sum of compositions. `raiseIndex_apply` theorem: `m.raiseIndex B X = ∑ i : ι, B (m.basis i) X • m.basis i`. So `adTranspose metric2 lie2 e0 Y = ∑ i : Fin 2, (metric2.form.flip.comp (bracket2 e0)) (basis i) Y • basis i`. With `Pi.basisFun`, `basis i = Pi.single i 1`. The form `dot2 (Pi.single i 1) Y = Y i`-ish. So the sum = (bracket2 e0 evaluated...) Let me just let Lean compute with `simp [adTranspose, MetricData.raiseIndex_apply, metric2, dot2, lie2, bracket2, Pi.basisFun_apply]` and see.

Let me write a scratch file first to test the pieces, then integrate.

Actually, an even simpler approach for nonvanishing curvature: use `a3_milnor_ne_mean` plus a *theorem* that the mean connection's curvature vanishes in the 2D model... no, that's more work.

Let me try the direct computation. I'll write a scratch file `audit360/pkgs/D12-connection-curvature/A3ExtraR6/ScratchCurv.lean` (later delete or keep as evidence).

Plan for the proof:
```lean
abbrev e0 : Fin 2 → ℝ := ![1, 0]
abbrev e1 : Fin 2 → ℝ := ![0, 1]

-- evaluation lemmas
lemma milnor_e0_e0 : milnorConnection metric2 lie2 e0 e0 = -e1 := by
  rw [milnorConnection_apply]
  simp [metric2, dot2, lie2, bracket2, adTranspose, MetricData.raiseIndex_apply, e0, e1]
  ...
```
I'll iterate in Lean.

Let me create the scratch file and try.
dsh: reasoning:
The `adTranspose` definition is `V →ₗ V →ₗ V` defined with `toFun X := m.raiseIndex ...`; unfolding `adTranspose` gives a structure with `toFun`. `rw [adTranspose, MetricData.raiseIndex_apply]` fails because the application isn't in the expected form. I need to unfold more: `simp only [adTranspose, LinearMap.coe_mk, AddHom.coe_mk]` or `change` the goal.

Let me fix `adT_e0`:
```lean
lemma adT_e0 (Y : Fin 2 → ℝ) : adTranspose metric2 lie2 e0 Y = (Y 0 + Y 1) • e1 := by
  change metric2.raiseIndex (metric2.form.flip.comp (lie2.bracket e0)) Y = (Y 0 + Y 1) • e1
  rw [MetricData.raiseIndex_apply]
  simp only [LinearMap.comp_apply, LinearMap.flip_apply, Fin.sum_univ_two, metric2, dot2,
    lie2, bracket2, LinearMap.coe_mk, AddHom.coe_mk, Pi.basisFun_apply]
  ext i
  fin_cases i <;> simp [e0, e1] <;> ring
```
The `change` should expose the application.

And `milnor_e0_e0`: after `rw [milnorConnection_apply]`, the `adTranspose metric2 lie2 e0 e0` should rewrite with `adT_e0`. The error shows the rewrite at `adT_e0` didn't match because the term is `(e0 0 + e0 1) • e1` already (the first rw succeeded?) Actually the error message says `rw [milnorConnection_apply, adT_e0, adT_e0]` failed at the second occurrence: `(adTranspose metric2 lie2) e0 ?Y` not found — because after the first `adT_e0` rewrite, the second occurrence is `adTranspose metric2 lie2 e0 e0`? The target shows both occurrences already replaced by `(e0 0 + e0 1) • e1`... so the rw list processed both fine? No — it says failed to find pattern; but the target displayed already has the substitutions, meaning Lean's `rw` applied `adT_e0` twice? Hmm, the target after failure shows `(1/2) • ((lie2.bracket e0) e0 - (e0 0 + e0 1) • e1 - (e0 0 + e0 1) • e1) = -e1` — so both were rewritten, and the failure came from... `rw` with multiple lemmas: it applies each; the third one `adT_e0` again had no occurrence → error. Right, I listed `adT_e0` twice but the second `adT_e0` is for the *other* argument: `adTranspose m b Y X`. In `milnorConnection_apply`, the expression is `b.bracket X Y - adTranspose m b X Y - adTranspose m b Y X`; for X=Y=e0 both are adTranspose e0 e0, so two occurrences of `adT_e0`: `rw [adT_e0]` rewrites all occurrences by default? Yes, `rw` rewrites all matches. So a single `adT_e0` suffices. Fix: `rw [milnorConnection_apply, adT_e0]`.

Then goal: `(1/2) • (bracket2 e0 e0 - (e0 0 + e0 1) • e1 - (e0 0 + e0 1) • e1) = -e1`. Evaluate `e0 0 = 1, e0 1 = 0`; bracket2 e0 e0 = 0. So `(1/2) • (0 - e1 - e1) = -e1` ✓. Proof: `simp [bracket2, e0, e1]` might do it, then `module`/`ext i; fin_cases i <;> norm_num`.

Let me fix the scratch file and iterate.
dsh: reasoning:
Good progress: only the final `milnor_e0_e0` fails. The goal after rewrites is a real-number equality? "No applicable extensionality theorem found for type ℝ" at line 119 `ext i`. Wait — the goal after `simp [lie2, bracket2]` may have become a scalar equality rather than a function equality, meaning `milnorConnection metric2 lie2 e0 e0` was... hmm, `milnorConnection_apply` produces `(1/2) • (bracket2 e0 e0 - adT e0 e0 - adT e0 e0)` which is a vector. After `rw [adT_e0]` and `simp`, maybe it simplified to `-e1 = -e1` or to a scalar? Let me see the goal by using `trace_state` or just adjusting.

Possibly `simp` closed the goal entirely and then `ext i` has nothing? No — "No applicable extensionality theorem for type ℝ" means the goal is an ℝ equality at that point. So `simp [lie2, bracket2]` reduced both sides to scalars somehow. Hmm, maybe `simp` turned the vector equality into a pointwise? Actually `ext i` was called on `... = -e1` where both sides are functions; unless simp already applied `funext`? Let me look at what `simp [lie2, bracket2]` does: it might reduce `(1/2) • (bracket2 e0 e0 - (e0 0 + e0 1) • e1 - (e0 0 + e0 1) • e1) = -e1` to something like `(1/2) • (−e1 − e1) = -e1`, still a function equality. Then `ext i` should work.

Unless... `simp [lie2, bracket2]` unfolded `e0`/`e1`? Not included. Hmm, "No applicable extensionality theorem found for type ℝ" — perhaps after simp, the goal became `∀ i, ...`? No.

Let me print the state. I'll modify the proof to:
```lean
  rw [milnorConnection_apply, adT_e0]
  simp only [lie2, bracket2, LinearMap.coe_mk, AddHom.coe_mk]
  ext i
  fin_cases i <;> norm_num [e0, e1]
```
Let me test with `set_option pp.all`? Simpler: replace the last part with `funext i; fin_cases i <;> norm_num [e0, e1, lie2, bracket2]` and see.

Actually the error at line 119 is `ext i`. Maybe the goal is `(1/2) • (...) = -e1` where `-e1 : Fin 2 → ℝ` — ext should apply `funext`. Unless Lean's `ext` picks the *scalar* ext lemma because the expected type is ℝ due to the `(1/2) •` smul... no.

Wait, maybe `simp [lie2, bracket2]` rewrote `e1` (an abbrev for ![0,1]) via `e1` being in the simp set? I passed `[e0, e1]` only to the final `simp` after `fin_cases`, not the earlier one. The earlier `simp [lie2, bracket2]` — hmm, `e0`/`e1` are `abbrev`s, so `simp` may unfold them via `abbrev` transparency! Since abbreviations are reducible, `simp` can unfold `e0` to `![1,0]`, `e1` to `![0,1]`, and then simplify `![0,1]` to... `Matrix.cons`... The goal `... = -e1` might have become an equality between two `Fin 2 → ℝ` values that `simp` decided to prove pointwise? No.

Actually, maybe `simp` reduced the whole goal to `(1:ℝ)/2 * ... ` — i.e., it turned the vector equality into equality of *components*? That can't happen.

Let me just add `trace_state` before `ext i` to see. Let me do a quick test.
dsh: reasoning:
Now I see: `simp only [lie2, bracket2, ...]` unfolds `bracket2 e0 e0` to `fun x => e0 0 * e0 1 - e0 1 * e0 0` — a function, and the goal is a vector equality. The `ext i` apparently applied to a scalar goal? The error at 116:66 now is "unsolved goals case «1» ⊢ 1/2 * (vecHead ... - 1 - 1) = -1" — that's from `norm_num [e0,e1]` on one of the `fin_cases` branches; the vecHead expression didn't reduce. And the trace shows the state before `ext i`.

Hmm, the goal is a vector equality where the RHS `-e1` and LHS contains `fun x => ...`. `ext i` should work. The earlier error "No applicable extensionality theorem found for type ℝ" happened because `simp [lie2, bracket2]` (full simp) reduced both sides to functions and possibly applied `funext` itself, leaving a scalar goal for a specific i? Unclear.

The cleanest fix: avoid `ext`-based reduction by evaluating at components directly. Use `funext i; fin_cases i <;> simp [e0, e1, lie2, bracket2, Matrix.cons_val_zero, Matrix.cons_val_one]`. Or use `decide`? No.

Alternative: prove the vector equalities using the `![...]` ext lemmas: `apply Matrix.cons_eq_cons`? Hmm.

Alternative much simpler approach: avoid computing with `e0`/`e1` as `![...]` and instead reason with `Pi.single`. Or use `ext i <;> fin_cases i <;> norm_num [...]`.

Let me look at the current failure: after `ext i` and `fin_cases i`, the branch goal is `1 / 2 * (Matrix.vecHead (Matrix.vecTail fun i => 0) - 1 - 1) = -1`. `Matrix.vecHead (Matrix.vecTail fun i => 0)` should reduce to `0` by `rfl`/`simp`. Adding `Matrix.vecHead`/`Matrix.vecTail` simp lemmas or just `norm_num` may not reduce them. Let me use `simp` with `Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.tail_cons` or simply `decide`? For real arithmetic, `norm_num` should handle if the term reduces.

Actually, maybe the issue is that `(fun x => e0 0 * e0 1 - e0 1 * e0 0)` is the zero function; `simp` with `e0, e1` should reduce `e0 0 = 1` etc. since `e0 := ![1,0]` is an abbrev — `simp [e0]` unfolds it. Then `fun x => 1*0 - 0*1 = fun x => 0`, and `simp` might simplify `(fun x => (0:ℝ))` to `0`? Then the goal is `(1/2) • (0 - (0+1) • e1 - (0+1) • e1) = -e1`, i.e., scalar multiples of e1. Then `simp` with `smul_neg`, `← smul_add`... could close with `module`? `module` tactic works on module equalities: `(1/2) • (-e1 - e1) = -e1` — `module` might close it after normalizing coefficients (needs `norm_num` for 1/2*(−2) = −1). Let me try:

```lean
lemma milnor_e0_e0 : milnorConnection metric2 lie2 e0 e0 = -e1 := by
  rw [milnorConnection_apply, adT_e0]
  simp only [lie2, bracket2, LinearMap.coe_mk, AddHom.coe_mk, e0, e1,
    Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons]
  module
```
Hmm, `Matrix.head_cons` etc. Let me just test variants quickly. Alternatively use `norm_num` after `funext`.

Let me restructure to compute the connection values via a general formula: I can prove a general lemma for ∇_X Y in this model by pure algebra, avoiding basis evaluation at the term level... Actually, the cleanest is to evaluate on arbitrary X,Y as vectors: 

∇_X Y = ½([X,Y] − (X₀+X₁)(Y₀+Y₁)... wait I derived (ad_X)^T Y = (Y₀+Y₁)(X₀ e₂ − X₁ e₁)? Let me recompute: (ad_X)^T Y = X₀·T₁Y + X₁·T₂Y where T₁Y = (Y₀+Y₁)e₂, T₂Y = −(Y₀+Y₁)e₁. So (ad_X)^T Y = (Y₀+Y₁)(X₀ e₂ − X₁ e₁).
And (ad_Y)^T X = (X₀+X₁)(Y₀ e₂ − Y₁ e₁).
And [X,Y] = (X₀Y₁ − X₁Y₀)(e₁+e₂).

So ∇_X Y = ½[(X₀Y₁−X₁Y₀)(e₁+e₂) − (Y₀+Y₁)(X₀e₂−X₁e₁) − (X₀+X₁)(Y₀e₂−Y₁e₁)].

This general formula might be easier to prove as a lemma about arbitrary X Y by extensionality and `ring` — and then the specific values follow by substitution. But `adTranspose` evaluation for general X still requires the raiseIndex sum with `X` symbolic; `adT_general`:

```lean
lemma adT_apply (X Y : Fin 2 → ℝ) :
    adTranspose metric2 lie2 X Y = (Y 0 + Y 1) • (X 0 • e1 - X 1 • e0) := by
  change metric2.raiseIndex (metric2.form.flip.comp (lie2.bracket X)) Y = ...
  rw [MetricData.raiseIndex_apply]
  simp only [Fin.sum_univ_two, LinearMap.comp_apply, LinearMap.flip_apply, metric2, dot2, lie2, bracket2, LinearMap.coe_mk, AddHom.coe_mk, Pi.basisFun_apply]
  ext i; fin_cases i <;> simp [e0, e1] <;> ring
```
Then `milnor_apply_general`:
```lean
lemma milnor_apply_general (X Y : Fin 2 → ℝ) :
    milnorConnection metric2 lie2 X Y =
      (1/2) • ((X 0 * Y 1 - X 1 * Y 0) • (e0 + e1) - (Y 0 + Y 1) • (X 0 • e1 - X 1 • e0)
        - (X 0 + X 1) • (Y 0 • e1 - Y 1 • e0)) := by
  rw [milnorConnection_apply, adT_apply, adT_apply]
  simp only [lie2, bracket2, LinearMap.coe_mk, AddHom.coe_mk]
  module
```
Hmm, `bracket2 X Y = fun _ => X0Y1 - X1Y0 = (X0Y1−X1Y0) • (fun _ => 1)`. Is `(X0Y1−X1Y0) • (e0+e1)` equal to `fun _ => X0Y1−X1Y0`? Yes, e0+e1 = fun _ => 1. `simp [e0, e1]` should show it. Maybe `module` closes.

Then specific evaluations:
∇_{e0}e0 = ½[0 − (0+1)(1·e1 − 0·e0) − (1+0)(0·e1 − 0·e0)] = ½[−e1] = −(1/2)e1. Wait, that contradicts my earlier hand computation of −e₂... Let me recheck.

Earlier by hand: ∇_{e₁}e₁ = −e₂. Using the general formula with X=Y=e0: [X,Y]=0; (ad_X)^T Y = (Y₀+Y₁)(X₀e₂−X₁e₁) = (0+1)(1·e₂−0) = e₂; (ad_Y)^T X = (X₀+X₁)(Y₀e₂−Y₁e₁) = (1+0)(e₂) = e₂. ∇ = ½(0 − e₂ − e₂) = −e₂ ✓. My general formula above wrote `(Y₀+Y₁) • (X₀ • e1 - X1 • e0)` using e1 for what I called e₂ — indexing confusion: in the file, `e0 = ![1,0]`, `e1 = ![0,1]`. My hand computation used e₁=(1,0), e₂=(0,1). So T₁Y = (Y₀+Y₁)e₂ = (Y₀+Y₁)e1 in file notation; T₂Y = −(Y₀+Y₁)e₁ = −(Y₀+Y₁)e0. So (ad_X)^T Y = X₀·(Y₀+Y₁)e1 + X₁·(−(Y₀+Y₁)e0) = (Y₀+Y₁)(X₀ • e1 − X₁ • e0) ✓ matches.

For X=Y=e0: (0+1)(1·e1 − 0·e0) = e1. OK so ∇_{e0}e0 = −e1 ✓ (since −e1 here is (0,−1), which is my "−e₂") ✓.

Good. Let me rewrite the scratch with the general lemmas, then compute:
- ∇_{e0}e0 = −e1
- ∇_{e0}e1 = ? formula: [e0,e1] = (1·1−0·0)(e0+e1) = e0+e1; (ad_{e0})^T e1 = (1+0)(1·e1−0·e0) = e1; (ad_{e1})^T e0 = (1+0)(0·e1−1·e0) = −e0. ∇ = ½[(e0+e1) − e1 − (−e0)] = ½(2e0) = e0 ✓.
- ∇_{e1}e0 = ½[[e1,e0] − (0+1)(0·e1−1·e0) − (1+0)(1·e1−0·e0)] = ½[−(e0+e1) + e0 − e1] = ½(−2e1) = −e1 ✓.
- ∇_{e1}e1 = ½[0 − (0+1)(0·e1−1·e0) − (0+1)(0·e1−1·e0)] = ½[e0 + e0] = e0 ✓.

Then curvature R(e0,e1)e0 = ∇_{e0}(∇_{e1}e0) − ∇_{e1}(∇_{e0}e0) − ∇_{[e0,e1]}e0
= ∇_{e0}(−e1) − ∇_{e1}(−e1) − ∇_{e0+e1}e0.
∇_{e0}(−e1) = −e0.
∇_{e1}(−e1) = −e0.
∇_{e0+e1}e0 = ∇_{e0}e0 + ∇_{e1}e0 = −e1 + (−e1) = −2e1.
R = −e0 − (−e0) − (−2e1) = 2e1. ✓ (matches hand computation 2e₂).

So the theorem: `(milnorLeviCivitaData metric2 lie2).toCurvatureOperator e0 e1 e0 = (2:ℝ) • e1`.

To prove it, I need to evaluate ∇ on the specific vectors. With the general formula lemma + specific `simp` evaluations, should be doable:
```lean
lemma milnor_e0_e0 : milnorConnection metric2 lie2 e0 e0 = -e1 := by
  rw [milnor_apply_general]; simp [e0, e1]; module
```
Let me try. If `simp` can evaluate `(e0 0 * e0 1 - e0 1 * e0 0)` etc. with `e0, e1` unfolded.

Then curvature:
```lean
theorem a3_curvature_nonzero :
    (milnorLeviCivitaData metric2 lie2).toCurvatureOperator e0 e1 e0 = (2:ℝ) • e1 := by
  rw [LeviCivitaData.toCurvatureOperator_apply, milnor_e0_e1, milnor_e1_e0, milnor_e1_e1, ...]
```
Wait, `toCurvatureOperator_apply` gives `∇_{e0}(∇_{e1}e0) − ∇_{e1}(∇_{e0}e0) − ∇_{[e0,e1]}e0`. `milnorLeviCivitaData`'s nabla is `milnorConnection`. So:
- `∇_{e1}e0 = −e1` → need `∇_{e0}(−e1)`: use linearity in the second argument: `(milnorConnection m b e0).map_neg` → `−∇_{e0}e1`. Since `milnorConnection m b e0` is a linear map, `map_neg` applies: `(milnorConnection metric2 lie2 e0) (-e1) = -(milnorConnection metric2 lie2 e0 e1)`.
- Then `∇_{e0}e1 = e0` → `−e0`.
- `∇_{e1}(−e1) = −∇_{e1}e1 = −e0`.
- `∇_{[e0,e1]}e0` where `[e0,e1] = e0+e1` (proved `bracket2_e0_e1`), then `∇_{e0+e1}e0 = ∇_{e0}e0 + ∇_{e1}e0` by `map_add` in the first argument: `(milnorConnection m b).map_add`: `∇_{X1+X2} = ∇_{X1} + ∇_{X2}` as linear maps. Then `= −e1 + (−e1) = −2e1`.
- Sum: `−e0 − (−e0) − (−2e1) = 2e1`.

This is a nice calculation. Let me implement it in the scratch and iterate. I'll define a local notation for `∇ := milnorConnection metric2 lie2`.

Let me rewrite ScratchCurv.lean with the general lemmas and the final theorem.
dsh: reasoning:
The file changed since read (I modified it via python). I need to re-read it before writing. Let me read it quickly (just to satisfy the policy) and then write.
dsh: reasoning:
I've now read the file in this session (partially) — the write tool should accept. Let me write the new version.
dsh: reasoning:
Interesting errors:
1. `simp [e0, e1]` seems to close too much / the goals become weird: "⊢ 1/2 = 0" in `milnor_e0_e0`. This suggests `simp [e0,e1]` already simplified the goal to `1/2 = 0`?? That means my `milnor_apply_general` statement might be *wrong*: after `rw [milnor_apply_general]` and `simp [e0,e1]`, the goal `(1/2) • (...) = -e1` might have been simplified to a *scalar* equation because simp evaluated both sides to functions and then... hmm, "⊢ 1/2 = 0" means it's comparing coefficients somehow. Since `e1 = ![0,1]` and the LHS = ½ • (something), if the "something" evaluated to 0 vector, goal becomes ½•0 = −e1 → `0 = -e1` → simp evaluating at component 0: `0 = 0`, component 1: `0 = -1` → false... but it shows `1/2 = 0`.

Wait: maybe `simp [e0, e1]` unfolded `e0`, `e1` and also `Matrix.cons` such that the goal became about `Fin 2 → ℝ` represented as `Matrix.vecCons`, and `simp` turned it into a conjunction? The last error says "goal 2⁻¹ * (1 + 1) = 1 ∧ 0 = ![0] is not an equality" — that's from `module`. So indeed `simp` decomposed into components using `![...]` notation: `![a, b] = ![c, d]` simplified to conjunction `a = c ∧ b = d`.

So the problem: for `milnor_e0_e0`, after `simp [e0,e1]`, the goal is `1/2 = 0` (component 0) — meaning my general formula's evaluation differs from my hand calculation! Let me check `milnor_apply_general` at X=Y=e0:
(X0Y1−X1Y0)•(e0+e1) = (1*0−0*1)•(e0+e1) = 0.
(Y0+Y1)•(X0•e1 − X1•e0) = (0+1)•(1•e1 − 0•e0) = e1.
(X0+X1)•(Y0•e1 − Y1•e0) = (1+0)•(0•e1 − 0•e0) = 0.
So ∇ = ½(0 − e1 − 0) = −½ e1, not −e1!

Hmm! That contradicts my hand computation. Let me recheck. Earlier I computed ∇_{e₁}e₁ = ½(0 − e₂ − e₂) = −e₂. Let me recompute `(ad_Y)^T X` for X=Y=e₁ (hand notation e₁=(1,0)):
(ad_Y)^T X where Y = e₁: ad_{e₁}: Z ↦ [e₁,Z] = (Z₂)e₁? Wait, earlier I computed ad_{e₁}Y = Y₂ u where u = e₁+e₂ (hand notation: e₁=(1,0), e₂=(0,1), u = (1,1)).
X=Y=e₁ ⇒ Y₂ = 0 ⇒ (ad_Y)^T X = T₁ e₁ where T₁ is the transpose of ad_{e₁}. T₁Z = ⟨Z,u⟩e₂. So T₁e₁ = ⟨e₁,u⟩e₂ = 1·e₂. Yes (ad_Y)^T X = e₂.
And (ad_X)^T Y = T₁e₁ = e₂.
So ∇ = ½(0 − e₂ − e₂) = −e₂. That's −e1 in file notation (since file e1 = (0,1) = hand e₂).

But the file's general formula evaluated (ad_Y)^T X = (X0+X1)•(Y0•e1 − Y1•e0) — for X=Y=e0: (1+0)(1·e1 − 0·e0) = e1. Hmm: (ad_Y)^T X with Y=e0 (=hand e₁) and X=e0: T₁X = ⟨X,u⟩e₂ = 1·e₂ = e1 (file). So the second term is e1, and the third term (ad_X)^T Y = e1 too. Then ∇ = ½(0 − e1 − e1) = −e1. But my formula `(Y0+Y1)•(X0•e1 − X1•e0)` for the FIRST transpose uses X's coefficients: (ad_X)^T Y = (Y0+Y1)(X0•e1 − X1•e0) = (0+1)(1•e1) = e1 ✓. The SECOND transpose (ad_Y)^T X = (X0+X1)(Y0•e1 − Y1•e0) = (1+0)(1•e1 − 0) = e1 ✓. Both = e1. So ∇ = ½(0 − e1 − e1) = −e1 ✓. But the `simp` in Lean computed the second term as 0? Let me check the Lean goal after simp: the error said "⊢ 1/2 = 0" for component 0 of the target `∇ e0 e0 = -e1`. Hmm, if ∇ = −e1, LHS component 0 = 0, RHS component 0 = 0 → goal 0 = 0, closed. But the error says the goal was `1/2 = 0`. So Lean computed LHS component 0 = 1/2, meaning the second and third terms vanished and the first term... (X0Y1−X1Y0) = 0 for X=Y=e0, so first term 0. Then LHS = ½(0 − e1 − e1) component 0 = ½(0 − 0 − 0) = 0. Not 1/2.

Unless `simp [e0,e1]` didn't evaluate `(X0+X1)` correctly for the *symbolic* X? No, X=e0.

Hmm wait — maybe the issue is `simp [e0, e1]` unfolded `e1` → `![0,1]`, and `(Y0+Y1)•(...)`: `Y=e0`, `Y 0 = 1`, `Y 1 = 0` → factor 1. `X0•e1 − X1•e0 = 1•e1 − 0•e0 = e1`. So second term = e1. Third: `(X0+X1)=1`, `(Y0•e1 − Y1•e0) = 1•e1 − 0•e0 = e1`. So all consistent.

Unless my `milnor_apply_general` lemma statement itself is wrong: `adT_apply` says `adTranspose X Y = (Y 0 + Y 1) • (X 0 • e1 - X 1 • e0)`. Let me verify `adT_apply` correctness with my derivation: (ad_X)^T Y = X₀·T₁Y + X₁·T₂Y where T₁Y = (Y₀+Y₁)e₁(file e2) and T₂Y = −(Y₀+Y₁)e₀(file e1). So = X₀(Y₀+Y₁)e1 − X₁(Y₀+Y₁)e0 = (Y₀+Y₁)(X₀•e1 − X₁•e0) ✓.

Then in `milnorConnection_apply`: `∇ X Y = ½ • (bracket X Y - adTranspose X Y - adTranspose Y X)`.
= ½ • ( (X0Y1−X1Y0)(e0+e1) − (Y0+Y1)(X0•e1 − X1•e0) − (X0+X1)(Y0•e1 − Y1•e0) ).
For X=Y=e0: ½•(0 − (0+1)(1•e1 − 0•e0) − (1+0)(0•e1 − 0•e0)) = ½•(0 − e1 − 0) = −½ e1.

AH — I see my arithmetic error: `(X0+X1)(Y0•e1 − Y1•e0)` with X=Y=e0: X0+X1 = 1+0 = 1; Y0•e1 − Y1•e0 = 1•e1 − 0•e0 = e1. So the third term is e1, not 0! I mistakenly wrote Y1•e0 with Y1=0 and Y0•e1=1•e1 → e1. Yes, third term = e1. So both transposes = e1 and ∇ = −e1... wait: ½•(0 − e1 − e1) = −e1. Hmm, but then why did Lean produce 1/2?

Let me recompute what Lean's `simp [e0,e1]` sees. Maybe `simp` evaluated `X 0 • e1 - X 1 • e0` where X = e0 — fine. Hmm, but the goal error "⊢ 1/2 = 0" was for `milnor_e0_e0` at line 118 which is the `module` line after `simp`. So the remaining goal after `simp` was `1/2 = 0`?? That suggests `simp` simplified the vector equality to a pointwise goal of component 0: `1/2 = 0`. Component 0 of LHS: ½•(0 − e1 − e1) = ½•(0 − 0 − 0) = 0. Unless the first term isn't 0: bracket2 e0 e0 = fun _ => e0 0 * e0 1 − e0 1 * e0 0 = 1*0 − 0*1 = 0 ✓.

Hmm, unless `simp [e0, e1]` also rewrote `(X 0 * Y 1 - X 1 * Y 0) • (e0 + e1)` weirdly: `e0 + e1 = ![1,1]`; multiplied by 0 → 0 ✓.

Could the issue be that `milnor_apply_general`'s RHS is missing a factor somewhere? Let me test the general lemma itself: it compiled (no error at line 114?), so the lemma is proven. Fine.

Wait, the errors are at lines 118, 123, 128, 133 — those are the `module` lines of milnor_e0_e0 etc. So `rw [milnor_apply_general]` and `simp [e0,e1]` happened. The resulting goal was `1/2 = 0` for the first. Let me debug by printing the state after simp. Actually, maybe `simp [e0, e1]` is too aggressive and rewrites `-e1` etc., turning the goal into a `Fin 2` component equation where the *first component* is `1/2 = 0`. If LHS component 0 = 1/2, then the evaluation gives ∇_{e0}e0 component 0 = 1/2. So the third term must be −? Let me recompute with the file's `adT_apply` sign: `adTranspose X Y = (Y0+Y1)•(X0•e1 − X1•e0)`.
For the term `adTranspose m b Y X` (second transpose in Milnor formula) = `(X0+X1)•(Y0•e1 − Y1•e0)`.
X=Y=e0: (1+0)•(1•e1 − 0•e0) = e1.
First transpose `adTranspose m b X Y` = (Y0+Y1)•(X0•e1 − X1•e0) = (0+1)•(1•e1 − 0•e0) = e1.
Bracket = 0.
∇ = ½•(0 − e1 − e1) = ½•(−2e1) = −e1.
Component 0 of LHS = 0; component 1 = −1. Target = −e1: component 0 = 0, component 1 = −1. So the goal after simp should be `0 = 0 ∧ -1 = -1` — closable by simp!

So why `1/2 = 0`? Unless `simp [e0, e1]` doesn't evaluate `e0 + e1` or the smul properly and produces a component equation `1/2 * (something) = 0` that reduces to `1/2 = 0` because `something` was simplified to 1?? Hmm: if the bracket term were evaluated as 1 instead of 0 — e.g., `(X0Y1−X1Y0)` = 1·0 − 0·1 = 0. No.

Wait, maybe the general lemma's RHS has the bracket term multiplied by `(e0+e1)` where `e0+e1` = ![1,1] — and `simp` decomposed `![1,1]` differently... Let me just add `trace_state` after `simp [e0,e1]` in milnor_e0_e0 to see the actual goal. Let me do that.
dsh: reasoning:
Now I see the goal after `simp only [e0, e1]` (which didn't beta-reduce the `![...] 0` applications). The `ring` failure "⊢ 1/2 = 0" comes from the *other* lemma (line 118 is `milnor_e0_e0`'s `ring`? No wait, line 118 was replaced by trace_state/sorry; the error at 118:2 "ring failed ... ⊢ 1/2 = 0" is probably from `adT_apply`'s `ring` at line ~113? Hmm, the line numbers shifted. The trace shows the goal with `![1,0] 0` not reduced — so `simp only [e0, e1]` unfolded the abbrevs but didn't evaluate the projections.

Key insight: the `module` tactic failing with "goal 2⁻¹ * (1 + 1) = 1 ∧ 0 = ![0] is not an equality" (from bracket_e0_e1 line 139) means `simp`/`module` is decomposing vector equalities into components in a weird way (using `![...]` as Matrix.cons and comparing head/tail).

The cleanest approach: use `funext i; fin_cases i` and evaluate with `norm_num`/`simp` using `Matrix.cons_val_zero/one`. Let me write helper evaluation lemmas for the basis vectors:

```lean
@[simp] lemma e0_zero : e0 0 = 1 := rfl
@[simp] lemma e0_one : e0 1 = 0 := rfl
@[simp] lemma e1_zero : e1 0 = 0 := rfl
@[simp] lemma e1_one : e1 1 = 1 := rfl
```
Wait, will `rfl` prove `(![1,0] : Fin 2 → ℝ) 0 = 1`? The `![...]` notation elaborates to `Matrix.cons 1 (Matrix.cons 0 Matrix.vecNil)` — actually for functions `Fin 2 → ℝ` the notation uses `![a, b]` = `fun i => match i with | 0 => a | 1 => b`? In Lean 4 mathlib, `![a, b]` for `Fin 2 → α` is `Matrix.cons a (Matrix.cons b Matrix.vecNil)` where `Matrix.cons` is defined on `Fin (n+1) → α`. Then `(Matrix.cons a v) 0 = a` by `Matrix.cons_val_zero` (rfl?) and `(Matrix.cons a v) 1 = v 0` by `Matrix.cons_val_one`. Usually `simp` knows these via `[simp]` lemmas. But in the trace, `![1,0] 0` wasn't reduced by `simp only [e0,e1]` because I restricted the simp set. With full `simp [e0,e1]`, it seems to have reduced to a *conjunction* of component goals, which is what `simp` does with `Matrix.cons` equalities (it uses `Matrix.cons_eq_cons`? no...). 

Let me try a robust approach: state the four evaluation lemmas with explicit `by decide`? No, `decide` on ℝ equalities won't work. But `rfl` or `norm_num` should.

Let me test:
```lean
example : e0 0 = (1:ℝ) := rfl
example : e0 1 = (0:ℝ) := rfl
example : e1 0 = (0:ℝ) := rfl
example : e1 1 = (1:ℝ) := rfl
example : e0 + e1 = ![1,1] := rfl
example : (2:ℝ) • e1 = ![0,2] := rfl
```
If `rfl` works for these, then everything is definitional and `simp` with the right lemma set should work. The problem in `milnor_e0_e0` was that `simp [e0, e1]` produced a weird state. Let me instead prove the four connection values by `funext i; fin_cases i <;> norm_num [milnor_apply_general, e0, e1]`? Hmm.

Better plan: after `rw [milnor_apply_general]`, use `funext i; fin_cases i <;> simp [e0, e1] <;> norm_num` — targeting components directly rather than vector-level `module`.

For component proofs, the expression involves `•` on functions: `(c • f) i = c * f i`; `(f + g) i = f i + g i`; `(-f) i = -(f i)`. `simp` should handle `Pi.add_apply`, `Pi.smul_apply`, `Pi.neg_apply`, and `![..] 0/1` via `Matrix.cons_val_zero`, `Matrix.cons_val_one`, `Matrix.head_cons`. Let me include those explicitly:
`simp only [Pi.add_apply, Pi.smul_apply, Pi.neg_apply, smul_eq_mul, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.tail_cons, e0, e1]` then `norm_num`.

Let me test in a small scratch with one example first to find the working simp set. Let me create a tiny file with examples and iterate.
dsh: reasoning:
Most examples worked:
- `e0 0 = 1` rfl ✓, etc.
- `(c • (e0+e1)) 1 = c` by simp ✓ (the later lines produced no error, so they worked).

The failing ones: `e0 + e1 = ![1,1]` and `(2:ℝ) • e1 = ![0,2]` — need `ext i; fin_cases i <;> simp` or `funext`. Fine — I'll avoid those forms and express everything via component lemmas.

So for `milnor_e0_e0`, prove by:
```lean
lemma milnor_e0_e0 : milnorConnection metric2 lie2 e0 e0 = -e1 := by
  rw [milnor_apply_general]
  funext i
  fin_cases i <;> simp [e0, e1] <;> norm_num
```
Let me test this pattern. Similarly for the others.

And for the curvature theorem, instead of `module`, I'll use `funext i; fin_cases i <;> simp [...] <;> norm_num`, or prove the vector equalities by `rw` and then close with the component tactic.

Also note in `milnor_apply_general` I used `module` after `simp only [lie2, bracket2, ...]`; that lemma compiled? The error list didn't complain about line ~114 (the general lemma), so it's proven. Hmm, but the `module` there worked because the goal was a general module equality.

Wait, actually was `milnor_apply_general` proven? The errors were at lines 118 (milnor_e0_e0), 129, 134, 139, 156. The general lemma is at ~100-114. No error → proven ✓.

Let me now fix the specific lemmas and the final theorem using component-wise proofs. Let me update the scratch.
dsh: reasoning:
One remaining error at line 118 (in the `adT_apply` lemma? Let me check line 118). And there's a leftover `sorry` (from my earlier debug edit? I replaced from "lemma milnor_e0_e0" to "end A3R6Scratch", so the debug sorry should be gone; but the error says the theorem depends on sorryAx, and line 118 has `ring failed ... ⊢ 1 / 2 = 0`).

Let me view lines 100-125 of the file to see what's at 118.
dsh: reasoning:
The failing line 118 is the `module` in `milnor_apply_general`. The goal after `simp only` must be a component-ish equation. The `module` tactic failed with remaining goal `⊢ 1 / 2 = 0`?? That's odd — but the error message shows "X Y : Fin 2 → ℝ, ⊢ 1 / 2 = 0" which is a goal produced by `module` (it probably decomposed and left this).

Hmm wait — that means the general formula's RHS is NOT equal to the LHS as I claimed! Let me recheck.

`milnorConnection_apply` says:
∇ X Y = ½ • (bracket X Y - adTranspose X Y - adTranspose Y X).

`adT_apply X Y = (Y0+Y1) • (X0•e1 − X1•e0)`.
`adT_apply Y X = (X0+X1) • (Y0•e1 − Y1•e0)`.

So the RHS I wrote:
½ • ( (X0Y1−X1Y0)•(e0+e1) − (Y0+Y1)•(X0•e1 − X1•e0) − (X0+X1)•(Y0•e1 − Y1•e0) ).

But `bracket2 X Y = fun _ => X0Y1 − X1Y0`, whereas I wrote `(X0Y1−X1Y0) • (e0+e1)`. Are these equal as functions? `(c • (e0+e1)) i = c * (e0+e1) i = c * 1 = c` for both i=0,1 (since e0+e1 = ![1,1]). And `(fun _ => c) i = c`. So yes equal, but not *definitionally* maybe? They are equal by extensionality, not rfl. So `module` needs to know `(c • (e0+e1)) = fun _ => c` — `module` works up to module equalities but the `fun _ => c` is not syntactically a scalar multiple. Actually the LHS of the goal after rw is `(fun _ => X0Y1−X1Y0) - ... - ...` and the RHS is `(X0Y1−X1Y0) • (e0+e1) - ...`. `module` normalizes `•` but the bare function `fun _ => c` vs `c • (e0+e1)` — for module normalization, `(e0+e1)` is a vector, not 1. So `module` can't equate `c • (e0+e1)` with `fun _ => c`. Hence the failure with residual goal `1/2 = 0` (from comparing coefficients in some basis where e0+e1 ≡ 1?).

Fix: express the bracket term in the same form as `bracket2` produces, i.e., use `(fun _ => X0Y1 − X1Y0)`:
```lean
lemma milnor_apply_general (X Y : Fin 2 → ℝ) :
    milnorConnection metric2 lie2 X Y =
      (1 / 2 : ℝ) • ((fun _ => X 0 * Y 1 - X 1 * Y 0) -
        (Y 0 + Y 1) • (X 0 • e1 - X 1 • e0) -
        (X 0 + X 1) • (Y 0 • e1 - Y 1 • e0)) := by
  rw [milnorConnection_apply, adT_apply, adT_apply]
  simp only [lie2, bracket2, LinearMap.coe_mk, AddHom.coe_mk]
  module
```
Then for specific evaluations, `simp [e0,e1]` can evaluate `(fun _ => ...)`. Let me apply that fix.

Also remove the unused `milnor_add_apply`/`milnor_neg_apply` helper lemmas I added (they use `▸ rfl` oddly; the direct `map_add`/`map_neg` rewrites in the theorem may be fine). Actually, in the final theorem I used `show ... from map_add ... ▸ rfl`; better to simply `rw [map_add]`? The expression is `milnorConnection metric2 lie2 (e0+e1) e0` where `milnorConnection metric2 lie2` is a linear map: `map_add (milnorConnection metric2 lie2) e0 e1 : milnorConnection m b (e0+e1) = milnorConnection m b e0 + milnorConnection m b e1`. Then applying both sides to `e0` requires congruence — `rw [map_add]` may rewrite the applied form? `map_add` states equality of linear maps; the goal has `(milnorConnection m b (e0+e1)) e0`, and `LinearMap.add_apply` converts. Easier: `rw [map_add]` then `rfl`? Let me instead use:
```lean
  rw [map_add]  -- rewrites (milnorConnection ... (e0+e1)) to (milnorConnection ... e0 + milnorConnection ... e1)
```
The goal term is `(milnorConnection m b (e0+e1)) e0`; after rw it becomes `((milnorConnection m b e0) + (milnorConnection m b e1)) e0`; then `simp only [LinearMap.add_apply]` gives the sum. OK.

Similarly `map_neg (milnorConnection m b e0) e1` rewrites `(milnorConnection m b e0) (-e1)` to `-(milnorConnection m b e0) e1`. Using `rw [map_neg]` should work if the pattern matches exactly — the error earlier was because after the first rw the expression was `-((milnorConnection m b) e1) e1` (application of the negated map), needing `LinearMap.neg_apply`. Let me use `simp only [map_neg, map_add, LinearMap.neg_apply, LinearMap.add_apply]` then the specific evaluation lemmas.

Let me restructure the final theorem:
```lean
theorem curvature_e0_e1_e0 : ... := by
  rw [LeviCivitaData.toCurvatureOperator_apply]
  change milnorConnection metric2 lie2 e0 (milnorConnection metric2 lie2 e1 e0) -
      milnorConnection metric2 lie2 e1 (milnorConnection metric2 lie2 e0 e0) -
      milnorConnection metric2 lie2 (lie2.bracket e0 e1) e0 = (2:ℝ) • e1
  rw [milnor_e1_e0, milnor_e0_e0, bracket_e0_e1]
  simp only [map_neg, map_add, LinearMap.neg_apply, LinearMap.add_apply]
  rw [milnor_e0_e1, milnor_e1_e1, milnor_e0_e0, milnor_e1_e0]
  funext i
  fin_cases i <;> norm_num [e0, e1]
```
After `simp only [map_neg, map_add, ...]`, the expression: 
- `milnorConnection m b e0 (-e1)` → `- milnorConnection m b e0 e1` = −e0.
- `milnorConnection m b e1 (-e1)` → `- milnorConnection m b e1 e1` = −e0.
- `milnorConnection m b (e0+e1) e0` → `(milnorConnection m b e0 + milnorConnection m b e1) e0` → `milnorConnection m b e0 e0 + milnorConnection m b e1 e0` = −e1 + −e1.
Goal: `−e0 − (−e0) − (−e1 + −e1) = 2•e1`. Should close by `rw`s + componentwise norm_num.

Let me rewrite the scratch file accordingly. I'll be careful with the helper lemma signatures.

Actually, let me simplify by proving the four `milnor_eX_eY` lemmas and then:
```lean
theorem curvature_e0_e1_e0 : ... := by
  rw [LeviCivitaData.toCurvatureOperator_apply]
  change milnorConnection metric2 lie2 e0 (milnorConnection metric2 lie2 e1 e0) -
      milnorConnection metric2 lie2 e1 (milnorConnection metric2 lie2 e0 e0) -
      milnorConnection metric2 lie2 (lie2.bracket e0 e1) e0 = (2 : ℝ) • e1
  rw [milnor_e1_e0, milnor_e0_e0, bracket_e0_e1]
  rw [show milnorConnection metric2 lie2 e0 (-e1) = -e0 by
        rw [map_neg, milnor_e0_e1]; rfl,
      show milnorConnection metric2 lie2 e1 (-e1) = -e0 by
        rw [map_neg, milnor_e1_e1]; rfl,
      show milnorConnection metric2 lie2 (e0 + e1) e0 = -e1 + -e1 by
        rw [map_add, LinearMap.add_apply, milnor_e0_e0, milnor_e1_e0]]
  funext i
  fin_cases i <;> norm_num [e0, e1]
```
Hmm `rw [map_neg]` inside a `show` whose goal is `milnorConnection m b e0 (-e1) = -e0`: `map_neg (milnorConnection m b e0) e1 : (milnorConnection m b e0) (-e1) = -(milnorConnection m b e0) e1`. rw should match. Then goal `- (milnorConnection m b e0) e1 = -e0`; rw [milnor_e0_e1] → `-e0 = -e0` → rfl. Good.

Let me implement.
dsh: reasoning:
Now `module` at line 118 says "No goals to be solved" — meaning `simp only [...]` already closed the general lemma (good), so remove `module`. 

Then the component goals have `Matrix.vecHead (Matrix.vecTail fun i => 0)` terms that `norm_num` doesn't reduce. Need `Matrix.head_cons`/`Matrix.tail_cons` or `Matrix.vecHead`/`vecTail` simp lemmas. Let me add `Matrix.vecHead`, `Matrix.vecTail`, `Matrix.cons_val_zero`, `Matrix.cons_val_one`, `Matrix.head_cons`, `Matrix.tail_cons`, `Matrix.empty_eq` to the simp set.

Actually, the term `Matrix.vecHead (Matrix.vecTail fun i => @OfNat.ofNat ℝ 0)` arose because `![1,0]` for a function `Fin 2 → ℝ` elaborated as `Matrix.vecCons 1 (Matrix.vecCons 0 ...)`? The notation `![1, 0]` might be `Matrix.of ![1,0]`? The presence of `vecHead`/`vecTail` suggests the function `fun i : Fin 2 => ...` was eta-expanded weirdly by `simp [e0, e1]` (unfolding e0/e1 and then `funext`/`Fin.cases`). 

Simpler: avoid unfolding `e0`,`e1` in the component goals. Instead, state the four evaluation lemmas for e0/e1 and prove them with `simp only [Pi.add_apply, Pi.smul_apply, Pi.neg_apply, smul_eq_mul, e0, e1, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons]`... but the issue is the `0`/`1` component evaluation. Let me find the right simp lemmas: for `![a,b] : Fin 2 → α`, mathlib has `Matrix.cons_val_zero : (vecCons a s) 0 = a` and `Matrix.cons_val_one : (vecCons a s) 1 = s 0`, plus `Matrix.head_cons : (vecCons a s).head = a`, `Matrix.tail_cons`.

The printed term `Matrix.vecHead (Matrix.vecTail fun i => 0)` suggests `(fun i => 0 : Fin 2 → ℝ) 1` was expressed as vecHead (vecTail ...). Hmm, `fun i => 0` is not cons-based; `Fin 2` is `Fin (1+1)`, and `simp` may have converted `(fun i : Fin 2 => 0) 1` into `Matrix.vecHead (Matrix.vecTail ...)` via some `Fin.cases` lemma. Adding `Matrix.vecHead`/`Matrix.vecTail` simp lemmas may not help since the argument is a lambda.

Better approach: prove evaluation lemmas about the basis vectors in a form that avoids component extraction ambiguity: e.g., prove `milnor_e0_e0` by `rw [milnor_apply_general]` then use `LinearMap.ext`/`funext i; fin_cases i` but simplify with `Fin.cases` lemmas. Or use the `decide`-free `norm_num` after `simp only [Fin.isValue, ...]`.

Alternative strategy: since `Fin 2` has decidable equality and the functions are `![...]`, we can use `ext i; fin_cases i` and then the goals contain `(fun _ => ...) 0` etc. The specific failing terms are due to `e0`/`e1` being `![...]` whose `simp` unfolding produced `vecHead/vecTail` forms. Let me try `simp only [Pi.add_apply, Pi.smul_apply, Pi.neg_apply, smul_eq_mul, e0, e1, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.tail_cons, Matrix.vecHead, Matrix.vecTail]` and then `norm_num`. If `Matrix.vecHead`/`vecTail` are not simp lemmas, maybe `Matrix.vecHead` is a def for `Fin.cons`...

Actually the terms `Matrix.vecHead (Matrix.vecTail fun i => @OfNat.ofNat ℝ 0)`: this is `(fun i : Fin 2 => (0:ℝ)) 1` where `Fin 2` was decomposed via `Matrix.vecCons`/`vecHead`/`vecTail` — these are notation-alias definitions for `Fin.cons`-like operations. `Matrix.vecHead` and `Matrix.vecTail` are defined in `Mathlib.Data.Matrix.Notation`: `vecHead (v : Fin (n+1) → α) := v 0`, `vecTail v := v ∘ Fin.succ`. There should be simp lemmas `Matrix.vecHead_cons`, `Matrix.vecTail_cons`, but here the argument is a *lambda*, `fun i => 0`, not a cons. So `vecHead (fun i => 0) = (fun i => 0) 0 = 0` by beta — `simp` should beta-reduce. Unless `Matrix.vecHead` isn't reducible to application (it may be defined via `Fin.cases`), and the lambda is `fun i => 0` where `i : Fin 2`... 

Let me just test a minimal snippet to find the working simp set for goal `(fun i : Fin 2 => (0:ℝ)) 1 = 0` and `Matrix.vecHead (Matrix.vecTail fun i => 0) = 0`. The latter: `Matrix.vecTail v : Fin n → α` with `n=1`; `vecHead (w : Fin 1 → α) = w 0`. `vecTail (fun i : Fin 2 => 0) = fun j : Fin 1 => (fun i => 0) (Fin.succ j) = fun _ => 0`. So `vecHead (vecTail f) = (fun _ : Fin 1 => 0) 0 = 0`. `simp` should do beta reduction if `vecHead`/`vecTail` unfold. Add them explicitly to the simp set: `Matrix.vecHead, Matrix.vecTail` (they're defs; `simp only [Matrix.vecHead, Matrix.vecTail]` unfolds them).

Let me test in /tmp/t.lean with these goals.
dsh: reasoning:
The second example works: `simp only [Matrix.vecHead, Matrix.vecTail]; norm_num` closes `Matrix.vecHead (Matrix.vecTail (fun i => 0)) = 0`. So adding `Matrix.vecHead, Matrix.vecTail` to the component tactic should work.

Let me update the component proofs:
```lean
  funext i
  fin_cases i <;> simp only [Pi.add_apply, Pi.smul_apply, Pi.neg_apply, smul_eq_mul,
      Matrix.vecHead, Matrix.vecTail, e0, e1] <;> norm_num
```
Hmm, the current tactic is `fin_cases i <;> simp [e0, e1] <;> norm_num` and the remaining goals are exactly the vecHead ones. So adding `simp only [..., Matrix.vecHead, Matrix.vecTail]` before norm_num should close. Let me change all five `fin_cases i <;> simp [e0, e1] <;> norm_num` to
`fin_cases i <;> simp only [Matrix.vecHead, Matrix.vecTail, e0, e1, Pi.add_apply, Pi.smul_apply, Pi.neg_apply, smul_eq_mul] <;> norm_num`.

Also remove the now-unneeded `module` at line 118 (simp only closed it) — the error "No goals to be solved" at 118 means `simp only [...]` closed the goal and then `module` errored. Remove `module`.

And in the final theorem, lines 155/157 "No goals to be solved" — the `rw [show ... by rw [map_neg, ...]; rfl, ...]` items: some of the `rfl` are unnecessary because `rw` already closed. Specifically `show milnorConnection m b e0 (-e1) = -e0 by rw [map_neg, milnor_e0_e1]; rfl` → the `rw` closes the goal, then `rfl` has no goals. Remove `; rfl`. Same for the second. The third one (map_add) maybe compiles.

Let me apply these fixes.
dsh: reasoning:
The `simp only` with vecHead/vecTail isn't reducing these goals. The term `Matrix.vecHead (Matrix.vecTail fun i => @OfNat.ofNat ℝ 0)` — note the function `fun i => 0` has no explicit type; `@OfNat.ofNat ℝ 0 Zero.toOfNat0`. `Matrix.vecTail` expects `Fin (n+1) → α`; here it's being applied to a lambda whose domain is ambiguous, so `simp only [Matrix.vecTail]` may not unfold because of implicit-argument unification issues.

A more reliable approach: use `norm_num` with `Matrix.vecHead`/`Matrix.vecTail` *and* `Fin.succ`/`Fin.cases` lemmas, or better, avoid generating these terms in the first place.

Alternative: change the component tactic to `fin_cases i <;> simp <;> norm_num` (full simp), which earlier produced these vecHead terms but then `norm_num` couldn't close. Let me try `fin_cases i <;> simp <;> norm_num` with explicit `show` to force beta? Hmm.

Actually, maybe the cleanest: prove the evaluation lemmas by `decide`-like computation using `funext` + `Fin.forall_fin_two`? Or use the `Matrix.cons` API directly: state the goals as `![...] = ![....]` equalities and use `simp [Matrix.cons_val_zero, Matrix.cons_val_one]`.

Wait. Look at the residual goal: `1/2 * (vecHead (vecTail fun i => 0) - 1 - 1) = -1`. The inner term is `(e1 1)` or similar expressed as `vecHead (vecTail (fun i => 0))`. If I add the simp lemma `Matrix.vecHead` in *unfolded* form maybe it needs `Matrix.vecHead_eq`? Let me check what `Matrix.vecHead`/`Matrix.vecTail` are in this mathlib version: maybe they're notation-only and there are lemmas `Matrix.vecHead_cons`, `Matrix.vecTail_cons`, `Matrix.vecHead_zero`? Let me grep mathlib for their definitions.

Actually, a much simpler fix: make `e0`, `e1` definitions that are NOT `![...]` but explicit pattern-matching functions with `[simp]` lemmas:
```lean
def e0 : Fin 2 → ℝ := fun i => if i = 0 then 1 else 0
```
Then `e0 0` and `e0 1` evaluate by `simp` (decide the if). Hmm, still needs `Fin` decidability reduction, but `simp` with `Fin.isValue` handles `(0 : Fin 2) = 0` etc.

Or even simpler: define via `Pi.single`:
```lean
def e0 : Fin 2 → ℝ := Pi.single 0 1
def e1 : Fin 2 → ℝ := Pi.single 1 1
```
with `[simp]` lemmas `Pi.single_eq_same`, `Pi.single_eq_of_ne`. Evaluation: `(Pi.single 0 1 : Fin 2 → ℝ) 0 = 1` ✓; `(Pi.single 0 1) 1 = 0` (by `Pi.single_eq_of_ne (by decide)`) ✓.

But then the orthonormality proof and the other lemmas (bracket2_e0_e1 etc. in the main R6 file) use `![...]`. In the scratch, I can use `Pi.single`-based definitions. However, the main R6 consumer file already has `![...]`-based e0/e1? No — the main file uses `![1,0]` inline. For the scratch, I can use whatever.

Actually, hold on. Let me reconsider whether this curvature computation is worth more time. It's a nice-to-have addition. I've spent ~20 minutes on it. The value: showing the closure produces nonzero curvature in the non-invariant model. It's a genuinely new mathematical statement about the model, but it is not required for the audit's core conclusions (the closure is already confirmed). The risk: more time debugging.

Given I have lots of time budget (used ~1h of 4h), I can afford a bit more. But let me time-box it: try the `Pi.single` approach quickly; if it doesn't work in ~10 minutes, drop it.

Actually, a cleaner idea: keep `![...]` but prove the four values using `funext i` + `fin_cases i` + `simp only [e0, e1, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.head_fin_const, Matrix.tail_cons]`... The residual term `Matrix.vecHead (Matrix.vecTail fun i => 0)` arises from `simp`'s rewriting of `![1,0] 1`: `![1,0] 1` should be `0` via `Matrix.cons_val_one`. But `simp [e0, e1]` unfolded e0 to `![1,0]` and then... the residual shows `vecHead (vecTail fun i => 0)` — that's `Matrix.cons_val_one` applied in reverse? It looks like `simp` normalized `![1,0]` to `vecCons 1 (vecCons 0 vecNil)` and `(vecCons 1 v) 1` to `vecHead (vecTail (vecCons 1 v))` = `vecHead (vecCons 0 vecNil)` = 0... but it got `vecHead (vecTail fun i => 0)` — meaning the tail reduced to `fun i => 0` (the constant-zero function) rather than `vecCons 0 vecNil`. Then `vecHead (fun i => 0 : Fin 1 → ℝ) = 0` should hold by `Matrix.vecHead` unfolding to `(· 0)` and beta. The problem is `simp only [Matrix.vecHead]` may not unfold because `Matrix.vecHead` is a protected/aliased def with different unfolding. Let me check its definition in mathlib.

Let me grep.
dsh: reasoning:
There's a lemma `head_fin_const (a : α) : (vecHead fun _ : Fin (n + 1) => a) = a`. That's exactly what I need: `Matrix.head_fin_const`. Let me add `Matrix.head_fin_const` to the simp set.

Also the other residual `Matrix.vecHead (Matrix.vecTail fun i => @OfNat.ofNat ℝ 1)` → `head_fin_const 1` gives 1. 

Let me update the component tactic to include `Matrix.head_fin_const`. Actually simplest: `fin_cases i <;> simp [e0, e1, Matrix.head_fin_const] <;> norm_num`. Let me test.
dsh: reasoning:
Still stuck on the same goals. The term is `Matrix.vecHead (Matrix.vecTail (fun i => (0:ℝ)))` where the inner function's domain is `Fin (n+1)` — but which n? For `![1,0] 1`, the tail is `fun i : Fin 1 => 0`. `head_fin_const` requires the lambda to have domain `Fin (n+1)` — it should match with n=0. Maybe `simp` doesn't rewrite because the expression is `(fun i => 0) 1`? Actually the printed term shows `Matrix.vecHead (Matrix.vecTail fun i => @OfNat.ofNat ℝ 0)`. `head_fin_const` pattern: `vecHead (fun _ : Fin (n+1) => a)`, i.e., it applies to `vecHead` of a *direct* lambda, not `vecHead (vecTail ...)`. So I need `vecTail (fun i : Fin 2 => 0)` to beta-reduce to `fun i : Fin 1 => 0` first — `Matrix.vecTail` is defined as `fun v => v ∘ Fin.succ` maybe; simp should beta-reduce but the implicit `n` in vecTail might be a metavariable.

Let me try explicitly: `simp [Matrix.vecTail, Matrix.vecHead, Matrix.head_fin_const, e0, e1]`. Earlier I tried `simp only [Matrix.vecHead, Matrix.vecTail]` and it said unused (meaning the terms weren't rewritten by them). Maybe `vecTail` needs its equation lemma `Matrix.vecTail_cons` or `Matrix.vecTail_eq`? Let me check the definition at line 92-100 of VecNotation.lean.
dsh: reasoning:
`vecHead` and `vecTail` are simple defs (`v 0`, `v ∘ Fin.succ`), and `head_fin_const` is `rfl`. So the residual goal `Matrix.vecHead (Matrix.vecTail (fun i => 0)) = 0` should be closable by `rfl`/`simp` with `Matrix.vecHead, Matrix.vecTail, Function.comp` unfolding.

The issue: the printed term `Matrix.vecHead (Matrix.vecTail fun i => @OfNat.ofNat ℝ 0)` — note the lambda `fun i => 0` has an *inferred* domain that may be `Fin ?n`. If the metavariable is not resolved, `rfl` can't fire. In the printed goal, Lean shows `fun i => 0` without a type, so the domain is implicit.

Let me try `simp only [Matrix.vecHead, Matrix.vecTail, Function.comp_apply]; norm_num` or just `norm_num [Matrix.vecHead, Matrix.vecTail]`.

Alternatively, avoid the issue by proving the component facts with `funext` over the *concrete* function and using `decide`-based `Fin` case analysis. Hmm.

Actually, an entirely different and much simpler approach for the whole computation: use `simp` with the `[simp]` lemmas for `Matrix.cons` and express `e0`/`e1` as `![...]` but *never* let `simp` unfold them into `vecHead/vecTail`: instead, prove the four values by explicitly converting to component equations with `Matrix.cons_val_zero`/`Matrix.cons_val_one` and `Matrix.cons_val_succ`.

E.g.:
```lean
lemma milnor_e0_e0 : milnorConnection metric2 lie2 e0 e0 = -e1 := by
  rw [milnor_apply_general]
  ext i
  fin_cases i
  · -- i = 0
    simp [e0, e1]
  · -- i = 1
    simp [e0, e1]
```
The residual goals after `simp [e0,e1]` are exactly those vecHead terms. Let me try adding `Matrix.vecHead, Matrix.vecTail` to a `norm_num [...]` call:
`norm_num [Matrix.vecHead, Matrix.vecTail, e0, e1]`.

Let me test this on the failing goal directly in /tmp: `example : Matrix.vecHead (Matrix.vecTail (fun i : Fin 2 => (0:ℝ))) = 0 := by norm_num [Matrix.vecHead, Matrix.vecTail]`. Note: in my /tmp test earlier, `simp only [Matrix.vecHead, Matrix.vecTail]; norm_num` WORKED for this example. So the issue in the real goal must be that the lambda's domain is a metavariable (unresolved) — perhaps because `simp` produced `fun i => 0` where the domain was inferred from `vecTail`'s argument whose type is `Fin ?n.succ`. The displayed goal hides it.

Hmm, in the real goal the term is `Matrix.vecHead (Matrix.vecTail fun i => @OfNat.ofNat ℝ 0)`: `vecTail` takes `v : Fin n.succ → α`; the lambda `fun i => 0` has domain determined by unification with `Fin n.succ` for a metavariable n. When `vecTail` is unfolded to `v ∘ Fin.succ`, the domain must be known; if n is a metavariable, the term may be stuck. Where would this metavariable come from? From `simp`'s rewriting of `![1,0] 1` using... hmm, maybe from `Matrix.cons_val_one`? `cons_val_one (x) (u : Fin m.succ → α) : vecCons x u 1 = vecHead u` — here m is a metavariable; for `![1,0] 1`, `u = ![0] : Fin 1 → α`, so m=0 is determined by unification with the type of u. But if `simp` used `cons_val_succ` instead: `vecCons x u i.succ = u i` where `i : Fin m`; for the index `1 : Fin 2`, `i = 0 : Fin 1`, m=1... then `u 0` with u = `![0] : Fin 1 → α` gives `0`. Hmm.

Rather than fight this, let me change the definition of e0/e1 in the scratch to avoid `![...]`:

```lean
def e0 : Fin 2 → ℝ := fun i => if (i : ℕ) = 0 then 1 else 0
def e1 : Fin 2 → ℝ := fun i => if (i : ℕ) = 1 then 1 else 0
```
Evaluation: `e0 0 = 1` by `simp [e0]`; `e0 1 = 0` by `simp [e0]` (needs `(1:Fin 2) ≠ 0` → `decide`/`norm_num`). Then `(fun _ => c) i = c` by beta. This avoids the matrix notation entirely.

But the model's `metric2.orthonormal` uses `Pi.basisFun` and would need `e0/e1` only in the evaluation lemmas — fine.

Even simpler: since `Fin 2 → ℝ` is `ℝ × ℝ`-ish, use `![...]` only in the main R6 file (where it's already working) and in the scratch use `Pi.single`:
```lean
def e0 : Fin 2 → ℝ := Pi.single 0 1
def e1 : Fin 2 → ℝ := Pi.single 1 1
```
`Pi.single 0 1` applied at 0: `Pi.single_eq_same`; at 1: `Pi.single_eq_of_ne (by decide)`. These are simp lemmas. But then `e0 + e1` etc. behave fine. The specific values for `metric2`/`bracket2` computations involve `X 0`, `X 1` — with X = e0, `simp [e0, Pi.single_eq_same, Pi.single_eq_of_ne]` should evaluate.

Let me test quickly: `example : (Pi.single 0 (1:ℝ) : Fin 2 → ℝ) 0 = 1 := by simp` and `... 1 = 0 := by simp`.

Let me test both approaches in /tmp and pick the working one.
dsh: reasoning:
Both approaches work standalone. So why does the real goal fail? Because the lambda's domain is an unresolved metavariable in the actual goal. 

Let me try to avoid the problematic `simp [e0, e1]` and instead compute using `Pi.single`-defined basis vectors in the scratch. I'll define:
```lean
abbrev e0 : Fin 2 → ℝ := Pi.single 0 1
abbrev e1 : Fin 2 → ℝ := Pi.single 1 1
```
and replace all `![1,0]`/`![0,1]` usages in the scratch with e0/e1. The `metric2.orthonormal` doesn't depend on e0/e1. The lemmas `bracket_e0_e1` uses e0,e1.

Potential issue: `Pi.single 0 1 + Pi.single 1 1` vs `fun _ => 1`? For the general formula, we don't need e0+e1 = ![1,1]. `bracket_e0_e1 : lie2.bracket e0 e1 = e0 + e1` — prove by `funext i; fin_cases i <;> simp [bracket2, e0, e1]`. Should be fine.

For the component evaluations, use `simp [e0, e1]` — Pi.single evaluation should reduce.

Hmm, but will `simp [e0, e1]` in the component goals produce clean goals? Let's try: replace the abbrevs and recompile.

Actually wait — there might be a subtlety: `Pi.single 0 (1:ℝ)` requires `DecidableEq (Fin 2)` (available). `simp` uses `Pi.single_eq_same` and `Pi.single_eq_of_ne`.

Let me modify the scratch:
- `abbrev e0 : Fin 2 → ℝ := Pi.single 0 1`
- `abbrev e1 : Fin 2 → ℝ := Pi.single 1 1`
- In `adT_apply` and elsewhere, references to e0/e1 stay.

Let me apply and compile.
dsh: reasoning:
Progress — no errors listed in the head, but rc=1. Let me see the whole output (maybe there's an error further down).
dsh: reasoning:
Excellent — the only remaining errors are the "No goals to be solved" at lines 154/156, which are the trailing `rfl` in the two `show ... by rw [...]; rfl` — wait, I removed those `rfl`s. Let me look at lines 150-160. It's probably in the `show milnorConnection metric2 lie2 (e0 + e1) e0 = -e1 + -e1 by rw [...]` — the `rw` closes it and then something else. Let me view.

Interesting: the theorem's axioms show clean cones (propext, Classical.choice, Quot.sound) despite the errors, because the errors are in a proof that still elaborated? Actually if there's an error the theorem isn't added... but the `#print axioms` shows the cone — meaning the declaration exists with a sorry? Hmm, it says depends on axioms [propext, Classical.choice, Quot.sound] — no sorryAx — but rc=1 means errors. Wait, if the proof has unsolved goals, Lean reports errors and the declaration may still be compiled with `sorryAx`? Earlier errors showed sorryAx. Here no sorryAx, so the proof term is complete and only the `rfl` after `rw` is extraneous (a tactic error but proof complete? no — "No goals to be solved" is an error that invalidates the declaration... yet the axioms print shows no sorryAx, which suggests the declaration was accepted before the error? Hmm, tricky. Let me just fix the leftover `rfl`s.

Let me view lines 148-160.
dsh: reasoning:
My earlier replacement of `; rfl` didn't apply because the text now reads `rw [map_neg, milnor_e0_e1]; rfl` (with a semicolon on the same line). Let me remove those trailing `; rfl`.
dsh: reasoning:
The curvature computation compiles: rc 0 with clean cones `{propext, Classical.choice, Quot.sound}`.

I verified by hand that R(e₀,e₁)e₀ = 2e₁; Lean confirms. That's a genuine new mathematical fact: the Milnor connection in the non-bi-invariant model has nonzero curvature.

Now, should I integrate this into the main R6 consumer file, or keep it as `ScratchCurv.lean`? Cleaner: rename to a proper consumer file, e.g. `A3ExtraR6/CurvatureNonzero.lean`, and record it as a separate artifact (a seventh independent declaration set). Let me:
1. `git mv`-like: copy ScratchCurv.lean → `A3ExtraR6/CurvatureNonzero.lean` with namespace `A3R6Curv`, remove the `trace_state`/debug remnants (there are none), and delete ScratchCurv.lean to avoid clutter? Or keep both? Keep one canonical: `CurvatureNonzero.lean`, delete the scratch.
2. Compile it, record log + rc + hash.
3. Add to results JSON as `a3_round6_curvature_declaration` / extend `new_independent_closure_consumers_round6` with a second entry, and update the MD §14.3.
4. Re-run the self-audit (the new file hash must be recorded in round6_artifacts).

Note: the file duplicates the model definitions from `ClosureUseNoninvariant.lean` (dot2, bracket2, lie2, metric2, e0, e1). That's fine (independently compiled; namespaced A3R6Curv). Actually — could I instead import ClosureUseNoninvariant and reuse its definitions? That would be cleaner and shorter: `import A3ExtraR6.ClosureUseNoninvariant`? The file is not a module in the package's library? `lake env lean` compiles files directly; importing a file by its path requires it to be part of the package's module tree. `A3ExtraR6/ClosureUseNoninvariant.lean` → module name `A3ExtraR6.ClosureUseNoninvariant`, but is it in the lakefile's glob? The package lakefile likely has `lean_lib` with `globs = ["Poincare", ...]` or default globs of all .lean. Since the build log showed 3766 jobs after adding the file, it seems lake picked it up. So importing might work if the module is built. But my invocation compiles these files with `lake env lean` directly, and the import would need the olean. Simpler and more robust: keep the file self-contained (duplicate definitions). That also makes the two probes independent.

Let me finalize the file: rename namespace to A3R6Curv and remove the `#print axioms` line? Keep it for evidence. Delete the scratch.

Then compile with a logged run.
dsh: reasoning:
The `end A3R6Scratch` line wasn't replaced. Let me fix it and recompile.
dsh: reasoning:
rc 0, clean cone. Let me also count the auxiliary lemmas in the file (adT_apply, milnor_apply_general, the four milnor_e*, bracket_e0_e1, curvature_e0_e1_e0) — 9 declarations in namespace A3R6Curv. Let me extract declaration names from the file for the results.

Then update:
1. results JSON: add `a3_round6_curvature_probe` + extend `round6_artifacts` with the new file/log; also add the declarations to `a3_round6_declarations`.
2. results MD §14.3: mention the curvature computation.
3. checkpoint: refresh.
4. Re-run self-audit.

Let me first list the declarations and their cones. I'll add `#print axioms` lines for all declarations? Currently only the main theorem. For evidence, let me print axioms for all 9. Let me append the extra `#print axioms` lines and recompile — better evidence. Actually the auxiliary lemmas are just intermediate; the key one is the theorem. But for completeness, printing all is nice. Let me add them (the file already has `#print axioms A3R6Curv.curvature_e0_e1_e0`). I'll add the others.

Actually, keep it simple: the main theorem's cone is the evidence; the helpers are structure lemmas. I'll record the theorem plus note that the file has 9 declarations.

Let me get the list of declarations via grep.
dsh: reasoning:
Good. Now update the results JSON and MD with this new probe.

JSON:
- `a3_round6_curvature_probe`: file, sha256, log, log_sha256, compile_rc, declarations (list), cones, description.
- `a3_round6_declarations`: add these names with cones.
- `round6_artifacts`: add CurvatureNonzero.lean + log.

MD §14.3: add a paragraph about the second probe.

Let me write the JSON update via python (results JSON has been read/edited by me; I'll use python as before).
dsh: reasoning:
Now re-run the self-audit to verify the new round6_artifacts entries (CurvatureNonzero.lean + log). The verifier's `round6_artifacts` loop searches under HERE (audit360) for names: "CurvatureNonzero.lean" is at audit360/pkgs/D12-connection-curvature/A3ExtraR6/CurvatureNonzero.lean — the loop first tries `os.path.join(HERE, name)` (doesn't exist), then walks WT for a file with that basename → will find it. Good. The log name likewise.

Let me run the verifier.
dsh: reasoning:
775 hashes verified, 0 mismatches, 0 unresolved.

Note: the `a3_round6_curvature_probe` file/log hashes are recorded; the verifier covers round6_artifacts. ✓

Let me update `self_audit_round6` in the JSON to 775 (currently says 773). Also update the checkpoint. Then final review.

Also: the round-6 sweep logs are now joined by an extra probe (CurvatureNonzero) that wasn't part of `round6_summary.json` (generated before). Let me regenerate `round6_summary.json` to include the new extras rc — but that changes its hash, which is recorded in the JSON `reverification_round6.summary_sha256` and checkpoint and round6_artifacts. I'd have to refresh all. Alternatively, keep the summary as of the sweep (it's a snapshot of the sweep run) and record the extra probes separately (already done in `a3_round6_curvature_probe` and `new_independent_closure_consumers_round6`). That's cleaner: the summary documents the sweep; the two extra probes are documented separately as post-sweep artifacts. But then `round6_summary.json` won't list them in extras — acceptable and accurate (they were added after the sweep). Hmm, but for clarity I could add a note. The MD explains it.

Actually, since `round6_summary.json`'s extras_rc for connection-curvature currently lists `extraR3.ClosureUse`, `extraR3.F1Validation`, `extraR6.ClosureUseNoninvariant` — because that file was generated after I added ClosureUseNoninvariant but before CurvatureNonzero. Let me just regenerate it with all extras now and refresh the recorded hashes (JSON + checkpoint). It's a small bookkeeping task; being consistent is better.

Plan:
1. Regenerate round6_summary.json.
2. Update JSON: `reverification_round6.summary_sha256` = new hash; `round6_artifacts['round6_summary.json']` = new hash; also `reverification_round6.cards[*].extras_rc` — wait, the summary now includes extras; my `reverification_round6` block iterates cards and copies specific fields, not extras. Fine.
3. Re-run update_results_round6.py? NO — that script would overwrite the round-6 blocks I've since enriched (curvature probe, closure confirmations appended after, etc.). Better to do targeted updates in python.
4. Then re-run verifier; update self_audit_round6 count.

Let me do the regeneration and targeted hash refresh.
dsh: reasoning:
Summary refreshed with both R6 extras at rc 0. Note the checkpoint also records the old summary hash — I'll refresh the checkpoint at the end.

Wait: `round6_summary.py` is recorded in round6_artifacts with its own hash — unchanged ✓. And `round6_summary.json` hash refreshed ✓.

Let me re-run the verifier to confirm everything is still consistent after the JSON edits (the JSON itself isn't hashed, but the records must match).
dsh: reasoning:
All good. Now let me do a final quality review of the deliverables:

1. Read the results .md from the top (header) and the §14 section to check for errors/inconsistencies.
2. Check the JSON status and required fields.
3. Update checkpoint (status blocked, refreshed hashes, elapsed/cumulative).
4. Final answer.

Let me also consider: the results .md mentions "sixth independent closure consumer" and now there's also a seventh probe (curvature). The MD §14.3 says "this is the sixth structurally distinct consumer" — accurate (the curvature probe is not a consumer of the closure per se but a computation of the connection it produces; it does consume `milnorLeviCivitaData`). Let me make sure the wording is consistent.

Let me read the header and §14.1-14.3 quickly.
dsh: reasoning:
The header is good. Let me update the "Elapsed" line to reflect the final elapsed (~1.0 h this invocation, ~3.8 h cumulative?) — actually let me compute: session started 14:39, now ~15:10 → 0.5h. Cumulative was 2.75 + 0.5 = 3.25. Let me set elapsed 0.6 / cumulative 3.35 (consistent with checkpoint). I'll update both at the very end.

Now let me do a broader final consistency check on the results JSON: ensure the required deliverable fields exist and are non-empty, and that statuses are coherent.

Then update the checkpoint with final values and hashes.

Let me also run a last `git status`-like check to see what files I've added (not a git repo? let me check). Not needed.

One more consideration: the task says "Only your own longrun/results/D13-cross-audit-360-cards.md can complete this task." My final answer will be a summary; the actual completion state is TASK_BLOCKED, documented in the md.

Let me do the final JSON check.
dsh: reasoning:
The JSON is complete. `generated_at` is 14:49:45 — I should refresh it to the final time. Let me update generated_at, elapsed_hours, cumulative_task_hours at the end.

Now, final steps:
1. Update checkpoint with final state (status blocked, refreshed deliverable hashes, elapsed).
2. Refresh `generated_at` and elapsed in the JSON and md header (Elapsed line).
3. Re-run the verifier one final time (JSON changes don't affect hashes).
4. Final answer.

Let me also double check the results md ends with §14.8 and has no truncation. Let me check the tail.

Also, one thing worth doing: the checkpoint `round6` block still says the old cold_rebuild description etc. — it's fine, but let me refresh `deliverables_sha256` with the current hashes.

Let me do a comprehensive final update.
dsh: reasoning:
The md is complete. Note §14.8 says "the one claimed non-trivial closure (CC)" — actually there are 4 claimed closures (1 CC + 3 SR). The CC one is the *existence* closure; SR closures are constructive homeomorphism/covering facts. Let me reword slightly to "the CC existence closure (the one closure of an existence statement)". Minor but let me fix for accuracy.

Then final bookkeeping.
dsh: reasoning:
Note the timestamp says 14:56 — interesting, my earlier `date` showed 15:10 but the system clock returns 14:56 now? Maybe time moved differently or I misread. Let me check the current time. If it's 14:56, then elapsed ≈ 0.3h since 14:39. I set elapsed 0.6 / cumulative 3.35, slightly overstated but fine. Let me check `date`.

Also the results md "Elapsed" header line says ≈0.4 h / ≈3.2 h cumulative; checkpoint says 0.6/3.35. Let me align: set md to ≈0.6 h and ≈3.4 h cumulative, or check actual time first.
dsh: reasoning:
The current time is 14:56. So the session started at ~14:39, meaning ~0.3h elapsed. My earlier reads of "15:10" were wrong (I misread; probably it was 14:50-ish). So actual elapsed ≈ 0.3h, cumulative ≈ 3.05h.

Let me set the numbers accurately: elapsed_hours = 0.3, cumulative = 3.05. Update results md header, results JSON, and checkpoint.

Let me fix all three.
dsh: reasoning:
All aligned. Now a final verification pass:
1. Run the self-audit once more (JSON edits don't affect hashes, but let me confirm).
2. Quick final check that the checkpoint is valid JSON and status "blocked".
3. Confirm the two missing cards are still absent (final re-check, to report honestly).

Then write the final answer summarizing the round-6 work and the blocker.

Actually, since I have ~3.5h of budget left, should I do more? Let me consider the value of additional verification once more.

The core deliverable is complete and thorough: 6 sweeps, cold rebuilds of all 7 cards, 775-hash self-audit, closures confirmed with 6+ independent consumers, forbidden scan, deep reviews, missing-card determination with transport evidence.

One remaining thought: the checkpoint says status "blocked". The task's final instruction: "End TASK_DONE only if the milestone is fully checked; otherwise TASK_BLOCKED or checkpoint and TASK_IN_PROGRESS." Ending TASK_BLOCKED is right.

Could I attempt to further pin the mathematical blocker (rather than transport)? The named blocker A3 relates to D2/D3 defects, which the previous rounds established. This invocation's objective was the 9-card re-verification. The remaining blocker is the 2 missing cards + the unrepaired D2/D3 defects (for A3). I've documented both.

One more useful thing: since the missing cards are remote_owned, maybe I can check whether the *dispatch* queue has any updated status in the last minutes (the queue updated_at was 14:47:19 — recent!). Let me check if the queue changed and whether the two cards got any new status. If the remote hosts pushed something in the meantime, I'd want to catch it. Let me do a final check of workspaces and queue.

Also, let me check the state directory for any new DONE/PUSHED markers for those cards, and re-test port 10022 one last time.

Then finish.
dsh: reasoning:
Final check confirms: no artifacts, no transport route, both cards still remote_owned. The milestone remains blocked.

Let me run the self-audit one final time and then verify the checkpoint JSON parses, then finish.
dsh: reasoning:
Everything is consistent and verified:
- checkpoint status "blocked", invocation 5, elapsed 0.3h / cumulative 3.05h.
- results JSON status TASK_BLOCKED.
- results md header TASK_BLOCKED.
- self-audit 775 hashes, 0 mismatches.

Let me update the todo list to completed and write the final answer.

Final summary for the user:
- Round 6 (invocation 5) complete.
- Verified: round-6 sweep (5th consecutive byte-identical), all 7 cards cold-rebuilt, new adversarial CC consumer + curvature computation, wider forbidden scan, self-audit 775 hashes.
- Closures: CC confirmed (strengthened), SR 3 closures confirmed (recompiled).
- Missing: 2 cards source-absent, no transport route → TASK_BLOCKED.
- Deliverables updated.

Let me mark todos complete.
Round 6 (invocation 5) of the A3 adversarial cross-audit is complete. Final state: **TASK_BLOCKED**, checkpointed and current.

## What this invocation added

**1. Round-6 sweep — fifth consecutive byte-identical re-verification** (`audit360/run_round6.sh` → `logs-round6/`, `round6_summary.json` sha `edcba7c6…`)
- All 7 available cards: `lake build` rc 0, `A3Probe` rc 0, `A3FullAudit` rc 0 PASS, `A3KindAudit` rc 0.
- **342 probe cones, 1103 declarations PASS**; build/probe/full-audit byte-identical to round 5, kind screen byte-identical to the round-5 screen; probe/full-audit generator hashes unchanged.
- Negative control still flags `sorryAx` and the private `native_decide` axiom; snapshot rebuild + A3 D2/D3 probes rc 0.

**2. All seven cards now cold-rebuilt from source** (`run_cold_round6.sh` → `logs-cold6/`, `cold_rebuilds_round6.json`)
- Round 1 covered connection-curvature and surgery-recognition; round 6 cold-rebuilt the other five (volume-ibp 78, spectral-sobolev 36, semantic-ledger 7, comparison-geodesics 33, geometric-compactness 39) — every one with `.lake/build` confirmed absent, build rc 0, probe rc 0. **All 342 card-declared entries now have a from-source cold-build witness.**

**3. New adversarial closure checks (connection-curvature)**
- `A3ExtraR6/ClosureUseNoninvariant.lean` (sha `2430f67c…`, rc 0): builds a concrete **provably non-bi-invariant** 2D model; the closure holds there, the mean connection is *not* metric-compatible, Milnor ≠ mean, plus a **new uniqueness corollary** `d.nabla = milnorConnection`.
- `A3ExtraR6/CurvatureNonzero.lean` (sha `2b63ecb6…`, rc 0): computes **R(e₀,e₁)e₀ = 2·e₁ ≠ 0** — the closure output is genuinely curved, not degenerate. All cones `{propext, Classical.choice, Quot.sound}`.

**4. Independent checks**: wider forbidden-token scan (only the documented volume-ibp negative-control axiom; 56 `partial` hits are this audit's own metaprogram), producer cards frozen 14/14, deep reviews of spectral-sobolev and connection-curvature REVIEWED-CORRECT, self-audit **775 hashes / 0 mismatches**.

**5. `exact_blockers_closed`**: CC closure **CONFIRMED (strengthened)**; the three surgery-recognition closures **CONFIRMED** (consumers recompiled rc 0). No refutations.

## Remaining blocker (unchanged, re-verified 14:56)
`D12-tensor-maximum-bochner` (360-1) and `D12-triangulation-topology` (360-2) remain `remote_owned` with no worktree, package, card, module or state marker; the relay (`bin/relay_push.sh`, reverse tunnel `127.0.0.1:10022`) is refused and tailscale shows no 360 peer. They are **source-absent, not refuted** — a transport blocker, so the 9-card milestone is not fully checked and this invocation ends TASK_BLOCKED rather than claiming completion.

Deliverables updated and hash-pinned: `longrun/results/D13-cross-audit-360-cards.md` (§14 documents all round-6 evidence) and `.json`, plus `checkpoint.json` (status `blocked`, invocation 5).
