dsh: reasoning:
Let me start by exploring the worktree to understand what we're working with.

Let me look at the directory structure first.
dsh: reasoning:
The worktree is empty. Let me look at the parent directories to find the D4 evolution result. The task says "Consume the D4 evolution result." Maybe there's a shared area. Let me explore the longrun directory.
dsh: reasoning:
Let me look at the D4_evolution_theorem worktree and the longrun README, queue, results, and state.
dsh: reasoning:
Now let's look at the D4 evolution theorem worktree source files (excluding .lake build). Let me look at the source .lean files.
dsh: reasoning:
Let me read the D4 evolution theorem result card, the audit prompt, and the queue. Then I'll inspect the promoted interfaces.
dsh: reasoning:
Now let's read the D4 evolution theorem sources. Let me start with the key files: Gibbs.lean, Functional.lean, Continuous.lean, Discrete.lean, Counterexample.lean, Bridge.lean, Evolution.lean.

Let me read them in parallel batches.
dsh: reasoning:
Now let me read Continuous.lean, Discrete.lean, Counterexample.lean, Bridge.lean, and Evolution.lean.
dsh: reasoning:
Now let me read Discrete.lean, Counterexample.lean, Bridge.lean, Evolution.lean, and Audit/EvolutionAudit.lean. Also the D2 sources for CurvatureODE (Evolution, Monotonicity, Bridge) and D3 Entropy (Functional, Certificate, Bridge) to understand the interfaces.
dsh: reasoning:
Now Bridge.lean and Evolution.lean, plus the audit file.
dsh: reasoning:
Now I need to understand the dependencies: D2 CurvatureODE (Evolution, Monotonicity, Bridge, State), D3 Entropy (Functional, Certificate, Bridge). Let me read those in the D4 worktree (they're byte-identical copies).

Let me check the CurvatureODE directory files.
dsh: reasoning:
Now let me read the D2 CurvatureODE sources (Evolution, Monotonicity, State) and D3 Entropy (Functional, Certificate, Bridge) to understand the interfaces and find potential weaknesses. I want to be thorough about what "promoted interfaces" means. The D4 result card says the promoted interfaces are the D4 cluster. But the task says "imports only the promoted interfaces" — likely means import `Poincare.Longrun.Evolution` (the umbrella) rather than internal files or copies. Actually maybe it means import only the promoted D4 interfaces (i.e., the umbrella module) and not depend on uncommitted files.

Let me read the D2/D3 sources.
dsh: reasoning:
Now let's read D3 Entropy Functional, Certificate, Bridge, and CurvatureODE/State + Bridge (for the transfer statement). Let me also read the umbrella Evolution.lean.
dsh: reasoning:
Now let me read the umbrella Evolution.lean and check the D4 worktree build state. Also check the queue.json to understand promotion status. Also, importantly, check whether the promoted interface is committed / available in a shared location. The task says "imports only the promoted interfaces". The D4 result card mentions the shared results directory. But the D4 worktree itself is the source. Since my worktree is empty, I need to set up a project. The question: what are "promoted interfaces"? Probably the D4 cluster as described in the card, i.e. `Poincare.Longrun.Evolution` umbrella (and its dependencies). Since the integrator may not have copied files yet, I should copy the D4 worktree's Poincare library into my audit worktree, but import only `Poincare.Longrun.Evolution` (the umbrella) in my verifier file.

Let me check queue.json and state dir for promotion info.
dsh: reasoning:
Now let me check the D4 evolution worktree's latest.log for the exact build commands and whether the oleans exist. Also check the D4 audit out file. Also read the umbrella Evolution.lean.

The key task: build a verifier in my worktree. I need:
1. Set up the project: copy lakefile.toml, lake-manifest.json, lean-toolchain, and symlink .lake/packages to the prebuilt mathlib packages. Then copy the D4 Poincare library (which includes D2/D3/D4 sources) into my worktree — but "imports only the promoted interfaces". My verifier file should import only `Poincare.Longrun.Evolution` (the promoted umbrella). Copying the library sources is necessary to have the modules available; that's the environment, not my import. Actually the cleanest: copy the whole D4 worktree's Poincare dir into my worktree, then build. My verifier file imports `Poincare.Longrun.Evolution` only.

Let me first check what's in the D4 worktree's .lake and whether oleans can be reused. The D4 worktree's `.lake/build` exists with oleans for Poincare modules. But my worktree is separate. Options:
- Copy the entire D4 worktree (including .lake) to my worktree? The .lake/packages is a symlink to the shared prebuilt mathlib. The build artifacts are in `.lake/build/lib/lean/...`. If I copy them, Lean may consider them stale if paths differ? Oleans don't embed absolute paths generally, but there might be a problem with the package build state. Simpler: copy sources + symlink packages, then `lake build Poincare` — that might take a while (8913 jobs), but mathlib is prebuilt so only the ~40 Poincare modules need building. D4's clean build took 3.4s for the last module; the whole build 8913 jobs but mostly mathlib already built. Actually "Built Poincare.Longrun.Evolution (3.4s)" and "Build completed successfully (8913 jobs)" — 8913 jobs includes mathlib trace checks? Hmm, with prebuilt mathlib, lake checks all 8913 modules? That might take minutes but let's see.

Actually, maybe better: create my worktree project with `lean_lib` name Poincare but only include my audit file as a separate driver outside the library, and copy the whole Poincare source. Then `lake env lean Audit/...` will elaborate the imported modules from source, compiling dependencies as needed... Actually `lake env lean file.lean` only elaborates that file and its imports; if imports' oleans are missing, Lean will error (it doesn't build dependencies automatically; lake env lean doesn't fetch build deps? Actually `lake env lean` runs lean with the search path; imported modules must have .olean files. If not built, it fails with "object file ... does not exist"). So we need `lake build Poincare` first.

Alternatively, `lake build` of the library. Let's estimate: the D4 clean build says 8913 jobs and the build log. Let me check the build log timing.

Actually, the simplest robust approach: copy the D4 worktree's entire directory (sources + .lake/build oleans) into my worktree, excluding maybe the symlink, then rebuild. Let me check disk usage.

Let me check the size and whether the oleans are usable directly. The D4 worktree .lake/build has Poincare oleans. If I copy them, then `lake env lean` my verifier file in my worktree may work immediately if the source file paths/hashes match. Lake stores trace hashes; if sources are copied byte-identically, hashes match, and paths... oleans don't depend on absolute paths for imports (the module name is the key). However, there's `.lake/build/lib/lean/Poincare/...olean` and lake's build state. If I run `lake env lean`, it just sets LEAN_PATH to `.lake/build/lib/lean` and `.lake/packages/*/.lake/build/lib/lean`, so it should find the oleans. But wait — the sandbox: copying files from D4 worktree to mine is a read outside my workspace and write inside; allowed (workspace-write allows reads elsewhere presumably; the earlier bash commands read outside fine).

But there's a subtlety: The oleans for the D4 modules were built against the same mathlib revision, same toolchain. Copying them should be fine. But relying on copied oleans isn't a clean verification; an adversarial verifier should rebuild from source to make sure the oleans match the source. Best: copy sources, copy lake config, symlink packages, and run `lake build Poincare` fresh (rm -rf .lake/build). Let me check how long the D4 build took by looking at its log.

Let me look at the logs in D4 worktree.
dsh: reasoning:
Now let me check the build logs and audit out. Also check the D2 Bridge (for TensorRicciFlowODEBridge) and State for scalarOfState. Let me read the audit out and the build log tail.
dsh: reasoning:
Interesting: `du -sh` on the worktree shows 536K but .lake is 172M? Probably because `.lake/packages` is a symlink and `.lake/build` is local... Actually `du -sh` on the worktree should include .lake/build. Maybe du followed the order and the second result 536K is because .lake is a separate filesystem? Hmm, 172M for .lake (probably the symlinked packages counted? du doesn't follow symlinks by default). Wait, du -sh .lake reported 172M, and du -sh worktree reported 536K. That's odd unless .lake is a mount point or bind mount. Let me check `ls -la .lake` and mounts.

Actually it could be that `.lake/build` is huge and `.lake/packages` is a symlink; du -sh on the worktree should include .lake/build. Unless `.lake` itself is a symlink! Let me check. If `.lake` is a symlink to somewhere else, then du -sh worktree wouldn't traverse it (du doesn't follow symlinks). And `du -sh .lake` would follow the command-line symlink? Actually du does not follow symlinks given as arguments by default either... hmm, but with `-H`? Let me check.

Let me check the structure carefully. This matters for setting up my worktree.
dsh: reasoning:
The D4 worktree .lake/build is local (172M) and .lake/packages symlinks to poincare-lab. So I can reuse the D4 worktree's oleans by copying .lake/build into my worktree, or just symlink my `.lake/build` to the D4 worktree's `.lake/build`? No — I shouldn't rely on the builder's oleans; a clean verifier should rebuild from source. But rebuilding is fine: `lake build Poincare` with prebuilt mathlib packages. The clean build log showed 8913 jobs but most were already built (only last ~35 built in a few seconds). Wait, they deleted `.lake/build` first, so 8913 jobs means it built all mathlib modules? That can't be — "Built Poincare.Longrun.CurvatureODE.ScalarODE (2.2s)" at job 8879, then Poincare.Basic etc. Hmm, the numbering: 8879/8913 — so most of the 8913 jobs are mathlib modules being checked? When mathlib is prebuilt in .lake/packages/mathlib/.lake/build, lake still reports "Built" jobs for them? Actually lake's job counting includes dependencies. The log starts at 8879 because head -10 shows the tail? No, head -10 shows the first 10 lines, which begin at [8879/8913]. That means the log only contains the last ~35 lines — maybe the log was overwritten by the final build. Let me check line count of Poincare.clean.build.log.

Anyway. The best approach for a genuinely independent verifier:
1. Create my own worktree project: copy lakefile.toml, lake-manifest.json, lean-toolchain from the D4 worktree (or poincare-lab).
2. Symlink .lake/packages to the shared packages.
3. Copy only the promoted sources: `Poincare/Longrun/Evolution.lean` + `Poincare/Longrun/Evolution/*.lean` + their dependencies (D2 CurvatureODE, D3 Entropy, etc.). But "imports only the promoted interfaces" — my verifier imports only `Poincare.Longrun.Evolution`. The library itself needs its deps. I could copy the entire `Poincare/` directory from the D4 worktree, byte-identically, and then verify hashes against the result card.

Actually, an important adversarial check: verify the claimed sha256 hashes of the D4 files against the actual files. And check that D2/D3 copies are byte-identical to their accepted cards (the D2/D3 worktrees). Let me do that.

Then write my verifier file `Audit/CounterexampleAudit.lean` (outside Poincare tree, so not part of library) importing only `Poincare.Longrun.Evolution`, in a fresh namespace (e.g., `namespace D4Verifier`), and:
- Re-state/re-check the main theorems in a fresh namespace (using the imported declarations).
- Attempt small counterexamples to weakened hypotheses: 
  - c ≥ 0 but not ≥1: already in cluster; I should produce my own independent ones with different data (e.g., Fin 2, c = 1/2, or find a *correct* weakening: maybe the true necessary condition is weaker than 1 ≤ c? Let's analyze).
  - Weaken `0 ≤ h` in discrete theorem: find counterexample with h < 0. The cluster has one for negative step (h=-1) but with a negative reaction — they use squareField (eval = lam² ≥ 0), h = -1, starting lam=1 gives step to 1 + (-1)(1) = 0. They claim it's the "discrete shadow of a negative reaction" but actually the reaction is nonnegative; the step is negative because h<0. So that's a valid counterexample to h≥0 with nonnegative reaction. Good.
  - Weaken nonnegativity of reaction: D2 ReactionField forces eval ≥ 0, so we can't instantiate with negative reaction without changing the structure. But we can consider a "weakened hypothesis" statement: if we formulate the discrete theorem with only h ≥ 0 and no reaction nonnegativity, it fails. Can we produce such a counterexample in Lean without modifying the structure? We can define a trajectory and a "field" function directly rather than a ReactionField. The theorem perelmanF_step_le uses ev.step and F.eval_nonneg. A weakened version "for any F : ι → (ι→ℝ) → ℝ with h ≥ 0 and traj (n+1) i = traj n i + h * F (traj n) i, perelmanF c (traj (n+1)) ≤ perelmanF c (traj n)" is false when F can be negative. We can state and disprove it. E.g., ι = Fin 1, c = 1, h = 1, traj 0 = 1, traj 1 = 0 with F(traj 0) = -1. Then perelmanF 1 (traj 1) = gibbsTerm 1 0 = 1, perelmanF 1 (traj 0) = 2 e^{-1} ≈ 0.7358, so 1 > 0.7358: increase. Indeed with negative reaction, functional increases. That's a clean counterexample to the weakened hypothesis "F arbitrary (no nonnegativity)".

  - Weaken `EvolutionRelation` continuous-time: e.g., drop componentwise continuity? The D2 theorem `le_left_of_hasDerivWithinAt_nonpos` requires continuity on [s,t]. Without continuity, derivative sign doesn't give monotonicity (e.g., Cantor function has derivative 0 a.e. but is nondecreasing, so that's not a counterexample; need a function with nonpositive derivative everywhere but increasing? That's impossible for a.e. derivative... but with "hasDerivWithinAt at every point of Ico s t" and no continuity, can we have f(s) < f(t)? Actually Darboux property: derivatives have Darboux property, but here we only assume right derivative nonpositive at each point, and no continuity. Hmm, a function with right derivative ≤ 0 everywhere but not nonincreasing? Consider f(t) = -floor(t)? Right derivative at integers? At integer n, right derivative is 0? f jumps down at integers, so it's decreasing. To get increasing with right derivative ≤0: Let f(t) = 0 for t ≤ 0 and f(t) = t for t>0? Right derivative at 0 is 1 > 0. Hmm. A function that jumps up: f(t) = 0 for t ≤ 0, f(t) = 1 for t > 0. Right derivative at 0 is 0 (since (f(h)-f(0))/h = 1/h → ∞ actually). Wait HasDerivWithinAt at 0 from the right: (f(0+h) - f(0))/h = (1-0)/h = 1/h which → +∞, not a finite derivative. So no. A jump up of size finite at a point gives infinite right derivative, so not ≤ 0. Actually there's a classical fact: if f has right derivative ≤ 0 everywhere (finite) on an interval, then f is nonincreasing, even without continuity? Let's think: Darboux-like arguments. There's a theorem: if f is differentiable from the right everywhere with f'_+(x) ≤ 0 then f is nonincreasing (this is true, I believe, using the "MVT for right derivatives" or a covering argument). Actually the standard proof via le_left_of_hasDerivWithinAt_nonpos uses continuity? Let me look at the D2 lemma. It's in CurvatureODE/Invariant.lean or ScalarODE.lean. Let me read it. The lemma `le_left_of_hasDerivWithinAt_nonpos` is likely in Invariant.lean. Let me read.

  - Weaken the finite-index type assumption? Fintype is needed. If ι is infinite, the sum doesn't converge. Could find a counterexample with infinite sum? Not easily in Lean.

  - Weaken `perelmanF_step_lt` hypotheses: check each of 1 < c, 0 < h, 0 < F.eval is needed. Produce counterexamples showing failure when each is dropped:
    * Drop 1 < c (use c = 1): with c = 1, h > 0, F.eval > 0, strict decrease may fail? gibbsTerm 1 is strictly decreasing even though derivative vanishes at 1 (gibbsTerm_strictAnti requires 1 < c, but gibbsTerm 1 2 < gibbsTerm 1 1 holds; is gibbsTerm 1 strictly decreasing? Let's compute: gibbsTerm 1 x = (1+x²)e^{-x}; derivative = -(x-1)² e^{-x} ≤ 0 with zero only at x=1; so it's strictly decreasing on all of ℝ (derivative negative except at one point). So even at c=1, strict decrease holds for u>0! Wait, but perelmanF_step_lt requires 1 < c i for the *component i* and strict positivity of F.eval at i. At c=1, gibbsTerm 1 is still StrictAnti? Let's verify mathematically: derivative ≤ 0 and zero only at isolated point, so the function is strictly decreasing. Yes! So hypothesis `1 < c i` in `perelmanF_step_lt` may be overstrong (not sharp): the strict decrease actually holds with `1 ≤ c i` (since even at c=1, gibbsTerm is strictly decreasing). Hmm — is that right? f(x) = (1+x²)e^{-x}. f'(x) = -(x-1)² e^{-x} ≤ 0, equality only at x=1. A function with f' ≤ 0 everywhere and f' = 0 only at a point is strictly decreasing. Yes. So the statement `perelmanF_step_lt` could be weakened to `1 ≤ c i`. That's an "overstrong hypothesis" finding! The result card says "The strict version perelmanF_step_lt requires 1 < c i" and claims "all three are needed" in the hand-off. But actually `1 < c i` is not needed for strict decrease; `1 ≤ c i` suffices, because the flat spot is isolated. However, wait: the proof of gibbsTerm_strictAnti uses `strictAnti_of_deriv_neg`, requiring derivative strictly negative everywhere. With c=1, derivative vanishes at x=1, so that proof doesn't apply. But the statement is still true; it needs a different proof (e.g., mean value theorem with strict derivative negativity on all but one point, or direct algebra). Let me check: for x < y, is gibbsTerm 1 y < gibbsTerm 1 x? We can prove: gibbsTerm 1 is StrictAnti via `strictAnti_of_deriv_neg` doesn't apply. Alternative: use `strictAntiOn_of_deriv_neg` on intervals excluding x=1? Or note that for c ≥ 1, gibbsTerm c = gibbsTerm 1 + (c-1)e^{-x}, and (c-1)e^{-x} is strictly decreasing for c>1 and constant at c=1. Hmm, for c=1 specifically, direct proof: f(y) - f(x) = ∫_x^y f' < 0 since f' < 0 except at one point (Lebesgue integral). In Lean, can use `intervalIntegral.integral_neg` / `strictMonoOn_of_deriv_pos`? There's `strictAnti_of_deriv_neg'`? Hmm. Alternatively, there might be a lemma `strictAnti_of_deriv_nonpos`? Not standard. We could prove it directly with `sub_lt_iff_lt_add`... Actually simpler: For x < y, use the MVT with the derivative nonpositive and the strict negativity somewhere... A cleaner elementary route: f(x) = (1+x²)e^{-x}. Suppose x<y. If y ≤ 1: derivative negative on [x,y], so strict. If x ≥ 1: same. If x<1<y: f(x) > f(1) > f(y) by strict decrease on each side. So we can prove it by case analysis using `strictAntiOn_of_deriv_neg` on `(-∞, 1]` and `[1, ∞)`. That's doable but might be some work. Actually there's a simpler route: `gibbsTerm 1 x = (1+x²)e^{-x}` and compare: f(x) - f(y) = e^{-x}(1+x²) - e^{-y}(1+y²) > 0 for x<y? Multiply by e^{x}: (1+x²) - e^{x-y}(1+y²). Hmm, y-x>0, e^{x-y} < 1. Not immediately.

    Alternatively, maybe we can show `gibbsTerm c` is StrictAnti for c ≥ 1 using `strictAnti_of_deriv_neg`? No, derivative zero at x=1 when c=1.

    But here's the thing: an adversarial verifier finding "overstrong hypothesis" is valuable. The task says "records any false, vacuous, or overstrong statement; proposes a corrected theorem if needed." So finding `1 < c i` overstrong (can be weakened to `1 ≤ c i`) is a legitimate audit result. But I need to be careful: is `perelmanF_step_lt`'s hypothesis really overstrong? Let's double check with the actual proof: it uses gibbsTerm_step_lt (1 < c) for component i and gibbsTerm_step_le (1 ≤ c) for others. If we only have 1 ≤ c i and 0 < h, 0 < F.eval i, then perelmanF strictly decreases? The component i term strictly decreases (need the c=1 case), and other components weakly decrease. So yes, statement holds with 1 ≤ c i. But hold on — what if c i = 1 exactly and the increment u = h * F.eval(traj n) i > 0: gibbsTerm 1 (x+u) < gibbsTerm 1 x holds. Yes.

    So there is a genuine overstrong hypothesis. To "propose a corrected theorem", I can state `perelmanF_step_lt'` with `1 ≤ c i` and prove it in my verifier file (needs the strict antitonicity at c=1). That would be a nice, constructive correction. But proving gibbsTerm 1 strict anti in my file requires some analysis. Let me think about how to do it in Lean elegantly.

    Option: prove `gibbsTerm_strictAnti_of_one_le` in the fresh namespace using existing lemmas. There's a mathlib lemma: `strictAntiOn_of_deriv_neg` for intervals, or `strictMonoOn_of_deriv_pos`. Let me search mathlib for `strictAnti_of_deriv_neg` and related.

    Actually, there's a neat trick: For c ≥ 1, `gibbsTerm c x = gibbsTerm 1 x + (c-1) * exp(-x)`. If c > 1, `(c-1)exp(-x)` is strictly decreasing, and gibbsTerm 1 is nonincreasing (Antitone from gibbsTerm_antitone 1 (le_refl 1)), so sum is strictly decreasing. If c = 1, it's gibbsTerm 1 itself. So we need strict antitonicity of gibbsTerm 1. Can we get it from `strictAnti_of_deriv_neg` applied to... hmm, f(x) = (1+x²)e^{-x}. Note f(x) = (c + x²)e^{-x} with c=1. 

    Another approach: f(x) - f(y) for x < y. Use the mean value theorem: f(y) - f(x) = f'(ξ)(y-x) for some ξ ∈ (x,y). f'(ξ) ≤ 0, so f(y) ≤ f(x). For strictness we need f'(ξ) < 0, i.e., ξ ≠ 1. If ξ = 1, we don't get strict. But then we can iterate/refine: if ξ=1 then x < 1 < y; apply MVT on [x,1] and [1,y] to get f(1) < f(x) and f(y) < f(1) (on those intervals the derivative is strictly negative everywhere since 1 is an endpoint), so f(y) < f(1) < f(x). Actually on [x,1] (with x<1), f' < 0 on the open interval (x,1), so f(1) < f(x) by strictMVT. Similarly f(y) < f(1). That works.

    In Lean, there is `strictMonoOn_of_deriv_pos` and `strictAntiOn_of_deriv_neg`. Let me check their exact names in mathlib v4.34.0-rc2. In mathlib: `strictMonoOn_of_deriv_pos : Convex ℝ s → ContinuousOn f s → (∀ x ∈ interior s, 0 < deriv f x) → StrictMonoOn f s`. And `strictAntiOn_of_deriv_neg`. Also `antitone_of_deriv_nonpos`, `strictAnti_of_deriv_neg`. Let me grep mathlib.

    Actually, maybe simpler: use `strictAnti_iff_lt`? Hmm.

    Alternatively, prove the strict decrease via an explicit algebraic inequality. Let's see: for x < y and c ≥ 1:
    gibbsTerm c y < gibbsTerm c x
    ⟺ (c+y²)e^{-y} < (c+x²)e^{-x}
    ⟺ (c+y²) < (c+x²)e^{y-x}.
    Let d = y-x > 0. Need c+y² < (c+x²)e^d. Since e^d > 1+d+d²/2+d³/6:
    (c+x²)e^d > (c+x²)(1+d+d²/2+d³/6) = c + cd + cd²/2 + ... + x² + x²d + ...
    Compare to c+y² = c+(x+d)² = c+x²+2xd+d².
    Need: cd + x²d + ... > 2xd + d². With c ≥ 1: cd ≥ d. x²d + (terms) ≥ 0. Need d + ... > 2xd + d², i.e., need x² d + c d²/2 + ... ≥ 2xd + d² - d. Hmm if x is large positive, x²d dominates 2xd. If x negative, 2xd negative, fine. Let's see: (c+x²)(1+d) - (c+(x+d)²) = c + cd + x² + x²d - c - x² - 2xd - d² = cd + x²d - 2xd - d² = d(c + x² - 2x - d) = d((x-1)² + c - 1 - d). This is > 0 iff d < (x-1)² + c - 1. If d is small this holds; but for large d may fail; need higher order terms. Since (c+x²)e^d ≥ (c+x²)(1+d+d²/2+d³/6), the difference ≥ (c+x²)(1+d+d²/2) - (c+(x+d)²) = d((x-1)²+c-1-d) + (c+x²)d²/2 ≥ d((x-1)² + c-1-d) + d²(c)/2. Hmm if x=1,c=1: difference ≥ d(0 + 0 - d) + d²/2 = -d² + d²/2 = -d²/2 < 0. Need more terms: with d³/6: ≥ -d² + d³/6 = d²(d/6 - 1) which is negative for d<6. Not enough. Need full exponential. This algebraic route is messy. Better use calculus.

    Actually, the cleanest: use `strictAntiOn_of_deriv_neg` on `Iic 1` and `Ici 1` and case analysis. Let me plan:
    - hderiv: deriv (gibbsTerm 1) x = -((x-1)^2) * exp(-x).
    - On `Iio 1` (or `Iic 1`?), interior of `Iic 1` is `Iio 1`, derivative strictly negative there (since (x-1)²>0). So `StrictAntiOn (gibbsTerm 1) (Iic 1)`.
    - Similarly `StrictAntiOn (gibbsTerm 1) (Ici 1)`.
    - For x<y: case y ≤ 1 → strictAntiOn on Iic 1; case 1 ≤ x → on Ici 1; case x<1<y → f(y) < f(1) < f(x) using both.
    This requires convexity and continuity lemmas. `convex_Iic`, `convex_Ici`, `continuous_gibbsTerm`. Let me check the signature of `strictAntiOn_of_deriv_neg` in this mathlib version.

    Alternatively, mathlib might have `strictAntiOn_of_deriv_neg` only requiring `ContinuousOn f s` and `∀ x ∈ interior s, deriv f x < 0`. Fine.

    Hmm, but maybe there's an even easier path: `gibbsTerm 1 x = (1+x²)e^{-x}`; consider h(x) = e^{x} * gibbsTerm 1 x = 1+x²? No wait, gibbsTerm 1 x = (1+x²)e^{-x}. Define F(x) = (1+x²) and compare... For x<y: (1+y²)e^{-y} < (1+x²)e^{-x} ⟺ (1+y²)/(1+x²) < e^{y-x}. Let φ(t) = log(1+t²). Then log ratio = φ(y)-φ(x) = ∫_x^y φ'(t) dt = ∫ 2t/(1+t²) dt. Need < y-x, i.e., ∫_x^y (2t/(1+t²) - 1) dt < 0. And 2t/(1+t²) ≤ 1 for all t (since (t-1)² ≥ 0), with equality only at t=1. So the integral is < 0 when x<y (integrand ≤0 everywhere and <0 a.e. / at least at some point). Still an integral argument. Similar complexity.

    Perhaps easiest in Lean: use `strictAntiOn_of_deriv_neg` on two halves. Let me check names.

  - Weaken `0 ≤ h` in `perelmanF_antitone_discrete`: cluster's counterexample exists. I can reproduce independently.

  - Weaken `0 < F.eval (traj n) i` in `perelmanF_step_lt`: if F.eval = 0 at the chosen i, no strict decrease (functional constant along that component). E.g., hamilton field at zero state: F.eval = 0, traj stays 0, functional constant → not strict. Easy counterexample.

  - Weaken `0 < h`: h=0 gives constant trajectory, no strict decrease.

  - Continuous theorem: `perelmanF_antitone` with hc weakened to `∀ i, 0 ≤ c i`: cluster's squareTraj counterexample shows failure. Independently reproduce with different data maybe.

  - Also check the `EvolutionRelation`'s derivative is only right derivative on `Ico 0 T`, and the theorem compares s,t ∈ Icc 0 T. Fine.

  - Potential vacuity: `perelmanF_antitone` is not vacuous because EvolutionRelation inhabited (unitReactionField + global flow), and hc satisfiable (c=1). The certificate lower bound 0 is valid. Check `perelmanF_nonneg` requires 0 ≤ c i, which follows from 1 ≤ c i. Fine.

  - Potential issue: `perelmanF_limit_le_of_discrete` uses `le_of_tendsto'` with `hmono : ∀ n, f n ≤ f 0`. `le_of_tendsto' : Tendsto u atTop (𝓝 a) → (∀ n, u n ≤ b) → a ≤ b`? Let me check: in mathlib, `le_of_tendsto' (h : Tendsto u l (𝓝 a)) (hb : ∀ᶠ n in l, u n ≤ b) : a ≤ b`. With `∀ n, ...` coerced via Eventually.of_forall. And `hmono : ∀ n, perelmanF c (state n) ≤ perelmanF c (state 0)`. So a = perelmanF c limit, b = perelmanF c (state 0). Correct orientation. Fine.

  - Check `perelmanF_limit_le_of_discrete` hypotheses: `Tendsto state atTop (𝓝 limit)` — for `ι → ℝ` with product topology; continuity of perelmanF gives limit. Correct.

  - The `continuousPerelmanCertificate` uses `hasDerivAt_F : ∀ t, 0 < t → HasDerivAt ...`, and they prove global derivative for all t. Fine.

  - Potential issue in `perelmanAntitoneCertificate`: the index type is `{t : ℝ // t ∈ Icc 0 T}` with the subtype order. `mono := fun a b hab => perelmanF_antitone F hc ev a.2 b.2 (show a.1 ≤ b.1 from hab)`. Correct: a ≤ b in subtype means a.1 ≤ b.1. Fine.

  - Overstrong statement candidates:
    * `perelmanF_antitone` requires `∀ i, 1 ≤ c i`. Is this sharp? The dissipation bracket is (lam-1)² + (c-1); for fixed c<1, if lam ranges over a region where (lam-1)² ≥ 1-c, the derivative could still be ≤0. So the hypothesis `1 ≤ c i` is sufficient but not necessary; a weaker pointwise condition `1 - c i ≤ (lam i - 1)²` for all states on the trajectory would suffice. But as a uniform condition over all trajectories, `1 ≤ c i` is essentially the natural sharp threshold (since lam=1 can occur). Counterexample at c=0 with trajectory through lam=1 shows that any c<1 with a trajectory through lam=1 fails. Could c = 1/2 with trajectory through lam=1 fail? bracket = 0 + (-1/2) = -1/2 <0, so derivative positive (since F.eval>0), so functional increases locally. So threshold sharp. Good. I can construct a counterexample for c = 1/2 (lam=1, F=squareField, one step h small? e.g., h=1 gives lam=2, bracket at lam=1 = -1/2; functional increase? gibbsTerm (1/2) 2 vs gibbsTerm (1/2) 1: (1/2+4)e^{-2} vs (1/2+1)e^{-1}: 4.5 e^{-2} ≈ 0.609 vs 1.5 e^{-1} ≈ 0.552. Yes increases! Nice: c = 1/2, one Euler step h=1 from lam=1 to 2 increases. Let me verify: 4.5 e^{-2} > 1.5 e^{-1} ⟺ 3 e^{-1} > 1 ⟺ e < 3. True (exp_one_lt_three... need e < 3: `Real.exp_one_lt_three`). Wait 4.5/1.5 = 3, e^{-2}/e^{-1} = e^{-1}. So inequality is 3 e^{-1} > 1 ⟺ 3 > e. Yes. So a second independent counterexample with c = 1/2 < 1. This attacks the weakened hypothesis `0 ≤ c` more strongly (c=1/2 ≥ 0 but < 1).

    * `perelmanF_step_lt`'s `1 < c i`: overstrong as discussed; correction with `1 ≤ c i`. I should verify carefully. Actually wait — the proof for the corrected theorem needs strict decrease of gibbsTerm 1. Let me make sure it's actually true. f(x) = (1+x²)e^{-x}. f'(x) = -(x-1)² e^{-x}. Yes, f is strictly decreasing. So `perelmanF_step_lt'` with 1 ≤ c i holds. I'll implement it.

    Hmm, but wait: in `perelmanF_step_lt`, other components j ≠ i only weakly decrease via gibbsTerm_step_le with `le_of_lt (hc j)` where hc : ∀ i, 1 < c i. If hc : ∀ i, 1 ≤ c i, other components fine. Component i: need strict. If c i = 1, use new strict lemma. So corrected theorem: `∀ i, 1 ≤ c i` (with same h > 0 and F.eval > 0 at i). Good.

    * `gibbsTerm_strictAnti` hypothesis `1 < c`: also overstrong (should be `1 ≤ c`). Same fix.
    * `gibbsTerm_step_lt` `1 < c`: overstrong; correction with `1 ≤ c`.

    * Check `gibbsTerm_antitone` requires `1 ≤ c`: sharp as shown.
    * `perelmanF_nonneg` requires `0 ≤ c i`: sharp (c = -1 gives (c+x²) negative for x=0: gibbsTerm (-1) 0 = -1 < 0). Counterexample easy.
    * `perelmanF_zero`: true.
    * `finiteReactionEntropyData_F`: true.
    * `finiteReactionEntropyData_F_antitone`: fine.
    * `perelmanF_dissipation_pos_at_c_zero`: check its statement: `0 < -∑ ...`. It's a true computation. Fine.
    * `perelmanF_eq_on_Icc_of_eq_at` (flat-spot rigidity): check correctness. If perelmanF at t equals initial, then constant on [0,t]. Proof: h1: F(s) ≤ F(0) via antitone 0≤s; h2: F(t) ≤ F(s) via antitone s≤t; heq: F(t) = F(0), so F(0) ≤ F(s) and F(s) ≤ F(0), antisymm gives F(s)=F(0). Wait h1 is F(s) ≤ F(0), and h2 after rw [heq] is F(0) ≤ F(s). le_antisymm h1 h2 : F(s) = F(0)? le_antisymm h1 h2 gives F(s) = F(0) with h1 : F(s) ≤ F(0) and h2 : F(0) ≤ F(s). Yes. Correct.

    * `two_sided_monotonicity`: true given D2.
    * `continuousPerelmanCertificate_nonvacuous`: uses unitGlobalFlow; lowerBound 0 ≤ F at 0. Fine.

    * Potential "vacuous" statement: `PerelmanApproximation`, `PerelmanEvolutionBoundary`, `FiniteMeshConvergence`, `FiniteRepresentsContinuousPerelman` are statement-only interfaces with no inhabitants — not theorems, so not false. The result card is honest about this. But I should record that `ContinuousPerelmanFMonotonicity` is a Prop with no content and the transfer theorem is conditional (not vacuous as a theorem, but the hypothesis has no known inhabitant — overstrong/unsatisfied? We could check whether `PerelmanApproximation` is inhabited: with `X = ι`, μ = count, E t = finiteReactionEntropyData c (traj t) and identification rfl, we can construct an inhabitant if we have EvolutionRelation and hc. That would show the transfer theorem's hypothesis is satisfiable (non-vacuous) — a nice check! Indeed: for any F, traj with EvolutionRelation F T traj and hc, set E := fun t => finiteReactionEntropyData c (traj t). Then identification holds by `finiteReactionEntropyData_F`. So `PerelmanApproximation` is inhabited. I can construct this witness in my verifier and then apply the transfer theorem to get `ContinuousPerelmanFMonotonicity E T` — demonstrating non-vacuity of the conditional transfer. That's a good verifier contribution. Note the subtlety: `ContinuousPerelmanFMonotonicity E T` with E t := finiteReactionEntropyData c (traj t) is exactly the finite statement; no actual continuous Perelman content. Still shows the hypothesis is satisfiable.

    * Also check `perelmanF_monotone_of_tensorBridge`: requires `TensorRicciFlowODEBridge` which has no inhabitant (per D2 card). Is it vacuous? It's a conditional implication; if the bridge type is empty, the theorem is vacuously true but not false. I can check whether the bridge type is provably empty or not — probably not provable. I should record it as "conditional, hypothesis has no known inhabitant; not independently non-vacuous". But maybe I can construct an inhabitant? Let me look at the D2 Bridge structure. If it's easy to inhabit trivially, then the theorem is non-vacuous but also the bridge may be weak. Let me read D2 Bridge.

Let me also examine `le_left_of_hasDerivWithinAt_nonpos` in Invariant.lean/ScalarODE.lean to see if there's any subtle issue with the continuous theorem (e.g., whether continuity hypothesis is actually used, whether the statement is correct).

Let me read Invariant.lean and ScalarODE.lean and State.lean.
dsh: reasoning:
Let me read ScalarODE.lean which likely has `le_left_of_hasDerivWithinAt_nonpos`.
dsh: reasoning:
Good. Now, let me check the D2 Bridge (TensorRicciFlowODEBridge) and D3 Bridge quickly for vacuity questions. Also let me verify hashes of D4 files vs result card, and D2/D3 files vs their accepted cards.

Let me read D2 Bridge briefly.
dsh: reasoning:
All hashes match the result card. Good.

Now, verify the D2/D3 copied sources are byte-identical to their accepted worktrees. The card claims hashes:
- D2 CurvatureODE Evolution d54e..., Monotonicity 466f..., Bridge 8bae...
- D3 Entropy Functional 3128..., Certificate c12f..., Bridge 0d48...

Let me verify against the D2_ricci_ode_cluster and D3_entropy_interface worktrees, and also the copies in D4.

Then set up my worktree. Let me check the D2/D3 worktrees.
dsh: reasoning:
Everything checks out so far. Now let me set up the plan:

1. Set up the audit worktree project: copy lakefile.toml, lake-manifest.json, lean-toolchain from D4 worktree; create `.lake` and symlink `.lake/packages` to the shared packages; copy `Poincare/` sources from the D4 worktree (byte-identical, then verify hashes).
2. Build the library (`lake build Poincare`) to ensure oleans exist. This should be fast-ish since mathlib is prebuilt. Actually, if `.lake/build` is empty, `lake build Poincare` will need to build all Poincare modules from Basic up; that's ~50 files with heavy imports (Mathlib.Analysis.Calculus...). Each took a few seconds in D4's log; total maybe 2-4 minutes. Fine. Alternatively I can copy D4's `.lake/build` for speed but then verify freshness by rebuilding? A clean rebuild is more adversarial. Let me do a clean build in my worktree (rm any build). Since my worktree is empty, just symlink packages and build.

Wait — the sandbox is workspace-write with root = my worktree. Symlinking `.lake/packages` to a path outside the workspace: creating a symlink is a write inside the workspace; allowed. Reading through it is reading outside; should be allowed (reads seemed allowed above). Let me be careful: `.lake/packages` symlink target is in poincare-lab (outside workspace). That's how D4 did it. Fine.

3. Write `Audit/CounterexampleAudit.lean` in a fresh namespace `D4Audit` importing only `Poincare.Longrun.Evolution`. Contents:
   - Sanity re-checks of main theorems in fresh namespace: restate `perelmanF_antitone` etc. with different variable names (instantiate), prove them.
   - Counterexamples:
     a. `c = 1/2` one-step Euler increase (attacking weakening of `1 ≤ c` to `0 ≤ c`).
     b. `c = 0` full continuous counterexample via squareTraj (independent re-check).
     c. `h < 0` discrete increase (attacking `0 ≤ h`).
     d. negative reaction counterexample to the weakened discrete statement without `ReactionField` nonnegativity.
     e. `perelmanF_step_lt` weakening: drop `0 < h` (h=0 → constant); drop `0 < F.eval` (F.eval=0 → constant). Show conclusions fail.
     f. `perelmanF_nonneg` sharpness: c = -1 gives negative value.
     g. Overstrong hypothesis correction: prove `gibbsTerm_strictAnti_of_one_le` and `perelmanF_step_lt_of_one_le` with `1 ≤ c i` — the corrected theorem.
     h. Maybe check that `perelmanF_antitone`'s hc cannot be weakened to pointwise-only? No.
     i. Check non-vacuity of `PerelmanApproximation` by constructing an inhabitant and applying transfer theorem.
     j. Check `ContinuousPerelmanFMonotonicity` is inhabited trivially? For E := constant finiteReactionEntropyData, yes. Fine.
     k. Check `perelmanF_limit_le_of_discrete`: maybe test with a concrete convergent sequence where the limit is attained (state n = limit all n) and strict cases. Also test that the conclusion can be strict: state n = 1/n? perelmanF c (state n) ≤ perelmanF c (state 0) needs monotone sequence. E.g. c=1, state n = 1/(n+1) (decreasing from 1 to 0), perelmanF 1 (1/(n+1)) increasing? gibbsTerm 1 is decreasing in x, so as x decreases to 0, perelmanF increases to 1. Then perelmanF (limit=0) = 1 > perelmanF(state 0 = 1) = 2/e. So hmono fails. Take state n = n/(n+1) increasing to 1: perelmanF decreases to 2/e. Then conclusion: 2/e ≤ 2/e. Works. Let me test the limit theorem with that. Also test a case where limit equals state 0 constant.

     Also check `tendsto_perelmanF` directly.

   - `#print axioms` on all my own audit declarations plus key promoted ones. That satisfies "runs #print axioms". I should also run `#print axioms` for the promoted main theorem in my file to independently confirm (the task says import only promoted interfaces and run #print axioms).

4. The verifier must compile with no `sorry`, `axiom`, `unsafe`, `native_decide`, `proof_wanted`. I'll grep to confirm.

5. Write result card `longrun/results/D4-counterexample-audit.md` and `.json` in my worktree (since shared results dir is outside sandbox). Also possibly the state dir? The state dir is outside workspace (longrun/state). My worktree path is /data3/.../worktrees/D4_counterexample_audit. The state and results dirs are outside. I'll write inside my worktree under `longrun/results/` mirroring D4's pattern. Let me check if I can write to /data3/.../longrun/results directly — the sandbox says workspace-write with workspace = my worktree. Attempting to write outside may be denied. I could try; the D4 worker reported it was denied. I'll follow the same pattern: write inside worktree, and try copying to shared; if denied, note it.

Let me now start setting up. First, create the project skeleton.

Plan for the verifier file content. Let me think carefully about the Lean proofs needed, especially the corrected strict lemma.

First, let me check available mathlib lemma names in this version. Let me grep the mathlib source for `strictAntiOn_of_deriv_neg`, `strictAnti_of_deriv_neg`, `antitone_of_deriv_nonpos`.

Also check `strictAntiOn_of_deriv_neg` signature. Let me find the mathlib directory.
dsh: reasoning:
Let me check the exact statement of `strictAntiOn_of_deriv_neg`.
dsh: reasoning:
`strictAntiOn_of_deriv_neg` requires continuity on D and deriv < 0 on interior D. For D = Iic 1, interior (Iic 1) = Iio 1. For x < 1, (x-1)² > 0, so deriv gibbsTerm 1 x = -((x-1)²) e^{-x} < 0. Good. Similarly for Ici 1.

Now I can prove `gibbsTerm_strictAnti_of_one_le (c : ℝ) (hc : 1 ≤ c) : StrictAnti (gibbsTerm c)` by cases on c = 1 or 1 < c:
- If 1 < c: use `gibbsTerm_strictAnti c hc` (from the imported cluster).
- If c = 1: case split x < y:
  * y ≤ 1: apply `strictAntiOn_of_deriv_neg (convex_Iic 1)` with continuity; gives StrictAntiOn f (Iic 1), then f y < f x.
  * x ≥ 1: StrictAntiOn f (Ici 1).
  * x < 1 < y: f y < f 1 (from Ici 1: 1 ≤ y, 1 < y) and f 1 < f x (from Iic 1: x < 1). Combine.

Actually, `StrictAntiOn f s` is defined as `∀ ⦃a⦄, a ∈ s → ∀ ⦃b⦄, b ∈ s → a < b → f b < f a`. Let me check `strictAntiOn` definition: `StrictAntiOn f s := ∀ ⦃a⦄, a ∈ s → ∀ ⦃b⦄, b ∈ s → a < b → f b < f a`. So `hIic y hy x hx hxy : f y < f x` where hxy : x < y. Good.

For continuity of gibbsTerm 1: `continuous_gibbsTerm`? The cluster uses `fun_prop` with `unfold gibbsTerm`. In my file, I can prove `continuousOn_const.add ...` similar to cluster. Actually `gibbsTerm c` is continuous: `Continuous fun x => (c + x^2) * Real.exp (-x)`. Let me just write:
```
have hcont : Continuous (fun x => gibbsTerm c x) := by
  unfold gibbsTerm; fun_prop
```
Then `hcont.continuousOn`.

Derivative computation: `deriv (gibbsTerm 1) x = -((x-1)^2) * Real.exp (-x)` via `(gibbsTerm_hasDerivAt 1 x).deriv` and ring.

Then for x ∈ interior (Iic 1) = Iio 1: x < 1 so (x-1)^2 > 0, exp > 0, so deriv < 0.

Let me write:
```
theorem gibbsTerm_strictAnti_one : StrictAnti (gibbsTerm 1) := by
  intro x y hxy
  ...
```
Wait StrictAnti f := ∀ ⦃a b⦄, a < b → f b < f a. Actually `StrictAnti f := ∀ ⦃a b⦄, a < b → f b < f a`. Let me check: in Mathlib `StrictAnti f := ∀ ⦃a b⦄, a < b → f b < f a`. Yes.

Approach for `gibbsTerm_strictAnti_of_one_le`:
```
theorem gibbsTerm_strictAnti_of_one_le (c : ℝ) (hc : 1 ≤ c) : StrictAnti (gibbsTerm c) := by
  rcases lt_or_eq_of_le hc with hlt | rfl
  · exact gibbsTerm_strictAnti c hlt
  · -- c = 1
    have hcont : Continuous (gibbsTerm 1) := by unfold gibbsTerm; fun_prop
    have hderiv : ∀ x, deriv (gibbsTerm 1) x = -((x - 1) ^ 2) * Real.exp (-x) := by
      intro x
      rw [(gibbsTerm_hasDerivAt 1 x).deriv]; ring
    have hleft : StrictAntiOn (gibbsTerm 1) (Iic 1) := by
      apply strictAntiOn_of_deriv_neg (convex_Iic 1) hcont.continuousOn
      intro x hx
      have hx' : x < 1 := by simpa using hx
      rw [hderiv x]
      have hsq : 0 < (x - 1) ^ 2 := sq_pos_of_ne_zero (by linarith)
      have hexp : 0 < Real.exp (-x) := Real.exp_pos _
      nlinarith
    have hright : StrictAntiOn (gibbsTerm 1) (Ici 1) := by
      apply strictAntiOn_of_deriv_neg (convex_Ici 1) hcont.continuousOn
      intro x hx
      have hx' : 1 < x := by simpa using hx
      rw [hderiv x]
      have hsq : 0 < (x - 1) ^ 2 := sq_pos_of_ne_zero (by linarith)
      have hexp : 0 < Real.exp (-x) := Real.exp_pos _
      nlinarith
    intro x y hxy
    rcases le_or_lt y 1 with hy | hy
    · exact hleft (le_refl 1) hy hxy  -- wait direction: StrictAntiOn f s := ∀ a ∈ s, ∀ b ∈ s, a < b → f b < f a
```
Careful with StrictAntiOn signature: `StrictAntiOn f s := ∀ ⦃a⦄, a ∈ s → ∀ ⦃b⦄, b ∈ s → a < b → f b < f a`. So `hleft (a:=x) hx_mem (b:=y) hy_mem hxy : f y < f x`. Good.

For the case x < y ≤ 1: need x ∈ Iic 1 (x<y≤1 so x<1, hence x≤1). `hleft (a := x) (by linarith) (b := y) hy hxy` where hy : y ∈ Iic 1. But hy from rcases is `y ≤ 1`. Need membership.

Case 1 ≤ x < y: `hright (a := x) (by simpa using hx1) (b := y) (by linarith) hxy`.

Case x < 1 < y: `have h1 : gibbsTerm 1 1 < gibbsTerm 1 x := hleft (le_refl 1) (by linarith) hx1` where hx1: x < 1. And `h2 : gibbsTerm 1 y < gibbsTerm 1 1 := hright (le_refl 1) (by linarith) hy1`. Then exact h2.trans h1. But careful with implicit args and membership. Let me structure with explicit variables.

Actually there's an even simpler approach avoiding case analysis: mathlib has `strictAnti_of_deriv_neg` variants only. But maybe `strictAntiOn_of_deriv_neg` on the whole real line fails at x=1. Alternatively, note `deriv (gibbsTerm 1) x ≤ 0` everywhere and use a lemma: if `deriv f ≤ 0` everywhere and the set of zeros of deriv is countable/nowhere dense, strict. There might be `strictAnti_of_deriv_nonpos`? Let me grep.

Actually maybe mathlib has `strictMono_of_deriv_nonneg`? Let me check around line 470-535.
dsh: reasoning:
No `strictAnti_of_deriv_nonpos`. The two-half approach is the way. Actually, another approach for `gibbsTerm 1` strict anti using `strictAntiOn_of_deriv_neg` on `Iio 1` and `Ioi 1` plus case analysis. Let me think about what's simplest for Lean:

```
theorem gibbsTerm_one_strictAnti : StrictAnti (gibbsTerm 1) := by
  have hcont : Continuous (gibbsTerm 1) := by unfold gibbsTerm; fun_prop
  have hderiv : ∀ x, deriv (gibbsTerm 1) x = -((x - 1) ^ 2) * Real.exp (-x) := ...
  have hneg : ∀ x, x ≠ 1 → deriv (gibbsTerm 1) x < 0 := by
    intro x hx
    rw [hderiv x]
    have hsq : 0 < (x - 1) ^ 2 := sq_pos_of_ne_zero (sub_ne_zero.mpr hx)
    have hexp : 0 < Real.exp (-x) := Real.exp_pos _
    nlinarith
  intro x y hxy
  -- need gibbsTerm 1 y < gibbsTerm 1 x
  by_cases hy : y ≤ 1
  · -- x < y ≤ 1: apply strictAntiOn_of_deriv_neg on Iic 1
    exact (strictAntiOn_of_deriv_neg (convex_Iic 1) hcont.continuousOn (fun z hz => hneg z (by have := interior_Iic 1 ▸ hz; ...)) ) (by linarith : x ∈ Iic 1) (by exact hy) hxy
  ...
```
Hmm interior_Iic: `interior (Iic a) = Iio a`. Let me check lemma name: `interior_Iic`. In mathlib, `interior_Iic : interior (Iic a) = Iio a`. Yes. So `hz : z ∈ interior (Iic 1)`, `rw [interior_Iic] at hz`, hz : z ∈ Iio 1, i.e., z < 1, so z ≠ 1.

Case hy : ¬ y ≤ 1 → 1 < y.
  by_cases hx : 1 ≤ x.
  · -- 1 ≤ x < y: strictAntiOn on Ici 1
    have : x ∈ Ici 1 := hx
    ...
  · -- x < 1 < y: combine
    push_neg at hx -- x < 1
    have h1 : gibbsTerm 1 1 < gibbsTerm 1 x := ... (on Iic 1)
    have h2 : gibbsTerm 1 y < gibbsTerm 1 1 := ... (on Ici 1)
    exact h2.trans h1

This is fine. Alternatively use `StrictAntiOn` with `Iio 1` and `Ioi 1` directly (open sets, interior = itself). Even simpler: `strictAntiOn_of_deriv_neg (convex_Iio 1)` on Iio 1; interior (Iio 1) = Iio 1 (since open). Then `hz : z ∈ interior (Iio 1)`; `rw [interior_Iio]`? Hmm, interior of open set: `IsOpen.interior_eq`. Could use `interior_Iio : interior (Iio a) = Iio a`. Let me check names: `interior_Iio`, `interior_Ioi` exist in mathlib (Mathlib/Topology/Order/...). I'll use `Iic`/`Ici` with `interior_Iic`/`interior_Ici` which I know exist (used in D3 Certificate: `interior_Ici`). For Iic: `interior_Iic` should exist.

Actually the D3 certificate uses `interior_Ici`. Let me check `interior_Iic` exists.

Now, for the corrected theorem:
```
theorem perelmanF_step_lt_of_one_le (F : ReactionField ι) {c : ι → ℝ} (hc : ∀ i, 1 ≤ c i)
    {h : ℝ} (hh : 0 < h) {traj : ℕ → ι → ℝ} (ev : DiscreteEvolution F h traj) {n : ℕ}
    {i : ι} (hi : 0 < F.eval (traj n) i) :
    perelmanF c (traj (n + 1)) < perelmanF c (traj n) := by
  unfold perelmanF
  refine Finset.sum_lt_sum (fun j _ => ?_) ⟨i, Finset.mem_univ i, ?_⟩
  · rw [ev.step n j]
    exact gibbsTerm_step_le (hc j) (mul_nonneg (le_of_lt hh) (F.eval_nonneg (traj n) j))
  · rw [ev.step n i]
    exact gibbsTerm_step_lt_of_one_le (hc i) (mul_pos hh hi)
```
where `gibbsTerm_step_lt_of_one_le {c x u} (hc : 1 ≤ c) (hu : 0 < u) : gibbsTerm c (x+u) < gibbsTerm c x := gibbsTerm_strictAnti_of_one_le c hc (lt_add_of_pos_right x hu)`.

Note `Finset.sum_lt_sum` signature: `Finset.sum_lt_sum : (∀ i ∈ s, f i ≤ g i) → (∃ i ∈ s, f i < g i) → ∑ f < ∑ g`. Yes.

Also `gibbsTerm_strictAnti_of_one_le` general c ≥ 1. For c > 1 use imported `gibbsTerm_strictAnti`; for c = 1 use the case analysis. Good.

Now, potential issue: my theorem is in a fresh namespace but uses imported declarations. Good.

Next, the counterexamples:

A. c = 1/2 one-step:
```
theorem counterexample_c_half :
    perelmanF (fun _ : Fin 1 => (1/2 : ℝ)) (fun _ => (1 : ℝ)) <
      perelmanF (fun _ : Fin 1 => (1/2 : ℝ))
        (eulerStep (squareField : ReactionField (Fin 1)) 1 (fun _ => (1 : ℝ))) := by
  rw [eulerStep_squareField_one]
  -- need (1/2 + 1^2) e^{-1} < (1/2 + 2^2) e^{-2}, i.e. 1.5 e^{-1} < 4.5 e^{-2}
  norm_num [perelmanF, gibbsTerm, Fin.sum_univ_one]
  ...
```
Let me compute: LHS = (1/2 + 1) e^{-1} = 1.5 e^{-1}; RHS = (1/2 + 4) e^{-2} = 4.5 e^{-2}. Inequality 1.5 e^{-1} < 4.5 e^{-2} ⟺ 1.5 e < 4.5 ⟺ e < 3. `Real.exp_one_lt_three` is in the Counterexample module? It used `Real.exp_one_lt_three` (mathlib lemma). Let me write a helper: `half_exp_ineq : (3/2) * Real.exp (-1) < (9/2) * Real.exp (-2)` from exp 1 < 3:
```
have h3 : Real.exp 1 < 3 := Real.exp_one_lt_three
have hpos : 0 < Real.exp 1 := Real.exp_pos 1
rw [Real.exp_neg, Real.exp_neg]
have hexp2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by rw [show (2:ℝ) = 1+1 by norm_num, Real.exp_add]
rw [hexp2]
-- goal: (3/2) * (exp 1)⁻¹ < (9/2) * (exp 1 * exp 1)⁻¹
field_simp
-- goal probably: 3 * exp 1 < 9  i.e. exp 1 < 3
linarith
```
Let me be careful with field_simp orientation. Alternatively prove the general fact: for c ≥ 0, gibbsTerm c 2 vs gibbsTerm c 1 increase iff (c+4)e^{-2} > (c+1)e^{-1} iff (c+4) > (c+1)e iff c(3-e) > e-4? Hmm. Let's just do the explicit computation.

Actually simpler: use the general one-step failure threshold: `gibbsTerm c (x+u) ≤ gibbsTerm c x` requires... For c<1, choose x=1, u=1. Condition gibbsTerm c 2 > gibbsTerm c 1 ⟺ (c+4)e^{-2} > (c+1)e^{-1} ⟺ c+4 > (c+1)e ⟺ c(e-1) < 4-e... wait: c + 4 > ce + e ⟺ c(1-e) > e-4 ⟺ c(e-1) < 4-e. For c=0: 0 < 4-e (true since e<4). For c=1/2: (e-1)/2 < 4-e ⟺ e-1 < 8-2e ⟺ 3e < 9 ⟺ e<3. True. Fine, so c=1/2 works. Let me verify numerically: 4.5 e^{-2} = 4.5 * 0.1353 = 0.609; 1.5 e^{-1} = 1.5*0.3679=0.5518. Yes.

B. Independent continuous counterexample with c = 0 (reuse squareTraj_evolution and squareTraj from cluster). I can restate:
```
theorem counterexample_continuous_c_zero :
    perelmanF (fun _ : Fin 1 => (0:ℝ)) (squareTraj 0) < perelmanF (fun _ : Fin 1 => (0:ℝ)) (squareTraj (1/2)) :=
  squareTraj_counterexample
```
That's just a re-export. To make it independent, maybe use c = 1/2 along a trajectory? For c = 1/2 and lam=1, derivative of perelmanF at that point = -F.eval*((0) + (c-1))*exp = -1*(-1/2)*e^{-1} = e^{-1}/2 > 0. Could construct a trajectory solving lam' = lam² with lam(0)=1 (squareTraj) and compute functional at t=0 and some small t where it increases. squareTraj t = 1/(1-t), at t=0 value 1, at t=1/2 value 2. Functional with c=1/2 at 2 vs at 1: increases as computed. So the same squareTraj works for c=1/2! Great: `perelmanF (1/2) (squareTraj 0) < perelmanF (1/2) (squareTraj (1/2))` — same numbers as counterexample A (gibbsTerm (1/2) 1 < gibbsTerm (1/2) 2). So one lemma can serve both. Let me create the inequality lemma `half_one_two` and use it for both discrete and continuous.

C. Negative step: h = -1, c = 1, squareField, start lam=1 → lam=0. `perelmanF 1 (0) = 1 > 2 e^{-1} = perelmanF 1 (1)`. The cluster has this. I can restate independently with a slightly different computation (maybe c=1, start lam = 1/2, h=-1: step to 1/2 - 1/4 = 1/4; functional at 1/4 vs 1/2: gibbsTerm 1 is decreasing, so moving left increases. Fine but the cluster's version is fine.)

D. Negative reaction counterexample (weakened statement without ReactionField). Formulate:
```
def negField : (Fin 1 → ℝ) → Fin 1 → ℝ := fun _ _ => -1
-- trajectory traj 0 = 1, traj (n+1) i = traj n i + 1 * negField (traj n) i
-- weakened statement: if traj (n+1) i = traj n i + h * G (traj n) i and 0 ≤ h then perelmanF c (traj (n+1)) ≤ perelmanF c (traj n)
```
Prove negation: `¬ (∀ (G : (Fin 1 → ℝ) → Fin 1 → ℝ) (traj : ℕ → Fin 1 → ℝ), 0 ≤ (1:ℝ) → (∀ n i, traj (n+1) i = traj n i + 1 * G (traj n) i) → perelmanF c (traj 1) ≤ perelmanF c (traj 0))` for c = 1. Actually simpler to state the counterexample instance directly:
```
theorem negative_reaction_breaks :
    ∃ (traj : ℕ → Fin 1 → ℝ), traj 0 = (fun _ => 1) ∧ traj 1 = (fun _ => 0) ∧
      perelmanF (fun _ => 1) (traj 0) < perelmanF (fun _ => 1) (traj 1) := ...
```
Hmm, that doesn't directly reference the weakened theorem. Better: define the weakened discrete evolution predicate with an arbitrary reaction function and show the monotonicity fails:
```
def WeakDiscreteEvolution (G : (Fin 1 → ℝ) → Fin 1 → ℝ) (h : ℝ) (traj : ℕ → Fin 1 → ℝ) : Prop :=
  ∀ n i, traj (n+1) i = traj n i + h * G (traj n) i

theorem weak_discrete_monotonicity_false :
    ¬ (∀ (G : (Fin 1 → ℝ) → Fin 1 → ℝ) (traj : ℕ → Fin 1 → ℝ) (h : ℝ),
        0 ≤ h → WeakDiscreteEvolution G h traj →
        ∀ n, perelmanF (fun _ => (1:ℝ)) (traj n) ≤ perelmanF (fun _ => (1:ℝ)) (traj 0)) := by
  intro h
  -- take G = -1, h = 1, traj n = 1 - n
  have := h (fun _ _ => -1) (fun n _ => 1 - n) 1 (by norm_num) ?_ 1
  · norm_num [perelmanF, gibbsTerm] at this  -- need 2*e^{-1} ≤ ...?
```
Let's compute: traj 0 = 1, traj 1 = 0. perelmanF 1 (traj 1) = (1+0)e^0 = 1. perelmanF 1 (traj 0) = 2e^{-1} ≈ 0.736. So `this : perelmanF 1 (traj 1) ≤ perelmanF 1 (traj 0)` is `1 ≤ 2e^{-1}`, false since e < 2. Get contradiction via `Real.exp_one_lt_two`? Actually 1 ≤ 2/e ⟺ e ≤ 2, false since 2 < e (`Real.exp_one_gt_two`). So:
```
have hlt : perelmanF 1 (traj 0) < perelmanF 1 (traj 1) := ... -- 2e^{-1} < 1
linarith
```
I can prove `two_exp_neg_one_lt_one : 2 * Real.exp (-1) < 1` using exp_one_gt_two: `rw [Real.exp_neg, inv_eq_one_div, mul_one_div, div_lt_one (Real.exp_pos 1)]; linarith [Real.exp_one_gt_two]`. Then `norm_num [perelmanF, gibbsTerm, WeakDiscreteEvolution] at this` to simplify, then linarith.

This is a strong audit finding: the nonnegativity of the reaction is essential and the D2 structure is what enforces it.

E. `perelmanF_step_lt` hypotheses necessity:
- Drop `0 < h`: h = 0 gives traj(n+1) = traj n, so equality. Show the conclusion fails:
```
theorem step_lt_needs_h_pos :
    ¬ (∀ (F : ReactionField (Fin 1)) (c : Fin 1 → ℝ) (traj : ℕ → Fin 1 → ℝ),
        (∀ i, 1 < c i) → (∀ n i, traj (n+1) i = traj n i + 0 * F.eval (traj n) i) →
        perelmanF c (traj 1) < perelmanF c (traj 0)) := ...
```
Maybe simpler: instantiate with c=2, hamilton field, traj = 0 (zero_discreteEvolution h=0). Then perelmanF c (traj 1) = perelmanF c (traj 0) = perelmanF c 0 = 2 (for Fin 1 c=2). Conclusion `2 < 2` false. Good.

Actually, we can more directly state: "the hypothesis 0 < h in perelmanF_step_lt is necessary: with h = 0 the conclusion is false even when 1 < c and F.eval > 0." Witness: F = unitReactionField (eval = 1 > 0), c = 2, h = 0, traj n = 0. Then traj (n+1) = traj n + 0*1 = traj n, so perelmanF c (traj 1) = perelmanF c (traj 0), and `¬ ... < ...`.

Wait, but the weakened statement must be a plausible theorem statement. The cleanest is to state the counterexample directly:
```
theorem step_lt_fails_at_h_zero :
    ¬ (perelmanF (fun _ : Fin 1 => (2:ℝ))
        ((fun n (_:Fin 1) => (0:ℝ)) 1) <
      perelmanF (fun _ : Fin 1 => (2:ℝ)) ((fun n (_:Fin 1) => (0:ℝ)) 0)) := by ...
```
That's a bit trivial. I think a better formulation: show a structure "the conclusion of perelmanF_step_lt with h=0 substituted is false at a concrete point". I'll phrase it as: `perelmanF c (traj (n+1)) = perelmanF c (traj n)` so the strict inequality is impossible. Record in card.

- Drop `0 < F.eval (traj n) i`: take F = hamilton (eval at 0 is 0), traj = zero, c = 2, h = 1. Then traj (n+1) = traj n + 1*0 = traj n, so equal; strict fails. Good.

- Drop `1 < c i`: Here's the subtle one. If we drop to `1 ≤ c i`, the conclusion still holds (that's our corrected theorem). So the hypothesis `1 < c i` is overstrong but the "weakened hypothesis" still yields a true statement. To show `1 < c` is not *necessary*, we prove the corrected theorem. Additionally, we can show the weaker assumption `c i < 1` fails (counterexample A). So the sharp condition is `1 ≤ c`.

F. `perelmanF_nonneg` sharpness: c = -1, lam = 0 → gibbsTerm (-1) 0 = -1 < 0. Prove `perelmanF (fun _ : Fin 1 => (-1)) (fun _ => 0) = -1`. So the hypothesis `0 ≤ c i` is necessary.

G. `gibbsTerm_antitone` sharpness: for c < 1, gibbsTerm c is not antitone: counterexample c = 1/2, x=1, y=2: gibbsTerm (1/2) 1 < gibbsTerm (1/2) 2. Same lemma as A.

H. Non-vacuity of PerelmanApproximation:
```
theorem perelmanApproximation_inhabited (F : ReactionField ι) {c : ι → ℝ} (hc : ∀ i, 1 ≤ c i)
    {T : ℝ} {traj : ℝ → ι → ℝ} (ev : EvolutionRelation F T traj) :
    Nonempty (PerelmanApproximation (X := ι) (μ := Measure.count) (fun t => finiteReactionEntropyData c (traj t)) F T traj c) := ...
```
Need `[MeasurableSpace ι] [MeasurableSingletonClass ι]`. Then apply `continuousPerelmanFMonotone_of_approximation` to get the conclusion, showing the transfer theorem's hypothesis is satisfiable. Note: `EntropyData ι (Measure.count : Measure ι)` requires `[MeasurableSpace ι]`. Fine.

Actually careful: `PerelmanApproximation E F T traj c` — the X and μ are implicit; E : ℝ → EntropyData X μ. Set X := ι, μ := Measure.count. identification: `∀ t ∈ Icc 0 T, (finiteReactionEntropyData c (traj t)).F = perelmanF c (traj t)` = finiteReactionEntropyData_F. Good.

I. `FiniteMeshConvergence` non-vacuity: construct with mesh = 1/(n+1) → 0 and state n = 0 → limit 0. Use `tendsto_one_div_add_atTop_nhds_zero_nat`? In mathlib: `tendsto_one_div_add_atTop_nhds_zero_nat : Tendsto (fun n : ℕ => 1 / (n + 1 : ℝ)) atTop (𝓝 0)`. And constant sequence. Then `FiniteMeshConvergence` inhabited. This shows the statement-only boundary is satisfiable (though trivially).

J. `perelmanF_limit_le_of_discrete` non-vacuity: apply with state n = n/(n+1), limit = 1. Need monotonicity of perelmanF c (state n) ≤ perelmanF c (state 0). We need hmono. With c = 1 and state n = n/(n+1) increasing from 0 to 1, gibbsTerm 1 decreasing in x, so perelmanF 1 (state n) ≤ perelmanF 1 (state 0). But note state 0 = 0, perelmanF 1 0 = 1, and perelmanF 1 (state n) ≥ 2/e; and ≤ 1. So hmono holds. Then conclusion perelmanF 1 1 ≤ perelmanF 1 0, i.e., 2/e ≤ 1, true. This just tests the theorem. To prove hmono: `gibbsTerm_antitone 1 le_rfl` applied to n/(n+1) ≥ 0. Since n/(n+1) ≥ 0, and gibbsTerm 1 antitone, gibbsTerm 1 (n/(n+1)) ≤ gibbsTerm 1 0. `nlinarith [Nat.cast_nonneg n, ...]`. Fine.

Hmm, but this is a lot of content. Let me be pragmatic: the audit file should be substantial but must compile. I'll aim for ~250-350 lines with the key findings. Priorities:
1. Re-check main theorem in fresh namespace (direct applications).
2. Counterexamples A (c=1/2 discrete), A' (c=1/2 continuous), C (h<0), D (negative reaction weakened statement), E (h=0, eval=0 necessity for strict), F (c<0 nonneg sharpness).
3. Corrected theorem with proof: `gibbsTerm_strictAnti_of_one_le`, `gibbsTerm_step_lt_of_one_le`, `perelmanF_step_lt_of_one_le`.
4. Non-vacuity witnesses: PerelmanApproximation inhabited; FiniteMeshConvergence inhabited.
5. `#print axioms` for all my declarations + promoted main declarations.

Also I should double check: is there any *false* statement in the promoted cluster? Let me scan more carefully for overstrong/false claims:

- `perelmanF_antitone` — true.
- `hasDerivWithinAt_perelmanF` — let me verify the derivative formula sign. perelmanF = Σ (c + lam²)e^{-lam}. d/dlam [(c+lam²)e^{-lam}] = 2lam e^{-lam} - (c+lam²)e^{-lam} = (2lam - c - lam²)e^{-lam} = -(lam² - 2lam + c)e^{-lam} = -((lam-1)² + (c-1))e^{-lam}. Yes: (lam-1)² = lam²-2lam+1, plus c-1 gives lam²-2lam+c. Correct. Along traj: d/dt = Σ -( (lam-1)² + (c-1)) e^{-lam} * F.eval. The theorem states `-∑ F.eval * ((lam-1)²+(c-1)) * exp` — same up to commutativity. Correct.
- `perelmanF_dissipation_nonpos` — true.
- `perelmanF_step_le` — uses `ev.step n i` rewriting traj (n+1) i = traj n i + h F.eval, then gibbsTerm_step_le with u = h F.eval ≥ 0. Correct.
- `perelmanF_antitone_discrete` induction: `Nat.le_induction` with base m≤m refl, succ: ih : perelmanF (traj n) ≤ perelmanF (traj m) and step: perelmanF (traj (n+1)) ≤ perelmanF (traj n); trans. Correct.
- `perelmanF_step_lt` — true, overstrong hypothesis.
- `perelmanAntitoneCertificate` — index subtype `{t // t ∈ Icc 0 T}`; `lowerBound := 0`; `lower_le` uses perelmanF_nonneg with `le_trans zero_le_one (hc i)`. Correct.
- `continuousPerelmanCertificate` — `hasDerivAt_F` for t with 0 < t (field requires only positive t). Provided for all t. `continuousOn_F` on Ici 0. Correct.
- `GlobalReactionFlow` is a Prop structure; `evolutionRelation` restriction: `hasDeriv := fun i t _ => (G.hasDeriv i t).hasDerivWithinAt`. Correct.
- `unitGlobalFlow`: traj t i = t, derivative 1 = eval. Correct.
- `zeroGlobalFlow`: hamilton eval at 0 = 0, derivative of constant 0. Correct.
- `continuousPerelmanCertificate_nonvacuous`: lowerBound = 0 ≤ F at 0. perelmanF c 0 = Σ c i ≥ 0 since 1 ≤ c i. Correct.
- `two_sided_monotonicity`: correct.
- `tendsto_perelmanF`: correct.
- `perelmanF_limit_le_of_discrete`: correct orientation.
- `continuousPerelmanFMonotone_of_approximation`: correct.
- `perelmanF_monotone_of_tensorBridge`: conditional; uses `evolutionRelation_of_bridge B`. Fine.
- `FiniteMeshConvergence`, `FiniteRepresentsContinuousPerelman`, `PerelmanEvolutionBoundary` — statement-only, honest.
- `finiteReactionEntropyData_F`: `EntropyData.F (finiteReactionEntropyData c lam) = perelmanF c lam`. Check: EntropyData.F = ∫ (R + gradSq) ρ dμ; with counting measure `integral_count` gives sum over finite type? `MeasureTheory.integral_count` states `∫ x, f x ∂Measure.count = ∑ i, f i`? For finite type. They `unfold ... ; rw [integral_count]`. If the lemma is `integral_count : ∫ x, f x ∂Measure.count = ∑' i, f i` (tsum), then for finite types tsum = sum. The proof compiles in D4, so fine.
- `finiteReactionEntropyData_W`: `rw [EntropyData.W_eq]; simp only [finiteReactionEntropyData]; rw [one_mul]; congr 1; unfold extra; rw [integral_count]`. Hmm, after `simp only [finiteReactionEntropyData]`, the sum should appear. It compiles. Fine.
- `gibbsTerm_deriv_at_one`: `deriv (gibbsTerm c) 1 = -(c-1)*exp(-1)`. Check: derivative = -((1-1)²+(c-1))e^{-1} = -(c-1)e^{-1}. Correct.
- `exp_neg_one_lt_four_exp_neg_two`: true.
- `perelmanF_dissipation_pos_at_c_zero`: `0 < -∑ i : Fin 1, squareField.eval (fun _=>1) i * ((1-1)²+(0-1)) * exp(-1)`. Compute: eval = 1, bracket = -1, exp(-1) > 0, so sum = -exp(-1), negative; minus sum = exp(-1) > 0. True.

Now, is `perelmanF_antitone`'s hypothesis `∀ i, 1 ≤ c i` overstrong? No — sharp as shown by c=1/2 counterexample (though only if trajectory can pass through lam=1 with positive reaction; yes).

Another potential issue: the continuous theorem requires `EvolutionRelation F T traj` on `[0,T]`, but the monotonicity conclusion compares s,t ∈ Icc 0 T. Fine.

What about the discrete theorem's hypothesis `hh : 0 ≤ h` — sharp (negative step counterexample).

Another audit angle: `perelmanF_step_lt` states strict decrease when *one* component has positive reaction. Is that right if other components' reactions are zero? Yes, one strictly decreasing term + others nonincreasing → strict. Fine.

Potential issue: In `perelmanF_step_lt`, hypothesis `hc : ∀ i, 1 < c i` is used for all j (via `le_of_lt (hc j)`) and i. If we weaken to `∀ i, 1 ≤ c i`, our corrected proof works. Good.

What about `gibbsTerm_strictAnti (c) (1 < c)`: overstrong → `1 ≤ c`. I'll record this as the main "overstrong statement" finding with correction.

Wait, but is it really overstrong? The definition of StrictAnti: `∀ a b, a < b → f b < f a`. For c=1, yes strictly decreasing. Let me double-check numerically: f(1)=2e^{-1}=0.7358, f(2)=5e^{-2}=0.6767, f(0)=1. f(0.5)=(1.25)e^{-0.5}=1.25*0.6065=0.7581, f(1)=0.7358. Yes decreasing. So `1 < c` is indeed overstrong.

Is `gibbsTerm_step_lt`'s `hu : 0 < u` needed? Yes: u=0 gives equality. Sharp.

Now, another thing to verify adversarially: the claim "no CFL restriction beyond h ≥ 0" — true since one-step inequality is monotonicity of gibbsTerm.

Another potential subtlety: the D4 card says "the exact dissipation identity ... The bracket ... is exactly the finite analogue of the integrand R + |∇f|²". Not a Lean claim.

Now let me also consider: does `perelmanF_antitone` hold if `c i ≥ 1` but `F.eval` is negative at some point? D2 forbids it; but as a statement about the ODE, if F.eval < 0 the trajectory could move backwards and perelmanF could increase. But within the cluster, F is a ReactionField so eval ≥ 0. The "weakened hypothesis" attack: define a structure like EvolutionRelation but without ReactionField nonnegativity. Our negative-reaction discrete counterexample covers the discrete analog. For continuous: could construct traj t = 1 - t with G = -1 (derivative -1), c=1; then perelmanF 1 (traj 1) = 1 > 2e^{-1} = perelmanF 1 (traj 0). This directly shows the continuous monotonicity fails if the reaction is negative. Nice: define `negTraj t = 1 - t`, and show `HasDerivWithinAt (fun s => negTraj s i) (-1) (Ici t) t` and continuity, i.e., an "EvolutionRelation-like" data with reaction -1. Then show the antitone conclusion fails. I can state a weakened predicate:
```
structure EvolutionRelationNoSign (G : (Fin 1 → ℝ) → Fin 1 → ℝ) (T : ℝ) (traj : ℝ → Fin 1 → ℝ) : Prop where
  continuous : ∀ i, ContinuousOn (fun t => traj t i) (Icc 0 T)
  hasDeriv : ∀ i, ∀ t ∈ Ico 0 T, HasDerivWithinAt (fun s => traj s i) (G (traj t) i) (Ici t) t
```
and then `negTraj` with G ≡ -1 is an instance, and monotonicity fails. That's an independent counterexample showing nonnegativity of the reaction is essential. Maybe I'll do the discrete version (D) and also a continuous version. The continuous version might be a bit more work but similar to squareTraj_evolution. Let me consider adding it — it's valuable. Actually, the negative reaction continuous counterexample is simpler than squareTraj: traj t = 1 - t, derivative -1, G = -1. c = 1: perelmanF 1 (traj 0) = 2e^{-1}, perelmanF 1 (traj 1) = 1, so increase. But careful: EvolutionRelation requires T and derivative on Ico 0 T; take T = 1. Then traj t = 1 - t on [0,1]. perelmanF 1 (traj 1) = 1 > 2e^{-1}. Great. I'll add it as `continuous_negative_reaction_counterexample`.

Now, is there any *false* statement? Let me think about `perelmanF_zero`: `perelmanF c (fun _ => 0) = scalarOfState c`. LHS = Σ (c i + 0)*e^0 = Σ c i. True.

`continuous_perelmanF`: true.

`perelmanF_nonneg`: needs 0 ≤ c i; true.

`gibbsTerm_nonneg`: needs 0 ≤ c; true.

`gibbsTerm_comp`: derivative of composition with right derivative d: -((f t -1)² + (c-1))e^{-f t} * d. Correct by chain rule.

`gibbsTerm_hasDerivAt`: correct.

`perelmanF_eq_on_Icc_of_eq_at`: true.

Now, one more potential issue: `AntitoneCertificate` packaging for the continuous case uses the subtype `{t : ℝ // t ∈ Icc 0 T}`. `mono := fun a b hab => perelmanF_antitone F hc ev a.2 b.2 (show a.1 ≤ b.1 from hab)`. Fine.

Potential vacuity: `perelmanAntitoneCertificate`'s `lower_le` uses `perelmanF_nonneg (fun i => le_trans zero_le_one (hc i))`. Fine.

What about `continuousPerelmanCertificate`'s `hasDerivAt_F` field: the structure requires `∀ t, 0 < t → HasDerivAt ...`. They give `intro t _; ...`. Fine.

OK. Now let me also think about whether the promoted D4 theorem is "nontrivial" — yes.

One more check: the task says "checks the theorem in a fresh namespace". I'll define in `namespace D4Audit` and restate the main theorems as my own theorems with `exact`/`apply` proofs, e.g.:
```
theorem audit_perelmanF_antitone ... := Poincare.Longrun.Evolution.perelmanF_antitone ...
```
Better to restate with the same statement and prove by exact application, showing the statement elaborated in a fresh namespace. That satisfies "checks the theorem in a fresh namespace".

Let me also include `example` checks with `#check`? `#check` produces output, fine. But `#print axioms` is required. I'll use `#print axioms` on my audit declarations and on key promoted ones.

Now let me think about the build setup. My worktree has `Poincare/` copied. The lakefile globs `Poincare.+`. My audit file is in `Audit/` (outside the library), so `lake build Poincare` won't build it; I run `lake env lean Audit/CounterexampleAudit.lean` which requires the library oleans to exist. So first `lake build Poincare`.

But wait: `lake env lean` might not automatically build dependencies; it needs the .olean files. Since I copied only sources and no `.lake/build`, I must run `lake build Poincare` first. Let me do that now in the background, since it might take several minutes.

Actually, hold on. There's a concern: `lake build Poincare` will build all modules under Poincare/ — that includes the D1/D2/D3 modules (Basic, Stage1, Geometry, PDE, Entropy, CurvatureODE, Evolution) plus maybe others. Let me check the full Poincare tree in D4 worktree.

Let me count files and check for heavy modules. Also, D4's build log said 8913 jobs; mathlib prebuilt means lake checks/traces but doesn't rebuild. Actually, with mathlib prebuilt in .lake/packages/mathlib/.lake/build, lake should not rebuild mathlib. The 8913 jobs include all mathlib modules as "already built" trace checks? Lake only builds what's needed. Hmm, when I run `lake build Poincare`, lake computes the dependency graph and builds only missing oleans. Since mathlib oleans exist, it should just build the ~40 Poincare modules. Let's start it in the background.

Let me first check the Poincare tree file list and set up the environment variables. ELAN_HOME=/data3/guoshaoyang/workdir/lean_poincare/elan, PATH=$ELAN_HOME/bin:$PATH.

Let me start the build in background.
dsh: reasoning:
34 modules. Note: `Poincare/Longrun/Entropy/AxiomAudit.lean` and `Poincare/Longrun/PDE/AxiomAudit.lean` contain `#print axioms` at build time (producing info output). They're part of the library. That's fine.

Now start `lake build Poincare` in the background.
dsh: reasoning:
While the build runs, let me design and write the verifier file. Let me first check some details in mathlib that I'll need:

1. `Real.exp_one_lt_three` — exists? The cluster used it. Yes.
2. `Real.exp_one_gt_two` — used in cluster. Yes.
3. `interior_Iic` — check.
4. `tendsto_one_div_add_atTop_nhds_zero_nat` — check name.
5. `Finset.sum_lt_sum` signature.
6. `Measure.count`, `integral_count`.

Let me grep mathlib for interior_Iic and the tendsto lemma.
dsh: reasoning:
`interior_Iic` requires `[NoMaxOrder α]` — ℝ has that. Good.

`tendsto_one_div_add_atTop_nhds_zero_nat` exists in `Mathlib/Analysis/SpecificLimits/Basic.lean`. Let me check its statement signature.

`Finset.sum_lt_sum` — let me find it.
dsh: reasoning:
`Finset.sum_lt_sum` exists (used in D4). Fine.

Now, let me think about what exactly `perelmanF (fun _ : Fin 1 => (1/2:ℝ)) (fun _ => 1)` reduces to and how to prove the inequality. `perelmanF` is a `Finset.sum` over `Fin 1`. I can use `Fin.sum_univ_one` to reduce: `perelmanF c lam = gibbsTerm (c 0) (lam 0)`. The cluster's `squareTraj_counterexample` uses `norm_num [perelmanF, gibbsTerm, squareTraj, Fin.sum_univ_one] at h ⊢`. Good pattern.

Inequality `gibbsTerm (1/2) 1 < gibbsTerm (1/2) 2`:
gibbsTerm (1/2) 1 = (1/2+1)e^{-1} = 3/2 e^{-1}
gibbsTerm (1/2) 2 = (1/2+4)e^{-2} = 9/2 e^{-2}
Need 3/2 e^{-1} < 9/2 e^{-2} ⟺ 3 e^{-1} < 9 e^{-2} ⟺ 3e < 9 ⟺ e < 3.

Proof:
```
theorem exp_one_lt_three : Real.exp 1 < 3 := Real.exp_one_lt_three
theorem half_counterexample_ineq : gibbsTerm (1/2) 1 < gibbsTerm (1/2) 2 := by
  have h3 : Real.exp 1 < 3 := Real.exp_one_lt_three
  have hpos : 0 < Real.exp 1 := Real.exp_pos 1
  rw [gibbsTerm, gibbsTerm, Real.exp_neg, Real.exp_neg]
  have hexp2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [show (2:ℝ) = 1 + 1 by norm_num, Real.exp_add]
  rw [hexp2]
  rw [inv_eq_one_div, inv_eq_one_div]  -- maybe not needed
  field_simp
  -- goal?
  linarith
```
Let me be careful. After `rw [Real.exp_neg, Real.exp_neg]`, the goal is:
`(1/2 + 1^2) * (Real.exp 1)⁻¹ < (1/2 + 2^2) * (Real.exp 2)⁻¹`.
After `rw [hexp2]`: `... (Real.exp 1 * Real.exp 1)⁻¹`.
`field_simp` should clear denominators giving something like `(1/2+1) * (exp 1 * exp 1) < (1/2+4) * exp 1`? Hmm, field_simp with the inverses: multiplying both sides by exp1*exp1 (positive, nonzero). It normalizes. Then `linarith` with h3.

Actually `field_simp` may leave the goal as `3 * Real.exp 1 < 9` or `1.5 * exp1 * exp1 < 4.5 * exp1`. Let's just try and iterate.

Alternatively, use `positivity` and `nlinarith`? Nonlinear. Let me just write it and test.

Now, `perelmanF` with `c := fun _ : Fin 1 => 1/2` and `lam := fun _ => 1`: `perelmanF c lam = gibbsTerm (c 0) (lam 0)` by `Fin.sum_univ_one`. `simp [perelmanF, Fin.sum_univ_one]` should do it.

Now, for the continuous c=1/2 counterexample: `perelmanF (1/2) (squareTraj 0) < perelmanF (1/2) (squareTraj (1/2))`. `squareTraj 0 = fun _ => (1-0)⁻¹ = 1`, `squareTraj (1/2) = (1-1/2)⁻¹ = 2`. So it reduces to the same inequality. Prove by `simpa [perelmanF, gibbsTerm, squareTraj, Fin.sum_univ_one] using half_counterexample_ineq` after norm_num simplification. Let me write:
```
theorem continuous_counterexample_c_half :
    perelmanF (fun _ : Fin 1 => (1/2:ℝ)) (squareTraj 0) <
      perelmanF (fun _ : Fin 1 => (1/2:ℝ)) (squareTraj (1/2)) := by
  have h := half_counterexample_ineq
  norm_num [perelmanF, gibbsTerm, squareTraj, Fin.sum_univ_one] at h ⊢
  exact h
```
The cluster used exactly this pattern.

Also verify that `squareTraj_evolution` gives the EvolutionRelation; so the counterexample is a legitimate trajectory.

Now the corrected theorem. Let me write the strict antitonicity carefully.

```
theorem gibbsTerm_strictAnti_of_one_le (c : ℝ) (hc : 1 ≤ c) : StrictAnti (gibbsTerm c) := by
  rcases lt_or_eq_of_le hc with hlt | rfl
  · exact gibbsTerm_strictAnti c hlt
  · -- c = 1
    have hcont : Continuous (gibbsTerm 1) := by unfold gibbsTerm; fun_prop
    have hderiv : ∀ x : ℝ, deriv (gibbsTerm 1) x = -((x - 1) ^ 2) * Real.exp (-x) := by
      intro x
      rw [(gibbsTerm_hasDerivAt 1 x).deriv]
      ring
    have hneg : ∀ x : ℝ, x ≠ 1 → deriv (gibbsTerm 1) x < 0 := by
      intro x hx
      rw [hderiv x]
      have hsq : 0 < (x - 1) ^ 2 := sq_pos_of_ne_zero (sub_ne_zero.mpr hx)
      have hexp : 0 < Real.exp (-x) := Real.exp_pos _
      nlinarith
    have hleft : StrictAntiOn (gibbsTerm 1) (Iic 1) := by
      refine strictAntiOn_of_deriv_neg (convex_Iic 1) hcont.continuousOn (fun x hx => ?_)
      rw [interior_Iic] at hx
      exact hneg x (ne_of_lt hx)
    have hright : StrictAntiOn (gibbsTerm 1) (Ici 1) := by
      refine strictAntiOn_of_deriv_neg (convex_Ici 1) hcont.continuousOn (fun x hx => ?_)
      rw [interior_Ici] at hx
      exact hneg x (ne_of_gt hx)
    intro x y hxy
    rcases le_or_lt y 1 with hy | hy
    · exact hleft (le_trans (le_of_lt hxy) hy) hy hxy
    · by_cases hx1 : 1 ≤ x
      · exact hright hx1 hy hxy
      · push_neg at hx1
        have h1 : gibbsTerm 1 1 < gibbsTerm 1 x := hleft (le_refl 1) (le_of_lt hx1) hx1
        have h2 : gibbsTerm 1 y < gibbsTerm 1 1 := hright (le_refl 1) hy (lt_of_le_of_ne (le_refl 1) ... )
```
Hmm wait, `StrictAntiOn f s` signature. Let me check its definition in mathlib:
`def StrictAntiOn (f : α → β) (s : Set α) : Prop := ∀ ⦃a⦄, a ∈ s → ∀ ⦃b⦄, b ∈ s → a < b → f b < f a`
Let me verify. In Mathlib/Order/Monotone/Defs.lean:
```
def StrictAntiOn (f : α → β) (s : Set α) : Prop := ∀ ⦃a⦄, a ∈ s → ∀ ⦃b⦄, b ∈ s → a < b → f b < f a
```
I'm fairly confident. So `hleft (ha : x ∈ Iic 1) (hb : y ∈ Iic 1) (hxy : x < y) : f y < f x`.

For the last case: x < 1 (hx1 : ¬ 1 ≤ x, push_neg gives x < 1) and 1 < y (hy : ¬ y ≤ 1, push_neg gives 1 < y).
- h1 : gibbsTerm 1 1 < gibbsTerm 1 x via `hleft (a := 1) (by exact le_refl 1) (b := x) (le_of_lt hx1) hx1`.
- h2 : gibbsTerm 1 y < gibbsTerm 1 1 via `hright (a := 1) (le_refl 1) (b := y) (le_of_lt hy) hy`.
- exact h2.trans h1.

Careful: after `push_neg at hx1`, hx1 : x < 1; `hy` after rcases is `¬ y ≤ 1`, so `push_neg at hy` gives 1 < y. Good.

Also note `rcases lt_or_eq_of_le hc with hlt | rfl`: if c = 1, the goal becomes `StrictAnti (gibbsTerm 1)`. Good. But careful: `rcases ... | rfl` replaces c by 1 in the goal. Fine.

Then:
```
theorem gibbsTerm_step_lt_of_one_le {c x u : ℝ} (hc : 1 ≤ c) (hu : 0 < u) :
    gibbsTerm c (x + u) < gibbsTerm c x :=
  gibbsTerm_strictAnti_of_one_le c hc (lt_add_of_pos_right x hu)

theorem perelmanF_step_lt_of_one_le (F : ReactionField ι) {c : ι → ℝ} (hc : ∀ i, 1 ≤ c i)
    {h : ℝ} (hh : 0 < h) {traj : ℕ → ι → ℝ} (ev : DiscreteEvolution F h traj) {n : ℕ}
    {i : ι} (hi : 0 < F.eval (traj n) i) :
    perelmanF c (traj (n + 1)) < perelmanF c (traj n) := by
  unfold perelmanF
  refine Finset.sum_lt_sum (fun j _ => ?_) ⟨i, Finset.mem_univ i, ?_⟩
  · rw [ev.step n j]
    exact gibbsTerm_step_le (hc j) (mul_nonneg (le_of_lt hh) (F.eval_nonneg (traj n) j))
  · rw [ev.step n i]
    exact gibbsTerm_step_lt_of_one_le (hc i) (mul_pos hh hi)
```

Now the negative reaction discrete counterexample. Let me define:
```
def negReaction : (Fin 1 → ℝ) → Fin 1 → ℝ := fun _ _ => -1
def negTrajDisc : ℕ → Fin 1 → ℝ := fun n _ => 1 - n

theorem negTrajDisc_step : ∀ n i, negTrajDisc (n+1) i = negTrajDisc n i + (1:ℝ) * negReaction (negTrajDisc n) i := by
  intro n i
  simp [negTrajDisc, negReaction]
  ring
```
`negTrajDisc (n+1) i = 1 - (n+1)`, RHS = `1 - n + 1*(-1) = -n`. LHS = `1 - n - 1 = -n`. Yes.

Then:
```
theorem negative_reaction_breaks_discrete :
    ¬ (∀ (G : (Fin 1 → ℝ) → Fin 1 → ℝ) (traj : ℕ → Fin 1 → ℝ) (h : ℝ),
        0 ≤ h → (∀ n i, traj (n+1) i = traj n i + h * G (traj n) i) →
        perelmanF (fun _ : Fin 1 => (1:ℝ)) (traj 1) ≤ perelmanF (fun _ : Fin 1 => (1:ℝ)) (traj 0)) := by
  intro H
  have hstep := H negReaction negTrajDisc 1 (by norm_num) negTrajDisc_step
  -- hstep : perelmanF 1 (negTrajDisc 1) ≤ perelmanF 1 (negTrajDisc 0)
  have hval : perelmanF (fun _ : Fin 1 => (1:ℝ)) (negTrajDisc 1) = 1 := by
    norm_num [perelmanF, gibbsTerm, negTrajDisc, Fin.sum_univ_one]
  have hval0 : perelmanF (fun _ : Fin 1 => (1:ℝ)) (negTrajDisc 0) = 2 * Real.exp (-1) := by
    norm_num [perelmanF, gibbsTerm, negTrajDisc, Fin.sum_univ_one]
  rw [hval, hval0] at hstep
  have hlt : 2 * Real.exp (-1) < 1 := by
    rw [Real.exp_neg, inv_eq_one_div, mul_one_div, div_lt_one (Real.exp_pos 1)]
    linarith [Real.exp_one_gt_two]
  linarith
```
Check `div_lt_one`: for `a / b < 1` with b > 0 ⟺ a < b. `div_lt_one (hb : 0 < b) : a / b < 1 ↔ a < b`. So `rw [mul_one_div, div_lt_one hpos]` gives goal `2 < Real.exp 1`; `linarith [Real.exp_one_gt_two]`. Good.

Note: `norm_num [perelmanF, gibbsTerm, negTrajDisc, Fin.sum_univ_one]` — negTrajDisc 1 = fun _ => 0, so gibbsTerm 1 0 = (1+0)*e^0 = 1. Should work. For negTrajDisc 0 = fun _ => 1, gibbsTerm 1 1 = 2*e^{-1}. `norm_num` might not simplify `Real.exp (-1)` but will leave `2 * Real.exp (-1)`. Let me use `simp` + `norm_num` maybe. Actually `norm_num [perelmanF, gibbsTerm, negTrajDisc, Fin.sum_univ_one]` should evaluate arithmetic and leave `Real.exp (-1)` symbolic. Fine. But careful: `norm_num` may rewrite `Real.exp 0 = 1`. Good.

Hmm, one concern: `H negReaction negTrajDisc 1 (by norm_num) negTrajDisc_step` — the weakened statement has `h` as an explicit argument after traj? The forall order: `(G) (traj) (h)`. Fine.

Now the continuous negative reaction counterexample:
```
def negTrajCont : ℝ → Fin 1 → ℝ := fun t _ => 1 - t

theorem negTrajCont_evolution :
    ∀ t ∈ Ico (0:ℝ) 1, HasDerivWithinAt (fun s => negTrajCont s 0) (-1) (Ici t) t := by
  intro t _
  have h : HasDerivAt (fun s : ℝ => 1 - s) (-1) t := (hasDerivAt_id t).const_sub 1
  exact h.hasDerivWithinAt
```
Then state the counterexample to a weakened monotonicity theorem:
```
theorem negative_reaction_breaks_continuous :
    perelmanF (fun _ : Fin 1 => (1:ℝ)) (negTrajCont 1) <
      perelmanF (fun _ : Fin 1 => (1:ℝ)) (negTrajCont 0) := ...
```
This shows that along a "trajectory" with derivative -1 (i.e., a negative reaction), the functional increases; so the D2 nonnegativity is essential. To tie it to the weakened statement, I can define:
```
structure NoSignEvolution (G : (Fin 1 → ℝ) → Fin 1 → ℝ) (T : ℝ) (traj : ℝ → Fin 1 → ℝ) : Prop where
  continuous : ∀ i, ContinuousOn (fun t => traj t i) (Icc 0 T)
  hasDeriv : ∀ i, ∀ t ∈ Ico 0 T, HasDerivWithinAt (fun s => traj s i) (G (traj t) i) (Ici t) t
```
and show `NoSignEvolution negReaction 1 negTrajCont`, plus the conclusion fails. But maybe simpler to just show the trajectory data and the failing inequality. I'll include the structure version for rigor: 
```
theorem no_sign_continuous_counterexample :
    ∃ (G : (Fin 1 → ℝ) → Fin 1 → ℝ) (traj : ℝ → Fin 1 → ℝ),
      NoSignEvolution G 1 traj ∧
      perelmanF (fun _ : Fin 1 => (1:ℝ)) (traj 0) < perelmanF (fun _ : Fin 1 => (1:ℝ)) (traj 1) := ...
```
Hmm, the direction: traj 0 = 1 gives 2e^{-1} ≈ 0.736, traj 1 = 0 gives 1. So perelmanF (traj 0) < perelmanF (traj 1): increase. Yes.

Let me structure it as: `∃ G traj, NoSignEvolution G 1 traj ∧ ¬ (∀ t ∈ Icc 0 1, perelmanF c (traj t) ≤ perelmanF c (traj 0))`. Actually the direct inequality is cleaner.

Now the h=0 and eval=0 necessity. For h=0:
```
theorem step_lt_fails_h_zero :
    ¬ (perelmanF (fun _ : Fin 1 => (2:ℝ)) ((fun _ => (0:ℝ)) : Fin 1 → ℝ) <
       perelmanF (fun _ : Fin 1 => (2:ℝ)) ((fun _ => (0:ℝ)) : Fin 1 → ℝ)) :=
  lt_irrefl _
```
That's too trivial. Better: show that with h = 0, the one-step trajectory has equal values:
```
theorem zero_step_constant (F : ReactionField ι) (c : ι → ℝ) (lam : ι → ℝ) :
    perelmanF c (eulerStep F 0 lam) = perelmanF c lam := by
  simp [perelmanF, eulerStep]
```
`eulerStep F 0 lam i = lam i + 0 * eval = lam i`. Then strict inequality `perelmanF c (eulerStep F 0 lam) < perelmanF c lam` is false. So:
```
theorem strict_step_needs_positive_step (F : ReactionField ι) (c : ι → ℝ) (lam : ι → ℝ) :
    ¬ perelmanF c (eulerStep F 0 lam) < perelmanF c lam := by
  rw [zero_step_constant]; exact lt_irrefl _
```
But careful: `perelmanF c (eulerStep F 0 lam)` and `perelmanF c lam` are definitionally equal after simp? `zero_step_constant` gives equality, then `lt_irrefl`. Fine. This shows h>0 needed (with 1 < c and eval > 0 satisfiable).

For eval=0:
```
theorem strict_step_needs_positive_reaction (F : ReactionField ι) (c : ι → ℝ) (h : ℝ) (lam : ι → ℝ)
    (i : ι) (hi : F.eval lam i = 0) :
    ¬ perelmanF c (fun j => eulerStep F h lam j) < perelmanF c lam := ...
```
Hmm, if only one component has eval 0 but others positive, the functional may still strictly decrease. So this statement as stated is false. The necessity is: the hypothesis `0 < F.eval (traj n) i` cannot be dropped *in the theorem*, i.e., there is an instance where eval = 0 and the strict conclusion fails. Take all components eval 0: F = hamilton at lam = 0, h arbitrary, traj constant 0 → functional equal. So:
```
theorem strict_step_needs_positive_reaction :
    ¬ perelmanF (fun _ : Fin 1 => (2:ℝ))
        (eulerStep (ReactionField.hamilton : ReactionField (Fin 1)) 1 (fun _ => (0:ℝ)))
      < perelmanF (fun _ : Fin 1 => (2:ℝ)) (fun _ => (0:ℝ)) := by
  simp [perelmanF, eulerStep, ReactionField.hamilton_eval, Fin.sum_univ_one]
  -- eulerStep = lam + 1*0 = lam
```
Actually `eulerStep hamilton 1 0 = fun _ => 0 + 1*(0² + Σ 0²) = 0`, so perelmanF c 0 = Σ c = 2. Equality, so not <. Prove by `simp [eulerStep, ReactionField.hamilton_eval]` then `lt_irrefl`. Let me write:
```
theorem strict_step_needs_positive_reaction :
    ¬ perelmanF (fun _ : Fin 1 => (2:ℝ))
        (eulerStep (ReactionField.hamilton : ReactionField (Fin 1)) 1 (fun _ => (0:ℝ)))
      < perelmanF (fun _ : Fin 1 => (2:ℝ)) (fun _ => (0:ℝ)) := by
  have h : eulerStep (ReactionField.hamilton : ReactionField (Fin 1)) 1 (fun _ => (0:ℝ)) = fun _ => (0:ℝ) := by
    funext i; simp [eulerStep]
  rw [h]
  exact lt_irrefl _
```
Note `ReactionField.hamilton_eval_zero` is a simp lemma. `simp [eulerStep]` should give `0 + 1 * 0 = 0`. Good.

And a positive control: with `1 ≤ c`, h > 0, eval > 0, strict decrease holds — that's the corrected theorem applied. Good.

Now the non-vacuity witnesses. For `PerelmanApproximation`:
```
theorem perelmanApproximation_nonvacuous [MeasurableSpace ι] [MeasurableSingletonClass ι]
    (F : ReactionField ι) {c : ι → ℝ} (hc : ∀ i, 1 ≤ c i) {T : ℝ} {traj : ℝ → ι → ℝ}
    (ev : EvolutionRelation F T traj) :
    Nonempty (PerelmanApproximation (X := ι) (μ := (Measure.count : Measure ι))
      (fun t => finiteReactionEntropyData c (traj t)) F T traj c) :=
  ⟨{ hc := hc, evolves := ev, identification := fun t _ => finiteReactionEntropyData_F c (traj t) }⟩
```
Then:
```
theorem transfer_nonvacuous [MeasurableSpace ι] [MeasurableSingletonClass ι]
    (F : ReactionField ι) {c : ι → ℝ} (hc : ∀ i, 1 ≤ c i) {T : ℝ} {traj : ℝ → ι → ℝ}
    (ev : EvolutionRelation F T traj) :
    ContinuousPerelmanFMonotonicity (fun t => finiteReactionEntropyData c (traj t)) T :=
  continuousPerelmanFMonotone_of_approximation
    ⟨hc, ev, fun t _ => finiteReactionEntropyData_F c (traj t)⟩
```
Wait: the structure fields are `hc`, `evolves`, `identification`. The `identification` field type is `FiniteRepresentsContinuousPerelman E c traj T` = `∀ t ∈ Icc 0 T, (E t).F = perelmanF c (traj t)`. So `fun t _ => finiteReactionEntropyData_F c (traj t)`. Good.

For `FiniteMeshConvergence`:
```
theorem finiteMeshConvergence_nonvacuous :
    FiniteMeshConvergence (fun n : ℕ => 1 / ((n : ℝ) + 1)) (fun _ (_ : Fin 1) => (0:ℝ)) (fun _ => (0:ℝ)) :=
  ⟨tendsto_one_div_add_atTop_nhds_zero_nat, fun i => tendsto_const_nhds⟩
```
Need `ι = Fin 1` explicit? `FiniteMeshConvergence (ι := Fin 1) ...`. Good. `tendsto_const_nhds : Tendsto (fun _ => a) l (𝓝 a)`. Good.

For `perelmanF_limit_le_of_discrete` non-vacuity: use state n = n/(n+1), limit = 1, c = 1. Prove hmono. Let me think about whether this is worth the effort. It tests the limit theorem. hmono: perelmanF c (state n) ≤ perelmanF c (state 0). state 0 = 0. state n = n/(n+1) ≥ 0. gibbsTerm 1 antitone, so gibbsTerm 1 (state n) ≤ gibbsTerm 1 0. So `perelmanF 1 (state n) = gibbsTerm 1 (state n 0) ≤ gibbsTerm 1 0 = perelmanF 1 (state 0)`. Proof:
```
theorem limit_passage_sanity :
    perelmanF (fun _ : Fin 1 => (1:ℝ)) (fun _ => (1:ℝ)) ≤
      perelmanF (fun _ : Fin 1 => (1:ℝ)) (fun _ => (0:ℝ)) := by
  apply perelmanF_limit_le_of_discrete (state := fun n _ => (n:ℝ)/(n+1)) (limit := fun _ => 1)
  · intro n
    have hle : (n:ℝ)/(n+1) ≥ 0 := by positivity
    have h0 : (0:ℝ) ≤ (n:ℝ)/(n+1) := hle
    have h := gibbsTerm_antitone 1 le_rfl h0
    -- h : gibbsTerm 1 (n/(n+1)) ≤ gibbsTerm 1 0
    simpa [perelmanF, Fin.sum_univ_one] using h
  · have h : Tendsto (fun n : ℕ => (n:ℝ)/(n+1)) atTop (𝓝 1) := by
      -- n/(n+1) = 1 - 1/(n+1)
      have : (fun n : ℕ => (n:ℝ)/(n+1)) = fun n : ℕ => 1 - 1/((n:ℝ)+1) := by
        funext n; field_simp; ring
      rw [this]
      simpa using tendsto_const_nhds.sub tendsto_one_div_add_atTop_nhds_zero_nat
    exact tendsto_pi_nhds.mpr (fun i => by fin_cases i; simpa using h)
```
Hmm, `Tendsto state atTop (𝓝 limit)` where state limit : ι → ℝ. `tendsto_pi_nhds` states `Tendsto f l (𝓝 g) ↔ ∀ i, Tendsto (fun x => f x i) l (𝓝 (g i))`. Let me check name: in mathlib `tendsto_pi_nhds : Tendsto f l (𝓝 g) ↔ ∀ i, Tendsto (fun x => f x i) l (𝓝 (g i))`. Yes, I believe it exists.

Also need `gibbsTerm_antitone 1 le_rfl : Antitone (gibbsTerm 1)`, so `h : (n/(n+1)) ≥ 0 → gibbsTerm 1 (n/(n+1)) ≤ gibbsTerm 1 0`? Antitone f means `a ≤ b → f b ≤ f a`. So with `0 ≤ n/(n+1)` we get `gibbsTerm 1 (n/(n+1)) ≤ gibbsTerm 1 0`. Good.

But `perelmanF c lam = ∑ i, gibbsTerm (c i) (lam i)`, for Fin 1 equals `gibbsTerm (c 0) (lam 0)` by `Fin.sum_univ_one`. `simpa [perelmanF, Fin.sum_univ_one] using h`. Good.

Then the conclusion: `perelmanF 1 (limit=1) ≤ perelmanF 1 (state 0 = 0)`: `2e^{-1} ≤ 1`; true; the theorem produces it. But note the theorem statement I wrote has `perelmanF ... (fun _ => 1) ≤ perelmanF ... (fun _ => 0)`, which is exactly the conclusion. Good.

Actually wait, `perelmanF_limit_le_of_discrete` conclusion is `perelmanF c limit ≤ perelmanF c (state 0)`. With state 0 = 0, limit = 1, that's `perelmanF 1 1 ≤ perelmanF 1 0`. Good.

This checks the limit theorem with a nontrivial convergent sequence.

Now, the audit should also check the D3 certificate inhabitants. E.g.:
- `perelmanAntitoneCertificate` applied: re-derive comparison in fresh namespace.
- `continuousPerelmanCertificate` applied to `unitGlobalFlow`, and evaluate `dissipation` field? We can check the certificate's lower bound and antitoneOn:
```
theorem certificate_sanity ... : (continuousPerelmanCertificate unitGlobalFlow hc).lowerBound = 0 := rfl
```
And apply `ContinuousAntitoneCertificate.F_le_initial`.

Let me also verify the "sign-convention" claim (nonincreasing vs Perelman's nondecreasing) — not a Lean issue, but record it.

Now, let me also think about whether to attack `perelmanF_antitone`'s conclusion sharpness: Is there a trajectory where perelmanF strictly decreases? Yes (unitGlobalFlow with c=1: traj t = t, perelmanF 1 (traj 1) < perelmanF 1 (traj 0)). Add a strictness sanity check.

Also check the flat-spot rigidity is non-vacuous: For unitGlobalFlow, perelmanF 1 (traj t) is strictly decreasing for t>0, so equality never occurs (except t=0), and the theorem's hypothesis is not satisfiable — that's fine, it's a conditional. Could check with a flat case: if F = 0? But ReactionField g_nonneg allows g = 0, a = 0, so F.eval = 0, traj constant; then perelmanF constant; flat-spot rigidity applies. E.g., zeroReactionField with constant trajectory. Then `perelmanF_eq_on_Icc_of_eq_at` gives constancy. Non-vacuity of flat-spot rigidity: construct F.eval = 0 and traj constant. Let me add:
```
def zeroReactionField : ReactionField ι where a := 0; g := 0; ...
```
Wait, `g := fun _ _ => 0`, `g_nonneg := fun _ _ => le_rfl`. Then `eval = 0`, and constant traj `fun _ _ => lam` satisfies EvolutionRelation (derivative 0 = eval). Then `perelmanF_eq_on_Icc_of_eq_at` with heq rfl yields constancy — a sanity check. But this is extra. Maybe include a small one.

Let me now also confirm the "promoted interfaces" interpretation: import only `Poincare.Longrun.Evolution`. My file will have exactly one import line. Good.

One more adversarial angle: the result card claims `FDissipation = 0` and the finite model carries no Bochner dissipation. Check: `finiteReactionEntropyData_FDissipation` true. Also check `HasConjugateWeight`: true.

Another: `finiteReactionEntropyData` requires `[MeasurableSpace ι] [MeasurableSingletonClass ι]`; `Integrable.of_finite` used. Fine.

Now let me check the build progress.
dsh: reasoning:
The build is progressing and near the end (8904/8913). Let me wait for it to finish while writing the verifier file. Let me check job output in a moment.

Let me now write the verifier file. I'll write it in one go, then compile and iterate.

Key considerations for compilation:
- Need imports: only `import Poincare.Longrun.Evolution`. But I need `Measure.count`, `Fin.sum_univ_one`, `tendsto_pi_nhds`, `strictAntiOn_of_deriv_neg`, etc. All these are in scope transitively because Poincare.Longrun.Evolution imports Mathlib pieces (Gibbs imports Mathlib.Analysis.Calculus.Deriv.MeanValue; Functional imports Entropy.Functional which imports Mathlib; Bridge imports Topology etc.). Since `import Mathlib` is transitively imported via Entropy.Functional, everything is available. Good — single import line still gives access to all mathlib.

- Namespace: `namespace D4Audit ... end D4Audit`. Within it, open the relevant namespaces.

- The file lives at `Audit/CounterexampleAudit.lean`. It's outside the library glob `Poincare.+` so `lake build Poincare` won't build it. I run `lake env lean Audit/CounterexampleAudit.lean`.

- `#print axioms` outputs go to stdout. Good.

Let me draft the file.

```lean
/-
D4-counterexample-audit — independent adversarial verifier.
...
-/
import Poincare.Longrun.Evolution

open Set Filter MeasureTheory
open scoped Topology BigOperators

namespace D4Audit

open Poincare.Longrun.CurvatureODE
open Poincare.Longrun.Entropy
open Poincare.Longrun.Evolution

universe w

variable {ι : Type w} [Fintype ι]

/-! ## 1. Re-checking the promoted theorem in a fresh namespace -/

theorem audit_perelmanF_antitone (F : ReactionField ι) {c : ι → ℝ} (hc : ∀ i, 1 ≤ c i)
    {T : ℝ} {traj : ℝ → ι → ℝ} (ev : EvolutionRelation F T traj) :
    ∀ ⦃s t : ℝ⦄, s ∈ Icc 0 T → t ∈ Icc 0 T → s ≤ t →
      perelmanF c (traj t) ≤ perelmanF c (traj s) :=
  perelmanF_antitone F hc ev

...
```

Note: inside `namespace D4Audit`, `perelmanF` resolves to `D4Audit.perelmanF`? No — inside namespace D4Audit, the name `perelmanF` resolves via open namespaces: `Poincare.Longrun.Evolution.perelmanF`. Since I `open Poincare.Longrun.Evolution`, the unqualified name resolves. But if I define a theorem named `audit_perelmanF_antitone`, no clash. However, defining `theorem perelmanF_antitone` would be a new name in D4Audit; I use `audit_` prefix to avoid confusion.

Potential ambiguity: `open Poincare.Longrun.CurvatureODE` and `open Poincare.Longrun.Entropy` and `open Poincare.Longrun.Evolution`. Names like `F` may conflict? `EntropyData.F` is namespaced. Unqualified `perelmanF` only in Evolution. Fine.

Now, `Fin 1` index type: `[Fintype (Fin 1)]` exists. `Subsingleton (Fin 1)`. Fine.

Let me write the full content carefully.

For the "fresh namespace check", I want to check the theorem statements are exactly as claimed. I could also use `#check @perelmanF_antitone` to display. But `#check` output isn't an error; it prints. Fine, I'll add a few `#check`s maybe. Actually `#print axioms` is required; `#check` optional.

Let me carefully write each proof.

### Section: counterexample c = 1/2

```
/-- The arithmetic inequality behind the `c = 1/2` counterexample: `3 e^{-1} < 9 e^{-2}`. -/
theorem three_exp_neg_one_lt_nine_exp_neg_two :
    3 * Real.exp (-1) < 9 * Real.exp (-2) := by
  have h3 : Real.exp 1 < 3 := Real.exp_one_lt_three
  have hpos : 0 < Real.exp 1 := Real.exp_pos 1
  have hexp2 : Real.exp (-2) = ((Real.exp 1) * (Real.exp 1))⁻¹ := by
    rw [show (-2 : ℝ) = -(1 + 1) by norm_num, Real.exp_neg, Real.exp_add]
  rw [Real.exp_neg, hexp2]
  rw [inv_eq_one_div, inv_eq_one_div]
  field_simp
  linarith
```
Let me trace: after rw, goal: `3 * (1 / exp1) < 9 * (1 / (exp1*exp1))`. field_simp multiplies by exp1² > 0: `3 * exp1 < 9`. linarith with h3. Should work. Note `field_simp` needs to know `exp1 ≠ 0`; it can derive from `Real.exp_pos`. It might need `positivity` or `norm_num`. Let me add `have hne : Real.exp 1 ≠ 0 := ne_of_gt hpos` and pass to field_simp? `field_simp` usually uses positivity facts in context. hpos is in context. Should be OK.

Then:
```
theorem gibbsTerm_half_one_lt_two : gibbsTerm (1/2) 1 < gibbsTerm (1/2) 2 := by
  have h := three_exp_neg_one_lt_nine_exp_neg_two
  norm_num [gibbsTerm] at h ⊢
  -- goal: (1/2 + 1) * exp(-1) < (1/2 + 4) * exp(-2) ; norm_num normalizes to (3/2)*... < (9/2)*...
  linarith
```
Hmm: after `norm_num [gibbsTerm]`, the goal should be `(1/2 + 1) * exp(-1) < (1/2 + 4) * exp(-2)` with 1^2=1, 2^2=4; `norm_num` normalizes to `3 / 2 * exp(-1) < 9 / 2 * exp(-2)` perhaps. h is `3 * exp(-1) < 9 * exp(-2)`. `linarith` can handle scaling by 1/2? linarith works over linear arithmetic with rational coefficients, so `(3/2) x < (9/2) y` from `3x < 9y` is fine. Good.

Then:
```
theorem counterexample_discrete_c_half :
    perelmanF (fun _ : Fin 1 => (1/2:ℝ)) (fun _ => (1:ℝ)) <
      perelmanF (fun _ : Fin 1 => (1/2:ℝ))
        (eulerStep (squareField : ReactionField (Fin 1)) 1 (fun _ => (1:ℝ))) := by
  rw [eulerStep_squareField_one]
  simpa [perelmanF, Fin.sum_univ_one] using gibbsTerm_half_one_lt_two
```
`eulerStep_squareField_one` is a simp/rewrite lemma in the cluster: `eulerStep squareField 1 (fun _ => 1) = fun _ => 2`. Good. After rw, `perelmanF (1/2) (fun _ => 1) < perelmanF (1/2) (fun _ => 2)`; `simpa [perelmanF, Fin.sum_univ_one]` turns both into gibbsTerm. Note `perelmanF c lam = ∑ i, gibbsTerm (c i) (lam i)`; `Fin.sum_univ_one` rewrites to `gibbsTerm (c 0) (lam 0)`. For `c = fun _ => 1/2`, `c 0` beta-reduces to 1/2. `simp` should handle.

Continuous version:
```
theorem counterexample_continuous_c_half :
    perelmanF (fun _ : Fin 1 => (1/2:ℝ)) (squareTraj 0) <
      perelmanF (fun _ : Fin 1 => (1/2:ℝ)) (squareTraj (1/2)) := by
  have h := gibbsTerm_half_one_lt_two
  norm_num [perelmanF, gibbsTerm, squareTraj, Fin.sum_univ_one] at h ⊢
  exact h
```
`squareTraj t = fun _ => (1-t)⁻¹`. `squareTraj 0` → `(1-0)⁻¹ = 1`; `squareTraj (1/2)` → `(1-1/2)⁻¹ = 2`. `norm_num` should compute. And the goal becomes the gibbsTerm inequality. Good. I also want to assert that `squareTraj` is a genuine EvolutionRelation:
```
theorem counterexample_continuous_c_half_is_evolution :
    EvolutionRelation (squareField : ReactionField (Fin 1)) (1/2) squareTraj :=
  squareTraj_evolution
```
And note the functional increases along a genuine trajectory with c = 1/2 < 1 but ≥ 0, showing `1 ≤ c` cannot be weakened to `0 ≤ c` in `perelmanF_antitone`.

Wait — but `perelmanF_antitone`'s counterexample with c = 1/2: the theorem would conclude `perelmanF c (traj (1/2)) ≤ perelmanF c (traj 0)`, and we have the opposite strict inequality. So indeed the weakened statement (with `0 ≤ c`) is false. Good. I can formalize the negation:
```
theorem antitone_fails_at_c_half :
    ¬ (perelmanF (fun _ : Fin 1 => (1/2:ℝ)) (squareTraj (1/2)) ≤
       perelmanF (fun _ : Fin 1 => (1/2:ℝ)) (squareTraj 0)) :=
  not_le.mpr counterexample_continuous_c_half
```
This directly records that the conclusion of `perelmanF_antitone` fails for this data if the hypothesis is weakened to `0 ≤ c`.

Similarly for the discrete theorem:
```
theorem antitone_discrete_fails_at_c_half :
    ¬ (perelmanF (fun _ : Fin 1 => (1/2:ℝ))
        (eulerStep (squareField : ReactionField (Fin 1)) 1 (fun _ => (1:ℝ))) ≤
       perelmanF (fun _ : Fin 1 => (1/2:ℝ)) (fun _ => (1:ℝ))) :=
  not_le.mpr counterexample_discrete_c_half
```
Good. And the hypothesis `0 ≤ (1/2)` holds, so this is a genuine weakened-hypothesis counterexample.

### Section: negative step (h ≥ 0 sharp)

The cluster has `perelmanF_step_increases_of_negative_step`. I'll restate independently with different data to make the audit self-contained: c = 1, start lam = 2, h = -1, squareField: eval at 2 = 4, step to 2 - 4 = -2. gibbsTerm 1 (-2) = (1+4)e^{2} = 5e² vs gibbsTerm 1 2 = 5e^{-2}; increase. That's a clean independent counterexample with numbers 5e² > 5e^{-2}, i.e., 1 > e^{-4}. Easy: e^2 > e^{-2} since 2 > -2 and exp strictMono. Actually the functional at -2 is much larger.

```
theorem negative_step_counterexample :
    perelmanF (fun _ : Fin 1 => (1:ℝ)) (fun _ => (2:ℝ)) <
      perelmanF (fun _ : Fin 1 => (1:ℝ))
        (eulerStep (squareField : ReactionField (Fin 1)) (-1) (fun _ => (2:ℝ))) := by
  have hstep : eulerStep (squareField : ReactionField (Fin 1)) (-1) (fun _ => (2:ℝ)) = fun _ => (-2:ℝ) := by
    funext i; fin_cases i; norm_num [eulerStep, squareField_eval]
  rw [hstep]
  have hexp : Real.exp (-2) < Real.exp 2 := Real.exp_lt_exp.mpr (by norm_num)
  have h5 : 0 < (5:ℝ) := by norm_num
  -- goal: gibbsTerm 1 2 < gibbsTerm 1 (-2) i.e. 5 e^{-2} < 5 e^{2}
  simp only [perelmanF, Fin.sum_univ_one, gibbsTerm]
  norm_num
  -- goal: 5 * exp(-2) < 5 * exp(2)
  exact mul_lt_mul_of_pos_left hexp h5
```
Hmm, `gibbsTerm 1 2 = (1+4)e^{-2} = 5 e^{-2}`; `gibbsTerm 1 (-2) = (1+4)e^{2} = 5 e^2`. After `simp only [perelmanF, Fin.sum_univ_one, gibbsTerm]`, goal is `(1 + 2^2) * exp(-2) < (1 + (-2)^2) * exp 2`; `norm_num` reduces to `5 * exp(-2) < 5 * exp 2`. Then `mul_lt_mul_of_pos_left hexp h5`? That gives `5 * exp(-2) < 5 * exp 2`. Wait `mul_lt_mul_of_pos_left : a < b → 0 < c → c * a < c * b`. So `mul_lt_mul_of_pos_left hexp h5 : 5 * exp(-2) < 5 * exp 2`. Good.

`Real.exp_lt_exp.mpr` — the lemma is `Real.exp_lt_exp : exp x < exp y ↔ x < y`. Let me check: In mathlib, `Real.exp_lt_exp : Real.exp x < Real.exp y ↔ x < y`. Yes. Actually it might be `Real.exp_lt_exp`. Let me verify. There's `Real.exp_lt_exp` and `Real.exp_le_exp`. I'm fairly confident.

Alternatively, `exact mul_lt_mul_of_pos_left (Real.exp_lt_exp.mpr (by norm_num)) (by norm_num)`. Fine.

Then record:
```
theorem antitone_discrete_fails_at_negative_step :
    ¬ (perelmanF (fun _ : Fin 1 => (1:ℝ))
        (eulerStep (squareField : ReactionField (Fin 1)) (-1) (fun _ => (2:ℝ))) ≤
       perelmanF (fun _ : Fin 1 => (1:ℝ)) (fun _ => (2:ℝ))) :=
  not_le.mpr negative_step_counterexample
```
And note h = -1 violates `0 ≤ h`, so `0 ≤ h` is necessary.

### Section: negative reaction

As designed. Let me also verify `negTrajDisc_step` proof:
```
def negReaction : (Fin 1 → ℝ) → Fin 1 → ℝ := fun _ _ => -1
def negTrajDisc : ℕ → Fin 1 → ℝ := fun n _ => (1:ℝ) - n

theorem negTrajDisc_step (n : ℕ) (i : Fin 1) :
    negTrajDisc (n+1) i = negTrajDisc n i + (1:ℝ) * negReaction (negTrajDisc n) i := by
  simp [negTrajDisc, negReaction]
  ring
```
`negTrajDisc (n+1) i = 1 - (n+1)`; RHS `= 1 - n + 1 * (-1)`. After simp: goal `1 - (↑(n+1)) = 1 - ↑n + -1` maybe; `ring` handles Nat.cast (push_cast?). `simp` with `Nat.cast_add`, `Nat.cast_one` might normalize. `ring` can handle `↑(n+1)`? `ring` normalizes casts of numerals but not `↑(n+1)`. Need `push_cast` first:
```
  simp [negTrajDisc, negReaction]
  push_cast
  ring
```
Let me write `simp only [negTrajDisc, negReaction]; push_cast; ring`. Or `simp [negTrajDisc, negReaction, Nat.cast_add, Nat.cast_one]` then ring. I'll use push_cast.

Then the negated statement. Note the type: `G : (Fin 1 → ℝ) → Fin 1 → ℝ`.

```
theorem weak_discrete_monotonicity_false :
    ¬ (∀ (G : (Fin 1 → ℝ) → Fin 1 → ℝ) (traj : ℕ → Fin 1 → ℝ) (h : ℝ),
        0 ≤ h → (∀ n i, traj (n + 1) i = traj n i + h * G (traj n) i) →
        ∀ n, perelmanF (fun _ : Fin 1 => (1:ℝ)) (traj n) ≤
          perelmanF (fun _ : Fin 1 => (1:ℝ)) (traj 0)) := by
  intro H
  have hstep : perelmanF (fun _ : Fin 1 => (1:ℝ)) (negTrajDisc 1) ≤
      perelmanF (fun _ : Fin 1 => (1:ℝ)) (negTrajDisc 0) :=
    H negReaction negTrajDisc 1 (by norm_num) (fun n i => negTrajDisc_step n i) 1
  have h1 : perelmanF (fun _ : Fin 1 => (1:ℝ)) (negTrajDisc 1) = 1 := by
    norm_num [perelmanF, gibbsTerm, negTrajDisc, Fin.sum_univ_one]
  have h0 : perelmanF (fun _ : Fin 1 => (1:ℝ)) (negTrajDisc 0) = 2 * Real.exp (-1) := by
    norm_num [perelmanF, gibbsTerm, negTrajDisc, Fin.sum_univ_one]
  rw [h1, h0] at hstep
  have hlt : 2 * Real.exp (-1) < 1 := by
    rw [Real.exp_neg, inv_eq_one_div, mul_one_div, div_lt_one (Real.exp_pos 1)]
    linarith [Real.exp_one_gt_two]
  linarith
```
`div_lt_one` needs `0 < exp 1`; `Real.exp_pos 1` provides it. `linarith [Real.exp_one_gt_two]` closes `2 < exp 1`. Good.

Wait: `hstep` has `≤`, and hlt says RHS < LHS... Let me recompute: hstep : perelmanF (traj 1) ≤ perelmanF (traj 0) → after rw: `1 ≤ 2 * exp(-1)`. hlt : `2 * exp(-1) < 1`. Contradiction via linarith. Good.

Continuous negative reaction:
```
def negTrajCont : ℝ → Fin 1 → ℝ := fun t _ => (1:ℝ) - t

theorem negTrajCont_hasDeriv (t : ℝ) :
    HasDerivWithinAt (fun s : ℝ => negTrajCont s 0) (-1) (Ici t) t := by
  have h : HasDerivAt (fun s : ℝ => (1:ℝ) - s) (-1) t := by
    simpa using (hasDerivAt_id t).const_sub (1:ℝ)
  exact h.hasDerivWithinAt
```
Check `HasDerivAt.const_sub`: `(hasDerivAt_id t).const_sub c : HasDerivAt (fun y => c - y) (0 - 1) t`? The derivative of `fun y => c - y` is `-1`. `const_sub` gives derivative `0 - 1 = -1`. So `simpa` should close. Good.

```
theorem negative_reaction_continuous_counterexample :
    perelmanF (fun _ : Fin 1 => (1:ℝ)) (negTrajCont 0) <
      perelmanF (fun _ : Fin 1 => (1:ℝ)) (negTrajCont 1) := by
  have hstep : negTrajCont 1 = (fun _ : Fin 1 => (0:ℝ)) := by
    funext i; norm_num [negTrajCont]
  have hstep0 : negTrajCont 0 = (fun _ : Fin 1 => (1:ℝ)) := by
    funext i; norm_num [negTrajCont]
  rw [hstep, hstep0]
  have hlt : 2 * Real.exp (-1) < 1 := by
    rw [Real.exp_neg, inv_eq_one_div, mul_one_div, div_lt_one (Real.exp_pos 1)]
    linarith [Real.exp_one_gt_two]
  simpa [perelmanF, gibbsTerm, Fin.sum_univ_one] using hlt
```
Check: LHS perelmanF 1 (negTrajCont 0) = perelmanF 1 (fun _ => 1) = 2e^{-1}; RHS = perelmanF 1 0 = 1. So goal `2e^{-1} < 1`. hlt matches. `simpa [perelmanF, gibbsTerm, Fin.sum_univ_one]` on hlt: hlt is `2 * Real.exp (-1) < 1`; target after simp is `(1 + 1^2) * exp (-1) < (1 + 0^2) * exp 0` → `2 * exp(-1) < 1`. `simpa` with `Real.exp_zero`? simp knows `Real.exp 0 = 1`. Good.

This shows that if the reaction is allowed to be negative (violating `ReactionField.g_nonneg`/`a_nonneg` via `eval ≥ 0`), the continuous monotonicity fails. I'll also state the NoSignEvolution structure and instantiate, to tie it to a weakened hypothesis statement:
```
structure NoSignEvolution (G : (Fin 1 → ℝ) → Fin 1 → ℝ) (T : ℝ) (traj : ℝ → Fin 1 → ℝ) : Prop where
  continuous : ∀ i, ContinuousOn (fun t => traj t i) (Icc 0 T)
  hasDeriv : ∀ i, ∀ t ∈ Ico 0 T, HasDerivWithinAt (fun s => traj s i) (G (traj t) i) (Ici t) t

theorem noSignEvolution_neg : NoSignEvolution negReaction 1 negTrajCont where
  continuous := fun i => by fun_prop  -- maybe
  hasDeriv := fun i t _ => by
    -- G (traj t) i = -1
    simpa [negReaction] using negTrajCont_hasDeriv t
```
Continuity: `fun t => negTrajCont t i = 1 - t`; `fun_prop` or `continuousOn_const.sub continuousOn_id`. Let me use `by unfold negTrajCont; fun_prop`. Hmm, `negTrajCont` is a def; `fun_prop` may need unfolding. `exact (continuousOn_const.sub continuousOn_id)` might work: `ContinuousOn (fun t : ℝ => (1:ℝ) - t) (Icc 0 1)`. Let me write:
```
  continuous := fun i => by
    show ContinuousOn (fun t : ℝ => (1:ℝ) - t) (Icc 0 1)
    fun_prop
```
Should be fine.

`hasDeriv`: goal `HasDerivWithinAt (fun s => negTrajCont s i) (negReaction (negTrajCont t) i) (Ici t) t`. Since `negTrajCont s i = 1 - s` and `negReaction _ i = -1`. `simpa [negReaction, negTrajCont] using negTrajCont_hasDeriv t`. But `negTrajCont_hasDeriv` is stated for index 0; for arbitrary i : Fin 1, `i = 0` by Subsingleton.elim. Since `negTrajCont s i` for i : Fin 1 is definitionally `1 - s` (the def ignores i), `simpa [negTrajCont]` should work for any i because the function `fun s => negTrajCont s i` unfolds to `fun s => 1 - s`. Good.

Then:
```
theorem no_sign_continuous_monotonicity_false :
    ¬ (∀ (G : (Fin 1 → ℝ) → Fin 1 → ℝ) (traj : ℝ → Fin 1 → ℝ),
        NoSignEvolution G 1 traj →
        perelmanF (fun _ : Fin 1 => (1:ℝ)) (traj 1) ≤
          perelmanF (fun _ : Fin 1 => (1:ℝ)) (traj 0)) := by
  intro H
  have h := H negReaction negTrajCont noSignEvolution_neg
  exact absurd h (not_le.mpr negative_reaction_continuous_counterexample)
```
`not_le.mpr : a > b → ¬ a ≤ b`; our counterexample `perelmanF (traj 0) < perelmanF (traj 1)`, i.e., `perelmanF (traj 1) > perelmanF (traj 0)`, so `not_le.mpr negative_reaction_continuous_counterexample : ¬ perelmanF (traj 1) ≤ perelmanF (traj 0)`. Good.

### Section: strict-step hypothesis necessity

```
theorem zero_step_constant (F : ReactionField ι) (c : ι → ℝ) (lam : ι → ℝ) :
    perelmanF c (eulerStep F 0 lam) = perelmanF c lam := by
  simp [perelmanF, eulerStep]

theorem strict_step_needs_pos_step (F : ReactionField ι) (c : ι → ℝ) (lam : ι → ℝ) :
    ¬ perelmanF c (eulerStep F 0 lam) < perelmanF c lam := by
  rw [zero_step_constant]
  exact lt_irrefl _
```
Hmm, `eulerStep F 0 lam` unfolds to `fun i => lam i + 0 * F.eval lam i`; simp gives `lam i + 0 = lam i`; so `eulerStep F 0 lam = lam` maybe by `funext`. `simp [perelmanF, eulerStep]` should simplify the summands: `gibbsTerm (c i) (eulerStep F 0 lam i)` → `gibbsTerm (c i) (lam i + 0 * ...)` → `gibbsTerm (c i) (lam i)`. So the sums are equal. `simp` proves `∑ gibbsTerm (c i) (...) = ∑ gibbsTerm (c i) (lam i)`. Good.

```
theorem strict_step_needs_pos_reaction :
    ¬ perelmanF (fun _ : Fin 1 => (2:ℝ))
        (eulerStep (ReactionField.hamilton : ReactionField (Fin 1)) 1 (fun _ => (0:ℝ)))
      < perelmanF (fun _ : Fin 1 => (2:ℝ)) (fun _ => (0:ℝ)) := by
  have h : eulerStep (ReactionField.hamilton : ReactionField (Fin 1)) 1 (fun _ => (0:ℝ)) =
      (fun _ => (0:ℝ)) := by
    funext i
    simp [eulerStep]
  rw [h]
  exact lt_irrefl _
```
Check `simp [eulerStep]` on goal `0 + 1 * (hamilton).eval (fun _ => 0) i = 0`: hamilton eval at 0 is simp lemma `ReactionField.hamilton_eval_zero`. Good.

Positive control for strict step with the corrected hypothesis:
```
theorem strict_step_positive_control :
    perelmanF (fun _ : Fin 1 => (1:ℝ))
        (eulerStep (squareField : ReactionField (Fin 1)) 1 (fun _ => (1:ℝ))) <
      perelmanF (fun _ : Fin 1 => (1:ℝ)) (fun _ => (1:ℝ)) := by
  -- apply corrected theorem with c = 1 (the overstrong hypothesis would need 1 < c)
  exact perelmanF_step_lt_of_one_le (squareField : ReactionField (Fin 1))
    (fun _ => le_refl 1) (h := 1) (by norm_num) unitDiscreteEvolution? ...
```
Hmm, need a DiscreteEvolution for squareField with h=1 and traj n = ...? The trajectory must satisfy traj (n+1) = traj n + 1 * eval. With squareField eval = lam². Starting at 1: traj 1 = 2. So define traj by recursion: traj 0 = 1, traj (n+1) = traj n + traj n². The theorem `perelmanF_step_lt_of_one_le` needs `DiscreteEvolution F 1 traj` and `F.eval (traj 0) 0 > 0`. I could define a concrete `DiscreteEvolution` using `Nat.rec`. Simpler: use `eulerStep` iteration:
```
def sqTraj : ℕ → Fin 1 → ℝ
  | 0 => fun _ => 1
  | n+1 => eulerStep (squareField : ReactionField (Fin 1)) 1 (sqTraj n)

theorem sqTraj_evolution : DiscreteEvolution (squareField : ReactionField (Fin 1)) 1 sqTraj where
  step := by
    intro n i
    -- sqTraj (n+1) i = eulerStep ... (sqTraj n) i = sqTraj n i + 1 * eval
    rfl  -- after unfolding eulerStep? sqTraj (n+1) = eulerStep ..., so i-th component equals eulerStep ... i = ...
```
`sqTraj (n+1)` is defined as `eulerStep ... (sqTraj n)`, so `sqTraj (n+1) i` unfolds to `(eulerStep F 1 (sqTraj n)) i` which is `sqTraj n i + 1 * F.eval (sqTraj n) i` by def of eulerStep. So `rfl` might work after `show`. Let me write:
```
theorem sqTraj_evolution : DiscreteEvolution (squareField : ReactionField (Fin 1)) 1 sqTraj where
  step := fun n i => rfl
```
Hmm, the goal after `intro n i` is `sqTraj (n+1) i = sqTraj n i + 1 * (squareField).eval (sqTraj n) i`. `sqTraj (n+1)` reduces to `eulerStep squareField 1 (sqTraj n)`, which reduces to `fun i => sqTraj n i + 1 * eval (sqTraj n) i`, so the i-th component is exactly RHS. `rfl` should work (defeq). Good.

Then:
```
theorem strict_step_positive_control :
    perelmanF (fun _ : Fin 1 => (1:ℝ)) (sqTraj 1) < perelmanF (fun _ : Fin 1 => (1:ℝ)) (sqTraj 0) := by
  apply perelmanF_step_lt_of_one_le (squareField : ReactionField (Fin 1))
    (c := fun _ : Fin 1 => (1:ℝ)) (fun _ => le_refl 1) (h := 1) (by norm_num) sqTraj_evolution
  -- goal: 0 < squareField.eval (sqTraj 0) 0
  simp [sqTraj, squareField_eval]
```
Wait the implicit args of `perelmanF_step_lt_of_one_le`: `(F) {c} (hc) {h} (hh) {traj} (ev) {n} {i} (hi)`. Applying with `(h := 1)` and ev and then the goal `0 < F.eval (traj n) i` with n, i metavariables. I should specify `(n := 0) (i := 0)`. Let me write:
```
  exact perelmanF_step_lt_of_one_le (squareField : ReactionField (Fin 1))
    (c := fun _ : Fin 1 => (1:ℝ)) (fun _ => le_refl 1) (h := 1) (by norm_num)
    (traj := sqTraj) sqTraj_evolution (n := 0) (i := 0) (by simp [sqTraj, squareField_eval])
```
Hmm, the hypothesis `hi` is `0 < F.eval (traj 0) 0`; `simp [sqTraj, squareField_eval]` proves `0 < 1^2`. Good.

But wait: `sqTraj 1` vs `sqTraj 0`: the theorem concludes `perelmanF c (traj (n+1)) < perelmanF c (traj n)`, so with n=0: `perelmanF c (sqTraj 1) < perelmanF c (sqTraj 0)`. Good.

This demonstrates the corrected theorem at c=1, where the original `perelmanF_step_lt` (needing 1 < c) would not apply.

### Section: nonneg sharpness for perelmanF_nonneg

```
theorem perelmanF_nonneg_sharp :
    perelmanF (fun _ : Fin 1 => (-1:ℝ)) (fun _ => (0:ℝ)) < 0 := by
  simp [perelmanF, gibbsTerm, Fin.sum_univ_one]
```
Compute: gibbsTerm (-1) 0 = (-1 + 0) * 1 = -1 < 0. `simp` should reduce to `-1 < 0`; maybe need `norm_num` after. Write `norm_num [perelmanF, gibbsTerm, Fin.sum_univ_one]`.

### Section: corrected theorem statements

I'll state:
```
theorem corrected_gibbsTerm_strictAnti (c : ℝ) (hc : 1 ≤ c) : StrictAnti (gibbsTerm c) := ...
theorem corrected_perelmanF_step_lt ... (hc : ∀ i, 1 ≤ c i) ... := ...
```
Already designed.

### Section: non-vacuity

As designed.

Also maybe check `perelmanF_step_le` non-vacuity with h=0? Not needed.

### Section: certificate checks

```
theorem certificate_lower_bound_zero ... :
    (perelmanAntitoneCertificate F hc ev).lowerBound = 0 := rfl
```
Actually `lowerBound` is a field of the structure; `rfl` proves `(perelmanAntitoneCertificate ...).lowerBound = 0`? The def was constructed with `lowerBound := 0`, so `rfl` works.

And apply the certificate:
```
theorem certificate_compare_sanity (F : ReactionField ι) {c : ι → ℝ} (hc : ∀ i, 1 ≤ c i)
    {T : ℝ} {traj : ℝ → ι → ℝ} (ev : EvolutionRelation F T traj) {a b : {t : ℝ // t ∈ Icc 0 T}}
    (hab : a ≤ b) : perelmanF c (traj b.1) ≤ perelmanF c (traj a.1) :=
  perelmanF_certificate_compare F hc ev hab
```

And the continuous certificate:
```
theorem continuousCertificate_sanity [MeasurableSpace ι] [MeasurableSingletonClass ι]
    {F : ReactionField ι} {traj : ℝ → ι → ℝ} (G : GlobalReactionFlow F traj)
    {c : ι → ℝ} (hc : ∀ i, 1 ≤ c i) {t : ℝ} (ht : 0 ≤ t) :
    (finiteReactionEntropyData c (traj t)).F ≤ (finiteReactionEntropyData c (traj 0)).F :=
  continuousPerelmanCertificate_le_initial G hc ht
```
Fine.

### Section: axiom print

At the end:
```
#print axioms audit_perelmanF_antitone
#print axioms corrected_perelmanF_step_lt
#print axioms counterexample_continuous_c_half
...
#print axioms Poincare.Longrun.Evolution.perelmanF_antitone
#print axioms Poincare.Longrun.Evolution.perelmanF_step_lt
...
```
The task says runs `#print axioms`. I'll print for all my declarations and the promoted main ones.

### Hygiene
No sorry/axiom/unsafe/native_decide/proof_wanted. I'll grep the file.

Now let me also double check the overstrong finding once more. `perelmanF_step_lt` requires `hc : ∀ i, 1 < c i`. Is there a case where c i = 1 and the functional does NOT strictly decrease despite h > 0 and F.eval (traj n) i > 0? The i-th term: gibbsTerm 1 (x + u) < gibbsTerm 1 x for u > 0. Yes strictly decreases. Other terms weakly decrease. Sum strictly decreases. So yes, overstrong. 

Also `gibbsTerm_strictAnti` (1 < c) and `gibbsTerm_step_lt` (1 < c) are overstrong. I'll record all three.

Let me also consider: is `gibbsTerm_antitone`'s `1 ≤ c` exactly sharp? Yes, for c < 1 take x=1, y=2 (both c+1, c+4 positive weights; need (c+1)e^{-1} < (c+4)e^{-2} for c=1/2 yes; for general c<1? c+4 > (c+1)e ⟺ c(e-1) < 4-e. For c=0: 0 < 4-e true. For c close to 1: (e-1) < 4-e ⟺ 2e < 5 ⟺ e < 2.5, false! So for c = 0.9, gibbsTerm 0.9 1 vs gibbsTerm 0.9 2: (1.9)e^{-1}=0.699 vs (4.9)e^{-2}=0.663 → decreases, so x=1,y=2 doesn't witness failure for c=0.9. But failure near x=1 with small increments: derivative at x=1 is -(c-1)e^{-1} > 0 for c<1, so locally increasing. E.g., c=0.9, x=1, y=1.1: derivative positive, so gibbsTerm increases. Any proof in Lean would need continuity/derivative argument. For the audit, c=1/2 with x=1,y=2 suffices to show the theorem fails when c<1 (since the theorem is ∀ c, so one counterexample at c=1/2 shows `1 ≤ c` can't be weakened to `0 ≤ c`). To show sharpness at the threshold for *all* c<1 would require more work; not necessary. The result card's claim "threshold sharp" is supported by derivative at c=1 vanishing. Fine.

Now, one more possible issue to check: the result card claims `perelmanF_step_increases_of_c_zero` shows `1 ≤ c` cannot be weakened to `0 ≤ c` — but it only shows c=0 fails. Our c=1/2 strengthens this (still ≥ 0). Good addition.

Another potential adversarial check: Is `perelmanF_antitone`'s conclusion true at s=t? Yes.

Is there any hidden `noncomputable`/Classical issue? No.

Another check: the umbrella imports `Counterexample` (which contains counterexamples), so the library depends on them. Not an issue.

Now let me also consider testing `hasDerivWithinAt_perelmanF` numerically: check the derivative formula at a specific point. E.g., F = unitReactionField, traj t = t, c = 1, t = 0: derivative of perelmanF along traj = -Σ F.eval * ((lam-1)²+(c-1)) * exp(-lam) at lam=0: -[1 * (1 + 0) * 1] = -1. And indeed perelmanF 1 (traj t) = (1+t²)e^{-t}; derivative at 0 = 0 - 1 = -1. I can derive the derivative value from the theorem:
```
theorem dissipation_sanity :
    HasDerivAt (fun s : ℝ => perelmanF (fun _ : Fin 1 => (1:ℝ)) (fun _ => s)) (-1) 0 := by
  have h := hasDerivAt_perelmanF (unitGlobalFlow (ι := Fin 1)) (fun _ : Fin 1 => (1:ℝ)) 0
  simpa using h
```
`hasDerivAt_perelmanF G c t` : `HasDerivAt (fun s => perelmanF c (traj s)) (-∑ i, F.eval (traj t) i * ...) t`. With G = unitGlobalFlow: traj t i = t, F.eval = 1. The derivative expression: `-∑ i : Fin 1, 1 * ((t - 1)^2 + (1-1)) * exp(-t)` at t=0 = `-∑ i, 1 * (1 + 0) * 1 = -1`. `simpa` should compute. Let me verify the expression matches: `unitReactionField_eval` is a simp lemma: `(unitReactionField : ReactionField ι).eval lam i = 1`. The `∑` over Fin 1 of `1 * ((t-1)^2 + (1-1)) * exp(-t)` at t=0: simp might reduce `Fin.sum_univ_one`, `Real.exp_zero`, etc. to `-1`. Should work with `simp [unitGlobalFlow]`? The trajectory `fun t _ => t` is part of the structure; after `hasDerivAt_perelmanF (unitGlobalFlow ...)`, the function is `fun s => perelmanF c ((fun t _ => t) s)` which is `fun s => perelmanF c (fun _ => s)`. `simpa` should handle beta reduction. Let me include it as a derivative sanity check.

Also check the exact dissipation identity orientation with a strict decrease: `perelmanF_le_initial` etc.

I think the file is getting big but manageable. Let me also think about whether to include the `two_sided_monotonicity` check — a direct re-derivation:
```
theorem audit_two_sided ... := two_sided_monotonicity F hc ev ht
```
Fine.

Let me now check the build job status, then write the file.
dsh: reasoning:
Build completed successfully in my worktree. Now write the verifier file.

Let me carefully compose `Audit/CounterexampleAudit.lean`.

One consideration: the `#print axioms` directives print to stdout. Fine.

Let me write the file now.

Also: the task says "imports only the promoted interfaces". I'll import only `Poincare.Longrun.Evolution`. Since the library modules are the promoted interfaces. Good.

Let me draft the complete file:

```lean
/-
# D4-counterexample-audit — independent adversarial verification

...
-/
import Poincare.Longrun.Evolution

open Set Filter MeasureTheory
open scoped Topology BigOperators

namespace D4Audit

open Poincare.Longrun.CurvatureODE
open Poincare.Longrun.Entropy
open Poincare.Longrun.Evolution

universe w

variable {ι : Type w} [Fintype ι]

/-! ## 1. The promoted main theorem, re-checked in a fresh namespace -/

theorem audit_perelmanF_antitone ...
```

Wait: there's a subtlety with `variable {ι : Type w} [Fintype ι]` and section variables. Theorems using `ι` will get implicit `{ι : Type w} [Fintype ι]` params. Fine.

For the `perelmanF` in `D4Audit` namespace: since I `open Poincare.Longrun.Evolution`, `perelmanF` resolves. But I should double check there's no name clash with `Poincare.Longrun.Entropy`'s stuff. `perelmanF` is unique.

Now, `Finset.sum_lt_sum` might need `DecidableEq`? No.

Let me write out the whole file and then compile. I'll be careful about:
- `not_le.mpr` requires `a > b` (i.e., `b < a`) to prove `¬ a ≤ b`. Check: `not_le.mpr : b < a → ¬ a ≤ b`. Yes.
- For `antitone_fails_at_c_half`, I want `¬ (perelmanF ... (squareTraj (1/2)) ≤ perelmanF ... (squareTraj 0))`; the counterexample gives `perelmanF ... (squareTraj 0) < perelmanF ... (squareTraj (1/2))`. So `not_le.mpr counterexample_continuous_c_half` where not_le expects `b < a` to prove `¬ a ≤ b`: here a = perelmanF (squareTraj (1/2)), b = perelmanF (squareTraj 0), and b < a is the counterexample. Good.

Let me write the code now. I'll include comments recording findings.

For `negTrajCont_hasDeriv`, let me double check `(hasDerivAt_id t).const_sub (1:ℝ)`. In mathlib, `HasDerivAt.const_sub (c : 𝕜) (h : HasDerivAt f f' x) : HasDerivAt (fun y => c - f y) (-f') x`? Let me check: `HasDerivAt.const_sub : HasDerivAt f f' x → HasDerivAt (fun y => c - f y) (-f') x`. Actually the method is `(hasDerivAt_id t).const_sub c` where `c` is the constant. Let me check mathlib:
```
theorem HasDerivAt.const_sub (hc : HasDerivAt f f' x) (c : 𝕜) :
    HasDerivAt (fun y => c - f y) (-f') x
```
Hmm, I recall `HasDerivAt.const_sub (c : 𝕜) (h : HasDerivAt f f' x) : HasDerivAt (fun y => c - f y) (-f') x`? Let me grep to be safe.

Actually, in the D4 Counterexample.lean they used `(hasDerivAt_id t).const_sub (1 : ℝ)` for `fun s => 1 - s`, so the method exists with that argument order. Good.

Let me also double check `Real.exp_one_gt_two` and `Real.exp_one_lt_three` exist (used in cluster; yes).

For `tendsto_pi_nhds`, let me verify the name. In mathlib: `theorem tendsto_pi_nhds {f : ι → ∀ i, π i} ... : Tendsto f l (𝓝 g) ↔ ∀ i, Tendsto (fun x => f x i) l (𝓝 (g i))`. Yes, `tendsto_pi_nhds` exists in Mathlib/Topology/Basic.lean or Pi. Let me grep quickly later.

Also, for `limit_passage_sanity`, the sequence `fun n : ℕ => (n:ℝ)/(n+1)`: I want Tendsto to 1. Alternative simpler: use `fun n => 1 - 1/(n+1)` directly as state, avoiding the equality proof:
```
apply perelmanF_limit_le_of_discrete (state := fun n (_:Fin 1) => 1 - 1/((n:ℝ)+1)) (limit := fun _ => 1)
· intro n
  have hle : 0 ≤ 1 - 1/((n:ℝ)+1) := by positivity
  have h := gibbsTerm_antitone 1 le_rfl hle
  -- h : gibbsTerm 1 (1 - 1/(n+1)) ≤ gibbsTerm 1 0
  simpa [perelmanF, Fin.sum_univ_one] using h
· exact tendsto_pi_nhds.mpr (fun i => by
    fin_cases i
    simpa using tendsto_const_nhds.sub tendsto_one_div_add_atTop_nhds_zero_nat)
```
Here `tendsto_const_nhds.sub tendsto_one_div_add_atTop_nhds_zero_nat : Tendsto (fun n => (1:ℝ) - 1/((n:ℝ)+1)) atTop (𝓝 (1 - 0)) = 𝓝 1`. `simpa` should close the goal `Tendsto (fun n => 1 - 1/((n:ℝ)+1)) atTop (𝓝 1)`. 

`positivity` for `0 ≤ 1 - 1/(n+1)`: positivity can prove `0 ≤ 1 - 1/(n+1)`? It needs `1/(n+1) ≤ 1`. `positivity` might handle it: `1/(n+1) ≤ 1` since n+1 ≥ 1. Not sure. Let me prove it manually:
```
have hn : (1:ℝ) ≤ (n:ℝ) + 1 := by
  have : (0:ℝ) ≤ n := Nat.cast_nonneg n
  linarith
have hle : 0 ≤ 1 - 1/((n:ℝ)+1) := by
  rw [sub_nonneg]
  rw [one_div_le_one (by positivity)]  -- hmm
```
Simpler: `1 - 1/(n+1) = n/(n+1) ≥ 0`:
```
have hle : 0 ≤ 1 - 1/((n:ℝ)+1) := by
  have h1 : (0:ℝ) < (n:ℝ) + 1 := by positivity
  rw [sub_nonneg, one_div_le_one h1]  -- goal: 1 ≤ n+1?
```
`one_div_le_one : 0 < a → (1 / a ≤ 1 ↔ 1 ≤ a)`. Let me check name: `one_div_le_one` might be stated as `one_div_le_one (ha : 0 < a) : 1 / a ≤ 1 ↔ 1 ≤ a`. Then goal `1 ≤ n + 1` from `0 ≤ n`. Let me write:
```
have hle : 0 ≤ 1 - 1 / ((n : ℝ) + 1) := by
  rw [sub_nonneg]
  rw [one_div_le_one (by positivity)]
  have : (0:ℝ) ≤ n := Nat.cast_nonneg n
  linarith
```
If `one_div_le_one` doesn't exist with that signature, alternative: `div_le_one`? `1/(n+1) ≤ 1` ⟺ `1 ≤ n+1` via `div_le_one (by positivity)`. There's `div_le_one (ha : 0 < b) : a / b ≤ 1 ↔ a ≤ b`. Since `1/(n+1) = 1 / (n+1)`, `div_le_one h1 : 1 / (n+1) ≤ 1 ↔ 1 ≤ n+1`. Use that:
```
rw [div_le_one (by positivity)]
```
Goal becomes `1 ≤ n+1`. Good. Let me use `div_le_one`.

Actually simplest: `have hle : 0 ≤ 1 - 1 / ((n : ℝ) + 1) := by positivity` might just work since positivity knows `1/(n+1) ≤ 1`? Not certain. I'll use the explicit route with `div_le_one`.

Hmm wait, `div_le_one` statement: `theorem div_le_one (ha : 0 < a) : b / a ≤ 1 ↔ b ≤ a`. For b = 1, a = n+1. So `rw [div_le_one h1]` transforms `1/(n+1) ≤ 1` into `1 ≤ n+1`. Good.

Then `gibbsTerm_antitone 1 le_rfl hle` where `hle : 0 ≤ 1 - 1/(n+1)` gives `gibbsTerm 1 (1 - 1/(n+1)) ≤ gibbsTerm 1 0` (Antitone: a ≤ b → f b ≤ f a; here a = 0, b = 1 - 1/(n+1), so need `0 ≤ 1 - 1/(n+1)` and get f b ≤ f a). Good.

`simpa [perelmanF, Fin.sum_univ_one] using h` — the goal is `perelmanF 1 (state n) ≤ perelmanF 1 (state 0)` where state 0 = `fun _ => 1 - 1/(0+1) = 0` and state n as above. After simp, state 0's component = `1 - 1/1 = 0`. So goal becomes `gibbsTerm 1 (1 - 1/(n+1)) ≤ gibbsTerm 1 0`. h matches. Good.

Now `perelmanF_limit_le_of_discrete` has implicit `{ι} [Fintype ι] {c} {state} {limit}`. Providing `(state := ...) (limit := ...)`. The hmono argument first, then hlim. Good.

Let me now write the file. I'll write it with the `write` tool.

Also need to double check: in section 1, `audit_perelmanF_antitone` type: I use `⦃s t : ℝ⦄` binder syntax. Fine.

One more thought on `#print axioms`: the file must not contain the string `sorry` etc. even in comments? The task says must not use `sorry`, `axiom`, `unsafe`, `native_decide`, `proof_wanted`. I'll avoid mentioning them except maybe in a hygiene comment. The D4 cluster had docstrings mentioning "No `sorry`, `axiom`..." and their hygiene grep looked for lines starting with the keywords. I'll avoid the words entirely in my file to make the grep trivially clean. Actually, I want a comment noting hygiene; I can phrase without the forbidden words. Better: avoid them entirely.

Also avoid `axiom` as a substring? `#print axioms` contains "axioms" — that's a directive, not the keyword `axiom`. The task explicitly asks to run `#print axioms`, so it's fine. A naive grep for `\baxiom\b` would match `axioms`? `\baxiom\b` would not match `axioms` because `s` is a word char (no boundary between m and s). Good. But I'll run a proper hygiene grep `^\s*(sorry|axiom|unsafe|native_decide|proof_wanted)\b` like the cluster.

Let me write the file now.

Also, I realize I should double-check `eulerStep_squareField_one` name resolution: it's `Poincare.Longrun.Evolution.eulerStep_squareField_one` and `squareField_eval`. Yes.

And `unitDiscreteEvolution` — not needed.

For the corrected theorem, `perelmanF_step_lt_of_one_le`, note the name `perelmanF_step_lt_of_one_le` and the hypothesis `hc : ∀ i, 1 ≤ c i`.

Potential issue with `Finset.sum_lt_sum`: the form in D4 was `Finset.sum_lt_sum (fun j _ => ...) ⟨i, Finset.mem_univ i, ...⟩`. Good.

Now writing.

Additional check: `strictAntiOn_of_deriv_neg` requires `hD : Convex ℝ D`; `convex_Iic 1`, `convex_Ici 1` exist. `hcont.continuousOn`. In the `hleft` proof:
```
refine strictAntiOn_of_deriv_neg (convex_Iic (1:ℝ)) hcont.continuousOn (fun x hx => ?_)
rw [interior_Iic] at hx
exact hneg x (ne_of_lt hx)
```
`hneg x : x ≠ 1 → deriv ... < 0`. `ne_of_lt hx : x ≠ 1` requires hx : x < 1. Good. But `interior_Iic` requires `[NoMaxOrder α]` — instance for ℝ exists. The rewrite direction: `interior (Iic 1) = Iio 1`; `rw [interior_Iic] at hx` gives `hx : x ∈ Iio 1` = `x < 1`. Good.

`StrictAntiOn` argument order: `hleft (ha) (hb) (hab)`. In my case:
- case `hy : y ≤ 1` (with hxy : x < y): `hleft (le_of_lt (lt_of_lt_of_le hxy hy)) hy hxy`. Simpler: `hleft (by linarith) hy hxy`. linarith with hxy, hy proves x ≤ 1. Good.
- case `1 ≤ x`: `hright hx1 hy_mem hxy` where hy_mem : y ∈ Ici 1 from `le_of_lt hy'` (hy' : 1 < y after push_neg).
- last case: as designed.

Let me handle the rcases carefully:
```
intro x y hxy
rcases le_or_lt y 1 with hy | hy
· exact hleft (by linarith) hy hxy
· -- hy : 1 < y
  by_cases hx1 : 1 ≤ x
  · exact hright hx1 (le_of_lt hy) hxy
  · push_neg at hx1
    have h1 : gibbsTerm 1 1 < gibbsTerm 1 x := hleft (le_refl 1) (le_of_lt hx1) hx1
    have h2 : gibbsTerm 1 y < gibbsTerm 1 1 := hright (le_refl 1) (le_of_lt hy) hy
    exact h2.trans h1
```
Wait: in the first branch `rcases le_or_lt y 1 with hy | hy`, first case `hy : y ≤ 1`; then `hleft (a := x) (by linarith) (b := y) hy hxy`. The implicit args: `StrictAntiOn f s` is `∀ ⦃a⦄, a ∈ s → ∀ ⦃b⦄, b ∈ s → a < b → f b < f a`. So `hleft hx_mem hy_mem hxy`. The a is inferred from hx_mem. Fine.

Second case `hy : 1 < y`.

`hright` on `Ici 1` similarly: `hright (a := x) hx1 (b := y) (le_of_lt hy) hxy` gives `gibbsTerm 1 y < gibbsTerm 1 x`. Good.

For the last: `h1 : gibbsTerm 1 1 < gibbsTerm 1 x` — here a = 1, b = x, need a < b i.e. 1 < x? No wait: `hleft` gives `f b < f a` for `a < b`. I want `f 1 < f x`, so a = x, b = 1 with x < 1. So `hleft (a := x) (mem) (b := 1) (mem) hx1 : f 1 < f x`. Good. So write `hleft (a := x) (le_of_lt hx1) (le_refl 1) hx1`.
`h2 : f y < f 1` — `hright (a := 1) (le_refl 1) (b := y) (le_of_lt hy) hy : f y < f 1`. Good.
Then `h2.trans h1 : f y < f x`. 

Now, `strictAntiOn_of_deriv_neg` returns `StrictAntiOn (gibbsTerm 1) (Iic 1)`. Let me make sure of the exact definition of StrictAntiOn to get argument order right. Let me grep mathlib.
dsh: reasoning:
`StrictAntiOn` order confirmed: `∀ a ∈ s, ∀ b ∈ s, a < b → f b < f a`. Good.

`tendsto_pi_nhds` exists. `div_le_one` exists. `exp_lt_exp` not found in Exp.lean — let me search elsewhere. Maybe `Real.exp_lt_exp`.
dsh: reasoning:
Hmm, `Real.exp_lt_exp` not found in the mathlib Exp.lean? Let me search more broadly.
dsh: reasoning:
`exp_lt_exp` is used unqualified in the Real namespace context (`Real.exp_lt_exp`). Let me find its statement: probably `Real.exp_lt_exp : Real.exp x < Real.exp y ↔ x < y`. Let me grep for the definition.
dsh: reasoning:
There's `exp_lt_exp` for the `Real.exp`? Line 521 in Canonical.lean is for `MonoidWithZero` canonical exp? Hmm, that's a generic `exp` (zpow?). Let me search in the `Real` namespace more specifically. The lemma `Real.exp_lt_exp` may be in `Mathlib/Analysis/SpecialFunctions/Exp.lean` as `theorem exp_lt_exp` inside `namespace Real`. Let me grep for "strictMono_exp" or "exp_strictMono".
dsh: reasoning:
`Real.exp_le_exp` exists. Let me find `Real.exp_lt_exp` in the Complex or other files. Search for "exp_lt_exp" under `namespace Real` context or the file that defines `Real.exp_le_exp`.
dsh: reasoning:
`Real.exp_lt_exp {x y : ℝ} : Real.exp x < Real.exp y ↔ x < y` exists in Mathlib/Analysis/Complex/Exponential.lean line 312 (namespace Real presumably). Good.

Now let me write the verifier file. I'll include everything designed. Let me be careful and complete.

One more: `Real.exp_one_lt_three` and `Real.exp_one_gt_two` — check they exist in mathlib. The D4 cluster used both, so yes.

Also `Real.exp_one_lt_d9` used by cluster. Not needed.

Let me write the file.

I also want to include a check that `perelmanF_antitone` is not vacuous: instantiate with `unitReactionField`, `unitGlobalFlow`, c = 1, T = 1, and derive a concrete comparison. That's a good "checks the theorem in a fresh namespace" plus non-vacuity. E.g.:
```
theorem nonvacuity_main :
    perelmanF (fun _ : Fin 1 => (1:ℝ)) ((fun t (_:Fin 1) => t) 1) ≤
      perelmanF (fun _ : Fin 1 => (1:ℝ)) ((fun t (_:Fin 1) => t) 0) :=
  audit_perelmanF_antitone (unitReactionField : ReactionField (Fin 1))
    (fun _ => le_refl 1) (GlobalReactionFlow.evolutionRelation (unitGlobalFlow (ι := Fin 1)) 1)
    (by norm_num) (by norm_num) (by norm_num)
```
Note `unitGlobalFlow` is a theorem with type `GlobalReactionFlow (unitReactionField) (fun t _ => t)`. `GlobalReactionFlow.evolutionRelation (unitGlobalFlow ...) 1 : EvolutionRelation unitReactionField 1 (fun t _ => t)`. Good.

And strict decrease:
```
theorem nonvacuity_strict :
    perelmanF (fun _ : Fin 1 => (1:ℝ)) ((fun t (_:Fin 1) => t) 1) <
      perelmanF (fun _ : Fin 1 => (1:ℝ)) ((fun t (_:Fin 1) => t) 0) := by
  have h := gibbsTerm_one_two_lt  -- gibbsTerm 1 2 < gibbsTerm 1 1
  simpa [perelmanF, Fin.sum_univ_one] using h
```
This uses the cluster's counterexample lemma. Good; or prove independently: gibbsTerm 1 2 < gibbsTerm 1 1, that's the cluster's `gibbsTerm_one_two_lt`. Fine to reuse (it's a promoted declaration).

Now, the audit file will be long. Let me write it.

Actually, one important consideration: I should double-check the claim that the corrected theorem `perelmanF_step_lt_of_one_le` is *not* derivable from the existing `perelmanF_step_lt` — i.e., the correction is genuinely a strengthening. At c = 1, existing requires 1 < 1 (false), so the existing theorem can't apply. My positive control uses c=1. Good.

Let me also think about whether there might be a subtlety: `perelmanF_step_lt_of_one_le`'s hypothesis `hc : ∀ i, 1 ≤ c i`; at c=1 and `F.eval (traj 0) 0 = 1 > 0`, h=1. Strict decrease. Yes.

Let me now write the file. I'll organize into sections with comments that will also feed the result card.

Let me write the content.

```lean
/-
# D4-counterexample-audit — adversarial verification of the D4 evolution cluster

This driver imports **only** the promoted umbrella interface
`Poincare.Longrun.Evolution` and re-examines its main theorem from a fresh namespace
`D4Audit`. It contains, in order:

1. fresh-namespace re-checks of the promoted monotonicity theorem and its consequences;
2. independent counterexamples to weakened hypotheses:
   * `0 ≤ c` (instead of `1 ≤ c`) fails already at `c = 1/2`, in discrete and continuous time;
   * `h < 0` (instead of `0 ≤ h`) fails;
   * a negative reaction (i.e. dropping the D2 `ReactionField.eval_nonneg` content) fails;
   * `0 < h` and `0 < F.eval` are needed for strict decrease;
   * `0 ≤ c` is needed for `perelmanF_nonneg`;
3. a corrected (sharpened) strict-decrease theorem: `1 < c i` can be weakened to `1 ≤ c i`,
   because the flat spot of `(1+x²)e^{-x}` at `x = 1` is isolated; the corrected statement is
   proved here;
4. non-vacuity witnesses for the statement-only approximation boundary;
5. `#print axioms` for every declaration of this file and for the promoted main declarations.

No forbidden proof escape is used in this file.
-/
```

Hmm "No forbidden proof escape" avoids the words. Good.

Now, writing the actual code. Let me write it all.

For the `#print axioms` section, I'll list:
- my audit theorems
- promoted: perelmanF_antitone, perelmanF_antitone_discrete, perelmanF_step_lt, perelmanF_nonneg, continuousPerelmanCertificate, perelmanF_limit_le_of_discrete, continuousPerelmanFMonotone_of_approximation, perelmanF_monotone_of_tensorBridge, squareTraj_counterexample.

Let me write the file now with the write tool.
dsh: reasoning:
Now compile it. Let me run `lake env lean Audit/CounterexampleAudit.lean` and capture output. It may have errors; iterate.
dsh: reasoning:
Several errors to fix. Note the `sorryAx` appears in declarations whose proofs failed (Lean inserts sorryAx on errors). Once fixed, they'll be clean.

Errors:
1. Line 87: `nonvacuity_main_theorem_strict` — the flow is `fun t _ => t`, so at t=1 state = 1, at t=0 state = 0. Goal after simp: `gibbsTerm 1 1 < gibbsTerm 1 0`. But `gibbsTerm_one_two_lt : gibbsTerm 1 2 < gibbsTerm 1 1`. Wrong direction/points. I should compare t=0 to t=2: `perelmanF 1 (traj 2) < perelmanF 1 (traj 0)` gives gibbsTerm 1 2 < gibbsTerm 1 0. `gibbsTerm_one_two_lt` is `gibbsTerm 1 2 < gibbsTerm 1 1`, not 0. So use traj 0 vs traj 2? Actually I want strict decrease along the flow: at t=2, value = gibbsTerm 1 2, at t=1 value = gibbsTerm 1 1. So `perelmanF 1 (traj 2) < perelmanF 1 (traj 1)` matches. Let me use that: `nonvacuity_main_theorem_strict : perelmanF 1 (traj 2) < perelmanF 1 (traj 1)`. Or prove `gibbsTerm 1 1 < gibbsTerm 1 0` directly: (1+1)e^{-1} < (1+0)e^0 = 1, i.e., 2e^{-1} < 1, which we have (`hlt`). Simpler: use `hlt : 2 * exp(-1) < 1` and simp. Let me change to compare t=1 vs t=0:
```
theorem nonvacuity_main_theorem_strict :
    perelmanF 1 (traj 1) < perelmanF 1 (traj 0) := by
  have hlt : 2 * Real.exp (-1) < 1 := ...
  simpa [perelmanF, gibbsTerm, Fin.sum_univ_one] using hlt
```
Goal after simp: `gibbsTerm 1 1 < gibbsTerm 1 0` = `2 e^{-1} < 1`. Good.

2. Line 178: "No goals to be solved" — in `counterexample_negative_step`, after `simp only [...]; norm_num;` the goal is closed by norm_num already, then `exact mul_lt_mul...` has no goals. Let me restructure: remove `norm_num` or the exact. After `simp only [perelmanF, Fin.sum_univ_one, gibbsTerm]`, goal is `(1 + 2^2) * exp(-2) < (1 + (-2)^2) * exp 2`. Then `norm_num` turns it into `5 * exp(-2) < 5 * exp 2` and may close it? `norm_num` probably can't close `5 * exp(-2) < 5 * exp 2` since it's nonlinear/transcendental. Hmm, the error says "No goals to be solved" at line 178, which is the `exact` line. So `norm_num` closed it. Actually maybe `norm_num` normalized and used `Real.exp_pos`? Unlikely to prove exp(-2) < exp(2). Wait, maybe after `norm_num`, the goal became `exp(-2) < exp 2` and norm_num knows `Real.exp` monotonic? No. Hmm, perhaps `norm_num` evaluated `(-2)^2 = 4`, `2^2=4`, so both sides have coefficient 5 and the goal is `5 * exp(-2) < 5 * exp 2`; `norm_num` might close using `positivity`-like reasoning? Unclear. Simplest: replace the last two lines with just `norm_num [Real.exp_lt_exp]`? Hmm. Or keep `simp only [...]` then `exact mul_lt_mul_of_pos_left (Real.exp_lt_exp.mpr (by norm_num)) (by norm_num)` — but need the arithmetic normalization of coefficients. Let me instead prove a general helper:

```
theorem five_exp_neg_two_lt_five_exp_two : 5 * Real.exp (-2) < 5 * Real.exp 2 :=
  mul_lt_mul_of_pos_left (Real.exp_lt_exp.mpr (by norm_num)) (by norm_num)
```
Then in the counterexample: `simpa [perelmanF, Fin.sum_univ_one, gibbsTerm] using five_exp_neg_two_lt_five_exp_two`. That avoids the norm_num closing issue. But careful: `gibbsTerm 1 2 = (1 + 2^2) * exp(-2)`; simp with `gibbsTerm` and `Fin.sum_univ_one` gives that, and `norm_num` needed for `(1+2^2)=5`. `simpa` normalizes numerals? `simp` can compute `1 + 2^2 = 5` via `norm_num`? Plain simp knows `2^2 = 4`? `pow_two`, then `1+4=5` via `norm_num` extension? `simp` has `OfNat` normalization? Not always. Use `norm_num` in the simpa set: `simpa [perelmanF, Fin.sum_univ_one, gibbsTerm] using ...` might leave `1 + 4`. Hmm. Let me do:
```
  have h := five_exp_neg_two_lt_five_exp_two
  simp only [perelmanF, Fin.sum_univ_one, gibbsTerm]
  norm_num
  exact h
```
The earlier failure was that norm_num closed the goal. That means norm_num transformed the goal to `5 * exp(-2) < 5 * exp 2` and then proved it? If norm_num can prove it, great — just delete the exact. But is it actually proved or did norm_num close via `decide`? Hmm, `norm_num` can prove `exp(-2) < exp 2`? No. Wait — maybe the issue is different: after `norm_num`, the goal might have become `5 * exp(-2) < 5 * exp 2` and `norm_num` closed it because both sides are positive and... no, norm_num doesn't do that.

Let me look: the error at line 178 says "No goals to be solved", and line 178 is the `exact mul_lt_mul_of_pos_left ...` line. So `norm_num` at line 177 closed the goal. Possibly because `norm_num` rewrote the goal to `True` by evaluating `Real.exp`? No. More likely: `norm_num` normalized `(-2)^2` and `2^2` and then used the fact that `exp(-2) < exp 2` is decidable? It's not.

Hold on — maybe `norm_num` closed it because the goal after `simp only` was `(1 + 2 ^ 2) * Real.exp (-2) < (1 + (-2) ^ 2) * Real.exp 2`, and `norm_num` proved it via `norm_num1` + `positivity`? `norm_num` does prove goals of the form `a < b` when both are numeric literals, but exp is not. Hmm.

Actually, I bet what happened: `norm_num` reduced the goal to `5 * Real.exp (-2) < 5 * Real.exp 2`, then applied `norm_num`'s `prove_lt` which may use `positivity` and the lemma `Real.exp_pos`... no.

Whatever the reason, if norm_num closes it, the theorem is proved without my exact. But I'm suspicious that norm_num might have closed it incorrectly? No, Lean kernel checks; if norm_num closed it, it's valid. But the `sorryAx` in `counterexample_negative_step`? The axiom list didn't show sorryAx for `counterexample_negative_step` — it shows [propext, Classical.choice, Quot.sound]. So the proof succeeded except for the "No goals" error? Wait, "No goals to be solved" is an error, so the declaration might still be added? In Lean, tactic errors cause the declaration to be marked with `sorryAx`? Actually `sorryAx` appears only for declarations that use `sorry` or failed proofs. The list shows `counterexample_negative_step` with clean axioms, meaning the proof term was elaborated successfully and the error was just at the end (after the goal was closed). In that case, removing the `exact` line fixes it.

But why did norm_num close `5 * exp(-2) < 5 * exp 2`? Hmm, maybe `norm_num` rewrote the whole thing to `True` by using `Real.exp_neg`, `Real.exp_pos`, etc. Whatever; I'll keep norm_num and drop the exact line. Actually, to be safe, let me restructure to avoid relying on norm_num's magic: use `nlinarith`? No. Let me test with a minimal approach after fixing other errors.

3. Line 273: `negative_reaction_continuous_counterexample` — `simpa [perelmanF, gibbsTerm, Fin.sum_univ_one] using hlt` leaves `(1+1) * exp(-1) < 1` instead of `2 * exp(-1) < 1`. So simp didn't normalize `1 + 1` to `2`. Fix: use `norm_num` to normalize: 
```
  have hlt : 2 * Real.exp (-1) < 1 := ...
  simp only [perelmanF, Fin.sum_univ_one, gibbsTerm]
  norm_num
  exact hlt
```
Or `simpa using hlt` after `norm_num` transforms goal. Let me do:
```
  rw [h0, h1]
  simp only [perelmanF, Fin.sum_univ_one, gibbsTerm]
  norm_num
  exact hlt
```
But the same "No goals" risk. Hmm. Alternative: state hlt as `(1 + 1) * Real.exp (-1) < 1`? Or use `show (2 * Real.exp (-1) < 1)` and `norm_num` on the goal to convert to that. Let me think: `simp only [perelmanF, Fin.sum_univ_one, gibbsTerm]` gives goal `(1 + 1 ^ 2) * Real.exp (-1) < (1 + 0 ^ 2) * Real.exp (-0)`? Wait, `negTrajCont 0 = fun _ => 1`, so `gibbsTerm 1 1 = (1 + 1^2) * exp(-1)`; `negTrajCont 1 = fun _ => 0`, so `gibbsTerm 1 0 = (1+0) * exp(0) = 1`. Goal: `(1+1)*exp(-1) < 1`. I want to close with hlt `2 * exp(-1) < 1`. Use `norm_num at hlt ⊢`? `norm_num at ⊢` normalizes both: goal becomes `2 * exp(-1) < 1`, hlt unchanged. Then `exact hlt`. The `norm_num` won't close the goal itself (I think). Let me use `norm_num1` maybe. Or simply:
```
  convert hlt using 1 <;> ring
```
Hmm, `convert` with ring: goal `(1+1)*exp(-1) < 1`, hlt `2*exp(-1) < 1`. `convert hlt using 1` generates goals `(1+1)*exp(-1) = 2*exp(-1)` and `1 = 1`; the second closes by rfl; first by ring. Let me write:
```
  convert hlt using 1
  ring
```
Actually `convert hlt using 1` produces one goal for the LHS and maybe one for RHS (which is identical, closed automatically). Let me try `norm_num` first; if it closes the goal, then hlt is unused and `exact` errors. To be robust, I'll write:
```
  have hmain : (1 + 1) * Real.exp (-1) < 1 := by
    convert hlt using 1
    ring
  simpa [perelmanF, Fin.sum_univ_one, gibbsTerm] using hmain
```
Hmm, `simpa` on hmain: goal is `(1+1^2)*exp(-1) < (1+0^2)*exp(-0)`; hmain is `(1+1)*exp(-1) < 1`. simp would need to normalize `1 + 1^2` to `1+1`, `exp(-0)` to 1, `1+0^2` to 1. `simp` can do `exp_neg`, `exp_zero`, `pow_two`, `mul_one`, and `1 + 1^2` → `1 + 1*1` → hmm. Let me just use `norm_num` to normalize the goal and then `exact hmain`:
```
  simp only [perelmanF, Fin.sum_univ_one, gibbsTerm]
  norm_num
  exact hmain
```
The `norm_num` may close it; if so, "no goals". Ugh. Let me test what norm_num does in this specific case. Actually, I can avoid the issue entirely: define the final goal in a form where simp directly matches hlt:
```
theorem negative_reaction_continuous_counterexample :
    perelmanF ... (negTrajCont 0) < perelmanF ... (negTrajCont 1) := by
  have h0 : negTrajCont 0 = (fun _ : Fin 1 => (1:ℝ)) := ...
  have h1 : negTrajCont 1 = (fun _ : Fin 1 => (0:ℝ)) := ...
  rw [h0, h1]
  have hmain : perelmanF (fun _ : Fin 1 => (1:ℝ)) (fun _ => (1:ℝ)) <
      perelmanF (fun _ : Fin 1 => (1:ℝ)) (fun _ => (0:ℝ)) := by
    simp only [perelmanF, Fin.sum_univ_one, gibbsTerm]
    norm_num
    ...
```
Circular.

Cleaner approach: prove a general helper lemma for the two values using the cluster's `gibbsTerm` definition and `norm_num` only at the very end:
```
theorem two_exp_neg_one_lt_one : 2 * Real.exp (-1) < 1 := by
  rw [Real.exp_neg, inv_eq_one_div, mul_one_div, div_lt_one (Real.exp_pos 1)]
  linarith [Real.exp_one_gt_two]
```
Then:
```
theorem gibbsTerm_one_zero_gt_one_one : gibbsTerm 1 1 < gibbsTerm 1 0 := by
  have h := two_exp_neg_one_lt_one
  norm_num [gibbsTerm] at h ⊢
  exact h
```
Here `norm_num [gibbsTerm] at h ⊢` should normalize the goal `(1+1^2)*exp(-1) < (1+0^2)*exp 0` to `2 * exp(-1) < 1` and h is already that. Does `norm_num at ⊢` close the goal? If it does, then `exact h` errors again. Hmm.

The robust pattern: `simpa only [gibbsTerm, ...] using h` after ensuring syntactic match. Let me compute exactly: goal after `rw [h0, h1]`:
`perelmanF c (fun _ => 1) < perelmanF c (fun _ => 0)` where c = fun _ => 1.
`perelmanF c lam` unfolds (delta) to `∑ i : Fin 1, gibbsTerm (c i) (lam i)`.
`Fin.sum_univ_one` : `∑ i : Fin 1, f i = f 0`. So `simp only [perelmanF, Fin.sum_univ_one]` gives `gibbsTerm (c 0) (1) < gibbsTerm (c 0) 0`; beta reduction gives `gibbsTerm 1 1 < gibbsTerm 1 0`.
`gibbsTerm 1 1` unfolds (if gibbsTerm is an abbrev, it's transparent to `simp only [gibbsTerm]`) to `(1 + 1^2) * Real.exp (-1)`. `gibbsTerm 1 0` → `(1 + 0^2) * Real.exp (-0)`.

So after `simp only [perelmanF, Fin.sum_univ_one, gibbsTerm]`: goal `(1 + 1^2) * Real.exp (-1) < (1 + 0^2) * Real.exp (-0)`. `norm_num` reduces `1^2=1`, `1+1=2`, `0^2=0`, `1+0=1`, `-0=0`, `Real.exp 0 = 1`, so goal `2 * Real.exp (-1) < 1`. Then `exact two_exp_neg_one_lt_one`.

Does `norm_num` also close `2 * exp(-1) < 1`? It didn't in the earlier failure for this theorem? The error at line 273 was a type mismatch: `hlt` had type `2 * exp(-1) < 1` but expected `(1+1) * exp(-1) < 1`. That means the `simpa` at line 273 did NOT normalize `1+1` to 2 (simpa's simp didn't have arithmetic normalization), and importantly the goal was `(1+1)*exp(-1) < 1`. So `norm_num` there would reduce it. Fine: use `simp only [...]` then `norm_num` then `exact hlt`; if norm_num closes it, I'll see "no goals" and can remove the exact. Let me just test.

Actually, a much simpler robust approach for these numeric goals: use `nlinarith` with exp bounds? No.

Let me take a different approach: use `linarith` after rewriting exp inequalities to linear ones. For `negative_reaction_continuous_counterexample`, I can prove the inequality as:
```
have hlt : 2 * Real.exp (-1) < 1 := two_exp_neg_one_lt_one
have hgoal : (1 + 1 ^ 2) * Real.exp (-1) < (1 + 0 ^ 2) * Real.exp (-0) := by
  norm_num
  exact hlt
```
If `norm_num` closes, remove exact.

Let me instead define helper lemmas with the *exact syntactic form* after unfolding, avoiding norm_num:
```
theorem gibbsTerm_one_one_lt_zero : gibbsTerm 1 1 < gibbsTerm 1 0 := by
  have h : (1 + 1 ^ 2) * Real.exp (-1) < (1 + 0 ^ 2) * Real.exp (-0) := by
    have h2 : 2 * Real.exp (-1) < 1 := two_exp_neg_one_lt_one
    norm_num at h2
    -- hmm
```
This is getting circular. Let me just use `nlinarith` with the fact `Real.exp (-1) < 1`:
`2 * exp(-1) < 1` follows from `exp(-1) < 1/2`, which follows from `Real.exp_neg` and exp 1 > 2. For the goal `(1+1^2)*exp(-1) < (1+0)*exp 0`: after `norm_num` it's `2*exp(-1) < 1`. Let me use:
```
  have hlt : 2 * Real.exp (-1) < 1 := ...
  simp only [perelmanF, Fin.sum_univ_one, gibbsTerm]
  norm_num1
  exact hlt
```
`norm_num1` does only numeral normalization without trying to close. That's a good idea! `norm_num1` normalizes numeric subterms and doesn't call the full `norm_num` closure machinery. Actually `norm_num1` is the core normalization tactic; it may still discharge trivial goals. Let me test.

Similarly for `nonvacuity_main_theorem_strict` and `counterexample_negative_step`.

4. Line 347: `le_or_lt` unknown identifier — it's `le_or_lt`? In Lean4/mathlib, the lemma is `le_or_lt : a ≤ b ∨ b < a`? Actually the names are `le_or_lt` in Lean core? Let me check: `le_or_lt` exists for linear orders as `le_or_lt : a ≤ b ∨ b < a`. Hmm, "Unknown identifier" suggests it's not exported or the name is `le_or_lt`? In Mathlib, there's `le_or_lt` in `Mathlib.Order.Defs.LinearOrder`? Let me grep. Common names: `le_or_lt`, `lt_or_le`, `le_total`, `lt_trichotomy`. Let me check mathlib.

5. Line 446: `limit_passage_sanity` — the goal `perelmanF 1 1 ≤ perelmanF 1 0` but the theorem produces `perelmanF 1 (limit) ≤ perelmanF 1 (state 0)` where state 0 = `1 - 1/(0+1) = 0`... wait the error says the theorem's conclusion is `perelmanF ?m.24 fun x => 1 ≤ perelmanF ?m.24 fun x => 1 - 1 / (↑0 + 1)` which is `perelmanF 1 (limit) ≤ perelmanF 1 (state 0)` = `perelmanF 1 (fun _ => 1) ≤ perelmanF 1 (fun _ => 1 - 1/(0+1))`. My goal is `perelmanF 1 (fun _ => 1) ≤ perelmanF 1 (fun _ => 0)`. They are definitionally equal? `1 - 1/(0+1)` doesn't reduce definitionally to 0 in Lean (division and Nat.cast). So `apply` fails to unify. Fix: state the goal as `perelmanF 1 (fun _ => 1) ≤ perelmanF 1 (fun _ : Fin 1 => 1 - 1/((0:ℕ)+1))` or use `show`/`convert`. Better: make the theorem statement use `state 0` expression:
```
theorem limit_passage_sanity :
    perelmanF (fun _ : Fin 1 => (1:ℝ)) (fun _ => (1:ℝ)) ≤
      perelmanF (fun _ : Fin 1 => (1:ℝ)) (fun _ => 1 - 1/((0:ℝ)+1)) := by
  apply perelmanF_limit_le_of_discrete ...
```
Hmm, but then the goal still needs the state 0 to match exactly. The `apply` should unify if I state it exactly as the theorem's conclusion. Alternatively, prove the theorem then `norm_num` the conclusion:
```
theorem limit_passage_sanity : ... := by
  have h := perelmanF_limit_le_of_discrete (c := fun _ : Fin 1 => (1:ℝ))
    (state := fun n (_:Fin 1) => 1 - 1/((n:ℝ)+1)) (limit := fun _ => (1:ℝ)) ?_ ?_
  · simpa using h
  · ...
```
`simpa` at the end can normalize `1 - 1/(0+1)` to 0? `norm_num` in simp set: `simpa` uses simp; `1/(0+1)` needs `div_one`? `1 / (↑0 + 1)` → `1/1 = 1` via `norm_num` maybe not in simp. Use `simpa using h` might fail. Better: state the goal with the exact expression and let the apply work:
```
theorem limit_passage_sanity :
    perelmanF (fun _ : Fin 1 => (1:ℝ)) (fun _ => (1:ℝ)) ≤
      perelmanF (fun _ : Fin 1 => (1:ℝ)) (fun _ => 1 - 1 / ((0:ℕ) + 1 : ℝ)) := by
  apply perelmanF_limit_le_of_discrete
    (state := fun n (_ : Fin 1) => 1 - 1 / ((n : ℝ) + 1)) (limit := fun _ => (1:ℝ))
  ...
```
The state 0 component is `1 - 1/((0:ℝ)+1)`, and I wrote `1 - 1/((0:ℕ)+1 : ℝ)`; `(0:ℕ)+1` cast to ℝ vs `(0:ℝ)+1` — unification may handle `Nat.cast_add`? Not defeq. Let me write exactly `(fun _ => 1 - 1 / ((0 : ℝ) + 1))`. That should unify with the state's `n=0` instantiation. The `state` lambda is `fun n (_:Fin 1) => 1 - 1/((n:ℝ)+1)`; at n=0 this is `1 - 1/((0:ℝ)+1)`. Defeq. Good.

Also the second bullet: `tendsto_pi_nhds.mpr (fun _ => ...)` with `_ : Fin 1`; fine.

6. Also, I noticed `#print axioms` output shows `sorryAx` for failed declarations, so after fixing all errors, I need to re-run and check no sorryAx. Also note the `#print axioms` for `Poincare.Longrun.Evolution.perelmanF_monotone_of_tensorBridge` and `PerelmanEvolutionBoundary` didn't appear in the output? The output was truncated at head -80 maybe. Let me check the tail later.

Now, `le_or_lt`: let me find the right lemma name.

Also, in `gibbsTerm_strictAnti_of_one_le`, error at line 347 is `rcases le_or_lt y 1 with hy | hy`. Let me use `rcases le_or_lt y 1` — unknown. Use `lt_or_ge y 1`? Common: `lt_or_ge`, `le_or_lt`, `lt_or_le`, `le_or_gt`. Let me grep mathlib.

Let me check these lemma names and the `norm_num1` tactic existence.
dsh: reasoning:
`norm_num1` exists. For `le_or_lt`, let me search more broadly — it might be `le_or_lt` in Lean core `Init/Order`? Let me just grep the whole mathlib for `theorem le_or_lt` or use `lt_or_ge`.
dsh: reasoning:
The grep over the whole mathlib timed out. Let me be targeted: search in Mathlib/Order/ directory only.
dsh: reasoning:
`lt_or_ge` exists: `a < b ∨ b ≤ a`. I need `y ≤ 1 ∨ 1 < y`. Use `lt_or_ge y 1 : y < 1 ∨ 1 ≤ y` — not what I want. Use `le_or_gt y 1 : y ≤ 1 ∨ 1 < y`. Yes! `le_or_gt (a b : α) : a ≤ b ∨ b < a`. So `le_or_gt y 1 : y ≤ 1 ∨ 1 < y`. Replace `le_or_lt` with `le_or_gt`.

Now let me fix all the issues:

1. `nonvacuity_main_theorem_strict`: change to compare t=1 vs t=0 with `two_exp_neg_one_lt_one` helper.
Let me add a helper:
```
theorem two_exp_neg_one_lt_one : 2 * Real.exp (-1) < 1 := by
  rw [Real.exp_neg, inv_eq_one_div, mul_one_div, div_lt_one (Real.exp_pos 1)]
  linarith [Real.exp_one_gt_two]
```
Then use it in three places (nonvacuity_main_theorem_strict, negative_reaction_continuous_counterexample, weak_discrete_monotonicity_false).

For `nonvacuity_main_theorem_strict`:
```
theorem nonvacuity_main_theorem_strict :
    perelmanF (fun _ : Fin 1 => (1 : ℝ)) ((fun t (_ : Fin 1) => t) 1) <
      perelmanF (fun _ : Fin 1 => (1 : ℝ)) ((fun t (_ : Fin 1) => t) 0) := by
  have hlt := two_exp_neg_one_lt_one
  simp only [perelmanF, Fin.sum_univ_one, gibbsTerm]
  norm_num1
  exact hlt
```
Let me trace: `((fun t (_:Fin 1) => t) 1) = fun _ => 1`; `gibbsTerm 1 1 = (1+1^2)*exp(-1)`. After `simp only [...]`, goal `(1 + 1^2) * exp(-1) < (1 + 0^2) * exp(-0)`. `norm_num1` should normalize numerals: `1^2=1`, `1+1=2`, `0^2=0`, `1+0=1`, `-0=0`; but `exp 0` → `Real.exp 0` not `1` unless simp lemma applied. `norm_num1` does some simp? It normalizes numeric expressions including `Real.exp 0 = 1`? Possibly. Let me use `simp only [perelmanF, Fin.sum_univ_one, gibbsTerm, Real.exp_zero, neg_zero, pow_two, mul_one]`... messy. Better: use `norm_num [perelmanF, gibbsTerm, Fin.sum_univ_one] at hlt ⊢` — but risk of closing. Let me test interactively with a scratch file to determine behavior. Actually, let me just use `convert`:
```
  convert hlt using 2 <;> norm_num
```
Hmm.

Alternative robust approach: express the values directly.
```
theorem gibbsTerm_one_values :
    gibbsTerm 1 1 = 2 * Real.exp (-1) ∧ gibbsTerm 1 0 = 1 := by
  constructor <;> norm_num [gibbsTerm]
```
If `norm_num [gibbsTerm]` can prove `(1+1^2)*exp(-1) = 2*exp(-1)` and `(1+0^2)*exp(-0) = 1`, good. Then:
```
theorem nonvacuity_main_theorem_strict : ... := by
  rw [show ((fun t (_:Fin 1) => t) 1) = fun _ => (1:ℝ) by rfl,
      show ((fun t (_:Fin 1) => t) 0) = fun _ => (0:ℝ) by rfl]
  simp only [perelmanF, Fin.sum_univ_one]
  rw [gibbsTerm_one_values.1, gibbsTerm_one_values.2]
  exact two_exp_neg_one_lt_one
```
Hmm, `rw` with the `show ... by rfl` might be awkward. Since `((fun t (_:Fin 1) => t) 1)` is defeq to `fun _ => 1`, `simp only [perelmanF, Fin.sum_univ_one]` will beta-reduce. Let me structure:
```
theorem nonvacuity_main_theorem_strict : ... := by
  have h1 : gibbsTerm 1 1 = 2 * Real.exp (-1) := by norm_num [gibbsTerm]
  have h0 : gibbsTerm 1 0 = 1 := by norm_num [gibbsTerm]
  simp only [perelmanF, Fin.sum_univ_one]
  rw [h1, h0]
  exact two_exp_neg_one_lt_one
```
After `simp only [perelmanF, Fin.sum_univ_one]`, goal is `gibbsTerm ((fun _ => 1) 0) ((fun t _ => t) 1 0) < gibbsTerm ((fun _ => 1) 0) ((fun t _ => t) 0 0)`. Beta reduction is part of `simp only`? `simp` does beta-reduce. The `Fin.sum_univ_one` lemma might need the function to be in the right form. Let me check `Fin.sum_univ_one` statement: `Fin.sum_univ_one {α : Fin 1 → Type*} ...`? Actually it's `Fin.sum_univ_one : ∑ i : Fin 1, f i = f 0`. With `perelmanF c lam = ∑ i, gibbsTerm (c i) (lam i)`, `simp only [perelmanF, Fin.sum_univ_one]` rewrites to `gibbsTerm (c 0) (lam 0)`. Then beta reduction of `(fun _ => 1) 0` → `1` and `((fun t (_:Fin 1) => t) 1) 0` → `1`. `simp only` includes beta reduction by default? `simp` uses `betaReduction`? I believe the simplifier beta-reduces applications of lambdas. Yes.

Then goal `gibbsTerm 1 1 < gibbsTerm 1 0`; `rw [h1, h0]`; goal `2 * exp(-1) < 1`; exact. 

Similarly, `dissipation_sanity` already worked (clean axioms), good.

2. `counterexample_negative_step` line 178: remove the trailing `exact` if norm_num closes. Let me instead write it robustly with helper values:
```
theorem counterexample_negative_step : ... := by
  have hstep : eulerStep ... = fun _ => (-2:ℝ) := ...
  rw [hstep]
  have h2 : gibbsTerm 1 2 = 5 * Real.exp (-2) := by norm_num [gibbsTerm]
  have hm2 : gibbsTerm 1 (-2) = 5 * Real.exp 2 := by norm_num [gibbsTerm]
  simp only [perelmanF, Fin.sum_univ_one]
  rw [h2, hm2]
  exact mul_lt_mul_of_pos_left (Real.exp_lt_exp.mpr (by norm_num)) (by norm_num)
```
This is clean and avoids norm_num closing. Note `gibbsTerm 1 (-2) = (1 + (-2)^2) * exp(-(-2)) = 5 * exp 2`. `norm_num [gibbsTerm]` should prove it.

3. `negative_reaction_continuous_counterexample` line 273: similarly:
```
  rw [h0, h1]
  have h1v : gibbsTerm 1 1 = 2 * Real.exp (-1) := by norm_num [gibbsTerm]
  have h0v : gibbsTerm 1 0 = 1 := by norm_num [gibbsTerm]
  simp only [perelmanF, Fin.sum_univ_one]
  rw [h1v, h0v]
  exact two_exp_neg_one_lt_one
```

4. `weak_discrete_monotonicity_false`: `hlt` currently proved inline; replace with `two_exp_neg_one_lt_one`. The `norm_num [perelmanF, gibbsTerm, negTrajDisc, Fin.sum_univ_one]` for h1/h0: h1 is `perelmanF 1 (negTrajDisc 1) = 1`; negTrajDisc 1 = fun _ => 0, so `gibbsTerm 1 0 = 1`. `norm_num` should prove. h0: `perelmanF 1 (negTrajDisc 0) = 2 * exp(-1)`; norm_num leaves `2 * exp(-1)`. These worked (no errors reported for those lines). Good. But note `norm_num` might prove h1/h0 fine.

5. `le_or_lt` → `le_or_gt`.

6. `limit_passage_sanity`: fix the statement to match the theorem's conclusion exactly, or use `convert`. Let me state:
```
theorem limit_passage_sanity :
    perelmanF (fun _ : Fin 1 => (1 : ℝ)) (fun _ => (1 : ℝ)) ≤
      perelmanF (fun _ : Fin 1 => (1 : ℝ)) (fun _ => 1 - 1 / ((0 : ℝ) + 1)) := by
  apply perelmanF_limit_le_of_discrete
    (state := fun n (_ : Fin 1) => 1 - 1 / ((n : ℝ) + 1)) (limit := fun _ => (1 : ℝ))
  · ...
  · ...
```
The conclusion of the applied theorem: `perelmanF c limit ≤ perelmanF c (state 0)` = `perelmanF 1 (fun _ => 1) ≤ perelmanF 1 (fun _ => 1 - 1/((0:ℝ)+1))`. This should match the goal exactly. Good.

But hold on, the second bullet `tendsto_pi_nhds.mpr (fun _ => ...)`: the goal is `Tendsto (fun n x => 1 - 1/((n:ℝ)+1)) atTop (𝓝 (fun x => 1))`. `tendsto_pi_nhds.mpr` expects `∀ i, Tendsto (fun x => (fun n (x:Fin 1) => ...) x i) atTop (𝓝 ((fun x => 1) i))` = `∀ i, Tendsto (fun n => 1 - 1/((n:ℝ)+1)) atTop (𝓝 1)`. `simpa using tendsto_const_nhds.sub tendsto_one_div_add_atTop_nhds_zero_nat` should work. Good.

Also in the hmono bullet, the goal is `perelmanF 1 (state n) ≤ perelmanF 1 (state 0)` = `gibbsTerm 1 (1 - 1/(n+1)) ≤ gibbsTerm 1 (1 - 1/(0+1))`. After `simpa [perelmanF, Fin.sum_univ_one] using h`, h : `gibbsTerm 1 (1 - 1/(n+1)) ≤ gibbsTerm 1 0`. The goal RHS is `gibbsTerm 1 (1 - 1/((0:ℝ)+1))`, which simp/norm_num should reduce to `gibbsTerm 1 0`. Does plain `simp` reduce `1 - 1/((0:ℝ)+1)` to 0? `simp` with `zero_add` gives `1 - 1/1`; `div_one` gives `1 - 1`; `sub_self` gives 0. All are simp lemmas (`Nat.cast_zero`? `(0:ℝ)` is a literal, `0 + 1` → `1` via `zero_add`, `1/1` → `1` via `div_one`, `1-1` → `0` via `sub_self`). So `simpa` should work. Good.

7. In `gibbsTerm_strictAnti_of_one_le`, after fixing `le_or_gt`, the rest should be fine. But also check the `hleft`/`hright` applications: `hleft (by linarith) hy hxy` — here `hleft : StrictAntiOn (gibbsTerm 1) (Iic 1)`, so first arg is membership `x ∈ Iic 1`. `by linarith` proves `x ≤ 1` from `hxy : x < y` and `hy : y ≤ 1`. Good. Then `hy : y ∈ Iic 1`. Good.

Second branch `hy : 1 < y` (from le_or_gt second disjunct). `hright hx1 (le_of_lt hy) hxy`: `hx1 : 1 ≤ x` membership in Ici 1; `le_of_lt hy : 1 ≤ y` membership; gives `gibbsTerm 1 y < gibbsTerm 1 x`. Good.

Third: after `push_neg at hx1`, hx1 : x < 1. `hleft (a := x) (le_of_lt hx1) (b := 1) (le_refl 1) hx1` gives `gibbsTerm 1 1 < gibbsTerm 1 x`. Good. `hright (a := 1) (le_refl 1) (b := y) (le_of_lt hy) hy` gives `gibbsTerm 1 y < gibbsTerm 1 1`. Good.

8. The `nonvacuity_main_theorem` worked.

9. Check `strict_step_needs_pos_reaction` worked (no error listed). Good.

10. `perelmanF_nonneg_sharp` worked.

11. `strict_step_positive_control`: had sorryAx because it depends on `perelmanF_step_lt_of_one_le` which failed. After fixing, should be fine. But wait, the error list didn't include line for `strict_step_positive_control`; it had sorryAx because of dependency. OK.

12. `transfer_nonvacuous`: no error, clean.

13. `finiteMeshConvergence_nonvacuous`: no error, clean.

14. `three_exp_neg_one_lt_nine_exp_neg_two` and `gibbsTerm_half_one_lt_two` worked.

Let me now apply the fixes with the edit tool.

First, add the `two_exp_neg_one_lt_one` helper. Where? Before `nonvacuity_main_theorem_strict`. Let me place it right after `nonvacuity_main_theorem`. Actually, it's used in section 4 too; definition order matters in Lean (must be before use). Place it right before `nonvacuity_main_theorem_strict` (line ~80).

Let me make the edits.
dsh: reasoning:
Wait, there's an ordering problem: `gibbsTerm_one_values` uses `gibbsTerm` but it's after `nonvacuity_main_theorem` — fine. But `two_exp_neg_one_lt_one` is used later in section 4, which is after. Good.

But now `gibbsTerm_one_two_lt` is no longer used in `nonvacuity_main_theorem_strict`; that's fine (it's a promoted lemma; I still reference `squareTraj_counterexample` etc. in #print axioms).

Now fix `counterexample_negative_step`.
dsh: reasoning:
Wait, in `negative_reaction_continuous_counterexample`, the goal after `rw [h0, h1]` is `perelmanF 1 (fun _ => 1) < perelmanF 1 (fun _ => 0)`. After `simp only [perelmanF, Fin.sum_univ_one]`, goal is `gibbsTerm 1 1 < gibbsTerm 1 0`; then rw values; goal `2*exp(-1) < 1`; exact. Good.

Now fix `le_or_lt` → `le_or_gt`.
dsh: reasoning:
Now, `gibbsTerm_one_values` is defined in section 1 but `counterexample_negative_step` is in section 3 and uses `gibbsTerm` — fine. `gibbsTerm_one_values` uses `norm_num [gibbsTerm]` to prove both conjuncts; let me double check the second: `gibbsTerm 1 0 = (1 + 0^2) * exp(-0) = 1 * exp 0 = 1`. `norm_num [gibbsTerm]` should handle `exp 0 = 1`? `norm_num` might not know `Real.exp 0 = 1` unless simp does. `norm_num [gibbsTerm]` runs simp with gibbsTerm; simp knows `Real.exp_zero`. Then `1 * 1 = 1`. Should be fine. First conjunct: `(1 + 1^2)*exp(-1) = 2 * exp(-1)`; norm_num normalizes `1+1=2`. Good.

Now recompile.
dsh: reasoning:
Error 1 at line 358: `exact hleft (by linarith) hy hxy` — linarith failed. Context: `hy : y ≤ 1`, `hxy : x < y`. Need `x ≤ 1`. `linarith` should do `x < y ≤ 1`. But maybe the goal is not `x ≤ 1`; `StrictAntiOn` first arg is `x ∈ Iic 1` which is `x ∈ Set.Iic 1` = `x ≤ 1`. Hmm, linarith failed maybe because `x`, `y` are implicit-bound with `⦃⦄`? No. Let me see the full error text.

Error 2 at line 363: "Application type mismatch" for `hleft (a := x) (le_of_lt hx1) (b := 1) (le_refl 1) hx1`. Since `StrictAntiOn f s := ∀ ⦃a⦄, a ∈ s → ∀ ⦃b⦄, b ∈ s → a < b → f b < f a`, using named args `(a := x)` should be fine... but the issue might be that `hleft` is a proof of `StrictAntiOn (gibbsTerm 1) (Iic 1)` and applying it with explicit `(a := x)` etc. Perhaps the membership argument `(le_of_lt hx1) : x ≤ 1` needs to be `x ∈ Iic 1`; `Set.Iic` membership is definitionally `x ≤ 1`, so should be fine. The error says "Application type mismatch: The argument ..." — let me see the full error.

Error 3 at line 456: still unify failure. The conclusion of `perelmanF_limit_le_of_discrete` after specifying state/limit is `perelmanF ?c (fun _ => 1) ≤ perelmanF ?c (fun _ => 1 - 1 / (↑0 + 1))`. My goal has `perelmanF (fun _ : Fin 1 => 1) ...` and `?c` should unify with that. But the error may be about `?m.24` (the index type ι) being ambiguous: since `perelmanF_limit_le_of_discrete` has implicit `{ι} [Fintype ι]`, and my goal's `Fin 1` should determine it. Hmm, the previous error message showed `?m.24` unresolved; maybe because I didn't specify `(c := ...)`. Let me provide `(c := fun _ : Fin 1 => (1:ℝ)) (ι := Fin 1)` explicitly. Also the state's `Fin 1` annotation should determine ι. Let me add explicit `(ι := Fin 1) (c := ...)`.

Let me see the full errors.
dsh: reasoning:
Interesting details:

1. Line 358: The goal after `exact hleft (by linarith) hy hxy` is `False`?! That means `hleft (by linarith) hy hxy` is being elaborated as a proof of `False`?? No wait — the `⊢ False` in the linarith error context means the `by linarith` block's goal is `False`. Why would the goal be `False`? Because `hleft` has type `StrictAntiOn (gibbsTerm 1) (Iic 1)`, and `StrictAntiOn` is `∀ ⦃a⦄, a ∈ s → ∀ ⦃b⦄, b ∈ s, a < b → f b < f a`. Applying `hleft` with `(by linarith)` as the first explicit argument (a ∈ Iic 1) and `hy` as second (b ∈ Iic 1) and `hxy` as third gives `gibbsTerm 1 y < gibbsTerm 1 x`... but the overall goal at line 358 is `gibbsTerm 1 y < gibbsTerm 1 x`, which should be closed by `exact`. Hmm, but the error context says `⊢ False` for the `by linarith` tactic. That suggests Lean elaborated the expected type of `(by linarith)` as `False`? Odd. 

Wait, look at the goal more carefully. In the `StrictAnti` proof, after `intro x y hxy`, the goal is `gibbsTerm 1 y < gibbsTerm 1 x`. `exact hleft (by linarith) hy hxy` — Lean unifies the expected type `gibbsTerm 1 y < gibbsTerm 1 x` with `hleft _ _ _`. `StrictAntiOn f s` unfolds to `∀ ⦃a⦄, a ∈ s → ∀ ⦃b⦄, b ∈ s, a < b → f b < f a`. So `hleft hx hy hxy : f y < f x`. The first argument's expected type is `x ∈ Iic 1`? Or `a ∈ s` where a is inferred from the expected result `f y < f x`: since the result is `f b < f a`, matching `f y < f x` gives b = y, a = x. So first argument expected type `x ∈ Iic 1` i.e. `x ≤ 1`. Why would linarith see goal `False`?

Ah, I see: `Iic 1` for `ℝ` is `Set.Iic 1`, and membership `x ∈ Iic 1` is *definitionally* `x ≤ 1` but maybe not syntactically. The error says "⊢ False" — that's strange. Unless... `hleft` was applied with implicit args in a different order and `(by linarith)` got the `∀ ⦃b⦄`? Hmm.

Actually wait, I bet the issue is that `linarith` is being asked to prove `x ∈ Iic 1`, which after whnf is `x ≤ 1`, and linarith's failure message shows the *negated* goal as `False`? The linarith error message format: it prints hypotheses and `⊢ False` because it works by contradiction. So the hypotheses include `x y : ℝ`, `hxy : x < y`, `hy : y ≤ 1`, and the negated goal. But the negated goal should be `¬(x ≤ 1)`, i.e., `1 < x`, which would appear as a hypothesis. It's not in the list! The hypotheses listed are hxy and hy, but no negated goal hypothesis. That means the goal linarith was trying to prove was literally `False` (no negated goal to add). So `(by linarith)` had expected type `False`.

Why? Possibly because the second explicit argument `hy : y ≤ 1` was interpreted as `y ∈ Iic 1` and the third `hxy : x < y`, and then `a` and `b`... Let's think about how Lean unifies. `hleft : StrictAntiOn f s` with f = gibbsTerm 1, s = Iic 1. When we write `hleft A hy hxy`, Lean elaborates arguments left to right without knowing the expected result? Actually it does know the expected type `f y < f x` for the whole application. But the first explicit argument A has expected type `?a ∈ s` where `?a` is a metavariable. Since the result type is `f ?b < f ?a`, unifying with `gibbsTerm 1 y < gibbsTerm 1 x` gives `?a = x`, `?b = y`. So A's type is `x ∈ Iic 1`. Then `by linarith` should have goal `x ∈ Iic 1`. But membership in `Iic` is `Set.Iic 1`, and `x ∈ Set.Iic 1` is notation for `x ≤ 1`. Hmm, but maybe `Set.Iic` is defined as `fun b => {a | a ≤ b}` and membership reduction requires `Set.mem_Iic`? Definitional reduction should work.

Unless... `Iic` in my file resolves to something else? I opened `Set` and there's `Set.Iic`. In the D4 code, `perelmanF_antitone` uses `s ∈ Icc 0 T` etc. Fine.

Hold on, maybe the issue is that `hleft`'s implicit arguments `⦃a⦄` and `⦃b⦄` are implicit, and writing `hleft (by linarith) hy hxy` means Lean treats `(by linarith)` as the *first explicit* argument, which is the membership for `a`. But then `hy` as the second explicit arg is membership for `b`. That's what I intended. The expected type for `(by linarith)` would be `?a ∈ Iic 1`. Hmm.

But the error shows `⊢ False`, suggesting that Lean already unified the whole application's type with the goal and then the tactic block `(by linarith)` is expected to produce... no.

Wait, maybe the problem is the opposite: the goal at that point isn't `gibbsTerm 1 y < gibbsTerm 1 x` but `False`! Look at line 357-358: `rcases le_or_gt y 1 with hy | hy` then `· exact hleft ...`. The `rcases` creates two goals. The first goal is `gibbsTerm 1 y < gibbsTerm 1 x` with hy : y ≤ 1. Fine. But the `linarith failed` message shows `hc : 1 ≤ 1` in context (from `hc` of the theorem after `rfl`?) and the goal `False`. So linarith's goal was False indeed.

OH WAIT. I see it now. `StrictAntiOn f s := ∀ ⦃a⦄, a ∈ s → ∀ ⦃b⦄, b ∈ s, a < b → f b < f a`. The expression `hleft (by linarith) hy hxy` — the `(by linarith)` is the membership for `a`, but Lean might be elaborating `(by linarith)` with expected type `x ∈ Iic 1`; `linarith` doesn't know how to prove membership directly because `x ∈ Iic 1` doesn't reduce? But then the error would show the goal `x ∈ Iic 1`, not `False`. Hmm, but the error shows linarith's *internal* contradiction state: hypotheses + `⊢ False`. It lists hypotheses: hxy, hy, but not `1 < x` (the negation). In linarith's error, it does print the goal as `⊢ False` and includes the negated goal in the hypotheses normally. The fact that it's absent suggests the goal was `False` outright.

Hmm, could `x ∈ Iic 1` have been simplified by `Set.mem_Iic` and then `linarith` was invoked on... no.

Let me reconsider: maybe the second explicit argument `hy` was expected to have type `x ∈ Iic 1` and `(by linarith)` expected `y ∈ Iic 1`? Order: `hleft ⦃a⦄ (a ∈ s) ⦃b⦄ (b ∈ s) (a < b)`. The explicit args in order: membership_a, membership_b, a<b. So first `(by linarith)` is membership_a = `x ∈ Iic 1`, `hy` membership_b = `y ∈ Iic 1`, `hxy` a<b = `x < y`. Since `hy : y ≤ 1` is definitionally `y ∈ Iic 1`, fine.

But the result type is `f b < f a` = `f y < f x`. The goal is exactly that. So the application should work.

Why `⊢ False`? Unless `linarith` was applied to a goal that is `False` because `Set.mem_Iic` is not recognized and Lean inserted... 

Hmm, let me look at line 358 again: `· exact hleft (by linarith) hy hxy`. Perhaps the issue: `Iic` is being parsed as `Set.Iic` but since I `open Set`, `Iic` is `Set.Iic`. Membership `x ∈ Set.Iic 1` is `x ≤ 1` definitionally. `linarith` should handle the goal `x ≤ 1` with hypotheses hxy : x < y, hy : y ≤ 1. It should succeed!

Unless the expected type of `(by linarith)` is not `x ≤ 1` but something like `x ∈ Iic y` (because `hy` unified first?). Hmm.

Actually, maybe the elaboration order is: Lean elaborates `hleft` applied to args without expected type propagation, creating metavariables for `a`, `b`. First arg `(by linarith)` has type `?a ∈ Iic 1`. Since `?a` is unknown, the tactic `linarith` runs on goal `?a ∈ Iic 1` — with `?a` a metavariable, linarith may reduce `?a ∈ Set.Iic 1` to `?a ≤ 1` and fail; the error message would show `⊢ ?a ≤ 1`, not False. Hmm.

Wait, actually the error message prints `⊢ False` and hypotheses including `hc : 1 ≤ 1`. `hc : 1 ≤ 1` is the theorem's hypothesis `hc : 1 ≤ c` after `rfl` substituted c=1. So linarith had context with hc. The goal False. There's no negated goal. This strongly suggests the tactic block's goal is literally `False`, which happens when... `by linarith` was given the goal `False` because the *expected type* of the first argument was `False`? How could `?a ∈ Iic 1` become `False`? 

OH! I think I see. `Set.Iic` might not be the name; maybe `Iic` here resolves to `Set.Iic` fine. But `hleft` has type `StrictAntiOn (gibbsTerm 1) (Iic 1)`. The first explicit argument type is `a ∈ Iic 1`. Hmm.

Hold on — maybe the real issue: after `intro x y hxy`, `x` and `y` are introduced, but `hxy : x < y`... the goal is `gibbsTerm 1 y < gibbsTerm 1 x`. Now `exact hleft ...` — Lean elaborates the term against expected type `gibbsTerm 1 y < gibbsTerm 1 x`. It elaborates `hleft` first: type `StrictAntiOn f s`, which unfolds to a Pi type. Then it processes the arguments. Fine.

Actually, you know what? Let me look at line 356-358 numbering in the file: the error says line 358 col 22, which is `(by linarith)`. The `linarith failed` message shows the *hypotheses of the whole context*, including `hc : 1 ≤ 1` (from the outer theorem), and `hleft`, `hright`, etc. That's the linarith goal context at that point. The goal is `False`. 

Hmm, linarith's error printing: it prints the context and `⊢ False` because internally it does `exfalso` and adds the negated goal. Actually I recall linarith's error message often shows `⊢ False` and includes the *negation* of the goal as a hypothesis. Here the negation of `x ≤ 1` would be `¬(x ≤ 1)` or `1 < x`. It's not there. Unless linarith's preprocessing turned `x ≤ 1` into `1 < x` and then... no.

Let me just test in isolation. Maybe the problem is that `Iic` membership doesn't reduce and linarith sees a non-arithmetic goal, then its error is weird. Simplest fix: avoid relying on definitional membership; provide `Set.mem_Iic.mpr (by linarith)` or use `show x ≤ 1 from by linarith`. Let me change:
```
· exact hleft (show x ≤ 1 by linarith) hy hxy
```
If `x ≤ 1` is not defeq to `x ∈ Iic 1` in the expected type, `show` will fail. But `Set.mem_Iic` is `Iff.rfl`-definable, so it should be defeq. Hmm, but the earlier error suggests something else.

Actually, wait. Look at error 2 at line 363: "The argument `le_refl 1` has type `1 ≤ 1` but is expected to have type `1 ∈ Iic 1`". So Lean does distinguish `1 ≤ 1` from `1 ∈ Iic 1`! That means `Iic` is NOT reducing to the `≤` form for membership. Why? Because `Iic` is `Set.Iic` which is defined via `Set.Ici` dual? Let me check: `Set.Iic a := {b | b ≤ a}`. Membership should be defeq. But Lean says the expected type is `1 ∈ Iic 1`, and `le_refl 1 : 1 ≤ 1` doesn't match. That means `1 ∈ Iic 1` is not syntactically/definitionally the same as `1 ≤ 1`? 

OH. I bet `Iic` here refers to the *interval* notation `Iic` from `Set` — yes. Hmm, but why not defeq? Maybe because `Set.Iic` is defined as `Ici` in the dual order: `def Iic (a : α) : Set α := {b | b ≤ a}` — defeq. Unless `open Set` isn't importing `Set.Iic` and `Iic` resolves to `Set.Iic`... 

Wait, maybe the issue is that `Iic 1` is `Set.Iic (1 : ?m)` where `?m` is a metavariable type, and the membership `1 ∈ Iic 1` is `1 ≤ 1` at type `?m`. The error says expected `1 ∈ Iic 1` — maybe the set is `Iic (1:ℝ)` and membership should be `1 ≤ 1`. The fact that `le_refl 1` was rejected is puzzling.

Hmm, unless `Iic` is `Finset.Iic`? No, `Iic 1` for a real can't be Finset.

Let me check: in mathlib, `Set.Iic` is notation? `Iic` is indeed `Set.Iic`. Membership: `Set.mem_Iic : a ∈ Iic b ↔ a ≤ b`. Is it `@[simp]`? The definition might be `def Set.Iic (a : α) : Set α := { b | b ≤ a }` — then `x ∈ {b | b ≤ a}` is defeq to `x ≤ a`. So `le_refl 1` should be accepted. Unless the elaborator is treating the expected type as an opaque application and not reducing it for the `exact`? `exact` should check defeq.

Wait, maybe the real problem: the expected type is `a ∈ s` where `s = Iic 1` and `a = x`, but `x` here is a *metavariable* or a bound variable that got shadowed? The error at line 363 mentions `(a := x) (le_of_lt hx1) (b := 1) (le_refl 1) hx1`; the argument `le_refl 1` is expected to have type `1 ∈ Iic 1`. Hmm.

Let me look at how mathlib defines `StrictAntiOn` again: `∀ ⦃a⦄ (_ : a ∈ s) ⦃b⦄ (_ : b ∈ s), a < b → f b < f a`. Note the membership proofs are *explicit* (not instance-implicit) but the `a` and `b` are implicit. When I write `hleft (a := x) (le_of_lt hx1) (b := 1) (le_refl 1) hx1`, the first explicit argument is `(le_of_lt hx1)` and the second is `(le_refl 1)`. But wait — after `(a := x)` and `(b := 1)`, the explicit arguments in order are: `(le_of_lt hx1) : x ∈ Iic 1`, `(le_refl 1) : 1 ∈ Iic 1`, `hx1 : x < 1`. Expected: `x ∈ Iic 1` and `1 ∈ Iic 1`. The error says `le_refl 1` has type `1 ≤ 1` but expected `1 ∈ Iic 1`. So indeed membership is not reducible to `≤` for `exact`/application. That's bizarre but it means `Set.Iic` is not definitionally `{b | b ≤ a}` in this mathlib version? Let me check the definition.

Let me grep `def Iic` in mathlib Order/Set.
dsh: reasoning:
`def Iic (b : α) := { x | x ≤ b }` and `mem_Iic : x ∈ Iic b ↔ x ≤ b := .rfl`. So membership IS defeq to `≤`. So why did `le_refl 1 : 1 ≤ 1` not have type `1 ∈ Iic 1`?

Hmm! Maybe because the expected type is `1 ∈ Iic (1 : ?m)` where `?m` is not yet ℝ, and `1 ≤ 1` is at type `?m` too, so it should still match...

Wait, look at the error again: 
```
The argument
  le_refl 1
has type
  1 ≤ 1
but is expected to have type
  1 ∈ Iic 1
```
This is bizarre given `.rfl` defeq. Unless the expected type is `1 ∈ Iic 1` where `Iic` is `Set.Iic` but `1` on the left is `OfNat.ofNat 1` at type `ℝ` and `1` on the right (the set) is at a different type — no, then `le_refl 1` wouldn't typecheck either.

Hmm, maybe the issue is that the expected type is `1 ∈ Iic 1` where `Iic` is *not* `Set.Iic` but `Set.Iic` from `Set` opened... Let me test a minimal snippet to understand. Actually, maybe the error arises because `hleft (a := x) (le_of_lt hx1) (b := 1) (le_refl 1) hx1` — the named args `(a := x)` and `(b := 1)` might not be recognized as the implicit binders `⦃a⦄` and `⦃b⦄` because `StrictAntiOn` is reducible? They should be.

Hold on, maybe the issue is that after `(a := x)`, the next explicit argument `(le_of_lt hx1)` is matched to `a ∈ s` — fine. Then `(b := 1)` sets b. Then `(le_refl 1)` is expected `1 ∈ s`. And the error says it's expected `1 ∈ Iic 1`. So defeq check failed between `1 ≤ 1` and `1 ∈ Iic 1`. But `mem_Iic` is `.rfl`, so they ARE defeq. Unless the type of the set `Iic 1` is not ℝ but `ℕ`? If `b := 1` sets b to `1 : ?m` where `?m` is unresolved and defaults to `ℕ`? Then `a := x : ℝ` and `b : ?m` with `a < b` requiring same type → `?m = ℝ`. So no.

Hmm wait, maybe `s` is not `Iic 1` but the goal's expected membership is `x ∈ Iic 1` where `Iic` was parsed with the *notation* `Iic` from `Set` — yes.

Actually, maybe the problem is that `1 ∈ Iic 1` unfolds to `(1 : ℝ) ≤ (1 : ℝ)` only after `whnf`, but Lean's `exact`/application argument elaboration checks with `isDefEq` which should unfold. It's really strange.

Let me look at the first error again: at line 358, the linarith goal was `False`. This suggests that the elaboration of `(by linarith)` had expected type... Let me think about `StrictAntiOn` in this mathlib version once more. We saw:
```
def StrictAntiOn (f : α → β) (s : Set α) : Prop :=
  ∀ ⦃a⦄ (_ : a ∈ s) ⦃b⦄ (_ : b ∈ s), a < b → f b < f a
```
Note: the arguments `a ∈ s` and `b ∈ s` are *named as `_`* in the binder, i.e., they are explicit binder arguments? In Lean 4, `∀ ⦃a⦄ (_ : a ∈ s) ⦃b⦄ (_ : b ∈ s), ...` — the `(_ : ...)` binders are *implicit*? No: `_` in a binder position is a name that's inaccessible but the binder itself is explicit unless marked `{...}`. Actually in Lean 4, `(x : α)` is explicit, `{x : α}` is implicit, `⦃x : α⦄` is strict-implicit. `(_ : α)` is explicit but inaccessible. So `StrictAntiOn` has explicit membership arguments. Good, that's what we assumed.

Hmm, but wait: in the D4 cluster, `AntitoneCertificate.F_le_of_le` uses `c.mono h`, and `perelmanF_antitone` etc. In `Continuous.lean` they don't directly apply StrictAntiOn.

Let me just test this in a small scratch file to see what's happening. I'll write a minimal file importing the same module and check.

Actually, maybe the issue is different: `hleft` is a `have` with type `StrictAntiOn (gibbsTerm 1) (Iic 1)`. When applying `hleft (by linarith) hy hxy`, Lean may first unify the result type `f b < f a` with the goal `gibbsTerm 1 y < gibbsTerm 1 x`. But `f b < f a` where f is `gibbsTerm 1` — unification gives b := y, a := x. Then first argument type `x ∈ Iic 1`. The tactic `by linarith` runs on goal `x ∈ Iic 1`. Now here's the thing: `x ∈ Iic 1` where `Iic` is `Set.Iic`, and `x` is a local constant of type ℝ, `1` is ℝ. `linarith` preprocesses the goal: it likely calls `elabTermEnsuringType` and uses `linarith`'s preprocessing which may not reduce `x ∈ Iic 1` to `x ≤ 1` — it might see the goal as a non-linear/non-arithmetic atom `x ∈ Iic 1` and fail with a weird error. The error message showing `⊢ False` with hypotheses is just how linarith reports failure after trying to prove the goal by contradiction? Hmm, but it should print the actual goal.

I recall that `linarith` error messages do print the goal as `⊢ False` sometimes, and they list the hypotheses *including* the negation of the goal only if the goal is an inequality. If the goal is not recognized as arithmetic, it might print `⊢ False` with no negation. Actually yes: linarith internally proves `False` from `¬ goal`; the error prints the context at that point, which includes the negated goal as a hypothesis. If the goal was `x ∈ Iic 1`, the negation `h : ¬(x ∈ Iic 1)` would appear. It doesn't. So the goal was literally `False`. 

OH WAIT. I think I finally get it! The `·` bullet after `rcases le_or_gt y 1 with hy | hy` — the first case goal is `gibbsTerm 1 y < gibbsTerm 1 x`. Then `exact hleft (by linarith) hy hxy`. Lean elaborates `(by linarith)` with an expected type. But since `hleft`'s first explicit argument is `a ∈ s`, and `a` is an implicit metavariable, Lean may first try to unify the *expected type* of the whole expression with the goal. That gives `f ?b < f ?a =?= gibbsTerm 1 y < gibbsTerm 1 x`, so `?b = y`, `?a = x`. Fine.

Then the first argument `(by linarith)` needs type `x ∈ Iic 1`. The tactic block runs. `linarith` on `x ∈ Iic 1`... The goal is `x ∈ Set.Iic 1`. Since `Set.mem_Iic` is `.rfl`, whnf gives `x ≤ 1`. Then linarith proves it from hxy, hy. It should work!

Unless... `Iic` is *shadowed* in my file by something else! I did `open Set`. In section 1 I also `open Poincare.Longrun.CurvatureODE` etc. Is there an `Iic` in those namespaces? Unlikely.

Hmm, let me look at the actual error once more:
```
Audit/CounterexampleAudit.lean:358:22: error: linarith failed to find a contradiction
hc : 1 ≤ 1
hcont : Continuous (gibbsTerm 1)
hderiv : ...
hneg : ...
hleft : StrictAntiOn (gibbsTerm 1) (Iic 1)
hright : ...
x y : ℝ
hxy : x < y
hy : y ≤ 1
⊢ False
```
Interesting: the context contains `hc : 1 ≤ 1` — this is from the `rcases lt_or_eq_of_le hc with hlt | rfl` case. The local context is inside the `rfl` branch. And it lists all the haves. So the linarith goal was indeed `False`. 

AH, I understand now! The `⊢ False` is because linarith's `elabTermForApply`... no. Let me look at how linarith reports: In Lean4 mathlib, `linarith` fails with "linarith failed to find a contradiction" and prints the hypotheses *including the negated target*, where the negated target is expressed as a hypothesis. Wait, but here there's no negated target hypothesis and the target is False. 

Hmm, maybe the target `x ∈ Iic 1` got negated to `h : ¬ (x ∈ Iic 1)`, and then `push_neg`/preprocessing turned it into `h : 1 < x`, and then linarith proved... no, if it had `1 < x`, `x < y`, `y ≤ 1`, linarith would succeed.

You know what — maybe the issue is that `linarith` at that position was invoked on the goal `x ≤ 1` and *succeeded*, but then the *outer* `exact` failed, and the error we see is from a different linarith invocation? No, there's only one.

Let me stop speculating and test minimally. I'll create a scratch file with a small lemma to see the behavior.

Actually, simpler hypothesis: `hleft (by linarith) hy hxy` — maybe Lean parsed `hleft (by linarith) hy hxy` with `hy` being consumed as the *second* implicit `⦃b⦄`? No, implicits aren't passed positionally.

Wait, maybe the problem is the `Set.Iic` vs `Iic` type ascription: `hleft : StrictAntiOn (gibbsTerm 1) (Iic 1)`. Since `Iic 1` for ℝ — but is `Iic` possibly `Set.Iic` from the `Set` namespace where `Set.Iic` takes an implicit type param and `1` is parsed as `1 : ℝ`? Fine.

Let me just write a minimal test file:
```lean
import Poincare.Longrun.Evolution
open Set
example (f : ℝ → ℝ) (h : StrictAntiOn f (Iic 1)) (x y : ℝ) (hxy : x < y) (hy : y ≤ 1) : f y < f x := by
  exact h (by linarith) hy hxy
```
Let me test this quickly. Also test the `le_refl` variant. This will clarify.

Also error 3: `limit_passage_sanity` — the conclusion `perelmanF ?m.45 fun x => 1` where `?m.45` is the implicit `c`. My goal has `perelmanF (fun x => 1) ...`. Unification should work. But the error says could not unify; maybe because `perelmanF`'s implicit type `{ι}` is not determined until the `c` is known, and the `apply` can't infer `?m.45` from the goal? It should. Hmm, the message: "could not unify ... (perelmanF ?m.45 fun x => 1) ≤ ... with the goal (perelmanF (fun x => 1) fun x => 1) ≤ ...". Wait, why not? `?m.45 := fun x => 1` is a perfectly good assignment. Unless `?m.45` has a type that can't be inferred because the goal's `perelmanF (fun x => 1)` has an ambiguous index type. In the goal, `perelmanF (fun _ : Fin 1 => 1) ...` — I annotated `fun _ : Fin 1 => (1:ℝ)`, so ι = Fin 1 is determined. But the theorem's `?m.45 : ?m.44 → ℝ` where `?m.44` is the index type with Fintype. The state `fun n (_ : Fin 1) => ...` determines `?m.44 = Fin 1`, and `limit := fun _ => (1:ℝ)` — the `_` there could be any type, but it must match `ι = Fin 1`. Since I gave `limit := fun _ => 1` without annotating the binder type, and `state := fun n (_ : Fin 1) => ...` fixes ι = Fin 1, then limit's binder gets Fin 1. Hmm, but the elaboration order: I pass `(state := ...)` and `(limit := ...)`; the `limit` binder type may be elaborated as a metavariable that remains unresolved. Let me annotate `limit := fun _ : Fin 1 => (1:ℝ)` and `c := fun _ : Fin 1 => (1:ℝ)` and `(ι := Fin 1)` explicitly. That should fix it.

Now let me test the StrictAntiOn membership issue with a scratch file.
dsh: reasoning:
Very interesting! The third example reveals the issue: `h hx (le_of_lt hy) hy` — the argument `hy : 1 < y` is expected to have type `x < y`! So the argument order of `StrictAntiOn` is NOT `(a ∈ s) (b ∈ s) (a < b)` as I thought. Let me re-read the definition:

```
def StrictAntiOn (f : α → β) (s : Set α) : Prop :=
  ∀ ⦃a⦄ (_ : a ∈ s) ⦃b⦄ (_ : b ∈ s), a < b → f b < f a
```

Hmm, in this definition, the binders are: `⦃a⦄` (implicit a), `(_ : a ∈ s)` (explicit), `⦃b⦄` (implicit b), `(_ : b ∈ s)` (explicit), then `a < b → f b < f a`. So `h : StrictAntiOn f (Iic 1)`; `h hx` gives the first explicit membership `a ∈ s` where a is implicit. Then the next explicit argument expected is `b ∈ s`. In my third example `h hx (le_of_lt hy) hy`: hx : 1 ≤ x (a ∈ Iic 1?) — wait, `s = Ici 1`, so membership is `1 ≤ x`; hx matches `a ∈ Ici 1` with a = x. Then `(le_of_lt hy) : 1 ≤ y` should match `b ∈ Ici 1` with b = y. Then `hy : 1 < y` should match `a < b = x < y`?? The error says `hy : 1 < y` expected type `x < y`. So the unification set `a = ?` such that `a ∈ Ici 1` from `hx`... Hmm, the error suggests that after the first two args, the expected `a < b` is `x < y`? But hy : 1 < y. That means `a` was inferred as `1`, not `x`!

Wait: `h hx` — hx : 1 ≤ x. The expected type of the first explicit arg is `?a ∈ Ici 1` = `1 ≤ ?a`. Unifying with `1 ≤ x` gives `?a = x`. Hmm, that gives a = x. Then `(le_of_lt hy) : 1 ≤ y` unifies with `?b ∈ Ici 1` giving b = y. Then `a < b` = `x < y`, and hy : 1 < y doesn't match. Right! Because in this third example I mislabeled: `hright (a := 1) (le_refl 1) (b := y) (le_of_lt hy) hy` was the intended call. In my scratch I wrote `h hx (le_of_lt hy) hy` where hx : 1 ≤ x is the membership for a=x, then b=y, then need `x < y`, but I passed `hy : 1 < y`. So the scratch example was just wrong. OK.

Now the second example: `h (a := x) (le_of_lt hx) (b := 1) (le_refl 1) hx` — error says `le_refl 1 : 1 ≤ 1` expected `1 ∈ Iic 1`. So membership `1 ∈ Iic 1` isn't defeq to `1 ≤ 1`?? But the first example: `h (by linarith) hy hxy` where `hy : y ≤ 1` was accepted as `y ∈ Iic 1` (no error about hy!). Only the `(by linarith)` failed, and `hy` was fine. So `y ≤ 1` matches `y ∈ Iic 1`, but `1 ≤ 1` doesn't match `1 ∈ Iic 1`?

OH! I see it now. `Iic 1` might be parsed with the numeral `1` at a *different type* than the membership's `1`. In the second example, `(a := x)` and `(b := 1)`: when I write `(b := 1)`, Lean parses `1` with an expected type from `b : ?m` where `?m` is the type of `s = Iic 1`, i.e., `?m = ℝ`. So `b = (1:ℝ)`. Then the expected membership `1 ∈ Iic 1`... hmm, the `1` on the left of `∈` comes from `b = 1`. The `1` inside `Iic 1` is from `s`. Both ℝ. So `1 ∈ Iic 1` should be defeq to `1 ≤ 1`.

But wait — maybe the issue is that `(a := x)` is not actually setting the implicit `a`! Because `StrictAntiOn` is a *def*, and the implicit binders `⦃a⦄ ⦃b⦄` may have been *auto-bound* or renamed? Named arguments `(a := x)` work for binders named `a`. The definition uses `⦃a⦄` so the name is `a`. Hmm.

Actually, hold on. The error for example 2 says the application shown is `h (le_of_lt hx) (le_refl 1)` — the named args `(a := x)` and `(b := 1)` were dropped in the error display. And it says `le_refl 1` expected type `1 ∈ Iic 1`. So `b` = 1 indeed.

Why isn't `1 ≤ 1` accepted as `1 ∈ Iic 1`? Let me test `example : (1:ℝ) ∈ Iic 1 := le_refl 1` and `example : (1:ℝ) ∈ Iic 1 := (mem_Iic.mpr (le_refl 1))`. Maybe the issue is that `Iic` in the context is not `Set.Iic` but a *different* `Iic` — e.g., `Set.Iic` vs `Finset.Iic`? If `Iic 1` were `Set.Iic 1`, then `1 ∈ Iic 1` would be `1 ≤ 1` by rfl. Let me check whether the `Iic` in the type is printed as `Iic` but is actually `Set.Iic`... 

Wait, actually maybe the problem is the *expected type* is `1 ∈ Iic 1` where the set is `Iic 1` and Lean's `exact`/defeq check for `le_refl 1` fails because `1 ∈ Iic 1` unfolds to `(1:ℝ) ≤ 1` and `le_refl 1` has type `(1:?m) ≤ 1` with `?m` not yet resolved, and the elaborator tries to unify the *types* before assigning `?m`... but expected type would fix ?m = ℝ. Hmm.

And in example 1, `hy : y ≤ 1` was accepted as membership `y ∈ Iic 1`, which means defeq works there. The difference: in example 2, the `(by linarith)` for the first arg got goal `False`... 

OH WAIT. I think I finally see it! Look at example 1: `h (by linarith) hy hxy`. The first explicit argument `(by linarith)` has expected type `?a ∈ Iic 1`. `linarith` fails with goal False. But `hy` (the second explicit arg) is accepted. The third `hxy : x < y` is accepted as `a < b` — which means a = x and b = y were already fixed *before* the first argument was elaborated?! How? Because Lean processes the *expected type* of the whole term first: `h ... : f b < f a` must equal `f y < f x`, so `b = y`, `a = x`. So the first arg's expected type is `x ∈ Iic 1`. Then linarith on `x ∈ Iic 1`... and it fails with goal False. And in example 2, `a = x`, `b = 1` from expected type `f 1 < f x`, so the first arg `le_of_lt hx : x ≤ 1` is accepted as `x ∈ Iic 1`; the second arg expected `1 ∈ Iic 1`, and `le_refl 1 : 1 ≤ 1` is rejected?! 

This is really strange: `x ≤ 1` accepted as `x ∈ Iic 1` but `1 ≤ 1` rejected as `1 ∈ Iic 1`. Unless... the second expected type is not `1 ∈ Iic 1` but `(1:ℝ) ∈ Iic (1:ℝ)` where the set `Iic 1` has a *different* type parameter... no.

Hmm, wait. What if `b := 1` did NOT set b, because the named binder is `b` but there are *two* binders named `_`? No, the name `b` is the implicit binder `⦃b⦄`. 

Let me test directly:
```
example : (1:ℝ) ∈ Iic 1 := le_refl 1
```
If this fails, then there's something weird about `Iic` and numerals. Let me test. Also test `Set.mem_Iic`.

Actually — I bet I know what it is! `Set.Iic` in this mathlib version is defined in `Mathlib/Order/Interval/Set/Defs.lean` as:
```
def Iic (b : α) := { x | x ≤ b }
```
But is it inside `namespace Set`? The file might define it in `namespace Set`. And the `1 ∈ Iic 1` printed type might actually be `1 ∈ Iic 1` where `Iic` is `Set.Iic`... 

Hmm, let me just run the test.
dsh: reasoning:
Fascinating: `example : (1:ℝ) ∈ Iic 1 := le_refl 1` fails, but `Set.mem_Iic.mpr (le_refl 1)` presumably works (line 7 had no error). And the annotated `show (1:ℝ) ∈ Iic (1:ℝ) from le_refl 1` at line 13 also failed. So `(1:ℝ) ∈ Iic (1:ℝ)` is NOT definitionally equal to `1 ≤ 1` in this environment, even though `mem_Iic : x ∈ Iic b ↔ x ≤ b := .rfl`.

Wait, the third example line 9 also failed: `example : (1:ℝ) ∈ Iic (1:ℝ) := le_refl 1`. So the numeral `1` inside `Iic 1` might not be ℝ! If `Iic 1`'s numeral is at a different type, then `1 ∈ Iic 1` is a membership where the left `1` is ℝ and the set is `Iic (1:?m)` — the expected type of the whole expression forces the membership type: `1 ∈ Iic (1:?m)` requires the element type and set type to match, so `?m = ℝ`. Hmm.

Unless `Iic` here is not `Set.Iic` but the *interval* notation from `Mathlib/Order/Interval/Set/Defs.lean` where `Iic` is defined in a namespace and there's also a `Set.Iic`... Actually, the error message at line 5 says expected type `1 ∈ Iic 1`, and the term `le_refl 1` has type `1 ≤ 1`. If these were defeq, no error. So the `∈` here is not `Set.Mem`? Or `Iic` is a custom operator?

Hold on — maybe the issue is that `Iic` in this environment is the **`Finset.Iic`**? No, numerals.

Wait, maybe `Iic` is from `Order.Iic` for `Set` but `1 ∈ Iic 1` uses `Membership.mem` for a different membership instance? Hmm.

Let me use `#check` to inspect. Let me write a test with `#check @Set.mem_Iic`, `#check (Iic (1:ℝ) : Set ℝ)`, `#check fun (x:ℝ) => (show x ∈ Iic 1 from _)`.

Actually, let me look at the definition file more carefully. Maybe `Set.Iic` is defined via `OrderDual`:
```
def Iic (b : α) := { x | x ≤ b }
```
with `.rfl` for mem_Iic. That means defeq. Unless there's a `@[simp]` or `Iic` is `Set.Iic` with a `SetLike` coercion...

Hmm, could it be that the `open Set` makes `Iic` refer to `Set.Iic`, but the notation `∈` for sets is `Set.Mem`... 

Let me check whether `le_refl 1` at line 5 has type `1 ≤ 1` where `1` is at type `?m` and the expected type `1 ∈ Iic 1` fixes `?m = ℝ`... The elaborator elaborates `le_refl 1` first (without expected type?) and then checks. `le_refl 1 : ?m ≤ ?m` with `?m` a metavariable; then the expected type `1 ∈ Iic 1` is `Set.Mem (1:ℝ) (Set.Iic (1:ℝ))` which whnf's to `(1:ℝ) ≤ (1:ℝ)`. Unifying `?m ≤ ?m` with `ℝ ≤ ℝ` gives `?m = ℝ`. Should work. Unless the elaborator doesn't whnf the expected type during unification with the term type because `Set.Iic` is marked `irreducible`? Is `Set.Iic` irreducible? Let me check for `@[irreducible]` on the def. The file showed:
```
def Iic (b : α) := { x | x ≤ b }
```
No attribute. Hmm.

Wait, maybe the `Iic` that appears in the error is *not* `Set.Iic` but something like `Set.Iic` with an extra `Set.Elem` wrapper? The error prints `1 ∈ Iic 1`. If it were `Set.Iic`, `1 ∈ Set.Iic 1` unfolds to `1 ≤ 1`.

Let me test with explicit `Set.Iic`: `example : (1:ℝ) ∈ Set.Iic 1 := le_refl 1`. And `example : (1:ℝ) ∈ Set.Iic (1:ℝ) := le_refl 1`. Let me also `#check @Set.mem_Iic` and `#check Set.Iic_def`.

Actually, here's a thought: `open Set` might not be enough to resolve `Iic` to `Set.Iic`; there might be a *global* `Iic` (e.g., in `Order` namespace) that shadows it. In Mathlib, `Iic` is actually in the *root* namespace? Let me check: the definition file Mathlib/Order/Interval/Set/Defs.lean — is it inside `namespace Set`? Let me look at the file.
dsh: reasoning:
Aha, line 29: `@[expose] public section`. The `@[expose]` attribute makes definitions in this section *exposed* (reducible for defeq checks?) Hmm, `@[expose]` means definitions are exposed for `whnf`? Actually `@[expose]` marks a section as exposing definitions so that `def` transparency includes them? In recent Lean, `@[expose]` makes the definitions reducible by `whnf`/kernel? Hmm, it should make them *more* reducible, not less.

But note: `Iic` is `def Iic (b : α) := { x | x ≤ b }` and `mem_Iic := .rfl`. So `1 ∈ Iic 1` should be defeq to `1 ≤ 1`.

Unless... `Set.Mem` is not reducible? `x ∈ s` is `Set.Mem x s` where `Set` is a structure with field `carrier`? In Lean 4 / mathlib, `Set α := α → Prop` (a reducible def). So `x ∈ s` is `s x` which is `(fun y => y ≤ b) x` beta-reduces to `x ≤ b`. All defeq.

So why does `le_refl 1` fail? Let me test with `exact` vs direct term. The error at line 5 uses `:=` (term mode). Maybe the elaborator doesn't unfold the expected type when elaborating `le_refl 1` because `le_refl`'s argument is a metavariable and unification happens at a higher transparency? The expected type `1 ∈ Iic 1` is given to `elabTermEnsuringType`, which uses `isDefEq` with default transparency `.reducible`? `Set.Iic` is a regular `def`, which at `.reducible` transparency does NOT unfold (regular defs unfold at `.instances`/`.semireducible`?). Hmm, in Lean 4, default transparency for elaboration is `.reducible`? Actually, `elabTermEnsuringType` uses `isDefEq` with `.default` transparency, which is `.instances`? I recall default is `.instances`? Hmm.

But `Set.Iic` is a def; `Set α` is a def too. If the elaborator doesn't unfold them, then `1 ∈ Iic 1` vs `1 ≤ 1` would be seen as syntactically different. But normally Lean unfolds regular defs during unification at default transparency. 

Wait — maybe `@[expose]` changes this? No, `@[expose]` was introduced in Lean 4.12 to allow definitions to be unfolded in the kernel for `whnf` even when they're in a `module`... The `module` keyword (line 6) means the file is a "module" with restricted visibility? `public section` + `@[expose]` exposes the definitions. This should be fine.

Let me just test with `show`:
```
example : (1:ℝ) ∈ Iic 1 := by exact le_refl 1
```
Maybe the term-mode `:=` fails but tactic-mode `exact` works? The error was term-mode. Let me test tactic mode. Actually in the original file, the failure at line 363 was in term mode `exact hleft ... (le_refl 1) ...` — the argument `(le_refl 1)` is elaborated in term mode against expected type `1 ∈ Iic 1`. So the issue is term-mode elaboration of the argument with expected type that doesn't unfold.

Simple fix: use `by simp` or `by exact le_refl 1` or `Set.mem_Iic.mpr (le_refl 1)` for these membership arguments. Let me test `(by simp)` — in scratch2 line 17 I wrote `(by simp)`; the errors listed only lines 5, 9, 13, so line 17 (the `by simp` version) compiled! Great. So `by simp` works as a membership proof.

Similarly, for `hleft (by linarith) hy hxy` — the `(by linarith)` has expected type `x ∈ Iic 1`. In tactic mode, `linarith` sees the goal `x ∈ Iic 1`; does tactic-mode `exact`/`linarith` whnf the goal? Apparently linarith failed with goal False — meaning the tactic goal was displayed as `x ∈ Iic 1`? Hmm, the error showed `⊢ False`. But if linarith treats the goal as `x ∈ Iic 1` (unreduced), it might fail weirdly. Fix: use `(by simp [Set.mem_Iic]; linarith)`? Or `(show x ≤ 1 by linarith)` — `show` uses defeq at default transparency; if `x ∈ Iic 1` is not defeq under default transparency, `show x ≤ 1` would fail. Let me test. Better: `(by simp only [Set.mem_Iic]; linarith)` or `(by exact Set.mem_Iic.mpr (by linarith))`. The latter is robust.

Actually, simpler: use `Set.mem_Iic.mpr` everywhere for Iic memberships and `Set.mem_Ici.mpr` for Ici. Let me rewrite the strict anti proof to use `Set.mem_Iic.mpr`/`Set.mem_Ici.mpr`.

Let me test in scratch:
```
example (f : ℝ → ℝ) (h : StrictAntiOn f (Iic 1)) (x y : ℝ) (hxy : x < y) (hy : y ≤ 1) :
    f y < f x := by
  exact h (Set.mem_Iic.mpr (by linarith)) (Set.mem_Iic.mpr hy) hxy
```
Let me test this plus the others. Also test `Set.mem_Ici.mpr`.

Also, why did `hy : y ≤ 1` get accepted in the earlier example as the second argument? Because in that context the expected type was `y ∈ Iic 1` and `hy : y ≤ 1`... but the error at line 7 was only about linarith, and `hy` was accepted? Actually the error was reported for the whole `exact`; the elaborator may have failed on the first argument before checking the second. So `hy` may not have been checked. In the third example, `(le_of_lt hy) : 1 ≤ y` was accepted as `y ∈ Ici 1`? The error there was about `hy` expected `x < y`, which means the first two args were accepted. So `1 ≤ y` was accepted as `y ∈ Ici 1`! But `le_refl 1` was NOT accepted as `1 ∈ Iic 1`. The difference: in the Ici case, the expected type `y ∈ Ici 1` unfolds to `1 ≤ y` and `le_of_lt hy : 1 ≤ y` matches syntactically after whnf. In the Iic case, expected `1 ∈ Iic 1` unfolds to `1 ≤ 1`; `le_refl 1 : 1 ≤ 1` should match. Hmm, unless the numeral `1` in the expected type is `OfNat.ofNat 1` at ℝ while `le_refl 1`'s `1` is at a metavariable type, and the unifier picks `?m = ℝ`... 

Whatever the cause, `Set.mem_Iic.mpr` is a robust workaround. Let me test.

Also, the `by linarith` goal `x ∈ Iic 1`: in tactic mode, linarith should be able to see through the membership? Apparently not (goal False). Let me use `Set.mem_Iic.mpr (by linarith)`.

Let me update the scratch and test.
dsh: reasoning:
Only the third example failed, and that's because I made an error in the example (I passed hx : 1 ≤ x for a=x, then b=y, then need x < y but gave hy : 1 < y). That example was wrong on my part. The first, second, and fourth examples compiled. So the workaround works.

Now let me fix the verifier file's `gibbsTerm_strictAnti_of_one_le` with `Set.mem_Iic.mpr` / `Set.mem_Ici.mpr`.

The cases:
```
rcases le_or_gt y 1 with hy | hy
· exact hleft (Set.mem_Iic.mpr (by linarith)) (Set.mem_Iic.mpr hy) hxy
· by_cases hx1 : 1 ≤ x
  · exact hright (Set.mem_Ici.mpr hx1) (Set.mem_Ici.mpr (le_of_lt hy)) hxy
  · push_neg at hx1
    have h1 : gibbsTerm 1 1 < gibbsTerm 1 x :=
      hleft (a := x) (Set.mem_Iic.mpr (le_of_lt hx1)) (b := 1) (Set.mem_Iic.mpr (le_refl 1)) hx1
    have h2 : gibbsTerm 1 y < gibbsTerm 1 1 :=
      hright (a := 1) (Set.mem_Ici.mpr (le_refl 1)) (b := y) (Set.mem_Ici.mpr (le_of_lt hy)) hy
    exact h2.trans h1
```

Also `push_neg` deprecation warning — use `push Not at hx1`? The warning suggests `push Not`. Let me use `simp only [not_le] at hx1` instead, to avoid deprecation. Actually `not_le` : `¬(a ≤ b) ↔ b < a`. So `simp only [not_le] at hx1` gives hx1 : 1 < x? Wait, hx1 : ¬ (1 ≤ x), so not_le gives `x < 1`. Good.

Now fix `limit_passage_sanity` by giving explicit `(ι := Fin 1) (c := ...)` and annotating the limit binder.

Let me apply the edits.
dsh: reasoning:
The issue: my goal's `(0 + 1)` has `0` elaborated as `(0:ℝ)`? In the goal it prints `1 - 1 / (0 + 1)` without the `↑`, meaning the `0` is a plain `OfNat 0` at type... The theorem's conclusion has `1 - 1 / (↑0 + 1)` where `↑0` is `Nat.cast 0` (from `n=0 : ℕ` cast to ℝ). My statement wrote `(0 : ℝ) + 1`, which prints as `0 + 1` (no cast). These are definitionally equal via `Nat.cast_zero` but not syntactically, and `apply` unifies at reducible transparency, where `Nat.cast_zero` doesn't fire.

Fix: state the goal exactly as the theorem's conclusion: `fun _ => 1 - 1 / ((↑(0:ℕ) : ℝ) + 1)` or `((0 : ℕ) : ℝ) + 1`. Let me write `1 - 1 / (((0 : ℕ) : ℝ) + 1)`. Then the printed form matches. Let me update.

Actually, even simpler: prove via `have h := perelmanF_limit_le_of_discrete ...` then `exact h` — the `have` would infer the conclusion type automatically from the arguments, and then `exact h` unifies h's type with my goal; same issue. So best to state the goal in the exact form.

Let me update the statement to `(fun _ => 1 - 1 / (((0 : ℕ) : ℝ) + 1))`.
dsh: reasoning:
Line 469:41 is in the hmono bullet: `rw [sub_nonneg, div_le_one (by positivity)]` — `div_le_one (by positivity)` where the type is ambiguous? Actually the error mentions `ContinuousSMul ℚ≥0 ?m.188`, which comes from `tendsto_one_div_add_atTop_nhds_zero_nat` whose type is polymorphic in `𝕜` with `[ContinuousSMul ℚ≥0 𝕜]`. In the hlim bullet, `tendsto_const_nhds.sub tendsto_one_div_add_atTop_nhds_zero_nat` — the `𝕜` needs to be inferred as ℝ. I did `simpa using ...`; maybe the metavariable isn't resolved. Let me annotate: `(tendsto_one_div_add_atTop_nhds_zero_nat (𝕜 := ℝ))`.

Wait, line 469 is the hmono bullet (the `· intro n` block starts at 460ish). Let me look at the current line numbers.
dsh: reasoning:
Line 469:41 is `tendsto_one_div_add_atTop_nhds_zero_nat` in the simpa. The `𝕜` metavariable is not resolved because `simpa` might not propagate the expected type into the argument before resolving typeclass. Let me annotate `(𝕜 := ℝ)`:

`simpa using tendsto_const_nhds.sub (tendsto_one_div_add_atTop_nhds_zero_nat (𝕜 := ℝ))`

The `tendsto_const_nhds` also needs its type resolved; `sub` should fix it. Let me also annotate `(tendsto_const_nhds : Tendsto (fun _ : ℕ => (1:ℝ)) atTop (𝓝 1))`. Let me write:

```
  · exact tendsto_pi_nhds.mpr (fun _ => by
      have h1 : Tendsto (fun _ : ℕ => (1 : ℝ)) atTop (𝓝 (1 : ℝ)) := tendsto_const_nhds
      have h2 : Tendsto (fun n : ℕ => 1 / ((n : ℝ) + 1)) atTop (𝓝 (0 : ℝ)) :=
        tendsto_one_div_add_atTop_nhds_zero_nat
      simpa using h1.sub h2)
```
That should resolve everything. Let me apply.
dsh: reasoning:
The verifier file compiles with exit 0 and no errors/warnings. Let me now:
1. Check the output for `sorryAx` and axiom sets.
2. Run hygiene greps for forbidden keywords.
3. Verify the file compiles from a clean state? It already did.
4. Check the full output file.

Let me look at the output.
dsh: reasoning:
Excellent:
- 56 declarations audited, all with exactly `[propext, Classical.choice, Quot.sound]`, no `sorryAx`.
- No forbidden keywords at line start in my verifier or the library.

Wait, `grep -c "depends on axioms"` gave 56, but my file has 41 audit declarations + 12 promoted = 53. Hmm, 56? Let me count: my `#print axioms` list: let me count. Section 10 list: 
1 audit_perelmanF_antitone
2 audit_perelmanF_antitone_discrete
3 audit_hasDerivWithinAt_perelmanF
4 audit_two_sided_monotonicity
5 nonvacuity_main_theorem
6 nonvacuity_main_theorem_strict
7 dissipation_sanity
8 three_exp_neg_one_lt_nine_exp_neg_two
9 gibbsTerm_half_one_lt_two
10 counterexample_discrete_c_half
11 antitone_discrete_fails_at_c_half
12 counterexample_continuous_c_half
13 counterexample_continuous_c_half_is_evolution
14 antitone_fails_at_c_half
15 counterexample_negative_step
16 antitone_discrete_fails_at_negative_step
17 negReaction
18 negTrajDisc
19 negTrajDisc_step
20 weak_discrete_monotonicity_false
21 NoSignEvolution
22 negTrajCont
23 negTrajCont_hasDeriv
24 noSignEvolution_neg
25 negative_reaction_continuous_counterexample
26 no_sign_continuous_monotonicity_false
27 zero_step_constant
28 strict_step_needs_pos_step
29 strict_step_needs_pos_reaction
30 gibbsTerm_strictAnti_of_one_le
31 gibbsTerm_step_lt_of_one_le
32 perelmanF_step_lt_of_one_le
33 perelmanF_nonneg_sharp
34 sqTraj
35 sqTraj_evolution
36 strict_step_positive_control
37 perelmanApproximation_nonvacuous
38 transfer_nonvacuous
39 finiteMeshConvergence_nonvacuous
40 limit_passage_sanity
41 certificate_lower_bound_zero
42 audit_certificate_compare
43 audit_continuous_certificate
44 audit_finiteReactionEntropyData_F

Hmm, I didn't include `two_exp_neg_one_lt_one` and `gibbsTerm_one_values` in the #print axioms list. 44 + 12 promoted = 56. Yes, 56. Good.

Wait, but I should add `two_exp_neg_one_lt_one` and `gibbsTerm_one_values` to the axiom audit for completeness. Let me add them. Also, the `#print axioms` on promoted declarations: 12 lines (perelmanF_antitone, perelmanF_antitone_discrete, perelmanF_step_lt, gibbsTerm_strictAnti, gibbsTerm_step_lt, perelmanF_nonneg, squareTraj_counterexample, continuousPerelmanCertificate, perelmanF_limit_le_of_discrete, continuousPerelmanFMonotone_of_approximation, perelmanF_monotone_of_tensorBridge, PerelmanEvolutionBoundary). The output has 56 lines total, all clean.

Let me view the full output to make sure the promoted declarations printed correctly.
dsh: reasoning:
All good. Now let me add the two missing declarations (`two_exp_neg_one_lt_one`, `gibbsTerm_one_values`) to the axiom audit. Then recompile.

Also, I want to do a couple more adversarial checks that I haven't yet:

1. Verify that the claimed "corrected theorem" is genuinely not implied by the original: at c=1 the original's hypothesis `1 < c` fails. That's evident. My positive control uses c=1. Good.

2. Check the overstrong claim more rigorously: is `gibbsTerm 1` really StrictAnti? My proof is kernel-checked, so yes.

3. I should double-check there's no *false* promoted statement. One more: `finiteReactionEntropyData_W` — the identity uses `extra` and `integral_count`. It's checked. Fine.

4. Check `perelmanF_antitone` is not overstrong in the `T` parameter: the theorem requires `s,t ∈ Icc 0 T`; if T < 0, Icc 0 T is empty, so the theorem is vacuous for T<0. The main theorem statement `perelmanF_antitone` quantifies over T with no sign condition; for T<0 the hypothesis `EvolutionRelation F T traj` might still be satisfiable (e.g., constant trajectory) and the conclusion is vacuous (no s,t). Not an issue. But worth noting: `perelmanF_le_initial` requires `ht : t ∈ Icc 0 T`, which is empty for T<0. Fine.

5. Potential vacuity of `perelmanF_step_lt_of_one_le`: I gave a positive control.

6. Should I check the "two_sided_monotonicity" statement for a sign error? It says perelmanF nonincreasing while scalarOfState nondecreasing. Both derived from the same ODE. Correct.

7. One more potential adversarial angle: the D4 card's claim "68/68 declarations with single axiom set". My independent audit of 56 declarations confirms a single axiom set. The cluster's own Audit/EvolutionAudit.lean has 68 lines. I could rerun the cluster's audit file in my worktree to independently reproduce 68/68. Since I copied the library but not Audit/EvolutionAudit.lean, I could copy that file and run it. That's a good independent reproduction. Let me copy `Audit/EvolutionAudit.lean` from D4 to my worktree as `Audit/PromotedEvolutionAudit.lean` and run it, verifying 68 lines all clean. Actually the task says my verifier file must import only the promoted interfaces — the D4 audit file does that too. But my own file already prints the key promoted declarations. Rerunning the promoted audit verbatim is a nice extra. However, adding a second file isn't required. I'll run it as a separate command (copy to a scratch name, run, then maybe delete or keep in Audit/ as evidence). Let me do it: copy `EvolutionAudit.lean` to `Audit/PromotedAudit.lean`, run `lake env lean`, count 68 and check no sorryAx. This reproduces the builder's claim independently.

8. Also verify the hash of my verifier file and record.

9. Write the result card (md + json) in `longrun/results/` in my worktree. Also try copying to the shared `longrun/results/` (may be denied by sandbox; the D4 card noted it was denied). Let me attempt the copy and see.

Before that, let me add the two declarations to the axiom list and recompile. Also, I realize I should double check the theorem `strict_step_needs_pos_step`: it says for any F, c, lam, `¬ perelmanF c (eulerStep F 0 lam) < perelmanF c lam`. Good.

One more check on the "overstrong" finding: I should confirm that the corrected theorem's hypothesis is not just `1 ≤ c i` but perhaps even weaker (pointwise). The audit card can note that `1 ≤ c i` is sharp for the *uniform* statement, but the corrected `perelmanF_step_lt_of_one_le` is the cleanest correction. Fine.

Let me also reconsider whether `perelmanF_step_lt`'s `1 < c i` is truly overstrong or whether there's a subtlety with the other components. Suppose c i = 1, h > 0, F.eval (traj n) i > 0. Then term i strictly decreases. Other terms j: gibbsTerm (c j) (x + u_j) ≤ gibbsTerm (c j) x with u_j ≥ 0 (since h > 0, eval ≥ 0). So the sum strictly decreases. Yes. My proof is kernel-checked. So `1 < c i` is overstrong. Good.

Now, let me also double check `perelmanF_step_lt_of_one_le`'s proof handles the case c i = 1 correctly: `gibbsTerm_step_lt_of_one_le (hc i) (mul_pos hh hi)` uses `gibbsTerm_strictAnti_of_one_le`, which covers c=1. Good. And the positive control at c=1 exercises exactly this.

Let me add the missing axiom prints and recompile. Then run the promoted audit.
dsh: reasoning:
Both audits pass:
- My verifier: 58 declarations, all with `[propext, Classical.choice, Quot.sound]`, no sorryAx.
- The promoted audit rerun: 68 declarations, all `depends on axioms` lines, no sorryAx. (The `grep -c "sorryAx"` returned 0 and exit code 1 because no matches — that's why the overall bash exit code is 1. Fine.)

Let me verify the promoted audit's distinct axiom sets too, and confirm 68 lines all with the same set.

Then:
- Run a fresh clean build check? My worktree's library was built from copied sources (byte-identical to D4's promoted sources). I already verified hashes. The verifier file compiles against those oleans. To be extra rigorous, I could delete `.lake/build` and rebuild + recompile the verifier. That takes ~1-2 min based on the earlier build. Actually the earlier build took under 2 minutes (I didn't time it, but it finished quickly). Let me do a clean rebuild to be safe? The D5 task is "clean rebuild" (independent verifier reproduces all promoted commits), which is a separate task. For D4 audit, the build I did was already from scratch in my own worktree (`.lake/build` didn't exist). So it was a clean build. Good. I can note that.

Wait, actually let me double check that the build in my worktree was from scratch: my worktree started empty, I copied sources and created `.lake/packages` symlink, then ran `lake build Poincare`. The `.lake/build` was created fresh. Yes, clean build.

- Now write the result card. It must record:
  * Task id, worktree, status, commands and exit codes.
  * What was verified.
  * Findings: false/vacuous/overstrong statements.
  * Corrected theorem.
  * Axiom results.
  * Hygiene.
  * Boundaries.

Findings summary:
1. No false statement found among the promoted declarations I audited. All 58 of my audit declarations + all 68 promoted-audit declarations have axioms exactly [propext, Classical.choice, Quot.sound], no sorryAx.
2. `perelmanF_antitone` / `perelmanF_antitone_discrete`: hypothesis `1 ≤ c` is sharp. Weakening to `0 ≤ c` fails at c = 1/2 both discrete and continuous (my counterexamples; the cluster had only c=0). Not overstrong.
3. `0 ≤ h` is necessary (negative step counterexample).
4. Nonnegativity of the reaction (D2 `ReactionField.eval_nonneg`) is essential: without it, discrete and continuous monotonicity fail (my counterexamples with G ≡ -1).
5. `perelmanF_step_lt`'s hypothesis `1 < c i` is **overstrong**: `1 ≤ c i` suffices. The flat spot of gibbsTerm 1 at x=1 is isolated, so gibbsTerm 1 is strictly antitone. Corrected theorem `perelmanF_step_lt_of_one_le` proved, with positive control at c=1. Same correction applies to `gibbsTerm_strictAnti` and `gibbsTerm_step_lt`.
6. The hypotheses `0 < h` and `0 < F.eval (traj n) i` in `perelmanF_step_lt` are both necessary.
7. `perelmanF_nonneg`'s `0 ≤ c i` is necessary (c=-1, lam=0 gives -1).
8. Non-vacuity: `PerelmanApproximation` is inhabited (by the finite datum itself), so `continuousPerelmanFMonotone_of_approximation` is not vacuous; but the canonical inhabitant carries no continuous content (honest boundary). `FiniteMeshConvergence` is inhabited. `perelmanF_limit_le_of_discrete` passes a nontrivial limit check.
9. `PerelmanEvolutionBoundary` and `TensorRicciFlowODEBridge` have no inhabitants (conditional transfer; not claimed proved). Not vacuous in the logical sense of the theorem (they're structures/Prop), but the transfer theorems are conditional. I did not construct an inhabitant of `TensorRicciFlowODEBridge` (would require manifold data); the D2 card already says none exists.
10. Sign convention: the functional is nonincreasing, opposite to Perelman's F. Documented by the builder; not a false statement since the cluster explicitly disclaims it.

Also note: the cluster's own card §7.1/7.2 used c=0; my c=1/2 counterexample is strictly stronger evidence that the threshold `1 ≤ c` can't be relaxed to `0 ≤ c` (it already fails for a positive c < 1).

One caveat about "vacuous": `perelmanF_antitone` is non-vacuous (I gave a witness). `perelmanF_step_lt`'s *original* statement is non-vacuous too (e.g., c=2, squareField, start lam=1). My corrected theorem is non-vacuous (positive control at c=1).

Let me also record the `#print axioms` evidence: 58/58 in my file, 68/68 in the rerun of the promoted audit.

Now, about the result card location: my workspace is the worktree; the shared `longrun/results/` is outside. Let me attempt to write there; if denied, keep the mirror in the worktree and note the delivery caveat (same as D4). Actually, per instructions "Approval policy: ask" and the sandbox is workspace-write. Writing outside the workspace would be denied. Let me try the copy and see the marker. If denied, I won't escalate because the policy says workspace-write is the enforcement and the D4 precedent is to mirror. Actually, the instructions say if a command is denied and a wider mode would let it succeed, I may escalate once with justification. But is it worth it? The D4 card was accepted with a mirror; the integrator action is to copy. The task doesn't explicitly require the shared dir. I'll mirror inside the worktree and note it. Let me try the copy to see if it's allowed (maybe /data3/.../longrun/results is writable by my sandbox? The D4 worker said it wasn't). Let me try.

First, let me write the result card files in the worktree. I'll write the markdown card and a JSON twin.

Let me also record exact commands:
- `lake build Poincare` exit 0 (clean build in this worktree; log longrun/logs/audit.build.log)
- `lake env lean Audit/CounterexampleAudit.lean` exit 0, 58 axiom lines, no sorryAx
- `lake env lean Audit/PromotedEvolutionAudit.lean` exit 0, 68 axiom lines, no sorryAx
- hygiene grep exit 1 (no matches)
- sha256 of my verifier file.

Let me compute the sha256 of my verifier file and get line counts.

Also, I should record the finding about the overstrong hypothesis precisely: `gibbsTerm_strictAnti`, `gibbsTerm_step_lt`, `perelmanF_step_lt` have `1 < c` where `1 ≤ c` suffices. I proved the corrected versions in the verifier file.

Before writing the card, let me also double check the promoted audit output's distinct axiom sets.
dsh: reasoning:
The shared results dir is not writable (permission denied, not a sandbox denial marker but a plain permission error — the directory is owned by guoshaoyang but mode drwxr-xr-x? Wait, owner is guoshaoyang and I'm presumably running as guoshaoyang... the files inside are mode 600/-rw------- owned by guoshaoyang. Hmm, the directory is drwxr-xr-x owned by guoshaoyang, so the owner should be able to write. Unless the process runs as a different user (sandbox user). The touch failed with Permission denied. Possibly the sandbox enforces workspace-write and blocks writes outside, but the error is EACCES. Either way, I'll mirror in my worktree, same as the D4 worker.

Note: the D4 card's `longrun/results/` mirror is in the D4 worktree. My worktree has `longrun/results/`. I'll write there.

Now let me write the result card. Let me compose a comprehensive but focused markdown card.

Card contents:
- Header: task id, status, worktree, toolchain, mathlib rev, model.
- §1 Consumed input: D4 promoted interfaces, hash verification of all 7 D4 modules + the D2/D3 consumed sources (byte-identical to accepted cards).
- §2 What the verifier does: imports only `Poincare.Longrun.Evolution`; fresh namespace `D4Audit`; 44 own declarations; counterexamples; corrected theorem; #print axioms.
- §3 Verdict: no false statement found; one overstrong hypothesis family found; several hypotheses sharp; boundaries honest.
- §4 Findings table.
- §5 Counterexamples (with the exact Lean names and values).
- §6 Corrected theorem (statement + why) — code snippet.
- §7 Axiom audit: commands, exit codes, counts, single axiom set.
- §8 Hygiene.
- §9 Non-vacuity.
- §10 Residual boundaries / follow-ups.
- §11 Files and delivery note.

Also the JSON twin with structured fields.

Let me write the markdown card first.

Important details to include:
- `lake build Poincare` exit 0 in this worktree (clean local `.lake/build`, shared prebuilt mathlib).
- `lake env lean Audit/CounterexampleAudit.lean` exit 0, 0 bytes on stderr? Actually stdout has the #print axioms output; exit 0.
- `lake env lean Audit/PromotedEvolutionAudit.lean` exit 0 (68 lines).
- Hygiene grep exit 1 (no matches).
- The overstrong finding: `perelmanF_step_lt`, `gibbsTerm_strictAnti`, `gibbsTerm_step_lt` with `1 < c`; corrected to `1 ≤ c` and proved (`gibbsTerm_strictAnti_of_one_le`, `gibbsTerm_step_lt_of_one_le`, `perelmanF_step_lt_of_one_le`), positive control at c=1 (`strict_step_positive_control`).
- Sharpness of `1 ≤ c`: `counterexample_discrete_c_half`, `counterexample_continuous_c_half` at c = 1/2 ≥ 0 (stronger than the builder's c=0 witnesses).
- `0 ≤ h` necessary: `counterexample_negative_step`.
- Reaction nonnegativity necessary: `weak_discrete_monotonicity_false`, `no_sign_continuous_monotonicity_false`.
- `0 < h` necessary: `strict_step_needs_pos_step`; `0 < F.eval` necessary: `strict_step_needs_pos_reaction`.
- `0 ≤ c` necessary for `perelmanF_nonneg`: `perelmanF_nonneg_sharp`.
- Non-vacuity of approximation: `perelmanApproximation_nonvacuous`, `transfer_nonvacuous`, `finiteMeshConvergence_nonvacuous`, `limit_passage_sanity`.
- Certificates: `certificate_lower_bound_zero`, `audit_certificate_compare`, `audit_continuous_certificate`.

Also record the D4 cluster's own counterexample module re-verified via `#print axioms` and the hash check.

Let me be careful about the claim "no false statement": I audited the promoted cluster's statements as imported; I didn't formally prove all of them independently, but I re-checked the main ones and ran the cluster's own audit. I should phrase precisely: "No false statement was found. The main statements are re-checked; the remaining promoted declarations are kernel-checked in the imported oleans and their axiom cones are clean. The one defect is overstrength, not falsity."

Also, I should note that `perelmanF_monotone_of_tensorBridge` and `PerelmanEvolutionBoundary` are conditional on uninhabited structures; I did not construct inhabitants. That's a documented boundary, not a defect.

Let me now write the card. I'll write both md and json.

For the JSON twin, include fields: task_id, status, worktree, toolchain, mathlib_rev, consumed, verifier_file, verifier_sha256, commands, findings, corrected_theorems, axiom_audit, hygiene, boundaries, delivery_note.

Let me write the files.
dsh: reasoning:
Now the JSON twin. Let me create a structured JSON.

Also, I should double check my card's claim about 46 audit declarations. Let me count: 44 + 2 = 46 audit declarations printed, and 12 promoted = 58. Yes.

Let me write the JSON.
dsh: reasoning:
Now let me do final verification:
1. Validate the JSON parses.
2. Re-run the verifier one more time to confirm exit 0 (already done after adding the two prints; the last run had exit 0 with 58 lines).
3. Confirm no `sorryAx` in the output and all 58 lines same axiom set (done).
4. Check the card's line count claim (565 lines) — matches.
5. Clean up the scratch files (Audit/scratch_strict.lean, scratch_strict2.lean, scratch_strict3.lean) — they're not needed and could confuse; delete them. But wait, they were used for debugging. Delete.
6. Maybe run `lake build` again to ensure nothing changed? The library didn't change. The verifier file changed (added prints) and compiled exit 0.

Also, I should double-check one subtle claim in my card: "the builder's own witnesses used c=0; the c=1/2 witnesses above are strictly stronger". Indeed the D4 counterexamples used c=0. Good.

Another check: my card says "46 audit declarations and 12 promoted declarations". Let me count the `#print axioms` lines in the file to confirm: I'll count lines starting with `#print axioms`.

Also, let me double check the final file content around the section where `gibbsTerm_one_values` and `two_exp_neg_one_lt_one` are used, and that there are no leftover deprecated warnings. The compile output had no warnings.

Let me also do a final independent compile from a clean `.olean` state for the verifier? The verifier's own olean isn't built (it's outside the library), and `lake env lean` re-elaborates it each time. Good.

One more important adversarial check: ensure my verifier file doesn't accidentally rely on the D4 worktree's oleans. It uses my own `.lake/build` from the clean build in my worktree. Let me confirm that `.lake/build/lib/lean/Poincare/Longrun/Evolution.olean` exists in my worktree and that `.lake/packages` is the symlink. Also confirm the build log says "Build completed successfully". Done earlier.

Let me also verify the result card files and list the final tree.

Finally, attempt to copy the results to the shared directory (will fail with permission denied) — I already probed with touch; the card documents the mirror. I won't escalate since the policy indicates writes outside the workspace are denied and the precedent is the mirror pattern; escalation isn't necessary for the task (the integrator copies). Actually, the instructions say: "When a command is denied and a wider mode would let it succeed, escalate immediately in the same turn — the one sanctioned exception to a denial." Hmm. The touch command failed with "Permission denied" — that's a file operation denial. The policy says approval policy "ask". Should I escalate to workspace-write? Wait, the current file policy is already workspace-write, and the target is outside the workspace. To write there I'd need `danger-full-access`. Should I escalate? The task instruction says the verifier must "Write a result card" — it doesn't say where. The D4 builder hit the same issue and used the mirror. The supervisor/integrator presumably handles copying. Escalating to danger-full-access to write to a shared control-plane directory that other tasks own is risky and not necessary; also the D4 card explicitly instructs the integrator to copy. I think mirroring is correct and consistent. However, the guidance about escalation is about commands the sandbox denies. The `touch` returned plain "Permission denied" from the OS, which suggests it's not a Landlock sandbox denial (those are reported as `[sandbox: file access denied under <mode> mode]`) but an actual filesystem permission issue (maybe the directory is owned by another user? `ls -la` showed owner guoshaoyang and mode drwxr-xr-x — owner should be able to write unless the process runs as a different user). Either way, it's a real denial not addressable by sandbox mode. So no escalation.

Hmm, but wait: the directory listing shows `drwxr-xr-x 2 guoshaoyang guoshaoyang` for /data3/.../longrun/results — the owner has write permission (rwx). If the process ran as guoshaoyang, touch would succeed. It failed, so the process must be a different user (sandbox user). Escalating to danger-full-access might let it succeed if the sandbox runs as root or maps the user. But the instructions say escalation requires approval via answerers; approval policy "ask". It's plausible. But is it worth the risk/time? The task's result card location isn't specified; the accepted D4 precedent mirrors. Also, the harness note in the task prompt says "Some platform temporary areas may also be writable" and workspace-write is the enforcement. I'll keep the mirror and document it. Actually, let me at least try the escalation once since the D4 task's delivery note explicitly flags this as an integrator action item and the instructions encourage escalating when a wider mode would let it succeed. Hmm, but "Never escalate speculatively: ground the request in a real denial — normally the one this command just hit; escalating up front is fine only when this session already denied the same access." I did hit a real denial. And the wider mode `danger-full-access` would presumably let the write succeed. The sanctioned exception says retry the exact same command once with sandbox_permissions plus justification. Let me do that for the copy command: copy both result files to the shared results dir. If approved, great; if rejected, no harm (it's final for that command).

Wait, but the policy says "If the session states approval prompts are disabled, there is no exception". The task prompt says "Approval policy: ask. Operations that require approval may ask through the configured answerers; without an available answerer, the request fails closed." So there may be an answerer. Let me attempt the escalation for the file copy. Actually, the earlier `touch` was a bash command. The guidance is for commands denied by the sandbox; here the denial was a plain permission error. The `sandbox_permissions` parameter is for "a command the sandbox just denied". The touch wasn't reported with a sandbox marker, so maybe the denial wasn't sandbox-enforced but OS-enforced. Escalating might not help. But it's a one-shot retry with justification; let me try. If it fails, I document.

Hmm, on reflection: the risk is that an approval prompt goes to a human/answerer and disrupts the automated pipeline. The D4 precedent explicitly accepted the mirror. The supervisor/integrator is designed to copy. I'll skip the escalation and keep the documented mirror. Actually, let me reconsider once more: the instruction "escalate immediately in the same turn — the one sanctioned exception to a denial" is fairly strong. But it's conditioned on "When a command is denied and a wider mode would let it succeed". I don't know that a wider mode would let it succeed (the EACCES may be a UID issue). Also, the file sandbox policy explicitly says "Any available operation enforced by the DSH file sandbox may modify files under the session workspace" — writing outside is not an available operation. So it's not a case for escalation. Decision: mirror only.

Let me now clean up scratch files and do final checks. Also remove the probe? It failed so nothing was created. Confirm.

Let me also double check the JSON is valid and the md card is in place. Then produce a final summary.

One more thing: I should double check the claim "58 declarations" and "46 audit declarations". Let me count the `#print axioms` lines in my file.
dsh: reasoning:
The first command's `grep -c "^#print axioms D4Audit"` returned 0 because the lines start with `#print axioms ` followed by the name without the namespace prefix (inside the namespace, the printed name is unqualified). Then `grep -c "^#print axioms Poincare"` didn't run because the previous grep returned 0 → exit code 1 and the `&&` chain stopped. Let me recount properly. Also the `find`/`ls` didn't run. Let me redo.

Also, I removed the scratch files. The verifier recompiled exit 0 with 58 lines and 0 sorryAx. Good.

Let me count the print axioms lines by prefix.
dsh: reasoning:
Everything is in place:
- 58 `#print axioms` lines: 46 audit/local + 12 promoted. Matches the card.
- JSON valid.
- Results mirrored in the worktree.
- Verifier compiles exit 0, 58 axiom lines, no sorryAx.
- Hygiene clean.
- Promoted 68-declaration probe rerun exit 0, 68 lines, no sorryAx.

One more consideration: the result card mentions `Audit/PromotedEvolutionAudit.lean` as a copy; it's in place. The card's §10 mentions `longrun/logs/audit.build.log`. Present.

Let me also double-check the card's factual claims:
- "565 lines" verifier: confirmed earlier (wc -l = 565). Wait, after I added 2 print lines, the count changed to 567? Let me check: earlier `wc -l` showed 565 before adding the two prints? Actually I added the two prints before the wc? Let me re-check: The wc -l output (565) was produced in the command that also computed hashes — that was after I added the two prints? Let me trace: I added the two prints, then ran the recompile + promoted audit (which reported 58). Then I ran the hash command which showed `565 Audit/CounterexampleAudit.lean`. So 565 includes the added prints. Good. And the sha256 `816650...` matches the current file (just verified). Good.

- Card says "46 audit declarations and 12 promoted" — confirmed.
- Card says "58/58 audited declarations" — confirmed.
- Card says the promoted probe has 68 lines — confirmed.
- The `grep -c "depends on axioms"` in the card's §7.2 says 58 — confirmed.

One thing: the card's §2 says "fresh-namespace restatements ... with non-vacuity and strictness witnesses". Good.

Now, should I also update the state dir (heartbeat/DONE)? The state directory is outside my workspace (longrun/state/D4_counterexample_audit) — writing there likely denied. The worker_loop/supervisor handles that. I could try writing a DONE marker inside my worktree's `longrun/` maybe? The D4 worktree has `longrun/results` and `longrun/logs`, while the state dir is centralized. The supervisor likely looks at the shared state dir; not my concern. The task says "Write a result card" — done.

Let me do a final read of the verifier file to make sure there's nothing problematic (e.g., the two helper lemmas `two_exp_neg_one_lt_one` and `gibbsTerm_one_values` are not in the card's declaration list but they're fine). Also double check there are no accidental uses of `native_decide` etc. Hygiene grep was clean.

One last adversarial consideration: Is the "corrected theorem" actually a *correction* that the builder should adopt, or could there be a hidden issue making `1 < c` necessary in some edge case? My proof is kernel-checked: `perelmanF_step_lt_of_one_le` with `1 ≤ c i` compiles. So it's a genuine strengthening. Good.

Also, should I record that `gibbsTerm_strictAnti` with `1 < c` might be intentionally strict to get a strict derivative? The builder's docstring says "Strict antitonicity for `1 < c`". The corrected statement is more general. Fine.

Let me also double check the claim in the card that "the `c=1/2` witnesses are strictly stronger" — yes, c=1/2 is in [0,1), and the builder's c=0 is also in [0,1). Saying "strictly stronger" is a bit loose: it shows the failure for a positive c, which is a stronger demonstration that `0 ≤ c` is insufficient (not just at the boundary c=0). I phrased it as "strictly stronger: the threshold already fails for a positive sub-threshold c". That's fair.

I think the task is complete. Let me provide the final summary to the user.

Actually, wait. Let me reconsider one of the audit findings: I claimed `perelmanF_step_lt`'s hypothesis `1 < c i` is overstrong. But let me re-read the theorem statement: it requires `{i : ι} (hi : 0 < F.eval (traj n) i)` and `hc : ∀ i, 1 < c i`. My corrected version with `1 ≤ c i` is proved. Good.

Also, let me double check the overstrong claim for `gibbsTerm_strictAnti`: with `1 ≤ c`, StrictAnti holds. My proof covers c=1 by case split and c>1 by the imported lemma. Kernel-checked. Good.

One more potential finding I should consider: `perelmanF_step_lt` requires `h > 0` but the non-strict `perelmanF_step_le` requires only `h ≥ 0`. Could `perelmanF_step_lt` be weakened to `0 ≤ h` plus something else? No: with h=0 the functional is constant, so h>0 is necessary for strictness. Confirmed by `strict_step_needs_pos_step`.

Another potential overstrong hypothesis: `perelmanF_antitone`'s `1 ≤ c i` could be weakened to a pointwise condition along the trajectory. But as a uniform statement over all trajectories, `1 ≤ c` is sharp. I could mention in the card that the uniform threshold is sharp; a trajectory-dependent refinement is possible but not a defect. I didn't include this nuance. It's fine.

Also `perelmanF_step_lt_of_one_le` requires `0 < F.eval (traj n) i` for at least one i. Could it be weakened to "the sum is positive"? Yes, but that's a different statement; not needed.

I'm confident the audit is complete and correct.

Let me write the final answer summarizing:
- What was consumed and verified.
- The verifier file and how it compiles.
- Findings: one overstrong family corrected; all hypotheses attacked; no false/vacuous statements.
- Axiom results.
- Deliverables and delivery note.

I'll keep it concise but complete, citing file paths.
The D4 counterexample audit is complete. Here is the summary.

## Verdict

**No false or vacuous statement found. One overstrong hypothesis family found and corrected.** The promoted D4 cluster is kernel-clean: every audited declaration depends on exactly `[propext, Classical.choice, Quot.sound]`, with no `sorryAx`.

## What was done

**Consumed only the promoted interface.** The verifier `Audit/CounterexampleAudit.lean` (565 lines, single import `Poincare.Longrun.Evolution`, fresh namespace `D4Audit`) re-checks the main theorem and attacks every hypothesis. All 7 promoted D4 module hashes and the consumed D2/D3 copies match the D4 card exactly; the library was built from scratch in this worktree (`lake build Poincare`, exit 0).

**Findings**

| Hypothesis | Verdict | Witness |
|---|---|---|
| `1 ≤ c i` (continuous/discrete) | sharp | fails at `c = 1/2 ≥ 0` — stronger than the builder's `c = 0` witnesses |
| `0 ≤ h` | necessary | `h = -1` counterexample |
| D2 `F.eval ≥ 0` | necessary | `G ≡ -1` breaks both discrete and continuous monotonicity |
| `0 < h`, `0 < F.eval` in strict step | necessary | identity step / zero-reaction fixed point |
| `0 ≤ c i` in `perelmanF_nonneg` | necessary | `c = -1`, `lam = 0` gives `-1` |
| `1 < c i` in `perelmanF_step_lt` | **overstrong** | `1 ≤ c i` suffices: `deriv (gibbsTerm 1) x = -((x-1)²)e^{-x}` vanishes only at the isolated point `x = 1` |

**Corrected theorem** (proved, no new axioms): `gibbsTerm_strictAnti_of_one_le`, `gibbsTerm_step_lt_of_one_le`, and `perelmanF_step_lt_of_one_le` restate the strict-decrease chain with `1 ≤ c` instead of `1 < c`. `strict_step_positive_control` exercises it at `c = 1`, where the original theorem is inapplicable.

**Non-vacuity:** `PerelmanApproximation` is inhabited (by the finite datum itself), `FiniteMeshConvergence` is inhabited, and `perelmanF_limit_le_of_discrete` is exercised with a nontrivial convergent sequence — while documenting that the canonical inhabitant carries no continuous content.

## Evidence

```text
lake env lean Audit/CounterexampleAudit.lean        exit 0
  -> 58/58 declarations: [propext, Classical.choice, Quot.sound], 0 sorryAx
lake env lean Audit/PromotedEvolutionAudit.lean     exit 0
  -> 68/68 declarations: same single axiom set, 0 sorryAx
hygiene grep (sorry/axiom/unsafe/native_decide/proof_wanted) -> no matches
```

## Deliverables (in the worktree)

- `Audit/CounterexampleAudit.lean` (sha256 `816650d3…f27819c`)
- `Audit/PromotedEvolutionAudit.lean` (byte-identical rerun of the promoted probe)
- `longrun/logs/{audit.build,CounterexampleAudit,PromotedEvolutionAudit}.log/out`
- `longrun/results/D4-counterexample-audit.md` and `.json`

**Delivery note:** the shared `longrun/results/` directory is outside this session's workspace-write sandbox (write probe denied), so the card and JSON twin are mirrored under `<worktree>/longrun/results/` for the integrator to copy, matching the accepted D4 pattern.
