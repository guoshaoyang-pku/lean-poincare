dsh: reasoning:
No checkpoint.json yet in this worktree. Let me look at the worktree structure: release/, longrun/, docs/, input/, tools/, tmp/.

Note: the task says worktree is /data/home/guoshaoyang/... but the actual sandbox workspace is /data3/guoshaoyang/... They're likely symlinked. Let me check.

Let me explore the release directory to find D13 sources.
dsh: reasoning:
Same directory. Now let's find D13 sources. The headline declarations: SmoothOverlapAtlas.globalWeightedIBP_of_cover_partial_ae, OverlapAtlas.halfSpaceAtlas_weightedIBP_unconditional, halfSpaceAtlas_greenIdentity, halfSpaceAtlas_laplacianIntegralZero, dilationAtlasTwo_weightedIBP_via_pou, manifoldWeightedIBP_of_atlasData, HeatKernelBridge.finiteLifetimeEntropyBridge_gaussian, HeatKernelBridge.monotoneOn_F_gaussian.

Let me grep for these.
dsh: reasoning:
The D13 declarations aren't in this worktree's release yet. The task says "Read the canonical DAG, relevant D10-D13 sources and prior cards." So I need to fetch the D13 sources from the main repo (upstream worktrees) — read-only. Let me look at the main repo at /data3/guoshaoyang/workdir/lean_poincare.

Let me check what's in the parent directories.
dsh: reasoning:
The D13 sources are in worktrees: D13-manifold-ibp-volume-form, D13-heatkernel-bridge-d10-d7, etc. The "D13 card" likely refers to a specific task card. Let me look at the D13 worktrees: D13-manifold-ibp-volume-form and D13-heatkernel-bridge-d10-d7.

Let me check the queue for the D13 card, and find the source files with the headline declarations.
dsh: reasoning:
There's a `worktrees/leaders/L4-geometric-critical-path` which appears to be the leader worktree with the D13 sources (including `release/Poincare/L4/ManifoldIBP/D13HeadlineAudit.lean`). Let me inspect that. Also the D13 card probably lives in the leader's results or in longrun/prompts.

Let me find the D13 card with "expanded hypotheses" and "blockers closed" claims.
dsh: reasoning:
Let me read my own prompt file (prompts/L4-child-d13-semantic-audit.md) to understand exact acceptance criteria, and read the D13 cards (D13-manifold-ibp-volume-form.md, D13-heatkernel-bridge-d10-d7.md) for the "expanded hypotheses" and "blockers closed" claims.

Also check state/L4-child-d13-semantic-audit for prior state.
dsh: reasoning:
Now let me read the D13 cards: D13-manifold-ibp-volume-form.md and D13-heatkernel-bridge-d10-d7.md from prompts, plus the leader's longrun results.

Let me first read the two D13 cards (prompts), and locate the source files for the 8 headline declarations.

Headline declarations:
1. SmoothOverlapAtlas.globalWeightedIBP_of_cover_partial_ae — POUConstruction.lean:60
2. OverlapAtlas.halfSpaceAtlas_weightedIBP_unconditional
3. halfSpaceAtlas_greenIdentity
4. halfSpaceAtlas_laplacianIntegralZero
5. dilationAtlasTwo_weightedIBP_via_pou
6. manifoldWeightedIBP_of_atlasData
7. HeatKernelBridge.finiteLifetimeEntropyBridge_gaussian
8. HeatKernelBridge.monotoneOn_F_gaussian

Let me find their locations.
dsh: reasoning:
Let me now read the D13 card results (longrun/results/D13-manifold-ibp-volume-form.md), focusing on "expanded_hypotheses" and "exact_blockers_closed".
dsh: reasoning:
Now let me look at the actual source file for the 8 headlines. Let me read POUConstruction.lean, PartialChartModelPOU.lean, SmoothAtlasModel.lean, Transfer.lean, HeatKernelBridge.lean.

Also I need to check whether the worktree release/ has these D13 files — no, my worktree's release is at an earlier state (D6-ish). I need to build an audit artifact in my own worktree. The plan:

1. Copy relevant D13 sources into my worktree? No source edits means I should not edit the D13 sources — but building an audit probe requires importing them. Options:
   - Create a symlink or copy of the leader's release Poincare tree into my worktree's third_party/reference (read-only reference), then create my own audit Lean file that imports those modules and does #check / #print axioms / instantiation.
   - Or use the leader worktree's release directly as a build root (read-only) but I can't write .lake build artifacts there (that would modify other worktrees — not allowed).
   
   Better: my worktree has `release/` with lakefile.toml, lean-toolchain, and Poincare tree. I can create a new package in my worktree, e.g. `audit/` with its own lakefile referencing mathlib from the pinned toolchain, and copy (not modify) the D13 sources into it. But copying is a lot; and the D13 sources depend on the rest of the Poincare tree (Longrun, Stage6...).

   Let me check size of the leader's release/Poincare tree and whether it's self-contained. Actually simplest: create a build directory in my worktree that contains a copy of the leader's `release/` (or symlink to it) plus my audit module. Symlinks might be fine for reading; lake build outputs to my own .lake. But if I point lakefile's `srcDir` at a symlinked Poincare dir, lake will write .lake inside the symlink target? Let's check the lakefile.

Let me first look at the my worktree release structure and leader release structure & sizes.

Time budget is 4 hours. Main deliverable: JSON ledger + MD report with per-declaration verdicts, evidence, interface fields, defects. Non-vacuity: kernel-checked instantiation per headline (the D13 model atlases) — that means I should compile a Lean file in my worktree that instantiates each headline from the D13 models, and print axioms.

Let me plan concretely:
- Step 1: read all 8 declarations fully with line numbers, plus the interface structures (ManifoldAtlasData etc.).
- Step 2: set up a build in my worktree: copy leader's release into my worktree? My release/ already has lakefile.toml etc. Let me check if my release/Poincare is a git-ignored or sparse checkout; the leader has D13 files at release/Poincare/D13. My worktree release has no D13 dir. Since "No source edits" applies to the audited sources — for the audit I need them available. I think copying (read-only import) the leader's release tree into my worktree as `audit_ref/` is acceptable and doesn't modify other worktrees. Actually simpler: my worktree's release/ is the build root; I can add the D13 subtree by copying the leader's release/Poincare/D13 into release/Poincare/D13 (adding files to my own worktree only). But that's an edit to my worktree's release. Task says "Build from release/ with the pinned toolchain" — so building within my worktree's release is expected. Hmm, but "no source edits; read-only" — that refers to the audited D13 sources; I should not modify them. Adding them to my worktree would be a copy. To preserve hashes, I can copy and verify sha256 matches the leader's.

Actually wait. Maybe the worktree's release has been re-synced... Let me check git status of my worktree. Also check if there's a manifest with source hashes.

Let me look at the structure and decide. Let me first inspect the leader's lakefile and my lakefile.
dsh: reasoning:
The leader's release is 2.4G (with .lake build). The leader release has a build cache. I need to build my audit in my own worktree, but building 499 lean files from scratch would take hours. Options:

1. Use the leader's `.lake/build` cache by copying/symlinking: if I copy the leader's release tree (2.4G) into my worktree it's heavy but doable? My disk quota? Let me check available disk space. Actually maybe I can point my lake build's `LEAN_PATH` at the leader's build lib dir and compile just my audit probe file against it, writing olean outputs to my own worktree. That avoids copying and avoids modifying the leader's tree. `lake env lean` with `LEAN_PATH` including the leader's `.lake/build/lib/lean` — that works if all dependencies' oleans are there. I can compile my audit file with `lake env lean` from my worktree (so mathlib is available) and set LEAN_PATH to include the leader's build lib. That's read-only on the leader's tree (only reading oleans).

Actually simpler: the leader's release `.lake/build/lib/lean` contains oleans for Poincare.* modules. I can write my audit file in my own worktree, e.g. `release/AuditD13/SemanticAudit.lean`, and compile it with:
```
cd release && LEAN_PATH=/path/to/leader/release/.lake/build/lib/lean lake env lean --root=... AuditD13/SemanticAudit.lean
```
Note: importing `Poincare.D13...` requires the module search path. Setting LEAN_PATH works with `lean` directly. Also mathlib is in my own release/.lake/packages? Let me check my worktree release/.lake: it has `build`, `config`, `packages`? Earlier `ls release/.lake` for mine... let me check. My release dir listing showed `.lake` with 4096 size. Let's check whether my worktree has mathlib built or can build.

Also check disk space and whether mathlib oleans exist somewhere shared.

Let me check:
- my release/.lake contents
- leader release/.lake/packages
- leader's build lib.
dsh: reasoning:
My worktree's `.lake/packages` is a symlink to D6_weekly_release/release/.lake/packages — so mathlib is available. And the leader's build lib has oleans for Poincare.D13 etc.

Plan: compile my audit probes with `LEAN_PATH` pointing at the leader's build lib plus my worktree's mathlib packages build lib. Let me test a trivial `#check` first.

Let me write a test file in my worktree tmp or release/AuditD13/. I'll use `lake env lean` from my release dir and set LEAN_PATH.

Actually, `lake env lean` sets LEAN_PATH to include `release/.lake/build/lib/lean` (empty) and package build libs. If I prepend the leader's lib, the import will resolve. Let me test.

First, let me verify the leader's build lib contains a compiled olean for the headline modules, e.g. Poincare/D13/ManifoldIBP/POUConstruction.olean.
dsh: reasoning:
Excellent — I can compile against the leader's oleans read-only, writing my audit files in my own worktree. 

Now let me do a thorough read of the sources. Let me set up a todo list and then read the 8 declarations in full with surrounding structure.

Key files:
1. release/Poincare/D13/ManifoldIBP/POUConstruction.lean (line 60) — SmoothOverlapAtlas.globalWeightedIBP_of_cover_partial_ae
2. release/Poincare/D13/ManifoldIBP/PartialChartModelPOU.lean (192, 484, 629) — halfSpaceAtlas_weightedIBP_unconditional, halfSpaceAtlas_greenIdentity, halfSpaceAtlas_laplacianIntegralZero
3. release/Poincare/D13/ManifoldIBP/SmoothAtlasModel.lean (129) — dilationAtlasTwo_weightedIBP_via_pou
4. release/Poincare/D13/ManifoldIBP/Transfer.lean (131) — manifoldWeightedIBP_of_atlasData
5. release/Poincare/D13/HeatKernelBridge.lean (412, 465) — finiteLifetimeEntropyBridge_gaussian, monotoneOn_F_gaussian

Also relevant structures: ManifoldAtlasData (Transfer.lean), SmoothOverlapAtlas (SmoothAtlas.lean?), ChartMetric, WeightedIBPStatement, FiniteLifetimeEntropyBridge.

Let me read these files. Start with POUConstruction.lean.
dsh: reasoning:
The statement is fully visible. Key things to check:
- htrans: ∀ i j y, A.chart j y ∈ A.chart i '' A.source i → A.transition i j y ∈ A.source i — this is coherence.
- hcover: finite chart cover of base chart image.
- hproper: preimage of compact under transition is compact.
- hbd: frontier of source has measure zero.
- Measurability hypotheses: hf, hv, hDu, hGuv — note that hf/hv are not obviously needed? hv used in measurable_pouPiece; hf used in integrability.
- hfc/huc/hvc: ContDiff ℝ 2 for ALL i (including empty sources, trivial).
- hvcc: HasCompactSupport (v ∘ chart b).
- htsupp_b: tsupport (v ∘ chart b) ⊆ A.source b.
- hD/hG: operator identifications on sources — these relate Du to the laplacian and Guv to gradInnerInverse. Note: hD in the statement above says `Du (A.chart i y) = ...` for `y ∈ A.source i`. Important: this is an identification of the given functions, not an assumption that the conclusion holds. But there is a subtlety: at points where y ∈ source i but chart i y equals chart b z for some z, coherence ensures consistency. Actually hD quantifies over all i, y ∈ source i.

Potential semantic issues:
1. hvsupp and hGuvsupp: v and Guv vanish outside chart b image. Fine.
2. hD/hG are "operator identifications" — they assert that Du/Guv are the chart expressions of the Laplacian/pairing. These are hypotheses that, if taken over all i, could be contradictory if charts overlap inconsistently — but atlas coherence (htrans) ensures consistency? We should check whether hD/hG are conclusion-equivalent: The conclusion states ∫ Du * v dμ = -∫ Guv dμ. hD/hG define Du and Guv as specific expressions. So it's not conclusion-equivalent; it's an identification of the integrands. Still, it's a "hidden interface": the theorem is conditional on these pointwise identities (which in a genuine manifold setting hold by definition of Δ_F and ⟨∇u,∇v⟩). Since the theorem couples Du to the Laplacian of u and Guv to gradInnerInverse, the IBP content is real: IBP of laplacian vs gradInnerInverse is the D12 chart theorem.

3. Potential vacuity: are there any atlases satisfying these hypotheses with nonempty support? That's what the non-vacuity check does. The D13 model atlases: dilationAtlasTwo_weightedIBP_via_pou is itself one headline; for globalWeightedIBP_of_cover_partial_ae, the POUConstruction file has instantiations at lines 345, 465. Let me read those.

4. Domains: The heat kernel headlines have hypotheses `t1 < τ0` and presumably integrals over (0, τ0) etc. Need to check the domain conventions ((0,T) vs (0,T]) and whether the statement is vacuous for τ0 ≤ 0.

Let me now read the rest of POUConstruction (lines 140-482) focusing on the instantiations.
dsh: reasoning:
Good — `halfSpaceAtlas_weightedIBP_of_cover` instantiates `globalWeightedIBP_of_cover_partial_ae`, so non-vacuity of the latter is witnessed by the half-space atlas (given the latter is itself non-vacuous — need a ChartMetric instance; G is a parameter! Note: the instantiations are *conditional on G : ChartMetric (n+1)* but don't require any hypotheses on G beyond the structure fields... ChartMetric is a structure with fields; need an inhabited instance to be truly non-vacuous. Does ChartMetric have an instance? Let's check the structure and whether there is a canonical example (e.g. euclideanMetric).

Also note that `halfSpaceAtlas_weightedIBP_of_cover` is proved *using* the headline, so it's a legit non-vacuity witness: it shows the hypotheses of the general theorem are satisfiable by a concrete atlas (hsAtlas). But if ChartMetric is empty, then... Let's check.

Now let me read the remaining parts of the files. Let me read PartialChartModelPOU.lean around 100-260 (halfSpaceAtlas model definitions and unconditional theorem), 460-700 (green identity, laplacian integral zero).
dsh: reasoning:
Now the green identity and laplacian integral zero. Let me read lines 460-678.
dsh: reasoning:
Now let me read SmoothAtlasModel.lean (dilationAtlasTwo_weightedIBP_via_pou) and Transfer.lean (manifoldWeightedIBP_of_atlasData).
dsh: reasoning:
Very interesting. `manifoldWeightedIBP_of_atlasData` has interface fields:
- integral_decomp (B-D13-MANIFOLD-GLUING)
- weighted_laplacian_compat, grad_inner_compat (B-D13-OPERATOR-COMPAT)

These are fields of the structure — they're assumptions, not proved. The claim "conditional interface" is honest. Need to check:
- Is `drift i` used as the weight exponent: `Real.exp (-drift i x)` — so drift is the entropy function f.
- `ChartSumData.globalWeightedIBP` requires hd: ContDiff ℝ 2 (A.drift i) etc. and hvc: HasCompactSupport. Then hypothesis hg: Integrable (Δu * v) and hg' needed for the rewrites. Note the conclusion doesn't require integrability... but the hypotheses include both integrability statements — hg is needed for integral_decomp, hg' for gradInnerIntegral_eq_chartSum. Fine — not conclusion-equivalent but a fairly direct integrability assumption.

Interesting: In this transfer theorem, u and v are functions on M, and the hypotheses include chartwise ContDiff and HasCompactSupport of v in every chart. That is a strong hypothesis; with disjoint atlas it's fine.

Potential hidden issue: `ChartSumData.globalWeightedIBP` may itself be an interface or proved. Let me check ChartSum.lean.

Also there's the question of whether `ManifoldAtlasData` is inhabited — i.e., is the theorem vacuous because no ManifoldAtlasData exists? The card says "ManifoldAtlasData ... (inhabited with proofs for the models)". We should verify with a kernel-checked instance. There's probably `dilationAtlasTwoData` in GlobalMeasure.lean or similar, used by `dilationAtlasTwoData_weightedIBP`. Let me search.

Let me also check `ChartSumData` structure and `globalWeightedIBP`.

Now the HeatKernelBridge headlines. Let me read those.
dsh: reasoning:
Good. Now the HeatKernelBridge headlines. Let me read the file around lines 350-520, and the structure FiniteLifetimeEntropyBridge.
dsh: reasoning:
Now I need the `FiniteLifetimeEntropyBridge` structure and its fields/domains, and `monotoneOn_of_bridge`. Let me find them.
dsh: reasoning:
Let me read the FiniteLifetimeEntropyBridge structure and monotoneOn_of_bridge in CertificateOn.lean, plus EntropyData structure (FDissipation), especially domains.
dsh: reasoning:
Now let me check `EntropyData` structure and `FDissipation`, `WeightedCalculus`, `EntropyData.extra`, and the `F` definition. Also check whether the `regularity` field is on Icc; f_derivative on Ioo(0,t1) — note that monotoneOn_of_bridge's monotoneOn uses `monotoneOn_of_deriv_nonneg` on the interior of Icc, so it needs the derivative on (0,t1) which is Ioo a b — good.

One thing to check for the D13 heat headline: `finiteLifetimeEntropyBridge_gaussian` provides fields at all `t ∈ Ioo 0 t1`. But hypotheses in the `conjugate_measure_evolution` proof use `gaussian_conjugate_heat τ0 t` with conditions `lt_trans ht.1 (lt_trans ht.2 ht1)` — need to check.

Also `monotoneOn_F_gaussian (τ0 t1 : ℝ) (ht1 : t1 < τ0)`: Statement `MonotoneOn (fun s => (gaussEntropyData τ0 t1 ht1 s).F) (Icc 0 t1)`. Domain: requires `t1 < τ0`. If `t1 ≥ τ0`, no statement. Note `Icc 0 t1` could be empty if t1 < 0, but ht1 : t1 < τ0 doesn't force t1 ≥ 0. So for t1 < 0, Icc 0 t1 = ∅ and the claim is trivially true. Not vacuous overall since t1 ∈ (0, τ0) gives a genuine interval. The card claims F is nondecreasing on its lifetime.

Key check: is `hbound : ∀ t ∈ Icc 0 t1, (E t).F ≤ upperBound` with upperBound = 1/(τ0 - t1) valid given that t1 could be ≤ 0? In `monotoneOn_F_gaussian`:
```
refine monotoneOn_of_bridge ... (1 / (τ0 - t1)) ?_
intro t ht
rw [F_gaussEntropyData ht1 ht.2]
exact one_div_le_one_div_of_le (sub_pos.mpr ht1) (sub_le_sub_left ht.2 τ0)
```
`one_div_le_one_div_of_le` requires 0 < a and a ≤ b then 1/b ≤ 1/a. Here ht.2: t ≤ t1, so τ0 - t1 ≤ τ0 - t. `sub_le_sub_left ht.2 τ0 : τ0 - t1 ≤ τ0 - t`. Positivity: sub_pos.mpr ht1 : 0 < τ0 - t1. So 1/(τ0 - t) ≤ 1/(τ0 - t1). Good, even if t1 ≤ 0 (then Icc 0 t1 = ∅ so hbound vacuous). Fine.

Now, the critical semantic question for `finiteLifetimeEntropyBridge_gaussian`: the docstring says "All six fields are proved for the explicit backward Gaussian on the lifetime [0, t₁]; nothing is assumed." I need to verify:
- the fields hold given `ht1 : t1 < τ0`; if t1 is very negative, gaussTau is clamped... The `f_derivative` field needs `HasDerivAt (fun s => (E s).F) (FDissipation (E t)) t` at t ∈ Ioo 0 t1. If Ioo 0 t1 = ∅ when t1 ≤ 0, vacuous. If t1 > 0, then t ∈ (0, t1) and τ(t) = τ0 - t. Fine.

But here's a subtle issue: is `gaussEntropyData`'s `F` = 1/τ(t) or something else? Since it's an EntropyData structure with fields (F, FDissipation, ρ, τ...). The claim "F(t) = 1/τ(t)" and monotone. Let me read EntropyData.

Also, the key question: "model-vs-manifold overclaims" — the heat kernel bridge is over the Euclidean chart Vec 2, which is a model. The card classifies these as "entire I4 Gaussian package and its FiniteLifetimeEntropyBridge consumption" as proved unconditional. The task asks whether they are labeled as model vs manifold. The headline statements are model-level (Vec 2, Euclidean chart); the D13 card semantic_class puts "Gaussian family (I4)" under `model`. So consistent.

Now, let me check the FiniteLifetimeEntropyBridge structure semantic: it's a `Prop` structure whose fields are statements. Not an axiom. The headline `finiteLifetimeEntropyBridge_gaussian` constructs it. Fine.

Now let me check `f_derivative` for the gaussian: statement says "HasDerivAt ... (EntropyData.FDissipation (E t)) t" and the proof rewrites 1/(τ0-t)^2 = FDissipation using `FDissipation_gaussEntropyData ht1 ht.2.le`. Need to check that's a real proof and not circular. Let me examine the file and the definitions.

Let me read HeatKernelBridge.lean lines 1-340 to understand gaussEntropyData and the supporting lemmas. Actually let me focus on key definitions. Let me grep for `def gaussEntropyData`, `def gaussPotential`, `gaussTau`, and the EntropyData structure.
dsh: reasoning:
Let me read the EntropyData structure fields to know whether F and FDissipation are fields or defined. Also, importantly: the `EntropyData` structure comes from upstream (Longrun/Entropy) — a "D3" interface. Let me read Functional.lean lines 30-140.
dsh: reasoning:
Now, let me check the D13 heat card (D13-heatkernel-bridge-d10-d7) for "expanded hypotheses"/"blockers closed" claims, and read remaining pieces: SmoothAtlas.lean's SmoothOverlapAtlas structure, the `weight` definition (e^{-f}), the globalMeasure. Also `ChartMetric` fields (density etc.). And check the OverlapIBPData instantiation for ManifoldAtlasData.

Also I need to check for subtle statement issues:
1. `globalWeightedIBP_of_cover_partial_ae`: hD/hG are assumptions over `∀ i y, y ∈ A.source i`. Note: if `source i` is empty for some i, vacuous. Also `hcover : A.chart b '' A.source b ⊆ ⋃ j, ...` — this says the base chart image is covered by the chart images of `chartOf`. Since `j : ι` with `[Fintype ι]`, and ι could be empty? If ι is empty, `⋃ j, ...` = ∅, so hcover says chart b '' source b = ∅, then hvsupp forces v = 0 (v m ≠ 0 → m ∈ chart b '' source b = ∅), so v = 0, and the conclusion is 0 = -0. Is that vacuous? It's a legitimate degenerate case, but if ι is empty then `Fintype ι` with `IsEmpty ι`... `⋃ j : ι` is empty. Then conclusion trivial. That's fine, not a defect (the theorem is still true). But it shows the cover hypothesis is doing work: with nonempty v, hcover must be satisfiable.

Question: Is `hcover` conclusion-equivalent? No.

Potential defect candidate: the hypothesis `hvsupp : ∀ m, v m ≠ 0 → m ∈ A.chart b '' A.source b` and `htsupp_b : tsupport (v ∘ chart b) ⊆ A.source b`. Both are "support" hypotheses; htsupp_b implies hvsupp? Let's see: if v m ≠ 0, then m ∈ support v ⊆ tsupport v. m ∈ chart b '' source b means ∃ y ∈ source b, chart b y = m. Take y = invFunOn... Hmm, not automatic. Since A.chart b might not be injective on source b. Actually in SmoothOverlapAtlas, chart i is injective? Let me check the structure. If chart b is injective, then choose y = A.chart b ⁻¹' {m}... Needs m ∈ range. So hvsupp is a genuine additional support hypothesis but is implied by htsupp_b? Given the atlas structure: chart i is a parametrization of source i (chart i '' source i is the chart domain), and `readback`... Let me read the SmoothOverlapAtlas structure to see what's given.

Let me now also check the D13 heat card claims. Then I'll start designing the Lean audit probe.

Actually, let me now think about what the Lean audit probe should contain. Requirements:
- Per headline: kernel-checked instantiation (non-vacuity) + #print axioms.
- Ideally also adversarial checks: e.g., show that hypotheses are satisfiable (existential witness), and check for vacuity by exhibiting a non-trivial instantiation where the statement is not trivially true (e.g., both sides nonzero).

The D13 model atlases:
1. For `globalWeightedIBP_of_cover_partial_ae`: the half-space atlas hsAtlas instantiates it (via `halfSpaceAtlas_weightedIBP_of_cover`, already in POUConstruction.lean, but that's a proof using the headline). A stronger non-vacuity witness: instantiate with concrete f, u, v that are nonzero, e.g. n=0 (Vec 1), G = euclidean chart metric, f = 0, u = some bump, v = bump. Then the conclusion is a nontrivial statement; and we can evaluate that the integrals are equal and not both trivially zero. But proving non-triviality of integrals is hard. Alternative: exhibit that the identity holds for a concrete instance where each side is a nonzero integral? That's expensive. A standard approach: verify the hypothesis set is satisfiable, and separately verify `tsupport` nonempty etc. Maybe demonstrate that for a specific nonzero v the hypotheses hold; the fact that the conclusion is not `0=0` follows syntactically because there is a nonzero `v` with compact support and the atlas has proper transitions etc.

Actually stronger: we can instantiate the headline and then derive a *concrete* identity using a v whose integral we can compute? Hard.

Alternatively, for non-vacuity I can check that the hypotheses are satisfiable with a nonzero v (which makes the statement contentful), plus the conclusion's two sides can be shown equal via the theorem; to show non-triviality, show that the left side is not forced to zero by e.g. `∫ Du * v` can be nonzero. Hmm. A cleaner non-vacuity demonstration: instantiate `halfSpaceAtlas_weightedIBP_unconditional` with f = 0, u = 1, v = a bump with ∫ v > 0. Then LHS = ∫ driftLaplacian 0 1 * v = 0, RHS = -∫ gradInnerInverse 1 v = -∫ 0 = 0. Both zero (since u constant, grad = 0). Not great.

Take u = x₀ (first coordinate) and v = bump: then gradInnerInverse u v = ⟨∇u, ∇v⟩ = ∂₀v. ∫ ∂₀ v = 0 for compactly supported v (integral of a partial derivative over ℝⁿ is zero) — consistent. LHS: Δ_F u * v with f = 0: Δu = 0 so LHS = 0, and RHS = -∫ ∂₀v = 0. So still 0=0 but through a nontrivial cancellation.

Better: use u with nonzero Laplacian, e.g. u = radSq (|x|²), f = 0, v = bump. Then LHS = ∫ Δu v = 2(n+1)∫v ≠ 0; RHS = -∫⟨∇u,∇v⟩. Nonzero. Proving that identity in Lean from the headline would require computing those integrals: ∫ Δu v = -∫⟨∇u,∇v⟩ is exactly the conclusion. Actually we can *use the headline* to prove it and then show both sides are equal to a computable value. Hmm, but the values are nontrivial integrals.

Alternative approach to non-vacuity that is standard and cheap: show the hypotheses are satisfiable AND the conclusion is not `0 = 0` by exhibiting a case where the right side is evaluated to a nonzero number? We can pick v and u so that gradInnerInverse u v has a known integral. E.g., u = radSq, v = gaussDensity? Not compactly supported. Hmm.

Actually maybe a cleaner route: there might already exist in D13 an evaluation lemma for a concrete instance (like the D13 `NegControl` or `StatementAudit` files). Let me look at the existing D13 audit/negcontrol files from the critical path review and cross-audit — those may already contain non-vacuity checks that I can reuse/verify. But my task is an *independent* semantic audit, so I should build my own probe but can reuse observations.

For the purposes of this audit, "Verify non-vacuity by exhibiting at least one kernel-checked instantiation per headline (the D13 model atlases)" — the deliverable says the instantiation should be the D13 model atlases. So:
- `globalWeightedIBP_of_cover_partial_ae`: instantiate with `hsAtlas` (half-space atlas) — kernel-checked application (this is exactly `halfSpaceAtlas_weightedIBP_of_cover`). I can write my own version in my probe with `#print axioms` to confirm no sorry.
- `halfSpaceAtlas_weightedIBP_unconditional`: instantiate at a concrete G. Need a ChartMetric inhabitant. Check `euclideanChartMetric`. Then f, u, v concrete: f = gaussPotential? Let's instantiate with f = 0 (ContDiff), u = a bump or 0, v = a bump with compact support in {y 0 < 1}. Even if v = 0 makes the conclusion 0=0, it's a legitimate instantiation but *vacuous* in the sense that it doesn't show content. Better: v = a specific nonzero bump. The existence of a nonzero bump is provable via `exists_contDiff_bump`; I can obtain v and vc and tsupport and then apply the theorem. Since the theorem's conclusion is an equation between integrals, and v is nonzero, the statement has content. That's a reasonable non-vacuity check: hypotheses satisfiable with nonzero test function, and the instantiated conclusion is a genuine (not syntactically trivial) identity.

To make it even stronger, I can *also* prove that the two sides are equal to specific numbers in a special case, e.g. choose u and v with disjoint support so both sides vanish? That doesn't demonstrate non-triviality.

Alternative: choose the concrete case where the equality can be reduced to a 1-D known integral: n = 0 (Vec 1), f = 0, u = fun _ => y 0 (i.e. first coordinate), v = deriv of a bump? Hmm, ⟨∇u,∇v⟩ = v' and Δu = 0; LHS = 0, RHS = -∫ v' = 0 (v compactly supported). So 0 = 0 but the RHS computation is nontrivial... Actually here the conclusion is literally `0 = -0`? ∫ v' over ℝ = 0 is a theorem (integral of derivative of compactly supported function). The identity is contentful but both sides zero.

I think the practical, honest approach: the non-vacuity witness is (a) hypotheses satisfiable with a nonzero test function (so not `v=0` degenerate), (b) the conclusion instantiated is a nontrivial equality of integrals that is *not* provable by `rfl`/`ring` (evidence: `#check` shows it's a real integral identity), (c) where feasible, show one concrete numerical instance with both sides computable and the equality non-trivial.

Actually here's a cleaner idea for a genuinely nontrivial numeric check: use the Gaussian identity? No.

Let me consider: choose u = radSq, f = 0, and v = a bump; then LHS = ∫ Δu · v = 2·(n+1)·∫v, RHS = -∫⟨∇u,∇v⟩ = -2∫ x·∇v. Both are computable if we know ∫ x·∇v = -(n+1)∫v (divergence theorem). So both equal 2(n+1)∫v. In Lean, proving RHS = 2(n+1)∫v requires the same IBP... circular.

Better numeric approach: pick a specific compactly supported v where ∫⟨∇u,∇v⟩ can be computed as a 1-D integral, e.g. u = y 0 (first coordinate), v = bump depending only on y 0 with ∫ v' = 0. Hmm.

What about u = fun y => y 0, v = fun y => y 0 * χ(y) where χ is a bump? Then Δu = 0 (f=0), LHS = 0. RHS = -∫ ∂₀(y₀χ) = -∫(χ + y₀ ∂₀χ). And 0 = ∫∂₀(y₀χ) = ∫χ + ∫ y₀∂₀χ. So the identity is again reduced to a general fact.

I think for audit purposes, the key non-vacuity claim is: the hypotheses are satisfiable with nonzero data, and one can *derive a number* from the theorem. To make it more convincing, I can prove a *corollary* of the headline where both sides are identified with the same simple expression by independent means — but that's essentially re-proving.

Actually, maybe I can produce stronger evidence of non-vacuity in a different way: use the theorem to derive a *concrete* inequality or equality that is false if the theorem were vacuous. Hmm.

Alternative: demonstrate that the hypotheses are satisfiable AND the conclusion is not a tautology by showing there exists an instantiation where the LHS is nonzero and the identity thus forces RHS nonzero. If I can prove "LHS ≠ 0" for a concrete instance, then the theorem's conclusion is contentful. E.g. f = 0, u = radSq, v = χ bump with χ ≥ 0 and ∫χ > 0, then LHS = ∫ 2(n+1)χ > 0. Proving ∫Δu·v > 0 requires computing Δu = 2(n+1) (a constant) — doable in Lean! Δ of radSq under Euclidean metric = 2(n+1)? For Vec (n+1) with euclideanChartMetric, laplacian radSq = 2(n+1). Then LHS = ∫ 2(n+1)·χ dμ where μ = e^{-f}ρ dx = ρ dx. Hmm, for the weighted measure with f = 0, weight = exp(-0)=1, so measure = globalMeasure volume = glued chart measure = volume on chart 0 image. The integral of the constant 2(n+1) times χ over the measure equals 2(n+1)∫χ dvolume restricted. That's positive iff ∫χ > 0. We could construct χ explicitly with positive integral. This is getting into real work but might be feasible.

Hmm, but time budget: 4 hours. The main deliverable is the audit ledger, not new proofs. Non-vacuity instantiations at the level of "hypotheses satisfiable with a nonzero test function; theorem applies; #print axioms shows no sorry" is likely what's expected. I'll aim for that, and additionally for the two heat headlines, the non-vacuity is much easier to make concrete because `monotoneOn_F_gaussian` fully evaluates F = 1/(τ0-t): I can derive a concrete numeric inequality, e.g. `1 ≤ F(3/4)` for τ0 = 1, t1 = 1/2 → 1/(1/4) = 4. That is a genuinely nontrivial numeric conclusion from the headline: `(gaussEntropyData 1 (1/2) h (1/4)).F = 4`. That's a great non-vacuity witness.

For `finiteLifetimeEntropyBridge_gaussian`, instantiate with τ0=1, t1=1/2: it's a `Prop`-valued structure instance; also derive `F` values.

For `manifoldWeightedIBP_of_atlasData`: instantiate with `dilationAtlasTwoData` — but I should do my *own* instantiation with concrete u, v. The existing `dilationAtlasTwoData_weightedIBP` uses interface hypotheses: hd, hu, hv, hvc, hg, hg'. I can instantiate with concrete u=0, v=0 trivially; better with nonzero data. Maybe I can use `dilationAtlasTwoData` with a bump v and u something, and prove the interface hypotheses are satisfiable. Since dilationAtlasTwoData has one chart (ι=1) with chart 0 = id and measure = ... Let me read OverlapIBPData.lean to see the instantiation and what the integrability hypotheses require.

Alternatively, for non-vacuity of `manifoldWeightedIBP_of_atlasData`, I can use the `dilationAtlasTwoData_weightedIBP` theorem itself as kernel-checked instantiation (it's an existing proof, and I can verify its axioms). Plus I could add my own concrete instantiation.

Let me now check the D13 heat card to see its claims. Also, let's check the leader's longrun results (L4-geometric-critical-path.md) for the D13 card claims about "expanded hypotheses" and "blockers closed".

Let me read the D13 heatkernel card JSON/md headlines.
dsh: reasoning:
Interesting: the D13-heatkernel card's `exact_blockers_closed` is empty! But our two heat headlines are in `Poincare/D13/HeatKernelBridge.lean` which is in the *leader* worktree (L4-geometric-critical-path), maybe part of the D13-manifold-ibp-volume-form task, not the heatkernel-bridge task. Indeed the D13-manifold-ibp card's exact_blockers_closed mentions "I4 (all five components) ... consumed by monotoneOn_F_gaussian, F_gauss_mono..." and "I4-residual defects". So the HeatKernelBridge.lean file is authored by the D13-manifold-ibp-volume-form track (its header says "Authors: D13-manifold-ibp-volume-form track").

So the "D13 card" referred to in my objective is most likely the D13-manifold-ibp-volume-form card (longrun/results/D13-manifold-ibp-volume-form.md/json), whose claims I already sampled: expanded_hypotheses list and exact_blockers_closed list. I should audit whether those claims are supported.

Let me now check which worktree is the canonical source of these files. There's worktrees/D13-manifold-ibp-volume-form/release/Poincare/D13/... and worktrees/leaders/L4-geometric-critical-path/release/Poincare/D13/... Are they identical? Let me hash both to determine which one built the oleans in the leader's .lake. Actually, my audit should pin exact source hashes. Let me compare the two trees.

Also I must check the leader's release is the integration target: my worktree's release is D6-era, so the D13 files live only in the leader and the D13 worktrees. The oleans I compile against are the leader's. I should verify that the leader's sources match the oleans (e.g., by rebuilding the specific modules? Rebuilding POUConstruction+PartialChartModelPOU etc. might take a while but is feasible — they're at the top of a dependency chain that's already built. Actually compiling those modules requires their dependencies' oleans, which exist. Compiling a handful of modules could take minutes each. But then my probe imports would use the freshly compiled oleans... but I can't write into the leader's .lake (shouldn't modify). I can copy the .lean sources into my own worktree and build them there — but that requires replicating the whole dependency tree's oleans. However, I can set LEAN_PATH to the leader's oleans and compile a copy of the D13 module in my worktree; Lean would produce oleans in my worktree for only those modules I compile. But the import graph: POUConstruction imports SmoothAtlasPartialAE etc. If I compile the copied POUConstruction.lean with LEAN_PATH including leader's lib, Lean will find the *leader's* olean for the imports and produce my own olean for POUConstruction (in the output path I choose). That works: `lean -o /my/out/Poincare/D13/.../POUConstruction.olean file.lean`? Actually `lean -o` produces a single olean; the module name is determined by the file's path/root. I'd need my own copy of the file placed at the same module path relative to my root, and use `-R` root option, output to a directory. Then I can verify that recompiling the copied source (byte-identical to the leader's original) yields the same declarations. This is a "recompile check" that guards against stale oleans. Given time constraints, maybe I can recompile 2-3 key modules: POUConstruction.lean, PartialChartModelPOU.lean, HeatKernelBridge.lean, Transfer.lean, SmoothAtlasModel.lean. Hmm, each may take 1-5 min. Let's see total budget: 4h. Let's first check whether leader's olean timestamps are newer than sources.

Actually a simpler and quite strong approach: copy the *entire* leader release source tree (Poincare + lakefile + toolchain + lake-manifest) into my worktree? Not needed — I can use `lake env lean` with `-o` output into my worktree. Let me check `lean --help` for `-o` and `-R`.

Plan for compile verification:
1. Copy the 5-8 audited source files (+ any files they redefine? no) into my worktree at `audit_src/Poincare/D13/...` preserving relative paths, with sha256 checks vs originals.
2. Compile each with `lean -R audit_src -o audit_build/lib/lean/...` using LEAN_PATH=leader lib + mathlib packages build lib. Wait — `-R` sets the root for module resolution so that imports of `Poincare.D13...` resolve to my copies; but then importing e.g. `Poincare.D13.ManifoldIBP.SmoothAtlasPartialAE` would resolve to my copy only if present in audit_src; otherwise LEAN_PATH. Actually with `-R root`, Lean resolves imports from the root directory first, then LEAN_PATH. If my root only contains the selected files, other imports fall back to the leader's oleans. Since my copies are byte-identical to leader sources, this is fine.

But wait: if I compile POUConstruction into my own olean path, and its module name is Poincare.D13.ManifoldIBP.POUConstruction, then my probe can import from my own build dir first. This gives an independent compile from source rather than trusting the leader's oleans. The cost: compiling each module from source. Let me test compile time on one.

Actually, `lake env lean` will also pass its own LEAN_PATH; I need to prepend. Let me test.

First, let me diff the leader's D13 tree vs the D13-manifold-ibp-volume-form worktree tree to see if identical.
dsh: reasoning:
The leader's D13 tree == D13-manifold-ibp-volume-form worktree's D13 tree except CriticalPathReview, so the canonical sources are the same. Sources are older than oleans (olean built after), good sign.

Now let me look at the remaining pieces I need:
1. `SmoothOverlapAtlas` structure (SmoothAtlas.lean) — fields, especially chart injectivity, measurable_readback, IsTotal, globalMeasure, weight, pouPiece, etc.
2. `hsAtlas` (PartialChartModel.lean) — the half-space model definitions.
3. `dilationAtlasTwoData` (OverlapIBPData.lean).
4. Check for any `sorry`/`axiom` in the audited files and in their dependency chain (at least the important ones).
5. Check `ChartMetric` structure.

Let me read SmoothAtlas.lean.
dsh: reasoning:
KEY FINDING CANDIDATE: `OverlapAtlas` structure (GlobalMeasure.lean:80) vs `SmoothOverlapAtlas` extends it. Let me read `OverlapAtlas` to see the invariant fields (e.g., `transition_chart`, `source` openness, `measurableSet_image`, etc.). Important: SmoothOverlapAtlas has `inj_chart` but OverlapAtlas may already have it. Let's read GlobalMeasure.lean lines 60-200 and OverlapIBP.lean weight definition.

Also I want to check `hsAtlas` definition and `dilationAtlasTwoData`.

Let me read the OverlapAtlas structure.
dsh: reasoning:
Important: OverlapAtlas's `cover` field says chart images cover M. `SmoothOverlapAtlas` adds injectivity globally.

Now `hsAtlas` (PartialChartModel.lean:529) is an `abbrev` for the half-space model. Note the half-space atlas has source 0 = {y0 < 1}, source 1 = {0 < y0}, and charts identity/dilation — these are NOT disjoint, so it's a genuinely overlapping atlas. But `cover`: ⋃ image = univ? source 0 ∪ source 1 = ℝ \ {y0 = 1} ∪ {y0 > 0}... source 0 = {y0<1} and source 1 = {0<y0}; union of images: chart 0 image = {y0<1}, chart 1 image = {0<y0} (dilation by 2 is a bijection of {0<y0}). Union = ℝ^{n+1} minus {y0 = 1}? No: if y0 ≥ 1 and y0 ≤ 0 impossible. If y0 = 1: not in {y0<1} and not in {0<y0}? y0=1 > 0 so yes it's in source 1 (image). So union = all of ℝ^{n+1}. Good.

Now let me look at the hsAtlas definition and halfSpaceAtlas_transition_coherence, plus check the "genuinely partial" claim: the atlas is not total? Wait, its `cover` covers univ, but "partial charts" means sources are proper subsets. The card says "genuinely partial half-space atlas halfSpaceAtlas (not total, null boundaries, proper transitions)". Hmm — "not total" likely means `IsTotal` (source = univ) fails, which is true since source 0 = {y0<1}.

Let me read PartialChartModel.lean around 480-560 for hsAtlas, and the relevant lemmas: hsChart_zero, hsSource_zero, hsSource_one, halfSpaceAtlas_transition_coherence, halfSpaceAtlas_transition_preimage_isCompact, halfSpaceAtlas_frontier_volume_zero.
dsh: reasoning:
Very good. Now let me check the D13-manifold-ibp card's claims in more detail, and importantly identify what "expanded hypotheses" claims might be overclaimed. Let me grep the card md for the 8 headlines.

Also I should check the remaining part of POUConstruction (lines 370-482), particularly the second instantiation (chartOne) and any non-vacuity conclusions, plus the SmoothAtlasPartialAE theorem `globalWeightedIBP_of_pou_partial_ae` that the headline consumes.

Let me read SmoothAtlasPartialAE.lean's main theorem statement to check what the headline genuinely removed (POU + integrability).
dsh: reasoning:
Now I have a clear picture of the headline delta: `globalWeightedIBP_of_cover_partial_ae` removes `hψ_*` (POU data), `hintL/hintR`, and adds `hcover`, `htsupp_b` (tsupport of v∘chart b ⊆ source b). Note the source theorem had `hvsupp` (support v in chart b image) but not `htsupp_b`. The new theorem derives support conditions.

Wait — one important semantic check: the headline drops `hGuvsupp`? No, it keeps it. And it keeps hvsupp. Actually it has both `hvsupp` and `htsupp_b`. Hmm, the source theorem has hvsupp but no htsupp_b. The new one adds htsupp_b, needed to construct the POU cover (needs tsupport inside source b). Is htsupp_b implied by the atlas? Not in general; it's a genuine hypothesis.

Semantic check on hvsupp: is it implied by htsupp_b + atlas? If v m ≠ 0 then m ∈ tsupport v. We'd need m ∈ chart b '' source b. In the half-space model, chart 0 = id so yes. In general, a function on M supported in the chart image... The hypothesis says any nonzero point of v is in the base chart image. Is this conclusion-equivalent? No.

Potential defect: `hvsupp` is actually derivable from the other data in some cases but not always. Not a defect.

Now, the key adversarial questions I need to resolve:

(A) Are any hypotheses contradictory? E.g., hD/hG for all i, y ∈ source i combined with htrans could conflict if two charts disagree... Actually hD/hG are definitions of Du/Guv in terms of f,u,v. If two charts i,i' both contain y-mapped points, coherence ensures chart expressions agree (because transition_chart_global + metric_transform + density transformation). Since `Du` is a function on M and hD requires Du(chart i y) = chart i laplacian, consistency across charts follows from the chart-independence lemmas in D12 (which are proven). So not contradictory. But is that provable? The D12 has `dilateMetric_driftLaplacian` etc. In the POUConstruction instantiation, they discharge hD via these lemmas for the half-space atlas. But note something important: in the instantiation, `hD` is stated as `G.driftLaplacian f u (A.chart i y) = (A.metric i).driftLaplacian ... y`, i.e., Du is defined as `G.driftLaplacian f u` on all of M. The consistency needed is that for i=1 (dilation chart), `G.driftLaplacian f u (2y) = dilateMetric G 2 ... ` which is `dilateMetric_driftLaplacian`. Good.

(B) Non-vacuity of `hcover`: for the half-space atlas, chart 0 image = {y0<1}, and chartOf : Fin 2 → ℕ = j. `⋃ j : Fin 2, chart j '' source j = univ ⊇ {y0<1}`. So hcover holds.

(C) For `halfSpaceAtlas_weightedIBP_unconditional`, hypotheses: G : ChartMetric (n+1), f,u,v C², v compactly supported, tsupport v ⊆ {y0<1}. Are there any hidden assumptions in ChartMetric? Let me check the ChartMetric structure — it might have fields like matrix positive definite, density = sqrt det, smoothness, but maybe also `density_pos` proven. Let me check whether ChartMetric is inhabited (e.g. `euclideanChartMetric`). Also check whether the C² hypothesis `ContDiff ℝ 2 v` is in the model's honest domain. It is.

(D) The heat headlines. `finiteLifetimeEntropyBridge_gaussian (τ0 t1) (ht1 : t1 < τ0)` — this is unconditional in τ0, t1 with t1 < τ0. Does it require τ0 > 0? Not explicitly; but if τ0 ≤ t1 then excluded. If t1 < τ0 but τ0 could be negative (e.g. τ0 = -1, t1 = -2), then on Ioo 0 t1 = ∅... Actually f_derivative is required on Ioo a b = Ioo 0 t1. For t1 ≤ 0, that's empty, so vacuous. `regularity` on Icc 0 t1: if t1 < 0, Icc is empty, vacuous. So the bridge is trivially inhabited for t1 ≤ 0. But for t1 > 0 and τ0 > t1 > 0, it's substantive. The statement is fine: hypotheses imply existence of the bridge for all t1 < τ0; the interesting case is 0 < t1 < τ0.

Wait — but there's a subtlety: `gaussEntropyData τ0 t1 ht1 t` is defined for all t and requires `τ_pos` at every t, which holds by clamping. Good.

But is `monotoneOn_F_gaussian` non-vacuous? For τ0=1, t1=1/2: F(s) = 1/(1-s) on [0,1/2], which is genuinely increasing. We can derive concrete values: F(0)=1, F(1/4)=4/3. Great non-vacuity evidence.

But hold on: there's a potential overclaim issue in the D13 card: it claims "I4 (all five components)" closed and "entire I4 Gaussian package and its FiniteLifetimeEntropyBridge consumption" as proved_unconditional. The heat headlines are about the *Euclidean* Gaussian model on Vec 2, not a manifold. That's a model-level result. The card's semantic_class correctly lists "Gaussian family (I4)" under model. Good. But it also lists "entire I4 Gaussian package..." under proved_unconditional. Both are compatible (proved statements about a model).

Now, "model-vs-manifold overclaims": `monotoneOn_F_gaussian` is a statement about the *Euclidean* Vec 2 chart and the clamped Gaussian. It does NOT assert Perelman monotonicity on a Ricci flow. The card is careful. I should check the *leader* summary card and the D13 card text for any overclaim language like "closes I4" meaning the general monotonicity. The exact_blockers_closed says: "I4 (all five components): f_derivative, weighted_ibp, bochner, conjugate_measure_evolution, regularity for the Gaussian family; consumed by monotoneOn_F_gaussian...". That is scoped to "for the Gaussian family". OK.

Now, is `monotoneOn_F_gaussian` conclusion-equivalent to a hypothesis? The proof uses `monotoneOn_of_bridge` with an upper bound. The bridge fields for the Gaussian are proven from explicit lemmas. Note the `weighted_ibp` field for the Gaussian is `restrictedWeightedIBP_euclideanChartCalculus (gaussPotential ...) ...` — which is the D12 chart IBP, restricted, presumably proven. Let me check `restrictedWeightedIBP_euclideanChartCalculus` for hidden assumptions.

Also `conjugate_measure_evolution` uses `Poincare.D13.HeatBridge.gaussian_conjugate_heat`. Let me check that lemma's statement and proof (it may itself assume something).

Let me check the dependency chain for sorries: search for `sorry`/`admit`/`axiom`/`native_decide`/`proof_wanted` in the D13 tree and in the specific dependencies. Let me do that now.
dsh: reasoning:
Good. No sorries in D13. Now let me look at the remaining part of POUConstruction (lines 370-482) to see the chartOne instantiation and any additional axioms, and check the `#print axioms` outputs at the end.

Also I should set up the audit build. Let me first estimate compile time for recompiling a D13 module from source in my worktree, to decide whether to do full source-recompilation checks.

Let me test compile POUConstruction.lean from a copy with LEAN_PATH pointing at leader lib. Actually simpler: use the leader's source file directly (read-only) with `-o` output into my worktree. `lean -R <root> -o <out>` where root = leader's release dir and out in my worktree. Since the source is at leader/release/Poincare/D13/.../POUConstruction.lean, using `-R leader/release` makes module name Poincare.D13.ManifoldIBP.POUConstruction. Import resolution: root first (finds the .lean files for all Poincare modules — Lean prefers .olean if up-to-date? Actually Lean searches for .olean in the search path; with `-R root`, it checks root for `Poincare/D13/.../X.olean`, not found, then LEAN_PATH. Hmm — actually Lean's `-R` adds to the search path for both source and olean? Let me just test. If it tries to recompile all dependencies from source, that would be too slow. To avoid that, copy only the target file into my own root `audit/src/Poincare/...` so `-R audit/src` finds my copy first for the target, and other modules fall back to LEAN_PATH.

Let me test with one file. Time it.

Actually, the point of recompiling: to verify that the *source* (not just the olean) compiles without sorry and matches the olean. Since the olean exists and was built after the source, and the source has no sorry, an important check is that the olean in the leader's build corresponds to the current source. I can check the olean's stored hash: Lake writes `.olean.hash` files? Let me check what POUConstruction.olean.hash contains. In newer Lean, `.olean.hash` may contain a hash of the olean? Or the source hash. Let me look.

Actually the most direct: recompile the source and compare the resulting olean's declaration signature / axiom printout. Even simpler: recompiling and using `#print axioms` from my freshly compiled olean gives the same content-level evidence. Let me test compile time.
dsh: reasoning:
The recompile took only 5.8s (it did elaborate — the #print axioms output appeared). The failure was just the output directory didn't exist (I created audit_build/lib/lean but not the nested Poincare/D13/ManifoldIBP). Let me fix and re-run.

Note the axiom output for the headline: propext, Classical.choice, Quot.sound — good (no sorryAx!).

Let me create the nested dir and recompile. Then build the full audit probe.

Important: also compare my recompiled olean with the leader's olean? Hash won't match because olean embeds paths? Possibly deterministic apart from path info. Let me instead compare the `.trace`-recorded source hash or simply trust the fresh compile. My audit will cite: fresh compile of the byte-identical source (sha256 recorded) produces declarations with the same types and axioms.

Now let me plan the probe contents. I'll create `audit_src/Poincare/L4/D13SemanticAudit.lean` in my worktree (module Poincare.L4.D13SemanticAudit), importing all relevant modules, and containing:

Section 1: Non-vacuity witnesses
1. `audit_globalWeightedIBP_instantiation`: instantiate `globalWeightedIBP_of_cover_partial_ae` at hsAtlas with a concrete nonzero bump v (via exists_contDiff_bump), f, u, e.g. n=0, G = euclideanChartMetric 1.
   But careful: constructing the bump and the hypotheses is work. Alternative: derive non-vacuity from `halfSpaceAtlas_weightedIBP_of_cover` (which is a kernel-checked instantiation of the headline at a concrete atlas, with hypotheses reduced to the C²/compact-support ones). Then also instantiate with concrete f,u,v.
   
   Actually I can make the instantiation concrete with minimal effort: use existing D13 model lemmas. For the half-space atlas instantiation we need:
   - htrans: halfSpaceAtlas_transition_coherence
   - hfc/huc/hvc: f,u,v ContDiff
   - hvcc: HasCompactSupport (v ∘ chart 0)
   - htsupp_b: tsupport (v ∘ chart 0) ⊆ source 0
   - hcover with ι = Fin 2, chartOf = id
   - hD/hG as equalities involving Du := G.driftLaplacian f u, Guv := G.gradInnerInverse u v — that's exactly what halfSpaceAtlas_weightedIBP_of_cover does.
   - hproper, hbd.
   All available via the existing `halfSpaceAtlas_weightedIBP_of_cover` and `_chartOne` declarations. So the strongest concrete instantiation: `halfSpaceAtlas_weightedIBP_of_cover_chartOne` with concrete f,u,v.
   
   For a *concrete numeric* non-vacuity, take f = 0, u = 0, v = 0? That's degenerate. Let me build a genuine one: n = 0 (Vec 1), G = euclideanChartMetric 1, f = 0, u = fun _ => 0, v = bump from exists_contDiff_bump on a nonempty compact. That still gives 0 = 0.

   Better: derive a nontrivial conclusion from the theorem by instantiating with u = gaussPotential-ish and v = a bump, then use the theorem to conclude something with nonzero content? The equality itself is content. To *prove* non-triviality of content in the audit, a cheap but rigorous route: show that the RHS integrand `G.gradInnerInverse u v` is not identically zero and the LHS integrand is not identically zero for the chosen data. E.g. u = fun y => y 0 and v = fun y => y 0 * χ y? Then gradInnerInverse u v = ∂₀v which is nonzero somewhere. And driftLaplacian f u with f=0 is 0 for u linear. Hmm LHS integrand zero.

   Take u = radSq, v = χ (bump). Then driftLaplacian 0 radSq = 2(n+1) (need lemma) — nonzero. gradInnerInverse radSq χ = 2 Σ y_i ∂_i χ — nonzero somewhere if χ nonconstant. Both integrands nonzero. Then the identity ∫ 2(n+1)χ = -∫ 2Σy∂χ is a nontrivial equality. Even without computing the integrals, the fact that both integrands are nonzero a.e. makes it contentful. Proving "not identically zero" is easy if I can prove the values at a point. For the bump χ from exists_contDiff_bump, we know χ = 1 on tsupport v... hmm. Actually I can construct an explicit bump: `Real.smoothTransition` based. There's `exists_contDiff_bump` producing a bump equal to 1 on K and supported in U. If K is a single point {0}, then χ(0) = 1 and Δ(radSq) = 2(n+1) so LHS integrand at 0 = 2(n+1)·1 ≠ 0. So the LHS integral is positive if χ ≥ 0 everywhere (the bump has range in [0,1] — `_hχ_range` unused in the instantiation but likely gives 0 ≤ χ ≤ 1). Then ∫ ≥ ... well it's ≥ 0; to show nonzero we'd need the integral > 0, but we can at least show the integrand is not identically zero and nonnegative, hence the integral is > 0 (by integral_pos_of... need measure of support positive). That's getting heavy but might be within reach. Hmm.

   Given time constraints, I'll demonstrate non-vacuity as: (a) the hypothesis set is satisfiable with nonzero, nonconstant data (kernel-checked application with explicit witnesses), and (b) the conclusion instantiates to a nontrivial integral identity; plus for one model case, I'll show a pointwise nonzero integrand. I think that plus the honest statement in the report is sufficient.

   Actually, for `manifoldWeightedIBP_of_atlasData` and `halfSpaceAtlas_laplacianIntegralZero`, there might be easier concrete evaluations.

2. `halfSpaceAtlas_weightedIBP_unconditional` non-vacuity: apply at G := euclideanChartMetric (n+1), f := 0, u := radSq, v := χ (bump). Kernel-checked application = the theorem's conclusion for this data. This shows hypotheses satisfiable and instantiation non-degenerate (v ≠ 0).

3. `halfSpaceAtlas_greenIdentity`: same data with U = V = χ; gives ∫Δχ·χ = ∫χ·Δχ (trivially true by commutativity... hmm, with U=V the identity is trivially symmetric). Better use U = χ, V = radSq·χ₂ distinct. But the identity for distinct U,V is contentful. Let me use U = bump1, V = bump2 distinct (both supported in source). Fine.

4. `halfSpaceAtlas_laplacianIntegralZero`: statement ∫ Δ V dμ = 0. Non-vacuity: instantiate with V = radSq·χ (bump). Then the claim is ∫ Δ(radSq·χ) dμ = 0, a nontrivial statement. And we can even evaluate: with f=0, μ = volume restricted to source 0 image... wait, globalMeasure for the half-space atlas: the measure is glued; is it just Lebesgue on the union of chart images with multiplicity? The half-space atlas is an *overlapping* atlas whose globalMeasure is defined via the "chart sum / gluing" construction with multiplicity? Interesting question — is globalMeasure a measure on M that counts overlaps once or twice? Let me look at the definition. If the charts overlap, the naïve sum of pushforwards would double-count. The D13 construction defines `globalMeasure` via a partition-of-unity-like `chartMeasure` mixture? Let me read GlobalMeasure.lean around line 292.

This matters semantically: the "Riemannian measure" of an overlapping atlas should be well-defined independent of the atlas. If globalMeasure is defined as a sum over charts, the overlap would be double-counted, and the IBP would be with respect to a measure that isn't the Riemannian volume. The D13 files mention `globalMeasure_restrict_eq_chartMeasure`, `chartMeasure_apply_eq` (density transformation law) — likely they use "chartMeasure i = pushforward of (density · volume) restricted to source i", and `globalMeasure = ∑ i, chartMeasure i`? But then on overlapping charts the measure counts overlaps multiple times... unless they prove chart-independence and use a POU to define a partition. Hmm, but the half-space atlas IBP theorem uses `(hsAtlas G).globalMeasure volume` — if that double-counts on the overlap, the IBP identity could still hold per chart... Let me actually read GlobalMeasure.lean lines 160-399 to see how globalMeasure is defined and what its properties are.

This is a potentially important semantic finding: "model-vs-manifold overclaims". Let me read carefully.
dsh: reasoning:
Excellent — the globalMeasure uses a disjointification (measurable partition), so no double counting: `chartPiece n = disjointed (fun k => chart k '' source k) n`. And `globalMeasure_apply_chart` proves independence of which chart is used (using `chartMeasure_apply_eq`). So the measure is honest. Good — this addresses a potential "overlap double-counting" defect. I should verify `chartMeasure_apply_eq` is proven and uses the density transform law.

Now, important semantic subtlety: `globalMeasure` is built as a sum over ℕ-indexed charts; for the half-space atlas, the partition assigns each point to the least chart index covering it (disjointed). Fine.

Another subtle issue: the theorem `globalMeasure_apply_chart` requires `[μ.IsAddHaarMeasure]` — volume on Vec d has it presumably.

Now let's think about the truly adversarial checks for each headline. Let me enumerate:

### H1: SmoothOverlapAtlas.globalWeightedIBP_of_cover_partial_ae
Statement: for `A : SmoothOverlapAtlas M (n+1)`:
- htrans, b, ι Fintype, chartOf, f u v Du Guv, measurability hf hv hDu hGuv,
- hvsupp, hfc huc hvc (C² chart expressions), hvcc (compact support of v∘chart b), htsupp_b (tsupport(v∘chart b) ⊆ source b),
- hcover (base chart image covered by finitely many chart images),
- hD, hG (operator identifications on all sources),
- hGuvsupp (support of Guv in base chart image),
- hproper (transitions proper),
- hbd (null frontiers)
Conclusion: ∫ Du·v d(globalMeasure.withDensity (weight f)) = -∫ Guv d(...).

Checks:
- conclusion-equivalent hypothesis? No: hD/hG relate the *integrands* to chart operators, which is what makes IBP nontrivial; but is `hG` perhaps strong enough to imply the conclusion directly? The conclusion would follow from the chart-level theorem which is proven. So no.
   However: there's an important nuance — hD/hG are hypotheses about Du and Guv, and the conclusion is about Du and Guv. In a genuine application, Du and Guv are the *defined* Laplacian and pairing. So this is fine (identification hypotheses, not conclusion-equivalent). But is `hG` on its own equivalent to the conclusion? No.
- contradictory? Only if no atlas with such data exists. The instantiation exists.
- vacuous? hcover + hvsupp: if v ≠ 0 somewhere, then chart b image nonempty, so ι nonempty and hcover is satisfiable. Non-vacuous by instantiation.
- wrong domain? The integral is over M vs the chart expression domain Vec (n+1). The support of v is in chart b image; fine.
- Hidden interface fields? None (no structures with assumption fields here except ChartMetric).

Potential subtlety: `hvsupp` requires support of v inside chart b image, but the POU construction only needs tsupport(v∘chart b) ⊆ source b. In the proof, do they use hvsupp? Yes, probably for the support transfer. Not a defect.

Another subtlety: `hfc : ∀ i, ContDiff ℝ 2 fun y => f (A.chart i y)` — quantified over ALL i : ℕ, including charts whose sources don't cover anything. Vacuous on empty sources; strong on nonempty ones but needed only for i in the cover and b. Not a defect.

A real potential defect: the theorem assumes `hvsupp` and `hGuvsupp` — support conditions for v and Guv. But the conclusion's RHS integrand is Guv; the theorem proves the identity on the whole manifold. Since v and Guv are supported in the base chart image, fine.

Another: `[Fintype ι]` cover with `chartOf : ι → ℕ`. OK.

Potential defect: does the proof actually use `hproper` for ALL i j, and does it hold for atlases? For the half-space model yes.

I think H1 is fine. One "hidden assumption" to check: The conclusion uses `A.globalMeasure volume` where volume is Lebesgue on Vec (n+1) — okay for the model. For a Riemannian manifold one would need the metric volume; here ChartMetric carries the density. Fine.

### H2: halfSpaceAtlas_weightedIBP_unconditional
Statement: G ChartMetric, f u v C², v compact support, tsupport v ⊆ {y0<1} ⊢ ∫ G.driftLaplacian f u · v d((hsAtlas G).globalMeasure volume).withDensity ((hsAtlas G).weight f) = -∫ G.gradInnerInverse u v d(...).

Checks:
- "unconditional" claim: unconditional *relative to G : ChartMetric (n+1)* and C² data. Is `ChartMetric` inhabited? Need to check euclideanChartMetric. If ChartMetric is inhabited with the Euclidean metric, the claim is substantiated. Also the theorem's hypotheses could be unsatisfiable if ChartMetric were empty. Let me check.
- Are the hypotheses consistent? Yes, instantiate.
- Is it really "no atlas hypotheses left"? The atlas is the fixed hsAtlas, so yes.
- Domain: tsupport v ⊆ {y 0 < 1}, i.e. the base chart source. Note the atlas is overlapping; the weight function uses `(hsAtlas G).weight f` = e^{-f(m)}, and the measure is the global Riemannian measure. Good.
- Potential issue: v must be C² AND compactly supported AND supported in a half-space; that's the standard restricted class. Fine.
- Potential overclaim: the theorem says "no POU or integrability hypotheses". True.
- Hidden assumption: `G.driftLaplacian f u` requires f, u C²; the measure weight uses f. Fine.

### H3: halfSpaceAtlas_greenIdentity
Statement: same data with U, V separately compactly supported in {y0<1}; conclusion ∫ Δ_F U · V = ∫ U · Δ_F V.
Checks: This is just two applications of H2 (IBP twice). Correct math: ∫ΔU·V = -∫⟨∇U,∇V⟩ and ∫U·ΔV = -∫⟨∇V,∇U⟩ = -∫⟨∇U,∇V⟩. Yes.
- Non-vacuity: H2 applies.
- Any issue about integrability? H2's integral is a Bochner/Lebesgue integral of possibly non-integrable functions; mathlib's `∫` returns 0 for non-integrable functions, so the identity could be "vacuous" in the sense that both sides might be 0 due to non-integrability. That's why the companion `halfSpaceAtlas_integrable_dirichlet` matters. For green identity they don't state integrability, but the docstring says "Both sides are integrals of integrable functions by halfSpaceAtlas_integrable_dirichlet". Note: H3's integrability follows from H2-applied-twice plus Dirichlet integrability? For ∫ΔU·V, the integrability is `halfSpaceAtlas_integrable_dirichlet F U`; yes with f=F, V=U. Fine.
- Domain check: both U and V need tsupport in {y0 < 1}; fine.

### H4: halfSpaceAtlas_laplacianIntegralZero
Statement: ∫ G.laplacian V d((hsAtlas G).globalMeasure volume) = 0 for C² V compactly supported with tsupport in {y0<1}.
Checks: uses unweighted measure (no density e^{-f}); good. Domain {y0<1}. Non-vacuity: V = bump.
- Potential issue: "the divergence theorem on the partial half-space atlas" — the proof reduces to the base chart and uses `laplacian_integral_eq_zero`, which requires compact support and C². But the support of ΔV ⊆ tsupport V ⊆ source 0, so the boundary term at {y0=1} vanishes because V ≡ 0 near it... wait, tsupport V ⊆ {y0 < 1}: if V is supported strictly inside the half-space, then ΔV is also supported strictly inside (needs `support_laplacian_subset` proven). Then on the base chart, ∫ ΔV = 0 by the chart theorem. But careful: the base chart source is {y0 < 1}, and the chart map is identity; the integral over the chart measure = ∫_{source 0} ΔV ρ dx. But the theorem `laplacian_integral_eq_zero` presumably states ∫_{ℝ^d} ΔV · ρ = 0 for V compactly supported (over all of ℝ^d). Then restricting to source 0 is fine since ΔV supported in source 0. And the boundary at y0 = 1 is not an issue because V is supported away from it. Good — as long as hVsupp is *topological* support. Yes: tsupport V ⊆ {y0<1} means V vanishes on an open neighborhood of the complement... actually closure of support inside the open set {y0<1}, so compactly supported away from the boundary.
- Hidden assumption: none.
- Potential semantic defect: the headline is about the *partial* atlas but the proof only uses chart 0. Fine.

### H5: dilationAtlasTwo_weightedIBP_via_pou
Statement: G ChartMetric, F U V C², V compact support, R, ψ : Fin 2 → Vec → ℝ with ψ smooth, tsupport ψ_i ⊆ ball 0 (R+1), sum ψ = 1 on tsupport V, and integrability of pieces hintL/hintR; conclusion ∫ G.driftLaplacian F U · V d((dilationAtlasTwoSmooth G).globalMeasure volume).withDensity (weight F) = -∫ G.gradInnerInverse U V d(...).
Checks:
- This theorem is *conditional*: it takes POU data ψ and integrability hypotheses hintL/hintR. It is NOT the unconditional claim. The card classifies it as... let me check. In the D13 card's expanded_hypotheses, "dilationAtlasTwo_weightedIBP_via_pou" isn't listed explicitly! Let me grep the card for it. The card says `globalWeightedIBP_of_pou` takes explicit POU data (now shown constructible). Need to check how the card classifies dilationAtlasTwo_weightedIBP_via_pou. Let me grep.
- Potential defect: the headline name suggests it's the consumed dilation atlas IBP, but it assumes the POU and integrability. If the card claims it as "proved_unconditional", that would be an overclaim. Let me check the card text.
- Also note tsupport ψ_i ⊆ ball(0,R+1) — a compact support condition; ψ smooth. hψ_sum only on tsupport V. hintL/hintR integrability.
- Is there an unconditional dilation analogue? `dilationAtlasTwoData_weightedIBP` via ManifoldAtlasData with hg/hg' integrability. Both are conditional on integrability. The card's semantic_class says "explicit-POU unconditional IBP on the partial half-space atlas" (that's H2). For the dilation atlas, "overlapping dilation atlas (dilationAtlas/dilationAtlasTwo, total)" is under model.

### H6: manifoldWeightedIBP_of_atlasData
Statement: A : ManifoldAtlasData M n ι; u v : M → ℝ; hd: ∀i C² drift; hu, hv chartwise C²; hvc chartwise compact support; hg, hg' integrability; conclusion ∫ A.driftLaplacianM u · v dA.μ = -∫ A.gradInnerM u v dA.μ.
Checks:
- Hidden interface fields: integral_decomp, weighted_laplacian_compat, grad_inner_compat. These are structure fields — assumed. The task explicitly asks to list "hidden interface fields (ManifoldAtlasData.integral_decomp etc.)".
- Conclusion-equivalence: are the interface fields equivalent to the conclusion? `integral_decomp` is a general gluing formula for all integrable g, `weighted_laplacian_compat`/`grad_inner_compat` are pointwise operator identifications — together they make the conclusion follow from ChartSumData.globalWeightedIBP (a chartwise theorem). Not conclusion-equivalent per se, but they are strong: the conclusion follows *directly* by rewriting. This is a "conditional interface" theorem: essentially a transfer lemma. The honest classification is "conditional interface". The card does classify ManifoldAtlasData as conditional_interface. Good.
- Is the hypothesis hd needed? Yes for chart IBP.
- Are hg/hg' conclusion-equivalent? hg is `Integrable (Δu·v) μ` and hg' `Integrable (⟨∇u,∇v⟩) μ`. These are needed to apply integral_decomp; they're not equivalent to the equality.
- Vacuity: is ManifoldAtlasData inhabited? Yes: `dilationAtlasTwoData` (Vec (n+1), n, 1). Need to verify its fields are actually constructed and consistent. Also `disjointAtlasData`. Let me check OverlapIBPData.lean lines 75-130.

### H7: finiteLifetimeEntropyBridge_gaussian
Statement: ∀ τ0 t1, t1 < τ0 → FiniteLifetimeEntropyBridge (gaussCalculus τ0 t1 ht1) (gaussEntropyData τ0 t1 ht1) 0 t1.
Checks:
- The structure is Prop; the theorem provides all six fields.
- Non-vacuity: need to check the bridge isn't trivially satisfied. For t1 > 0 it's substantive. Demonstrate with concrete τ0=1, t1=1/2.
- Overclaim check: docstring "All six fields are proved for the explicit backward Gaussian on the lifetime [0, t₁]; nothing is assumed." True for the structure fields. But note: `finiteLifetimeEntropyBridge_gaussian` itself has no hypotheses other than ht1. Is it a "model" result? It's a theorem about the Euclidean Vec 2 model, using `euclideanChartCalculus` and the explicit Gaussian. The card classifies it under proved (about the model). Good.
- Domain issue: the fields are required on Ioo 0 t1 (interior). If t1 ≤ 0, the interval is empty. So the interesting content requires 0 < t1. Is that a "wrong domain (0,T) vs (0,T]" issue? The bridge's regularity is on Icc 0 t1 (closed), derivative on Ioo (open). That's correct for the monotonicity argument.
- A subtle check: `gaussTau` clamping means beyond t1, but fields only on Ioo 0 t1 where t < t1 so τ = τ0 - t. The `weighted_ibp` field uses `gaussEntropyData τ0 t1 ht1 t` and `exp_neg_gaussPotential` with `gaussTau_pos ht1 t` — fine.
- `f_derivative`: uses `hasDerivAt_gaussianF τ0 t ht.1 (lt_trans ht.2 ht1)`, then rewrites with `FDissipation_gaussEntropyData ht1 ht.2.le` — wait, it rewrites `1/(τ0-t)^2` to FDissipation, but the derivative of F is `-(1/(τ0-t)^2)`? Let's check `hasDerivAt_gaussianF` sign. F(t) = 1/τ(t) with τ decreasing: d/dt 1/(τ0-t) = +1/(τ0-t)². So derivative = +1/τ² = FDissipation ≥ 0. Good, consistent with F nondecreasing.

### H8: monotoneOn_F_gaussian
Statement: MonotoneOn (fun s => (gaussEntropyData τ0 t1 ht1 s).F) (Icc 0 t1) given t1 < τ0.
Checks:
- Non-vacuity: τ0=1, t1=1/2: F(s) = 1/(1-s) for s ≤ 1/2. Monotone. Derive concrete: F(0) = 1, F(1/2) = 2. And F(0) ≤ F(1/2) follows.
- Domain: Icc 0 t1 closed. If t1 ≤ 0, the set is empty (or a point), trivially monotone. Non-vacuous for 0 < t1 < τ0.
- Conclusion-equivalence? No; the upper bound hypothesis is discharged using explicit formula.
- Is the statement genuinely about "the D4 certificate"? `monotoneOn_of_bridge` is a general reduction from the bridge; the monotonicity conclusion follows. Fine.

Now potential real defects to hunt for:
1. The D13 card's claim "halfSpaceAtlas_greenIdentity / _dirichletEnergy: two C^2 compactly supported functions with topological support in the base chart source." — fine.
2. The card claims U7/I4 blockers closed. Need to check whether these "blockers closed" are consumed downstream (the D13 card's own instruction: "never claim Perelman or a named blocker closure without a constructed downstream checked use"). The card's exact_blockers_closed list has "consumed by ..." statements. I should verify at least that the claimed consumers exist as kernel-checked declarations (e.g., `dilationAtlasTwoData_weightedIBP` consumes ManifoldAtlasData).
3. Check for any hypothesis in the headline list that is *unused* or too strong in a way that makes the theorem non-general. E.g., `hvsupp` in H1 might be derivable from `htsupp_b`? If htsupp_b holds: tsupport (v∘chart b) ⊆ source b. Given m with v m ≠ 0, is m ∈ chart b '' source b guaranteed? v m ≠ 0 implies m ∈ support v. Since v∘chart b is supported in source b, ... only says points y outside source b have v(chart b y) = 0. It does not say m is in the image. But note the atlas cover: m ∈ chart i '' source i for some i. Then hD/hG relate chart expressions at that chart. So hvsupp is a genuine extra support restriction. Is it needed? In the proof, hvsupp might be used to show `∫ Du*v = ∫ Du * (∑ lift(piece))`. Since v supported in chart b image, yes needed. Not a defect.

4. Potential defect in H1: `hGuvsupp` and `hvsupp` are "*support* of the function is in the base chart image" — but the conclusion is an identity of integrals over the whole space; if v were not supported in a chart image, the POU construction wouldn't apply. Fine.

5. Hidden field: `ChartMetric.smooth` requires ContDiff ℝ ⊤ (analytic?) coefficients. The card says: "ChartMetric.smooth at the D12 pin asks for ContDiff R top (= analytic) coefficients, with SmoothChartMetric the honest C^inf variant." Wait — ContDiff ℝ ⊤ means smooth (C^∞) in mathlib's `⊤ : WithTop ℕ∞` = ∞... Actually `ContDiff ℝ n f` with n = ⊤ means smooth (all derivatives). `⊤ : WithTop ℕ∞` is `↑⊤` (ω), not analytic. The card says "(= analytic)" which might be a misstatement. Let me check ChartMetric's fields. This could be a card inaccuracy but not a theorem defect. Let me look.

Let me check ChartMetric in D12.
dsh: reasoning:
Confirmed: `ChartMetric.smooth` uses `ContDiff ℝ ⊤` = C^∞ (smooth), not analytic. The D13 card's phrase "asks for ContDiff R top (= analytic) coefficients" is a misstatement (analytic in mathlib is `AnalyticAt`/`ContDiff ℝ ω`). That's a minor card inaccuracy worth noting (it doesn't affect the theorems). Actually wait, in mathlib `ContDiff ℝ n f` for `n : WithTop ℕ∞`; `⊤` is `ω` (i.e., `↑⊤` where `⊤ : ℕ∞` is infinity) = smooth. `ContDiff ℝ ω` means analytic. The notation in Lean: `⊤ : WithTop ℕ∞` = `((⊤ : ℕ∞) : WithTop ℕ∞)` = ω = smooth. Analytic would be written `ω`? Hmm, in mathlib `ContDiff ℝ ω f` where `ω : WithTop ℕ∞` is the top element denoting analyticity. Let me recall: mathlib defines `ℕ∞` = ℕ ∪ {∞} and `WithTop ℕ∞` = ℕ∞ ∪ {⊤} = ℕ ∪ {∞, ω}. In `ContDiff`, `n : WithTop ℕ∞`, where `(n : ℕ∞)` means n-times differentiable, `⊤` (i.e. ω) means analytic, and `↑∞` means smooth. Yes! In mathlib: "ContDiff 𝕜 n f" where n : WithTop ℕ∞; `ω` (the top) means analytic, `∞` means smooth. `⊤` in `WithTop ℕ∞` IS ω (the top), so `ContDiff ℝ ⊤` = analytic! Let me verify: In Mathlib.Analysis.Calculus.ContDiff.Defs: `ContDiff 𝕜 n f` with n : WithTop ℕ∞. The instance: `ω` is the top of `WithTop ℕ∞`. And `⊤ : WithTop ℕ∞` = ω. There's notation `∞` for `(⊤ : ℕ∞)`. So indeed `⊤ : WithTop ℕ∞` denotes ω = analytic, and `∞ : WithTop ℕ∞` denotes smooth. So `ContDiff ℝ ⊤` is analytic. Hmm! The card says "(= analytic)" — so the card is right and `SmoothChartMetric` with `ContDiff ℝ ∞` is the C^∞ variant. Good thing I checked. Let me confirm with a quick Lean check: `#check (show (⊤ : WithTop ℕ∞) = ω from rfl)` or check that `ContDiff ℝ ⊤` needs analytic. Simplest: `example : (⊤ : WithTop ℕ∞) = ω := rfl`. I'll include in the probe.

Okay. Now let me check `euclideanChartMetric` to confirm ChartMetric is inhabited with density 1.

Then, let's check what the D13 card says specifically about the 8 headlines. Let me grep the card md.
dsh: reasoning:
The card's line 31 makes the claim: "no atlas, POU, measurability or integrability hypotheses remain" for H2. That's true.

Note the card says at line 201: "hintL/hintR of halfSpaceAtlas_weightedIBP_unconditional and of globalWeightedIBP_of_cover_partial_ae" — implying the unconditional and cover theorems still have hintL/hintR; but they don't (they're constructed). Actually the sentence may be loose: "consumed by hintL/hintR of ... and by halfSpaceAtlas_integrable_dirichlet". Hmm, the exact_blockers_closed in JSON says "U7-GLOBAL-INTEGRABILITY: ... consumed by hintL/hintR of the unconditional and cover theorems and by halfSpaceAtlas_integrable_dirichlet." This looks stale/inaccurate: H2 (unconditional) has no hintL/hintR hypotheses, and H1 constructs hintL/hintR internally. It might refer to the internal `hintL`/`hintR` local hypotheses in the proof of `halfSpaceAtlas_weightedIBP_unconditional` (there are indeed local `hintL`, `hintR` in its proof via the POU theorem). Minor wording issue, not a defect in theorem.

Now let me read the full card md sections 1-2 (headline claims) and the "expanded hypotheses" section to check for overclaims, especially about dilationAtlasTwo_weightedIBP_via_pou.
dsh: reasoning:
The card is careful and mostly honest: it lists §6 limitations. Now, the audit task asks me to report whether the D13 card's 'expanded hypotheses' and 'blockers closed' claims are supported. So my job: check each claim against kernel reality.

Notable: claim 6 says "`ChartMetric.smooth` at the D12 pin still asks for `ContDiff ℝ ⊤` (= analytic) coefficients, the honest `C^∞` variant being `SmoothChartMetric`." I need to verify that `⊤ : WithTop ℕ∞` is indeed ω (analytic). In mathlib, `ContDiff 𝕜 n f` where `n : WithTop ℕ∞`. The notation: `∞` is `(⊤ : ℕ∞)` coerced, and `ω` is `⊤ : WithTop ℕ∞`. Let me verify in Lean directly.

Also the D13 card's expanded_hypotheses (JSON) says: "`halfSpaceAtlas_weightedIBP_of_cover`: same data with base chart 0; `halfSpaceAtlas_weightedIBP_of_cover_chartOne`: tsupport v inside {0 < y 0} (base chart 1)." OK.

Now, let me note something potentially significant for the audit: H2 (`halfSpaceAtlas_weightedIBP_unconditional`) is stated for `G : ChartMetric (n+1)` *arbitrary*, but the atlas `hsAtlas G` is built from G with charts identity and dilation-by-2. Is this a genuine Riemannian metric consistency? The atlas's `metric_transform` field for charts 0 and 1 requires `dilateMetric G 2` to satisfy the tensor transformation law w.r.t. dilation transition — proven via `metric_transform_chartTransition`. But note: the atlas has charts only for i=0 and i=1? No! `hsChart i = if i = 0 then id else 2•` and `hsSource i = if i=0 then {y0<1} else {y0>0}`. So charts i ≥ 1 are ALL the dilation chart with source {y0 > 0}. That means the atlas has infinitely many charts, all identical for i ≥ 1. Overlaps between chart 1 and chart 2: source 1 = source 2 = {0<y0}, and chart 1 = chart 2 = 2•; transition 1 2 = id; fine, consistent. This is a redundant atlas, not a defect. But wait — does that hurt the "Riemannian measure" well-definedness? No, since chartMeasure_apply_eq proves chart independence.

Hmm, but here's a subtle thing: the charts i≥1 are duplicates, and `cover` = ⋃ chart i '' source i = univ. The disjointification chartPiece assigns each point to the least index chart image containing it — chart 0 gets {y0<1} \ (nothing before it) → chartPiece 0 = {y0<1}; chartPiece 1 = {y0>0} \ {y0<1} = {y0 ≥ 1}; chartPiece n≥2 = ∅. So the measure is the honest one. Good.

Now let me check the `SmoothOverlapAtlas` `IsTotal` notion and the "not total" claim; fine.

Next, let me examine the exact potential for the "wrong domain (0,T) vs (0,T]" issue. In D13 heat: `FiniteLifetimeEntropyBridge C E a b` fields on Ioo a b and regularity on Icc a b; `monotoneOn_of_bridge` gives MonotoneOn on Icc. Correct. For H8 with a=0, b=t1. Domain [0,t1]. No (0,T) vs (0,T] mismatch.

But look at `finiteLifetimeEntropyBridge_gaussian`: the structure requires `regularity : ContDiffOn ℝ 1 (fun s => (E s).F) (Icc a b)`. For the Gaussian, F(s) = 1/(τ0-s) on Icc 0 t1 provided t1 ≤ t1 (yes) and τ0 - t > 0 for t ≤ t1 < τ0. ContDiffOn on Icc. Provided via `contDiffOn_gaussianF τ0 t1 ht1`. Fine.

One more possible issue: `gaussEntropyData`'s F on the *whole* real line uses clamped τ; but the bridge only uses it on [0,t1]. Fine.

Now let me check the D13 card's claim "I4-residual defects (proved): ... the forall-t>0 conjugate-heat/regularity forms are unsatisfiable for nontrivial finite-lifetime data". That's a claim about refutations; not among my 8 headlines, but relevant to whether "blockers closed" are supported. I'll spot check `Bridge.RealCalculus.unrestrictedWeightedIBPStatement_false` exists and compiles.

Now, key question: are there any genuine defects? Let me look for potential issues:

**Potential defect A**: `globalWeightedIBP_of_cover_partial_ae`'s hypothesis `hproper : ∀ i j, ∀ K, IsCompact K → IsCompact (A.transition i j ⁻¹' K)` quantifies over ALL i,j even though only finitely many charts are in the cover. Harmless.

**Potential defect B**: In H1, the measurability hypothesis `hf : Measurable f` is required but is it used? Probably in integrability. Fine.

**Potential defect C**: In H1 the conclusion integrates over `(A.globalMeasure volume).withDensity (A.weight f)`. `A.weight f` is `ENNReal.ofReal (exp (-(f m)))` — an ENNReal density; withDensity of an ENNReal function. Fine.

**Potential defect D**: H5 `dilationAtlasTwo_weightedIBP_via_pou` claims in its docstring "The general global IBP theorem consumed on the dilation model... The remaining inputs are the POU data and integrability of the constructed piece integrands." So it is honest about being conditional. But is it listed in the card's expanded_hypotheses? Not explicitly. The card's U7-GLOBAL-POU row says it's consumed by `dilationAtlasTwo_weightedIBP_via_pou`. The card's §3 item 6 mentions pre-existing interfaces. Hmm, the 8 headlines include H5 — task says "for each of the 8 headline declarations ... list every hypothesis and its domain/quantifier". So I'll do exactly that. Classification for H5: conditional interface (POU data + integrability supplied).

**Potential defect E**: H6 `manifoldWeightedIBP_of_atlasData` — the structure's `integral_decomp` requires `Integrable g μ` as an input to the identity; fine. But note the semantics of `ManifoldAtlasData.μ`: it's an abstract measure, and `integral_decomp` is the gluing assumption. This is the hidden interface field. The D13 card lists it under "conditional_interface" and in remaining_blockers? Let me check the remaining_blockers JSON of the card.

Let me also check whether `dilationAtlasTwoData` truly inhabits the structure with all fields (not just some). Read OverlapIBPData.lean fully.

Then: check for a potential defect where `ManifoldAtlasData.integral_decomp`'s chart sum uses `Fin ι` and requires disjointness. For the dilation atlas (single chart ι=1) it's trivially fine. For a genuinely overlapping atlas like the half-space one, is there a `ManifoldAtlasData` instance? The card says "inhabited with proofs for the models" — the models being the disjoint model and the dilation (single-chart) model. For the overlapping half-space atlas there's no instance (you'd need a partition). That's a limitation, but the card's §6.7 mentions no manifold-level F-functional, etc. Is this "hidden interface field" concern? The task explicitly asks to check hidden interface fields like `ManifoldAtlasData.integral_decomp`. So I should report: H6 assumes integral_decomp (a global gluing identity, equivalent in content to the existence of a partition-of-unity chart decomposition), weighted_laplacian_compat, grad_inner_compat. It is a *transfer* theorem; not a proof that such data exist for a manifold, but instantiations exist for the single-chart dilation model and the disjoint model. Good.

**Potential defect F**: Is `dilationAtlasTwoData`'s `integral_decomp` proof actually checking something meaningful, or is it trivial because ι=1 and the measure is defined as the chart measure? It's a real proof of the gluing identity for the single chart. Fine.

Now let me read OverlapIBPData.lean.
dsh: reasoning:
Very thorough card. Now let me start building the audit probe. Time check: roughly 1 hour elapsed? Let me check the time. I should write checkpoint.json soon.

Let me plan the Lean audit file. I'll write `audit_src/Poincare/L4/D13SemanticAudit.lean` (module `Poincare.L4.D13SemanticAudit`) with the following content:

```lean
import Poincare.D13.ManifoldIBP.POUConstruction
import Poincare.D13.ManifoldIBP.PartialChartModelPOU
import Poincare.D13.ManifoldIBP.SmoothAtlasModel
import Poincare.D13.ManifoldIBP.Transfer
import Poincare.D13.ManifoldIBP.OverlapIBPData
import Poincare.D13.HeatKernelBridge
```

Then:
- `#check` full statements of all 8 (for exact statement capture).
- Non-vacuity section:
  - `theorem audit_h2_nonvacuous : ∃ (v : Vec 1 → ℝ), HasCompactSupport v ∧ tsupport v ⊆ {y | y 0 < 1} ∧ v ≠ 0` via exists_contDiff_bump on a compact K inside U. Actually `exists_contDiff_bump` signature: (K U : Set) (hK : IsCompact K) (hU : IsOpen U) (hKU : K ⊆ U) → ∃ χ, ContDiff ℝ ∞ χ ∧ (range stuff) ∧ (χ = 1 on K) ∧ tsupport χ ⊆ U. Let me check the exact signature in SmoothPartition.lean.
  
  Then instantiate the unconditional theorem with f = 0, u = 0 (or radSq), v = χ and derive the identity. To make it strongly non-vacuous, use `u := fun y => y 0` and `v := χ`? Then Δu = 0 (for euclidean metric, f=0) and RHS = -∫ gradInnerInverse = ... hmm.
  
  Let me use u := radSq-ish. Is there a lemma `laplacian_radSq`? In EuclideanChart there is `radSq` and lemmas. Let me check what's available: `Poincare.D13.EuclideanChart.ChartMetric.radSq`, `laplacian_radSq`? Let me grep.

  Actually for the non-vacuity check, the crucial kernel-checked facts I want:
  (1) The hypotheses of each headline are satisfiable with a nonzero v.
  (2) The conclusion is instantiated (so the theorem is applied, not just hypothesised).
  
  To witness (2) I'll produce a `have h := theorem ...` and maybe restate. The compilation itself proves the instantiation.

  For extra strength, I can prove for the H4 (laplacian integral zero) instance that the integrand is nonzero at a point: e.g. V = radSq * χ where χ is a bump equal to 1 at 0; then G.laplacian V 0 = G.laplacian radSq 0 * 1 + ... hmm product rule. Simpler: V := radSq. But radSq isn't compactly supported. V := χ (bump) with χ = 1 at 0; laplacian of χ at 0 isn't determined.

  Alternative: use `V := fun y => radSq y * χ y` where χ ≡ 1 on a neighborhood of 0 (exists_contDiff_bump gives χ = 1 on tsupport? or on K). If K = closed ball 1 and U = ball 2, χ = 1 on closed ball 1, then V = radSq * χ and near 0, V = radSq, so ΔV(0) = Δ radSq (0) = 2(n+1) ≠ 0. Proving this in Lean requires the product rule with χ ≡ 1 eventually near 0 (𝕟𝕫). Doable but fiddly with `fderiv` of products. Might take 30-60 min. 

  Given the objective is a semantic audit rather than a proof sprint, I'll do a simpler, still meaningful non-triviality check:
  - For H7/H8, get *numeric* values: `(gaussEntropyData 1 (1/2) h (1/4)).F = 4` and monotonicity `F(0) ≤ F(1/2)` with values 1 and 2. That's genuine content with numbers.
  - For H2/H3/H4, instantiate with the explicit `Real.smoothTransition`-based bump? The half-space model's POU uses `exists_contDiff_bump` on tsupport v. If I take v := hsStep (the smooth transition, which is 0 for y0 ≤ 1/4 and 1 for y0 ≥ 1/2), it's C^∞, compactly supported? No — hsStep is 1 for y0 ≥ 1/2, not compactly supported. Take v := hsPsi χ j for a compactly supported χ. Hmm.

  Simplest concrete nonzero compactly supported smooth v: use `exists_contDiff_bump` with K = closedBall 0 1, U = ball 0 2 ⊆ {y0 < 1}. Then v = χ, nonzero at 0 (χ = 1 on K so χ 0 = 1) — that gives a witness that v ≠ 0 and even v 0 = 1. Then the theorem's LHS integrand is Δ_F u · v, RHS integrand is ⟨∇u,∇v⟩. With f=0 and u = 0: LHS = 0, RHS = 0. The identity is trivial for these data.
  
  Take u := fun y => y 0 (first coordinate) and f := 0: Δu = 0 (needs a lemma: laplacian of a coordinate function = 0). RHS = -∫ ∂₀χ. Both 0. Still trivially consistent but the RHS is a nontrivial statement (integral of a derivative = 0).
  
  Take u := radSq and f := 0: Δu = 2(n+1) ≠ 0; gradInnerInverse u χ = 2 Σ y_i ∂_i χ. Then the identity is ∫2(n+1)χ = -∫2Σy∂χ — a genuine divergence identity. To *prove* the two integrands are not both trivially zero, I can show the LHS integrand at 0 is 2(n+1) ≠ 0. But proving Δ(radSq) = 2(n+1) needs the EuclideanChart lemmas. Let me check if `laplacian_radSq` exists.

Let me grep for available lemmas about radSq and laplacian.

Actually, wait. I could use H4 (laplacianIntegralZero) to *derive* a nontrivial concrete conclusion: ∫ ΔV dμ = 0 with V = radSq·χ. Since I can't easily compute the integral, deriving 0=0-ish... 

Alternative strong non-vacuity: use H2 with u = radSq, v = χ and *derive* ∫2(n+1)χ dμ = -∫2Σy∂χ dμ. That's a nontrivial identity. Then also prove the RHS is not identically zero by computing at a point: at y where χ is decreasing along some direction... needs a specific bump with known derivative.

I think for a 4-hour budget, the appropriate depth is:
- Kernel-checked instantiation per headline with explicit data (hypotheses discharged), showing the theorems are applicable to nonzero test data.
- For at least a couple, numeric evaluation (heat F values; maybe a 1-D explicit bump with computable integral).

Actually here's a cleaner numeric non-vacuity for H2/H3/H4 that's easy: take **n = 0** so Vec 1 = ℝ, and use `v := fun y => Real.smoothTransition (2 - y 0) * Real.smoothTransition (y 0 + 2)`? That's compactly supported in (-2,2) and smooth, but proving C² and support is fiddly. `exists_contDiff_bump` gives existence but no explicit formula.

Hmm, what about using the fact that `hsPsi χ j` are built from a bump χ? Still existence-based.

Honestly, the standard notion of "non-vacuity" in these audits (as seen in the D13 card's "halfSpaceAtlas_integrable_dirichlet ... (non-vacuity)") is: exhibit a kernel-checked application with a nonzero test function and note integrability. I'll follow that, and add numeric content where cheap (heat F).

Let me check available lemmas for radSq laplacian to decide.

Actually, here's a MUCH better idea for genuine numeric non-vacuity of H2/H4 in **dimension n=0 with a Gaussian-like compactly supported function**... no.

Alternatively use the fact that `globalMeasure` for the half-space atlas equals? For V supported in source 0 and using chart 0 = id, ∫ ΔV dμ = ∫_{y0<1} ΔV(y) ρ(y) dy. With f = 0 and G = euclidean, ρ = 1, so H4 says ∫_{y0<1} ΔV dy = 0 for V supported in {y0<1}. For V(y) = radSq(y)·χ(y) with χ = 1 near 0, we have ∫ΔV = 0. Could compute ∫ Δ(radSq χ) = ∫ (2(n+1)χ + 4 y·∇χ + radSq Δχ). Not simpler.

OK let me keep it reasonable. But one thing I *can* do cheaply and rigorously: prove that the LHS integrand of H2 instantiated at u := radSq, f := 0, v := χ (with χ = 1 at 0) is nonzero at 0. Need `G.laplacian radSq = 2 * (n+1)` or at least `≠ 0` at 0. Let me look for existing lemmas.

Let me grep EuclideanChart for radSq laplacian lemmas.
dsh: reasoning:
`exists_contDiff_bump` gives χ with 0≤χ≤1, χ=1 on K, tsupport ⊆ U. Good — that gives me a nonzero v with v 0 = 1 when 0 ∈ K.

No radSq-laplacian lemma found by that grep. Let me check what `radSq` is and whether there are lemmas like `partialDeriv_radSq`. The HeatKernelBridge imports `radSq_differentiableAt partialDeriv_radial ...`. Let me check `laplacian` of radSq maybe in GaussianMoment/GaussianF.

Actually, let's not go down this path. Instead, I'll do a cleaner non-vacuity check for the *IBP* headlines using a case where both sides are provably zero *for independent reasons*, and a case where the identity has nontrivial content because the integrands are nonzero.

Hmm, alternatively: for H4 (laplacianIntegralZero) I can use H2's Dirichlet energy at u=v=V to derive -∫|∇V|² = ∫ ΔV·V; that's separate.

Let me reconsider: The task says "Verify non-vacuity by exhibiting at least one kernel-checked instantiation per headline (the D13 model atlases)." So the requirement is really: per headline, exhibit a kernel-checked instantiation using the D13 model atlases. That's: apply the theorem to hsAtlas/dilationAtlasTwoData/gauss data and verify hypotheses hold. The "non-vacuity" is then: the hypothesis bundle is satisfiable with a nonzero test function / a nontrivial atlas. I'll additionally show `v ≠ 0` and `v 0 = 1` for the bumps, which rules out the degenerate `v = 0` instantiation, and I'll note the honest limitation that I do not numerically evaluate the IBP integrals (with the exception of the heat case, where I do).

Let me now design the Lean file concretely.

```lean
import Poincare.D13.ManifoldIBP.POUConstruction
import Poincare.D13.ManifoldIBP.PartialChartModelPOU
import Poincare.D13.ManifoldIBP.SmoothAtlasModel
import Poincare.D13.ManifoldIBP.Transfer
import Poincare.D13.ManifoldIBP.OverlapIBPData
import Poincare.D13.HeatKernelBridge

noncomputable section
open MeasureTheory Set Filter Metric Function
open scoped BigOperators ENNReal NNReal Topology Matrix Function ContDiff

namespace Poincare.L4.D13Audit
open Poincare.D13.ManifoldIBP
open Poincare.D12.VolumeIBP
```

### Statement capture
Use `#check @...` lines for the 8, plus `#print axioms` for each.

### Non-vacuity witnesses

**Shared bump**: 
```lean
theorem exists_bump_source0 (n : ℕ) :
    ∃ χ : Vec (n+1) → ℝ, ContDiff ℝ ∞ χ ∧ (∀ x, 0 ≤ χ x ∧ χ x ≤ 1) ∧ χ 0 = 1 ∧
      HasCompactSupport χ ∧ tsupport χ ⊆ {y : Vec (n+1) | y 0 < 1} := by
  have hK : IsCompact (closedBall (0 : Vec (n+1)) (1/2 : ℝ)) := isCompact_closedBall _ _
  have hU : IsOpen ({y : Vec (n+1) | y 0 < 1} ∩ ball (0:Vec (n+1)) 1) := ...
  have hKU : closedBall 0 (1/2) ⊆ ... := ...
  obtain ⟨χ, hsm, hrange, hone, htsupp⟩ := exists_contDiff_bump hK hU hKU
  refine ⟨χ, hsm, hrange, ?_, ?_, ?_⟩
  · have : (0 : Vec (n+1)) ∈ closedBall 0 (1/2) := by simp [norm_zero]; linarith
    exact hone 0 this
  · exact IsCompact.of_isClosed_subset (isCompact_closedBall _ _) (isClosed_tsupport _) (fun y hy => ball_subset_closedBall (htsupp hy).2)
  · exact fun y hy => (htsupp hy).1
```
Note: `0 : Vec (n+1)` works since Vec d = Fin d → ℝ has Zero. Need `mem_closedBall` with dist 0 0 = 0 ≤ 1/2.

Also need `ContDiff ℝ 2 χ` from `ContDiff ℝ ∞ χ` — use `.of_le` with `le_top`? Careful with WithTop ℕ∞ ordering: `∞ ≤ 2`? No! `ContDiff ℝ ∞` means smooth; `ContDiff ℝ 2` is *weaker*. `(2 : WithTop ℕ∞) ≤ ∞`. So `hsm.of_le (by norm_num : (2:WithTop ℕ∞) ≤ ∞)` or `hsm.of_le le_top`? For `n : WithTop ℕ∞`, `∞` = `(⊤ : ℕ∞)` coerced, and `⊤ : WithTop ℕ∞` = ω. Hmm: `le_top` gives `n ≤ ⊤` which is ω, not ∞. In Lean, `ContDiff ℝ ∞ χ` — the `∞` notation is `((⊤ : ℕ∞) : WithTop ℕ∞)`. And `2 ≤ ∞` should hold. Does `hsm.of_le (show (2 : WithTop ℕ∞) ≤ ∞ from by norm_num)` work? There might be a simp lemma. In the D13 code they do `(G.smooth i j).of_le le_top` to get `ContDiff ℝ 1` from `ContDiff ℝ ⊤`, which means `1 ≤ ⊤ = ω`. So for `∞` they'd use `of_le le_top`? If `∞ : WithTop ℕ∞` is `↑(⊤ : ℕ∞)`, is `∞ ≤ ⊤`? No! `⊤` is ω > ∞. So `le_top` wouldn't give ∞. Let me check mathlib's notation: I recall in mathlib `ContDiff 𝕜 n f` with `n : WithTop ℕ∞`; `ω` is `⊤`; `∞` is `(⊤ : ℕ∞)` coerced. The coercion `↑ : ℕ∞ → WithTop ℕ∞` preserves order, and `↑⊤ < ⊤` in WithTop. So `le_top` gives `n ≤ ω`. To get from `∞` to `2`: `(2 : WithTop ℕ∞) ≤ ∞` — true since 2 ≤ ∞ and both are in the ℕ∞ range.

I'll test with a quick Lean snippet.

Let me check how D13 code converts `ContDiff ℝ ∞` to `ContDiff ℝ 2`: in PartialChartModelPOU, `contDiff_hsStep : ContDiff ℝ ∞ (hsStep)`. They use `(hψ_sm j).continuous` etc. In POUConstruction: `A.metric b).driftLaplacian_continuous _ _ (hfc b) (huc b)` takes C². Let me find where `ContDiff ℝ ∞ → ContDiff ℝ 2` occurs: in `contDiff_hsPsi` used as `hψ_sm`. In the D12 chart theorem, `ContDiff ℝ 2` needed. Search for `of_le` in D13.

Let me just test in Lean.

**H1 instantiation**: use `halfSpaceAtlas_weightedIBP_of_cover` (already a kernel-checked instantiation) — but for independence I want to apply `globalWeightedIBP_of_cover_partial_ae` directly with the half-space atlas, or just cite that `halfSpaceAtlas_weightedIBP_of_cover` is its instantiation. The latter is kernel-checked already and reduces to hypothesis satisfiability. But the instantiation theorem `halfSpaceAtlas_weightedIBP_of_cover` is itself in the same file as the headline (same author). For an *independent* audit, the key is that the *kernel* checks the application; the proof term of `halfSpaceAtlas_weightedIBP_of_cover` contains `A.globalWeightedIBP_of_cover_partial_ae htrans 0 ...` — I can verify via `#print` that the headline is actually used. Let me do `#print Poincare.D13.ManifoldIBP.OverlapAtlas.halfSpaceAtlas_weightedIBP_of_cover` and grep for `globalWeightedIBP_of_cover_partial_ae`. That's evidence, but the proof term might be huge. Alternative: my own direct application in the audit file with concrete data.

Let me write my own direct instantiation of H1 at the half-space atlas with n=0, G = euclideanChartMetric 1, f = 0, u = 0? To make it nondegenerate, u = χ (the bump), v = another bump. But the hD/hG hypotheses need Du := G.driftLaplacian f u and Guv := G.gradInnerInverse u v; with f = 0, u = 0: Du = Δ0 = 0, Guv = ⟨∇0,∇v⟩ = 0. Trivial.
With u = v = χ: Du = Δχ, Guv = |∇χ|². The identity ∫Δχ·χ = -∫|∇χ|² — nontrivial (Dirichlet energy). And both integrands are not obviously zero. This is a good non-vacuity instance: it says ∫ (Δχ)χ = -∫|∇χ|². We know |∇χ|² ≥ 0 and ∫|∇χ|² = -∫Δχ·χ; since χ is a bump equal to 1 at 0 with values in [0,1], Δχ(0) < 0, so the identity has definite content. Even better, using H3/H2's Dirichlet variant is `halfSpaceAtlas_dirichletEnergy`.

For the *direct* instantiation of H1 with the half-space atlas, I need to discharge:
- htrans: `halfSpaceAtlas_transition_coherence`
- hfc/huc/hvc: for f=0 trivially; for u=v=χ need α: ContDiff 2 (χ ∘ chart i). For i=0 chart is id; for i≠0 chart is 2•, composition of C² χ with linear map. `χ` is `ContDiff ℝ ∞`, so `.of_le` to 2, then `.comp (contDiff_const_smul _)`.
- hvcc: HasCompactSupport (v ∘ chart 0) — from v compact support and chart 0 = id, or from tsupport ⊆ ball.
- htsupp_b: tsupport (v ∘ chart 0) ⊆ source 0 — from our bump's tsupport ⊆ {y0<1}.
- hcover: trivial with chartOf := fun j : Fin 2 => (j : ℕ) and j=0.
- hD/hG: need `∀ i y, y ∈ source i → Du (chart i y) = ...`. If I define Du := G.driftLaplacian f u and Guv := G.gradInnerInverse u v where f = 0, u = v = χ — but then Du is a function on Vec (n+1) = M (M = Vec (n+1) here). hD becomes `G.driftLaplacian 0 χ (chart i y) = (metric i).driftLaplacian (fun z => 0) (fun z => χ (chart i z)) y` for y ∈ source i. For i = 0: rfl after rewriting metric 0 = G and chart 0 = id. For i ≠ 0: need `G.driftLaplacian 0 χ (2•y) = (dilateMetric G 2).driftLaplacian 0 (χ ∘ 2•) y` — that's `dilateMetric_driftLaplacian` (with the correct direction). Similarly hG via `dilateMetric_gradInnerInverse`. These lemmas exist in D12 (used in POUConstruction lines 311, 322). So a direct instantiation is feasible by copying the pattern from `halfSpaceAtlas_weightedIBP_of_cover`, but with my own f, u, v.

Actually, simpler and equally valid: instantiate `globalWeightedIBP_of_cover_partial_ae` at the **half-space atlas with base chart 0 and data f = 0, u = χ, v = χ**, by reusing the existing `halfSpaceAtlas_weightedIBP_of_cover`? No — that theorem already has f, u, v universally quantified! `halfSpaceAtlas_weightedIBP_of_cover (G) (f u v) (hf hu hv) (hvc) (hvsupp)`. So I can simply *apply* `halfSpaceAtlas_weightedIBP_of_cover` with my concrete f=0, u=χ, v=χ. That is a kernel-checked instantiation of the headline H1 at the model atlas (because `halfSpaceAtlas_weightedIBP_of_cover`'s proof term literally applies the headline). Even more directly: since `halfSpaceAtlas_weightedIBP_of_cover` is proved *from* H1, instantiating it at concrete data is an instantiation of H1. I'll do both: apply `halfSpaceAtlas_weightedIBP_of_cover` with concrete data (which yields the Dirichlet-type identity), AND `#print` the theorem to confirm the headline appears in its proof term.

Good — this is cheap and rigorous.

**H2 instantiation**: apply `halfSpaceAtlas_weightedIBP_unconditional` at G := euclideanChartMetric (n+1), f := 0, u := χ, v := χ (with χ the bump). Need ContDiff 2 χ etc. Also `HasCompactSupport χ`, `tsupport χ ⊆ {y0<1}`.

**H3 instantiation**: apply `halfSpaceAtlas_greenIdentity` with F = 0, U = χ₁, V = χ₂ (two different bumps: one with K = closedBall 0 1/2, another with K = closedBall (some point) ...). Let me construct two bumps χ₁, χ₂ with distinct supports. E.g. χ₁ around 0; χ₂ around (1/4) e₀? Need both tsupport in {y0<1}. Use K = closedBall (c) r with c chosen so closedBall ⊆ {y0<1} ∩ ball 0 1. Let c = (1/4) • e₀. Hmm, but then χ₁, χ₂ both equal 1 near their centers, and the identity is ∫ΔU·V = ∫U·ΔV, nontrivial for distinct bumps. Good.

**H4 instantiation**: apply `halfSpaceAtlas_laplacianIntegralZero` with V := χ.

**H5 instantiation**: apply `dilationAtlasTwo_weightedIBP_via_pou` with concrete ψ. Need ψ : Fin 2 → Vec(n+1) → ℝ satisfying hψ_sm, hψ_supp, hψ_sum on tsupport V, and the integrability hypotheses hintL/hintR. The integrability hypotheses are the annoying part. Could take V = 0 (then tsupport V = ∅ and hintL/hintR become integrability of zero, easy: `integrable_zero`). But V = 0 is degenerate. Alternatively take ψ_i = 1/2 constants: hψ_sm ✓, hψ_supp needs tsupport (const 1/2) = univ ⊆ ball 0 (R+1) — false. So need ψ_i compactly supported with sum 1 on tsupport V. Take ψ_0 = χ (bump), ψ_1 = 1 - χ: then sum = 1 everywhere, but tsupport ψ_1 = univ (since 1-χ = 1 outside tsupport χ) — not in a ball. Hmm. Instead: since the POU is over the *dilation atlas* whose charts cover everything (total), and hψ_supp just needs each ψ_i supported in a ball of radius R+1. So take ψ_0 = χ, ψ_1 = χ' where χ + χ' = 1 on tsupport V. Construct χ, χ' both compactly supported with sum 1 on a compact K containing tsupport V: use exists_contDiff_bump: choose χ with support in ball 0 (R+1) and χ = 1 on tsupport V; then ψ_1 = 0. That works: ψ_0 = χ, ψ_1 = 0, sum = 1 on tsupport V, both smooth, supports in ball 0 (R+1) (for ψ_1, tsupport 0 = ∅). Then hintL and hintR: the pieces are pouPiece 0 V χ and pouPairing 0 j U V (ψ j) — integrability needs to be established. With ψ_1 = 0 the pairing is 0; with ψ_0 = χ, the piece is the lift of χ·V. Integrability of `G.driftLaplacian F U * pouPiece 0 V χ` against the weighted measure — I can discharge using the D13 `integrable_globalMeasure_withDensity_of_supported` as in the proofs, but that's work.

Given time, maybe instantiate H5 with U = 0, V = χ: then hintL is integrable of (Δ0 * piece) = 0; hintR integrand is `pouPairing 0 j 0 V ψ` = gradInnerInverse of (0, V) = 0 pointwise? `pouPairing b i u v ψ := lift b (ψ * ?) ...` hmm. Let me look at the definition of pouPairing. If u = 0 then pairing = 0 and integrability is easy. But then conclusion is 0 = -0 (LHS ∫ Δ0·V = 0; RHS ∫0 = 0). Degenerate content.

Alternatively use U = V = χ (the bump): then hintL/hintR integrability. The RHS integrand is `pouPairing 0 j U V ψ_j` = lift of (ψ_j * gradInnerInverse U V) for j=0 and 0 for j=1. Since ψ_0 = χ has compact support and gradInnerInverse χ χ is supported in tsupport χ (compact) with continuous chart expression, integrability follows. I could prove it with `integrable_globalMeasure_withDensity_of_supported` like the existing proof. That's maybe 30 lines of Lean. Feasible but time-consuming.

Given the audit nature, I could instead demonstrate non-vacuity of H5's *hypotheses* by referencing the existing `dilationAtlasTwoData_weightedIBP` (a consumed consequence of H6, different theorem) — no.

Hmm wait — the D13 card claims H5 is a headline in the sense of "the general global IBP theorem consumed on the dilation model". Its hypotheses are POU + integrability. A cheap non-vacuity instantiation: take **R large and V = 0**? Degenerate.

Alternative: take U = 0 but V = χ. Then conclusion is ∫ 0 = -∫ pouPairing(...0, χ...) . What is pouPairing with u=0? `pouPairing b i u v ψ = lift b (fun y => ψ y * (metric i).gradInnerInverse (u∘chart i) (v∘chart i) y)`. With u = 0: gradInnerInverse 0 v = Σ g⁻¹ ∂0 ∂v = 0. So RHS integrand is 0. Conclusion 0 = -0. Not contentful, but the *hypotheses* hintL/hintR are discharged non-trivially? Not really.

OK here's another thought: maybe I can use the *existing* `dilationAtlasTwoData_weightedIBP` as the non-vacuity witness for H5's sibling H6, and for H5 simply check the statement and note that H5 is a conditional interface whose POU+integrability hypotheses are *satisfiable* — exhibiting a concrete instantiation where ψ is a genuine bump and V is the Gaussian-like? The integrability of the pieces could be discharged by `integrable_globalMeasure_withDensity_of_supported`... 

Let me look at whether there's an existing theorem that instantiates `dilationAtlasTwo_weightedIBP_via_pou` with concrete ψ. Search for callers.

Let me also reconsider the time budget. I've used maybe 50 minutes. I have ~3h. The Lean probe with 8 instantiations will take maybe 1-1.5h of iteration. Then the ledger + report ~45 min. OK, feasible. Let me be pragmatic: aim for kernel-checked instantiations for all 8; where a hypothesis bundle is heavy (H5 integrability), use the existing caller if any, else construct.

Let me check callers of dilationAtlasTwo_weightedIBP_via_pou and of manifoldWeightedIBP_of_atlasData.
dsh: reasoning:
There's `Poincare/L4/ManifoldIBP/D13HeadlineAudit.lean` in the leader's release! Let me read it — it might already contain headline audit probes and non-vacuity instantiations that I can verify/reuse, or it might be exactly the "D13 headline layer" audit. My task is independent, so I should write my own probe, but reading it helps me understand intent and avoid duplication.

Let me read it.
dsh: reasoning:
This existing file is an axiom audit only (8 headline axiom prints). My task goes further: semantic audit + non-vacuity instantiations.

Now let me set up my probe. Let me first check the compile environment and a few Lean-side details with a scratch file:
1. `ContDiff ℝ ∞ χ → ContDiff ℝ 2 χ`.
2. `#print axioms` for the 8.
3. `IsCompact.of_isClosed_subset` usage.
4. exact full statements via `#check @`.

Let me write the initial probe file with the statement captures and axiom prints, compile it, then iterate adding the instantiations.

Also note: the leader's release `Poincare/L4/ManifoldIBP/D13HeadlineAudit.lean` exists — its olean presumably too. My audit is separate.

Let me also check the toolchain/lean path details: my worktree release/.lake/packages symlink points to D6_weekly_release's packages (mathlib). The leader's build lib has oleans compiled with the same toolchain? The leader's toolchain is v4.34.0-rc2, same as mine. Let me confirm my release lake-manifest matches the leader's. Also confirm mathlib rev. My audit will use the leader's oleans (Poincare.*) and D6_weekly_release mathlib oleans. Are the leader's oleans built against the same mathlib? The leader's .lake/packages is its own symlink; let me check if it points to the same mathlib revision. If the leader's oleans depend on mathlib oleans from a *different* directory with the same content, LEAN_PATH must include both: my lake env LEAN_PATH gives mathlib from my packages; the leader's Poincare oleans reference mathlib constants by name, which will resolve from my mathlib oleans if versions match. If the mathlib revisions differ, we'd get errors or silently wrong behavior. Let me compare lake-manifest.json of both.

Let me do that quickly, then write the probe.
dsh: reasoning:
Same mathlib packages (both point to D6_weekly_release/release/.lake/packages). 

Now let me write the audit probe. I'll do it in stages:

Stage A: statement capture + axiom prints + `#print` of key definitions. Compile. 
Stage B: add non-vacuity witnesses.

Let me create the file step by step. First write a scratch test to resolve syntax questions (ContDiff ∞ → 2, `(⊤ : WithTop ℕ∞) = ω`).

Let me check what `Vec` is: `Poincare.D12.VolumeIBP.Vec d := Fin d → ℝ` presumably. In my file, `open Poincare.D12.VolumeIBP` and `Vec` should resolve.

Let me write the scratch.
dsh: reasoning:
The heredoc failed because the directory didn't exist when the redirect happened (mkdir came after). Let me create the dir first.

Note: `lake env lean -R audit_src` — but the root flag: I should double check that `-R` works and that imports resolve correctly. Earlier the POUConstruction compile worked (it found imports via LEAN_PATH). Let me redo.
dsh: reasoning:
Just need `open Metric`. Also the first three examples passed (no errors reported for them). Let me fix.

Then I'll write the full probe.

Let me now carefully write the audit file. Plan the content:

```lean
/-
  L4 D13 semantic audit probe (independent)
-/
import Poincare.D13.ManifoldIBP.POUConstruction
import Poincare.D13.ManifoldIBP.PartialChartModelPOU
import Poincare.D13.ManifoldIBP.SmoothAtlasModel
import Poincare.D13.ManifoldIBP.Transfer
import Poincare.D13.ManifoldIBP.OverlapIBPData
import Poincare.D13.HeatKernelBridge

noncomputable section
open MeasureTheory Set Filter Metric Function
open scoped BigOperators ENNReal NNReal Topology Matrix Function ContDiff

namespace Poincare.L4.D13Audit
open Poincare.D12.VolumeIBP
open Poincare.D13.ManifoldIBP
```

1) Statement capture with `#check @` (8 lines) — output is the exact elaborated type.

2) Full-body `#print axioms` (8 lines).

3) Bump constructor:
```lean
/-- A concrete nonzero smooth bump supported in the base half-space source. -/
theorem exists_bump_halfSpace (n : ℕ) :
    ∃ χ : Vec (n + 1) → ℝ, ContDiff ℝ ∞ χ ∧ (∀ x, 0 ≤ χ x ∧ χ x ≤ 1) ∧ χ 0 = 1 ∧
      HasCompactSupport χ ∧ tsupport χ ⊆ {y : Vec (n + 1) | y 0 < 1} := by
  have hK : IsCompact (closedBall (0 : Vec (n + 1)) (1 / 2 : ℝ)) := isCompact_closedBall _ _
  have hU : IsOpen ({y : Vec (n + 1) | y 0 < 1} ∩ ball (0 : Vec (n + 1)) 1) :=
    (isOpen_lt (continuous_apply (0 : Fin (n + 1))) continuous_const).inter isOpen_ball
  have hKU : closedBall (0 : Vec (n + 1)) (1 / 2 : ℝ) ⊆
      {y : Vec (n + 1) | y 0 < 1} ∩ ball (0 : Vec (n + 1)) 1 := by
    intro y hy
    have h1 : dist y 0 ≤ 1 / 2 := by simpa [dist_eq_norm] using hy
    constructor
    · have : |y 0| ≤ ‖y‖ := by ... -- need coordinate bound
```

Hmm, the y 0 < 1 part: if dist y 0 ≤ 1/2 then ‖y‖ ≤ 1/2 so |y 0| ≤ 1/2 < 1. There is a lemma `PiLp.norm_apply_le` or for `Vec d = Fin d → ℝ` with sup norm: `norm_le_pi_norm`? `‖y 0‖ ≤ ‖y‖` is `norm_apply_le` maybe. Let me search for the norm on Vec: D12 defines `Vec d := Fin d → ℝ` with the sup norm instance from Pi types. Lemma `norm_le_pi_norm : ‖f i‖ ≤ ‖f‖` exists for Pi types with sup norm. Let me test.

Simpler alternative to avoid the norm bound: choose K as the closed ball of radius 1/2 in the *product sup norm* and U = {y | y 0 < 1} ∩ ball 0 1 — the containment still needs the coordinate bound. Alternatively choose U = {y | y 0 < 1} and K = closedBall 0 (1/2) and prove with `norm_le_pi_norm`.

Or choose K to be a smaller explicit compact set: K = {y | y 0 ≤ 1/2 ∧ ∀ i≠0, y i = 0}? Compact as an image of a closed interval; more work.

Let me use `norm_le_pi_norm`. Test.

4) H2 instantiation:
```lean
theorem audit_halfSpaceAtlas_weightedIBP_unconditional_nonvacuous :
    ∃ (χ : Vec 1 → ℝ) (hχ : ContDiff ℝ ∞ χ) ... 
```
Simpler: a theorem that applies the headline:
```lean
theorem h2_instantiated :
    ∫ m, (ChartMetric.euclideanChartMetric 1).driftLaplacian 0 χ m * χ m
      ∂(((hsAtlas (n := 0) (ChartMetric.euclideanChartMetric 1)).globalMeasure volume).withDensity
        ((hsAtlas (n := 0) (ChartMetric.euclideanChartMetric 1)).weight 0))
    = -∫ m, (ChartMetric.euclideanChartMetric 1).gradInnerInverse χ χ m ∂(...) :=
  halfSpaceAtlas_weightedIBP_unconditional (n := 0) _ 0 χ χ (contDiff_zero ...) hχ2 hχ2 hχc hχsupp
```
Here `Vec 1 = Fin 1 → ℝ`, n=0 so n+1 = 1.

Need `ContDiff ℝ 2 (fun _ : Vec 1 => (0:ℝ))` = contDiff_const. `ContDiff ℝ 2 χ` from hχ.of_le. `HasCompactSupport χ`, `tsupport χ ⊆ {y | y 0 < 1}`.

5) H1 instantiation: use `halfSpaceAtlas_weightedIBP_of_cover` with concrete f=0, u=χ, v=χ; plus `#print` showing the headline in the proof term? Actually simpler: I can apply `globalWeightedIBP_of_cover_partial_ae` directly by copying the pattern? No — just apply `halfSpaceAtlas_weightedIBP_of_cover` which is the headline applied. That's a kernel-checked instantiation: the conclusion is an instance of H1's conclusion, obtained via H1. To be more explicit, I'll cite both: (a) `#print halfSpaceAtlas_weightedIBP_of_cover` shows `globalWeightedIBP_of_cover_partial_ae` is used; (b) my concrete instance.

Even better: I can directly instantiate H1 with the *dilation* atlas? H1 requires `htrans`, `hproper`, `hbd` — the half-space model satisfies all. Simplest is to reuse the existing cover theorem.

Actually, let me directly call H1 in my audit file using the hsAtlas with f=0,u=v=χ and *discharge* the hypotheses by calling the same helper lemmas. It's ~40 lines but gives a truly independent instantiation (my own proof term applying H1). The hD/hG require the dilateMetric lemmas. Let me see the exact form needed:

hD : ∀ i y, y ∈ A.source i →
      Du (A.chart i y) = (A.metric i).driftLaplacian (fun z => f (A.chart i z)) (fun z => u (A.chart i z)) y
with Du := fun m => (G.driftLaplacian f u) m? In `halfSpaceAtlas_weightedIBP_of_cover` they pass `Du := G.driftLaplacian f u`, and prove hD as `G.driftLaplacian f u (A.chart i y) = (A.metric i).driftLaplacian ...`. With f = 0, u = χ:
- i = 0: chart 0 = id, metric 0 = G → rfl.
- i ≠ 0: chart i = 2•, metric i = dilateMetric G 2 → need `dilateMetric_driftLaplacian G 2 h2 f u hf hu y : (dilateMetric G 2 h2).driftLaplacian (f ∘ 2•) (u ∘ 2•) y = G.driftLaplacian f u (2•y)` (in the .symm direction). So hD: `G.driftLaplacian f u (2•y) = (dilateMetric G 2).driftLaplacian (0) (χ ∘ 2•) y` — from the lemma's symm with f := 0 and u := χ: `(dilateMetric_driftLaplacian G 2 h2 0 χ hf hu y).symm` where the lemma states `(dilateMetric G 2 h2).driftLaplacian (fun z => 0 (2•z)) (fun z => χ (2•z)) y = G.driftLaplacian 0 χ (2•y)`. Good.

Similarly hG via `dilateMetric_gradInnerInverse G 2 h2 u v hu hv y` requiring `Differentiable` hypotheses: in POUConstruction they use `(hu.differentiable (by simp))` and `(hv.differentiable (by simp))` where hu : ContDiff ℝ 2 u. I'd pass `(hχ2.differentiable (by simp))`.

This is doable. But is it worth 40 lines vs. just applying the existing cover theorem? The existing cover theorem's proof term *is* the H1 application, so applying it is a legitimate kernel-checked instantiation of H1 (transitively). The task says "exhibiting at least one kernel-checked instantiation per headline (the D13 model atlases)". Since `halfSpaceAtlas_weightedIBP_of_cover` is literally proved by `exact A.globalWeightedIBP_of_cover_partial_ae ...`, and I'll verify by `#print` that the proof term contains the headline, applying the cover theorem is fine. But to be robust and independent, I'll do a direct application for H1 in my own file. Let me try; if it takes too long, fall back.

Hmm — actually there's a subtlety: H1's conclusion with f=0, u=χ, v=χ gives ∫ Δχ·χ = -∫|∇χ|², which is exactly the Dirichlet energy. Good, contentful.

6) H3 green identity: apply with F=0, U=χ1, V=χ2.

7) H4 laplacianIntegralZero: apply with V=χ.

8) H5 dilationAtlasTwo_weightedIBP_via_pou: need ψ and hintL/hintR. Let me think about the easiest nondegenerate instantiation:
Take n=0 (Vec 1), G = euclideanChartMetric 1, F = 0, U = 0, V = χ, ψ_0 = χ, ψ_1 = 0? Then:
- hψ_sm ✓ (ContDiff χ, contDiff_zero)
- hψ_supp: tsupport χ ⊆ ball 0 (R+1) for some R. From HasCompactSupport χ we get bounded support → ∃R, tsupport χ ⊆ closedBall 0 R ⊆ ball 0 (R+1). Fine.
- hψ_sum: on tsupport V = tsupport χ, χ x + 0 = 1? But χ is a bump = 1 only on K, not on all of tsupport χ. Take ψ_0 = 1 on tsupport V; since tsupport V is compact, `exists_contDiff_bump` with K = tsupport V, U = ball 0 (R+1) gives ψ_0 = 1 on tsupport V and tsupport ψ_0 ⊆ U. Then ψ_1 = 0. Sum = 1 on tsupport V ✓.
- hintL: ∀ j, Integrable (fun m => G.driftLaplacian F U m * pouPiece 0 V (ψ j) m) μ. With U = 0: driftLaplacian 0 0 = 0 → integrand 0 → integrable_zero ✓.
- hintR: pouPairing 0 j 0 V ψ_j = 0 → integrable_zero ✓.
Conclusion: ∫ 0 * V dμ = -∫ 0 dμ → 0 = -0. Degenerate content but a legitimate instantiation showing hypothesis satisfiability.

Better: U := χ (same as V). Then:
- hintL: Integrable (fun m => G.driftLaplacian 0 χ m * pouPiece 0 χ ψ_j m) μ.
  - j = 1: pouPiece 0 χ 0 = lift of 0 = 0 → integrand 0 → integrable.
  - j = 0: pouPiece 0 χ ψ_0 where ψ_0 = 1 near tsupport χ... The product ψ_0 * (χ ∘ chart 0) is compactly supported (χ compact) and smooth; the lift is supported in chart 0 image; integrability can be shown by `integrable_globalMeasure_withDensity_of_supported`. But that requires: measurability of driftLaplacian*piece, support in chart image, and integrability of the chart expression against the reference measure — i.e. exactly the pattern in `halfSpaceAtlas_integrable_dirichlet`. Writing this generically for the dilation atlas requires its own support lemmas (`support_lift_subset`, `pouPiece`...). Doable but maybe 40+ lines.

Alternative to make hintL/hintR easy while keeping U,V nonzero: choose ψ_0 = 0 and ψ_1 = ... no, sum must be 1 on tsupport V.

Hmm, what if V has compact support and we choose ψ_0 = bump = 1 on tsupport V, ψ_1 = 0. For hintL with U := V := χ: the integrand is Δχ · lift(ψ_0 * χ). Since ψ_0 * χ is compactly supported and smooth, the lift is supported in chart 0 image (dilationAtlasTwoSmooth chart 0 = id since image 0 = univ). Integrability: use `OverlapAtlas.integrable_globalMeasure_withDensity_of_supported` — I need the statement. Let me read IntegrableTransfer.lean line ~100.

Actually, I could circumvent by using the *existing* generic theorem at the dilation atlas: is there a theorem `dilationAtlasTwo_integrable...`? Let me look at SmoothAtlasModel.lean lines 190-253 for the tail of the proof of `dilationAtlasTwo_weightedIBP_via_pou` — it discharges the same things via the POU construction. Hmm no, it passes hintL/hintR as hypotheses (that's the point of the theorem) — wait, re-reading: `dilationAtlasTwo_weightedIBP_via_pou` has hintL/hintR as hypotheses. So there's no concrete instantiation in-tree.

Let me check DisjointModel.lean:254 — `manifoldWeightedIBP_of_atlasData (disjointAtlasData D f hm)` — that's H6 instantiated (disjoint model). Let me read that part; it might discharge hg/hg' or take them as hypotheses too.

Let me read the tail of DisjointModel and IntegrableTransfer to decide.

Actually, for H5's non-vacuity, maybe the cleanest is: take n=0, F=0, U=0, V=χ (nonzero), ψ_0 = bump = 1 on tsupport χ, ψ_1 = 0. Then hintL/hintR are trivial (zero integrands) since U = 0 makes both integrands zero:
- hintL integrand: driftLaplacian F U = driftLaplacian 0 0 = 0.
- hintR integrand: pouPairing 0 j U V ψ_j = lift (gradInnerInverse (fun _ => 0) ...) = lift of 0 = 0? gradInnerInverse of u=0 is zero; the lift of the zero function is zero. So integrable_zero.
Conclusion: ∫ 0·V = -∫ 0, i.e. 0 = 0 — content-degenerate but hypothesis-satisfiable with V ≠ 0.

To get content, U := χ and discharge hintL/hintR with the transfer lemma. Let me look at its statement and see how hard.

Let me read IntegrableTransfer.lean.
dsh: reasoning:
There's a good non-vacuity witness for H6: `disjointAtlas_weightedIBP` — full instantiation of H6 with integrability discharged, on the disjoint atlas model with chartwise C² data. I can instantiate that further with concrete data (e.g. ι=1, f=0, u=v=χ) — but `v : Fin ι → Vec(n+1) → ℝ` and needs compact support. Applying `disjointAtlas_weightedIBP` with u=v=χ is easy.

For H5, I'll write the instantiation with U = V = χ and discharge hintL/hintR using `integrable_globalMeasure_withDensity_of_supported` at the dilation atlas (i=0, chart 0 = id, image = univ). Actually since the dilation atlas is total with chart 0 = id, the support condition is automatic. The integrability reduces to: Integrable (fun y => exp(-F y) * (Δχ y * lift(ψ*χ) y) * density y) volume. With F = 0 and χ smooth compact support, and lift(chart 0) = the function itself on the image (chart 0 = id, source = univ). So the chart expression is continuous compactly supported → integrable. This is basically what `halfSpaceAtlas_integrable_dirichlet` does for the half-space atlas. Doing it for the dilation atlas requires knowing `dilationAtlasTwoSmooth`.chart 0 = id and globalMeasure = chartMeasure 0. Hmm.

Alternatively, I can make hintL/hintR trivially integrable by choosing ψ_0 = 0? No, sum = 1 on tsupport V requires something.

New idea: choose V := 0 as well. Then tsupport V = ∅, and hψ_sum is vacuous; choose ψ_0 = 0, ψ_1 = 0. All integrability trivial. But fully degenerate.

Middle ground: U := 0, V := χ, ψ_0 = bump = 1 on tsupport χ, ψ_1 = 0. Then hintL/hintR integrands vanish because U = 0, so they're discharged by `integrable_zero`. The conclusion is 0 = -0. Hmm, still degenerate conclusion-wise but V ≠ 0 and ψ_0 is a genuine bump. It shows the hypothesis bundle is satisfiable in a nonempty-support way; the conclusion is trivial only because U = 0.

Hmm, what about U := 1 (constant)? Then Δ_F U = Δ1 - ⟨∇f,∇1⟩ = 0 (laplacian of constant = 0). Same.

What if U := V := χ and I just take the *pairing* term: hintR integrand is `pouPairing 0 j U V ψ_j`, which for j=0 is lift of gradInnerInverse U (ψ_0·V). Nonzero. But to prove integrability I'd need real work.

Let me estimate the work for H5 with U=V=χ:
Using `integrable_globalMeasure_withDensity_of_supported` at i=0 for the dilation atlas:
- `hf : Measurable F` (F=0), `hg : Measurable g` where g = fun m => driftLaplacian F U m * pouPiece 0 V ψ_j m. Need measurability of g: driftLaplacian 0 χ is continuous (lemma `driftLaplacian_continuous`), pouPiece is a lift of a measurable function... `measurable_pouPiece` exists (used in POUConstruction line 111: `A.measurable_pouPiece b hv (hψ_sm j)`). For hintR: `A.measurable_pouPairing`.
- `hsupp : ∀ m, g m ≠ 0 → m ∈ chart 0 '' source 0` — for the dilation atlas chart 0 = id and source 0 = univ, so trivial (`⟨m, mem_univ m, rfl⟩`).
- `hint : Integrable (fun y => exp(-(F (chart 0 y))) * g (chart 0 y) * density 0 y) volume` — chart 0 = id, density 0 = G.density, so it's `fun y => exp(-0) * (Δχ y * pouPiece 0 χ ψ_j y) * G.density y`. Now pouPiece 0 χ ψ_j (y) = lift 0 (ψ_j * (χ ∘ chart 0)) (chart 0 y) = ψ_j y * χ y when y ∈ source 0 = univ. So the chart expression is continuous and compactly supported (product of ψ_j compact support and χ compact support) → integrable. 

But I'd need to unfold `lift` and `pouPiece` and use `lift_apply_chart` — via `A.lift_apply_chart` with hy : y ∈ source 0 (mem_univ). And chart 0 = id for dilationAtlasTwoSmooth. That's several rewrites.

Total maybe 30-50 lines with iteration. It's doable, but is it the best use of time? The audit's core deliverable is verdicts + evidence. Non-vacuity must be kernel-checked per headline. I'll do the full U=V=χ instantiation for H5 if time permits; otherwise fall back to the U=0 + nonzero V instantiation and report honestly that the H5 instantiation has trivial conclusion content but nontrivial hypothesis satisfiability.

Actually — wait. Much better idea for H5: use the **existing** `dilationAtlasTwoData_weightedIBP` as an *independent route*? No, that's H6.

Alternatively, can I get a contentful H5 instantiation by taking **V := χ and ψ_0 := the POU**, and note that `hintL`/`hintR` are *hypotheses* which are exactly the interface conditions. A non-vacuity witness with U=0 shows satisfiability. I think it's acceptable but weaker. Let me try the full version, timeboxed.

Now H7/H8: 
- H7: `finiteLifetimeEntropyBridge_gaussian 1 (1/2) (by norm_num)` gives a `FiniteLifetimeEntropyBridge` instance. 
- H8: `monotoneOn_F_gaussian 1 (1/2) (by norm_num)` gives MonotoneOn on Icc 0 (1/2). Plus numeric: `(gaussEntropyData 1 (1/2) h (1/4)).F = 4` via `F_gaussEntropyData`. And `F(0) = 1`, `F(1/2) = 2`, so `1 ≤ 2`. Also `F_gauss_mono` gives 1/(1-s) ≤ 1/(1-t).

Also for H7 I can show the bridge is *not* vacuous by deriving from it `monotoneOn_of_bridge` (already H8).

Let me also verify the *domain* claims:
- `finiteLifetimeEntropyBridge_gaussian` requires ht1 : t1 < τ0; if t1 ≥ τ0 the statement doesn't apply. Note: the card says the lifetime is [0, t1] with τ(t) = τ0 - t > 0 — requires t1 < τ0. Fine.
- Non-vacuity of `f_derivative` field requires t1 > 0 to be non-vacuous; with t1 = 1/2 > 0 the interval Ioo 0 (1/2) is nonempty. I can witness a specific point: `(finiteLifetimeEntropyBridge_gaussian 1 (1/2) h).f_derivative (1/4) ⟨by norm_num, by norm_num⟩` gives `HasDerivAt ... (FDissipation ...) (1/4)`. Good.

Now let me also prepare the *defect hunt* evidence:
1. Check `#print axioms` for all 8 → expect [propext, Classical.choice, Quot.sound].
2. Check no `sorryAx`.
3. Check the "hidden interface fields" list for H6 by `#print ManifoldAtlasData` — extract the fields.
4. Check the claim "ContDiff ℝ ⊤ = analytic" → verify `(⊤ : WithTop ℕ∞) = ω` (rfl) and that `ContDiff ℝ ω` is analyticity. I can `#check @ContDiff` / `#check contDiff_analyticAt`? Let me check mathlib: `ContDiff 𝕜 ω f ↔ Analytic 𝕜 f`? There's `ContDiff.analyticAt` requiring `n = ω`. Yes: `ContDiff.analyticAt (hn : n = ω)`? Actually the lemma is `AnalyticAt` from `ContDiff ℝ ω`. I'll just verify the type of `ContDiff`'s n argument and that `ω` is top. The card's claim "(= analytic)" is then correct.
5. Check `SmoothChartMetric` exists with `ContDiff ℝ ∞`. Yes (ChartMetricBridge.lean:134).
6. Check the D13 card's claim about `hintL`/`hintR` (line 200) — mild inaccuracy.
7. Check the "I4-residual defects" claim: `Bridge.RealCalculus.unrestrictedWeightedIBPStatement_false` exists. Let me verify by grep/compile.
8. Check `dilationAtlasTwo_laplacianIntegralZero` exists (mentioned in card line 203). Let me grep. If it doesn't exist, that's a card inaccuracy.

Let me grep for these.
dsh: reasoning:
Good, all card-claimed declarations exist. Now let me write the audit probe. I'll build it incrementally.

First, checkpoint.json — I'm ~1h in. Let me write it now with what's established, then continue.

Actually let me first write the probe skeleton and get statement captures compiled, then checkpoint.

Let me write the main file now. I'll start with statement capture + axioms + bump + H2/H3/H4/H7/H8 instantiations (easy ones), then add H1/H5/H6.

For H1, let me check whether applying `halfSpaceAtlas_weightedIBP_of_cover` suffices. To be more independent I'd rather construct my own. But time. Compromise: I'll write my own direct application of H1 using the same hypothesis-discharge pattern, because it also serves as a "semantic audit" check of the hypothesis list (I'd be literally discharging each one). Let me try; it's the best evidence. If it takes >40 min, fall back.

Actually — a cheaper independent instantiation of H1: apply H1 at the **dilation atlas** (`dilationAtlasTwoSmooth`)? It's total; hypotheses:
- htrans: trivially true since sources are univ (transition_mem gives mem_univ).
- hcover: trivial (univ ⊆ ⋃ ...) since cover = univ.
- hproper: need compact preimages of compact under dilation transitions — there's `dilationAtlasTwo_transition_preimage_isCompact`? Let me check... Actually `dilationAtlasTwoSmooth` transition maps are identity/dilations; there might be a lemma. Let me grep SmoothAtlasModel for `preimage_isCompact`.
- hbd: frontier univ = ∅, volume 0.
- hvsupp/htsupp_b: sources are univ, so trivial.
- hfc/huc/hvc: same as before.
- hD/hG: same dilation lemmas.
This is a *total* atlas though; the headline is for partial charts with null boundaries. Using it on the total atlas is still a legitimate instantiation (the theorem doesn't require partiality). And there's already `dilationAtlasTwo_weightedIBP_via_pou` (H5) using a different engine.

Simplest of all for H1: apply the existing `halfSpaceAtlas_weightedIBP_of_cover` with concrete nonzero data — one line. And separately, verify with `#print` that its proof term uses H1. Let me do that, plus for extra rigor, write my own direct application at the half-space atlas. Let me just attempt the direct application; the pattern is available in POUConstruction lines 262-352, so I can adapt with minimal thought.

Given H2 is already the unconditional half-space theorem (which subsumes the same conclusion), my H1 direct instantiation would produce the same statement as H2's instance. That's fine.

Let me now write the file. I'll write it in one go and iterate on errors.

Key details:
- `A.chart`, `A.source` are fields; for `hsAtlas G`, `(hsAtlas (n:=n) G).chart i` etc.
- For H1 direct application, I need to name `A := hsAtlas (n:=0) G` with G := euclideanChartMetric 1. Then `Vec (0+1) = Vec 1`. Note: H1 is stated with `{n : ℕ}` and `A : SmoothOverlapAtlas M (n+1)`. So for n=0, `A : SmoothOverlapAtlas (Vec 1) 1`. `hsAtlas (n := 0) G : SmoothOverlapAtlas (Vec (0+1)) (0+1)`. Since `0+1` defeq `1`, fine.

Let me write the direct H1 instantiation with:
- htrans := halfSpaceAtlas_transition_coherence (n := 0) G
- b := 0
- ι := Fin 2, chartOf := fun j => (j : ℕ)
- f := fun _ => 0, u := χ, v := χ, Du := G.driftLaplacian (fun _ => 0) χ, Guv := G.gradInnerInverse χ χ
- hf := measurable_const, hv := hχ2.continuous.measurable, hDu := (G.driftLaplacian_continuous _ _ contDiff_const hχ2).measurable, hGuv := (G.gradInnerInverse_continuous χ χ hχ2 hχ2).measurable
- hvsupp: ∀ m, χ m ≠ 0 → m ∈ chart 0 '' source 0 = univ. `⟨m, trivial, rfl?⟩`: chart 0 = id so `A.chart 0 m = m` — need to show `(1:ℝ) • m = m`. In the cover proof they did: `refine ⟨m, ?_, ?_⟩; · rw [hsource0]; exact ...; · rw [hchart0]; simp`. I'll mirror.
- hfc: ∀ i, ContDiff ℝ 2 (fun y => 0) := fun i => contDiff_const
- huc/hvc: ∀ i, ContDiff ℝ 2 (fun y => χ (A.chart i y)) — case i=0: chart = id → simpa using hχ2; else chart = 2• → hχ2.comp (contDiff_const_smul _).
- hvcc: HasCompactSupport (fun y => χ (A.chart 0 y)): chart 0 = id → from hχc via `hχc.comp_...`? Simplest: `by rwa [hchart0_fun]` like in the cover proof where hchart0_fun : (fun y => v (A.chart 0 y)) = v. For χ: `have hchart0_fun : (fun y => χ (A.chart 0 y)) = χ := by funext y; rw [hchart0]; simp` then `rw [hchart0_fun]; exact hχc`.
- htsupp_b: `by rw [hchart0_fun]; exact hχsupp`.
- hcover: `fun y hy => mem_iUnion.mpr ⟨0, hy⟩` — careful: needs `A.chart 0 '' A.source 0 ⊆ ⋃ j : Fin 2, ...`, and `hy : y ∈ A.chart 0 '' A.source 0`. `mem_iUnion.mpr ⟨0, hy⟩` works since (0 : Fin 2) casts to 0.
- hD: ∀ i y, y ∈ source i → (G.driftLaplacian 0 χ) (A.chart i y) = (A.metric i).driftLaplacian (fun z => 0) (fun z => χ (A.chart i z)) y. Note `fun z => f (A.chart i z)` with f = 0 is `fun z => 0`. Case i=0: rw [hchart0, hmetric0]; simp. Case i≠0: rw [hchart_ne hi, hmetric_ne hi]; exact (dilateMetric_driftLaplacian G 2 h2 0 χ contDiff_const hχ2 y).symm.
   Careful with the direction of `dilateMetric_driftLaplacian`. In POUConstruction line 311: `exact (dilateMetric_driftLaplacian G 2 (by norm_num) f u hf hu y).symm` for goal `G.driftLaplacian f u (A.chart i y) = (A.metric i).driftLaplacian ...`. So the lemma states `(dilateMetric G 2 h2).driftLaplacian (f ∘ 2•) (u ∘ 2•) y = G.driftLaplacian f u (2•y)`. Good.
   But here `(fun z => χ (A.chart i z))` needs to be syntactically `fun z => χ ((2:ℝ) • z)`. After `rw [hchart_ne hi]` it becomes that. And `(A.metric i)` becomes `dilateMetric G 2 h2`. The lemma's instantiation `u := χ` gives `(dilateMetric G 2 h2).driftLaplacian (fun z => 0 (2•z)) (fun z => χ (2•z)) y = G.driftLaplacian 0 χ (2•y)`. The `fun z => 0 (2•z)` is defeq to `fun z => 0`? `(fun z => 0 (2 • z))` — `0 (2•z)` means zero function applied, = 0. Definitional? `0` as a function is `Pi.zero_apply`... might need `simp only [Pi.zero_apply]` or `funext`. Hmm. Simpler: state hD case i≠0 as `show G.driftLaplacian (fun _ => 0) χ (2 • y) = ...`? Let me just write `simpa only [hchart_ne hi, hmetric_ne hi] using (dilateMetric_driftLaplacian G 2 (by norm_num) (fun _ : Vec 1 => (0:ℝ)) χ contDiff_const hχ2 y).symm`. The `simpa` will handle the `0 ∘ 2•` vs `0` mismatch via `Pi.zero_apply`. Should be OK.

- hG: analogous with `dilateMetric_gradInnerInverse G 2 h2 χ χ (hχ2.differentiable (by simp)) (hχ2.differentiable (by simp)) y`.
   Wait, in POUConstruction they use `(hu.differentiable (by simp))` where hu : ContDiff ℝ 2 u. For `ContDiff.differentiable` the second arg is a proof that `1 ≤ n`? `hχ2.differentiable (by simp : (1:WithTop ℕ∞) ≤ 2)`. OK.
- hGuvsupp: ∀ m, Guv m ≠ 0 → m ∈ chart 0 '' source 0. Guv = G.gradInnerInverse χ χ. Support of gradInnerInverse ⊆ tsupport χ? There's `support_gradInnerInverse_subset G u v`. Used in POUConstruction line 335: `exact hvsupp (support_gradInnerInverse_subset G u v hm)`. So hGuvsupp: `fun m hm => ⟨m, hχsupp (support_gradInnerInverse_subset G χ χ hm), by ...⟩` with the chart 0 = id fact.
- hproper: `fun i j K hK => halfSpaceAtlas_transition_preimage_isCompact (n := 0) G i j hK`
- hbd: `halfSpaceAtlas_frontier_volume_zero (n := 0) G`

Then the resulting theorem:
```
theorem auditH1_cover_partial_ae_instantiated :
    ∫ m, (G.driftLaplacian 0 χ) m * χ m ∂((A.globalMeasure volume).withDensity (A.weight 0))
      = -∫ m, G.gradInnerInverse χ χ m ∂(...) := ...
```

That's a substantial but mechanical block. I'll write it.

Also H2 direct: `halfSpaceAtlas_weightedIBP_unconditional (n := 0) G 0 χ χ contDiff_const hχ2 hχ2 hχc hχsupp`.

H3: `halfSpaceAtlas_greenIdentity (n:=0) G 0 χ1 χ2 contDiff_const hχ1_2 hχ2_2 hχ1c hχ2c hχ1supp hχ2supp`.

H4: `halfSpaceAtlas_laplacianIntegralZero (n:=0) G χ hχ2 hχc hχsupp`.

H7: `finiteLifetimeEntropyBridge_gaussian 1 (1/2) (by norm_num)`.
H8: `monotoneOn_F_gaussian 1 (1/2) (by norm_num)` + numeric facts.

H6: `disjointAtlas_weightedIBP` instantiated with ι=1, D := {metric := fun _ => G}, f := fun _ => 0, u := fun _ => χ, v := fun _ => χ. Need:
- hm: ∀ i, Measurable (f i) — measurable_const.
- hd: ContDiff 2 0; hu/hv: ContDiff 2 χ; hvc: HasCompactSupport χ.
Type: the conclusion is about `(disjointAtlasData D f hm).μ` and functions `fun m : DisjointAtlas 1 1 => u m.1 m.2`. Note `DisjointAtlas n ι` is some type (Fin ι × Vec (n+1)?). Let me check its definition — from Blocked.lean: `ManifoldAtlasData (Fin ι × Vec (n+1)) n ι`. Let me read DisjointModel.lean lines 40-110 for `DisjointAtlas`.

Alternatively instantiate H6 via `manifoldWeightedIBP_of_atlasData` with `dilationAtlasTwoData` and concrete u=χ, v=χ, discharging hg/hg' by... those integrability hypotheses are the hard part. The disjoint model theorem discharges them, so `disjointAtlas_weightedIBP` is the best witness. Let me use it.

H5: as discussed.

Let me now check `DisjointAtlas` and `disjointAtlasData` signature.
dsh: reasoning:
The second grep found nothing — the lemma names `dilateMetric_driftLaplacian` etc. must be in a different file. Let me find them.
dsh: reasoning:
Good. `support_gradInnerInverse_subset` is likely in D12/EuclideanChart (used as `open`?). In POUConstruction it's used unqualified: `support_gradInnerInverse_subset G u v hm`. It comes from `Poincare.D13.EuclideanChart.ChartMetric` maybe. It's in scope through imports.

Now, let me write the audit file. I'll write it incrementally: first the capture + axioms + easy instantiations, compile, then add H1/H5/H6.

Let me create the bump lemma first and test it in a scratch. Then build up.

Note on `exists_bump_halfSpace`: need `norm_le_pi_norm`. Let me test that.

Also for two distinct bumps with different centers: I'll parametrize the center. Let me write a general lemma:
```lean
theorem exists_bump_in_halfSpace (n : ℕ) (c : Vec (n+1)) (r : ℝ) (hr : 0 < r)
    (hc : c 0 + r < 1) :
    ∃ χ, ContDiff ℝ ∞ χ ∧ (∀ x, 0 ≤ χ x ∧ χ x ≤ 1) ∧ (∀ x, dist x c ≤ r → χ x = 1) ∧ HasCompactSupport χ ∧ tsupport χ ⊆ {y | y 0 < 1}
```
Simpler: fix K = closedBall c r, U = {y | y 0 < 1}. Need K ⊆ U: if dist y c ≤ r then |y 0 - c 0| ≤ r so y 0 ≤ c 0 + r < 1. Use `pi_norm_le_iff`? For `Vec d = Fin d → ℝ` with sup norm: `dist_eq_norm`, `norm_le_pi_norm : ‖y i‖ ≤ ‖y‖`. Let me test: `have hcoord : |y 0 - c 0| ≤ ‖y - c‖ := norm_le_pi_norm (y - c) 0`. Then `dist y c = ‖y - c‖`. So `y 0 - c 0 ≤ |...| ≤ r`, giving y 0 < 1.

Also need `HasCompactSupport χ`: tsupport χ ⊆ U = {y0 < 1} which is not compact! For compact support, I should choose U = {y0<1} ∩ ball c (r+1) or similar (bounded). Let me define:
K = closedBall c r, U = {y | y 0 < 1} ∩ ball c (r+1). Then K ⊆ U (need closedBall c r ⊆ ball c (r+1): `closedBall_subset_ball (by linarith)`). Then tsupport χ ⊆ U gives compact support via `IsCompact.of_isClosed_subset (isCompact_closedBall c (r+1)) ... (fun y hy => ball_subset_closedBall (htsupp hy).2)`.

Good, that's clean. For the base bump around 0 with r = 1/2: need 0 0 + 1/2 < 1 ✓, and r > 0.

Now the two bumps: χ₁ around c=0, r=1/2. χ₂ around c = (some vector with c 0 + r < 1). Let me pick c = fun i => if i = 0 then (1/4:ℝ) else 0, r = 1/4 → c 0 + r = 1/2 < 1. But then χ₁ and χ₂ may still overlap; doesn't matter, I just need them distinct. Actually for Green's identity I need U and V arbitrary; using the same χ twice would make it trivially symmetric (⟨∇U,∇V⟩ symmetric); using distinct χ₁, χ₂ is better. Even simpler: use χ for U and a *different* explicit function for V? The bump construction with different centers is fine.

Hmm, actually the "distinctness" isn't kernel-checkable easily. It doesn't matter for non-vacuity: both are nonzero test functions, and the identity for two arbitrary functions is contentful regardless of whether they're equal. Using χ1 = χ2 = χ is fine but then it reduces to symmetry. I'll use two different centers to be safe.

Let me now write the file. I'll be careful about names and syntax, and iterate.

One more thing: H1's conclusion with `Du := G.driftLaplacian (fun _ => 0) χ` — note `G.driftLaplacian f u` where f = 0. Let me check the signature of `driftLaplacian`: `driftLaplacian (f u : Vec d → ℝ) : Vec d → ℝ`. And `gradInnerInverse (u v : Vec d → ℝ) : Vec d → ℝ`.

Let me write the file now.

Also, for H6 I need `ChartSumData` construction: `({ metric := fun _ => G } : ChartSumData 0 1)`. And `disjointAtlas_weightedIBP` is in namespace `Poincare.D13.ManifoldIBP`? It's at the end of DisjointModel.lean inside `namespace Poincare.D13.ManifoldIBP` and maybe `namespace OverlapAtlas`? Let me check the exact name: `#print axioms` in DisjointModel's audit will tell. Let me grep.
dsh: reasoning:
`Poincare.D13.ManifoldIBP.disjointAtlas_weightedIBP`. Good.

Now let me write the audit probe file. I'll write the whole thing and iterate.

Let me carefully construct. I'll put the file at `audit_src/Poincare/L4/D13SemanticAudit.lean` in module `Poincare.L4.D13SemanticAudit`.

Content plan (with the constraints: no sorry/axiom/etc.):

```lean
import Poincare.D13.ManifoldIBP.POUConstruction
import Poincare.D13.ManifoldIBP.PartialChartModelPOU
import Poincare.D13.ManifoldIBP.SmoothAtlasModel
import Poincare.D13.ManifoldIBP.Transfer
import Poincare.D13.ManifoldIBP.OverlapIBPData
import Poincare.D13.ManifoldIBP.DisjointModel
import Poincare.D13.HeatKernelBridge

noncomputable section
open MeasureTheory Set Filter Metric Function
open scoped BigOperators ENNReal NNReal Topology Matrix Function ContDiff

namespace Poincare.L4.D13Audit

open Poincare.D12.VolumeIBP
open Poincare.D13.ManifoldIBP

/-! ## 0. Statement capture -/
#check @Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.globalWeightedIBP_of_cover_partial_ae
...
/-! ## 1. Axiom cones -/
#print axioms ...
/-! ## 2. Structure fields (hidden interfaces) -/
#print Poincare.D13.ManifoldIBP.ManifoldAtlasData
/-! ## 3. Non-vacuity witnesses -/

-- bump
theorem exists_bump_in_halfSpace ...

-- H2
theorem h2_instantiation ...

...
```

Note: `#print ManifoldAtlasData` prints the whole structure — good evidence. Might be large but fine.

Let me write it and compile. I'll do it in chunks to manage errors: chunk 1 = capture/axioms/print + bump lemma. Compile. Then chunk 2 = instantiations.

Let me write chunk 1+2 together but be ready to iterate.

Details for the H1 direct instantiation. Let me define:

```lean
theorem h1_globalWeightedIBP_of_cover_partial_ae_instantiated :
    ∃ (χ : Vec 1 → ℝ), ContDiff ℝ ∞ χ ∧ χ 0 = 1 ∧ HasCompactSupport χ ∧
      tsupport χ ⊆ {y : Vec 1 | y 0 < 1} ∧
      (∫ m, (ChartMetric.euclideanChartMetric 1).driftLaplacian (fun _ => 0) χ m * χ m
          ∂(((hsAtlas (n := 0) (ChartMetric.euclideanChartMetric 1)).globalMeasure volume).withDensity
            ((hsAtlas (n := 0) (ChartMetric.euclideanChartMetric 1)).weight (fun _ => 0)))
        = -∫ m, (ChartMetric.euclideanChartMetric 1).gradInnerInverse χ χ m
          ∂(((hsAtlas (n := 0) (ChartMetric.euclideanChartMetric 1)).globalMeasure volume).withDensity
            ((hsAtlas (n := 0) (ChartMetric.euclideanChartMetric 1)).weight (fun _ => 0))) := by
  obtain ⟨χ, hsm, hrange, h1, hcc, hsupp⟩ := exists_bump_in_halfSpace 0 0 (1/2) (by norm_num) (by norm_num)
  refine ⟨χ, hsm, h1, hcc, hsupp, ?_⟩
  ... apply headline ...
```

Hmm, making it existential means the application is inside. That's fine. But it's cleaner to have a section with fixed χ. Let me structure:

```lean
section Bump
variable {n : ℕ}

theorem exists_bump_in_halfSpace (n : ℕ) (c : Vec (n+1)) (r : ℝ) (hr : 0 < r) (hc : c 0 + r < 1) :
    ∃ χ : Vec (n+1) → ℝ, ContDiff ℝ ∞ χ ∧ (∀ x, 0 ≤ χ x ∧ χ x ≤ 1) ∧
      (∀ x, dist x c ≤ r → χ x = 1) ∧ HasCompactSupport χ ∧
      tsupport χ ⊆ {y : Vec (n+1) | y 0 < 1} := ...
```

Then each instantiation does `obtain ⟨χ, hsm, hrange, hone, hcc, hsupp⟩ := exists_bump_in_halfSpace n c r hr hc`.

For the H1 application the term is long; I'll write it carefully.

One concern: `hsAtlas (n := 0) (ChartMetric.euclideanChartMetric 1)` — `hsAtlas` is `abbrev hsAtlas (G : ChartMetric (n + 1)) : SmoothOverlapAtlas (Vec (n + 1)) (n + 1) := halfSpaceAtlas G` with an implicit `n` inferred from G's type. Since G : ChartMetric 1, n+1 = 1 means n = 0 by unification. `hsAtlas G` should work without specifying n.

Let me now write the file. I'll write it in full, compile, and iterate on errors.

For H5, let me first do the easy version (U = 0, V = χ nonzero) and then, if time permits, upgrade to U = V = χ.

Actually, let me think about H5 more: maybe I can instantiate with the *POU data that the half-space model uses*, i.e. ψ := hsPsi χ' j for a bump χ' — but that's for the half-space atlas, not the dilation atlas. H5 is about `dilationAtlasTwoSmooth`. The POU ψ needs to be subordinate to the dilation cover with supp ⊆ ball 0 (R+1), which is easy: ψ_0 = bump = 1 on tsupport V, ψ_1 = 0.

For hintL/hintR with U = V = χ: Let me look at what's needed. `dilationAtlasTwoSmooth n G` = `dilationAtlas n G` with SmoothOverlapAtlas structure. Its chart 0 = id (dilationAtlasTwo_chart_zero), source 0 = univ (total). Then:

hintL j: Integrable (fun m => G.driftLaplacian 0 χ m * (dilationAtlasTwoSmooth n G).pouPiece 0 χ (ψ j) m) ((...).globalMeasure volume).withDensity ((...).weight 0).

Use `OverlapAtlas.integrable_globalMeasure_withDensity_of_supported (A := dilationAtlasTwoSmooth n G) volume 0 (f := fun _ => 0) (g := ...) measurable_const hg_meas hsupp hint`:
- hsupp: ∀ m, g m ≠ 0 → m ∈ A.chart 0 '' A.source 0. Since chart 0 = id and source 0 = univ: `⟨m, mem_univ m, by simp [dilationAtlasTwo_chart_zero]⟩`. Actually `A.chart 0 '' A.source 0 = univ` — need `A.chart 0 m = m`. There's `dilationAtlasTwo_chart_zero`. In the SmoothAtlasModel proof they use `hchart0 : (dilationAtlasTwoSmooth n G).chart 0 = fun x => (1:ℝ) • x`. So `simp [hchart0]`.
- hg_meas: Measurable g. g = fun m => G.driftLaplacian 0 χ m * pouPiece ... m. driftLaplacian continuous → measurable; `A.measurable_pouPiece b hv (hψ_sm j)` where hv : Measurable χ. So `(G.driftLaplacian_continuous 0 χ contDiff_const hχ2).measurable.mul (A.measurable_pouPiece 0 hχ_meas (hψ_sm j))`.
- hint: Integrable (fun y => exp(-(0 (A.chart 0 y))) * g (A.chart 0 y) * A.density 0 y) volume. Rewrite chart 0 = id and density 0 = G.density, then g (chart 0 y) = Δχ y * pouPiece ... y. Now pouPiece 0 χ ψ_j (y) = A.lift 0 (fun z => ψ_j z * χ (A.chart 0 z)) y. With chart 0 = id and y ∈ univ: `A.lift_apply_chart (hy := mem_univ y)` gives `A.lift 0 φ y = φ y`. So g(y) = Δχ y * (ψ_j y * χ y). This is continuous with compact support (tsupport (ψ_j * χ) ⊆ tsupport ψ_j ∩ tsupport χ, both compact) → integrable.

So I need to prove continuity and compact support of `fun y => Real.exp (-(0)) * (G.driftLaplacian 0 χ y * (ψ j y * χ y)) * G.density y`. With F=0: exp(-0)=1. Continuous: Δχ continuous × ψ_j smooth × χ smooth × density C¹. Compact support: contained in tsupport ψ_j (compact) — since the factor ψ_j y appears. So `IsCompact.of_isClosed_subset hψ_cc_j (isClosed_tsupport _) (closure_minimal ... )`.

This is very similar to the halfSpaceAtlas_integrable_dirichlet proof. ~40 lines. Let me attempt it. If it gets messy, fall back.

Hmm, actually, maybe a smarter approach for H5: use **the existing theorem `halfSpaceAtlas_dirichletEnergy`**? No, different atlas.

OR: note H5's statement is *implied* by H2-like reasoning... no.

Let me just try. Actually, wait: is there an even easier contentful instantiation? What if U = V = χ and ψ_0 = 1 (constant) violates compact support; what if tsupport V is empty? no.

Alternatively, use `R` such that hψ_supp. Fine.

Let me start writing. I'll create the file with everything except H5's hard integrability, using a placeholder-free approach: for H5, I'll first write the U=0 version to get a compiling artifact, then upgrade.

Actually, you know what — there's a subtlety with U=0: `G.driftLaplacian 0 0` — is it defeq to 0? Probably not by rfl; `driftLaplacian f u := laplacian u - ...` with derivatives of 0. So proving the integrand is 0 requires unfolding. `hintL` goal: Integrable (fun m => G.driftLaplacian 0 0 m * pouPiece 0 χ ψ_j m) μ. I can prove `Integrable (fun _ => 0) μ` and use `.congr` with pointwise equality `G.driftLaplacian 0 0 m = 0`. Is there a lemma `driftLaplacian_zero`? Maybe not. I can prove it: `G.driftLaplacian 0 0 m = G.laplacian 0 m - ...`; laplacian of zero function = 0 via `fderiv_zero` etc. There might be `ChartMetric.laplacian_const` or `laplacian_zero`. Let me grep. This might be as much work as the real thing.

Alternative for making hintL/hintR trivially provable: choose V = 0 (then ψ can be 0; hint integrands are 0 via mul_zero and lift of zero). Then all integrability trivial by `integrable_zero` + congruence. But then U,V both zero → fully degenerate.

OK let me just do the real H5 instantiation with U = V = χ (nonzero) and prove hintL/hintR via the transfer lemma. I'll timebox to ~45 min.

Let me check available measurability/continuity lemmas:
- `A.measurable_pouPiece` signature? Used as `A.measurable_pouPiece b hv (hψ_sm j)` where hv : Measurable v and hψ_sm : ContDiff ℝ ∞ (ψ j). Let me grep its statement.
- `A.lift_apply_chart` we saw.
- `A.support_lift_subset`.
- For pouPiece with chart 0 (which is id), on the image.

Also for the compact support of `fun y => Δχ y * (ψ j y * χ y) * density y`: tsupport ⊆ tsupport ψ_j (compact) — because the function vanishes where ψ_j = 0. `hψ_cc j : HasCompactSupport (ψ j)`.

Let me look at `measurable_pouPiece` signature.
dsh: reasoning:
Good. Now let me write the file. I'll start with the core (capture, axioms, bump, H2/H3/H4/H6/H7/H8) and get it compiling, then tackle H1 and H5.

Let me write it now.

For the H1 direct application I'll adapt from POUConstruction. Let me write carefully.

Also note: for H1, the `Vec (n+1)` in my audit is `Vec 1` when n=0. `hsAtlas G` requires `G : ChartMetric (n+1)`. With `G : ChartMetric 1`, n is inferred as 0.

Let me define a local notation to keep it readable:
```lean
private abbrev G1 : ChartMetric 1 := ChartMetric.euclideanChartMetric 1
```

Hmm, `euclideanChartMetric` is in namespace `Poincare.D12.VolumeIBP.ChartMetric`, and we `open Poincare.D12.VolumeIBP`, so `ChartMetric.euclideanChartMetric 1`.

Let me write the file.

For H3, need two bumps. I'll do:
```lean
theorem h3_green_instantiated :
    ∃ (χ₁ χ₂ : Vec 1 → ℝ), ... ∧
      (∫ m, G1.driftLaplacian (fun _ => 0) χ₁ m * χ₂ m ∂μH)
        = ∫ m, χ₁ m * G1.driftLaplacian (fun _ => 0) χ₂ m ∂μH := by
```
where μH = ((hsAtlas G1).globalMeasure volume).withDensity ((hsAtlas G1).weight (fun _ => 0)).

Note `hsAtlas G1` needs `n := 0`? `hsAtlas (G : ChartMetric (n+1))` with G : ChartMetric 1 → n = 0. Let me write `hsAtlas G1`.

Let me write the whole thing.

Also for H4:
```lean
theorem h4_laplacianIntegralZero_instantiated :
    ∃ χ, ... ∧ ∫ m, G1.laplacian χ m ∂((hsAtlas G1).globalMeasure volume) = 0
```

For H6 via disjointAtlas_weightedIBP:
```lean
theorem h6_atlasData_instantiated :
    ∃ χ : Vec 1 → ℝ, ... ∧
      (∫ m, (disjointAtlasData (D := ({metric := fun _ => G1} : ChartSumData 0 1)) (fun _ => (fun _ : Vec 1 => 0)) (fun _ => measurable_const)).driftLaplacianM (fun m => χ m.2) m * (fun m => χ m.2) m ∂...μ)
      = -∫ m, ... .gradInnerM (fun m => χ m.2) (fun m => χ m.2) m ∂...μ := by
```
Hmm, `disjointAtlas_weightedIBP D f u v hm hd hu hv hvc` with D : ChartSumData n ι, f u v : Fin ι → Vec (n+1) → ℝ. Take n=0, ι=1, D := {metric := fun _ => G1}, f := fun _ => 0, u := fun _ => χ, v := fun _ => χ. Then the conclusion is about functions `(fun m => u m.1 m.2)`.

To make it existential, I'd have to write the whole conclusion inside. Fine.

Maybe simpler: write these as *theorems with explicit hypotheses-free statements* that apply the headline; the mere compilation proves the instantiation. I'll make each theorem existentially quantify χ and include the concluded identity as the last conjunct. That way each theorem is a self-contained kernel-checked witness.

Let me write it. For readability I'll define abbreviations at the top:

```lean
abbrev G1 : ChartMetric 1 := ChartMetric.euclideanChartMetric 1
abbrev A1 : SmoothOverlapAtlas (Vec 1) 1 := hsAtlas G1
abbrev μ1 : Measure (Vec 1) := (A1.globalMeasure volume).withDensity (A1.weight (fun _ => 0))
abbrev μD : Measure (DisjointAtlas 0 1) := (disjointAtlasData (D := ({metric := fun _ => G1} : ChartSumData 0 1)) (f := fun _ => (fun _ : Vec 1 => 0)) (fun _ => measurable_const)).μ
```

Careful: `A1` as an abbrev with `hsAtlas G1`; `A1.globalMeasure` etc. should work.

Hmm, `hsAtlas` is an abbrev in namespace `Poincare.D13.ManifoldIBP.OverlapAtlas`? Let me check: in PartialChartModel.lean line 529, `abbrev hsAtlas` — inside which namespace? The file has `namespace Poincare.D13.ManifoldIBP` then `namespace OverlapAtlas`? Let me check. In POUConstruction they call `hsAtlas (n := n) G` inside `namespace OverlapAtlas`. So `hsAtlas` is `Poincare.D13.ManifoldIBP.OverlapAtlas.hsAtlas`. With `open Poincare.D13.ManifoldIBP`, is `OverlapAtlas.hsAtlas` accessible as `hsAtlas`? Only if `open OverlapAtlas` too, or if the namespace is open. In POUConstruction, they're inside `namespace OverlapAtlas`. I'll add `open Poincare.D13.ManifoldIBP.OverlapAtlas` to my file.

Similarly `halfSpaceAtlas_weightedIBP_unconditional` etc. are in `Poincare.D13.ManifoldIBP.OverlapAtlas`, and `globalWeightedIBP_of_cover_partial_ae` is in `Poincare.D13.ManifoldIBP.SmoothOverlapAtlas`. The `#print axioms` uses full names.

Also `dilationAtlasTwoSmooth` is in `Poincare.D13.ManifoldIBP.OverlapAtlas`.

Let me check the namespace structure of PartialChartModel.lean quickly.
dsh: reasoning:
Good. Now let me write the file.

I'll write the first version now. Then compile and iterate.

For H1, I'll write the full direct application. Let me draft it based on the existing proof pattern.

```lean
theorem h1_cover_partial_ae_direct (χ : Vec 1 → ℝ) (hsm : ContDiff ℝ ∞ χ) (hχ2 : ContDiff ℝ 2 χ)
    (hcc : HasCompactSupport χ) (hsupp : tsupport χ ⊆ {y : Vec 1 | y 0 < 1}) :
    ∫ m, G1.driftLaplacian (fun _ => 0) χ m * χ m ∂μ1
      = -∫ m, G1.gradInnerInverse χ χ m ∂μ1 := by
  have htrans := halfSpaceAtlas_transition_coherence (n := 0) G1
  refine A1.globalWeightedIBP_of_cover_partial_ae htrans 0 (fun j : Fin 2 => (j : ℕ)) 
    (fun _ => 0) χ χ (G1.driftLaplacian (fun _ => 0) χ) (G1.gradInnerInverse χ χ)
    measurable_const hχ2.continuous.measurable
    (G1.driftLaplacian_continuous _ _ contDiff_const hχ2).measurable
    (G1.gradInnerInverse_continuous χ χ hχ2 hχ2).measurable
    ?hvsupp ?hfc ?huc ?hvc ?hvcc ?htsupp ?hcover ?hD ?hG ?hGuvsupp ?hproper ?hbd
  ...
```

Wait, `A1` is an abbrev for `hsAtlas G1` — but `globalWeightedIBP_of_cover_partial_ae` is a theorem in namespace `SmoothOverlapAtlas` with `variable {A : SmoothOverlapAtlas M (n+1)}`, so `A.globalWeightedIBP_of_cover_partial_ae` works via dot notation.

The proofs of the subgoals:
- hvsupp : ∀ m, χ m ≠ 0 → m ∈ A1.chart 0 '' A1.source 0. 
  ```
  intro m hm
  refine ⟨m, ?_, ?_⟩
  · rw [show A1.source 0 = {y : Vec 1 | y 0 < 1} from hsSource_zero]
    exact hsupp (subset_tsupport χ (Function.mem_support.mpr hm))
  · rw [show A1.chart 0 = fun y : Vec 1 => (1:ℝ) • y from hsChart_zero]; simp
  ```
- hfc : ∀ i, ContDiff ℝ 2 (fun y => (fun _ => 0) (A1.chart i y)) := fun i => contDiff_const
- huc/hvc : ∀ i, ContDiff ℝ 2 (fun y => χ (A1.chart i y)):
  ```
  intro i
  by_cases hi : i = 0
  · subst hi; rw [show A1.chart 0 = fun y => (1:ℝ) • y from hsChart_zero]; simpa [Function.comp_def] using hχ2.comp (contDiff_const_smul (1:ℝ))
  · rw [show A1.chart i = fun y => (2:ℝ) • y from hsChart_ne hi]; simpa [Function.comp_def] using hχ2.comp (contDiff_const_smul (2:ℝ))
  ```
- hvcc : HasCompactSupport (fun y => χ (A1.chart 0 y)):
  ```
  have hf : (fun y : Vec 1 => χ (A1.chart 0 y)) = χ := by funext y; rw [show A1.chart 0 = ...]; simp
  rw [hf]; exact hcc
  ```
- htsupp_b : tsupport (fun y => χ (A1.chart 0 y)) ⊆ A1.source 0: `by rw [hf, show A1.source 0 = ...]; exact hsupp`
- hcover : A1.chart 0 '' A1.source 0 ⊆ ⋃ j : Fin 2, A1.chart ((j:ℕ)) '' A1.source ((j:ℕ)): `fun y hy => mem_iUnion.mpr ⟨0, hy⟩`
- hD : ∀ i y, y ∈ A1.source i → (G1.driftLaplacian (fun _ => 0) χ) (A1.chart i y) = (A1.metric i).driftLaplacian (fun z => (fun _ => 0) (A1.chart i z)) (fun z => χ (A1.chart i z)) y:
  ```
  intro i y _
  by_cases hi : i = 0
  · subst hi
    rw [show A1.chart 0 = fun y => (1:ℝ) • y from hsChart_zero, show A1.metric 0 = G1 from hsMetric_zero G1]
    simp only [one_smul]
  · rw [show A1.chart i = fun y => (2:ℝ) • y from hsChart_ne hi, show A1.metric i = dilateMetric G1 2 (by norm_num) from hsMetric_ne hi G1]
    exact (dilateMetric_driftLaplacian G1 2 (by norm_num) (fun _ : Vec 1 => (0:ℝ)) χ contDiff_const hχ2 y).symm
  ```
- hG similar with `dilateMetric_gradInnerInverse G1 2 (by norm_num) χ χ (hχ2.differentiable (by simp)) (hχ2.differentiable (by simp)) y`.
- hGuvsupp : ∀ m, G1.gradInnerInverse χ χ m ≠ 0 → m ∈ A1.chart 0 '' A1.source 0:
  ```
  intro m hm
  refine ⟨m, ?_, ?_⟩
  · rw [show A1.source 0 = {y : Vec 1 | y 0 < 1} from hsSource_zero]
    exact hsupp (support_gradInnerInverse_subset G1 χ χ hm)
  · rw [show A1.chart 0 = fun y : Vec 1 => (1:ℝ) • y from hsChart_zero]; simp
  ```
- hproper : `fun i j K hK => halfSpaceAtlas_transition_preimage_isCompact (n := 0) G1 i j hK`
- hbd : `halfSpaceAtlas_frontier_volume_zero (n := 0) G1`

Names: `hsChart_zero`, `hsChart_ne`, `hsMetric_zero`, `hsMetric_ne`, `hsSource_zero` — are these in namespace OverlapAtlas? In POUConstruction they're used unqualified inside `namespace OverlapAtlas` — yes, they're in `Poincare.D13.ManifoldIBP.OverlapAtlas`. With `open Poincare.D13.ManifoldIBP.OverlapAtlas` they're accessible.

Also `halfSpaceAtlas_transition_coherence` etc. same namespace.

Potential issue: `A1` as an `abbrev` — dot notation `A1.globalWeightedIBP_of_cover_partial_ae` should work.

Another potential issue: the `show A1.chart 0 = ...` with `A1` being an abbrev for `hsAtlas G1` — the lemma `hsChart_zero` states `(halfSpaceAtlas (n:=n) G).chart 0 = fun y => 1 • y`? Let me check its exact statement.

Let me grep hsChart_zero statement.
dsh: reasoning:
`hsChart_zero` states `hsChart 0 = fun y => 1 • y` where `hsChart` is the chart function of `halfSpaceAtlas`. But the `halfSpaceAtlas` structure's `chart` field is `hsChart`? Presumably `halfSpaceAtlas G` has `chart := hsChart` definitionally, which is why `rw [show (halfSpaceAtlas G).chart i = hsChart i from rfl]` was used in some proofs. In POUConstruction they wrote `rw [hchart0]` with `hchart0 : A.chart 0 = fun y => (1:ℝ) • y` obtained as `hsChart_zero`. So there must be defeq. Let me just use `show A1.chart 0 = fun y => (1:ℝ) • y from hsChart_zero` and hope. If not, use `hsChart_zero` with a `rfl` step. Let me check the `halfSpaceAtlas` definition to see the field values.

Let me look at PartialChartModel.lean around 380-440.
dsh: reasoning:
Let me find the actual `halfSpaceAtlas` definition (the structure literal).
dsh: reasoning:
`chart := hsChart` so `(halfSpaceAtlas G).chart 0` is defeq to `hsChart 0`, and `hsChart_zero : hsChart 0 = fun y => 1 • y`. So `show A1.chart 0 = fun y => (1:ℝ) • y from hsChart_zero` should work (defeq). Same for metric/source.

Now let me write the audit file. This is the big step. I'll write it and compile.

Let me think about H5's integrability proof in detail before writing, since it's the riskiest.

H5 statement (n=0 case, G := G1):
```
theorem h5_dilationAtlasTwo_weightedIBP_via_pou_instantiated :
    ∃ (χ ψ0 ψ1 : Vec 1 → ℝ),
      ContDiff ℝ ∞ χ ∧ HasCompactSupport χ ∧ χ 0 = 1 ∧
      ContDiff ℝ ∞ ψ0 ∧ ContDiff ℝ ∞ ψ1 ∧
      (∀ x ∈ tsupport χ, ψ0 x + ψ1 x = 1) ∧
      (∃ R, tsupport ψ0 ⊆ ball 0 (R+1) ∧ tsupport ψ1 ⊆ ball 0 (R+1)) ∧
      (∫ m, G1.driftLaplacian 0 χ m * χ m ∂μDil)
        = -∫ m, G1.gradInnerInverse χ χ m ∂μDil
```
Hmm, `dilationAtlasTwo_weightedIBP_via_pou` signature: `(G) (F U V) (hF hU hV : ContDiff ℝ 2) (hVc : HasCompactSupport V) {R : ℝ} (ψ : Fin 2 → Vec (n+1) → ℝ) (hψ_sm) (hψ_supp) (hψ_sum) (hintL) (hintR)`.

I'll apply with F := fun _ => 0, U := χ, V := χ. hψ_supp : ∀ i, tsupport (ψ i) ⊆ ball 0 (R+1). Take R = the bound for χ's support: from `HasCompactSupport χ`, get `∃ R, tsupport χ ⊆ closedBall 0 R` via `Metric.isBounded_iff_subset_closedBall`. Then closedBall 0 R ⊆ ball 0 (R+1). Set ψ0 := bump = 1 on tsupport χ with support in ball 0 (R+1); ψ1 := 0.

But then hintL for j=0: integrand Δχ · pouPiece 0 χ ψ0; j=1: Δχ · pouPiece 0 χ 0 = Δχ · lift(0) = 0. hintR for j=0: pouPairing 0 0 χ χ ψ0 = lift(⟨∇χ, ∇(ψ0·χ)⟩); j=1: pouPairing 0 1 χ χ 0 = lift(⟨∇χ, ∇(0·χ)⟩) = 0.

So I need:
(a) Integrability of `fun m => G1.driftLaplacian 0 χ m * A.pouPiece 0 χ ψ0 m` w.r.t. `(A.globalMeasure volume).withDensity (A.weight 0)`, where A := dilationAtlasTwoSmooth 0 G1.
(b) Same for `fun m => A.pouPairing 0 0 χ χ ψ0 m`.

Also for hintL j=1 / hintR j=1, need integrability of the zero-ish functions. Those need congruence to `0` — `A.pouPiece 0 χ 0` is `A.lift 0 (fun y => 0 * χ (A.chart 0 y))` = lift of the zero function. Is `A.lift 0 (fun _ => 0) = fun _ => 0`? `lift b φ m = if m ∈ chart b '' source b then φ (invFunOn ...) else 0`. With φ = 0, this is 0 in both branches; `simp [SmoothOverlapAtlas.lift]` should prove it. Then `Δχ * 0 = 0`. So `Integrable (fun _ => 0) μ` + `.congr`. Similarly pairing.

For (a) and (b), use `integrable_globalMeasure_withDensity_of_supported` with i = 0:
- A.chart 0 = id, A.source 0 = univ (from IsTotal). `A.IsTotal` is `dilationAtlasTwoSmooth_isTotal n G`.
- Need `hsupp : ∀ m, g m ≠ 0 → m ∈ A.chart 0 '' A.source 0`: with hchart0 : A.chart 0 = fun x => (1:ℝ) • x and hsource : A.source 0 = univ: `⟨m, mem_univ m, by rw [hchart0]; simp⟩`.
- Need `hint : Integrable (fun y => Real.exp (-(0 (A.chart 0 y))) * g (A.chart 0 y) * A.density 0 y) volume`.
  With A.chart 0 = id and A.density 0 = G1.density (need `hsMetric_zero`-like lemma for dilationAtlasTwoSmooth: `dilationAtlasTwo_metric_zero`? In SmoothAtlasModel they have `hmetric0 : (dilationAtlasTwoSmooth n G).metric 0 = G`. Let me check its name: `dilationAtlasTwo_metric_zero`. And `density` is defined as `(metric i).density`, so `A.density 0 = G1.density` by rfl after rw.
  
  For (a): g (A.chart 0 y) = Δχ y * pouPiece 0 χ ψ0 (A.chart 0 y) = Δχ y * (ψ0 y * χ (A.chart 0 y)) using `pouPiece_apply` (line 97: `rw [pouPiece, A.lift_apply_chart (h.source_univ b ▸ mem_univ y)]` — the lemma `pouPiece_apply` presumably states `A.pouPiece b v ψ (A.chart b y) = ψ y * v (A.chart b y)`). Actually line 95-97 is inside `lemma pouPiece_apply (h : A.IsTotal) ...`? Let me check the exact statement.

Let me read SmoothAtlasIBP.lean lines 85-130.
dsh: reasoning:
Good: `two_le_infty` is the lemma for `2 ≤ ∞`. Useful.

Now, `pouPairing_apply` with b=0, i=0, u=v=χ, ψ=ψ0:
`A.pouPairing 0 0 χ χ ψ0 (A.chart 0 y) = (A.metric 0).gradInnerInverse (fun z' => χ (A.chart 0 z')) (fun z' => ψ0 (A.transition 0 0 z') * χ (A.chart 0 z')) y`.

With chart 0 = id, metric 0 = G1, transition 0 0 = id → `G1.gradInnerInverse χ (fun z' => ψ0 z' * χ z') y`.

So the chart expression for hintR is `G1.gradInnerInverse χ (fun z => ψ0 z * χ z) y * G1.density y` (weight exp(-0)=1). Continuous with compact support: ψ0 * χ compact support (tsupport ψ0 compact ∩ tsupport χ compact); gradInnerInverse of C² functions is continuous; density continuous. Good.

Similarly for hintL chart expression: `(G1.driftLaplacian 0 χ y) * (ψ0 y * χ y) * G1.density y`. Continuous; compact support ⊆ tsupport ψ0.

So the required `hint` integrability follows from `Continuous.integrable_of_hasCompactSupport`.

I'll write a helper lemma for the dilation atlas:
```lean
lemma integrable_of_compactSupport_chart0 (A : SmoothOverlapAtlas (Vec (n+1)) (n+1)) (h : A.IsTotal) (G : ChartMetric (n+1))
    (hchart0 : A.chart 0 = fun x => (1:ℝ) • x) (hmetric0 : A.metric 0 = G) ...
```
Hmm, might be easier to inline for the specific case.

Let me write the H5 instantiation concretely:

```lean
theorem h5_dilationAtlasTwo_via_pou_instantiated :
    ∃ (χ ψ0 : Vec 1 → ℝ) (R : ℝ),
      ContDiff ℝ ∞ χ ∧ HasCompactSupport χ ∧ χ 0 = 1 ∧
      ContDiff ℝ ∞ ψ0 ∧ tsupport ψ0 ⊆ ball (0 : Vec 1) (R + 1) ∧
      (∀ x ∈ tsupport χ, ψ0 x + 0 = 1) ∧
      (∫ m, G1.driftLaplacian (fun _ => 0) χ m * χ m ∂μDil)
        = -∫ m, G1.gradInnerInverse χ χ m ∂μDil := by
  obtain ⟨R0, hR0⟩ := Metric.isBounded_iff_subset_closedBall (0 : Vec 1) |>.mp ?  -- hcc.isBounded
  ...
```

Let me structure with `R` determined inside; then use `dilationAtlasTwo_weightedIBP_via_pou` with `R := R` and ψ := ![ψ0, 0]. Hmm, `Fin 2 → ...` via `fun j => if j = 0 then ψ0 else 0`, or `![ψ0, 0]` (Matrix.cons notation requires `Fin.cons`). Simpler: `fun j : Fin 2 => if j = 0 then ψ0 else 0`.

Then hψ_sm j: by fin_cases j; simp [hψ0sm, contDiff_const]. Need to handle if-then. Let me write `fun j => by fin_cases j <;> simp [hψ0sm]`.

hψ_supp j: tsupport (if j=0 then ψ0 else 0) ⊆ ball 0 (R+1): fin_cases; simp [hψ0supp]; for the zero branch `tsupport 0 = ∅` (simp should give `tsupport 0 = ∅`? `tsupport_eq_empty_iff`?). Let me handle: `· simp [hψ0supp]` and `· simp` maybe gives `tsupport (fun _ => 0) ⊆ ball 0 (R+1)`; since `support 0 = ∅`, `tsupport 0 = closure ∅ = ∅`; `simp [tsupport]`? There's `tsupport_zero`. I'll see.

hψ_sum: `∀ x ∈ tsupport χ, ∑ i : Fin 2, (if i = 0 then ψ0 x else 0) = 1`. `Fin.sum_univ_two` gives ψ0 x + 0 = ψ0 x = 1 (from ψ0 = 1 on tsupport χ). Good.

hintL j: by fin_cases j:
- j=0: use transfer lemma. Need to construct `g m := G1.driftLaplacian 0 χ m * A.pouPiece 0 χ ψ0 m`, then `A.integrable_globalMeasure_withDensity_of_supported volume 0 (f := fun _ => 0) (g := g) measurable_const hgmeas hsupp hint`. Wait the theorem's name: `OverlapAtlas.integrable_globalMeasure_withDensity_of_supported`. With `open ... OverlapAtlas`, `A.integrable_globalMeasure_withDensity_of_supported volume 0 measurable_const hgmeas hsupp hint`.
- j=1: integrand is `fun m => Δχ m * A.pouPiece 0 χ (fun _ => 0) m`. `A.pouPiece 0 χ (fun _ => 0) = fun _ => 0`, so integrand = fun _ => 0 → `integrable_zero`. Need congr: `(integrable_zero _ _).congr (ae_of_all _ (fun m => by simp [SmoothOverlapAtlas.pouPiece, SmoothOverlapAtlas.lift]))`. Hmm, is `lift 0 (fun _ => 0) m = 0`? `lift` is defined with `if ... then φ (...) else 0`; φ = 0 so yes by `simp [SmoothOverlapAtlas.lift]`. Let me double check the `lift` definition is `if m ∈ chart b '' source b then φ (invFunOn ...) else 0`; simp with `Function.comp` etc. `simp [SmoothOverlapAtlas.lift]` should close. Actually `lift` might be defined with `Function.comp`. Let me look at lines 60-66 of SmoothAtlas.lean: 
```
def lift (b : ℕ) (φ : Vec d → ℝ) : M → ℝ := by
  classical
  exact fun m => if m ∈ A.chart b '' A.source b then φ (Function.invFunOn (A.chart b) (A.source b) m) else 0
```
So `lift b (fun _ => 0) m = if h then 0 else 0 = 0`. `simp [SmoothOverlapAtlas.lift]` should work (splitting on the if).

For the chart expression hint for j=0:
`hint : Integrable (fun y => Real.exp (-((fun _ => 0) (A.chart 0 y))) * g (A.chart 0 y) * A.density 0 y) volume`.
I want to show this is integrable because it equals `fun y => (G1.driftLaplacian 0 χ y * (ψ0 y * χ y)) * G1.density y` (with exp(-0)=1). Prove `Continuous` + `HasCompactSupport` of that, then `.congr`.

Approach: prove a lemma
```lean
have hcont : Continuous (fun y : Vec 1 => G1.driftLaplacian (fun _ => 0) χ y * (ψ0 y * χ y) * G1.density y) := ...
have hcc : HasCompactSupport (fun y => G1.driftLaplacian ... * (ψ0 y * χ y) * G1.density y) := ...
have hint0 : Integrable (fun y => ...) volume := hcont.integrable_of_hasCompactSupport hcc
```
Then `refine hint0.congr ?_` with a.e. equality to the goal's function. The goal's function: `fun y => Real.exp (-(0)) * g (A.chart 0 y) * A.density 0 y`. Need to rewrite `A.chart 0 = id`, `A.density 0 = G1.density`, `g (A.chart 0 y) = Δχ y * pouPiece 0 χ ψ0 (chart 0 y) = Δχ y * (ψ0 y * χ (chart 0 y))` via `A.pouPiece_apply hT 0 χ ψ0 y` (needs `A.IsTotal` hT and `A.chart 0 y`). Actually `pouPiece_apply hT b v ψ y : A.pouPiece b v ψ (A.chart b y) = ψ y * v (A.chart b y)`. With b=0 and chart 0 = id: `= ψ0 y * χ y`. Good.

The a.e. equality proof: `filter_upwards with y; simp only [...]; rw [...]`. Might need care with `Function.comp`.

For hcc: `HasCompactSupport (fun y => Δχ y * (ψ0 y * χ y) * G1.density y)`. Since `ψ0` has compact support and the function vanishes where ψ0 = 0:
```lean
refine IsCompact.of_isClosed_subset hψ0cc (isClosed_tsupport _) (closure_minimal ?_ (isClosed_tsupport _))
intro y hy
rw [Function.mem_support] at hy
by_contra hyt
have h0 : ψ0 y = 0 := Function.notMem_support.mp (fun hm => hyt (subset_tsupport _ hm))
exact hy (by simp [h0])
```
Good — that pattern is from the existing proofs.

For hintR j=0: goal integrability of `fun m => A.pouPairing 0 0 χ χ ψ0 m`; chart expression via `pouPairing_apply hT 0 0 χ χ ψ0 y` = `(A.metric 0).gradInnerInverse (fun z' => χ (A.chart 0 z')) (fun z' => ψ0 (A.transition 0 0 z') * χ (A.chart 0 z')) y`. With chart 0 = id, metric 0 = G1, transition 0 0 = id: `G1.gradInnerInverse χ (fun z => ψ0 z * χ z) y`. Continuous (gradInnerInverse_continuous with C² arguments; `fun z => ψ0 z * χ z` is C² since ψ0 is C^∞ and χ C²... need ContDiff 2 (ψ0 * χ): `hψ0sm.of_le two_le_infty |>.mul hχ2`). Compact support: contained in tsupport ψ0. Then the same pattern.

`A.transition 0 0 = fun y => y` for the dilation atlas: `dilationTransition_zero_zero 2` (used in SmoothAtlasModel as `htrans00`). 

OK. This is a chunk of work but let's go. Let me write the entire file now.

Also for H5's conclusion: note the theorem's conclusion is stated with `G.driftLaplacian F U` and `G.gradInnerInverse U V` and measure `((dilationAtlasTwoSmooth n G).globalMeasure volume).withDensity ((dilationAtlasTwoSmooth n G).weight F)`. With F=0, U=V=χ, n=0.

Let me define:
```lean
abbrev A2 : SmoothOverlapAtlas (Vec 1) 1 := dilationAtlasTwoSmooth 0 G1
abbrev μ2 : Measure (Vec 1) := (A2.globalMeasure volume).withDensity (A2.weight (fun _ => 0))
```
Note `dilationAtlasTwoSmooth n G` — check its signature/namespace. It's in `OverlapAtlas` namespace, `def dilationAtlasTwoSmooth (n : ℕ) (G : ChartMetric (n+1))`. Good.

Let me check `hT := dilationAtlasTwoSmooth_isTotal n G` type: `(dilationAtlasTwoSmooth n G).IsTotal`. Good.

Now H6 via disjointAtlas_weightedIBP: 
```lean
theorem h6_atlasData_instantiated :
    ∃ χ : Vec 1 → ℝ, ContDiff ℝ ∞ χ ∧ HasCompactSupport χ ∧ χ 0 = 1 ∧
      (∫ m, (disjointAtlasData (D := ({metric := fun _ => G1} : ChartSumData 0 1)) (fun _ => (fun _ : Vec 1 => 0)) (fun _ => measurable_const)).driftLaplacianM (fun m => χ m.2) m * (fun m => χ m.2) m ∂(disjointAtlasData ...).μ)
      = -∫ m, (disjointAtlasData ...).gradInnerM (fun m => χ m.2) (fun m => χ m.2) m ∂(...).μ := by
  obtain ⟨χ, hsm, ...⟩ := exists_bump_in_halfSpace 0 0 (1/2) ...
  refine ⟨χ, hsm, hcc, h1, ?_⟩
  exact disjointAtlas_weightedIBP (D := {metric := fun _ => G1}) (f := fun _ => fun _ => 0)
    (u := fun _ => χ) (v := fun _ => χ) (fun _ => measurable_const) (fun _ => contDiff_const)
    (fun _ => hχ2) (fun _ => hχ2) (fun _ => hcc)
```
Wait: `disjointAtlas_weightedIBP D f u v hm hd hu hv hvc` where f u v : Fin ι → Vec (n+1) → ℝ, and the conclusion mentions `(disjointAtlasData D f hm).driftLaplacianM (fun m => u m.1 m.2)`. With `u := fun _ => χ`, `(fun m : DisjointAtlas 0 1 => u m.1 m.2) = fun m => χ m.2`. Good.

Note `DisjointAtlas n ι = Fin ι × Vec (n+1)`, so `m.2 : Vec 1`. Good.

But careful: `ChartSumData 0 1` — the structure is `ChartSumData (n ι : ℕ)` with `metric : Fin ι → ChartMetric (n+1)`. So `({metric := fun _ => G1} : ChartSumData 0 1)`. Good.

`disjointAtlasData D f hm` requires `hm : ∀ i, Measurable (f i)`. Good.

One issue: the theorem's `n` and `ι`: `disjointAtlas_weightedIBP (D : ChartSumData n ι)`. With D : ChartSumData 0 1, n=0, ι=1.

Also I need the μ abbreviation to be type-correct. Let me define μD as the full expression each time (abbrev with `D` and `f` args):

```lean
abbrev D0 : ChartSumData 0 1 := { metric := fun _ => G1 }
abbrev f0 : Fin 1 → Vec 1 → ℝ := fun _ => fun _ => 0
abbrev DData : ManifoldAtlasData (DisjointAtlas 0 1) 0 1 := disjointAtlasData D0 f0 (fun _ => measurable_const)
abbrev μD : Measure (DisjointAtlas 0 1) := DData.μ
```

Then the conclusion: `∫ m, DData.driftLaplacianM (fun m => χ m.2) m * (fun m => χ m.2) m ∂μD = -∫ m, DData.gradInnerM (fun m => χ m.2) (fun m => χ m.2) m ∂μD`.

And the proof: `exact disjointAtlas_weightedIBP D0 f0 (fun _ => χ) (fun _ => χ) (fun _ => measurable_const) (fun _ => contDiff_const) (fun _ => hχ2) (fun _ => hχ2) (fun _ => hcc)`. The conclusion of that call should be defeq to what I need? The call gives `(∫ m, (disjointAtlasData D0 f0 ...).driftLaplacianM (fun m => (fun _ => χ) m.1 m.2) m * ...)`. With `DData` being an abbrev, defeq should hold. Good.

Now H7/H8:
```lean
theorem h7_bridge_instantiated :
    FiniteLifetimeEntropyBridge (gaussCalculus 1 (1/2) (by norm_num)) (gaussEntropyData 1 (1/2) (by norm_num)) 0 (1/2) ∧
    (gaussEntropyData 1 (1/2) (by norm_num) (1/4)).F = 4 := by
  constructor
  · exact finiteLifetimeEntropyBridge_gaussian 1 (1/2) (by norm_num)
  · rw [F_gaussEntropyData (by norm_num : (1/2:ℝ) < 1) (by norm_num : (1/4:ℝ) ≤ 1/2)]
    norm_num
```
Careful: `F_gaussEntropyData {τ0 t1} (ht1 : t1 < τ0) {t} (ht : t ≤ t1) : (gaussEntropyData τ0 t1 ht1 t).F = 1/(τ0 - t)`. With τ0=1, t1=1/2, t=1/4: `1/(1-1/4) = 4/3`. So F(1/4) = 4/3, not 4. Let me recompute: τ0=1, F(s) = 1/(1-s): F(0)=1, F(1/4)=4/3, F(1/2)=2. So use 4/3.

Also I want a numeric monotonicity witness:
```lean
theorem h8_monotoneOn_instantiated :
    MonotoneOn (fun s : ℝ => (gaussEntropyData 1 (1/2) (by norm_num) s).F) (Icc 0 (1/2)) ∧
    (gaussEntropyData 1 (1/2) (by norm_num) 0).F = 1 ∧
    (gaussEntropyData 1 (1/2) (by norm_num) (1/2)).F = 2 := by
  refine ⟨monotoneOn_F_gaussian 1 (1/2) (by norm_num), ?_, ?_⟩ <;> rw [F_gaussEntropyData ...] <;> norm_num
```

Also the "model vs manifold" check: I can note in the report that the heat headlines are over `Vec 2` Euclidean chart.

Additional adversarial check: verify that the H8 statement is *false* if the domain were extended past t1 (i.e. the finite-lifetime restriction is essential). E.g. F(s) = 1/(1-s) for s ∈ [0,1/2] is increasing; on [0,1) it's still increasing. Hmm, not a good falsity. The real content is that monotonicity holds with the *actual* F functional (integral definition) — the bridge connects them.

Another check for H7: is the bridge's `weighted_ibp` field provable for the *unrestricted* statement? The card says the unrestricted form is false. Fine.

Now, what about `monotoneOn_F_gaussian` being conclusion-equivalent to hypotheses? The proof of H8 decomposes via `monotoneOn_of_bridge` and explicit computation. Not circular.

I think I have enough. Let me now also collect evidence for the interface fields:
- `#print ManifoldAtlasData` (all fields).
- `#print FiniteLifetimeEntropyBridge`.
- `#print SmoothOverlapAtlas`? Maybe too long. Just list fields manually in the ledger with line numbers.
- `#print ChartMetric`.

Let me now write the file. It'll be long. I'll write it and compile.

One more: for the "full statement" evidence, I'll save the `#check @...` outputs to a file. Since my probe's stdout will contain them, I'll capture to a log file with tee.

Let me write the file now.
dsh: reasoning:
Now compile it. Expect many errors; iterate. Time check: let me also note the time.
dsh: reasoning:
The command was cut off by the tool's default timeout? No — I set timeoutMs but the bash tool has a default cap. It says "[timed out after 60000ms]" - the tool applied its own timeout (60s default). I need to pass `timeoutMs` explicitly and/or run in background. The compile printed the #check statements then continued. Let me rerun with a larger timeoutMs (e.g. 900000) or run in background and poll with job_output.

Given compile may take several minutes (elaborating the proofs), let me run it in the background with output to a log file.

Actually, using `run_in_background: true` for the bash call. Then I can continue reading sources while it compiles.

Let me do that: run the compile with output tee'd to audit_build/compile1.log.
dsh: reasoning:
While that compiles, let me write the checkpoint.json (I'm ~1h15m in) and continue reading relevant sources for the semantic analysis.

Checkpoint content: task, phase, findings so far, evidence, next steps.

Let me also verify the standalone recompilation of the D13 source files (fresh compile from source) — I'll do that in parallel later.

Let me write checkpoint.json now.
dsh: reasoning:
The compile job completed with exit code 0 for the wrapper (but the inner exit is in the log). Let me read the log tail.
dsh: reasoning:
Several issues:
1. `Poincare.D13.Riemannian.SmoothChartMetric` unknown — need `import Poincare.D13.Riemannian.ChartMetricBridge` or the module `Poincare.D13.Riemannian`. Let me check the namespace inside ChartMetricBridge.lean — it might be under `Poincare.D13.Riemannian` but the file isn't imported by D13.ManifoldIBP? POUConstruction imports... Let me just import `Poincare.D13.Riemannian.ChartMetricBridge`.
2. "Ambiguous term" at many lines — likely `volume` ambiguity? Line 83 is in `top_eq_omega`? Let me look at the actual messages. Line 83: `theorem top_eq_omega : (⊤ : WithTop ℕ∞) = ω := rfl` — "Ambiguous term" could be about `ω` notation? With `open scoped ContDiff`, `ω` might be ambiguous with something else. Let me see full messages.
3. `gaussEntropyData` unknown — the heat lemmas are in namespace `Poincare.D13.HeatKernelBridge`? Let me check: `finiteLifetimeEntropyBridge_gaussian` is referenced as `Poincare.D13.HeatKernelBridge.finiteLifetimeEntropyBridge_gaussian`. So the namespace is `Poincare.D13.HeatKernelBridge`. I need `open Poincare.D13.HeatKernelBridge`.

Let me look at the errors in detail.
dsh: reasoning:
Ah, `Vec` is ambiguous because `open Poincare.D13.ManifoldIBP` brings in `Poincare.D13.ManifoldIBP.Vec` (probably an abbrev defined in that namespace). I need to not open `Poincare.D13.ManifoldIBP` (or qualify). Since `Poincare.D13.ManifoldIBP` re-exports `Vec`? Let me check: `D13.ManifoldIBP.Vec` — maybe defined in ChartSum or elsewhere. Simplest fix: remove `open Poincare.D13.ManifoldIBP` and use fully qualified names for `ManifoldAtlasData`, `DisjointAtlas`, `disjointAtlasData`, `disjointAtlas_weightedIBP`. Or keep the open and use `Poincare.D12.VolumeIBP.Vec` everywhere. Since the headline statements printed use `D12.VolumeIBP.Vec`, I'll define `abbrev Vec' := Poincare.D12.VolumeIBP.Vec`? Hmm.

Actually the cleanest: don't `open Poincare.D13.ManifoldIBP`; instead use `open Poincare.D13.ManifoldIBP.OverlapAtlas` and refer to `ManifoldAtlasData`, `ChartSumData`, `DisjointAtlas`, `disjointAtlasData`, `disjointAtlas_weightedIBP`, `SmoothOverlapAtlas` with full names or local notation. But `halfSpaceAtlas_weightedIBP_unconditional` etc. are in OverlapAtlas, so opening that gives them. `SmoothOverlapAtlas.globalWeightedIBP_of_cover_partial_ae` uses dot notation, fine.

Wait, but `A1.globalWeightedIBP_of_cover_partial_ae` uses dot notation on a SmoothOverlapAtlas term — fine without opening.

Also `hsAtlas` and `hsChart_zero` are in OverlapAtlas namespace → available.

So: remove `open Poincare.D13.ManifoldIBP`; add explicit references:
- `ManifoldAtlasData` → `Poincare.D13.ManifoldIBP.ManifoldAtlasData`
- `ChartSumData` → `Poincare.D13.ManifoldIBP.ChartSumData`
- `DisjointAtlas` → `Poincare.D13.ManifoldIBP.DisjointAtlas`
- `disjointAtlasData` → `Poincare.D13.ManifoldIBP.disjointAtlasData`
- `disjointAtlas_weightedIBP` → `Poincare.D13.ManifoldIBP.disjointAtlas_weightedIBP`
- `dilationAtlasTwoSmooth`, `dilationAtlasTwo_chart_zero`, `dilationAtlasTwo_metric_zero`, `dilationAtlasTwoSmooth_isTotal` are in OverlapAtlas (opened).
- `SmoothOverlapAtlas.pouPiece_apply`, `SmoothOverlapAtlas.pouPiece`, `SmoothOverlapAtlas.lift` → namespace SmoothOverlapAtlas, so refer as `SmoothOverlapAtlas.pouPiece_apply` etc. (SmoothOverlapAtlas is in ManifoldIBP namespace; with the open removed I need full path `Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.pouPiece_apply` or add `open Poincare.D13.ManifoldIBP.SmoothOverlapAtlas`.) Hmm, adding `open ... SmoothOverlapAtlas` might bring in `lift`/`pouPiece` names but not `Vec` ambiguity (SmoothOverlapAtlas namespace doesn't define Vec). Let me add that open.

But careful: `open Poincare.D13.ManifoldIBP.SmoothOverlapAtlas` — does it define a `Vec`? Unlikely.

Also `hsStep`, `hsPsi` are in OverlapAtlas. Fine.

Now, `isOpen_ball` ambiguity: the issue is `IsOpen.inter` with `ball` ambiguous due to `Vec` being ambiguous. Should resolve once Vec is fixed. Actually error says expected `IsOpen ((i : Fin (n+1)) → ?m i)` — the `isOpen_ball` is for the pseudo-metric ball but the expected type is a Pi type... That's because `ball` resolved to something else? With Vec ambiguity fixed it should be fine.

Line 109-110: `exists_bump_zero` errors were cascading from Vec ambiguity.

Also `Poincare.D13.Riemannian.SmoothChartMetric`: let me find the correct name — ChartMetricBridge.lean: namespace? Let me check.

And the heat namespace: need `open Poincare.D13.HeatKernelBridge`.

Let me check namespaces.
dsh: reasoning:
Good. Now let me fix:
1. Remove `open Poincare.D13.ManifoldIBP` → replace with `open Poincare.D13.ManifoldIBP.SmoothOverlapAtlas` and `open Poincare.D13.HeatKernelBridge`. Qualify ManifoldIBP-level names.
2. Add `import Poincare.D13.Riemannian.ChartMetricBridge` (or `Poincare.D13.Riemannian.ChartMetricBridge`), and refer to `Poincare.D13.Riemannian.SmoothChartMetric`.
3. `SmoothOverlapAtlas.pouPiece` etc.: with `open ...SmoothOverlapAtlas`, names `pouPiece`, `lift`, `pouPiece_apply` are directly available. But `lift` may conflict with something. I'll use `SmoothOverlapAtlas.pouPiece` fully qualified? With `open Poincare.D13.ManifoldIBP.SmoothOverlapAtlas`, `pouPiece` works. Let me use the qualified `SmoothOverlapAtlas.pouPiece` — hmm, `SmoothOverlapAtlas` itself is `Poincare.D13.ManifoldIBP.SmoothOverlapAtlas`, not accessible without opening ManifoldIBP. I can add `open Poincare.D13.ManifoldIBP` but then Vec is ambiguous again...

Solution: alias at the top:
```lean
namespace Poincare.L4.D13Audit
open Poincare.D12.VolumeIBP
open Poincare.D13.ManifoldIBP.OverlapAtlas
open Poincare.D13.ManifoldIBP.SmoothOverlapAtlas
open Poincare.D13.HeatKernelBridge
abbrev Vec' (d : ℕ) := Poincare.D12.VolumeIBP.Vec d  -- no, if Vec ambiguous...
```
With `open Poincare.D13.ManifoldIBP` removed, `Vec` resolves to `D12.VolumeIBP.Vec` (from the open) uniquely. `Poincare.D13.ManifoldIBP.Vec` is only reachable if ManifoldIBP is opened or qualified. So `Vec` is unambiguous. Good.

For `SmoothOverlapAtlas.pouPiece` — with `open ...SmoothOverlapAtlas`, the definition `pouPiece` is accessible unqualified. But inside term `SmoothOverlapAtlas.pouPiece` I'd need the namespace name; since `SmoothOverlapAtlas` is not opened as a namespace prefix... Actually `Poincare.D13.ManifoldIBP.SmoothOverlapAtlas` is a namespace AND a structure name. Referring to `SmoothOverlapAtlas.pouPiece` requires `SmoothOverlapAtlas` to be resolvable — it is, if `Poincare.D13.ManifoldIBP` is open, or by full path. Since I want to avoid opening ManifoldIBP, I can write `Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.pouPiece` or use the short name `pouPiece` (opened). For `simp [pouPiece, lift]` I need the names; with the opens, `pouPiece` and `lift` are in scope. But `lift` might conflict with other `lift`s (e.g. `Monad.lift`?). I'll use `SmoothOverlapAtlas.lift`? Hmm. Let me just write the `simp` with the equation lemmas via `rfl`-style: `funext m; by_cases hm : m ∈ A2.chart 0 '' A2.source 0 <;> simp [Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.lift, hm]`. Actually the full path is safe and unambiguous.

Similarly `pouPiece` def: use full path `Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.pouPiece`.

Let me also fix `A2.pouPiece_apply hT ...` — `pouPiece_apply` is a lemma in the SmoothOverlapAtlas namespace; `A2.pouPiece_apply` dot notation should work.

Also `A2.measurable_pouPiece`, `A2.measurable_pouPairing` — dot notation, fine.
`A2.integrable_globalMeasure_withDensity_of_supported` — that's in OverlapAtlas namespace (IntegrableTransfer is `namespace OverlapAtlas`), so dot notation works.
`A2.density` — OverlapAtlas.density, dot notation works.

Another issue: `IsOpen.inter` with `isOpen_ball`: once Vec resolves, `ball c (r+1)` should be `Metric.ball`. Since `open Metric`, fine.

`exists_bump_zero` had errors cascading. Also `hone 0 (by simp [dist_self])` — `hone : ∀ x, dist x c ≤ r → χ x = 1` with c=0, r=1/2; `dist 0 0 ≤ 1/2` → `simp` should work; the error says unsolved `sorry () ≤ 2⁻¹` — cascade from Vec ambiguity.

Let me now apply the fixes with edits. There are many occurrences; simplest to rewrite the header and replace `SmoothOverlapAtlas.pouPiece`/`lift` references appropriately. Let me apply targeted edits.

Changes:
1. Header: 
```
open Poincare.D12.VolumeIBP
open Poincare.D13.ManifoldIBP
open Poincare.D13.ManifoldIBP.OverlapAtlas
```
→
```
open Poincare.D12.VolumeIBP
open Poincare.D13.ManifoldIBP.OverlapAtlas
open Poincare.D13.HeatKernelBridge
```
2. Add import `Poincare.D13.Riemannian.ChartMetricBridge`.
3. `#print Poincare.D13.Riemannian.SmoothChartMetric` — fine with the import.
4. `ManifoldAtlasData` → `Poincare.D13.ManifoldIBP.ManifoldAtlasData` (occurrences: DData abbrev, H6 statement uses `DData : ManifoldAtlasData (DisjointAtlas 0 1) 0 1`).
5. `ChartSumData` → `Poincare.D13.ManifoldIBP.ChartSumData`.
6. `DisjointAtlas` → `Poincare.D13.ManifoldIBP.DisjointAtlas`.
7. `disjointAtlasData` → `Poincare.D13.ManifoldIBP.disjointAtlasData`.
8. `disjointAtlas_weightedIBP` → `Poincare.D13.ManifoldIBP.disjointAtlas_weightedIBP`.
9. `SmoothOverlapAtlas.pouPiece` → `Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.pouPiece` (2 occurrences), `SmoothOverlapAtlas.lift` → full path (4 occurrences).
10. Also `EntropyData` and `gaussEntropyData` etc. need `open Poincare.D13.HeatKernelBridge`; `EntropyData` is `Poincare.Longrun.Entropy.EntropyData` — is it already in scope? The error only complained about gaussEntropyData/monotoneOn_F_gaussian/F_gaussEntropyData. `EntropyData` might be resolvable via open namespaces from imports? `Poincare.Longrun.Entropy.Functional` is imported and defines `EntropyData` in namespace `Poincare.Longrun.Entropy`. Hmm, `EntropyData` may be exported as `Poincare.Longrun.Entropy.EntropyData`. Is it in scope unqualified? The code in HeatKernelBridge uses `EntropyData` unqualified inside `namespace Poincare.D13.HeatKernelBridge` — that suggests there's an `open` or the name is short. Let me check HeatKernelBridge's opens. Alternatively `EntropyData` is in root namespace? Let me grep the structure declaration's namespace: `structure EntropyData (X : Type u) ...` at Functional.lean:41 — what namespace is that file in? Let me check.

Let me check quickly.
dsh: reasoning:
So I need `open Poincare.Longrun.Entropy` too. Let me apply all edits now.
dsh: reasoning:
Oops: `#print Poincare.D13.ManifoldIBP.ManifoldAtlasData` became `#print Poincare.D13.ManifoldIBP.Poincare.D13.ManifoldIBP.ManifoldAtlasData` because I replaced "ManifoldAtlasData" inside the already-qualified name. Also `Poincare.D13.ManifoldIBP.OverlapAtlas.OverlapAtlas`? Let me check other double-qualifications: the replacements applied to `Poincare.D13.ManifoldIBP.ChartSumData`? The original text already had `Poincare.D13.ManifoldIBP.ChartSumData`? Let me check: in the original file I wrote `abbrev D0 : ChartSumData 0 1` — no prefix. But `#print Poincare.D13.ManifoldIBP.ManifoldAtlasData` was already qualified. Also `#print axioms Poincare.D13.ManifoldIBP.manifoldWeightedIBP_of_atlasData` — contains "manifoldWeightedIBP_of_atlasData", not affected. But `Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.globalWeightedIBP...` — the replacement of "SmoothOverlapAtlas.pouPiece" didn't touch it.

Let me fix the double prefix occurrences.
dsh: reasoning:
While it compiles, let me collect source hashes and prepare the ledger data. Also verify the D13 card's claims list.

Let me compute sha256 of the audited source files and the key interface files.

Also, let me think about how to verify the claim "the D13 card's expanded hypotheses and blockers closed are supported". I'll structure the ledger around:

1. Per-declaration entries with:
   - name, file, line, statement (verbatim captured from #check)
   - hypotheses list with domain/quantifier
   - classification: proved / conditional / model / statement-only / upstream
   - adversarial checks:
     - conclusion_equivalent_hypothesis: verdict
     - contradictory_or_vacuous: verdict
     - domain_checks: verdict ((0,T) vs (0,T] etc.)
     - model_vs_manifold: verdict
     - hidden_interface_fields: list
   - non_vacuity: witness + command + lines
   - axioms

2. Card claims audit: expanded_hypotheses claims matching the actual statements; blockers_closed support.

Let me now gather exact evidence: source hashes.
dsh: reasoning:
Progress. Issues:
1. `exists_contDiff_bump` unknown — it's `Poincare.D13.ManifoldIBP.exists_contDiff_bump`? With `open ...OverlapAtlas` not ManifoldIBP, the name isn't in scope. It's in namespace `Poincare.D13.ManifoldIBP`. Let me check where it's defined (SmoothPartition.lean namespace). Probably `Poincare.D13.ManifoldIBP`. I'll reference `Poincare.D13.ManifoldIBP.exists_contDiff_bump`.
2. `two_le_infty` unknown — it's used in SmoothAtlasIBP.lean; where is it defined? Probably `Poincare.D13.ManifoldIBP.two_le_infty` or in a `ContDiff` namespace. Let me grep.
3. `dilationAtlasTwo_chart_ne` unknown — check actual name: in SmoothAtlasModel I saw `dilationAtlasTwo_chart_zero` and `dilationChart_of_ne`. The "chart_ne" for dilationAtlasTwoSmooth may not exist; use `dilationChart_of_ne 2 hi` with the show. Let me check names in SmoothAtlasModel.
4. Line 101: `linarith failed` in exists_bump_in_halfSpace's hKU proof.
5. Line 118 "Function expected" — something in exists_bump_zero.
6. Line 150 "No goals to be solved" — extra tactic.
7. Type mismatches at 213, 233, 244 — probably due to `hχ2.differentiable (by simp)` or the `hχc'` thing; need to see.
8. `FiniteLifetimeEntropyBridge` unknown — it's `Poincare.D13.CertificateOn.FiniteLifetimeEntropyBridge` and the CertificateOn namespace isn't opened. HeatKernelBridge opens it. Add `open Poincare.D13.CertificateOn`.

Let me look at the detailed messages.
dsh: reasoning:
Findings:
1. `two_le_infty` is private in SmoothAtlasIBP; I need my own: `(show (2 : WithTop ℕ∞) ≤ ∞ from by norm_num)` or define a private lemma in my file. I'll define `private lemma two_le_infty' : (2 : WithTop ℕ∞) ≤ ((⊤ : ℕ∞) : WithTop ℕ∞) := by simp` — careful with `∞` notation; use the explicit form. Actually simplest: replace `two_le_infty` with `(by norm_num : (2 : WithTop ℕ∞) ≤ ((⊤ : ℕ∞) : WithTop ℕ∞))`. But `norm_num` worked in my scratch test. I'll define a local lemma `two_le_infty_audit`.

2. `exists_contDiff_bump` → `Poincare.D13.ManifoldIBP.exists_contDiff_bump`.

3. `SmoothOverlapAtlas` unknown — need to qualify: `Poincare.D13.ManifoldIBP.SmoothOverlapAtlas` in the abbrev type and in the `funext m; simp [Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.pouPiece, ...]`. Actually I replaced `SmoothOverlapAtlas.pouPiece` with the full path, but the abbrev type `SmoothOverlapAtlas (Vec 1) 1` still needs qualification. Let me add `abbrev SmoothAtlas := Poincare.D13.ManifoldIBP.SmoothOverlapAtlas`? Hmm, that could be nice: `abbrev SA (M : Type*) [MeasurableSpace M] (d : ℕ) := Poincare.D13.ManifoldIBP.SmoothOverlapAtlas M d`. Or just use the full name in the two abbrevs.

Also the `#check @Poincare...` lines printed fine.

4. Type mismatch `Vec (0+1)` vs `Vec 1`: `hsAtlas G1` with `G1 : ChartMetric 1` gives n inferred = 0, so `Vec (0+1)`; my `μ1` abbrev is typed `Measure (Vec 1)`. `Vec (0+1)` and `Vec 1` are defeq but not syntactically identical; the `exact` failed due to the `μ1` abbrev not unfolding? Since μ1 is an abbrev, it should unfold... The error says "has type ... Vec (0+1) ... but expected Vec 1". Since `Vec` is an abbrev for `Fin d → ℝ`, `Vec (0+1)` and `Vec 1` should be defeq by `Nat.add_zero`? Actually `0+1` reduces to `1` by `Nat.zero_add`/rfl. Hmm, `0 + 1` is definitionally `1`? `Nat.add 0 1` reduces to `1` by rfl (Nat.add is defined by recursion on the second argument: `add 0 1 = succ (add 0 0) = 1`). Yes defeq. But `hsAtlas G1` — the n is not 0 but an unassigned metavariable? It inferred n from `G1 : ChartMetric 1` by unifying `ChartMetric (n+1)` with `ChartMetric 1`: `n+1 =?= 1` → n := 0? Unification of `n+1` with `1` gives n := 0 but the resulting type keeps `0+1` vs `1`; defeq should handle that. Hmm, but the failure is real, so `Vec (0+1)` vs `Vec 1` isn't defeq? `Vec d := Fin d → ℝ`; `Fin (0+1)` vs `Fin 1` — these are defeq only if `0+1` reduces to `1`. `Nat.add 0 1`: `Nat.add` is defined by `add m 0 = m` and `add m (succ n) = succ (add m n)`? In Lean 4 core, `Nat.add` is defined with `[implemented_by]`? Actually `Nat.add` is a function with equations `n + 0 = n` and `n + succ m = succ (n + m)`. So `0 + 1 = succ (0 + 0) = succ 0 = 1`, which is definitional. So `Fin (0+1)` should be defeq `Fin 1`. 

But maybe n was inferred as something else. The message shows `Vec (0 + 1)` for the actual and `Vec 1` for expected. Weird that defeq fails. Maybe because `hsAtlas` is an `abbrev` with `n` explicit? Wait, `hsAtlas (G : ChartMetric (n+1))` with implicit n; when called as `hsAtlas G1`, does Lean infer n? Maybe n is left as a metavariable that gets assigned 0 during elaboration but the term keeps `?n` reduced to `0` — should be fine.

Hmm, but the failure might be because my `μ1` abbrev was defined with `A1` where `A1 : SmoothOverlapAtlas (Vec 1) 1 := hsAtlas G1` — the type ascription forces `Vec 1` but `hsAtlas G1` has type `SmoothOverlapAtlas (Vec (0+1)) (0+1)`. That's defeq so fine. Then `μ1 : Measure (Vec 1)`.

Then the actual statement from the theorem application has `Vec (0+1)`. So defeq check between the two integral statements failed — maybe not because of Vec but because of the measure: `((hsAtlas G1).globalMeasure volume).withDensity ((hsAtlas G1).weight fun x => 0)` vs `μ1 = (A1.globalMeasure volume).withDensity (A1.weight (fun _ => 0))`. Since A1 is an abbrev, it should unfold. Hmm.

Unless... `hsAtlas G1` is elaborated with a *different* n metavariable in each occurrence, e.g. n := 0 in A1 and n := ? in the theorem call, and the theorem call's n was inferred from `G1` as 0 too. So both `0+1`.

The issue may be that `exact` doesn't unfold `abbrev`s? Abbrevs are reducible, and `exact` uses `whnf` with default transparency which unfolds reducible definitions (`abbrev` is `@[reducible]`). Hmm.

Wait, maybe the real difference: expected type has `∂μ1` where μ1 := `(A1.globalMeasure volume).withDensity (A1.weight (fun _ => 0))`, and `A1 := hsAtlas G1` where the implicit n is a *different* metavariable? Actually `A1 : SmoothOverlapAtlas (Vec 1) 1 := hsAtlas G1` — when defining A1, the `n` of hsAtlas is inferred from the expected type `SmoothOverlapAtlas (Vec 1) 1`, i.e. n = 0. Fine.

Hmm, the statement of the theorem as printed:
```
∫ (m : Vec (0 + 1)), G1.driftLaplacian ... ∂((hsAtlas G1).globalMeasure volume).withDensity ((hsAtlas G1).weight fun x => 0) = ...
```
vs expected
```
∫ (m : Vec 1), ... ∂μ1 = ...
```
Since `μ1` is an abbrev, it unfolds to `(A1.globalMeasure volume).withDensity (A1.weight (fun _ => 0))` and `A1` unfolds to `hsAtlas G1`. So the only difference is `Vec (0+1)` vs `Vec 1`... and `(hsAtlas G1)` with possibly different implicit n instances.

Hmm — `hsAtlas` has signature `abbrev hsAtlas (G : ChartMetric (n + 1)) : SmoothOverlapAtlas (Vec (n+1)) (n+1)`, with `n` implicit. When elaborating, n is a metavariable; unification with `G1 : ChartMetric 1` requires `n+1 =?= 1`. Lean's unifier may assign `n := 1 - 1`? No, it would use `Nat.succ.inj` to get n := 0 (perhaps via `Nat.add` unification which is not injective on the first argument? `n + 1` is `Nat.add n 1` = `Nat.succ n` after reduction, so n := 0). OK.

Maybe the problem is that the two terms have different *implicit instance* arguments for `[MeasurableSpace (Vec ...)]` or the `volume` instance. Actually likely the difference is the `MeasureTheory.volume` instance on `Vec (0+1)` vs `Vec 1`: `volume` for `Fin (0+1) → ℝ` vs `Fin 1 → ℝ` — the Pi measure instances are defeq, fine.

Rather than debug, I can simply state my abbreviations to match exactly: define `A1 := hsAtlas (n := 0) G1` explicitly, and use `Vec 1` with `n := 0`? The theorem's type will still print `Vec (0+1)`.

Simplest workaround: don't use `exact`; use `simpa only [μ1] using (theorem ...)`. Or add `show ... from` with the right form. Or define my theorems' conclusions using the exact syntactic form produced by the theorem: write `Vec (0+1)` everywhere? That's ugly. Alternatively make the statement's measure `μ1` a plain `let`-free abbrev and use `simpa [μ1, A1]`.

Let me check: the error shows the actual has `(hsAtlas G1)` while expected has `μ1`. `exact` should unfold μ1 (reducible). Unless... `abbrev` in Lean 4 is `@[reducible]` — yes, `abbrev` is automatically reducible. Hmm.

Wait, maybe the definition of `μ1` in my file got mangled: `abbrev μ1 : Measure (Vec 1) := (A1.globalMeasure volume).withDensity (A1.weight (fun _ => 0))`. And `A1 : SmoothOverlapAtlas (Vec 1) 1 := hsAtlas G1`. Since `hsAtlas G1 : SmoothOverlapAtlas (Vec (0+1)) (0+1)`, the ascription requires defeq — accepted. So `A1` is a constant with value `hsAtlas G1`. 

The `exact` failed with a type mismatch, which means the unifier gave up at default transparency (`reducible`?) — `exact` uses `withTransparency .reducible`? For `exact`, Lean uses default transparency `.default` which unfolds reducible and semireducible? Actually `.default` = `.instances`+...? In Lean 4, `TransparencyMode.default` unfolds `reducible` definitions but not `semireducible`. `abbrev` is reducible, so `μ1` should unfold. Also `Vec` is `abbrev` so it unfolds too. And `A1` unfolds.

Unless the mismatch is the *implicit n* in `hsAtlas`: in expected, `A1` unfolds to `hsAtlas G1` where the implicit `n` argument is a *metavariable that got assigned* — when the abbrev was elaborated, n := 0 was assigned, so the term is `@hsAtlas 0 G1`? The printed form `hsAtlas G1` hides the implicit. Both should be `@hsAtlas 0 G1`.

Hmm, maybe the difference is the `Vec (0+1)` vs `Vec 1`: `Fin (0+1)` vs `Fin 1`. Is `0 + 1` defeq to `1` in Lean 4? `Nat.add` is defined in Init.Prelude with `@[extern]`? Let me check: Lean 4 core defines `Nat.add` as `protected def Nat.add : Nat → Nat → Nat | m, 0 => m | m, succ n => succ (m + n)`. So `0 + 1` = `Nat.add 0 (succ 0)` = `succ (Nat.add 0 0)` = `succ 0` = `1`. Defeq yes. Hmm, but the kernel's definitional reduction of `Nat.add` recursion is fine.

BUT: `Fin (0+1)` vs `Fin 1` — Fin is a structure with `val : Nat` and `isLt : val < n`. `Fin 1` vs `Fin (0+1)` — defeq if the Nat args are defeq. Fine.

Hmm, so why the mismatch? Let me look further down the log; maybe the mismatch is actually in the *measure*: actual `((hsAtlas G1).globalMeasure volume).withDensity ((hsAtlas G1).weight fun x => 0)` vs expected `μ1`. Since `μ1` is `Measure (Vec 1)` — hmm, the `Measure` type parameter: actual `Measure (Vec (0+1))` vs expected `Measure (Vec 1)`. 

Let me test a minimal example to understand. Actually, the simplest robust fix: define the abbreviations with `n := 0` explicit and declare my theorem conclusions with `Vec (0 + 1)`... no.

Alternative robust fix: use `simpa only [μ1, A1] using ...`. If the terms are defeq, `simpa` with the abbrevs unfolded will work (simpa uses `simp` which is more aggressive? simp uses reducible transparency too).

Hmm, actually maybe `exact` fails because of the `∂` notation: `∂μ1` elaborates to `Measure.restrict`? No, `∂` is notation for `MeasureTheory.Measure` application in integral notation: `∫ x, f x ∂μ` = `∫ x, f x ∂μ`. The `∂` is part of the notation, not a function.

Wait — look closely at the expected type: `∫ (m : Vec 1), G1.driftLaplacian (fun x => 0) χ m * χ m ∂μ1 = -∫ ...`. And actual: `∂((hsAtlas G1).globalMeasure volume).withDensity ((hsAtlas G1).weight fun x => 0)`. The parser: `∫ m, f m ∂μ` — `∂μ` binds μ. Fine.

Let me test in isolation. Actually, maybe the issue is that `A1` and the theorem call used different *universe/instance* for `volume`: `volume` on `Vec (0+1)` vs `Vec 1` requires the same instance; if `Vec (0+1)` unfolds to `Fin (0+1) → ℝ` and `Vec 1` to `Fin 1 → ℝ`, the `MeasureSpace` instances for Pi types are elaborated via typeclass search and could produce syntactically different but defeq terms. Normally fine.

Hmm, let me just test with a tiny snippet in the scratch file:

```lean
example (χ : Vec 1 → ℝ) (hχ2 : ContDiff ℝ 2 χ) (hcc : HasCompactSupport χ) (hsupp : tsupport χ ⊆ {y : Vec 1 | y 0 < 1}) :
    ∫ m, G1.driftLaplacian (fun _ => 0) χ m * χ m ∂μ1 = -∫ m, G1.gradInnerInverse χ χ m ∂μ1 :=
  halfSpaceAtlas_weightedIBP_unconditional (n := 0) G1 (fun _ => 0) χ χ contDiff_const hχ2 hχ2 hcc hsupp
```
which is exactly what failed. Let me test variations:
- `exact halfSpaceAtlas_weightedIBP_unconditional (n := 0) ...` with `μ1` replaced by the explicit measure.
- Or define μ1 without type ascription? 

Actually! I bet the issue is `A1 : SmoothOverlapAtlas (Vec 1) 1 := hsAtlas G1` — when I write `hsAtlas G1` here, Lean unifies `ChartMetric (n+1)` with `ChartMetric 1`, but since `G1 : ChartMetric 1` and `ChartMetric` is a structure, the unification `n+1 =?= 1` might assign `n := 0`... but then `A1` would have type `SmoothOverlapAtlas (Vec (0+1)) (0+1)`. Fine.

Hmm, what if `1` in `ChartMetric 1` is `OfNat.ofNat 1` and `n+1` is `HAdd.hAdd n 1` — during unification, `HAdd.hAdd n 1` whnf-reduces to `Nat.add n 1` = `Nat.succ n`; `1` whnf-reduces to `Nat.succ 0`; so `n := 0`. Then the *type* of `hsAtlas G1` is `SmoothOverlapAtlas (Vec (Nat.succ 0)) (Nat.succ 0)` = `Vec (0+1)`? The numeral `1` in `Vec 1` is `Nat.succ 0` too. Both defeq.

I'm going in circles. Let me just test empirically with `simpa` and `show`.

Given time, let me use a different approach: write the abbreviations *exactly* as the theorem produces:

```lean
abbrev G1 : ChartMetric (0+1) := ChartMetric.euclideanChartMetric (0+1)
```
Hmm, `euclideanChartMetric 1` where 1 = 0+1... 

Actually, better: let me look at what `#check @halfSpaceAtlas_weightedIBP_unconditional` printed: `∀ {n : ℕ} (G : ChartMetric (n + 1)) ...`. When called with `(n := 0)`, the result type is `∫ (m : Vec (0 + 1)), ... ((hsAtlas (n:=0) G1).globalMeasure volume)...`. My `μ1` uses `A1 = hsAtlas G1` where the implicit n is inferred as `?n` from the ascription `SmoothOverlapAtlas (Vec 1) 1`: unify `Vec (?n+1)` with `Vec 1` → `?n+1 =?= 1` → `?n := 0`. Fine.

Hmm, wait: the *actual* shows `(hsAtlas G1)` without `(n := 0)` — printer hides implicits. The expected shows `μ1`.

Let me try: maybe `abbrev` with Greek letter μ1 is fine but `∂μ1` parses as `∂ μ1` where `∂` is `MeasureTheory.Measure`... no, `∂` is notation in the integral.

OK, empirical debugging is faster. Let me write a scratch file with several variants and compile.

Also fix the other errors first:
- `exists_contDiff_bump` → qualified.
- `two_le_infty` → local.
- `SmoothOverlapAtlas` in abbrev type → qualified.
- `dilationAtlasTwo_chart_ne` → use `show A2.chart i = fun y => (2:ℝ) • y from dilationChart_of_ne 2 hi`? Let me check what lemmas exist for dilationAtlasTwo chart: in SmoothAtlasModel they used `show dilationChart (d := n+1) 2 i = fun x => (2:ℝ) • x; exact dilationChart_of_ne 2 hi`. So `dilationChart_of_ne 2 hi`. Is `dilationChart_of_ne` in namespace OverlapAtlas? Used unqualified in SmoothAtlasModel inside OverlapAtlas. But wait, is `dilationAtlasTwoSmooth`'s chart defeq to `dilationChart 2`? In SmoothAtlasModel they did `show (dilationAtlasTwoSmooth n G).chart i = fun x => (2:ℝ) • x` then `exact dilationChart_of_ne 2 hi`... Actually they wrote:
```
have hchart_ne : ∀ {i : ℕ}, i ≠ 0 → (dilationAtlasTwoSmooth n G).chart i = fun x => (2:ℝ) • x := by
  intro i hi
  show dilationChart (d := n + 1) 2 i = fun x => (2:ℝ) • x
  exact dilationChart_of_ne 2 hi
```
So mirror that.

- Line 150 "No goals to be solved": let me look at the exact context (line 150 in my file). It's in `exists_bump_zero`? Let me read lines 140-155 of my file.

- `FiniteLifetimeEntropyBridge` → `Poincare.D13.CertificateOn.FiniteLifetimeEntropyBridge`; add `open Poincare.D13.CertificateOn`.

- line 118 "Function expected at SmoothOverlapAtlas" → the abbrev `A1 : SmoothOverlapAtlas (Vec 1) 1` — qualify.

- line 101 linarith failure: in exists_bump_in_halfSpace, the goal after `refine ⟨?_, ...⟩` is `y 0 < 1`. I have `this : y 0 - c 0 ≤ |y 0 - c 0|`, `hcoord : |y 0 - c 0| ≤ dist y c ≤ r`, `hc : c 0 + r < 1`. Need: y 0 = (y 0 - c 0) + c 0 ≤ |...| + c 0 ≤ r + c 0 < 1. `linarith` should do it with hcoord, hdist, hc, this. It failed with `⊢ False` — because after `refine ⟨?_, ...⟩` I wrote `have : ... := le_abs_self _; linarith` — the goal is `y 0 < 1`, and linarith should solve. Hmm, it says linarith failed to find a contradiction with goal `False`? That happens when the goal was negated? Odd. Maybe `refine ⟨?_, closedBall_subset_ball (by linarith) hy⟩` — the `by linarith` there is the *second* goal (the ball inclusion needs `r < r+1`), and the "linarith failed" at line 101 might refer to that inner one... The error context shows goal `False` after `⊢` — with hy etc. It printed `⊢ False`, so it's from the `by linarith` in `closedBall_subset_ball (by linarith) hy`? That linarith is proving `r < r + 1` — trivial. Hmm.

Actually line numbers: my file line 101 is `    linarith` (the last line of the hKU proof). The context shows hcoord, this, hc, hdist, hr and goal False — that's the `linarith` in `closedBall_subset_ball (by linarith)`? No wait, `closedBall_subset_ball` needs `r < r+1`.

Let me look at my file lines 95-115 to map. Let me read the file region.

Let me do all these fixes and recompile. I'll read the relevant portion of my file first.
dsh: reasoning:
I see: line 99 `refine ⟨?_, closedBall_subset_ball (by linarith) hy⟩` — the `by linarith` is the *first* subgoal processed? No: `refine ⟨?_, closedBall_subset_ball (by linarith) hy⟩` — the `by linarith` inside the second component is elaborated first (before the first `?_`), and at that point the local context has `hy : y ∈ closedBall c r` but the `linarith` needs `r < r+1`; it should work... unless the `closedBall_subset_ball` argument's type is `r < r+1`, linarith solves it. But the error context shows the goal `False` in the context of the *first* subgoal's linarith at line 101. So line 101's `linarith` failed.

Goal after refine is `y 0 < 1`. Context has:
- hdist : dist y c ≤ r
- hcoord : |y 0 - c 0| ≤ dist y c
- this : y 0 - c 0 ≤ |y 0 - c 0|
- hc : c 0 + r < 1
Goal: y 0 < 1. linarith should combine: y0 - c0 ≤ |...| ≤ dist ≤ r, so y0 ≤ c0 + r < 1. Why fail? Because `hcoord` has `dist y c` and `hdist` has `dist y c` — linarith handles linear arithmetic over reals with `|y 0 - c 0|` and `dist y c` treated as atoms. `this : y0 - c0 ≤ |y0 - c0|`, `hcoord : |y0-c0| ≤ dist y c`, `hdist : dist y c ≤ r`, `hc : c0 + r < 1`. Target `y0 < 1`. This is linear: y0 - c0 ≤ r → y0 ≤ c0 + r < 1. linarith should find it... unless `dist y c ≤ r` is not syntactically `dist y c` in hdist — the error shows `hdist : dist y c ≤ r`, fine.

Wait, the error says "linarith failed to find a contradiction" and the goal displayed is `⊢ False`. In Lean 4, when the goal is `y 0 < 1`, linarith works by contradiction, so it shows `⊢ False` after negation. It failed. Maybe because `hc : c 0 + r < 1` and `hr : 0 < r`... Let's see: it needs `y0 - c0 ≤ |y0-c0|` — `this` provides that; but the chain is y0 ≤ c0 + r. From `this` and `hcoord` and `hdist`: y0 - c0 ≤ r, so y0 ≤ c0 + r; with hc: y0 < 1. That's linear. Strange.

Unless `linarith` can't handle the `dist`/`|·|` atoms because `hcoord`'s RHS `dist y c` and `hdist`'s LHS `dist y c` are syntactically identical? They look identical. Hmm, but there might be a coercion: `dist y c` in hcoord is `dist y c` where dist is `NNReal`? No, `dist : ℝ`.

Hold on — maybe the issue is the goal after `refine ⟨?_, ...⟩` is not `y 0 < 1`. `closedBall c r ⊆ {y | y 0 < 1} ∩ ball c (r+1)`: for `y ∈ closedBall c r`, membership in the intersection is `y 0 < 1 ∧ y ∈ ball c (r+1)`. The refine `⟨?_, ...⟩` gives first goal `y 0 < 1`. Fine.

Maybe linarith is confused by `y 0` (function application) — it treats `y 0` as an atom `y 0` and `c 0` as another; fine.

Let me just replace with a manual proof:
```lean
have h1 : y 0 - c 0 ≤ r := le_trans this hcoord |>.trans hdist
linarith
```
Actually `le_trans this hcoord : y0 - c0 ≤ dist y c`, then `.trans hdist : y0 - c0 ≤ r`. Then `linarith` with hc. That should work. Or directly `linarith [this, hcoord, hdist, hc]`. Let me use explicit `linarith only [...]`.

Hmm, one possible culprit: `|y 0 - c 0|` where `y 0 - c 0` might be elaborated as `HSub.hSub` with different instances... unlikely.

I'll rewrite as:
```lean
    have hle : y 0 - c 0 ≤ r := (le_trans (le_abs_self (y 0 - c 0)) hcoord).trans hdist
    linarith
```

Now the Vec (0+1) vs Vec 1 issue. Let me test quickly in a scratch file. Actually, let me think again: `abbrev G1 : ChartMetric 1 := ...`. The `1` here is `OfNat.ofNat 1`. In `hsAtlas G1`, the implicit n unifies `n+1 =?= 1`. `n+1` is `HAdd.hAdd n 1` → `Nat.add n 1` → stuck on `n` (since first arg is a metavariable, `Nat.add` doesn't reduce). Unification of `Nat.add ?n 1 =?= 1`: Lean's unifier has special support for `Nat.add`? It might not assign ?n := 0 but instead assign via `Nat.succ.inj`: `Nat.add ?n 1` whnf-reduces to `Nat.succ ?n` (since `Nat.add m (succ n)` reduces regardless of m? `Nat.add m (succ n) = succ (Nat.add m n)`, and `1 = succ 0`, so `Nat.add ?n 1` → `succ (Nat.add ?n 0)` → `succ ?n`). Yes! `Nat.add m 0 = m` reduces. So `?n+1` reduces to `succ ?n`, unify with `succ 0` → `?n := 0`. Good, defeq.

So `hsAtlas G1 : SmoothOverlapAtlas (Vec (0+1)) (0+1)` with the `0` literally inserted — and `Vec (0+1)` where `0+1` is `Nat.add 0 1` which reduces to `Nat.succ (Nat.add 0 0)` = `Nat.succ 0` = 1. So `Vec (0+1)` is defeq to `Vec 1`. 

Then why did `exact` fail?! Let me look at the full error message more carefully — maybe the mismatch is elsewhere, e.g. `G1.driftLaplacian (fun x => 0) χ` vs the same but with `fun x => 0` elaborated with different binder types. The printed actual has `G1.driftLaplacian (fun x => 0) χ` and expected `G1.driftLaplacian (fun x => 0) χ`. Looks the same.

Let me see the rest of the error message (it was truncated). Let me print more lines from the log around the first Type mismatch.
dsh: reasoning:
The mismatch is exactly `Vec (0+1)` vs `Vec 1`. So `Vec (0+1)` is NOT defeq to `Vec 1` in the kernel's eyes? That's surprising... unless `Vec` was elaborated with an explicit type argument that is `0+1` as an *expression* vs `1`. Definitional equality should still hold.

OH WAIT. I see it: `Vec (0+1)` — but `Vec` here is `Poincare.D12.VolumeIBP.Vec d := Fin d → ℝ`. `Fin (0+1)` vs `Fin 1`. `Fin` in Lean 4 is `Fin : Nat → Type` defined as a structure `{val : Nat // val < n}`. Definitional equality of `Fin (0+1)` and `Fin 1` requires `0+1` defeq `1`. Should be.

Hmm, but maybe Lean's `exact` uses `isDefEq` with default transparency, and the expected type mentions `μ1` which is an `abbrev` — abbreviations are reducible so should unfold. Unless the `μ1` did NOT unfold because it's a *local* abbrev in a namespace? `abbrev` = `def` with `@[reducible]`, should unfold.

The error message prints the expected type with `μ1` still folded, which suggests the unifier tried to unify `Vec (0+1)` with `Vec 1` and failed *first*, before unfolding μ1. And it failed because... hmm.

Wait — maybe `0 + 1` is NOT defeq to `1` because `HAdd.hAdd 0 1` for `Nat` uses `Nat.add` but with `instAddNat`; `Nat.add 0 1` should reduce. Actually in Lean 4, `Nat.add` is defined via `[implemented_by]` and the *equation compiler* with `@[simp]` lemmas; the kernel reduction of `Nat.add 0 1`: `Nat.add` is a recursive function defined by `Nat.rec`-style? In Lean 4 core:
```
protected def Nat.add : Nat → Nat → Nat
  | m, 0 => m
  | m, succ n => succ (m + n)
```
This compiles to a recursor-based definition where reduction on the *second* argument occurs. `Nat.add 0 (succ 0)` → `succ (Nat.add 0 0)` → `succ 0`. So defeq. I'm fairly confident.

Unless the printed `(0 + 1)` is actually `Nat.succ 0` pretty-printed as `0 + 1`? The printer might display `Nat.succ 0` as `1`, not `0+1`. It printed `Vec (0 + 1)` which means the expression literally contains `HAdd.hAdd 0 1`, not `Nat.succ 0`. That happens when the implicit `n` was assigned `0` and the type expression `n + 1` got the assignment substituted, leaving `0 + 1` *without* reduction. But defeq checking should reduce it...

Unless `HAdd.hAdd 0 1` doesn't reduce because there are *multiple* `HAdd` instances in scope and `Vec`'s argument `n + 1` was elaborated with a specific instance `instHAdd`... For Nat it's `instAddNat`, and `HAdd.hAdd` unfolds to `Nat.add`. Should reduce.

Hmm, is it possible that the `1` in `Vec 1` (my abbrev type) is not `OfNat.ofNat 1` but a *different* `OfNat` instance, e.g. `Nat` vs `ℝ`? In `abbrev μ1 : Measure (Vec 1)`, `Vec 1` — `1 : Nat` via OfNat Nat. In `hsAtlas`, `n + 1` where n : Nat — also Nat. Fine.

Let me just empirically test in a scratch:
```lean
example : (Vec (0+1) : Type) = Vec 1 := rfl
example (f : Vec 1 → ℝ) (x : Vec (0+1)) : f x = f x := rfl
```
Let me test quickly. If `rfl` works, then the mismatch must be elsewhere (the μ1 unfolding). If `rfl` fails, the type isn't defeq and I need `simp`/`norm_num` rewriting or `Nat.add_zero`.

Actually — I recall now: `0 + 1` in Lean 4 *is* defeq to `1`, but `Vec (0+1)` vs `Vec 1` — hmm.

Alternatively, the mismatch might be because the theorem application's `Vec (0+1)` is actually `Vec (0 + 1)` where the `HAdd.hAdd` was elaborated at a *different type*? No.

Let me stop speculating and test.

Other fixes needed:
- `hcc₂`/`hsupp₂` destructuring mismatch at line 257: I wrote `obtain ⟨χ₂, hsm₂, _h2, hcc₂, hsupp₂⟩ := exists_bump_in_halfSpace ...` but `exists_bump_in_halfSpace` returns `⟨χ, hsm, hrange, hone, hcc, hsupp⟩` — 6 components; I destructured 5 (missing hrange). So `hcc₂` got `hone` etc. Fix by adding `_hrange₂`: `⟨χ₂, hsm₂, _hr₂, _hone₂, hcc₂, hsupp₂⟩`.
- Similarly the first obtain in h3: `⟨χ₁, hsm₁, h1, hcc₁, hsupp₁⟩ := exists_bump_zero` — exists_bump_zero returns 5 components `⟨χ, hsm, h1, hcc, hsupp⟩` ✓ (that's fine).
- `two_le_infty` → local lemma.
- `exists_contDiff_bump` → qualified.
- `SmoothOverlapAtlas` → qualified in abbrev type.
- `dilationAtlasTwo_chart_ne` → `dilationChart_of_ne`.
- `FiniteLifetimeEntropyBridge` → qualified + open CertificateOn.
- Line 150 "No goals to be solved" — line 150 in the file: let me check. It's around `h1_cover_partial_ae_instantiated`? Line 150 is in the middle of the H1 proof. Let me view lines 138-215.

Let me read that region.
dsh: reasoning:
The `refine` at line 143 has 13 placeholders (?_ count): let me count the expected arguments after `...measurable ...measurable`: hvsupp, hfc, huc, hvc, hvcc, htsupp_b, hcover, hD, hG, hGuvsupp, hproper, hbd = 12 goals. My refine has `?_ (fun _ => contDiff_const) ?_ ?_ ?_ ?_ ?_ ?_ ?_ ?_ ?_ ?_` = 1+1+10 = 12? Let me count the `?_`: positions: `?_` (1), then `(fun _ => contDiff_const)` (hfc, solved inline), then `?_ ?_ ?_ ?_ ?_ ?_ ?_ ?_ ?_ ?_` = 10. Total goals to solve after the refine = 1 + 10 = 11, plus the inline solved one = 12. ✓. Then my subsequent bullet proofs: lines 150 (1), 155 (huc), 161 (hvc), 167 (hvcc), 168 (htsupp), 169 (hcover), 171 (hD), 179 (hG), 187 (hGuvsupp), 192 (hproper), 193 (hbd) = 11 bullets. ✓.

But the error at line 150 "No goals to be solved" suggests the refine consumed more than expected — maybe `measurable_const` didn't fill hf because of type inference, or `(fun _ => contDiff_const)` filled hfc AND huc? `(fun _ => contDiff_const)` is `∀ i, ContDiff ℝ 2 (fun y => (fun _ => 0) (A.chart i y))` — fine.

Actually the "No goals to be solved" error at line 150: it means after the first `?_`... hmm, possibly because of the earlier error at line 118 (`SmoothOverlapAtlas` unknown), the type of A1 is broken so the refine's argument structure was mis-parsed. Cascading errors. Let me fix the definite issues first and recompile.

Also line 149's placeholder count might be off; I'll re-check after fixing Vec.

Let me now test the Vec defeq question in scratch and apply all fixes.

Fixes list:
1. `exists_bump_in_halfSpace`: use explicit hle.
2. `exists_contDiff_bump` → `Poincare.D13.ManifoldIBP.exists_contDiff_bump` (2 occurrences).
3. `SmoothOverlapAtlas` in abbrev types → `Poincare.D13.ManifoldIBP.SmoothOverlapAtlas` (A1, A2).
4. `two_le_infty` → local `two_le_infty_audit`.
5. `FiniteLifetimeEntropyBridge` → `Poincare.D13.CertificateOn.FiniteLifetimeEntropyBridge`; add `open Poincare.D13.CertificateOn`.
6. H3 obtain: fix 6-tuple.
7. `dilationAtlasTwo_chart_ne` → `show ... from dilationChart_of_ne 2 hi`.
8. Vec mismatch: test and fix (maybe use `show` with explicit `Vec (0+1)` or `simpa`).

Let me test Vec defeq.
dsh: reasoning:
Need `open MeasureTheory` and `noncomputable section`. In my main file I have `noncomputable section` and `open MeasureTheory`, so the abbrev issue is only in this scratch. Let me add those.

Note: `abbrev` with noncomputable content fails to compile as an abbrev! "failed to compile definition, consider marking it as 'noncomputable' because it depends on 'hsAtlas', which is 'noncomputable'". In my main file, `abbrev A1` is inside `noncomputable section`, so it should be fine (the section marks it noncomputable). Good.

Let me fix scratch.
dsh: reasoning:
Interesting! The scratch compiles: `rfl` proves `Vec (0+1) = Vec 1`, and the `exact`-style application works with `μ1`. So why did the main file fail? Difference: in the main file I have `open Poincare.D13.HeatKernelBridge` and `open Poincare.Longrun.Entropy` too — those might shadow `Vec`? No, the error said ambiguous earlier, now qualified. Hmm.

Wait, in the main file the failing theorem h2_statement is at line 209-213, and the error says the expected type is `Vec 1, ... ∂μ1`. The scratch's version works. Difference: in the main file, `G1` and `μ1` are the same. Hmm, but the main file also has `open Poincare.D13.ManifoldIBP.SmoothOverlapAtlas` — could that introduce a different `Vec`? No.

Maybe the problem is that in the main file some earlier error poisoned `μ1`'s definition. Specifically, `abbrev A1 : SmoothOverlapAtlas (Vec 1) 1 := hsAtlas G1` failed to elaborate because `SmoothOverlapAtlas` was unknown (error at line 118) — so `A1`'s definition is `sorry`-like, making `μ1` garbage. Yes! That's the cascade. Once I qualify `SmoothOverlapAtlas`, everything downstream should work.

Let me now apply all fixes.
dsh: reasoning:
Oops, my h3 fix used a placeholder name. I need to replace it with the actual call to `exists_bump_in_halfSpace`. Let me fix that.
dsh: reasoning:
While compiling, let me continue the semantic analysis and start drafting the ledger JSON structure. Let me collect the exact hypothesis lists with line numbers for each headline. I have most already. Let me also verify some remaining questions:

1. Does `halfSpaceAtlas_weightedIBP_unconditional` have any hidden assumption via `hsAtlas G` being a specific atlas? No.

2. Check that `dilationAtlasTwo_weightedIBP_via_pou` requires `hψ_sum : ∀ x ∈ tsupport V, ∑ i, ψ i x = 1` — the POU only needs to sum to 1 on tsupport V, fine.

3. Hidden interface fields for H6: integral_decomp, weighted_laplacian_compat, grad_inner_compat. I'll list from `#print`.

4. For H1: is `hcover` conclusion-equivalent? No. But there's a subtle point worth checking: is `hcover` *implied* by the other hypotheses (making it redundant/harmless)? Not implied — the atlas cover gives ⋃ i chart i '' source i = univ ⊇ chart b '' source b, but hcover restricts to the *finite* subfamily chartOf. So hcover is a genuine finiteness hypothesis.

5. Is `hproper` needed? It's used for the compact support of transition preimages in the POU construction. For the half-space model it holds.

6. Are any hypotheses contradictory? Consider an atlas where `hvsupp` and `htsupp_b` conflict with `hcover`... no.

7. "wrong domains ((0,T) vs (0,T])": For the heat kernel, the bridge's `f_derivative`/`weighted_ibp`/etc. are on `Ioo a b` = (0,t1) and regularity on `Icc a b`; monotonicity on `Icc`. That's the correct combination. One thing to check: `monotoneOn_of_bridge` uses `monotoneOn_of_deriv_nonneg` which requires `Convex ℝ (Icc a b)` and derivative on the interior. Fine.

8. Potential issue: in `finiteLifetimeEntropyBridge_gaussian`, the `conjugate_measure_evolution` field is required for all `x : Vec 2` at interior t. The proof uses `gaussian_conjugate_heat τ0 t ...` which requires `0 < τ0 - t`; they pass `lt_trans ht.1 (lt_trans ht.2 ht1)`. Fine.

9. Model-vs-manifold: H7/H8 are Euclidean Vec 2 model statements. H2/H3/H4/H5 are model atlases. H1 is general but conditional on atlas structure. H6 is conditional on interface fields. The card classifies accordingly.

10. One more check: the D13 card claims the `globalWeightedIBP_of_cover_partial_ae` has "measurability" hypotheses; and it says "NO POU and NO integrability hypotheses" — true.

11. Card claim: "No hypothesis is equivalent to a conclusion" — I should evaluate each headline:
   - H1: hD/hG identify Du/Guv with the Laplacian/pairing. The conclusion is the IBP identity for those. Not equivalent per se. But one could argue: given hD/hG, the conclusion is *exactly* the statement "the chart-level weighted IBP holds for every chart, assembled" — which is proven separately (D12 chart IBP) and is not an assumption. So no.
   - H6: the interface fields essentially *define* μ and the operators such that the conclusion reduces to ChartSumData.globalWeightedIBP. Is `integral_decomp` + `weighted_laplacian_compat` + `grad_inner_compat` equivalent to the conclusion? No: the fields are stronger/structural (an integral decomposition for all integrable g and pointwise operator identities). The conclusion is one identity. So not equivalent, but the theorem is a *transfer* result whose content is mostly in the interface. That's the honest reading.
   - H7: the bridge structure's fields are exactly what's needed for monotonicity; H7 proves them for the Gaussian. No conclusion-equivalence.
   - H8: proved from the bridge + an explicit upper bound; no circularity. The `hbound` is discharged by computing F = 1/(τ0-t) ≤ 1/(τ0-t1), not assumed.

12. Check the claim in the card's expanded_hypotheses #4: "halfSpaceAtlas_greenIdentity / _dirichletEnergy: two C² compactly supported functions with topological support in the base chart source." For `halfSpaceAtlas_greenIdentity`, yes (U and V). For `_dirichletEnergy`, one function V (used twice). Fine.

13. The card §3 item 5 lists `integrable_globalMeasure_withDensity_of_supported` hypotheses: "measurability of f and g, support of g in one chart image, and integrability of the weighted chart expression". Verified (IntegrableTransfer.lean:99-104).

14. Claim in §5 row "U7-GLOBAL-INTEGRABILITY": "consumed by hintL/hintR of halfSpaceAtlas_weightedIBP_unconditional and of globalWeightedIBP_of_cover_partial_ae". As noted, these theorems have NO hintL/hintR hypotheses; the integrability is constructed internally (local `hintL`/`hintR` in the proofs). This is imprecise but not a false mathematical claim. I'll flag it as a minor documentation inaccuracy (low severity).

15. The card says in §2/semantic_class: "explicit-POU unconditional IBP on the partial half-space atlas and its two cover-route instances" — verified.

16. The card's "POU construction: exists_smooth_partitionOfUnity_subordinate applied to the finite open cover overlapOf ... ∩ ball 0 (R+1) inside globalWeightedIBP_of_cover_partial_ae" — verified in POUConstruction lines 89-101.

Wait, one thing to check: in POUConstruction, the construction uses `exists_smooth_partitionOfUnity_subordinate W hWo hvcc hKW`. Let me check that lemma's statement (in SmoothPartition.lean) — does it require anything else (like local finiteness or second countability)? Let me read its signature to be sure the POU hypothesis is truly discharged.

Let me also check `exists_smooth_partitionOfUnity_subordinate`'s statement for hidden hypotheses.
dsh: reasoning:
Good progress! Remaining issues:
1. `two_le_infty_audit` unknown — my `private lemma` was inserted but the replacement of `two_le_infty` inside the lemma name happened? Let me check: I inserted the lemma then did `s.replace("two_le_infty","two_le_infty_audit")` — that also renamed the declaration itself to `two_le_infty_audit_audit`? No wait — order: I first replaced the opens block inserting the lemma named `two_le_infty_audit`, then later `s.replace("two_le_infty","two_le_infty_audit")` renamed it to `two_le_infty_audit_audit`. Let me check the file.

2. `support_gradInnerInverse_subset` unknown — need to qualify or import. It's likely `Poincare.D13.EuclideanChart.ChartMetric.support_gradInnerInverse_subset`. Let me grep.

3. linarith at line 105 still failing — same hKU proof? Let me look at the error context.

4. line 306, 385, 390: rewrite failures in H5 (chart rewrites).
5. line 367: `SmoothOverlapAtlas.pouPairing` (unqualified since my replacement only handled pouPiece/lift).
6. line 308/347: application type mismatch.

Let me look at the details.
dsh: reasoning:
Fixes needed:
1. Rename `two_le_infty_audit_audit` → `two_le_infty_audit`. But `private` lemmas are fine to use within the file. Let me fix.
2. Line 306: in `hχc'`, `rw [hchart0]` fails for `A2.chart (↑0)` where `↑0` is `(0 : Fin 2)` cast to Nat, syntactically `↑⟨0,...⟩` not `0`. Need `show`-based approach: `fin_cases i` instead of `by_cases hi : i = 0`. Use `fin_cases i` and then `· change ContDiff ℝ 2 fun y => χ (A2.chart 0 y); rw [hchart0]; ...`. Since `Fin 2` has two elements, `fin_cases i` gives goals with `i = 0` and `i = 1` concretely. Let me restructure:
```lean
  have hχc' : ∀ i : Fin 2, ContDiff ℝ 2 (fun y : Vec 1 => χ (A2.chart i y)) := by
    intro i
    fin_cases i
    · change ContDiff ℝ 2 (fun y : Vec 1 => χ (A2.chart 0 y))
      rw [hchart0]
      simpa [Function.comp_def] using hχ2.comp (contDiff_const_smul (1 : ℝ))
    · change ContDiff ℝ 2 (fun y : Vec 1 => χ (A2.chart 1 y))
      rw [show A2.chart 1 = fun y : Vec 1 => (2 : ℝ) • y from dilationChart_of_ne 2 (by norm_num)]
      simpa [Function.comp_def] using hχ2.comp (contDiff_const_smul (2 : ℝ))
```
3. Lines 385-390: the `ψ` I pass is `fun j : Fin 2 => if j = 0 then ψ₀ else fun _ => 0`, but in the goal it shows `(if (fun i => i) ⟨1,⋯⟩ = 0 then ψ₀ else ...)` — the `j` in `hintL j` is a `Fin 2` but the theorem's `hintL`/`hintR` are applied to `j : Fin 2`, and my `fin_cases j` gives `j = 0`/`j = 1` as `Fin 2` terms, but the goal's `j` is displayed as `↑((fun i=>i) ⟨1,⋯⟩)`. Hmm, in the refine, the `ψ` argument is `(fun j : Fin 2 => if j = 0 then ψ₀ else fun _ => 0)`, and the goal for hintL is `∀ j : Fin 2, Integrable (...) (ψ j)`. When I `intro j; fin_cases j`, the goal should have `ψ 1` = `if (1:Fin 2) = 0 then ψ₀ else 0` which needs `simp` to reduce the if. The displayed goal shows `(if (fun i => i) ⟨1, ⋯⟩ = 0 then ...)` — the `j` wasn't substituted by `fin_cases`; `fin_cases` on `Fin 2` produces goals with `j` replaced by `⟨0, _⟩`/`⟨1, _⟩` but the occurrence inside `ψ` may not rewrite because `ψ` was not unfolded (it's a lambda). After `fin_cases j`, `simp only [Fin.isValue, ↓reduceIte]` should reduce. In my code I did `rw [hpiece1]` which failed because the target still has the `if`. Let me use `simp only [...]` before rewriting:
```lean
    · simp only [Fin.isValue, Fin.reduceEq, ↓reduceIte]
      rw [hpiece1]
      simpa using (integrable_zero (Vec 1) μ2)
```
The `if (1:Fin 2) = 0` needs `decide`/`Fin.reduceEq`. `simp` should handle `(1 : Fin 2) = 0` via `Fin.isValue`/`reduceIte`. Let me write `simp only [Fin.isValue, one_ne_zero, ↓reduceIte]`? The condition is `(1 : Fin 2) = 0`; `simp` knows `Fin.isValue` and `OfNat.ofNat_ne_zero`? Let's just use `simp` (full) to normalize the ite.

4. Line 347 application type mismatch — need details. Let me look at log lines 258-300.

5. `support_gradInnerInverse_subset` unknown — qualify. Let me find its full name.

6. linarith at 105 still failing — let me see the context at log lines ~203.

Let me view the log around 195-232 and 258-300.
dsh: reasoning:
1. linarith at line 105: goal `y 0 < 1`, hypotheses: hdist, hcoord, hle : y0 - c0 ≤ r, hc : c0 + r < 1. Wait — `hle` is `y 0 - c 0 ≤ r`, and hc is `c 0 + r < 1`. To derive y0 < 1: y0 ≤ c0 + r < 1. linarith should do it. Why "failed to find a contradiction" with goal False? Odd. Unless `hc : c 0 + r < 1` and the target `y 0 < 1` involve `c 0` and `y 0` as applications — linarith handles atoms. Hmm.

Wait, maybe the problem is the goal is not `y 0 < 1` but something with a coercion, e.g. `y 0 < 1` where `1 : ℝ` vs the set `{y | y 0 < 1}` elaborated with `OfNat.ofNat 1 : ℝ`. Should be fine.

Could there be a *type* issue: `y : Vec (n+1)` where `Vec (n+1) = Fin (n+1) → ℝ`, `y 0 : ℝ`, fine.

Hmm, linarith failing on such a simple goal is suspicious. Maybe the issue is that `linarith` can't handle the fact that `hle` was derived with `le_trans` involving `|...|`; but hle is stated as `y 0 - c 0 ≤ r`. Then hc: `c 0 + r < 1`. Goal `y 0 < 1`. Linear: (y0 - c0 ≤ r) + (c0 + r < 1) → y0 < 1. Yes.

Unless `linarith` treats `y 0` and `c 0` correctly but the `1` in the goal is `OfNat` at type ℝ while in `hc` also ℝ. Fine.

Let me test this tiny proof in isolation. Actually, maybe the issue is that the goal after `refine ⟨?_, ...⟩` isn't `y 0 < 1` but `y ∈ {y | y 0 < 1}` i.e. `y 0 < 1` — same.

Hmm, the error says "linarith failed to find a contradiction", and prints context ending with `⊢ False`. So the goal was `y 0 < 1`. With hypotheses including `hle : y 0 - c 0 ≤ r` and `hc : c 0 + r < 1`. linarith must derive `y0 - 1 < 0`. It should.

OH WAIT. Maybe the issue is that `hle` has `r` where `r : ℝ` but the `c 0` appears as `c 0` and everything is fine...

Let me test in scratch:
```lean
example (c y : ℝ) (r : ℝ) (hle : y - c ≤ r) (hc : c + r < 1) : y < 1 := by linarith
```
If that works, something else is off. Maybe the `hle` I produced has a coercion to `NNReal`? No.

Hmm — could `y 0` be parsed as `y` applied to `0 : Fin (n+1)` and c 0 similarly; linarith treats them as atoms. It compares atoms syntactically; `y 0` in the goal and in hle are identical.

Let me just test the exact context via a scratch file. Actually, easier: replace `linarith` with an explicit `nlinarith`/`linarith only [hle, hc]` or a calc:
```lean
have : y 0 ≤ c 0 + r := by linarith only [hle]
linarith only [this, hc]
```
If linarith is being weird, explicit `linarith only [...]` may help. Or use `nlinarith`.

Alternatively, avoid the whole issue: choose U = {y | y 0 < 1} ∩ ball c (r+1), and prove K ⊆ U by:
```lean
intro y hy
have hdist : dist y c ≤ r := hy
constructor
· have h1 : y 0 ≤ c 0 + dist y c := by
    have := norm_le_pi_norm (y - c) 0
    ... 
```
Let me just test the simple linarith first.

2. `support_gradInnerInverse_subset` is in `Poincare.D13.ManifoldIBP` (SmoothAtlasPartial.lean:39) — need qualification `Poincare.D13.ManifoldIBP.support_gradInnerInverse_subset`.

3. `hχc'` type: it's `∀ i : Fin 2, ContDiff ℝ 2 fun y => χ (A2.chart (↑i) y)` but `measurable_pouPairing` expects `ContDiff ℝ 2 fun y => u (A.chart i y)` with `i : ℕ`. So pass `(hχc' 0)` with the right coercion. Let me define `huc0 : ContDiff ℝ 2 (fun y => χ (A2.chart 0 y)) := hχc' 0`? The coercion `(↑(0:Fin 2) : ℕ)` vs `0` — need `simpa` or use `hχc' 0` (defeq?). I'll define hχc0 explicitly:
```lean
  have hχc0 : ContDiff ℝ 2 (fun y : Vec 1 => χ (A2.chart 0 y)) := by
    rw [hchart0]; simpa [Function.comp_def] using hχ2.comp (contDiff_const_smul (1:ℝ))
  have hχc1 : ContDiff ℝ 2 (fun y : Vec 1 => χ (A2.chart 1 y)) := ...
```
Then use `hχc0` for i=0 and `hχc1` for i=1. Simpler.

4. `SmoothOverlapAtlas.pouPairing` at line 367 — my hpair1 used `SmoothOverlapAtlas.pouPairing` (not replaced since I only replaced pouPiece and lift). Qualify fully.

5. The `if` reduction: use `simp only [Fin.isValue, ↓reduceIte]` or `decide`-based. Let me instead avoid the `if` entirely by defining ψ via `Fin.cases`? Cleaner: define
```lean
  let ψ : Fin 2 → Vec 1 → ℝ := fun j => if j = 0 then ψ₀ else 0
```
and prove helper lemmas `ψ_zero : ψ 0 = ψ₀` and `ψ_one : ψ 1 = 0` by `simp [ψ]`. Then `rw [show (fun j => ψ j) ...]`. Actually the issue is the goal displays `ψ` unfolded with the `if` not reduced because `1 : Fin 2` vs `0 : Fin 2` decision needs `decide`. In Lean, `if (1 : Fin 2) = 0 then ... else ...` — the instance `Decidable (1 = 0)` for Fin is decidable; `simp` should reduce via `Fin.isValue` and `decide`? Hmm, `↓reduceIte` requires the condition to be literally `True`/`False` after reduction; `(1 : Fin 2) = 0` reduces to `False` via `decide`-based Decidable instance? `instDecidableEqFin` is defined by deciding on val equality, which for literals reduces. `simp` has `Fin.isValue` and `reduceIte`; typically `simp` handles `if (1 : Fin 2) = 0`. Let me just use `simp only [Fin.isValue, one_ne_zero, if_false]`? Hmm `one_ne_zero` is for `0 ≠ 1` in Nat.

Simplest robust approach: `fin_cases j` then `simp` (full simp) to normalize. Full `simp` worked in `hψ_sm`? Let's see: for hψ_supp I wrote `fin_cases j; · simpa using hψ₀supp; · simp [tsupport]` — the second branch `simp [tsupport]` may not have worked (no error reported for line ~375? Actually errors at 385/390 only). Let me restructure using a local definition and explicit lemmas:

```lean
  let ψ : Fin 2 → Vec 1 → ℝ := fun j => if j = 0 then ψ₀ else fun _ => 0
  have hψ0 : ψ 0 = ψ₀ := by simp [ψ]
  have hψ1 : ψ 1 = fun _ => 0 := by simp [ψ]
```
Then in the refine, pass `ψ`. The hint goals will be `Integrable (fun m => ... (ψ j) m) ...` for `j : Fin 2`; after `fin_cases j`, the goal has `ψ 0`/`ψ 1` which I can `rw [hψ0]`/`rw [hψ1]` (maybe after `change`).

For hψ_sm: `intro j; fin_cases j <;> simp [hψ0, hψ1, hψ₀sm]` — hmm, need ContDiff of ψ 0 and ψ 1. Use:
```lean
  · intro j
    fin_cases j
    · rw [hψ0]; exact hψ₀sm
    · rw [hψ1]; exact contDiff_const
```
But `fin_cases j` on `j : Fin 2` in a `∀ j` goal: after `intro j; fin_cases j`, goal 1 is `ContDiff ℝ ∞ (ψ 0)` (with ψ not unfolded). `rw [hψ0]` then works. 

Same for hψ_supp:
```lean
  · intro j
    fin_cases j
    · rw [hψ0]; exact hψ₀supp
    · rw [hψ1]; simp [tsupport]
```
`tsupport (fun _ => 0) = ∅`; `simp [tsupport]` should close `∅ ⊆ ball ...`.

For hψ_sum: goal `∀ x ∈ tsupport χ, ∑ i, ψ i x = 1`; `intro x hx; rw [Fin.sum_univ_two, hψ0, hψ1]; simp [hψ₀one x hx]`. Hmm, `hψ0 : ψ 0 = ψ₀` is a function equality, `rw [hψ0]` in `∑ i, ψ i x` — the sum is `ψ 0 x + ψ 1 x`; rw should rewrite inside.

For hintL:
```lean
  · intro j
    fin_cases j
    · rw [hψ0]; exact hintL0
    · rw [hψ1]; rw [hpiece1]; exact integrable_zero _ _
```
Wait `hpiece1 : A2.pouPiece 0 χ (fun _ => 0) = fun _ => 0`; after `rw [hψ1]` the integrand becomes `Δχ m * A2.pouPiece 0 χ (fun _ => 0) m`, so `rw [hpiece1]` should work (hpiece1 is a function equality; `rw` rewrites `A2.pouPiece 0 χ (fun _ => 0)` to `fun _ => 0`). Then `simp` gives `Integrable (fun m => Δχ m * 0) μ2`; need congruence to zero: `simpa using (integrable_zero (Vec 1) μ2)`.

Similarly hintR.

For hχc' I'll create hχc0/hχc1.

Let me also double check `A2.measurable_pouPairing 0 0 hχc0 hχc0 hψ₀sm` — `measurable_pouPairing (b i) (huc : ContDiff ℝ 2 fun y => u (A.chart i y)) (hvc) (hψ)`. With u=v=χ and i=0: needs `ContDiff ℝ 2 (fun y => χ (A.chart 0 y))` = hχc0. ✓.

6. `A2.pouPiece_apply hT 0 χ ψ₀ y` — check it works with A2's dot notation. It should.

7. For the `hintL0` transfer, I wrote `A2.integrable_globalMeasure_withDensity_of_supported volume 0 (f := ...) (g := ...) measurable_const ?_ ?_ ?_` — the hypothesis order: `(μ) (i) {f g} (hf : Measurable f) (hg : Measurable g) (hsupp) (hint)`. Wait the signature: `(μ) [IsAddHaarMeasure] (i) {f g} (hf) (hg) (hsupp) (hint)`. I passed `measurable_const ?_ ?_ ?_`: hf := measurable_const (for f := fun _ => 0), hg := ?, hsupp := ?, hint := ?. That's 4 goals: I provided ?_ for hg, ?_, ?_ = 3 placeholders. ✓ (I wrote `measurable_const ?_ ?_ ?_`).

Hmm, but in my code I have:
```lean
    refine A2.integrable_globalMeasure_withDensity_of_supported volume 0
      (f := fun _ => 0) (g := fun m => ...) measurable_const ?_ ?_ ?_
    · exact (G1.driftLaplacian_continuous ...).measurable.mul (...)
    · intro m _ ...
    · refine (Continuous.integrable_of_hasCompactSupport ?_ ?_).congr ?_
```
✓.

8. `μ2` vs the theorem's measure: should be fine.

9. Also errors at 442/443: in `h7_bridge_instantiated`? Those are `two_le_infty_audit` unknown (rename will fix).

Let me now apply all these fixes with careful edits. I'll rewrite the H5 section and fix the small things.

Also the linarith issue: let me test the minimal example.

Let me apply fixes:
(a) rename lemma back to `two_le_infty_audit`.
(b) `support_gradInnerInverse_subset` → `Poincare.D13.ManifoldIBP.support_gradInnerInverse_subset`.
(c) replace hχc' block with hχc0/hχc1.
(d) rewrite the ψ handling in H5.
(e) linarith → explicit calc.

For (e), let me test first.
dsh: reasoning:
Both work in isolation! So why did linarith fail in the main file at line 105? Let me look at the exact code at line 99-106 now (after edits). Maybe my `hle` line has a type annotation issue or the goal is different: `refine ⟨?_, closedBall_subset_ball (by linarith) hy⟩` — the first `?_` is the *second* goal in the refine order? No: `⟨?_, ...⟩` the first component. Hmm.

Wait — maybe the failing linarith is the `(by linarith)` inside `closedBall_subset_ball`! In the log context, `hy : y ∈ closedBall c r`, and the goal for that linarith would be `r < r + 1` — trivial. But the context printed `hle : y 0 - c 0 ≤ r` and `⊢ False`, meaning the goal was `y 0 < 1` (negated). So it's the outer linarith.

Let me look at the current file lines 93-106 to see exactly what's there.
dsh: reasoning:
The code looks identical to the working scratch. Difference: the scratch used `Vec 2` with `(0 : Fin 2)` for `norm_le_pi_norm`, while here `Fin (n+1)`. And `linarith` failed at line 105. Hmm, in the scratch I used a `Vec 2` and it worked. The only difference is `hcoord`'s proof: here via `rw [dist_eq_norm]; simpa using norm_le_pi_norm (y - c) (0 : Fin (n+1))`. In the scratch I took hcoord as a hypothesis.

Maybe the derived `hle` isn't what I think because `le_abs_self` produced something with a different elaboration... The log showed `hle : y 0 - c 0 ≤ r` and `⊢ False` (goal `y 0 < 1`), with `hc : c 0 + r < 1`. That's exactly the scratch case which works. Unless... the log I read was from compile3 which used the *old* code? The linarith error line 105 in compile3 corresponds to the current line 105 (`linarith`) — yes, after my edit the hle line was already added (I applied it before compile3). And it still failed.

Difference from scratch: in the main file, `open Poincare.D13.ManifoldIBP.OverlapAtlas`, `open Poincare.D13.HeatKernelBridge`, `open Poincare.Longrun.Entropy`, `open Poincare.D13.CertificateOn` — could `linarith` be affected by notations? Unlikely.

Hmm wait — maybe the goal is not `y 0 < 1` but `y ∈ {y | y 0 < 1}` where the `1` is elaborated with `OfNat.ofNat 1` at type ℝ... `linarith` normalizes.

Let me try to reproduce more faithfully in scratch: define the same theorem with `n` and the same hcoord proof, compile. Actually, maybe the problem is in the `refine ⟨?_, closedBall_subset_ball (by linarith) hy⟩` — the *first* `by linarith` inside the second component is elaborated BEFORE the first `?_`, and at that point... no, the context would still lack hle (hle is after the refine).

AH WAIT. I see it now! `closedBall_subset_ball (by linarith) hy` — the `by linarith` there is a tactic block for the *inequality* argument `r < r+1`. But the error context shows `hle` present, meaning the error is the outer `linarith` at line 105 — after hle. Goal `y 0 < 1`.

Let me test the exact snippet including `refine ⟨?_, closedBall_subset_ball (by linarith) hy⟩` structure. Maybe the `?_` first goal is not `y 0 < 1` but `y ∈ ... ∧ ...`? With `refine ⟨?_, ?_⟩` the first goal is the first component of the conjunction: `y 0 < 1`. Fine.

Hmm, but `⊢ False` with all those hypotheses means linarith's proof by contradiction. Let me try `linarith only [hle, hc]` in the main file — maybe `linarith`'s preprocessing chokes on `dist`/`|·|` atoms elsewhere in the context (e.g., `hcoord : |y 0 - c 0| ≤ dist y c` and `hdist`, plus `hy : y ∈ closedBall c r` which unfolds to `dist y c ≤ r`? There's both `hy` (unfolded to `dist y c ≤ r` in the log? The log shows `hy : y ∈ closedBall c r`) — the `|...|` atom might confuse linarith's ring normalization? Unlikely.

Let me be pragmatic: replace the final `linarith` with:
```lean
    have : y 0 ≤ c 0 + r := by linarith only [hle]
    linarith only [this, hc]
```
If `linarith only [hle]` works, fine. Or use `nlinarith only [...]`. Or simply:
```lean
    calc y 0 = (y 0 - c 0) + c 0 := by ring
      _ ≤ r + c 0 := by linarith only [hle]
      _ < 1 := by linarith only [hc]
```
Let me use the calc version — fully robust.

Let me now apply all fixes with a Python script for precision, rewriting the H5 section.

Actually, let me rewrite the H5 theorem entirely via the edit tool. First, let me view the current H5 section.
dsh: reasoning:
Now let me apply the H5 fixes:

1. Rename `two_le_infty_audit_audit` → `two_le_infty_audit` (fix the private lemma name).
2. Replace hχc' block with hχc0/hχc1.
3. Fix hpair1 qualification: `Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.pouPairing`.
4. Use a local `ψ` definition with helper equalities.
5. Fix linarith in exists_bump_in_halfSpace with calc.
6. Qualify `support_gradInnerInverse_subset`.
7. Fix `hψ₀one` usage: `Poincare.D13.ManifoldIBP.exists_contDiff_bump` returns hone : ∀ x ∈ K, χ x = 1. In h5_nonvacuous I destructure it as `hψ₀one`; then `fun x hx => hψ₀one x hx` where hx : x ∈ tsupport χ ✓.

Let me do the edits.

For the ψ part, rewrite:
```lean
  refine dilationAtlasTwo_weightedIBP_via_pou (n := 0) G1 (fun _ => 0) χ χ
    contDiff_const hχ2 hχ2 hχc (R := R)
    (fun j : Fin 2 => if j = 0 then ψ₀ else fun _ => 0)
    ?_ ?_ ?_ ?_ ?_
```
→
```lean
  let ψ : Fin 2 → Vec 1 → ℝ := fun j => if j = 0 then ψ₀ else fun _ => 0
  have hψ0 : ψ 0 = ψ₀ := by simp [ψ]
  have hψ1 : ψ 1 = fun _ : Vec 1 => (0 : ℝ) := by simp [ψ]
  refine dilationAtlasTwo_weightedIBP_via_pou (n := 0) G1 (fun _ => 0) χ χ
    contDiff_const hχ2 hχ2 hχc (R := R) ψ ?_ ?_ ?_ ?_ ?_
  · intro j
    fin_cases j
    · rw [hψ0]; exact hψ₀sm
    · rw [hψ1]; exact contDiff_const
  · intro j
    fin_cases j
    · rw [hψ0]; exact hψ₀supp
    · rw [hψ1]; simp [tsupport]
  · intro x hx
    rw [Fin.sum_univ_two, hψ0, hψ1]
    simp [hψ₀one x hx]
  · intro j
    fin_cases j
    · rw [hψ0]; exact hintL0
    · rw [hψ1, hpiece1]; simpa using (integrable_zero (Vec 1) μ2)
  · intro j
    fin_cases j
    · rw [hψ0]; exact hintR0
    · rw [hψ1, hpair1]; simpa using (integrable_zero (Vec 1) μ2)
```
Concerns: `fin_cases j` where j : Fin 2 — the goal after intro j is e.g. `ContDiff ℝ ∞ (ψ j)`. `fin_cases j` should substitute j := 0 and j := 1. Then `rw [hψ0]` works if the goal contains `ψ 0` literally. It should.

For hψ_sum: goal `∀ x ∈ tsupport χ, ∑ i, ψ i x = 1`. After `intro x hx; rw [Fin.sum_univ_two, hψ0, hψ1]` the goal becomes `ψ₀ x + 0 = 1`; `simp [hψ₀one x hx]` should close. But `rw [hψ0]` on `ψ 0 x` — hψ0 is `ψ 0 = ψ₀`, rewriting `ψ 0` inside the sum: the sum after Fin.sum_univ_two is `ψ 0 x + ψ 1 x`; `rw [hψ0]` rewrites `ψ 0` to `ψ₀` giving `ψ₀ x + ...`. ✓

For hintL: goal `Integrable (fun m => Δχ m * A2.pouPiece 0 χ (ψ 0) m) μ2` after `rw [hψ0]` → `... (ψ₀) ...` = hintL0. Wait, hintL0 is `Integrable (fun m => G1.driftLaplacian (fun _ => 0) χ m * A2.pouPiece 0 χ ψ₀ m) μ2` — matches ✓.

For hintL j=1: `rw [hψ1, hpiece1]` → integrand `Δχ m * (fun _ => 0) m` = `Δχ m * 0`; need `Integrable (fun m => Δχ m * 0) μ2` from `integrable_zero`; `simpa using ...` should handle `mul_zero`.

Hmm, careful: `rw [hpiece1]` where hpiece1 : A2.pouPiece 0 χ (fun _ => 0) = fun _ => 0 — after `rw [hψ1]`, the term is `A2.pouPiece 0 χ (fun _ => 0)`; rw should replace it. ✓

Also `hpiece1` proof used `simp [Poincare...pouPiece, Poincare...lift]` — the def is `lift b (fun y => ψ y * v (chart b y))`; with ψ = 0, this is `lift b (fun _ => 0)`; `simp [lift]` should unfold the `if` and close since both branches are 0. Good.

`hpair1` similarly with `pouPairing` — the definition is `lift i (fun z => gradInnerInverse (u∘chart i) (fun z' => ψ (transition b i z') * v (chart i z')) z)`; with ψ = 0 the inner function is `gradInnerInverse ... (fun _ => 0)` = 0 (since gradInnerInverse is linear in second arg — needs `simp` to know `gradInnerInverse u 0 = 0`). Hmm, `simp [pouPairing, lift]` would leave `gradInnerInverse (fun z' => χ ...) (fun z' => 0 * ...) z` which simplifies to `gradInnerInverse ... 0 z`; is there a simp lemma `gradInnerInverse_zero`? Maybe not. Let me check ChartMetric for `gradInnerInverse_zero`/`gradInnerInverse_eq_zero...`. Alternatively, define hpair1 via the atlas's `pouPairing` with `ψ = 0`; the inner is `fun z' => (0 : Vec 1 → ℝ) (A2.transition 0 1 z') * χ (A2.chart 1 z')` = `0 * χ ...` = 0; then `gradInnerInverse u 0 z`. Let me check if there's a simp lemma. Let me grep for `gradInnerInverse_zero` or `gradInnerInverse.*zero` in D12/D13.

Actually, in POUConstruction's proof of `halfSpaceAtlas_integrable_dirichlet` they used `A.gradInnerInverse_eq_zero_of_eventuallyEq_zero` (line 455). And POUAssemblyAE might have simp lemmas. Let me grep.

Alternatively, avoid needing hpair1: choose ψ₀ = 0? No.

Better: choose the second POU function also nonzero but with compact support? We need ψ0 + ψ1 = 1 on tsupport χ, and both compactly supported. Take ψ1 = χ' - something... Simplest: ψ0 = bump = 1 on tsupport χ (support in ball), ψ1 = 0 requires the zero pairing.

Alternative: use ψ0 = 1 - ψ1 with both compactly supported? Sum = 1 everywhere, and supports compact: ψ0 = χ_A (bump = 1 on a big ball, support in a bigger ball), ψ1 = χ_A - χ_B where... no, hard.

Alternative: Let me just check the gradInnerInverse simp lemmas.
dsh: reasoning:
`gradInnerInverse_eq_zero_of_eventuallyEq_zero` requires the function to be eventually zero near y — used in POUAssemblyAE. For hpair1, the function `fun z' => (fun _ => 0) (A2.transition 0 1 z') * χ (A2.chart 1 z')` is *identically* 0 (0 * anything = 0), so a simple `simp [pouPairing, lift]` might work if `gradInnerInverse u (fun _ => 0) y` simplifies. Let me check the definition of `gradInnerInverse`: `Σ j k, metricInnerInverse j k * partialDeriv j u * partialDeriv k v`. With v = 0, `partialDeriv k 0 = 0` — is there a simp lemma `ChartMetric.partialDeriv_zero`? Probably `simp` can handle `partialDeriv k (fun _ => 0)` by unfolding `partialDeriv` (fderiv of constant = 0) — `simp [ChartMetric.partialDeriv]` might work.

Let me just test hpair1's proof in a scratch:
```lean
example : A2.pouPairing 0 1 χ χ (fun _ : Vec 1 => (0:ℝ)) = fun _ => 0 := by
  funext m
  simp [Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.pouPairing,
        Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.lift, ChartMetric.gradInnerInverse,
        ChartMetric.partialDeriv]
```
Might need more unfolds. Alternatively, since I only need `Integrable` of the pairing, I could use the same transfer lemma with the *chart expression* being identically zero, proving `hint` directly:
```lean
    · -- hintR j=1
      refine A2.integrable_globalMeasure_withDensity_of_supported volume 0
        (f := fun _ => 0) (g := fun m => A2.pouPairing 0 1 χ χ (fun _ => 0) m)
        measurable_const ?_ ?_ ?_
      ...
      · -- chart expression is identically zero
        refine (integrable_zero _ _).congr ?_
        filter_upwards with y
        ...
```
Still needs to know the pairing evaluated at chart 0 y is zero. For i=1 ≠ chart 0... The lift is over chart 1, so evaluating at chart 0 y is not directly the `pouPairing_apply` form. Messy.

Alternative cleaner approach: choose ψ₁ = 0 but prove integrability of the pairing via its *measurability + support in chart 1* and the chart expression for chart 1:
`A2.pouPairing 0 1 χ χ 0 (A2.chart 1 y) = gradInnerInverse (χ ∘ chart 1) (fun z' => 0 (transition 0 1 z') * χ (chart 1 z')) y = gradInnerInverse ... (fun _ => 0) y = 0`. So the chart-1 expression is 0. Use `integrable_globalMeasure_withDensity_of_supported volume 1` with hsupp: support ⊆ chart 1 image (chart 1 is surjective since total, so trivial) and hint: the chart expression is integrable — it's `exp(-0) * g (chart 1 y) * density 1 y` = `pairing (chart 1 y) * density` = 0. So I need `A2.pouPairing 0 1 χ χ 0 (A2.chart 1 y) = 0` — via `A2.pouPairing_apply hT 0 1 χ χ 0 y` (which uses `IsTotal` and gives the chart-1 expression) then simplify the inner function to zero and use `gradInnerInverse_eq_zero_of_eventuallyEq_zero` or direct simp.

Actually, `pouPairing_apply hT b i u v ψ y : A.pouPairing b i u v ψ (A.chart i y) = (A.metric i).gradInnerInverse (fun z' => u (A.chart i z')) (fun z' => ψ (A.transition b i z') * v (A.chart i z')) y`. With ψ = 0: the second function is `fun z' => 0 * χ (chart 1 z')` = 0. So the RHS is `gradInnerInverse u 0 y`.

Now `ChartMetric.gradInnerInverse` — let me find its definition location to know the simp behavior.

Actually, simplest of all: avoid ψ₁ = 0 by choosing **ψ₀ = 0 and ψ₁ = bump**, with the *base chart b = 1*? No — H5's `pouPiece 0 V (ψ j)` and `pouPairing 0 (j:ℕ)` use base chart b = 0 hardcoded in the statement. So the nonzero piece must be ψ₀ (base chart 0). And ψ₁ must supply the rest of the partition on tsupport V. If ψ₀ = 1 on tsupport V, then ψ₁ = 0 on tsupport V; making ψ₁ = 0 globally is the simplest.

Alternatively take ψ₀ = ψ₁ = (1/2)·bump with bump = 1 on tsupport V: then ψ₀ + ψ₁ = 1 on tsupport V, both compactly supported in a ball, both smooth, both nonzero! Then hintL/hintR for j=1 are also nontrivial but structurally identical to j=0 (just with ψ₁ instead of ψ₀). The chart expressions are compactly supported continuous. That avoids the zero-pairing issue entirely and makes both pieces nonzero. hψ_sum: ψ₀ x + ψ₁ x = bump x = 1 on tsupport χ ✓.

That's better! Let me restructure H5:
- Take ψ₀ = ψ₁ = fun x => (1/2) * bump x where bump = 1 on tsupport χ and tsupport bump ⊆ ball 0 (R+1).
- hψ_sm: (1/2) * bump is C^∞.
- hψ_supp: tsupport (c • bump) ⊆ tsupport bump ⊆ ball ✓ (need `tsupport_const_mul_subset` or `support_const_mul_subset`; `tsupport` of `fun x => c * bump x` with c ≠ 0 equals tsupport bump. There's `tsupport_const_mul`? Let me check: `tsupport_const_mul` might exist in mathlib. Alternatively prove: if c*bump x ≠ 0 then bump x ≠ 0, so support ⊆ support bump, then closure monotone. Easy to prove inline.
- hψ_sum: on tsupport χ: (1/2)*1 + (1/2)*1 = 1 ✓.
- hintL for j: integrand = Δχ * pouPiece 0 χ (1/2 • bump) — need integrability for both j=0 and j=1 (same function actually, since both ψ are equal!). So I can prove one lemma `hintL_bump` for `fun m => Δχ m * A2.pouPiece 0 χ ((1/2) * bump) m` and use it for both j. Similarly hintR.

This is cleaner: only two integrability proofs, both for the same nonzero piece. And the chart expressions are compactly supported continuous.

Let me define:
```lean
  let ψb : Vec 1 → ℝ := fun x => (1/2) * χb x   -- where χb is the bump
```
Hmm, I'll pass χb and the half factor.

In the theorem `h5_dilationAtlasTwo_via_pou_instantiated`, take parameters χ (test function), χb (bump), with hχb_one : ∀ x ∈ tsupport χ, χb x = 1, hχbsm, hχbsupp, hχbc. Then ψ := fun _ j?? Actually ψ : Fin 2 → Vec 1 → ℝ := fun _ => fun x => (1/2) * χb x.

Then:
- hψ_sm: intro j; exact contDiff_const.mul hχbsm
- hψ_supp: intro j; support of (1/2)*χb ⊆ ball: 
  ```lean
  have : tsupport (fun x : Vec 1 => (1/2:ℝ) * χb x) ⊆ tsupport χb :=
    closure_mono (fun x hx => by
      rw [Function.mem_support] at hx ⊢
      exact fun h0 => hx (by rw [h0, mul_zero]))
  exact this.trans hχbsupp
  ```
  Wait direction: if (1/2)*χb x ≠ 0 then χb x ≠ 0. So support (c*χb) ⊆ support χb, and tsupport ⊆ tsupport. ✓
- hψ_sum: `intro x hx; rw [Fin.sum_univ_two]; simp [hχb_one x hx]` — goal `(1/2)*χb x + (1/2)*χb x = 1`; with χb x = 1 → `1/2 + 1/2 = 1` by norm_num. So `rw [hχb_one x hx]; norm_num`.
- hintL: ∀ j, Integrable (fun m => Δχ m * A2.pouPiece 0 χ (fun x => (1/2)*χb x) m) μ2 — same for both j, so prove once `hintLb` and use `fun j => hintLb`.
- hintR: similarly `hintRb`.

Proof of hintLb: transfer with chart 0:
- g_meas: measurable: driftLaplacian continuous × measurable_pouPiece (needs Measurable χ and ContDiff ∞ (c*χb)) ✓.
- hsupp: chart 0 '' source 0 = univ ✓.
- hint: Integrable (fun y => exp(-0) * (Δχ y * pouPiece...(chart 0 y)) * density) volume. Chart expression: Δχ y * ((1/2)*χb y * χ y) * G1.density y, continuous with compact support (⊆ tsupport χb) → integrable. Then `.congr` with the goal function. Need to rewrite `A2.pouPiece 0 χ (fun x => (1/2)*χb x) (A2.chart 0 y) = ((1/2)*χb y) * χ y` via `A2.pouPiece_apply hT 0 χ _ y` + `hchart0`.

- hintRb: chart expression `G1.gradInnerInverse χ (fun z => (1/2)*χb z * χ z) y * density`. Continuous with compact support ⊆ tsupport χb ✓.

This avoids the zero-pairing issue. 

Now, the packaged h5_nonvacuous: take bump χb from `exists_contDiff_bump (K := tsupport χ) (U := ball 0 (R₀+1))`, and pass χb, with hχb_one from hone, hχbsm, hχbsupp, hχbc from compactness.

Let me rewrite the whole H5 section.

Also the `hpair`/`hpiece` helper statements need updating.

Let me write the new H5 section text and replace lines 288-413.

Also, I realize I should double check `A2.pouPiece_apply hT` name — for the SmoothOverlapAtlas, `pouPiece_apply (h : A.IsTotal) (b) (v) (ψ) (y)`. ✓.

Let me now write the replacement. I'll use the write tool for the whole file? No — targeted replacement of the H5 block via Python (safer with exact strings). Let me use Python to replace from the h5 docstring to just before `/-! ### H6`.

Let me write the new block.

```lean
/-- **H5 (dilationAtlasTwo_weightedIBP_via_pou) non-vacuity**. The atlas is total; the partition
of unity is the two-chart family `ψ j = (1/2)·χb` with `χb = 1` on the support of the non-zero
test function `χ`, so both POU hypotheses and both integrability hypotheses are discharged with
the atlas's total-chart interface. -/
theorem h5_dilationAtlasTwo_via_pou_instantiated
    (χ χb : Vec 1 → ℝ) (hχ2 : ContDiff ℝ 2 χ) (hχc : HasCompactSupport χ)
    (hχbsm : ContDiff ℝ ∞ χb) (hχbc : HasCompactSupport χb) (R : ℝ)
    (hχbsupp : tsupport χb ⊆ ball (0 : Vec 1) (R + 1))
    (hχbone : ∀ x ∈ tsupport χ, χb x = 1) :
    ∫ m, G1.driftLaplacian (fun _ => 0) χ m * χ m ∂μ2
      = -∫ m, G1.gradInnerInverse χ χ m ∂μ2 := by
  have hT : A2.IsTotal := dilationAtlasTwoSmooth_isTotal 0 G1
  have hchart0 : A2.chart 0 = fun x : Vec 1 => (1 : ℝ) • x :=
    dilationAtlasTwo_chart_zero (n := 0) G1
  have hmetric0 : A2.metric 0 = G1 := dilationAtlasTwo_metric_zero (n := 0) G1
  have htrans00 : A2.transition 0 0 = fun y : Vec 1 => y := dilationTransition_zero_zero 2
  -- the partition-of-unity function used for both charts
  let ψb : Vec 1 → ℝ := fun x => (1 / 2 : ℝ) * χb x
  have hψbsm : ContDiff ℝ ∞ ψb := contDiff_const.mul hχbsm
  have hψbsupp : tsupport ψb ⊆ ball (0 : Vec 1) (R + 1) := by
    refine (closure_mono ?_).trans hχbsupp
    intro x hx
    rw [Function.mem_support] at hx ⊢
    exact fun h0 => hx (by simp [ψb, h0])
  have hψbc : HasCompactSupport ψb :=
    IsCompact.of_isClosed_subset hχbc (isClosed_tsupport ψb) hψbsupp
  have hψbone : ∀ x ∈ tsupport χ, ψb x = 1 := by
    intro x hx
    simp [ψb, hχbone x hx]
  -- chart-0 expressions of the lifted piece and pairing
  have hpiece : ∀ y : Vec 1, A2.pouPiece 0 χ ψb (A2.chart 0 y) = ψb y * χ y := by
    intro y
    rw [A2.pouPiece_apply hT 0 χ ψb y, hchart0]
    simp
  have hpair : ∀ y : Vec 1, A2.pouPairing 0 0 χ χ ψb (A2.chart 0 y)
      = G1.gradInnerInverse χ (fun z => ψb z * χ z) y := by
    intro y
    rw [A2.pouPairing_apply hT 0 0 χ χ ψb y, hmetric0, htrans00, hchart0]
    simp only [one_smul]
  have hintL : Integrable (fun m => G1.driftLaplacian (fun _ => 0) χ m * A2.pouPiece 0 χ ψb m) μ2 := by
    refine A2.integrable_globalMeasure_withDensity_of_supported volume 0
      (f := fun _ => 0) (g := fun m => G1.driftLaplacian (fun _ => 0) χ m * A2.pouPiece 0 χ ψb m)
      measurable_const ?_ ?_ ?_
    · exact (G1.driftLaplacian_continuous _ _ contDiff_const hχ2).measurable.mul
        (A2.measurable_pouPiece 0 hχ2.continuous.measurable hψbsm)
    · intro m _
      exact ⟨m, mem_univ m, by rw [hchart0]; simp⟩
    · refine (Continuous.integrable_of_hasCompactSupport ?_ ?_).congr ?_
      · exact (((G1.driftLaplacian_continuous _ _ contDiff_const hχ2).mul
          (hψbsm.continuous.mul hχ2.continuous)).mul G1.density_contDiff_one.continuous)
      · refine IsCompact.of_isClosed_subset hψbc (isClosed_tsupport _)
          (closure_minimal ?_ (isClosed_tsupport _))
        intro y hy
        rw [Function.mem_support] at hy
        by_contra hyt
        have h0 : ψb y = 0 := Function.notMem_support.mp fun hm => hyt (subset_tsupport _ hm)
        exact hy (by simp [h0])
      · filter_upwards with y
        rw [hpiece y, hchart0, hmetric0]
        simp [A2.density]
  have hintR : Integrable (fun m => A2.pouPairing 0 0 χ χ ψb m) μ2 := by
    refine A2.integrable_globalMeasure_withDensity_of_supported volume 0
      (f := fun _ => 0) (g := fun m => A2.pouPairing 0 0 χ χ ψb m)
      measurable_const ?_ ?_ ?_
    · refine A2.measurable_pouPairing 0 0 ?_ ?_ hψbsm
      · rw [hchart0]; simpa [Function.comp_def] using hχ2.comp (contDiff_const_smul (1 : ℝ))
      · rw [hchart0]; simpa [Function.comp_def] using hχ2.comp (contDiff_const_smul (1 : ℝ))
    · intro m _
      exact ⟨m, mem_univ m, by rw [hchart0]; simp⟩
    · refine (Continuous.integrable_of_hasCompactSupport ?_ ?_).congr ?_
      · refine (G1.gradInnerInverse_continuous χ (fun z => ψb z * χ z) hχ2
          (hψbsm.of_le two_le_infty_audit |>.mul hχ2)).mul G1.density_contDiff_one.continuous
      · refine IsCompact.of_isClosed_subset hψbc (isClosed_tsupport _)
          (closure_minimal ?_ (isClosed_tsupport _))
        intro y hy
        rw [Function.mem_support] at hy
        by_contra hyt
        have h0 : ψb y = 0 := Function.notMem_support.mp fun hm => hyt (subset_tsupport _ hm)
        exact hy (by simp [h0])
      · filter_upwards with y
        rw [hpair y, hchart0]
        simp [A2.density]
  refine dilationAtlasTwo_weightedIBP_via_pou (n := 0) G1 (fun _ => 0) χ χ
    contDiff_const hχ2 hχ2 hχc (R := R) (fun _ => ψb) ?_ ?_ ?_ ?_ ?_
  · intro j; exact hψbsm
  · intro j; exact hψbsupp
  · intro x hx
    rw [Fin.sum_univ_two]
    rw [hψbone x hx, hψbone x hx]
    norm_num
  · intro j; exact hintL
  · intro j; exact hintR
```

Concerns:
- `hψbc` via `IsCompact.of_isClosed_subset hχbc (isClosed_tsupport ψb) hψbsupp` — the subset argument should be `tsupport ψb ⊆ tsupport χb` not `⊆ ball`. Since hχbc : HasCompactSupport χb means `IsCompact (tsupport χb)`. I need `tsupport ψb ⊆ tsupport χb`. Let me define that instead:
  ```lean
  have hψb_tsupp : tsupport ψb ⊆ tsupport χb := by
    refine closure_mono ?_
    intro x hx
    rw [Function.mem_support] at hx ⊢
    exact fun h0 => hx (by simp [ψb, h0])
  have hψbsupp : tsupport ψb ⊆ ball (0 : Vec 1) (R + 1) := hψb_tsupp.trans hχbsupp
  have hψbc : HasCompactSupport ψb :=
    IsCompact.of_isClosed_subset hχbc (isClosed_tsupport ψb) hψb_tsupp
  ```
- `hψbone`: `simp [ψb, hχbone x hx]` gives `(1/2)*1 = 1`? That's false! ψb = (1/2)*χb, so with χb x = 1, ψb x = 1/2, not 1. For the sum to be 1 we need ψ₀ + ψ₁ = (1/2)*χb + (1/2)*χb = χb = 1 on tsupport χ ✓. So `hψbone` should be `ψb x = 1/2`. Let me name it `hψbhalf : ∀ x ∈ tsupport χ, ψb x = 1/2` and then the sum proof: `rw [hψbhalf x hx, hψbhalf x hx]; norm_num`.

- In the sum goal with `fun _ => ψb` as ψ: `∑ i : Fin 2, ψb x` = `ψb x + ψb x`; `rw [Fin.sum_univ_two]` then the goal is `ψb x + ψb x = 1`.

- `hintL`/`hintR` are for j arbitrary: the goal is `∀ j, Integrable (fun m => ... (ψ j) m) ...` where ψ = `fun _ => ψb`. `intro j; exact hintL` — the integrand has `(fun _ => ψb) j` which is defeq to ψb; `exact` should accept. To be safe: `intro j; simpa using hintL`.

- `A2.measurable_pouPairing 0 0 ?_ ?_ hψbsm` — I provide the two C² proofs inline. But `measurable_pouPairing` signature: `(b i) {u v} {ψ} (huc : ContDiff ℝ 2 fun y => u (A.chart i y)) (hvc) (hψ)`. With u = v = χ, i = 0. Fine.
  Hmm, but there's a subtlety: `rw [hchart0]; simpa [Function.comp_def] using hχ2.comp (contDiff_const_smul (1:ℝ))` — goal `ContDiff ℝ 2 (fun y => χ (A2.chart 0 y))`. After `rw [hchart0]` it's `ContDiff ℝ 2 (fun y => χ ((1:ℝ) • y))`. `hχ2.comp (contDiff_const_smul (1:ℝ)) : ContDiff ℝ 2 (χ ∘ fun y => (1:ℝ)•y)`. `simpa [Function.comp_def]` should close. ✓

- `μ2` in hintL goal: `μ2 := (A2.globalMeasure volume).withDensity (A2.weight (fun _ => 0))` and the transfer lemma's conclusion is `Integrable g ((A.globalMeasure volume).withDensity (A.weight f))` with f := fun _ => 0, A := A2 → defeq to μ2 ✓.

Let me now write this block. I'll use Python to replace the region between the H5 docstring start and the H6 marker.

Also fix:
- linarith in exists_bump_in_halfSpace → calc.
- `support_gradInnerInverse_subset` → `Poincare.D13.ManifoldIBP.support_gradInnerInverse_subset`.
- the private lemma rename.

Let me apply.
dsh: reasoning:
While compiling, let me draft the semantic verdicts and the ledger structure. Let me also prepare the adversarial analysis content.

Let me think carefully about each headline's adversarial assessment, since this is the core deliverable.

### H1: SmoothOverlapAtlas.globalWeightedIBP_of_cover_partial_ae
Classification: proved theorem (conditional on standard geometric hypotheses on the atlas + operator identifications). General (any SmoothOverlapAtlas), not model-specific.

Hypotheses (13 groups):
1. htrans: ∀ i j y, chart j y ∈ chart i '' source i → transition i j y ∈ source i. Coherence of the atlas (transition lands in source). Domain: all i,j : ℕ, y : Vec (n+1). NOTE: OverlapAtlas already has `transition_mem : ∀ i j y, y ∈ overlapOf chart source i j → transition i j y ∈ source i` where `overlapOf i j = source j ∩ chart j ⁻¹' (chart i '' source i)`. htrans is essentially the same without requiring y ∈ source j! I.e. htrans is *stronger* than the structure field: it drops the `y ∈ source j` conjunct. Is htrans stronger than `transition_mem`? `transition_mem` gives the conclusion only for y ∈ source j ∩ chart j⁻¹(image i). htrans gives it for all y with chart j y ∈ image i (no source-j requirement). For y ∉ source j, the structure's `transition` is unconstrained by transition_mem, but `transition_chart_global` is also only constrained on the overlap... wait no, SmoothOverlapAtlas has `transition_chart_global : ∀ i j y, chart i (transition i j y) = chart j y` for ALL y. And `inj_chart`, `transition_chart_global` together with `chart j y ∈ chart i '' source i` gives: there's z ∈ source i with chart i z = chart j y. Then chart i (transition i j y) = chart j y = chart i z, so by injectivity transition i j y = z ∈ source i. So htrans is *derivable* from SmoothOverlapAtlas's structure fields (`inj_chart` + `transition_chart_global`)! Interesting: htrans is a redundant hypothesis (in the sense that it's implied by the structure). Wait, but the atlas structure has `transition_mem` only on the overlap; yet with `transition_chart_global` (global) and `inj_chart` (global injectivity), we get htrans for free:
   Given chart j y ∈ chart i '' source i, ∃ z ∈ source i, chart i z = chart j y. Since chart i (transition i j y) = chart j y = chart i z, injectivity gives transition i j y = z ∈ source i. ✓
   So `htrans` is a theorem of the structure, not an extra assumption! That's a notable semantic finding: the "coherence" hypothesis is redundant for SmoothOverlapAtlas. It's not a defect (redundant hypotheses are harmless), but it means the card's claim that htrans is one of the "structural atlas hypotheses" is accurate, though it could have been discharged. Actually wait — is it used in the proof? Probably yes, but it could be replaced by a lemma. Let me verify this derivation in Lean in my audit probe! That's a nice adversarial finding: htrans is derivable (not an assumption) — so the hypothesis list is *weaker* than stated, and the theorem is stronger than its statement suggests. Actually careful: this derivation needs `chart j y ∈ chart i '' source i` — exactly the hypothesis. And `transition_chart_global i j y : chart i (transition i j y) = chart j y`. Yes. So:
   ```lean
   theorem htrans_of_structure (A : SmoothOverlapAtlas M (n+1)) :
       ∀ i j y, A.chart j y ∈ A.chart i '' A.source i → A.transition i j y ∈ A.source i := by
     rintro i j y ⟨z, hz, hzy⟩
     have h : A.chart i (A.transition i j y) = A.chart i z := by
       rw [A.transition_chart_global, hzy]
     exact h ▸ ? -- need: transition i j y = z, then rewrite
   ```
   Use `A.inj_chart i h : A.transition i j y = z` then `rw [this]; exact hz`.
   
   I should add this to the audit probe as evidence that htrans is not an independent assumption. Good finding!

   Hmm wait, but is `A.transition_chart_global` really global (∀ y)? Yes: `transition_chart_global : ∀ i j y, chart i (transition i j y) = chart j y`. And `inj_chart : ∀ i, Function.Injective (chart i)`. So yes, htrans is derivable. Excellent — this is a genuine (positive) semantic finding: the headline's htrans hypothesis is redundant; the theorem is at least as strong as stated. Not a defect.

2. b : ℕ (base chart index).
3. ι finite type, chartOf : ι → ℕ (finite chart cover).
4. f u v Du Guv : M → ℝ.
5. hf, hv, hDu, hGuv measurable.
6. hvsupp: ∀ m, v m ≠ 0 → m ∈ chart b '' source b.
7. hfc/huc/hvc: ∀ i, ContDiff ℝ 2 (chart expressions) — over all i : ℕ.
8. hvcc: HasCompactSupport (v ∘ chart b).
9. htsupp_b: tsupport (v ∘ chart b) ⊆ source b.
10. hcover: chart b '' source b ⊆ ⋃ j, chart (chartOf j) '' source (chartOf j).
11. hD: ∀ i y, y ∈ source i → Du (chart i y) = (metric i).driftLaplacian ... y.
12. hG: ∀ i y, y ∈ source i → Guv (chart i y) = (metric i).gradInnerInverse ... y.
13. hGuvsupp: ∀ m, Guv m ≠ 0 → m ∈ chart b '' source b.
14. hproper: ∀ i j K, IsCompact K → IsCompact (transition i j ⁻¹' K).
15. hbd: ∀ i, volume (frontier (source i)) = 0.

Conclusion: ∫ Du·v d((globalMeasure volume).withDensity (weight f)) = -∫ Guv d(...).

Adversarial checks:
- conclusion-equivalent hyp? No. hD/hG are integrand identifications. The equality itself is derived from the chart-level IBP (D12 `chart_weighted_ibp`), applied chart-by-chart with a POU; the chart theorem is not an assumption.
- contradictory? No — instantiated (H1 probe + halfSpaceAtlas_weightedIBP_of_cover).
- vacuous? No — probe gives nonzero χ with χ 0 = 1 and a genuine Dirichlet identity.
- wrong domains? Support conditions are topological (tsupport ⊆ open source), avoiding boundary issues; the null-boundary hbd handles the a.e. chart transfer. No (0,T) domain issue.
- model vs manifold? General theorem over an abstract SmoothOverlapAtlas (not a mathlib manifold; the "manifold" is an abstract measurable space with an atlas structure). Honest.
- hidden interface fields? SmoothOverlapAtlas (extends OverlapAtlas): chart, source, isOpen_source, measurable_chart, injOn_chart, cover, measurableSet_image, metric, transition, transition_mem, transition_chart, transition_diff, metric_transform; plus isOpen_overlap, inj_chart, measurable_readback, contDiff_transition, transition_chart_global. Also ChartMetric fields (g, smooth, posDef).
  Redundancy found: htrans is derivable from inj_chart + transition_chart_global.
  Extra observation: `hvsupp` is implied by `htsupp_b` only if... no. But note `hvsupp` + htsupp_b together imply v is supported in the base chart image in both senses.
  Another observation: hD/hG are required for ALL i (including charts with empty source — vacuous there), but the proof only needs them for i ∈ range chartOf ∪ {b}. Strengthening (harmless).

### H2: halfSpaceAtlas_weightedIBP_unconditional
Classification: proved theorem (model). Unconditional relative to G : ChartMetric (n+1) (inhabited by euclideanChartMetric) and C²/compact-support data.
Hypotheses: G; f,u,v C²; v compactly supported; tsupport v ⊆ {y0<1}.
Adversarial:
- conclusion-equivalent? No.
- contradictory/vacuous? No; probe with nonzero χ gives ∫Δχ·χ = -∫|∇χ|².
- domain: the base chart source of the partial atlas; the support condition is *strict* (tsupport in the open half-space), needed for the bump. Note: v is required C² on ALL of Vec (n+1) — standard.
- model-vs-manifold: model atlas (identity + dilation by 2 on two half-spaces), not a general manifold. Card labels it model.
- hidden interface: none beyond ChartMetric. The atlas's Riemannian measure is the glued one (chart-independence proven), but note the measure used in the conclusion is `(hsAtlas G).globalMeasure volume`; the chart-dependence is discharged.
Potential subtlety worth flagging: the theorem is stated for `hsAtlas G` where G is an arbitrary ChartMetric; the atlas's metric on chart i≥1 is `dilateMetric G 2`. So the "model" is a family of atlases, one per chart metric G. Not a defect.
Another subtlety: The conclusion integrates Δ_F u w.r.t. the *weighted* measure e^{-f}dμ; f is only C² (not necessarily bounded below); the integrability is constructed from compact support of v. Fine.

### H3: halfSpaceAtlas_greenIdentity
Classification: proved theorem (consumed consequence of H2 applied twice).
Hypotheses: G; F,U,V C²; U,V compactly supported with tsupport in {y0<1}.
Adversarial:
- conclusion-equivalent? No. Derivation: H2 twice + symmetry of gradInnerInverse.
- vacuous? No; probe with two distinct bumps.
- domain: same.
- Note: the statement does NOT include integrability hypotheses; the docstring claims non-vacuity via `halfSpaceAtlas_integrable_dirichlet`, which is a separate theorem (for the Dirichlet case with U=V). For the Green identity with U ≠ V, integrability of the two sides follows from the Dirichlet integrability applied to U and to V separately? ∫ΔU·V integrable: need |∫ΔU·V| — the Dirichlet theorem gives Integrable (ΔU·U) and Integrable |∇U|², not Integrable (ΔU·V) for V ≠ U. However, since both U,V are compactly supported C², the integrands ΔU·V and U·ΔV are continuous with compact support in the chart, hence integrable — this can be proven by the same transfer argument. So the identity is non-vacuous, but the specific companion theorem cited (halfSpaceAtlas_integrable_dirichlet) only covers the U=V case. Minor documentation gap; I can verify in the audit whether integrability for distinct U,V is provable (it is, by the same method; my probe's `h2_dirichlet_two_bumps` uses U=χ₁, V=χ₂ and the identity holds).

Actually, I could add to my probe a kernel-checked integrability statement for the Green case with two distinct bumps, using the same transfer lemma. That would strengthen the non-vacuity claim for H3. Let me consider adding it if time permits. Actually the H3 instance's integrability of Δχ₁·χ₂ follows from compact support; I can prove:
```lean
theorem h3_integrable_both (χ₁ χ₂) ... :
    Integrable (fun m => G1.driftLaplacian 0 χ₁ m * χ₂ m) μ1 ∧ Integrable (fun m => χ₁ m * G1.driftLaplacian 0 χ₂ m) μ1
```
via the transfer lemma with chart 0. That's ~30 lines. Worth it for H3's non-vacuity claim. Maybe if time permits (we're ~2h in).

### H4: halfSpaceAtlas_laplacianIntegralZero
Classification: proved theorem (model).
Hypotheses: G; V C²; compact support; tsupport V ⊆ {y0<1}.
Adversarial:
- conclusion-equivalent? No. Proof: transfer to chart 0 (support argument, `support_laplacian_subset`), then D12 chart theorem `laplacian_integral_eq_zero`.
- vacuous? No: probe with nonzero bump; the integral is 0, which is a genuine conclusion (not trivially 0=0; it asserts cancellation).
- Hmm: is `∫ G.laplacian V dμ = 0` non-vacuous? Since ∫ of a compactly supported continuous function over a measure that's Lebesgue-like: the statement is contentful. But can we provide a non-trivially-zero integrand? `support_laplacian_subset` shows the laplacian is supported where V is; with V = χ a bump, Δχ ≠ 0 somewhere. So contentful.
- Potential caveat: the theorem uses the UNWEIGHTED globalMeasure (no entropy weight). Consistent with "divergence theorem".
- The claim in the card that this closes "U7-DIVERGENCE / partial-atlas model": the chart-level theorem was already in D12; the new content is the transfer to the partial atlas measure. Accurate.

### H5: dilationAtlasTwo_weightedIBP_via_pou
Classification: conditional interface (POU data ψ + integrability of pieces supplied as hypotheses). The card lists it under "U7-GLOBAL-POU (invocation 6) ... consumed by dilationAtlasTwo_weightedIBP_via_pou" — it's a consumed application of `globalWeightedIBP_of_pou` at the dilation model, NOT an unconditional theorem. The card's semantic_class puts `globalWeightedIBP_of_pou*` with supplied POU data under conditional_interface. It does NOT list `dilationAtlasTwo_weightedIBP_via_pou` explicitly in expanded_hypotheses nor semantic_class; the statement's own docstring is honest ("The remaining inputs are the POU data and integrability").
Adversarial:
- conclusion-equivalent? The integrability hypotheses hintL/hintR are exactly the integrability needed to make the two sides' integrals meaningful; they are not the equality.
- vacuous? No: probe instantiates with a genuine POU ψ and integrability constructed.
- hidden interface: none beyond the hypotheses; uses `dilationAtlasTwoSmooth` (total atlas).
- Domain: POU sum required only on tsupport V; ψ supported in ball 0 (R+1) — compact.
- Potential defect candidate: the headline's name could suggest it's the dilation analogue of H2 (unconditional). It is not: it retains POU+integrability hypotheses. The card's headline table (lines 30-33) doesn't claim otherwise; §5 row says it's "consumed by dilationAtlasTwo_weightedIBP_via_pou" which is accurate. The card's §3 (expanded hypotheses) does not mention this theorem — a documentation gap: one of the eight "headline" declarations is not covered by the "expanded hypotheses" section. Worth flagging: the card's `expanded_hypotheses` list omits H5's explicit POU+integrability hypotheses (they're implied by item 6's reference to the pre-existing `globalWeightedIBP_of_pou` family). This is a *documentation completeness* gap, not a mathematical overclaim, since H5 is not claimed unconditional. Let me double-check the card's JSON expanded_hypotheses: it has entries for H1, H2, halfSpaceAtlas_of_cover*, green/dirichlet, integrable_globalMeasure, "Pre-existing interfaces unchanged" (mentions globalWeightedIBP_of_pou_partial_ae and ManifoldAtlasData), and "No hypothesis equivalent to a conclusion". H5 is not explicitly listed. Confirmed omission.

### H6: manifoldWeightedIBP_of_atlasData
Classification: conditional interface (transfer).
Hidden interface fields (assumed, not proved):
- ManifoldAtlasData.μ (the measure),
- chart, metric, drift,
- integral_decomp (B-D13-MANIFOLD-GLUING): ∀ g integrable, ∫ g dμ = Σ_i ∫ g(chart i x) e^{-drift i x} ρ_i(x) dx,
- driftLaplacianM, gradInnerM,
- weighted_laplacian_compat (B-D13-OPERATOR-COMPAT): ∀ u i x, Δ^M u (chart i x) = chart Δ_{drift i} (u ∘ chart i) x,
- grad_inner_compat: ∀ u v i x, gradInnerM u v (chart i x) = chart gradInnerInverse (u∘chart i) (v∘chart i) x.
Plus explicit hypotheses: hd (chart drifts C²), hu/hv (chartwise C²), hvc (chartwise compact support), hg/hg' (integrability).
Adversarial:
- conclusion-equivalent? No, but the theorem is essentially a rewriting: after the interface fields, the conclusion is exactly `ChartSumData.globalWeightedIBP`, a proved chartwise theorem. The mathematical content is the interface fields, which are *assumed*. So the honest classification is conditional transfer; the card says so.
- vacuous? No: inhabited by `dilationAtlasTwoData` (ι=1) and `disjointAtlasData`; probe instantiates the disjoint model with nonzero bump and discharges integrability via `disjointAtlas_weightedIBP`.
- domain: hvc requires compact support of v∘chart i in EVERY chart; for a disjoint atlas that's fine; for a general manifold atlas with overlapping charts, the same manifold function v must have compactly supported chart expressions — stronger than compact support on M. Also hg/hg' are integrability assumptions, not proved.
- Potential defect: `integral_decomp` is a strong gluing assumption; for a genuinely overlapping atlas it's not automatic (needs a partition of unity). The D13 card flags this as "conditional interface" and lists B-D13-MANIFOLD-GLUING as closed? Let me check: the card's exact_blockers_closed mentions "U7-GLOBAL-MEASURE / VOLUME-FORM ... globalMeasure_apply_chart ... consumed by dilationAtlasTwoData_weightedIBP through manifoldWeightedIBP_of_atlasData". And the "remaining_blockers" (§6, JSON) — let me check the JSON remaining_blockers to see if B-D13-MANIFOLD-GLUING is listed as remaining. Earlier I saw exact_blockers_closed includes U7-GLOBAL-MEASURE; and the Blocked.lean file defines `B-D13-MANIFOLD-GLUING` — let me read Blocked.lean to see if it's a *remaining* blocker or closed. The card's §5 row says "U7-GLOBAL-MEASURE / VOLUME-FORM ... closed content ... consumed by dilationAtlasTwoData_weightedIBP". But `Blocked.lean` has a theorem `∀ n ι A, ...` (line 47) — likely stating that the general integral_decomp for an arbitrary overlapping atlas is NOT constructed (a named blocker). Let me read Blocked.lean.

Let me also check the card JSON's remaining_blockers.

### H7: finiteLifetimeEntropyBridge_gaussian
Classification: proved theorem (model: Euclidean Vec 2, explicit backward Gaussian, clamped lifetime).
Hypotheses: τ0 t1 : ℝ, ht1 : t1 < τ0. (Note: no positivity of τ0/t1; if t1 ≤ 0 the interval (0,t1) is empty and the statement is trivially satisfied. Non-vacuous for 0 < t1 < τ0.)
Conclusion: FiniteLifetimeEntropyBridge (gaussCalculus ...) (gaussEntropyData ...) 0 t1 — a Prop structure with 6 fields.
Adversarial:
- conclusion-equivalent? No; the fields are proved from explicit lemmas (F formula, FDissipation formula, restricted IBP, Laplacian compatibility, Bochner, conjugate heat, C¹ regularity).
- vacuous? No: probe at τ0=1, t1=1/2 with F(1/4)=4/3 and the interior derivative field instantiated.
- domain checks: fields on Ioo 0 t1 (derivative-type) and Icc 0 t1 (regularity) — correct; clamping keeps τ > 0 for all real t while the fields see the genuine backward time on the lifetime.
- model vs manifold: explicitly Euclidean chart Vec 2; NOT a manifold statement. Card lists Gaussian family under model. The card's remaining blocker §6.7 states there is no manifold WeightedCalculus instance. Honest.
- hidden fields: FiniteLifetimeEntropyBridge is a Prop structure whose fields are all assumed in `monotoneOn_of_bridge`; H7 discharges them for the Gaussian. EntropyData (from D3 upstream) carries fields R, gradSq, f, ρ, τ, τ_pos, n, riccHess, ρ_nonneg, integrable_F, integrable_W.

### H8: monotoneOn_F_gaussian
Classification: proved theorem (model; consumed conclusion via D4 certificate reduction).
Hypotheses: τ0 t1, ht1 : t1 < τ0.
Conclusion: MonotoneOn (fun s => (gaussEntropyData τ0 t1 ht1 s).F) (Icc 0 t1).
Adversarial:
- conclusion-equivalent? No: derived from the bridge + a *discharged* upper bound 1/(τ0-t1) (`monotoneOn_of_bridge` needs hbound; it's proven, not assumed).
- vacuous? No: F(0)=1, F(1/2)=2 at τ0=1, t1=1/2; monotonicity is a genuine ordering. If t1 ≤ 0 the interval is empty/trivial; the substantive case requires 0 < t1 < τ0.
- domain: Icc 0 t1 (closed), correct; the bridge supplies the interior derivative and closed-interval C¹.
- model vs manifold: Euclidean Vec 2 model; no Perelman monotonicity claim. The card is explicit.
- One more check: `gaussEntropyData` is defined with clamping for t > t1; but the MonotoneOn is on Icc 0 t1, so the clamped region is not used except at t = t1 exactly (τ(t1) = τ0 - t1 either way). Fine.

Now let me read Blocked.lean and the card's remaining_blockers to complete the H6 assessment.
dsh: reasoning:
Excellent finding: `Blocked.lean` explicitly records B-D13-MANIFOLD-GLUING as an interface field, not a theorem, and says none of these Props is used as a hypothesis. So H6's `integral_decomp` is honestly disclosed. Note the card's exact_blockers_closed row "U7-GLOBAL-MEASURE / VOLUME-FORM (invocations 3–5): globalMeasure, chartMeasure_apply_eq, globalMeasure_apply_chart, globalMeasure_restrict_eq_chartMeasure, globalMeasure_apply_chart_volumeForm, dilationAtlasTwoData | consumed by dilationAtlasTwoData_weightedIBP through manifoldWeightedIBP_of_atlasData" — this is about the *model* instantiations (dilation/disjoint), not the general atlas. And B-D13-MANIFOLD-GLUING remains in remaining_blockers presumably. Let me check the card JSON remaining_blockers.

Now let me check compile4.
dsh: reasoning:
Wait — the card's remaining_blockers list does NOT include B-D13-MANIFOLD-GLUING or B-D13-SMOOTH-POU! It includes B-D13-RIEMANNIAN-METRIC-BRIDGE-PACKAGING, D12 ChartMetric.smooth, U7-GLOBAL-BOUNDARY, B-D13-CLOSED-MANIFOLD, B-D13-STOKES, B-D13-ORIENTED-ATLAS, Manifold-level F-functional. B-D13-MANIFOLD-GLUING is mentioned in `Blocked.lean` as a blocker but not in the card's remaining_blockers list. Hmm, but the card's §6 item 1 is about the Riemannian metric bridge packaging; item 7 mentions the manifold-level F-functional. Where does B-D13-MANIFOLD-GLUING appear in the card? Let me grep the card for MANIFOLD-GLUING.

Actually the exact_blockers_closed row "U7-GLOBAL-MEASURE / VOLUME-FORM (invocations 3–5)" claims the *measure gluing* was closed via `globalMeasure`, `chartMeasure_apply_eq`, `globalMeasure_apply_chart`, etc. That's for the `OverlapAtlas.globalMeasure` construction (which IS constructed and proven chart-independent). But `ManifoldAtlasData.integral_decomp` (the abstract interface for an arbitrary atlas) is separate and remains an interface field per Blocked.lean. The card's semantic_class explicitly says "Conditional interface (explicit hypotheses): ... ManifoldAtlasData and ChartSumData". So the card does disclose it as conditional. The remaining_blockers list doesn't name B-D13-MANIFOLD-GLUING, but §6 doesn't either. It's a minor documentation omission: `Blocked.lean` names it but the card's remaining_blockers list doesn't include it. Let me grep the card for "GLUING".

Now compile4 errors:
1. Line 336: "don't know how to synthesize implicit argument `f`" — in `A2.measurable_pouPairing 0 0 ?_ ?_ hψbsm`? The signature is `measurable_pouPairing (b i : ℕ) {u v : M → ℝ} {ψ} (huc) (hvc) (hψ)`. Since u, v are implicit, Lean must infer them from the expected type `Measurable (A2.pouPairing 0 0 ?u ?v ψb)`. The goal is `Measurable (fun m => A2.pouPairing 0 0 χ χ ψb m)` so u,v should be inferrable... but the error says at line 336:13. Let me look at the code around 330-340.

2. Line 376: rewrite failed for `ψb`-related sum maybe.
3. Line 379: type mismatch after simplification.

Let me look at the log details.
dsh: reasoning:
Errors:
1. Line 336 & 336:14: the `refine (Continuous.integrable_of_hasCompactSupport ?_ ?_).congr ?_` — Lean can't infer the implicit function `f` for `Integrable.congr`/`Continuous.integrable_of_hasCompactSupport`. I need to provide the function explicitly. In the previous version this worked (compile3 had no error at that line, interesting: compile3 error at 347 was different). In the new code, the `.congr` target isn't determining `f`. Let me restructure:

```lean
    · have hint : Integrable (fun y : Vec 1 => Real.exp (-((fun _ => 0) (A2.chart 0 y))) * g (A2.chart 0 y) * A2.density 0 y) volume := by
        refine (Continuous.integrable_of_hasCompactSupport (f := fun y => ...) ?_ ?_).congr ?_
```
Let me name the function explicitly:
```lean
      · -- hint
        let Φ : Vec 1 → ℝ := fun y => G1.driftLaplacian (fun _ => 0) χ y * (ψb y * χ y) * G1.density y
        have hΦcont : Continuous Φ := ...
        have hΦcc : HasCompactSupport Φ := ...
        have hΦint : Integrable Φ volume := hΦcont.integrable_of_hasCompactSupport hΦcc
        refine hΦint.congr ?_
        filter_upwards with y
        ...
```
This avoids inference issues since `hΦint` is a fixed term. Then the `.congr` is `Integrable.congr hΦint (Filter.EventuallyEq ...)`; the expected type is `Integrable (fun y => ...) volume`, so `refine hΦint.congr ?_` should work: `Integrable.congr : Integrable f μ → f =ᵐ[μ] g → Integrable g μ`. Here the goal is `Integrable G volume` and hΦint : Integrable Φ volume; `.congr` will instantiate f := Φ, g := G from the goal. Should be fine.

Actually the error arises because I wrote `refine (Continuous.integrable_of_hasCompactSupport ?_ ?_).congr ?_` — the anonymous term's implicit f isn't determined by the expected type before elaboration of `.congr`. Using a named `have` fixes it.

2. Line 376: `rw [Fin.sum_univ_two, hψbhalf x hx, hψbhalf x hx]` — after `Fin.sum_univ_two` the goal is `ψb x + ψb x = 1` but the `rw` with `hψbhalf x hx` — it says pattern `ψb x` not found in `1/2 + 1/2 = 1`, meaning the first rw already rewrote both occurrences? Actually the error shows the target after the first two rewrites was `1/2 + 1/2 = 1` and the *second* `hψbhalf x hx` failed because `ψb x` no longer occurs. So I should write `rw [Fin.sum_univ_two, hψbhalf x hx]` then `norm_num`. Or `simp [Fin.sum_univ_two, hψbhalf x hx]`.

3. Line 379: `simpa using hintR` — goal expects `A2.pouPairing 0 (↑j) χ χ ψb` where j is not yet fin_cases'd in that branch? I wrote `· intro j; simpa using hintR` — the goal has `(fun _ => ψb) j` and `↑j`; hintR has `0`. Since j is arbitrary, `simpa` can't close. I need `intro j; fin_cases j <;> simpa using hintR` (for both j=0 and j=1 the pairing is the same? hintR is for i=0; for j=1 the goal has `pouPairing 0 1 χ χ ψb`. Hmm! In `dilationAtlasTwo_weightedIBP_via_pou`, hintR is `∀ j : Fin 2, Integrable (fun m => pouPairing 0 (↑j) U V (ψ j) m) ...`. For j=1 the chart index is 1, so my `hintR` (proved for index 0) doesn't match. I need integrability for both j=0 and j=1 pairings, i.e. `pouPairing 0 0 ...` and `pouPairing 0 1 ...`.

So I need to generalize `hintR` to `∀ i : ℕ, Integrable (fun m => A2.pouPairing 0 i χ χ ψb m) μ2` (or at least i ∈ {0,1}). The transfer argument works for any i via chart i (the atlas is total: chart i is surjective and source = univ, so hsupp is trivial; the chart expression is `(metric i).gradInnerInverse (χ∘chart i) (ψb ∘ transition 0 i * χ ∘ chart i)` continuous with compact support — support in chart i image is automatic by totality, and the chart expression's compact support comes from ψb).

Simplest: prove `hintR_all : ∀ i : ℕ, Integrable (fun m => A2.pouPairing 0 i χ χ ψb m) μ2` for i ∈ {0,1} only? The `∀ j : Fin 2` needs `pouPairing 0 (↑j)` with `↑j ∈ {0,1}`. I can prove `∀ i, Integrable ...` uniformly using the transfer lemma with chart i:
- hsupp : ∀ m, g m ≠ 0 → m ∈ A2.chart i '' A2.source i. Since A2 is total, chart i is surjective and source = univ: `hT.surj_chart i` gives `∀ m, ∃ x, A2.chart i x = m`; with source = univ (`hT.source_univ i`), m ∈ image. So `⟨x, by rw [hT.source_univ i]; exact mem_univ x, hx⟩`.
- hint: the chart expression `exp(-0) * pouPairing (chart i y) * density i y` = `(metric i).gradInnerInverse (χ∘chart i) (fun z' => ψb (transition 0 i z') * χ (chart i z')) y * density i y` via `pouPairing_apply hT 0 i χ χ ψb y`. Continuous: gradInnerInverse_continuous needs ContDiff 2 (χ∘chart i) and ContDiff 2 (fun z' => ψb (transition 0 i z') * χ (chart i z')). `transition 0 i` is C² (contDiff_transition), ψb smooth, χ C² → composition C². Compact support: the chart expression is supported in `tsupport (fun z' => ψb (transition 0 i z'))` which is the preimage of tsupport ψb under transition 0 i; is that compact? Not obviously — need properness of the transition. Hmm, for the dilation atlas, transition 0 i is either id (i=0) or 2• (i≠0), which is proper, so preimage of compact is compact. But proving that requires the `hproper` hypothesis of the atlas... `dilationAtlasTwoSmooth` might have a lemma. This is getting heavy.

Alternative: only need i ∈ {0,1} and I can handle i=1 with a direct argument: `pouPairing 0 1 χ χ ψb` — use the transfer with chart 1, and the chart expression with transition 0 1 = 2•:
- `hψb (transition 0 1 z') * χ (chart 1 z')` — support in `(fun z' => ψb (2•z'))`'s tsupport = `2⁻¹ • tsupport ψb` (bounded) → compact. Since ψb is compactly supported and 2• is a homeomorphism, the preimage of tsupport ψb is compact. Proving in Lean: `(fun z => ψb (2 • z))` has compact support because `HasCompactSupport.comp_left`? There are lemmas: `HasCompactSupport.comp_homeomorph`? Hmm.

Easier approach: avoid the whole issue by making BOTH ψ components supported such that the pairing supports are easy. Note the pairing's chart-i expression is supported where `ψ (transition 0 i z') * χ (chart i z') ≠ 0`, i.e. where ψb(transition 0 i z') ≠ 0 → z' ∈ transition 0 i ⁻¹' tsupport ψb. For i=1: transition 0 1 = 2•, preimage = (1/2)•tsupport ψb, compact.

Alternatively, note that `tsupport ψb ⊆ ball 0 (R+1)`; the preimage under 2• is `ball 0 ((R+1)/2)`, compact. But proving compactness of the support of the *composite* requires care but is doable: `HasCompactSupport (fun z => ψb (2 • z))` via `IsCompact.of_isClosed_subset (isCompact_closedBall 0 ((R+1)/2)) ...`.

Hmm, this adds complexity. Alternative approach: **use the existing theorem's `hintR` hypotheses with the POU data supplied, and prove hintR for both j by using the transfer lemma at chart j**. For j=1 the needed compact support is as above. Let me think about whether `simp`/mathlib can do it:

Actually, maybe simpler: prove `HasCompactSupport (fun z : Vec 1 => ψb (c • z))` for c ≠ 0 via:
```lean
have : HasCompactSupport (fun z : Vec 1 => ψb ((2:ℝ) • z)) := by
  have h := hψbc.comp_homeomorph (Homeomorph.smulOfNeZero (2:ℝ) h2)  -- ?
```
There is `HasCompactSupport.comp_homeomorph (hf : HasCompactSupport f) (e : X ≃ₜ Y) : HasCompactSupport (f ∘ e)`. The map `z ↦ 2 • z` is `Homeomorph.smulOfNeZero 2 h2` (a homeomorphism of a normed space). If that exists in mathlib: `Homeomorph.smulOfNeZero (c : 𝕜) (hc : c ≠ 0) : E ≃ₜ E`. Let me check.

Then `(hψbc.comp_homeomorph (Homeomorph.smulOfNeZero 2 h2))` gives HasCompactSupport (ψb ∘ (2•)) = HasCompactSupport (fun z => ψb (2•z)). 

Then the product with χ (chart 1 z) is compactly supported (support ⊆ support of the first factor).

This is doable. Alternatively, for j=1, note chart 1 = 2•, so `χ (chart 1 z) = χ (2•z)`; the product is supported where ψb(2z) ≠ 0.

Actually hold on. Let me reconsider: maybe I can choose R and POU such that `ψb` itself has compact support *in a small ball* and then for i=1 preimage is a smaller ball. Fine.

Let me simplify the whole H5 by proving a general lemma for the dilation atlas:

```lean
theorem h5_pairing_integrable (i : ℕ) (χ ψb : Vec 1 → ℝ) (hχ2 : ContDiff ℝ 2 χ) (hψbsm : ContDiff ℝ ∞ ψb) (hψbc : HasCompactSupport ψb) :
    Integrable (fun m => A2.pouPairing 0 i χ χ ψb m) μ2
```
using transfer at chart i with:
- hsupp via totality.
- hint: chart expression contour + compact support. For compact support of the chart expression I need the support of `fun z => ψb (A2.transition 0 i z) * χ (A2.chart i z)`. Hmm, in the general case the transition is C² but not obviously proper. For the dilation atlas, `transition 0 i` is either id or 2•. I'd need to case on i.

Alternatively: since the pairing lift is over chart i, and `A2.pouPairing 0 i ... = lift i (...)`, its support is contained in `chart i '' support (fun z => gradInnerInverse ...)`. And `support (gradInnerInverse u φ) ⊆ support φ` (there's a lemma `support_gradInnerInverse_subset`). And `support φ ⊆ (transition 0 i) ⁻¹' support ψb`-ish. So the manifold function `pouPairing 0 i` is supported in the *chart i image of a set* which may not be compact... but for integrability via transfer, I need the *chart expression* compactly supported, which requires the preimage compact.

Hmm OK. Let me just do the case analysis on i ∈ {0,1} for hintR:
- i=0: chart 0 = id → easy (as before).
- i=1: chart 1 = 2• → preimage of tsupport ψb under 2• is compact via homeomorphism.
And the `∀ j : Fin 2` in the theorem application: I'll supply `fun j => by fin_cases j; · exact hintR0; · exact hintR1`.

Similarly hintL: hintL is `∀ j : Fin 2, Integrable (fun m => G1.driftLaplacian F U m * pouPiece 0 V (ψ j) m) ...` — the `pouPiece` doesn't depend on j except through ψ j; since all ψ j = ψb, hintL is the same for both j. But the goal for j is `... * pouPiece 0 χ ((fun _ => ψb) j) m` — defeq to ψb. `simpa using hintL` should work since `(fun _ => ψb) j` reduces to ψb definitionally. Good — no issue for hintL.

Wait, in the error at line 379, the failing one was hintR with `↑j`. And hintL's `simpa` apparently succeeded? The log shows errors at 379 only for the R case (line 376 is the sum, 379 is `simpa using hintR`). Let me look: my code:
```
  · intro j; exact hψbsm
  · intro j; exact hψbsupp
  · intro x hx
    rw [Fin.sum_univ_two, hψbhalf x hx, hψbhalf x hx]
    norm_num
  · intro j; simpa using hintL
  · intro j; simpa using hintR
```
Line 376 = the sum rw; line 379 = `simpa using hintR`. hintL at line 378 apparently fine.

So I need hintR for i=1 as well.

Let me write:
```lean
  have hintR1 : Integrable (fun m => A2.pouPairing 0 1 χ χ ψb m) μ2 := by
    refine A2.integrable_globalMeasure_withDensity_of_supported volume 1
      (f := fun _ => 0) (g := fun m => A2.pouPairing 0 1 χ χ ψb m)
      measurable_const ?_ ?_ ?_
    · refine A2.measurable_pouPairing 0 1 ?_ ?_ hψbsm
      · -- ContDiff 2 (χ ∘ chart 1)
        rw [show A2.chart 1 = fun y : Vec 1 => (2:ℝ) • y from dilationChart_of_ne 2 (by norm_num)]
        simpa [Function.comp_def] using hχ2.comp (contDiff_const_smul (2:ℝ))
      · same
    · intro m _
      obtain ⟨x, hx⟩ := hT.surj_chart 1 m
      exact ⟨x, by rw [hT.source_univ 1]; exact mem_univ x, hx⟩
    · -- chart expression compactly supported
      have h1 : A2.chart 1 = fun y : Vec 1 => (2:ℝ) • y := dilationChart_of_ne 2 (by norm_num)
      have h2 : (2:ℝ) ≠ 0 := by norm_num
      -- the function z ↦ ψb (2 • z) has compact support
      have hcomp : HasCompactSupport (fun z : Vec 1 => ψb ((2:ℝ) • z)) :=
        hψbc.comp_homeomorph (Homeomorph.smulOfNeZero (2:ℝ) h2)
      ...
```
Let me check `Homeomorph.smulOfNeZero` exists and its type. In mathlib: `Homeomorph.smulOfNeZero (c : 𝕜) (hc : c ≠ 0) : E ≃ₜ E`? I recall `Homeomorph.smulOfNeZero`. Let me test in scratch. Also `HasCompactSupport.comp_homeomorph`.

Then:
```lean
      have hcont : Continuous (fun y : Vec 1 => Real.exp (-((fun _ => 0) (A2.chart 1 y))) * A2.pouPairing 0 1 χ χ ψb (A2.chart 1 y) * A2.density 1 y) := by
        rw [h1]
        -- after rw: chart 1 = 2•; use pouPairing_apply hT to rewrite the pairing
        ...
```
The pairing evaluated at chart 1 y: `A2.pouPairing 0 1 χ χ ψb (A2.chart 1 y)` = via `A2.pouPairing_apply hT 0 1 χ χ ψb y` = `(A2.metric 1).gradInnerInverse (fun z' => χ (A2.chart 1 z')) (fun z' => ψb (A2.transition 0 1 z') * χ (A2.chart 1 z')) y`. With `htrans01 : A2.transition 0 1 = fun y => (2:ℝ) • y` (from dilationTransition_zero_of_ne), the second function is `fun z' => ψb (2•z') * χ (2•z')`.

So the chart expression is:
`exp(-0) * gradInnerInverse (χ∘2•) (ψb∘2• * χ∘2•) y * density`.
Continuity: gradInnerInverse_continuous with C² arguments (χ∘2• is C², ψb∘2• * χ∘2• is C²). Compact support: support ⊆ support (ψb∘2•) which is compact (homeomorphism preimage of tsupport ψb — actually support (ψb∘2•) = 2⁻¹ • support ψb; tsupport ⊆ 2⁻¹ • tsupport ψb which is compact).

Hmm, `HasCompactSupport (fun z => ψb (2•z))` gives IsCompact (tsupport (ψb∘2•)). The product `(ψb∘2•) * (χ∘2•)` has support ⊆ support (ψb∘2•) ⊆ tsupport, so the product's tsupport ⊆ tsupport(ψb∘2•) (closure monotone) → compact. Then the whole expression's support ⊆ that.

This is all doable but adds ~40 lines. Let me simplify by factoring a general lemma:

```lean
lemma comp_smul_hasCompactSupport {f : Vec 1 → ℝ} (hf : HasCompactSupport f) {c : ℝ} (hc : c ≠ 0) :
    HasCompactSupport (fun z : Vec 1 => f (c • z)) :=
  hf.comp_homeomorph (Homeomorph.smulOfNeZero c hc)
```
Let me check `Homeomorph.smulOfNeZero` in mathlib. Search in the leader's mathlib? Let me grep the mathlib source in D6_weekly_release packages.

Actually there's a simpler approach avoiding compact-support-preimage: **strengthen the POU choice so ψb is supported in a small ball around 0**; then `fun z => ψb (2•z)` is supported in compact ball. But same proof burden.

Wait — alternative clean approach: use `hψbc` for ψb, and note the pairing's chart expression is supported in `tsupport (fun z => ψb (c•z) * χ (c•z))`. For the transfer lemma I need `Integrable` of the chart expression, which I can get from `hcomp.mul hcont` etc.

Let me check mathlib for `HasCompactSupport.comp_homeomorph` and `Homeomorph.smulOfNeZero`.

Actually, maybe an even simpler way exists: the D13 layer might already have a lemma stating that the dilation transition preimages of compacts are compact — `dilationAtlasTwo_transition_preimage_isCompact`? Let me grep. If it exists for `dilationAtlasTwoSmooth`, I can use it. Let me search.
dsh: reasoning:
`Homeomorph.smulOfNeZero` exists. Let me check `HasCompactSupport.comp_homeomorph` exists.

Actually there's possibly `HasCompactSupport.comp_smul`? Let me grep for `comp_homeomorph` in mathlib.

Given time (I'm ~2.5h in of 4h), I should be pragmatic. Option: simplify H5's non-vacuity instantiation by choosing **V = χ and ψ all supported inside a *ball*, and use hintL/hintR only for j=0, with the other POU function chosen to make j=1 trivial**. Earlier the trivial route required `pouPairing 0 1 χ χ 0` and `pouPiece 0 χ 0` integrability, which needed zero-function simplifications. But wait — for the *ψ₁ = 0* route, hintL j=1 needs `Integrable (fun m => Δχ m * pouPiece 0 χ 0 m)`. I can prove the *chart expression* is zero via transfer at chart 0:
- `pouPiece 0 χ 0 (chart 0 y) = 0 * χ y = 0` via `pouPiece_apply`.
So `hintL1`: transfer at chart 0 with g := fun m => Δχ m * pouPiece 0 χ (0) m; hsupp trivial; hint: the chart expression `exp(-0) * (Δχ y * pouPiece 0 χ 0 (chart 0 y)) * density y` = 0 a.e., integrable via congr with zero. Easy.

- hintR j=1: `Integrable (fun m => A2.pouPairing 0 1 χ χ 0 m)`. Transfer at chart **1** requires the chart-1 expression; but I can also transfer at chart **0** if I can show the support of `pouPairing 0 1 χ χ 0` is inside chart 0 image — it's inside chart 1 image; since the atlas is total, chart 1 image = univ = chart 0 image. So hsupp at i=0 is trivial too! The transfer lemma's hsupp only needs support ⊆ chart 0 image, which is univ (total). So I can use i=0 for hintR j=1 as well!
Then hint: `Integrable (fun y => exp(-0) * pouPairing 0 1 χ χ 0 (chart 0 y) * density 0 y)`. I need to show this function is (a.e.) zero or at least integrable. `pouPairing 0 1 χ χ 0 (chart 0 y)` = `lift 1 (fun z => gradInnerInverse (χ∘chart 1) (0 * χ∘chart 1)) (chart 0 y)`. Using `lift_apply_chart`? chart 0 y ∈ chart 1 '' source 1 (total → yes, via surjectivity), so `lift 1 φ (chart 0 y) = φ (invFunOn (chart 1) (source 1) (chart 0 y))` and φ = gradInnerInverse (χ∘chart 1) (fun z' => 0 * ...) = gradInnerInverse ... 0. So the function value is `gradInnerInverse u 0 z₀`. To show that's 0, use `gradInnerInverse_eq_zero_of_eventuallyEq_zero` — but that's in namespace OverlapAtlas for a partial atlas and requires `φ =ᶠ[𝓝 y] 0`; here φ is *identically* 0, so `Filter.EventuallyEq.refl` / `eventually_of_forall`. Then the whole hint chart expression is 0 → integrable.

So with ψ₁ = 0, all four hint obligations reduce to integrability of functions that are pointwise-zero-ish, but proving the zero-ness needs the `lift` machinery + `gradInnerInverse_eq_zero_of_eventuallyEq_zero`. That's maybe 20 lines. Hmm, comparable to the ψ₁ = ψ₀ route but avoids homeomorphism compactness.

Actually WAIT. Much simpler: use the ψ₀ = ψ₁ = (1/2)χb route and prove **hintR for i=1** using transfer at chart **0** too (since total ⇒ support in chart 0 image is automatic). Then the chart expression at chart 0 for `pouPairing 0 1 χ χ ψb` still involves the lift over chart 1 evaluated at chart 0 y — not directly simplifiable to a nice form.

Hmm no. The transfer lemma's `hint` requires integrability of the chart-0 expression `g (chart 0 y) * density 0 y`. For g = pouPairing 0 1 ..., the value at chart 0 y is the lift over chart 1 at an arbitrary point, which is `φ(invFunOn (chart 1) (source 1) (chart 0 y))` — messy but we know it's *supported* in chart 1 image... circular.

OK, decision: implement the case analysis with chart 1 (homeomorphism compact support), which is the mathematically clean route, but let me make it as short as possible by proving a general helper for the dilation atlas:

```lean
/-- For the total dilation atlas, the lifted pairing against a compactly supported POU function
is integrable for the weighted global measure. -/
lemma dilationAtlasTwo_pouPairing_integrable
    (i : ℕ) (χ ψb : Vec 1 → ℝ) (hχ2 : ContDiff ℝ 2 χ) (hψbsm : ContDiff ℝ ∞ ψb)
    (hψbc : HasCompactSupport ψb) :
    Integrable (fun m => A2.pouPairing 0 i χ χ ψb m) μ2 := by
  have hT : A2.IsTotal := dilationAtlasTwoSmooth_isTotal 0 G1
  have hchart0 : A2.chart 0 = fun x : Vec 1 => (1 : ℝ) • x := dilationAtlasTwo_chart_zero (n := 0) G1
  have h2 : (2 : ℝ) ≠ 0 := by norm_num
  have hchart_smul : ∀ {i : ℕ}, i ≠ 0 → A2.chart i = fun y : Vec 1 => (2 : ℝ) • y := by
    intro i hi
    show dilationChart (d := 1) 2 i = fun y : Vec 1 => (2 : ℝ) • y
    exact dilationChart_of_ne 2 hi
  -- compact support of the pulled-back POU function
  have hcomp : HasCompactSupport (fun z : Vec 1 => ψb (A2.transition 0 i z)) := by
    by_cases hi : i = 0
    · subst hi
      rw [show A2.transition 0 0 = fun y : Vec 1 => y from dilationTransition_zero_zero 2]
      simpa using hψbc
    · rw [show A2.transition 0 i = fun y : Vec 1 => (2 : ℝ) • y from dilationTransition_zero_of_ne 2 hi]
      exact hψbc.comp_homeomorph (Homeomorph.smulOfNeZero (2 : ℝ) h2)
  ...
```
Then the transfer:
```lean
  refine A2.integrable_globalMeasure_withDensity_of_supported volume i
    (f := fun _ => 0) (g := fun m => A2.pouPairing 0 i χ χ ψb m) measurable_const ?_ ?_ ?_
  · -- measurable
    refine A2.measurable_pouPairing 0 i ?_ ?_ hψbsm
    · -- ContDiff 2 (χ ∘ chart i)
      by_cases hi : i = 0
      · subst hi; rw [hchart0]; simpa [Function.comp_def] using hχ2.comp (contDiff_const_smul (1:ℝ))
      · rw [hchart_smul hi]; simpa [Function.comp_def] using hχ2.comp (contDiff_const_smul (2:ℝ))
    · same
  · -- support in chart i image
    intro m _
    obtain ⟨x, hx⟩ := hT.surj_chart i m
    exact ⟨x, by rw [hT.source_univ i]; exact mem_univ x, hx⟩
  · -- chart expression
    refine (Continuous.integrable_of_hasCompactSupport ?_ ?_).congr ?_
    · -- continuity of the chart expression
      have hcont : Continuous (fun y : Vec 1 => Real.exp (-((fun _ => 0) (A2.chart i y))) *
          A2.pouPairing 0 i χ χ ψb (A2.chart i y) * A2.density i y) := by
        -- rewrite the pairing at chart i using pouPairing_apply
        ...
      exact hcont
```
Hmm, for continuity I need `A2.pouPairing 0 i χ χ ψb (A2.chart i y)` = gradInnerInverse (χ∘chart i) (ψb∘transition 0 i * χ∘chart i) y. That's `A2.pouPairing_apply hT 0 i χ χ ψb y`. Since `A2.chart i y` appears inside, I can rewrite the whole function using `funext`/`simp_rw [A2.pouPairing_apply hT 0 i χ χ ψb]`. Then continuity follows from `gradInnerInverse_continuous` with C² arguments:
- `ContDiff ℝ 2 (fun z' => χ (A2.chart i z'))` (from above),
- `ContDiff ℝ 2 (fun z' => ψb (A2.transition 0 i z') * χ (A2.chart i z'))`: ψb∘transition is C^∞ (transition C², ψb C^∞), times χ∘chart i C².
Then `gradInnerInverse_continuous _ _ huc hvc` gives continuity; multiply by density (C¹ continuous) and exp(-0) constant.

- compact support: the chart expression's support ⊆ support (ψb ∘ transition 0 i) ⊆ tsupport → compact via hcomp. Note the chart expression is `exp(-0) * (gradInnerInverse ... ) * density`; the gradInnerInverse is supported where the second argument is supported (support_gradInnerInverse_subset) ⊆ support (ψb∘transition * χ∘chart) ⊆ support (ψb∘transition). So the whole expression is compactly supported:
```lean
      refine IsCompact.of_isClosed_subset hcomp (isClosed_tsupport _) (closure_minimal ?_ (isClosed_tsupport _))
      intro y hy
      rw [Function.mem_support] at hy
      by_contra hyt
      have h0 : ψb (A2.transition 0 i y) = 0 := Function.notMem_support.mp fun hm => hyt (subset_tsupport _ hm)
      -- then the chart expression at y is 0
      ...
```
This requires showing the chart expression is 0 when ψb(transition 0 i y) = 0: gradInnerInverse u (fun z' => ψb(transition 0 i z') * χ(chart i z')) y — at z'=y the second function is 0 but gradInnerInverse involves *derivatives* of it, not just the value at y. So the compact support argument needs care: if ψb(transition 0 i y) = 0 merely at the point y, the derivatives need not vanish. However, the support of the *function* `fun z' => ψb (transition 0 i z') * χ (chart i z')` is contained in `(transition 0 i) ⁻¹' support ψb` intersected with support χ. And `support (gradInnerInverse u φ) ⊆ support φ` (lemma `support_gradInnerInverse_subset`) — actually derivative support ⊆ support φ for C¹ φ (true: if φ ≡ 0 on a neighborhood, derivatives vanish; support of fderiv ⊆ closure of support? The lemma `support_gradInnerInverse_subset` is proven in D13 and states exactly support ⊆ support of the second argument? Let me check its statement. If so:
support(chart expr) ⊆ support(gradInnerInverse u φ) ⊆ support φ ⊆ support(ψb∘transition) ⊆ tsupport(ψb∘transition) which is compact. 

So:
```lean
      refine IsCompact.of_isClosed_subset hcomp (isClosed_tsupport _) (closure_minimal ?_ (isClosed_tsupport _))
      intro y hy
      rw [Function.mem_support] at hy ⊢
      exact fun hψ0 => hy (by ... )
```
Let me structure: I want `support(chartExpr) ⊆ tsupport (ψb∘transition)`. Since `tsupport (ψb∘transition)` is closed, `closure_minimal` with `support ⊆ tsupport` works:
```lean
      intro y hy
      have hsuppφ : y ∈ support (fun z' => ψb (A2.transition 0 i z') * χ (A2.chart i z')) := by
        by_contra h0
        rw [Function.mem_support] at hy
        -- hy : chartExpr y ≠ 0
        ...
```
Hmm, this is getting deep. Let me simplify: prove **`HasCompactSupport (chartExpr)`** by showing chartExpr ≠ 0 → ψb(transition 0 i y) ≠ 0. Is that true? chartExpr y = exp(-0) * gradInnerInverse u φ y * density y where φ = ψb∘transition * χ∘chart. If ψb(transition 0 i y) = 0 but its derivative isn't, gradInnerInverse could be nonzero. So no. But if y ∉ tsupport (ψb∘transition), then ψb∘transition ≡ 0 on a neighborhood of y (since y is in the complement of the closed tsupport, which is open), so φ ≡ 0 on a neighborhood, hence gradInnerInverse u φ y = 0 (by `gradInnerInverse_eq_zero_of_eventuallyEq_zero`). So the correct argument: support(chartExpr) ⊆ tsupport(ψb∘transition). And indeed my `closure_minimal` target is `tsupport chartExpr ⊆ tsupport (ψb∘transition)`:
```lean
      refine IsCompact.of_isClosed_subset hcomp (isClosed_tsupport _) (closure_minimal ?_ (isClosed_tsupport _))
      intro y hy
      rw [Function.mem_support] at hy
      by_contra hyt
      have hφ0 : (fun z' => ψb (A2.transition 0 i z') * χ (A2.chart i z')) =ᶠ[𝓝 y] 0 := by
        have : (tsupport (fun z => ψb (A2.transition 0 i z)))ᶜ ∈ 𝓝 y :=
          (isClosed_tsupport _).isOpen_compl.mem_nhds hyt
        filter_upwards [this] with z hz
        have : ψb (A2.transition 0 i z) = 0 := Function.notMem_support.mp fun hm => hz (subset_tsupport _ hm)
        simp [this]
      have hgrad : (A2.metric i).gradInnerInverse (fun z' => χ (A2.chart i z')) (fun z' => ψb (A2.transition 0 i z') * χ (A2.chart i z')) y = 0 :=
        Poincare.D13.ManifoldIBP.gradInnerInverse_eq_zero_of_eventuallyEq_zero (A := A2) i _ _ hφ0
      -- chartExpr y = 0
      simp [chartExpr_def, hgrad] at hy
```
This is ~15 lines. Plus continuity. Total maybe 60 lines for the general hintR lemma. Given time constraints (≈1.5h left), is this the best use? The H5 non-vacuity could instead be established with the simpler ψ₁=0 route where hintR j=1 is *also* proven via transfer at chart 0 with the zero-pairing... which also needs `gradInnerInverse u 0 = 0` pointwise. Using `gradInnerInverse_eq_zero_of_eventuallyEq_zero` with the identically-zero function is easier: the function is literally `fun z' => (fun _ => 0) (transition 0 1 z') * χ (chart 1 z')` which simp reduces to `fun _ => 0`; then `gradInnerInverse ... (fun _ => 0) y` — need a simp lemma for that. `gradInnerInverse_eq_zero_of_eventuallyEq_zero (A := A2) 1 _ (fun _ => 0) (Filter.EventuallyEq.refl _ 0)`? The lemma requires `φ =ᶠ[𝓝 y] 0`; `Filter.EventuallyEq.refl` might not exist; use `eventually_of_forall (fun z => rfl)`.

Hmm, both routes need similar machinery. Let me go with the (1/2)χb route and write the general lemma. Actually, let me simplify the general lemma by *only proving it for i=0 and i=1* — no, general with case analysis on i=0 is fine.

Time check: ~2h35m elapsed. I have ~1h25m. Let me be efficient: write the general lemma once, compile, then write the ledger. If the lemma proves too fiddly after 2 attempts, fall back to a *weaker but honest* H5 non-vacuity: instantiate with ψ = (0,0) and V = 0 (fully degenerate but hypotheses satisfiable), and report H5's non-vacuity as "hypotheses satisfiable only in the degenerate case in this audit; the theorem is a conditional interface" — but the task demands non-vacuity per headline with the model atlases. Hmm. Better: U=0, V=χ nonzero, ψ=(ψb,0): then hintL/hintR integrability for j=0 requires the nonzero piece integrability (which I already have via chart 0 transfer, easy) — wait, that's the ψ₁=0 route.

Let me reconsider the ψ₁ = 0 route carefully:
- hintL j=0: integrability of `Δχ * pouPiece 0 χ ψb` — my existing hintL proof (chart 0 transfer) works. ✓
- hintL j=1: integrand `Δχ * pouPiece 0 χ (fun _ => 0)`; I can prove this by transfer at chart 0 with hint: the chart expression is 0. Since `pouPiece 0 χ 0 (chart 0 y) = 0 * χ y = 0` via `pouPiece_apply`, the hint reduces to integrability of `fun y => exp(-0) * (Δχ y * 0) * density y` which is integrable by `integrable_zero` + congr. ✓ (easy)
- hintR j=0: my existing hintR proof (chart 0 transfer). ✓
- hintR j=1: `Integrable (fun m => pouPairing 0 1 χ χ (fun _ => 0) m) μ2`. Transfer at chart 0: hsupp trivial; hint: integrability of `fun y => exp(-0) * pouPairing 0 1 χ χ 0 (chart 0 y) * density 0 y`. Need this to be integrable. I can show `pouPairing 0 1 χ χ 0 = fun _ => 0` as a *function*, i.e. `hpair1`. That requires showing the lift of `gradInnerInverse (χ∘chart1) (fun z' => 0 * χ(chart1 z'))` is 0 — the inner function is identically 0, so gradInnerInverse (...) 0 y = 0 via `gradInnerInverse_eq_zero_of_eventuallyEq_zero`; then `lift 1 (fun _ => 0) = 0` by `simp [lift]`. Doable in a `funext` proof (~12 lines).
  Then integrability of the zero function. ✓

So the ψ₁=0 route requires: (a) hpair1 function-zero proof; (b) hintL j=1 proof. The (1/2)χb route requires: hintR for i=1 with full compact-support machinery (~60 lines). The ψ₁=0 route is probably ~30 lines. Let me go with ψ₁=0!

But then the conclusion content: with U = V = χ and ψ₀ = bump (nonzero), ψ₁ = 0 — the POU is genuine (sums to 1 on tsupport χ) and the RHS/LHS integrands are nonzero; conclusion is the nontrivial identity ∫Δχ·χ = -∫|∇χ|² on the dilation atlas. That's non-vacuous content. 

So let me revert to the earlier structure (ψ₀ = ψb bump with ψb = 1 on tsupport χ; ψ₁ = 0) but fix:
- hintL j=1 via transfer at chart 0 with zero chart expression,
- hintR j=1 via the hpair1 zero-function lemma.

Plan for hpair1:
```lean
  have hpair1 : A2.pouPairing 0 1 χ χ (fun _ : Vec 1 => (0 : ℝ)) = fun _ => 0 := by
    funext m
    rw [Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.pouPairing,
        Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.lift]
    split_ifs with hm
    · -- m ∈ chart 1 '' source 1
      -- value = gradInnerInverse (χ∘chart1) (fun z' => 0 * χ(chart1 z')) (invFunOn ...)
      have hφ : (fun z' : Vec 1 => (fun _ : Vec 1 => (0:ℝ)) (A2.transition 0 1 z') * χ (A2.chart 1 z')) = fun _ => 0 := by
        funext z'; simp
      rw [hφ]
      exact Poincare.D13.ManifoldIBP.gradInnerInverse_eq_zero_of_eventuallyEq_zero (A := A2) 1 _ _
        (eventually_of_forall (fun z => rfl))
    · rfl
```
Hmm, `gradInnerInverse_eq_zero_of_eventuallyEq_zero` is stated in namespace `Poincare.D13.ManifoldIBP.OverlapAtlas`? Let me check: SmoothAtlasPartial.lean has `namespace OverlapAtlas`? Line 39 is inside... Let me check the file's namespaces: earlier grep showed line 39 `lemma support_gradInnerInverse_subset` and I qualified it as `Poincare.D13.ManifoldIBP.support_gradInnerInverse_subset`, so that file's namespace is `Poincare.D13.ManifoldIBP` (not OverlapAtlas). And `gradInnerInverse_eq_zero_of_eventuallyEq_zero` at line 112 uses `A.pouPiece` etc. and `A.metric i` — let me check the namespace and the exact name. It's likely `Poincare.D13.ManifoldIBP.gradInnerInverse_eq_zero_of_eventuallyEq_zero`. Let me verify with grep of the namespace.

Also `lift`'s definition uses `if m ∈ A.chart b '' A.source b then φ (Function.invFunOn ...) else 0` — after `rw [lift]` and `split_ifs with hm`, the goal in the first branch involves `Function.invFunOn`; the claim is the value is 0 because φ ≡ 0. So actually I don't need `gradInnerInverse_eq_zero...`: φ is *identically* zero, so `gradInnerInverse u (fun _ => 0) z = 0` — I can rewrite φ to `fun _ => 0` and then use the eventual-zero lemma or a direct simp. Let me structure:

```lean
  have hgrad_zero : ∀ (z : Vec 1), (A2.metric 1).gradInnerInverse (fun z' => χ (A2.chart 1 z'))
      (fun _ : Vec 1 => (0:ℝ)) z = 0 := by
    intro z
    exact Poincare.D13.ManifoldIBP.gradInnerInverse_eq_zero_of_eventuallyEq_zero (A := A2) 1 _ _
      (eventually_of_forall (fun _ => rfl))
  have hpair1 : A2.pouPairing 0 1 χ χ (fun _ : Vec 1 => (0 : ℝ)) = fun _ => 0 := by
    funext m
    rw [Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.pouPairing,
        Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.lift]
    split_ifs with hm
    · simp only [Pi.zero_apply, zero_mul]
      exact hgrad_zero _
    · rfl
```
Careful: the `lift` is `A2.lift 1 (fun z => gradInnerInverse ...)`; `lift` is a def in namespace SmoothOverlapAtlas with `(b) (φ)`. After `rw [pouPairing, lift]` the term becomes `if h : ... then (fun z => ...) (invFunOn ...) else 0`. `split_ifs with hm` splits; first branch goal `gradInnerInverse (χ∘chart1) (fun z' => 0 * χ(chart1 z')) (invFunOn ...) = 0`. `simp only [Pi.zero_apply, zero_mul]` simplifies the inner function to `fun _ => 0`? It rewrites `0 * χ (...)` to `0` but the function `fun z' => 0` may need `funext`/eta. `simp` might not reduce `(fun z' => 0 * χ ...)` to `(fun _ => 0)` definitionally — but `gradInnerInverse` applied to a lambda; `hgrad_zero` expects `(fun _ => 0)`. They're defeq (both are functions with the same values; eta). Actually `fun z' => 0 * χ(...)` is not syntactically `fun _ => 0` but is defeq via beta/eta? No — these are different lambda terms; definitional equality of functions requires the bodies to be defeq: `0 * χ(...)` vs `0` — `zero_mul` is a theorem, not definitional. So I need to rewrite the function itself: `have hφ : (fun z' : Vec 1 => (0:ℝ) * χ (A2.chart 1 z')) = fun _ => 0 := by funext z'; simp`. Then `rw [hφ]; exact hgrad_zero _`.

Or use `gradInnerInverse_eq_zero_of_eventuallyEq_zero` directly with the *given* function: the function `fun z' => 0 * χ(...)` is *identically* 0, so `eventually_of_forall (fun z => by simp)` gives the eventual equality. Simpler:
```lean
    · refine Poincare.D13.ManifoldIBP.gradInnerInverse_eq_zero_of_eventuallyEq_zero (A := A2) 1
        (fun z' => χ (A2.chart 1 z')) (fun z' => (0:ℝ) * χ (A2.chart 1 z')) ?_
      exact eventually_of_forall (fun z => by simp)
```
Wait, the lemma's statement: `gradInnerInverse_eq_zero_of_eventuallyEq_zero {i} (u φ : Vec (n+1) → ℝ) {y} (hφ : φ =ᶠ[𝓝 y] 0) : (A.metric i).gradInnerInverse u φ y = 0`. So it needs `{A : SmoothOverlapAtlas ...}` and `A.metric i`. With `(A := A2)` and i=1. ✓

For hintL j=1 (ψ₁ = 0 → pouPiece 0 χ 0):
Transfer at chart 0:
```lean
  have hintL1 : Integrable (fun m => G1.driftLaplacian (fun _ => 0) χ m * A2.pouPiece 0 χ (fun _ : Vec 1 => (0:ℝ)) m) μ2 := by
    refine A2.integrable_globalMeasure_withDensity_of_supported volume 0
      (f := fun _ => 0) (g := fun m => G1.driftLaplacian (fun _ => 0) χ m * A2.pouPiece 0 χ (fun _ => 0) m)
      measurable_const ?_ ?_ ?_
    · exact (G1.driftLaplacian_continuous _ _ contDiff_const hχ2).measurable.mul
        (A2.measurable_pouPiece 0 hχ2.continuous.measurable contDiff_const)
    · intro m _; exact ⟨m, mem_univ m, by rw [hchart0]; simp⟩
    · -- chart expression is 0
      refine (integrable_zero (Vec 1) volume).congr ?_
      filter_upwards with y
      rw [hpiece0 y]  -- A2.pouPiece 0 χ 0 (chart 0 y) = 0
      simp
```
where `hpiece0 : ∀ y, A2.pouPiece 0 χ (fun _ => 0) (A2.chart 0 y) = 0` via `pouPiece_apply` then `simp`.
Hmm, `integrable_zero (Vec 1) volume` has type `Integrable (fun _ => 0) volume`; the goal is `Integrable (fun y => exp(-0) * (Δχ y * pouPiece...(chart 0 y)) * density 0 y) volume`. `.congr` with a filter_upwards proof that the LHS function equals 0 pointwise: `filter_upwards with y; rw [hpiece0 y]; simp`. Should work.

Actually the `.congr` direction: `Integrable.congr : Integrable f μ → f =ᵐ[μ] g → Integrable g μ`. Goal `Integrable g volume`; `(integrable_zero ...).congr h` where h : (fun _ => 0) =ᵐ g — `filter_upwards with y` gives goal `0 = g y`; `rw [hpiece0 y]; simp` should close (0 = 0). ✓

Similarly hintR j=1:
```lean
  have hintR1 : Integrable (fun m => A2.pouPairing 0 1 χ χ (fun _ : Vec 1 => (0:ℝ)) m) μ2 := by
    rw [hpair1]
    exact integrable_zero _ _
```
Since hpair1 is a function equality, `rw [hpair1]` changes the integrand to `fun _ => 0`. Then `integrable_zero`. ✓

So total additions: hpair1 + hintL1. Much less than the general lemma. Let me implement this: revert the ψ to `fun j => if j = 0 then ψb else 0` with `hψ0 : ψ 0 = ψb`, `hψ1 : ψ 1 = (fun _ => 0)`.

Let me rewrite the H5 section again with this design. I'll keep the ψb = bump (not halved) equal to 1 on tsupport χ.

New H5 block:

```lean
theorem h5_dilationAtlasTwo_via_pou_instantiated
    (χ ψb : Vec 1 → ℝ) (hχ2 : ContDiff ℝ 2 χ) (hχc : HasCompactSupport χ)
    (hψbsm : ContDiff ℝ ∞ ψb) (hψbc : HasCompactSupport ψb) (R : ℝ)
    (hψbsupp : tsupport ψb ⊆ ball (0 : Vec 1) (R + 1))
    (hψbone : ∀ x ∈ tsupport χ, ψb x = 1) :
    ∫ ... := by
  ... (hT, hchart0, hmetric0, htrans00, hpiece, hpair, hintL0 for ψb, hintR0 for ψb)
  -- the degenerate second POU piece
  have hzero : (fun _ : Vec 1 => (0:ℝ)) = 0 := rfl -- maybe not needed
  have hpiece0 : ∀ y : Vec 1, A2.pouPiece 0 χ (fun _ : Vec 1 => (0:ℝ)) (A2.chart 0 y) = 0 := by
    intro y; rw [A2.pouPiece_apply hT 0 χ _ y, hchart0]; simp
  have hintL1 : Integrable (fun m => G1.driftLaplacian (fun _ => 0) χ m * A2.pouPiece 0 χ (fun _ : Vec 1 => (0:ℝ)) m) μ2 := by ...
  have hpair1 : A2.pouPairing 0 1 χ χ (fun _ : Vec 1 => (0:ℝ)) = fun _ => 0 := by ...
  have hintR1 : Integrable (fun m => A2.pouPairing 0 1 χ χ (fun _ : Vec 1 => (0:ℝ)) m) μ2 := by rw [hpair1]; exact integrable_zero _ _
  let ψ : Fin 2 → Vec 1 → ℝ := fun j => if j = 0 then ψb else fun _ => 0
  have hψ0 : ψ 0 = ψb := by simp [ψ]
  have hψ1 : ψ 1 = (fun _ : Vec 1 => (0:ℝ)) := by simp [ψ]
  refine dilationAtlasTwo_weightedIBP_via_pou (n := 0) G1 (fun _ => 0) χ χ contDiff_const hχ2 hχ2 hχc (R := R) ψ ?_ ?_ ?_ ?_ ?_
  · intro j; fin_cases j
    · rw [hψ0]; exact hψbsm
    · rw [hψ1]; exact contDiff_const
  · intro j; fin_cases j
    · rw [hψ0]; exact hψbsupp
    · rw [hψ1]; simp [tsupport]
  · intro x hx
    rw [Fin.sum_univ_two, hψ0, hψ1]
    simp [hψbone x hx]
  · intro j; fin_cases j
    · rw [hψ0]; exact hintL0
    · rw [hψ1]; exact hintL1
  · intro j; fin_cases j
    · rw [hψ0]; exact hintR0
    · rw [hψ1]; exact hintR1
```

Note: `hintL0` must be for ψb (not (1/2)χb). Fine.

One concern: after `rw [hψ1]` the goal integrand has `pouPiece 0 χ (fun _ => 0)` — matches hintL1's integrand. ✓

For `hpair1`, the inner function in `pouPairing` after `rw [pouPairing, lift]`... Let me write it more robustly:
```lean
  have hpair1 : A2.pouPairing 0 1 χ χ (fun _ : Vec 1 => (0:ℝ)) = fun _ => 0 := by
    funext m
    rw [Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.pouPairing,
        Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.lift]
    split_ifs with hm
    · refine Poincare.D13.ManifoldIBP.gradInnerInverse_eq_zero_of_eventuallyEq_zero
        (A := A2) 1 (fun z' => χ (A2.chart 1 z'))
        (fun z' => (fun _ : Vec 1 => (0:ℝ)) (A2.transition 0 1 z') * χ (A2.chart 1 z')) ?_
      exact eventually_of_forall (fun z => by simp)
    · rfl
```
Hmm, `rw [pouPairing, lift]` — `pouPairing` and `lift` are in namespace `Poincare.D13.ManifoldIBP.SmoothOverlapAtlas`; using full names in `rw` might be `Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.pouPairing` which is the def. OK.

But there's a subtlety: `A2.pouPairing` is `SmoothOverlapAtlas.pouPairing A2` with implicit `A`; the equation lemma should rewrite. Alternatively `unfold Poincare...pouPairing Poincare...lift` might work. Let me use `simp only [Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.pouPairing, Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.lift]` then `split_ifs`.

Let me now rewrite the H5 block again. Also fix the earlier `hintL0`/`hintR0` proofs to use ψb (they already do) — I'll keep the parts from the current block for the ψb case (the hψb_tsupp helpers etc.).

Let me generate the new block.
dsh: reasoning:
While that compiles, let me start drafting the ledger JSON. I have most of the content. Let me also add the htrans-redundancy theorem to the probe if compile5 succeeds — it's a valuable independent finding. I'll add it in the next iteration.

Let me draft the ledger structure now (as a Python dict written to JSON at the end). Key data:

evidence:
- source files + sha256
- compile commands
- axiom outputs

Per declaration entry fields:
{
 "id": "H1",
 "name": "...",
 "file": "...",
 "line": N,
 "statement_captured": "printed type (abridged)",
 "hypotheses": [ {name, kind, domain, quantifier, note} ],
 "conclusion": "...",
 "classification": "proved theorem (general, conditional on atlas structure)",
 "domain_checks": {...},
 "adversarial": {
    "conclusion_equivalent_hypothesis": {"verdict":"no", "reason":...},
    "contradictory_hypotheses": {"verdict":"no", ...},
    "vacuous": {"verdict":"no", "witness":...},
    "wrong_domain": {...},
    "model_vs_manifold": {...},
    "hidden_interface_fields": [...]
 },
 "non_vacuity": {"witness": ..., "lean_decl": "Poincare.L4.D13Audit.h1_nonvacuous", "command": ..., "result": "compiled, axioms ..."},
 "axioms": ["propext","Classical.choice","Quot.sound"],
 "defects": [...]
}

Card claims audit:
- expanded_hypotheses: per item, supported/partially/unsupported with evidence.
- exact_blockers_closed: per item.
- findings: minor inaccuracies.

Let me check the compile.
dsh: reasoning:
Many errors. Let me look at the details. The rw failures at 405-421 are probably in the final refine (hψ0/hψ1 rewrites) and hintL0/hintL1. Let me inspect.
dsh: reasoning:
Several issues:

1. Line 339: `rw [hpiece y, hchart0, hmetric0]` in the hintL0 `.congr` proof. The goal after `rw [hpiece y, hchart0]` has `A2.density 0 y` vs `G1.density y`; `hmetric0 : A2.metric 0 = G1` but `A2.density 0` needs unfolding to `(A2.metric 0).density`. So `rw [hmetric0]` fails; need `show` or `simp [OverlapAtlas.density, hmetric0]`. Since `density` is a def in OverlapAtlas namespace: `A2.density 0 = (A2.metric 0).density := rfl`. So `rw [hmetric0]` should work after unfolding `density`... it doesn't because `A2.density 0 y` is `(A2.metric 0).density y`? The error shows the target has `A2.density 0 y` and the pattern `A2.metric 0` not found — meaning `A2.density 0 y` didn't unfold (density is a regular def, not reducible for rw matching). Use `simp only [OverlapAtlas.density, hmetric0]` or `change ... (A2.metric 0).density y` then rw. Simplest: `simp [OverlapAtlas.density, hmetric0]`? The `density` def might be `A.density i y := (A.metric i).density y`. In the namespace OverlapAtlas, the name is `Poincare.D13.ManifoldIBP.OverlapAtlas.density`. With `open ...OverlapAtlas`, `density` may resolve. I'll write `simp only [Poincare.D13.ManifoldIBP.OverlapAtlas.density, hmetric0, hchart0, hpiece y]` and then ring/simp for `Real.exp (-0) = 1`.
   Actually the goal: `G1.driftLaplacian 0 χ y * (ψb y * χ y) * G1.density y = Real.exp (-0) * (G1.driftLaplacian 0 χ (1•y) * (ψb y * χ y)) * (A2.metric 0).density y`. With hmetric0, RHS becomes `exp(-0) * (...) * G1.density y`; `simp` gives `Real.exp 0 = 1`? `Real.exp_neg, Real.exp_zero`; `simp` should close with `ring` after. Let me write:
   ```lean
   filter_upwards with y
   simp only [hpiece y, hchart0, one_smul, Pi.zero_apply, zero_sub, Real.exp_neg, Real.exp_zero]
   rw [show A2.density 0 = G1.density from by rw [Poincare...density, hmetric0]]  -- hmm
   ring
   ```
   Let me use: `have hdens : A2.density 0 = G1.density := by simp [Poincare.D13.ManifoldIBP.OverlapAtlas.density, hmetric0]` then `rw [hpiece y, hchart0, hdens]; simp; ring`.

   Wait, `A2.density` — is `density` a field/def of OverlapAtlas? Yes, `def density (i) (y) := (A.metric i).density y`. With A2 : SmoothOverlapAtlas which extends OverlapAtlas, `A2.density 0` works by dot notation. The full name is `Poincare.D13.ManifoldIBP.OverlapAtlas.density`.

2. Line 362: in the compact-support proof of hintR0, I ended with `exact hy (by simp [h0])` but the goal after `rw [Function.mem_support] at hy` is `G1.gradInnerInverse χ (fun z => ψb z * χ z) y * G1.density y ≠ 0` and I need to derive a contradiction from `ψb y = 0`. But the gradInnerInverse is not determined by the point value at y (derivatives!). Hmm — this is the same issue I identified earlier. For ψb with compact support, if y ∉ tsupport ψb, then ψb ≡ 0 on a neighborhood of y, so gradInnerInverse u (ψb * χ) y = 0 by `gradInnerInverse_eq_zero_of_eventuallyEq_zero`. So the compact-support proof needs that lemma, not simple pointwise vanishing. I need to fix both hintL0 and hintR0 compact-support proofs:
   - For **hintL0**, the chart expression is `Δχ y * (ψb y * χ y) * density y` — pointwise value depends on ψb y, so the simple argument works (support ⊆ support ψb). The error at 339 is a rewrite issue, not the support argument. Wait, the error at 362 is `unsolved goals` in the hintR0's hcc proof (the goal `gradInnerInverse ... ≠ 0 → ψb y ≠ 0` shown). Yes: for hintR0 I need the eventual-zero argument.
   
   Fix for hintR0's compact support:
   ```lean
        · -- compact support of the pairing chart expression
          rw [show (fun y : Vec 1 => G1.gradInnerInverse χ (fun z => ψb z * χ z) y * G1.density y)
              = (fun y => G1.gradInnerInverse χ (fun z => ψb z * χ z) y * G1.density y) from rfl]
          refine IsCompact.of_isClosed_subset hψbc (isClosed_tsupport _) (closure_minimal ?_ (isClosed_tsupport _))
          intro y hy
          rw [Function.mem_support] at hy
          by_contra hyt
          have hφ0 : (fun z => ψb z * χ z) =ᶠ[𝓝 y] 0 := by
            have hmem : (tsupport ψb)ᶜ ∈ 𝓝 y := (isClosed_tsupport ψb).isOpen_compl.mem_nhds hyt
            filter_upwards [hmem] with z hz
            have hz0 : ψb z = 0 := Function.notMem_support.mp fun hm => hz (subset_tsupport _ hm)
            simp [hz0]
          have hgrad0 := Poincare.D13.ManifoldIBP.gradInnerInverse_eq_zero_of_eventuallyEq_zero (A := A2) 0 (fun z => χ (A2.chart 0 z)) (fun z => ψb z * χ z) hφ0
          -- need to connect: our gradInnerInverse uses G1, χ directly (after hchart0 etc.)
   ```
   Hmm, in hintR0's hcc, the function is `fun y => G1.gradInnerInverse χ (fun z => ψb z * χ z) y * G1.density y` — the gradInnerInverse is the *G1* one, not `(A2.metric 0)`. So use the lemma with `G := G1`? The lemma `gradInnerInverse_eq_zero_of_eventuallyEq_zero` is stated for an atlas A: `(A.metric i).gradInnerInverse u φ y = 0`. I need the chart-metric version. Maybe `ChartMetric` has its own version? Let me check the namespace of that lemma — it's `Poincare.D13.ManifoldIBP.gradInnerInverse_eq_zero_of_eventuallyEq_zero` (unknown per error at 390!). The error says unknown identifier. So the name is different. Let me check SmoothAtlasPartial.lean's namespace: earlier grep showed line 39 `lemma support_gradInnerInverse_subset` and I qualified it as `Poincare.D13.ManifoldIBP.support_gradInnerInverse_subset` (which compiled fine). The file has `namespace Poincare.D13.ManifoldIBP`? Let me check the actual namespace of `gradInnerInverse_eq_zero_of_eventuallyEq_zero` and `support_gradInnerInverse_subset`.

Let me check the file header/namespaces.

3. Line 381: `(integrable_zero (Vec 1) volume)` — wrong argument order: `integrable_zero (α) (μ)`? The error says `integrable_zero (Vec 1) ?m` has type `∀ (μ : Measure (Vec 1)), Integrable 0 μ` — so it's `integrable_zero α : ∀ μ, Integrable 0 μ`. I should write `integrable_zero (Vec 1) volume` — that's what I did? The error shows `integrable_zero (Vec 1) ?m.1010` — it seems `volume` was consumed as the second explicit argument but it's a Measure, not... Hmm, the error says "The argument volume has type Measure ?m but is expected to have type Type ?u" — so `integrable_zero` takes `(α : Type)` then `(μ : Measure α)`? Then `integrable_zero (Vec 1) volume` should be fine... unless the error is about `(integrable_zero (Vec 1) volume).congr` where `.congr` is being parsed as field access on the *proof* and Lean interprets `Integrable.congr` differently. The `.congr` on an `Integrable` term: `Integrable.congr h h'` — for `h : Integrable f μ`, `h.congr h'`. The error "Invalid field `congr`: The environment does not contain `Function.congr`" suggests Lean resolved `(integrable_zero (Vec 1) volume).congr` weirdly because `integrable_zero (Vec 1) volume` had a type error → it interpreted `.congr` as something else. Let me check the signature of `integrable_zero`: maybe it's `integrable_zero (μ : Measure α)` (implicit α). So `integrable_zero volume` should work; `integrable_zero (Vec 1) volume` passes Vec 1 as μ — type error! Yes, that's it: `integrable_zero` has implicit α, so `integrable_zero volume : Integrable (fun _ => 0) volume`. Fix: use `integrable_zero volume` (and for μ2: `integrable_zero μ2`).

4. Line 390: `Poincare.D13.ManifoldIBP.gradInnerInverse_eq_zero_of_eventuallyEq_zero` unknown. Find the right name/namespace.
5. Line 393: "No goals to be solved" — the `· rfl` after the first branch maybe.
6. Line 397: `integrable_zero _ _` — fix to `integrable_zero μ2`.
7. Lines 405-421: `rw [hψ0]` fails because the goal shows `ψ ((fun i => i) ⟨0,⋯⟩)` — the `fin_cases j` didn't substitute `j` by `0` in the *type ascription* of ψ? The goal is `ContDiff ℝ ∞ (ψ ((fun i => i) ⟨0, ⋯⟩))` — j was replaced by `⟨0, _⟩` cast to ℕ? Hmm, `ψ : Fin 2 → ...`, so `ψ j` with j : Fin 2; after fin_cases, `ψ ⟨0, _⟩`. `hψ0 : ψ 0 = ψb` should rewrite `ψ ⟨0,_⟩` since `⟨0,_⟩ = 0` by proof irrelevance... rw matches up to syntactic equality of the index terms; `⟨0, ⋯⟩` vs `0` (i.e. `OfNat.ofNat 0`) — `(0 : Fin 2)` is `⟨0, by decide⟩`? Actually `OfNat.ofNat` for Fin is defined as `⟨0, ...⟩`, so they should be syntactically equal after reduction. But the error shows `ψ ((fun i => i) ⟨0, ⋯⟩)` — there's a cast `(fun i => i)`? That's from `fin_cases` producing `⟨0, ⋯⟩` with a coercion. Hmm.

The issue may be that `dilationAtlasTwo_weightedIBP_via_pou`'s `hψ_sm : ∀ i : Fin 2, ContDiff ℝ ∞ (ψ i)` and I applied `intro j; fin_cases j` — the goal after `fin_cases` might have `ψ ⟨0, ⋯⟩` where `⟨0,⋯⟩ : Fin 2` indeed. `hψ0 : ψ 0 = ψb`; `rw [hψ0]` should unify `ψ 0` with `ψ ⟨0,⋯⟩` if `0` elaborates to the same `⟨0,⋯⟩`. The proof component might differ (proof irrelevance should handle it in unification? `Fin.mk 0 h1` vs `Fin.mk 0 h2` — `rw` uses keyed matching that may require syntactic equality, not defeq). The printed term `(fun i => i) ⟨0, ⋯⟩` suggests the coercion from `Fin 2` to... hmm, there's `↑` somewhere.

Simplest fix: avoid `rw` and use `simpa [ψ] using hψbsm` after `fin_cases`. Since `ψ` is a let-def, `simp [ψ]` should reduce `ψ ⟨0,_⟩` to `ψb` (if the condition `⟨0,_⟩ = 0` reduces to True). Or use `change ContDiff ℝ ∞ ψb` — the `change` tactic uses defeq, which handles proof irrelevance. Let me write:
```lean
  · intro j
    fin_cases j
    · change ContDiff ℝ ∞ ψb; exact hψbsm
    · change ContDiff ℝ ∞ (fun _ : Vec 1 => (0:ℝ)); exact contDiff_const
```
Similarly for the others:
```lean
  · intro j
    fin_cases j
    · change tsupport ψb ⊆ ball (0:Vec 1) (R+1); exact hψbsupp
    · change tsupport (fun _ : Vec 1 => (0:ℝ)) ⊆ ball (0:Vec 1) (R+1); simp [tsupport]
  · intro x hx
    change ψb x + (fun _ : Vec 1 => (0:ℝ)) x = 1
    rw [hψbone x hx]; norm_num  -- or simp
  · intro j
    fin_cases j
    · change Integrable (fun m => G1.driftLaplacian (fun _ => 0) χ m * A2.pouPiece 0 χ ψb m) μ2
      exact hintL0
    · change Integrable (fun m => G1.driftLaplacian (fun _ => 0) χ m * A2.pouPiece 0 χ (fun _ : Vec 1 => (0:ℝ)) m) μ2
      exact hintL1
  · intro j
    fin_cases j
    · change Integrable (fun m => A2.pouPairing 0 0 χ χ ψb m) μ2; exact hintR0
    · change Integrable (fun m => A2.pouPairing 0 1 χ χ (fun _ : Vec 1 => (0:ℝ)) m) μ2; exact hintR1
```
`change` uses defeq, more robust than rw.

8. The hpair1 proof used `simp only [pouPairing, lift]` then `split_ifs` — with `change`-based final refines it should be fine; but the `gradInnerInverse_eq_zero_of_eventuallyEq_zero` name needs fixing.

Let me find the correct name. Let me check the namespace in SmoothAtlasPartial.lean.
dsh: reasoning:
- `support_gradInnerInverse_subset` is `Poincare.D13.ManifoldIBP.support_gradInnerInverse_subset` ✓ (already used).
- `gradInnerInverse_eq_zero_of_eventuallyEq_zero` is `Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.gradInnerInverse_eq_zero_of_eventuallyEq_zero` (inside `namespace SmoothOverlapAtlas` from line 59). So the full name is `Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.gradInnerInverse_eq_zero_of_eventuallyEq_zero`.

Also note `support_gradInnerInverse_subset` returns `support ⊆ tsupport φ` — good for the hintL compact-support argument but for hintR we need the eventual-zero reasoning (as noted).

Wait — for hintR0's compact support, I can use `support_gradInnerInverse_subset` more cleverly: the chart expression is `gradInnerInverse χ (ψb*χ) y * density y`; its support ⊆ support (gradInnerInverse χ (ψb*χ)) ⊆ tsupport (ψb * χ) ⊆ tsupport ψb — which is compact! And `tsupport (ψb*χ) ⊆ tsupport ψb` by closure_mono (support_mul_subset_left). So I don't need the eventual-zero lemma at all — I can use `support_gradInnerInverse_subset`!

So for hintR0's compact support:
```lean
        · refine IsCompact.of_isClosed_subset hψbc (isClosed_tsupport _)
            (closure_minimal ?_ (isClosed_tsupport _))
          intro y hy
          rw [Function.mem_support] at hy
          -- hy : gradInnerInverse χ (ψb*χ) y * density y ≠ 0
          have hy' : G1.gradInnerInverse χ (fun z => ψb z * χ z) y ≠ 0 := fun h0 => hy (by rw [h0, zero_mul])
          have hsupp := Poincare.D13.ManifoldIBP.support_gradInnerInverse_subset G1 χ (fun z => ψb z * χ z) hy'
          -- hsupp : y ∈ tsupport (fun z => ψb z * χ z)
          refine subset_tsupport _ ?_  -- need y ∈ support (ψb * χ)
          ...
```
`hsupp : y ∈ tsupport (fun z => ψb z * χ z)` and I need `y ∈ tsupport ψb`. Since tsupport (ψb*χ) ⊆ tsupport ψb (support (ψb*χ) ⊆ support ψb via mul_eq_zero). I can prove:
```lean
          have : tsupport (fun z => ψb z * χ z) ⊆ tsupport ψb :=
            closure_mono (fun z hz => by
              rw [Function.mem_support] at hz ⊢
              exact fun h0 => hz (by simp [h0]))
          exact this hsupp
```
That works and avoids the eventual-zero lemma. Similarly for hintL0's compact support, the chart expression's support ⊆ support ψb (pointwise, no derivatives) — the simple argument works.

But then hpair1 (`pouPairing 0 1 χ χ 0 = 0`) still needs the eventual-zero lemma — the inner function is identically zero, so the lemma applies. Use the full name `Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.gradInnerInverse_eq_zero_of_eventuallyEq_zero` with A := A2 and i := 1.

Hmm wait, actually for hpair1, since the inner function is *identically* `fun _ => 0`, I can instead prove `gradInnerInverse u (fun _ => 0) y = 0` directly via the lemma or via `simp [ChartMetric.gradInnerInverse, ChartMetric.partialDeriv]`. Using the lemma is safer.

Now `hintL1` (the ψ₁=0 pouPiece) uses `hpiece1` and `integrable_zero volume` — fix argument.

Let me now rewrite the affected parts. I'll be systematic:

(a) hintL0 congr proof ending: replace
```lean
      · filter_upwards with y
        rw [hpiece y, hchart0, hmetric0]
        simp [A2.density]
```
with
```lean
      · filter_upwards with y
        have hdens : A2.density 0 = G1.density := by
          simp [Poincare.D13.ManifoldIBP.OverlapAtlas.density, hmetric0]
        rw [hpiece y, hchart0, hdens]
        simp
```
Careful: the goal after `filter_upwards` is `(fun y => Δχ y * (ψb y*χ y) * G1.density y) y = (fun y => exp(-0) * (Δχ (chart 0 y) * pouPiece...) * A2.density 0 y) y` i.e. `G1... y * ... * G1.density y = exp(-0) * (G1... (1•y) * (ψb y*χ y)) * A2.density 0 y`. After `rw [hpiece y, hchart0, hdens]` → LHS `G1... y * (ψb y*χ y) * G1.density y`, RHS `exp(-0) * (G1... (1•y) * (ψb y*χ y)) * G1.density y`. Then `simp` should reduce `1•y = y`, `exp(-0) = 1`, and `ring`. Let me use `simp only [one_smul, Pi.zero_apply, neg_zero, Real.exp_zero, one_mul]` then `ring`. Let me write `rw [...]` then `simp [mul_assoc, mul_comm, mul_left_comm]`? Simpler: after the rw, `ring_nf`? The terms involve function values; `ring` should close after the rewrites. I'll use `simp only [...]` followed by `ring`.

(b) hintR0 congr proof ending: replace
```lean
      · filter_upwards with y
        rw [hpair y, hchart0]
        simp [A2.density]
```
with
```lean
      · filter_upwards with y
        have hdens : A2.density 0 = G1.density := by
          simp [Poincare.D13.ManifoldIBP.OverlapAtlas.density, hmetric0]
        rw [hpair y, hchart0, hdens]
        simp
```
(c) hintR0's compact-support argument: replace the pointwise argument with the support_gradInnerInverse_subset route.

(d) hintL1: `(integrable_zero (Vec 1) volume)` → `(integrable_zero volume)`; also the congr goal: after `rw [hpiece1 y]` goal `0 = exp(-0) * (Δχ y * 0) * A2.density 0 y`; `simp` closes.

(e) hpair1: fix lemma name to `Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.gradInnerInverse_eq_zero_of_eventuallyEq_zero`; also the `simp only [pouPairing, lift]` + `split_ifs` may need adjusting; and the first branch goal is `(fun z => gradInnerInverse (χ∘chart1) (fun z' => 0 * χ(chart1 z')) z) (invFunOn ...) = 0` — the lemma needs `y := invFunOn ...`; the `refine ... ?_` with the exact function. Let me write it as:
```lean
    · refine Poincare.D13.ManifoldIBP.SmoothOverlapAtlas.gradInnerInverse_eq_zero_of_eventuallyEq_zero
        (A := A2) 1 (fun z' => χ (A2.chart 1 z'))
        (fun z' => (fun _ : Vec 1 => (0 : ℝ)) (A2.transition 0 1 z') * χ (A2.chart 1 z')) ?_
      exact eventually_of_forall (fun z => by simp)
```
The lemma's implicit `{y}` should be inferred from the goal. OK.

(f) hintR1: `rw [hpair1]; exact integrable_zero _ _` → `rw [hpair1]; exact integrable_zero μ2`? Actually `integrable_zero μ2 : Integrable (fun _ => 0) μ2`; after `rw [hpair1]` the goal is `Integrable (fun _ => 0) μ2` ✓. So `exact integrable_zero μ2`. Hmm, the error at 397 says `integrable_zero ?m ?m` has type `∀ μ, Integrable 0 μ` — so `integrable_zero` first arg is implicit α. So `integrable_zero μ2` gives `Integrable 0 μ2` where `0` is the zero function ✓. But the goal after rw is `Integrable (fun m => (fun x => 0) m) μ2` which is defeq to `Integrable 0 μ2`? Should be, via eta. `simpa using (integrable_zero μ2)`.

(g) The final refine with `change`-based bullets.

(h) The earlier `hintL0`/`hintR0` are defined before the final refine; the `change` in the final bullets must match the *Integrable* types. Since `hintL0`'s type is `Integrable (fun m => G1.driftLaplacian (fun _ => 0) χ m * A2.pouPiece 0 χ ψb m) μ2` and the goal after `change` should be syntactically that.

Let me apply all these fixes with Python.

Also note: the `IsCompact.of_isClosed_subset hψbc` argument expects `tsupport (chartexpr) ⊆ tsupport ψb` for hintR0 now.

Let me write the fixes.
dsh: reasoning:
While compiling, let me add the `htrans`-redundancy theorem to a separate small probe file (or append later). Actually, let me wait for compile6 results first to avoid conflicting edits.

Meanwhile, let me draft the ledger JSON content. I'll write a Python script that constructs the JSON with all the audit data. Let me prepare the per-headline hypothesis tables.

Let me write the JSON builder now, then fill in the compile evidence after the probe succeeds. I'll write the file `longrun/results/L4-child-d13-semantic-audit.json` in my worktree.

Let me define the content carefully.

Also, note the objective says deliverables: `longrun/results/L4-child-d13-semantic-audit.md` and `.json` — in MY worktree (the task says "deliver longrun/results/..." and "Only your own longrun/results/L4-child-d13-semantic-audit.md can complete this task"). So paths: /data3/.../L4-child-d13-semantic-audit/longrun/results/.

Let me build the JSON with Python (a script file so it's reproducible). I'll write it after I have the final compile evidence.

Let me check the compile.
dsh: reasoning:
Fewer errors. Let me look at them.
dsh: reasoning:
Remaining issues:
1. Line 340 (`simp` made no progress) and 374: `simp` at the end of the congr proofs. After `rw [hpiece y, hchart0, hdens]` the goal is `G1... y * (ψb y * χ y) * G1.density y = Real.exp (-0) * (G1... (1•y) * (ψb y * χ y)) * G1.density y`. `simp` made no progress means it can't close; need `simp only [one_smul, Pi.zero_apply, neg_zero, Real.exp_zero, one_mul]` then `ring`. Let me replace `simp` with `simp only [one_smul, Pi.zero_apply, neg_zero, Real.exp_zero, one_mul]; ring`. Hmm, `(fun x => 0) (A2.chart 0 y)` in the LHS may appear as `G1.driftLaplacian (fun x => 0) χ ((fun x => 1•x) y)`; after rw [hchart0]? Wait, the goal after `rw [hpiece y, hchart0, hdens]`: hchart0 rewrites `A2.chart 0` occurrences but `(fun x => 1•x) y` beta-reduces? `rw` on `A2.chart 0` replaces the term with `fun x => 1•x`; then applied to y it's `(fun x => 1•x) y` which is defeq to `1•y` but not syntactically. `simp only [one_smul]` may not fire on `(fun x => 1•x) y` unless beta reduction happens (simp does beta reduce). Let me use `simp only [one_smul, Pi.zero_apply, neg_zero, Real.exp_zero, one_mul, mul_one]` and then `ring`. Safer: `ring_nf` after `simp only [...]`? Let me use:
```lean
      rw [hpiece y, hchart0, hdens]
      simp only [one_smul, Pi.zero_apply, neg_zero, Real.exp_zero, one_mul]
      ring
```
If `simp only` leaves `(fun x => 1 • x) y`, `one_smul` won't match; but `simp` does beta-reduction as part of its process, so `(fun x => 1•x) y` → `1 • y` → `y`. I think it works. To be safe, add `Function.comp_def`? Let me just try `simp only [...]` with a fallback `ring_nf`.

2. Line 391: `integrable_zero volume` — the error shows `integrable_zero` has signature `(ε' : Type) [TopologicalSpace] [ESeminormedAddMonoid] (μ : Measure α)`. So the first explicit argument is the codomain type ε'! So it's `integrable_zero ℝ volume`? Hmm: `integrable_zero (α : Type) [TopologicalSpace α] [ESeminormedAddMonoid α] (μ : Measure α') : Integrable 0 μ`? The error says:
```
integrable_zero ?m.1076 : ∀ (ε' : Type) [TopologicalSpace ε'] [ESeminormedAddMonoid ε'] (μ : Measure ?m.1076), Integrable 0 μ
```
So `integrable_zero (α)` where α = domain, and then it takes the *codomain* ε'. Hmm, actually maybe the signature is `integrable_zero (μ : Measure α) : Integrable (0 : α → ε) μ` with α implicit and ε explicit? The printed type says: `∀ (ε' : Type) [...] (μ : Measure ?m.1076), Integrable 0 μ`. So after providing the domain `?m.1076` (implicitly from volume's type), the next explicit arg is ε' the codomain. So the call should be `integrable_zero ℝ volume`? But then `Integrable 0 μ` with `0 : Vec 1 → ℝ`. Hmm, actually the expected type in the goal is `Integrable (fun y => ...) volume` with ℝ-valued functions. So `(integrable_zero ℝ volume)`? Let me instead avoid `integrable_zero` and use `integrable_const 0`? Or `integrable_zero` with named args: `integrable_zero (μ := volume)`. Hmm.

Simplest: use `integrable_const (0 : Vec 1 → ℝ)`? `integrable_const` might need finite measure. For volume on Vec 1 (infinite measure), the zero function is integrable: `integrable_zero`. Let me check the exact signature via a scratch `#check @integrable_zero`.

3. Line 402: `gradInnerInverse_eq_zero_of_eventuallyEq_zero (A := A2) 1 u φ hφ` — the argument order is `{i} (u φ) {y} (hφ)`. I passed `1 (fun z' => χ...) (fun z' => ...) ?_`. The error says the third argument (the function) was expected to be `hφ : ... =ᶠ 0`. So the signature must be `{i : ℕ} (u : ...) {y : ...} (hφ : φ =ᶠ[𝓝 y] 0)` with φ implicit! Let me re-read the lemma:
```
lemma gradInnerInverse_eq_zero_of_eventuallyEq_zero {i : ℕ} (u φ : Vec (n + 1) → ℝ)
    {y : Vec (n + 1)} (hφ : φ =ᶠ[𝓝 y] 0) :
```
Hmm, from the earlier sed output:
```
lemma gradInnerInverse_eq_zero_of_eventuallyEq_zero {i : ℕ} (u φ : Vec (n + 1) → ℝ)
    {y : Vec (n + 1)} (hφ : φ =ᶠ[𝓝 y] 0) :
    (A.metric i).gradInnerInverse u φ y = 0 := by
```
Wait, the sed output showed:
```
/-- The pairing vanishes where the second function vanishes identically nearby. -/
lemma gradInnerInverse_eq_zero_of_eventuallyEq_zero {i : ℕ} (u φ : Vec (n + 1) → ℝ)
    {y : Vec (n + 1)} (hφ : φ =ᶠ[𝓝 y] 0) :
```
Yes: `(u φ : ...)` both explicit. So my call `... 1 (fun z' => χ ...) (fun z' => ...) ?_` should be `i := 1, u := ..., φ := ..., hφ := ?_` — but the error says the *third* arg (the φ I passed) was expected to have type `(fun z' => χ ...) =ᶠ[𝓝 ?] 0`, meaning the second explicit arg after `1` was `u` and the third `φ`... no wait: the error says "The argument `fun z' => ...` has type `Vec 1 → ℝ` but is expected to have type `(fun z' => χ (A2.chart 1 z')) =ᶠ[𝓝 ?m.1130] 0`". So the lemma's signature actually has `φ` *implicit* and `hφ` as the third explicit argument! I.e. `lemma ... {i} (u) {φ} {y} (hφ : φ =ᶠ 0)`. The sed output I read was from a different lemma maybe (line 112 vs the actual). Actually the error message clearly indicates hφ position. Let me just use named arguments: `(A := A2) (i := 1) (u := fun z' => χ (A2.chart 1 z')) (φ := fun z' => ...) (hφ := eventually_of_forall ...)`. Named args will resolve.

Also need the `y` argument: implicit, inferred from goal.

4. Line 403 "No goals to be solved" — cascade from 402.

5. Line 422: the `change` in the sum bullet fails: target is `∑ i, ψ i x = 1` and my pattern `ψb x + (fun _ => 0) x = 1` isn't defeq because the sum over Fin 2 isn't definitionally `ψ 0 x + ψ 1 x` (needs `Fin.sum_univ_two`). So do `rw [Fin.sum_univ_two]` first, then `change ψb x + ... = 1`, or simply `rw [Fin.sum_univ_two, hψ0, hψ1]` then `simp [hψbone x hx]`. Earlier the rw failed due to `ψ ((fun i => i) ⟨0,...⟩)`. Let me use `simp only [Fin.sum_univ_two]` then `change ψb x + (fun _ : Vec 1 => (0:ℝ)) x = 1; rw [hψbone x hx]; norm_num`. After `Fin.sum_univ_two`, the goal is `ψ 0 x + ψ 1 x = 1`; `change ψb x + (fun _ => 0) x = 1` uses defeq of ψ 0 with ψb (let-def unfolding + if-reduction). Should work.

Let me also fix the other `change` bullets if they fail similarly (the log only complains about 422; others may have passed or the log is truncated). Let me apply fixes and recompile.

Also `#check @integrable_zero` to get the signature right.
dsh: reasoning:
`integrable_zero α ε' μ` — so `integrable_zero (Vec 1) ℝ volume`? But `α` is explicit and can be inferred from μ. Using `(integrable_zero (μ := volume))` works (as the scratch shows). Let me use named argument `(integrable_zero (μ := volume))` and `(integrable_zero (μ := μ2))`.

Now apply the remaining fixes:
1. congr endings: replace `simp` with `simp only [one_smul, Pi.zero_apply, neg_zero, Real.exp_zero, one_mul]; ring`.
2. hintL1: `(integrable_zero volume)` → `(integrable_zero (μ := volume))`.
3. hintR1: `(integrable_zero μ2)` → `(integrable_zero (μ := μ2))`.
4. hpair1: use named args for the lemma.
5. sum bullet: `rw [Fin.sum_univ_two]` then `change`.

Let me apply.
dsh: reasoning:
While that compiles, let me start writing the ledger JSON builder script. I'll create `longrun/results/` directory and a script `tmp/build_ledger.py`. Actually simpler: write the JSON directly with Python inline once the probe succeeds. Let me draft the content in a file now so I can finalize quickly.

Let me create the results directory and draft the ledger. I'll write it after I know the probe result, but I can prepare the text.

Time: ~3h elapsed. ~1h left. I need to hurry.

Plan for the remaining time:
1. compile7 result → fix if needed (aim: ≤ 2 more iterations, ~20 min).
2. Add htrans-redundancy probe (optional, small). Maybe skip if time is tight — I can state it as a mathematical observation with the derivation inline in the report (it's a short argument, but not kernel-checked). Better to kernel-check it if cheap: it's a 6-line theorem. I'll add it in the same file after the compile succeeds, in the final compile.
3. Write ledger JSON + MD (~25 min).
4. Final compile with everything, capture logs, hashes.
5. Checkpoint + TASK_DONE/TASK_BLOCKED.

Let me draft the JSON structure now in the results dir as a builder script.

Actually, to save time, I'll write the JSON directly using Python with a dict, embedding the probe evidence. Let me prepare it in a file `tmp/make_ledger.py` and run once.

Let me think about the exact "verdict" strings and defect list.

Findings (defects / discrepancies):
D1 (positive finding, not a defect): `htrans` in H1 is derivable from `SmoothOverlapAtlas.inj_chart` + `transition_chart_global`; it is a redundant hypothesis. Verified in probe (if I add it).
D2 (minor documentation inaccuracy): card §5 row U7-GLOBAL-INTEGRABILITY says integrability transfer is "consumed by hintL/hintR of halfSpaceAtlas_weightedIBP_unconditional and of globalWeightedIBP_of_cover_partial_ae" — these theorems have no hintL/hintR hypotheses; integrability is constructed internally (local `hintL`/`hintR` in the proofs, POUConstruction.lean:115-236, PartialChartModelPOU.lean:~440-465). Not a mathematical overclaim; wording is loose.
D3 (documentation gap): the card's `expanded_hypotheses` does not have an explicit entry for `dilationAtlasTwo_weightedIBP_via_pou` (H5); it is a conditional POU+integrability theorem. The card classifies the `globalWeightedIBP_of_pou*` family as conditional interface in `semantic_class` and H5's own docstring is honest, so this is an omission rather than an overclaim.
D4 (consistency check passed): `ManifoldAtlasData.integral_decomp` etc. are disclosed as interface fields; `Blocked.lean` names `B-D13-MANIFOLD-GLUING` as an interface field, but the card's `remaining_blockers` list does not include that name (it appears only in Blocked.lean). Minor.
D5 (verified accurate): the claim that ChartMetric.smooth is `ContDiff ℝ ⊤` = analytic (ω) and SmoothChartMetric is the C^∞ variant — verified (`top_eq_omega`).
D6 (verified): `halfSpaceAtlas_integrable_dirichlet` covers U=V; the Green identity with distinct U,V has no companion integrability theorem in-tree, but the identity's two sides are integrable by the same transfer argument (probe: H3 instantiation with distinct bumps). Minor documentation gap, not a defect in the theorem.
D7 (H1/H2 "no vacuity"): verified by kernel-checked instantiations with nonzero bumps.

Card claim support summary:
- expanded_hypotheses: supported for H1, H2, cover instances, green/dirichlet, integrable transfer, interface unchanged, no-conclusion-equivalence. Omission for H5. One loose phrase (hintL/hintR).
- exact_blockers_closed: all 9 rows checked for existence of the cited declarations and at least one checked consumer; supported, with the caveat that "closed" means "proved as conditional/model statements", which the card scopes (I4 for the Gaussian; U7 rows scoped to the model atlas/constructions). B-D13-MANIFOLD-GLUING remains an interface (disclosed in semantic_class and Blocked.lean).

Now let me write the JSON. I'll structure:

{
  "schema": "l4-d13-semantic-audit/1",
  "task_id": ...,
  "generated_utc": ...,
  "audit_scope": {...},
  "source_hashes": {...},
  "compile_evidence": {...},
  "axiom_evidence": {...},
  "non_vacuity_artifact": {...},
  "interface_fields_assumed": {...},
  "declarations": [ 8 entries ],
  "card_claim_audit": {"expanded_hypotheses": [...], "exact_blockers_closed": [...], "verdict": ...},
  "defects": [...],
  "classification_summary": {...},
  "not_claimed": [...]
}

Let me check compile7 first.
dsh: reasoning:
Hmm, some of my Python replacements didn't apply (the congr ending `simp` still there at line 340). Probably because the actual file text differs (e.g. the earlier replacement produced different content). Let me look at the current file around lines 330-410 and fix by direct editing.
dsh: reasoning:
Now I see the remaining errors:
- Line 340: `simp [Poincare.D13.ManifoldIBP.OverlapAtlas.density, hmetric0]` made no progress. The name `Poincare.D13.ManifoldIBP.OverlapAtlas.density` — maybe `density` is defined in the `OverlapAtlas` namespace, and `A2.density 0` is not reducible via that simp lemma because the equation lemma is `OverlapAtlas.density` with signature `A.density i y = (A.metric i).density y`? Hmm, `density` is a def so `simp [density]` should unfold. "made no progress" means simp couldn't simplify `A2.density 0 = G1.density` — maybe because `A2` is an abbrev for `dilationAtlasTwoSmooth 0 G1` whose `metric 0` is `G1` **definitionally** but the simp lemma `hmetric0 : A2.metric 0 = G1` exists; the issue is `A2.density 0` uses `density` from... wait, is `A2.density` even defined? `SmoothOverlapAtlas` extends `OverlapAtlas`, and `density` is defined in `namespace OverlapAtlas` as `def density (i) (y) := (A.metric i).density y`. Dot notation `A2.density` should work.

Hmm, "simp made no progress" means after unfolding density and rewriting hmetric0, the goal wasn't closed or changed? That can't be. Unless `Poincare.D13.ManifoldIBP.OverlapAtlas.density` is not the right constant and simp ignores unknown lemmas? If the name were unknown, we'd get an error, not "no progress". Wait — the error at line 340 is inside the `have hdens` proof: goal `A2.density 0 = G1.density`; simp with density + hmetric0 should close by rfl. "made no progress" suggests simp didn't even unfold.

Maybe `density` for SmoothOverlapAtlas is shadowed: `SmoothOverlapAtlas` might define its own `density`? Let me check: in GlobalMeasure.lean, `OverlapAtlas.density`. Hmm. Or maybe the issue is that `A2.density 0` elaborates using a *different* `density` (e.g., `ChartMetric.density` via coercion?) No.

Simplest fix: prove hdens via `rfl`-like approach:
```lean
have hdens : A2.density 0 = G1.density := by
  rw [show A2.density 0 = (A2.metric 0).density from rfl, hmetric0]
```
Or use `funext`:
```lean
have hdens : A2.density 0 = G1.density := by
  show (A2.metric 0).density = G1.density
  rw [hmetric0]
```
`show ... from rfl` may fail if density isn't definitionally equal to metric.density; but by definition it is. Let me use:
```lean
have hdens : A2.density 0 = G1.density := by
  change (A2.metric 0).density = G1.density
  rw [hmetric0]
```
`change` should work if defeq. If `A2.density` is `OverlapAtlas.density A2 0` and that def is `(A2.metric 0).density`, then defeq ✓.

Actually, maybe the problem is that `Poincare.D13.ManifoldIBP.OverlapAtlas.density` simp lemma isn't being used because `A2.density 0` unfolds to `(A2.metric 0).density` which then… hmm whatever. Use `change`.

- Line 343 "No goals to be solved": after `rw [hpiece y, hchart0, hdens]`, maybe the goal got closed by `simp only` and `ring` had no goals? Actually the error order: line 340 error (`simp` no progress) — that's the `have hdens` proof failing, so the subsequent `rw [..., hdens]` fails to find hdens? Then line 343 `ring` "no goals"? Confusing. Once hdens is fixed, likely fine.

- Line 375: same hdens issue in hintR0.
- Line 376: `rw [hpair y, hchart0, hdens]` fails to find `A2.chart 0` in the target (because hpair already rewrote?). The target shown earlier was `G1.gradInnerInverse χ (fun z => ψb z * χ z) y * G1.density y = Real.exp (-0) * G1.gradInnerInverse ... * A2.density 0 y` — so `A2.chart 0` was already gone (hpair y rewrote it). So `rw [hpair y, hchart0, hdens]` fails on hchart0. Remove hchart0 from that rw: `rw [hpair y, hdens]`.
- Line 393: `integrable_zero (μ := volume)` — "failed to synthesize instance of type class" (probably needs the codomain type ε'). Use `integrable_zero (Vec 1) ℝ volume`? Actually `integrable_zero α ε' μ` with α explicit: `integrable_zero (Vec 1) ℝ volume`. Hmm, but the goal's function is `fun y => exp(-0) * (Δχ y * pouPiece...(chart 0 y)) * A2.density 0 y` and I want to replace with zero. Let me avoid `integrable_zero` entirely: prove the goal by `Integrable.congr` with `integrable_const 0`? Or use `MeasureTheory.integrable_zero`? Hmm.

  Alternative: show the chart expression is *continuous with compact support* → integrable, using the same machinery as hintL0. That's more code.

  Or: prove `Integrable (fun _ : Vec 1 => (0:ℝ)) volume` via `integrable_zero (Vec 1) ℝ volume` and check. From the scratch: `integrable_zero : ∀ (α : Type) {m} (ε' : Type) [TopologicalSpace ε'] [ESeminormedAddMonoid ε'] (μ : Measure α), Integrable 0 μ`. So `integrable_zero (Vec 1) ℝ volume` should work (α := Vec 1, ε' := ℝ). The named-arg version `(μ := volume)` failed to synthesize because α and ε' weren't determined. Let me use `(integrable_zero (Vec 1) ℝ volume)`.
  
  Then `.congr` with the pointwise equality. Since the target is `Integrable (fun y => ...) volume` and `integrable_zero (Vec 1) ℝ volume : Integrable (fun _ => 0) volume` — the congr proof: `filter_upwards with y; rw [hpiece1 y]; simp` gives `0 = exp(-0) * (Δχ y * 0) * A2.density 0 y` → `simp` closes. But is the LHS of the a.e. equality `0` (the zero function) and RHS the target function? `Integrable.congr h h'` where h : Integrable f, h' : f =ᵐ g, goal Integrable g. `filter_upwards with y` starts from `f y = g y` = `0 = ...`. Then `rw [hpiece1 y]` rewrites the RHS's pouPiece to 0 → `0 = exp(-0) * (Δχ y * 0) * A2.density 0 y`; `simp` closes. ✓
  
- Line 405: `eventually_of_forall` unknown — it's in `Filter` namespace: `Filter.eventually_of_forall`. I opened `Filter`, so it should be available... The error says unknown identifier. Maybe the name is `Filter.EventuallyEq.refl`? Hmm, `eventually_of_forall` should exist in mathlib (Filter.eventually_of_forall : (∀ x, p x) → ∀ᶠ x, p x). It might require `open Filter` — I have `open MeasureTheory Set Filter Metric Function`. Hmm, but the error is at line 405. Maybe the identifier got shadowed by `SmoothOverlapAtlas`... Let me use `Filter.eventually_of_forall`.

Let me fix these:
1. hdens both places → `change (A2.metric 0).density = G1.density; rw [hmetric0]`.
2. hintR0 rw → `rw [hpair y, hdens]`.
3. integrable_zero → `(integrable_zero (Vec 1) ℝ volume)`.
4. eventually_of_forall → `Filter.eventually_of_forall`.

Also the `simp` at line 396 after `rw [hpiece1 y]` — fine.

Let me apply.
dsh: reasoning:
While waiting, let me write the ledger JSON builder script. Time: ~3h15m. Let me prepare the file content now.

I'll write `tmp/make_ledger.py` that emits the JSON. I'll include the final probe result as a parameter (success). Let me write it now, and adjust after the compile.

Actually, let me first write the ledger content in a Python script with all the details. This will take a while but is the main deliverable. Let me draft it carefully.

Also need the final `#print axioms` outputs. Let me capture them from the compile8 log: the log contains #check and #print axioms outputs. I'll extract the relevant lines for the ledger evidence.

Let me write the JSON generator with a placeholder for compile status, then run it after the compile.
dsh: reasoning:
Almost there! Only 3 errors:
1. Line 344 & 380: "No goals to be solved" — the `ring` after `simp only [...]` closed the goal already (because simp did everything). So remove the `ring` lines.
2. `Filter.eventually_of_forall` unknown → the correct name is `Filter.Eventually.of_forall` or `eventually_of_forall` in `Filter`? In mathlib it's `Filter.Eventually.of_forall : (∀ x, p x) → ∀ᶠ x, p x`. Let me use `Filter.Eventually.of_forall`.

Let me fix and recompile.
dsh: reasoning:
While waiting, let me prepare the markdown report. I'll write it after the compile, but I can draft it now.

The markdown report structure:
# L4-child-d13-semantic-audit — independent semantic audit of the D13 manifold-IBP/entropy headline layer
- Scope & method
- Environment & artifacts (hashes, commands, exits)
- Per-headline verdicts (table + details)
- Adversarial findings
- Interface fields actually assumed
- Card claims assessment
- Classification
- Not claimed
- TASK_DONE or TASK_BLOCKED

Let me draft it in the results dir after compile.

Let me check the compile.
dsh: reasoning:
The audit probe compiles cleanly (EXIT=0) with all 8 headline axiom cones = [propext, Classical.choice, Quot.sound], and the non-vacuity instantiations + the htrans-redundancy theorem. 

Now:
1. Extract the full statement capture (#check outputs) into evidence files.
2. Run the ledger builder.
3. Write the markdown report.
4. Update checkpoint.
5. Final checks: grep the probe for forbidden tokens; record the probe hash; verify exit codes.

Let me first extract key evidence: 
- The #check outputs (first ~200 lines) and interface prints, saved as `release/audit_build/evidence/statements.txt`.
- The axiom lines: `release/audit_build/evidence/axioms.txt`.
- The non-vacuity theorem checks: I could add `#print axioms` for the probe's own non-vacuity theorems! That's important: the probe theorems themselves must be axiom-clean (no sorryAx). The compile is the proof, but let me add `#print axioms` lines for h1_nonvacuous, h2_nonvacuous, h3_nonvacuous, h4_nonvacuous, h5_nonvacuous, h6_nonvacuous, h7_bridge_instantiated, h8_monotoneOn_instantiated, htrans_derivable, and top_eq_omega/smooth_lt_analytic. That's a final compile (quick, ~1-2 min) and gives strong evidence. Let me add those #print axioms lines and recompile as compile10 (final).

Let me do that now.
dsh: reasoning:
While the final compile runs, let me write the markdown report. I'll base it on the ledger JSON content. Let me write it now.

Also I need to check that my `make_ledger.py` reads `compile8.log` — I should point it to `compile_final.log`. Let me update that. And the probe hash will change after appending the axiom lines, so the ledger must be generated *after* the final compile. Fine.

Let me write the markdown report now (with the final numbers), then run the ledger builder, then finalize.

Let me check the compile result first to make sure everything is fine, then write both.
dsh: reasoning:
The final compile exited 0 with 0 errors; the wrapper's exit code 1 is from `grep -c error` returning 1 (no matches) — that's fine.

Now let me:
1. Extract evidence files from the final log.
2. Update make_ledger.py to use compile_final.log and also include the probe axiom cones + htrans theorem.
3. Run ledger builder.
4. Write markdown report.
5. Update checkpoint.json.
6. Final verification: forbidden tokens in probe, source hashes.

Let me do step 1-2.
dsh: reasoning:
All probe theorems are axiom-clean. Note `top_eq_omega` doesn't appear in the summary because it's a `rfl` proof whose axioms print as "does not depend on any axioms". Let me check the log for that. Also, let me verify there's no `sorryAx` anywhere in the probe's output.

Now update make_ledger.py: LOG → compile_final.log, probe sha (computed at runtime — fine), and add the probe axiom evidence and the htrans finding. Let me patch it and run.

Also, the `axioms_from_log` regex: it searches for `'<name>' depends on axioms: [...]` with re.S — the log has line breaks within the bracket. My regex `\[(.*?)\]` with re.S should capture. For `top_eq_omega` the output is probably "does not depend on any axioms" → returns None. That's fine; I'll note it.

Let me update the ledger script:
- LOG path
- name H1 hypothesis htrans note: now verified
- add "probe_theorems" evidence
- card audit finding #1 now kernel-checked.

Then run it.

Also add to D["audit_probe"]: "probe_axiom_cones" list.

Let me patch with sed.
dsh: reasoning:
The ledger is written. Let me verify the JSON is valid and inspect the key sections. Then write the markdown report.

Time check: ~3h40m. ~20 min left. Need to be quick.

Let me write the markdown report now.
dsh: reasoning:
Now final verifications:
1. Forbidden token scan of my probe.
2. Verify the probe hash in the ledger matches (I appended lines after generating? No — I appended the axiom lines BEFORE running make_ledger.py, so the hash is current. Let me verify: make_ledger.py computed sha256(PROBE) at run time after the final compile; yes, run after appending. Good.)
3. Update checkpoint.json with the final state.
4. Verify the markdown ends with exactly TASK_DONE.
5. Double-check the ledger's source hashes match the current leader files, and that the probe hash matches.

Let me also double check that `Blocked.lean` hash is in the ledger JSON (SRC dict includes it, yes).

Let me run the verification.
dsh: reasoning:
The probe's forbidden-token grep found only the docstring line — acceptable. The markdown ends with `TASK_DONE` exactly on the last line. The JSON is written. The probe hash matches.

Wait — one important detail: my markdown has "TASK_DONE" as the last line but the requirement says "End the markdown card with exactly TASK_DONE only for the intended acceptance milestone". ✓.

Now update checkpoint.json with the final state, and do a final review of the ledger for correctness (e.g., the H1 statement's mention of "nterface" typo in the card audit — let me fix that typo: "operator identifications/nterface fields" → "operator identifications/interface fields"). Also check the JSON for that typo.

Also I should double check one claim in the markdown: "the intentional axiom negativeControl recorded by the D13 card" — the card says "exactly 1 hard token: the intentional axiom negativeControl" in the compile evidence. But my grep over Poincare/D13 returned only docstrings; the negativeControl axiom must be elsewhere (maybe in Audit.lean? The card says 47 D13 files, one hard token). Let me verify quickly: grep for `axiom` with word boundary in Poincare/D13. My earlier grep showed only docstrings, with `grep -v "^.*--"` filter. Let me re-run a stricter grep to confirm whether negativeControl exists in the D13 tree. If not, I should adjust the wording. Let me check.

Also the ledger JSON might have a typo: "identifications/nterface". Let me fix in both files.

Let me check negativeControl.
dsh: reasoning:
Confirmed: `axiom negativeControl : False` in D13/Audit.lean:44 (never used by any theorem) and `negControlBadAxiom` in CriticalPathReview/NegControl.lean (also a negative control). My markdown statement is accurate.

Now let me fix the typo in the ledger JSON and the markdown, add the final checkpoint, and do a last sanity check that the ledger's `card_claim_audit` doesn't misstate anything.

Fix: "operator identifications/nterface fields" → "operator identifications/interface fields".

Then update checkpoint.json.

Time: ~3h50m. Let me finish quickly.
dsh: reasoning:
Everything is in place. Let me do a final review of the deliverables and one last check that no source files in other worktrees were modified (I only wrote in my worktree: release/audit_src, release/audit_build, longrun/results, checkpoint.json, tmp/make_ledger.py). The leader's oleans were only read (LEAN_PATH). Let me verify by checking that the leader's source files' mtimes/hashes are unchanged — I never wrote there. My only writes were in my own worktree.

Let me also double check the `.olean` I wrote for POUConstruction in audit_build (fine, worktree-local).

One more check: the `longrun/queue.updated.json` etc. untouched.

Let me do a final quick listing and confirm the summary to the user. Also — time budget: I'm at ~3h55m. Let me wrap up with a concise final message.

Actually, one last important verification: the task says "Verify non-vacuity by exhibiting at least one kernel-checked instantiation per headline (the D13 model atlases)." I have h1..h8_nonvacuous, all compiled and axiom-clean. ✓

"Record source hashes, exact compile commands and exits, #print axioms" ✓ (ledger + evidence files).

"classify each result as proved, conditional, model, statement-only or upstream source claim" ✓.

"Report honestly whether the D13 card's 'expanded hypotheses' and 'blockers closed' claims are supported." ✓ (section 4).

Deliverables at longrun/results/*.md and *.json ✓; markdown ends with exactly TASK_DONE ✓.

Let me give the final summary.
The audit is complete. Final state:

## Deliverables (in this worktree)

| artifact | content |
|---|---|
| `longrun/results/L4-child-d13-semantic-audit.md` | short report, per-headline verdicts, card-claim assessment, ends with exactly `TASK_DONE` |
| `longrun/results/L4-child-d13-semantic-audit.json` | 43 KB ledger: 8 declarations with full hypothesis lists (domains/quantifiers), interface fields, verdicts, defects F1–F5, source sha256, compile commands/exits, axiom cones |
| `release/audit_src/Poincare/L4/D13SemanticAudit.lean` | independent probe (statement capture + `#print axioms` + 8 kernel-checked non-vacuity instantiations + `htrans_derivable`) — compiled from source, **EXIT=0**, axiom-clean |
| `release/audit_build/{compile_final.log,evidence/*}` | raw compile evidence |
| `checkpoint.json` | hourly checkpoints (2 written, final = complete) |

## Key results

- **All 8 headlines are kernel-clean**: `[propext, Classical.choice, Quot.sound]`, no `sorryAx`, no forbidden tokens in any D13 source (only docstring mentions; the two intentional `axiom` negative controls are unused).
- **Non-vacuity verified per headline** on the D13 model atlases: H1 half-space atlas (direct application with all 12 hypothesis groups discharged), H2–H4 non-zero bumps, H5 with a genuinely constructed compactly supported POU and all four integrability obligations discharged, H6 via `disjointAtlas_weightedIBP`, H7/H8 at τ₀=1, t₁=1/2 with concrete values `F(0)=1, F(1/4)=4/3, F(1/2)=2`.
- **No conclusion-equivalent, contradictory, vacuous or wrong-domain hypothesis; no model-vs-manifold overclaim.** The `(0,T)` vs `(0,T]` split in the heat bridge is correct (derivatives on `Ioo 0 t₁`, regularity/monotonicity on `Icc 0 t₁`); atlas supports are strict topological supports inside open sources.
- **Hidden interface fields are real and disclosed**: `ManifoldAtlasData.integral_decomp`, `weighted_laplacian_compat`, `grad_inner_compat` (H6, assumed — `Blocked.lean` names `B-D13-MANIFOLD-GLUING` explicitly). H5 is a conditional interface (supplied POU + integrability), not unconditional.
- **Card assessment**: `expanded hypotheses` and `blockers closed` are substantially supported. Findings are documentation-level only: **F1** `htrans` is redundant (kernel-checked derivation), **F2** no dedicated expanded-hypotheses entry for H5, **F3** loose `hintL`/`hintR` wording in the U7-GLOBAL-INTEGRABILITY row, **F4** the Green-identity integrability companion covers only `U=V`, **F5** `B-D13-MANIFOLD-GLUING` absent from `remaining_blockers`.

`TASK_DONE` requests independent acceptance of this audit only; it is not a Poincaré proof and no D13 source was modified.
