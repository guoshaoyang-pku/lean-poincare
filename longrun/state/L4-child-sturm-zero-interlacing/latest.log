dsh: reasoning:
The task appears to be already complete with a long history of acceptance passes (up to cp22, pass-5). Let me look at the current state more carefully. The checkpoint says "complete" with all gates PASS. But the invocation says "Continue from existing files and checkpoint.json in this worktree; do not restart completed work." and "Model: use configured host model only; do not invoke until quota recovery is recorded." Hmm, there's a note about quota recovery. Let me check the result card and see the state.

Let me look at the README, the result file, and see if there's any remaining work. The current date appears to be Sep 12. Let me check the timestamps.

Let me read the results file first (the only file that can complete this task), plus check for pending items.
dsh: reasoning:
The task appears complete through pass-5 (cp22) very recently (11:18 today, which is ~11:19 now). This looks like the work has already been done by previous invocations. The current invocation asks to "Continue from existing files and checkpoint.json in this worktree; do not restart completed work. This invocation is limited to four hours."

Note the runtime context line: "Model: use configured host model only; do not invoke until quota recovery is recorded." Hmm, that's from the task metadata. Possibly there's a quota issue.

Let me check the queue to see what the current instruction is — maybe there's a new invocation request. Let me check queue.updated.json and any task notes.

Also I should verify the current state is genuinely complete and consistent: run the gates, check the result card. Since the work is claimed complete with pass-5, my role may be to do a fresh independent verification pass and finalize. Let me read the result card (the only file that can complete the task) and check the state.

Let me first look at the queue and understand the acceptance criteria vs. what's delivered. Key acceptance points:
1. instantiate engine with k1=1 (u1=sin) and k2=0 (u2=t) on (0,pi) to obtain first positive zero of sin < pi, OR the sharper two-curvature interlacing for k2 <= k1.

Wait — the first branch is mathematically impossible: sin has no zero in (0,pi); its first positive zero is pi, not < pi. Hmm, the acceptance says "obtain that the first positive zero of sin is < pi" — that's false. Unless they mean comparing sin (k1=1) with t (k2=0) and deriving that the zero of sin comes before... no. Sturm comparison: if u1'' + k1 u1 = 0, u2'' + k2 u2 = 0 with k2 <= k1, then zeros of u1 interlace... Actually the standard Sturm comparison theorem: if k2 <= k1 and u2 has two consecutive zeros a<b, then u1 has a zero in (a,b). Also if u1 has two consecutive zeros a<b, then u2 has at most one zero in (a,b)... Anyway, with k1=1 (sin) and k2=0 (t): zeros of t are just at 0 (and t(0)=0). u2 = t has no positive zero in (0,pi). Sturm says between consecutive zeros of u2 there's a zero of u1 — vacuous since u2 has no two consecutive positive zeros. So the natural conclusion is that sin's first positive zero is < the second zero of t, which doesn't exist. The previous work notes say "acceptance branch (1) k2=0,u2=t on (0,pi) is refuted (sin has no zero in (0,pi)); replaced by the two-curvature interlacing k2=1/2". Hmm, but the checkpoint cp11 notes mention `sin_zero_linear_zeroFree_interlacing (degenerate k2=0 statement)`.

Hmm wait. Let me think again: maybe there's a *sharper* statement: if k2 <= k1 with k2 not identically... Actually the correct sharp statement: u1 (larger curvature) zero occurs before u2's zero: for u1'' + k1 u1 = 0 with u1(0)=0, u1'(0)=1, and u2'' + k2 u2=0 with u2(0)=0, u2'(0)=1, if k2 <= k1 then first positive zero of u1 <= first positive zero of u2 (when the latter exists). With k2=0, u2 = t has no positive zero — so this is again vacuous/trivial in the sense that no bound obtained. Hmm. But if we take k1=1 and k2=0... The proposed statement "first positive zero of sin is < pi": pi is not the zero of t; it's where t... no.

Actually maybe they intend u2 = t on (0, pi) meaning the "comparison solution" is t, and the conclusion would be "the first positive zero of sin is less than the first positive zero of t" — but t has no positive zero, so vacuous. So the branch is refuted, and the stronger option "sharper two-curvature interlacing for k2 <= k1" applies. The previous work used k2 = 1/2 (sin(t/sqrt2) has first zero sqrt2*pi > pi). Good.

So the deliverable already exists. My job in this new invocation: since the checkpoint says complete, I need to do independent acceptance again (pass-6?) or verify that everything is still valid, then confirm TASK_DONE. But wait — the task says "Only your own longrun/results/L4-child-sturm-zero-interlacing.md can complete this task." That file exists and was regenerated at 11:18. Let me read it to see its content and whether it ends with TASK_DONE.

Let me read the result card. It's 57KB — large. Let me read key sections.

Also let me check the queue file to see whether there's a new task statement waiting.
dsh: reasoning:
The task is already marked TASK_DONE through pass-5. This invocation (a new one) wants me to continue/inspect. The prudent action: perform a fresh independent acceptance pass (pass-6) to verify the frozen state has not changed, re-run gates, verify hashes, and regenerate/append a section 8g documenting this invocation's verification. Then end with TASK_DONE.

But first, let me confirm the state actually is intact: run the gates script, check hashes, verify the statements. Also note the task instruction "Model: use configured host model only; do not invoke until quota recovery is recorded." Hmm this likely refers to not spawning subagents with other models. I'll do the work directly.

Let me read the result card's key sections to understand what was delivered, and read the main Lean module to verify the theorem statements. Also check the gate runner.

Let me look at §3, §4, §6, §10 and the Lean signatures.
dsh: reasoning:
The task is complete. This new invocation is likely a continuation/re-verification. The current runtime notes "Model: use configured host model only; do not invoke until quota recovery is recorded." Hmm — maybe the previous invocation had hit a quota limit, and this is a resumed invocation. But the checkpoint shows pass-5 complete at 03:18 UTC = 11:18 local, and it's 11:19 local now — just a minute later. So this is likely the same session continuation, and the harness restarted me. 

Wait — actually the checkpoint file mtime is 11:18 and now it's 11:19:39. The previous pass-5 was just completed. This new invocation might be a "continue" invocation triggered by the longrun driver. Hmm. But there's nothing left? Let me check the queue's task entry for my task id and any status/pending instructions.

Let me grep the queue for L4-child-sturm-zero-interlacing.
dsh: reasoning:
The task is complete, all gates pass, and the card ends with TASK_DONE. This new invocation is likely a "continuation invocation" — the sixth pass. The prior pass-5 was at 11:17-11:18 local; current time 11:19. Hmm, wait: maybe the previous invocation hit its quota/time limit right after finishing pass-5, and now I'm invoked again. The proper action per the task instructions: "Continue from existing files and checkpoint.json in this worktree; do not restart completed work."

Given the deliverable is complete and verified 5 times, the highest-value action now is:
1. Re-verify the frozen state (hashes match checkpoint, gates pass) — quick.
2. Run a genuinely fresh independent acceptance pass (pass-6) with a NEW probe that exercises the key theorems and audits axioms, to serve as this invocation's independent acceptance.
3. Append §8g to the card, write checkpoint cp23, and end with TASK_DONE.

But is that useful? There's a risk of "acceptance theater" — repeatedly re-verifying. However the task says "this is independent acceptance" and each invocation is expected to independently accept. The requirement "Save a compile-checked checkpoint at least every hour" and "Only your own longrun/results/L4-child-sturm-zero-interlacing.md can complete this task" — the file exists; my invocation should ensure it reflects the current state and hash-freshness.

Let me first check whether the state is genuinely consistent right now: run final_integrity_check.py and the gates. Also, verify the input files are byte-identical (read-only inputs untouched). Then decide.

Actually, wait. Let me consider the possibility that the "current invocation" is the same one that produced pass-5 — i.e., the conversation was reset/forked and I'm asked to continue. The runtime context says "Current runtime context. This snapshot supersedes earlier runtime-context snapshots." and the task text includes "Continue from existing files and checkpoint.json in this worktree; do not restart completed work. This invocation is limited to four hours."

Given all gates passed and the card is frozen, the correct completion is to do a light fresh verification and report TASK_DONE with this invocation's acceptance recorded. Let me be careful not to invalidate hashes unnecessarily: if I regenerate the card, the card hash changes, requiring checkpoint update. That's fine, it's the established workflow (cp per pass).

Let me check how much time budget: 4 hours. Plenty. But no need to overdo.

Plan:
1. Verify environment (elan/lake available), run `python3 tools/final_integrity_check.py` and `python3 tools/run_sturm_gates.py` fresh.
2. Verify deliverable hashes match checkpoint cp22.
3. Write a fresh probe (pass-6) that:
   - imports the release modules,
   - re-derives key top-level corollaries via a distinct route (e.g., instantiate the alternating statement at a specific n, use firstPositiveZero bounds, check the engine hypotheses are genuinely satisfied for the constructed data),
   - uses `#print axioms` on all deliverable declarations in a fail-closed way,
   - checks no `sorry`, no forbidden tokens,
   - Maybe a genuinely independent check: verify that the theorem statements are not vacuous by evaluating witnesses, and verify the engine hypothesis discharge is via constructed data (proof-term provenance: `#print sturm_zero_comparison` usage? Actually proof-term provenance means checking that the final theorem's proof term actually depends on `sturm_zero_comparison`, via `#print` output containing the name, similar to pass-5's check).
4. Run and collect evidence; append §8g; regenerate card; write checkpoint cp23.

Let me first read the remaining card sections 7, 8f to know what pass-5 did, so pass-6 can add something new rather than duplicate. Let me read lines 210-608.
dsh: reasoning:
The work is extremely thorough. This is pass-6 territory. The most useful thing I can do that is *new* and not theater:

1. Verify the tree's frozen state (hashes, gates) — quick re-run.
2. Add a *genuinely different* independent check that earlier passes haven't done. Ideas:
   - An **adversarial/mutation test**: verify that the axiom audit and gates would actually fail if a violation were introduced (fail-closed sensitivity testing). E.g., create a temp copy of the audit module with a declaration using an extra axiom, verify the audit detects it. Pass-5 checked proof-term provenance and cones; a mutation test of the fail-closed auditor would validate the *gates themselves* (meta-level soundness), which is valuable for independent acceptance.
   - Check the semantic content more deeply: e.g., verify the literal acceptance branch (1) is indeed impossible by machine-checking `∀ b > 0, ¬ (fun t => t) b = 0` and the engine's requirement, plus verify the "two-curvature interlacing" is genuinely non-vacuous with a numeric witness computed at high precision (floating point cross-check vs Lean's `norm_num`-free reasoning). E.g. verify π < √2·π by interval arithmetic in Python independently (trivial though).
   - Verify that the statement of a key theorem is not weakened relative to the acceptance wording: parse signatures from evidence/signatures.txt and compare to the acceptance requirements. Pass-2/3/4 did signature checks but maybe not an explicit requirements-to-signature mapping check.
   - Test that the delivered theorems have no unused-hypothesis weakening: e.g., instantiate `firstPositiveZero_le_pi_sqrt` with K and k where the hypothesis is satisfied.

A mutation test of the fail-closed axiom auditor is genuinely valuable and new: it demonstrates the audit isn't vacuous. Also a "negative control" run confirming that removing a hypothesis breaks the theorem isn't possible without editing the module (and we must not edit deliverable sources — the hashes are frozen). But we can create a temp file importing the deliverable and state a *wrong* variant (e.g., drop the curvature hypothesis from the zero-counting corollary: claim a zero in (0,π/√K] for arbitrary Jacobi data with u 0 = 0) and show it is not provable / can be refuted by a concrete counterexample (u = t with k=0, K=1: first positive zero of t is... t has no positive zero; firstPositiveZero t = sInf ∅ = 0 ≤ π — hmm that's ≤. Better counterexample: a Jacobi solution with k < K that is zero-free on (0, π/√K], e.g. u = sin(t/2) with k = 1/4, K=1: sin(t/2) has first positive zero at 2π > π. So the zero statement fails without k ≥ K. That's the "necessity of curvature hypothesis" — pass-2 probe already did `probe2_zero_counting_needs_curvature_bound`. So not new.

New idea: a **counterexample-based refutation of the literal branch** is already done.

Another new idea: verify the *cross-check* two-sidedness numerically: the deliverable says `conjugate_point_bound_via_engine` derives the leader's conclusion, and `conjugate_point_bound_cross_check` invokes the leader theorem. Pass-3 checked the leader's complementary files compile. Pass-4 checked main module doesn't import ConjugatePointBound. Hmm.

A genuinely new and relevant check: **detect whether any delivered theorem's proof uses `Classical.choice`/`propext` unnecessarily** — not required.

I think the mutation/sensitivity test of the *fail-closed gates* is the strongest new contribution: it validates that the accept/reject machinery actually rejects a poisoned artifact. Steps:
- Copy the audit module to a temp file, add `axiom poisoned : False` and a theorem `poisoned_thm : False := poisoned`, and confirm the audit driver flags it (out-of-cone / forbidden token). But the audit module is generated with a fixed list of declarations? Let me check `tools/run_sturm_gates.py` and the audit module to see how it audits.

Let me read the audit module and gate driver to understand the mechanism, and then design the mutation test.

Also I should check whether there are "queue" preservation constraints: don't modify queue/manifest/input.

Let me also review logs/ directory listing and evidence/acceptance-pass5.json to know what pass-5 recorded, and check that checkpoint.json cp22 is the latest.

Plan is:
1. Read the audit module + gate driver.
2. Run a full fresh re-verification: gates + final integrity (these are cheap ~30-60s).
3. Write pass-6 driver `tools/acceptance_pass6.py` that does:
   a. checkpoint/card hash match,
   b. input byte-identity,
   c. closure rebuild,
   d. fresh probe (new data points) with fail-closed cone audit,
   e. **gate-sensitivity mutation test**: take a copy of the audit module + a poisoned lemma, run the audit logic on the poisoned file, assert the tooling rejects it (both the axiom cone and forbidden-token scan), and confirm the pristine file passes. This proves the acceptance machinery is fail-closed and not vacuous.
   f. preservation check of shared queue/manifest/input files.
4. Append §8g to card via make_result_card.py (need to read the script's structure to add section).
5. Write checkpoint cp23 and update `current`.
6. Final integrity check, then report TASK_DONE.

That's a solid, honest pass-6. Time budget is large. Let me inspect the gate driver and audit module first.
dsh: reasoning:
Now I understand the gates. The axiom audit mechanism: parse `#print axioms` output, require all expected declarations present, no extra/missing, cones subset of allowed.

**Mutation test design (pass-6, new)**:
Create `tmp/audit_mutation/` with:
1. A poisoned audit module `tmp/mutation_poisoned_audit.lean` that:
   - imports the deliverable,
   - defines `axiom mutation_poison : False` (a new axiom) and `theorem mutation_poison_thm : False := mutation_poison`,
   - `#print axioms` for a couple of pristine declarations AND the poisoned one.
   Then run the same parsing logic from `run_sturm_gates.py` (import it as a module) on the output and assert:
   - the poisoned cone contains `mutation_poison` (not in ALLOWED_AXIOMS) → the auditor's violation check fires.
   - Also test the `missing` branch: if a `#print axioms` line is deleted, the expected list from a source still lists it → missing fires.
   - Also test the forbidden-token scan on a poisoned source containing `sorry` → the scan reports hard matches > 0.
   
   This is testing the *detector*, which is legitimate meta-level acceptance work: it demonstrates the fail-closed claim in §8 has teeth.

2. Also a **statement-level mutation** check (semantic sensitivity): take the acceptance claims and check that they're not restatements:
   - `firstPositiveZero_sin_lt_half_model` is strictly stronger than an equality; e.g., verify that the *opposite* inequality is refutable? We can prove `¬ (√2*π ≤ π)` in the probe — actually that's auto from the theorem. Hmm.
   - Better: verify the engine's conclusion is not obtainable without the engine's hypotheses, already done in pass-2.

Perhaps an even better new semantic check: **kernel-level statement identity check**. Use `#check` / `#print` to confirm that the *statements* of key deliverable theorems are propositionally distinct from the engine's conclusion (i.e., not a restatement). E.g., `exists_zero_of_curvature_lt` has conclusion `∃ c ∈ Ioo a b, u1 c = 0` whereas `sturm_zero_comparison`'s conclusion might be a disjunction `(∃ c, ...) ∨ (∀ t ∈ Ioo, k1 t = k2 t)`. We can extract the types and check they're not syntactically/e Definitionally equal. Pass-5 checked proof-term provenance (proof mentions the engine). A new check: parse `#print <thm>` output for the *type* and compare with the engine's type, requiring they differ (non-restatement) — plus confirm the deliverable statement is *strictly stronger* in the sense that it does not contain the engine's alternative disjunct. Hmm, hard to automate soundly. We can at least check that the conclusion of the delivered theorems is not syntactically identical to any engine conclusion: compare normalized type strings.

Actually there's a cleaner, genuinely meaningful semantic check that I can machine-verify: **the delivered zero-counting theorem implies the literal acceptance branch's conclusion is false**, which was done. 

Another new test with real value: **`#reduce`/`decide`-free numeric sanity**: compute with high-precision floats that the claimed zeros are zeros: e.g. sin(pi)=0, jacobiSol K (pi/sqrt K) = sin(sqrt K * pi/sqrt K) = sin(pi) = 0. That's trivial.

I think the strongest new pass-6 content:
(A) **Gate sensitivity (mutation) testing** — poisoned source detection by both the axiom auditor and the forbidden-token scanner, and deletion-detection (missing declaration) in the audit parser. Proves the fail-closed machinery is not vacuous. This is new vs passes 1–5.
(B) A fresh probe with new data (to satisfy "fresh probe" per pass) — maybe use different curvature values not used before, e.g. (K,k)=(7,9), shifted interlacing at negative n, horizon H, Wronskian at 2π/3, and literal-branch checks. And a *new* kind: verify `firstPositiveZero_mem_of_normalized` attainment at an irrational-ish point; verify the `wronskian_deriv` with explicit data.
(C) Preservation checks: queue/manifest/input unmodified; deliverable hashes match cp22; inputs byte-identical.
(D) Full project closure rebuild + audit re-run.

Then append §8g to card and checkpoint cp23.

Let me look at how acceptance_pass5.py is structured so I can reuse its helpers (it's 18KB). Let me read it.
dsh: reasoning:
Good. Now let me design pass-6. I'll write `tools/acceptance_pass6.py` with checks:

1. `deliverable_hashes_match_checkpoint_cp22` — hash check against checkpoint cp22 (independent of card).
2. `inputs_byte_identical_direct_sha256` — 13/13.
3. `shared_queue_manifest_input_preserved` — mtimes predate cp1.
4. `closure_rebuild_exit0_jobs` and `closure_rebuild_left_sources_unchanged`.
5. `probe_compiles_clean` — fresh probe `tmp/acceptance_probe_r6.lean` with new data.
6. `probe_axiom_cones_reported_once`, `probe_axiom_cones_in_allowed_set`.
7. `probe_source_no_forbidden_tokens`.
8. `audit_rerun_all_in_cone` + `audit_covers_every_own_declaration`.
9. **NEW: `gate_sensitivity_axiom_audit_rejects_poison`** — the mutation test.
10. **NEW: `gate_sensitivity_forbidden_scan_rejects_sorry`** — poisoned source with `sorry` gets flagged by `input/d5-tools/scan_forbidden.py`.
11. **NEW: `gate_sensitivity_missing_declaration_detected`** — remove a `#print axioms` from a copy, run parser against the copy's expected list → missing non-empty.
12. **NEW: `statement_not_restatement_of_engine`** — check the delivered theorem types (from `#print` output or from signatures) differ from the engine's conclusion: verify `exists_zero_of_curvature_lt` type does not contain the engine's disjunction structure... Let's think about what's cleanly machine-checkable.

Actually a clean non-restatement check: the engine `sturm_zero_comparison` (I should read its exact statement) presumably concludes `(∃ c ∈ Ioo a b, u1 c = 0) ∨ (∀ t ∈ Ioo a b, k1 t = k2 t)`. The deliverable's `exists_zero_of_curvature_lt` concludes `∃ c ∈ Ioo a b, u1 c = 0` under the extra hypothesis of strict interior excess. So to check non-restatement, we can verify the delivered type is not syntactically equal to the engine's type, and more meaningfully: verify (in a probe) that the delivered theorem applies where the engine's conclusion alone would not give the zero without the alternative — i.e., demonstrate that the delivered theorem's conclusion is the unconditional zero statement. Machine check: `#check` the delivered theorem and confirm its type string contains `∃` and not `∨`... that's fragile but okay as a report artifact. Hmm.

Better: **kernel-level check that the deliverable's theorem is strictly stronger than the engine's**: prove in a probe that `exists_zero_of_curvature_lt`'s conclusion is NOT derivable from the engine hypotheses plus the engine's Disjunction in the case that the equality alternative fails? Hmm, actually the engine *does* conclude the disjunction; with strict excess the second disjunct is false, so the first holds — that's exactly what `exists_zero_of_curvature_lt` does. So the content is real.

Actually the strongest non-restatement evidence is pass-5's proof-term provenance plus the fact that the type differs. I could add a check: parse the `#print <decl>` type for the delivered theorem and the `#print <engine>` type, verify they are not syntactically identical (after whitespace normalization). Combined with proof-provenance, that shows: the proof uses the engine, and the statement isn't the engine's statement. That is a meaningful machine check for "not a restatement".

Let me do that: new check `statement_distinct_from_engine` using `#print` output in the probe (or in a separate lean run). The probe can print `#print Poincare.L4.GeodesicComparison.exists_zero_of_curvature_lt` and `#print Poincare.D12.ComparisonGeodesics.sturm_zero_comparison`; parse the elaborated types from the output and compare normalized strings. But `#print` prints the full proof term, not just the type. Better use `#check` output: `#check @foo` prints `foo : type` — that's types only. So probe includes `#check @...` for delivered and engine decls; parse lines; compare.

Actually more interesting: check that the delivered interlacing conclusion is *strictly stronger* by comparing the hypothesis count and the conclusion shape. Automated: count top-level forall binders? Fragile. Let me instead do:
- Extract the printed type of `Poincare.D12.ComparisonGeodesics.sturm_zero_comparison` and of `Poincare.L4.GeodesicComparison.exists_zero_of_curvature_lt` from `#check` output.
- Verify they are not equal (non-restatement), AND verify that the engine's type contains a disjunction `∨` while the delivered type does not (strictly stronger conclusion under extra hypothesis). Hmm, what if the engine's type doesn't contain ∨? Let me read the engine's signature first.

Let me read SturmComparison.lean's key theorems and the main deliverable's statements (at least the top part).

Also for the mutation test, I need to be careful: adding an `axiom` and `#print axioms` of it. The audit parser: `expected = re.findall(r"#print axioms (\S+)", audit_src)` from the *poisoned copy*; parsed cones include `mutation_poison` in cone of `mutation_poison_thm`. The check `bad = [a for a in cone if a not in ALLOWED]` — note: is `mutation_poison` reported in the cone? For `axiom mutation_poison : False`, `#print axioms mutation_poison_thm` would report `[mutation_poison]`. Yes. So the auditor flags it. Good — the test then asserts that the auditor's logic returns a violation, i.e., we're validating the detector.

Wait, but there's a subtlety: does the Lean `#print axioms` output for a declaration depending on a user axiom report the axiom name? Yes: "depends on axioms: [mutation_poison]".

Also test `Lean.ofReduceBool`/`trustCompiler`? Not needed.

For the forbidden-token scan: `input/d5-tools/scan_forbidden.py` — let me check its interface; it scans a directory. I can create `tmp/mutation_scan/` with a file containing `sorry` and run the scanner on it, asserting hard_match_count > 0. Let me read that tool.

Also, the scan's comment/string awareness: writing "sorry" inside a comment should NOT be flagged. That's a good sensitivity+false-positive check: a file with `-- sorry` in a comment should report 0 hard matches while a file with an actual `sorry` term should report ≥1. Let me check the tool.

Let me read the scanner and the engine statements.
dsh: reasoning:
Now I understand the engine. `sturm_zero_comparison` concludes a disjunction. The deliverable's `exists_zero_of_curvature_lt` concludes the first disjunct outright under a strict excess hypothesis. Great — non-restatement check: delivered type has no `∨` and engine's does.

Let me verify `exists_zero_of_curvature_lt` statement in the deliverable:

Let me grep it.
dsh: reasoning:
Now I have everything I need. Let me plan pass-6 concretely.

**Files to create:**
1. `tmp/acceptance_probe_r6.lean` — fresh probe with new content, including:
   - New curvature windows not used by earlier probes: (K,k) = (5,7), interlacing with (k1,k2) = (4,1) or (9,4) on shifted intervals, negative-index interlacing, Wronskian at new point, literal-branch refutation, and importantly:
   - `#check @...` lines for the non-restatement check: delivered `exists_zero_of_curvature_lt`, engine `sturm_zero_comparison`, delivered `firstPositiveZero_le_pi_sqrt`, engine-consumed `wronskian_antitoneOn_of_le`, etc. And `#print axioms` lines for fail-closed cone audit.
   - A theorem that uses the engine in a *different* way: e.g., apply `exists_zero_of_curvature_lt` with a = -1, b = 1 using odd/even data? Need JacobiSolutionOn on (a,b), u1 a = 0, etc. Simpler: use shifted `jacobiSol` data as prior probes did, but with fresh constants.

Hmm — what would be genuinely new and mathematically meaningful? Ideas:
   - **Interlacing on a shifted interval with k2 ≠ 0 and non-symmetric data**: shift by `a = -3` and use `K = 4`, comparing `jacobiSol 9` shifted vs `jacobiSol 4` shifted on `(-3, -3+π/2)`; conclude `jacobiSol 9 (c - (-3))`... Actually with the shifted model, `jacobiSolShift K a t = jacobiSol K (t - a)`. On the interval (a, a+π/√K) the model K has zero at both endpoints (0 and π/√K shifted). For higher curvature 9: zero strictly inside. New constants and negative shift = fresh.
   - **Zero counting with a solution that is NOT a pure model**: e.g. `u = sin(3t)/3` on `(0, π/9)`? Curvature k = 9, K = 9 equality → endpoint zero at π/3... Let's use `K = 4`, `u = sin(3t)/3` (k = 9 ≥ 4) on horizon `π/4`? sin(3t) has zero at π/3 > π/4, so the zero in (0, π/√K] = (0, π/2] exists (π/3 ≤ π/2). But wait `firstPositiveZero u = π/3 ≤ π/2`. This exercises the theorem on a solution whose first zero is strictly inside the bound window. Good and new.
   - **Sharpness**: `u = sin(t/2)/(1/2)` with k = 1/4 and K = 1/4 equality: first zero 2π = π/√(1/4) = 2π. Endpoint equality case.
   - **Attainment**: `firstPositiveZero_mem_of_normalized` on `u = sin(3t)/3`: firstPositiveZero = π/3, attained.

   I'll write a handful of new theorems:
   - `p6_shift_interlacing` with a = -3, K_high = 9 (u1 = shift 9), K_low = 4 (u2 = shift 4), interval (-3, -3+π/2): zero of u1 strictly inside.
   - `p6_zero_count_K4_k9` : u = sin(3·)/3, k = 9 (constant), K = 4, horizon π/2, u 0 = 0 → zero in (0, π/2].
   - `p6_firstZero_le` : firstPositiveZero (fun t => sin (3*t)/3) ≤ π/2.
   - `p6_firstZero_eq` : firstPositiveZero (fun t => sin (3*t)/3) = π/3.
   - `p6_wronskian_antitone_data` : instantiate `wronskian_antitoneOn_of_le` with (k1,u1) = (9, sin3) and (k2,u2) = (4, sin2): on [0, π/2]? Need sign hypothesis 0 ≤ u1*u2 on (0, π/2): sin3t ≥ 0 for t ∈ (0, π/3), becomes negative after. So choose interval [0, π/6]: sin3t ≥ 0, sin2t ≥ 0. AntitoneOn W [0, π/6]. Then W 0 = 0 gives W ≤ 0 → `sin3t·2cos2t... ` hmm derivative of sin3t is 3cos3t; W = u1 du2 - u2 du1 = sin3t·2cos2t - sin2t·3cos3t ≤ 0 at t. Fine but let's not overcomplicate — the theorem statement can be the inequality.
   - `p6_literal_branch_refuted`: `¬ ∃ c ∈ Ioo 0 π, Real.sin c = 0` (uses prior art theorem; new route? pass-2 did it). Better: `p6_linear_no_zero`: `∀ b > 0, ¬ (fun t => t) b = 0` — direct.
   - `p6_engine_conclusion_not_a_restatement`: use `#check` to compare types, parsed in Python.
   - `p6_cone_*`: `#print axioms` for each new theorem.

2. `tmp/acceptance_probe_r6_audit.lean`? Could just add `#print axioms` in the same probe file (pass-5 did it in one file). Simpler: one probe file with `#print axioms` lines at the end. But careful: `#print axioms` for a theorem must come after its declaration. Fine.

3. `tmp/mutation/`:
   - `tmp/mutation/poisoned_axiom_audit.lean`: imports deliverable + `axiom p6_mutation_axiom : False` + `theorem p6_mutation_thm : False := p6_mutation_axiom` + `#print axioms` for the poison and for a couple of pristine declarations. Compile it; run the same parse+allow-list logic; assert violations detected (poison out of cone). Note: this file must NOT be in `release/` to avoid contaminating the release tree/hash manifest. It lives in `tmp/mutation/`, compiled via `lake env lean ../tmp/mutation/...` from release. Compiling an extra file creates olean? `lake env lean` on a file outside the package — it will try to write `.olean` next to the source in tmp/mutation. That's fine (tmp is ours).
   - `tmp/mutation/sorry_scan.lean`: a file containing `theorem bad : False := by sorry` (fake code; it doesn't need to compile). Run `scan_forbidden.py tmp/mutation` → expect hard_match_count ≥ 1.
   - `tmp/mutation/comment_only_scan.lean`: file with `-- sorry` and `/- axiom -/` in comments → expect 0 hard matches. This validates no false positives / the comment awareness.
   - `tmp/mutation/missing_decl_audit.lean`: a copy of the audit module with one `#print axioms` line deleted; run the parser with the *original* expected list → missing non-empty (tests that a truncated report can't pass). This doesn't need compiling; it's a parse-level test. To make it stronger, actually compile the truncated module and check the parser finds a missing declaration. It's cheap (imports cached).

   The mutation tests will be executed by the pass-6 driver and recorded in `evidence/acceptance-pass6.json`.

4. `tools/acceptance_pass6.py` — the driver with the checks listed.

5. Card §8g appended — need to edit `tools/make_result_card.py` to add a section (the script generates the md). Let me look at how §8f is generated in make_result_card.py to add §8g similarly. Actually maybe the card is generated from a template with sections hard-coded. Let me grep for "8f" in make_result_card.py.

Also need to check `tools/write_checkpoint.py` usage.

Let me look at the card generator structure around 8f.
dsh: reasoning:
Good. Now, before making changes, let me do a quick baseline verification that the current state is intact: run `final_integrity_check.py` (fast) and check deliverable hashes vs cp22. That tells me the frozen state matches.

Let me also set up a todo list since this is multi-step.

Then implement pass-6.

Let me first run a quick baseline check (not the full rebuild) to confirm state.
dsh: reasoning:
Baseline intact. Now let me write the pass-6 probe. First, I need to understand the exact names/signatures available in the deliverable to write a compiling probe. Let me read key declarations from the main module: `jacobiSolShift`, `firstPositiveZero`, `modelJacobiSolutionOn`, etc. And the imports available in the probe (the probe imports `Poincare.L4.GeodesicComparison.SturmInterlacingConjugateCrossCheck`, presumably).

Let me look at the beginning of the main module (imports, namespace, definitions).
dsh: reasoning:
Now let me look at the signatures I'll need for the probe: `jacobiSolShift`, `firstPositiveZero`, `firstPositiveZero_mem_of_normalized`, `exists_jacobi_zero_on_Ioc_pi_sqrt`, etc. Let me grep them.
dsh: reasoning:
Now I have a good picture. Let me write the pass-6 probe. I want it to compile with minimal pain. I'll construct data similar to prior probes but with fresh constants. To minimize risk, let me look at the pass-5 probe to reuse idioms (it compiled).
dsh: reasoning:
Now I'll write the pass-6 probe. I want new data and new theorems. Let me design carefully so it compiles.

Probe content:

P6-1: literal branch refuted + the linear model's inapplicability, and the honest degenerate statement:
```lean
theorem p6_literal_branch_status :
    (¬ ∃ c ∈ Ioo (0:ℝ) Real.pi, Real.sin c = 0) ∧
    firstPositiveZero Real.sin = Real.pi ∧
    (∀ b : ℝ, 0 < b → ¬ (fun t : ℝ => t) b = 0) ∧
    (∀ t ∈ Ioc (0:ℝ) Real.pi, (fun t : ℝ => t) t ≠ 0) :=
  ⟨sin_no_zero_in_Ioo_zero_pi, firstPositiveZero_sin, fun b hb => by simpa using hb.ne',
   fun t ht => by simpa using ht.1.ne'⟩
```

P6-2: two-curvature interlacing on the NEGATIVE shifted interval with fresh curvatures (k1=9 vs k2=4 shifted by a=-3): on (-3, -3+π/2) the shifted model with K=4 has zeros at endpoints? jacobiSolShift 4 (-3) t = jacobiSol 4 (t+3); at t = -3: jacobiSol 4 0 = 0. At t = -3 + π/√4 = -3 + π/2: jacobiSol 4 (π/2) = 0 since π/√4 = π/2. Yes. And positive between. u1 = jacobiSolShift 9 (-3): zero at t = -3 + π/3, strictly inside since π/3 < π/2. 

```lean
theorem p6_negative_shift_interlacing :
    ∃ c ∈ Ioo (-3 : ℝ) (-3 + Real.pi / 2), jacobiSolShift 9 (-3) c = 0 := by
  have h := exists_zero_of_curvature_lt
    (k₁ := fun _ : ℝ => 9) (k₂ := fun _ : ℝ => 4)
    (u₁ := jacobiSolShift 9 (-3)) (du₁ := jacobiDerivShift 9 (-3))
    (ddu₁ := fun t => -(9 * jacobiSolShift 9 (-3) t))
    (u₂ := jacobiSolShift 4 (-3)) (du₂ := jacobiDerivShift 4 (-3))
    (ddu₂ := fun t => -(4 * jacobiSolShift 4 (-3) t))
    (a := -3) (b := -3 + Real.pi / 2)
    (by linarith [Real.pi_pos]) (fun t _ => by norm_num)
    (jacobiSolShift_jacobiSolutionOn 9 (-3) _ _)
    (jacobiSolShift_jacobiSolutionOn 4 (-3) _ _)
    (jacobiSolShift_zero ...) ...
```
Wait: is there a `jacobiSolShift K a a = 0` lemma? `jacobiSolShift K a a = jacobiSol K 0 = 0` via `jacobiSol_zero K`. Need `u₂ b = 0` where b = -3 + π/2: `jacobiSolShift 4 (-3) (-3 + π/2) = jacobiSol 4 (π/2) = 0`. Is there a lemma `jacobiSol K (π/√K) = 0`? Yes: `modelJacobiSol_firstZero hK : jacobiSol K (π/√K) = 0`. With K=4, π/√4 = π/2, so need `π/√4 = π/2`; sqrt 4 = 2 (`Real.sqrt_sq`/norm_num should do `Real.sqrt 4 = 2`). I'd write `by norm_num [Real.sqrt_eq_zero']`? Actually `norm_num` knows `Real.sqrt 4 = 2`? In recent mathlib, `norm_num` can evaluate `Real.sqrt` of perfect squares: `norm_num` handles `Real.sqrt 4`. Safer: `have hsqrt4 : Real.sqrt 4 = 2 := by rw [show (4:ℝ) = 2^2 by norm_num, Real.sqrt_sq (by norm_num)]`.

And positivity of u₂ on (-3, -3+π/2): `jacobiSolShift_pos (K:=4) (a:=-3) (by norm_num)` gives `∀ t ∈ Ioo (-3) (-3 + π/√4), 0 < ...`; need `π/√4` syntactically equal to `π/2`. Since `Real.sqrt 4` doesn't reduce definitionally, I should state the interval as `-3 + Real.pi / Real.sqrt 4` everywhere, and separately note that equals `-3 + π/2`. Better: state the theorem with `Real.pi / Real.sqrt 4` and include the equality in the conclusion? Simplest: use b = -3 + π/√4 and prove existence in that interval; then also provide the numeric comparison as a separate statement. Let's use `-3 + Real.pi / Real.sqrt 4`.

Similarly for u1 = shift 9: it has a zero strictly inside; explicitly at -3 + π/√9 = -3 + π/3.

P6-3: zero counting on a NON-model solution with strictly-inside first zero: u(t) = sin(3t)/3, k = 9, K = 4. Note the D10 model `jacobiSol K` is defined as sin(√K t)/√K probably; for K=9, jacobiSol 9 t = sin(3t)/3. Indeed `jacobiSol_of_pos`. I could use `jacobiSol 9` directly (that's a model though—same as prior probes which used jacobiSol 10 etc.). To be fresh, use K = 4 and jacobiSol 9: k=9 ≥ 4 on [0, π/2]; first zero of jacobiSol 9 is π/3 ≤ π/2. That's a strict-inside case (not previously used? P5 used K=9,k=10; pass-2 used K=9,k=16 etc.). Let's use K = 4, k = 9:
```lean
theorem p6_zero_count_strictly_inside :
    (∃ c ∈ Ioc (0:ℝ) (Real.pi / Real.sqrt 4), jacobiSol 9 c = 0) ∧
    firstPositiveZero (jacobiSol 9) ≤ Real.pi / Real.sqrt 4 ∧
    firstPositiveZero (jacobiSol 9) = Real.pi / Real.sqrt 9 ∧
    jacobiSol 9 (Real.pi / Real.sqrt 9) = 0 := ...
```
with `k := fun _ => 9`, `K := 4`. `exists_jacobi_zero_on_Ioc_pi_sqrt (K:=4) (k:=fun _=>9) ...` requires `4 ≤ 9` on Icc 0 (π/√4) — `fun _ _ => by norm_num`. `JacobiSolutionOn (fun _ => 9) (jacobiSol 9) (jacobiDeriv 9) ...` = `modelJacobiSolutionOn 9 _`. Good.

P6-4: horizon form at fresh (K=4, k=9, H=3) and attainment on jacobiSol 9.

P6-5: interior-bound form at fresh (K=16, k=25, u=jacobiSol 25): zero in (0, π/4].
Actually jacobiSol 25 first zero at π/5 ≤ π/4 — strict inside. Use `exists_jacobi_zero_on_Ioc_pi_sqrt_of_interior_bound (K:=16) (k:=fun _=>25)`.

P6-6: Wronskian with fresh data (k1 = 9, u1 = jacobiSol 9; k2 = 4, u2 = jacobiSol 4) on [0, π/6]? Need sign: u1 u2 ≥ 0 on (0, π/6): jacobiSol 9 > 0 on (0, π/3) ∋ π/6 region, jacobiSol 4 > 0 on (0, π/2). So sign holds. Then AntitoneOn W [0, π/6] and W(0)=0... W 0 = u1 0 * du2 0 - u2 0 * du1 0 = 0 - 0 = 0. So `wronskian_antitoneOn_of_le` gives antitone; then `W (π/6) ≤ W 0 = 0`. Hmm, need W 0 = 0 lemma. Simpler: state the antitone property and an inequality at two points:
```lean
theorem p6_wronskian_fresh_data :
    AntitoneOn (wronskian (jacobiSol 9) (jacobiDeriv 9) (jacobiSol 4) (jacobiDeriv 4)) (Icc 0 (Real.pi/6)) ∧
    wronskian ... (Real.pi/6) ≤ wronskian ... 0 := ...
```
W 0: `jacobiSol_zero 9`, `jacobiSol_zero 4`, so W 0 = 0*du2 0 - 0*du1 0 = 0 by simp. Should be fine: `by simp [wronskian, jacobiSol_zero]`. Need to check `wronskian` is unfolded easily — it's `u₁ * du₂ - u₂ * du₁` presumably. In pass-5 they used `wronskian Real.sin Real.cos (fun t => t) (fun _ => 1)`.

`wronskian_antitoneOn_of_le` signature: `(_hab : a ≤ b) (hk : ∀ ⦃t⦄, t ∈ Ioo a b → k₂ t ≤ k₁ t) (hsign : ...) (h1) (h2)`. Implicit binder style for hk is `⦃t⦄`. With k1,k2 constants, `fun t _ => by norm_num` works.

Then: `hAnti (left_mem_Icc.mpr (by linarith [Real.pi_pos])) ⟨by linarith [Real.pi_pos], by linarith [Real.pi_pos]⟩ (by linarith [Real.pi_pos])` — from pass-5's usage: `wronskian_sin_linear_antitoneOn (left_mem_Icc.mpr Real.pi_pos.le) ⟨..., ...⟩ (by linarith)`. Wait, but `wronskian_sin_linear_antitoneOn` is presumably a specialized wrapper. In pass-5, `wronskian_sin_linear_antitoneOn` is a deliverable theorem with signature maybe `(ha : a ∈ Icc 0 π) (hb : b ∈ Icc 0 π) (hab : a ≤ b) : W a ≤ W b`? Let me check its statement. Actually for the probe I can just use `hAnti` directly: `AntitoneOn W (Icc 0 (π/6))`, then apply `hAnti hx hy hxy` where hx : 0 ∈ Icc..., etc. That gives `W y ≤ W x` for x ≤ y (antitone means x ≤ y → f y ≤ f x). So with x = 0, y = π/6: `W (π/6) ≤ W 0`. Fine.

P6-7: equality case with fresh K = 7: `jacobiSol 7 (π/√7) = 0` and zero in (0, π/√7] via `exists_jacobi_zero_on_Ioc_pi_sqrt (K:=7) (k:=fun _=>7)`.

P6-8: non-restatement witness: prove that the engine's equality alternative is *false* on the strict-gap data while the engine's disjunction holds — i.e., demonstrate the deliverable's strict-gap theorem does strictly more than the engine. Concretely:
```lean
theorem p6_engine_alternative_not_achieved :
    ¬ (∀ t ∈ Ioo (0:ℝ) (Real.pi / Real.sqrt (1/2)), (1:ℝ) = 1/2) := by
  intro h; have := h (Real.pi/4) ⟨by linarith [Real.pi_pos], by linarith [Real.pi_pos]⟩; norm_num at this
```
That's trivial arithmetic, but it documents that on the acceptance data the engine's second disjunct is false, so the engine alone (without the strict-excess hypothesis and the disjunction-elimination) does not directly yield the zero. Combined with a theorem that instantiates the *engine* on the same data to get the disjunction, and the delivered theorem to get the zero:
```lean
theorem p6_engine_dichotomy_vs_delivered :
    ((∃ c ∈ Ioo (0:ℝ) (Real.pi / Real.sqrt (1/2)), Real.sin c = 0) ∨
      (∀ ⦃t : ℝ⦄, t ∈ Ioo (0:ℝ) (Real.pi / Real.sqrt (1/2)) → (1:ℝ) = 1/2)) ∧
    (∃ c ∈ Ioo (0:ℝ) (Real.pi / Real.sqrt (1/2)), Real.sin c = 0) ∧
    ¬ (∀ ⦃t : ℝ⦄, t ∈ Ioo (0:ℝ) (Real.pi / Real.sqrt (1/2)) → (1:ℝ) = 1/2) := ...
```
Hmm, the first conjunct via `sturm_zero_comparison` directly (raw engine, not the deliverable wrapper) — good, shows the engine is genuinely invoked and the deliverable eliminates the equality alternative. But `sturm_zero_comparison` with hk on Icc; need `u2 b = 0`, `u2pos`, `HasDerivAtR u2 (du2 b) b`. Use the same data as `sin_zero_interlaces_half_model`. The strict excess and elimination uses `exists_zero_of_curvature_lt`. This is nice: it's a "not a restatement" machine-checked witness: engine gives `A ∨ B`, delivered gives `A`, and `¬B` is proved, so the delivered theorem's conclusion is strictly more informative than the engine's on this data.

Let me write:
```lean
theorem p6_engine_vs_delivered_strictness :
    (¬ (∀ t ∈ Ioo (0:ℝ) (Real.pi / Real.sqrt (1/2)), (1:ℝ) = 1/2)) ∧
    (∃ c ∈ Ioo (0:ℝ) (Real.pi / Real.sqrt (1/2)), Real.sin c = 0) := by
  have hK : (0:ℝ) < 1/2 := by norm_num
  have hb : 0 < Real.pi / Real.sqrt (1/2) := div_pos Real.pi_pos (Real.sqrt_pos_of_pos hK)
  constructor
  · intro h
    have := h (Real.pi / 4) ⟨by linarith [Real.pi_pos], by linarith [Real.pi_pos, Real.pi_lt_four]⟩
    norm_num at this
  · exact exists_zero_of_curvature_lt (k₁ := fun _ : ℝ => 1) (k₂ := fun _ : ℝ => 1/2)
      ... (hstrict := ⟨Real.pi/4, ⟨...⟩, by norm_num⟩)
```
Careful with `Real.pi / Real.sqrt (1/2)` vs `Real.pi / 4` for membership: need π/4 < π/√(1/2). π/√(1/2) = π√2 ≈ 4.44 > π/4. Prove by `nlinarith [Real.pi_pos, Real.sqrt_pos_of_pos hK, Real.sq_sqrt (le_of_lt hK)]`? Let's see: need π/4 < π/√(1/2). Since positive, equivalent to √(1/2) < 4 ⟺ 1/2 < 16 ✓. `nlinarith [Real.sq_sqrt (show (0:ℝ) ≤ 1/2 by norm_num), Real.sqrt_nonneg (1/2), Real.pi_pos]` might do. Alternatively use the already-proved `sin_zero_interlaces_half_model.2 : Real.pi ∈ Ioo 0 (π/√(1/2))`. Then for the hstrict witness, we need some t in the interval where 1/2 < 1 — any t, e.g. t = π/4 but membership. Actually since k1=1, k2=1/2 constants, hstrict just needs any point in the interval. We can use `t = Real.pi / 2` and prove membership from `sin_zero_interlaces_half_model.2`? That gives π ∈ Ioo 0 (π/√(1/2)); π/2 < π < bound ✓. Use `⟨Real.pi/2, ⟨by linarith [Real.pi_pos], by linarith [sin_zero_interlaces_half_model.2.2, Real.pi_pos]⟩, by norm_num⟩`.

Hmm, I need to check what `sin_zero_interlaces_half_model` states exactly. Let me grep. Also better: to reduce compile risk, I can simply reuse the existing deliverable instance theorem for the existential conjunct and add the `¬∀` part. The `¬∀` part is trivial arithmetic.

P6-9: **type non-restatement via #check** — I'll add `#check @...` lines and parse them in Python:
```
#check @Poincare.L4.GeodesicComparison.exists_zero_of_curvature_lt
#check @Poincare.D12.ComparisonGeodesics.sturm_zero_comparison
#check @Poincare.L4.GeodesicComparison.firstPositiveZero_le_pi_sqrt
#check @Poincare.L4.GeodesicComparison.exists_jacobi_zero_on_Ioc_pi_sqrt
#check @Poincare.D12.ComparisonGeodesics.wronskian_antitoneOn_of_le
#check @Poincare.L4.GeodesicComparison.wronskian_sin_linear_antitoneOn
```
Then Python parses `#check` output (Lean prints `name : type` possibly multi-line), normalizes whitespace, and asserts:
- delivered type ≠ engine type (non-restatement),
- engine type contains `∨` and delivered does not (strictly stronger conclusion shape),
- delivered `exists_jacobi_zero_on_Ioc_pi_sqrt` type differs from prior-art `exists_jacobi_zero_before_pi_sqrt` type (non-duplication) — check that the prior art conclusion contains `∃ c ∈ Ioo` while ours has `Ioc` closed endpoint. Actually check strings differ.
- `firstPositiveZero_le_pi_sqrt` type contains `firstPositiveZero` and `≤ π/√K`.

Parsing `#check` multi-line output: Lean's output format for `#check @foo` is:
```
@foo : ∀ {args}, ... 
```
possibly wrapped. I'll match blocks by locating lines starting with `@name :` and joining until the next blank line / next `@`. Simpler: parse the whole output with a regex on names: split on `\n(?=@?Poincare...)`. Or ask for `#check @foo` and then use `#print` ... Hmm. Robust approach: run each `#check` in a *separate* Lean invocation? Costly (each ~2s) but fine. Or put each `#check` on its own line and parse with a state machine: a new entry starts when a line contains `@` + fully qualified name + ` :`. Continuation lines are indented. That works.

Actually there's a subtlety: `#check @foo` prints `@Poincare.L4...exists_zero_of_curvature_lt : ∀ ...`. Good.

Then in Python, for the "contains ∨" test: the engine's type has `∨`, delivered type should not. But careful: the delivered theorem's *hypotheses* might contain `∨`? `exists_zero_of_curvature_lt`: no ∨. `firstPositiveZero_le_pi_sqrt`: no. Good.

Another check: `sturm_zero_comparison` type vs `firstPositiveZero_le_pi_sqrt` type — obviously different.

I'll define pairs:
1. (`exists_zero_of_curvature_lt` vs engine `sturm_zero_comparison`): distinct, delivered has no `∨`, engine has `∨` → strict-strengthening on the strict-excess subcase.
2. (`exists_jacobi_zero_on_Ioc_pi_sqrt` vs prior art `exists_jacobi_zero_before_pi_sqrt`): distinct (non-duplication).
3. (`wronskian_sin_linear_antitoneOn` vs engine `wronskian_antitoneOn_of_le`): distinct (instantiated, not restated).
4. (`no_positive_solution_past_pi_sqrt` vs leader `conjugate_point_bound`): distinct and delivered has strictly fewer hypotheses (count `→`? or count binders). I can compare hypothesis counts by counting top-level `{...}` / `(...)` binders before the final `:`... fragile. Simpler: check distinct + report both types for the semantic review; plus check delivered type does not mention `B`/`t₀`.

Hmm. Actually let me measure "number of explicit hypothesis binders" as the count of `→` at top level in the normalized type string. For `conjugate_point_bound` there should be many `→` and for `no_positive_solution_past_pi_sqrt` fewer. Because implicit binders `{...}` also produce arrows? In Lean 4, `#check @foo` prints `@foo : ∀ {k u du ddu : ℝ → ℝ} {K : ℝ}, 0 < K → ...`. Every forall binder is followed by `,` then the body; arrows appear for non-dependent hypotheses. Counting `→` gives hypothesis-ish count. So check `arrows(delivered) < arrows(leader)`. That's a decent machine check of "strictly weaker hypotheses" — report as such with the numbers.

I need to be careful that this is a *heuristic*, so I'll label it as a heuristic count in the report, not a proof. Fine.

P6-10: Also verify semantic non-vacuity of `firstPositiveZero_le_pi_sqrt` **with** existence: the probe can prove that there exists data where the bound is attained with equality (`firstPositiveZero_modelJacobiSol`). Already in deliverable. Skip.

Now the mutation tests. Driver-side (Python), no Lean needed except the poisoned audit module:

Mutation A (axiom audit rejects a new axiom):
- Create `tmp/mutation/poisoned_audit.lean`:
```lean
import Poincare.L4.GeodesicComparison.SturmInterlacing
axiom p6_mutation_axiom : False
theorem p6_mutation_thm : False := p6_mutation_axiom
#print axioms Poincare.L4.GeodesicComparison.p6_mutation_thm
#print axioms Poincare.L4.GeodesicComparison.modelJacobiSolutionOn
```
Wait, `theorem p6_mutation_thm : False := p6_mutation_axiom` inside namespace. The `#print axioms` must use the full name. Compile with `lake env lean ../tmp/mutation/poisoned_audit.lean` from release. Since tmp/mutation has no lakefile, `lake env lean` uses the release package env; imports resolve. Output: `'Poincare...p6_mutation_thm' depends on axioms: [p6_mutation_axiom]`. Then Python applies `ALLOWED` and must find a violation. Compile exit should be 0 (it's a valid file). 

Note: `axiom` in the file — the forbidden scanner would flag it if scanned, but this file is in tmp, not scanned by the release gate. Fine.

Mutation B (missing declaration detection): Driver parses the poisoned audit's `#print axioms` list as "expected", but deletes one entry from the parsed report (simulate truncation) — actually better: create `tmp/mutation/truncated_audit.lean` = copy of poisoned file minus the line `#print axioms ...modelJacobiSolutionOn`; compile it; expected list from the *pristine deliverable audit module* should include modelJacobiSolutionOn; the truncated output won't report it → `missing` non-empty → auditor fails closed. This tests the missing-declaration branch. 

Actually simpler and equally valid: expected list from the truncated source itself wouldn't include it (then no missing). The point is the gate compares expected (from the audit module source) vs reported; truncation is only detectable if expected is computed independently. In `run_sturm_gates.py`, expected comes from the same source file it compiles, so a truncated source would pass the missing check! Hmm — that's a real property: if someone deletes both the source line and the declaration... Actually deleting a `#print axioms` line from the audit source removes it from `expected` too, so it passes. But the *coverage* check (pass-4/5: audit covers every own declaration) catches that — pass-5 has `audit_covers_every_own_declaration` computed from DECL_RE over the deliverable modules. So the real fail-closed protection against truncation is the coverage check. My mutation test should test that: parse declarations from the two deliverable modules, check coverage. For the truncated audit, coverage should fail. 

So Mutation B: `truncated_audit.lean` = poisoned file without the `#print axioms` for `modelJacobiSolutionOn`; compile; then run both (i) the parser → expected from truncated source (no missing) and (ii) the coverage check against own declarations → uncovered non-empty → fail. Assert the coverage detector fires. That's honest: I'll report which mechanism catches which mutation.

Mutation C (forbidden-token scan): create two files in tmp/mutation/scan_targets/:
- `real_sorry.lean`: `theorem p6_sorry_target : False := by sorry`
- `comment_only.lean`: `-- sorry axiom unsafe\n/- admit native_decide -/\ntheorem p6_clean : True := trivial`
Run `input/d5-tools/scan_forbidden.py tmp/mutation/scan_targets` (cwd ROOT) — but the scanner's `os.walk(root)` and `os.path.relpath(path, root)`: fine. Assert: hard_match_count == 1 (from real_sorry) and no match in comment_only; exit code 1. This validates both sensitivity and comment-awareness (no false positive).

Also test the string-awareness? e.g. a file with the word "sorry" inside a Lean string — probably not needed.

Mutation D (deliverable hash tamper detection): Copy `SturmInterlacing.lean` to tmp, flip a byte, and verify the hash check would flag mismatch. This is trivially true (hash function), low value. Skip; but maybe include a cheap "detector" test: the card hash comparison loop flags a mismatched hash. I can simulate by computing sha of a modified copy vs card hash → different. It's tautological. Skip.

Better Mutation D: **statement-weakening detector**? Not machine-checkable in general.

I'll do A, B, C. That's solid.

Also the pass-6 driver re-runs the standard checks: closure rebuild, probe compile, cones, source hygiene, audit re-run, coverage, hashes vs checkpoint cp22, input byte-identity, preservation, no-collision, non-restatement (`#check` parse), mutation results. Save `evidence/acceptance-pass6.json`.

One important consideration: the closure rebuild deletes `release/.lake/build/lib/lean/Poincare` and rebuilds. That takes ~25s. Fine.

Another consideration: compiling `tmp/mutation/*.lean` will create `.olean` files in tmp/mutation (lean writes them next to source when invoked via `lake env lean`? Actually `lean` writes .olean next to the .lean file by default). Fine, tmp is ours.

Let me now check names available: `sinJacobiSolutionOn` signature (a b)? From pass-5: `sinJacobiSolutionOn 0 _` so it takes two explicit args (a b) and returns JacobiSolutionOn (fun _ => 1) sin cos (fun t => -sin t) a b. Good.

Let me check `modelJacobiSol_pos` signature: `modelJacobiSol_pos (hK) : ∀ t ∈ Ioo 0 (π/√K), 0 < jacobiSol K t`.

`jacobiSol_zero`: `jacobiSol K 0 = 0`.
`jacobiDeriv_zero`: `jacobiDeriv K 0 = 1`.
`hasDerivAt_jacobiSol K t : HasDerivAtR (jacobiSol K) (jacobiDeriv K t) t`.

For `exists_zero_of_curvature_lt` on the half-model data I need the same arguments as `sin_zero_interlaces_half_model` uses. Instead of reconstructing, maybe simplest: use the deliverable theorem `sin_zero_interlaces_half_model` and add the strictness negation. But to show the engine is *directly* consumed in the probe, I could instantiate `sturm_zero_comparison` on the same data — that's what pass-5 did in the deliverable itself. Hmm.

I think a cleaner non-restatement demo that compiles easily: prove
```lean
theorem p6_engine_dichotomy_on_acceptance_data :
    (∃ c ∈ Ioo (0:ℝ) (Real.pi / Real.sqrt (1/2)), Real.sin c = 0) ∨
      (∀ ⦃t : ℝ⦄, t ∈ Ioo (0:ℝ) (Real.pi / Real.sqrt (1/2)) → (1:ℝ) = 1/2) := by
  have hK : (0:ℝ) < 1/2 := by norm_num
  exact sturm_zero_comparison (a := 0) (b := Real.pi / Real.sqrt (1/2))
    (div_pos Real.pi_pos (Real.sqrt_pos_of_pos hK))
    (fun t _ => by norm_num)
    (sinJacobiSolutionOn 0 _)
    (modelJacobiSolutionOn (1/2) _)
    Real.sin_zero (jacobiSol_zero (1/2)) (modelJacobiSol_firstZero hK)
    (modelJacobiSol_pos hK) (hasDerivAt_jacobiSol (1/2) _)
```
This calls the raw engine directly. Then:
```lean
theorem p6_equality_alternative_false :
    ¬ (∀ ⦃t : ℝ⦄, t ∈ Ioo (0:ℝ) (Real.pi / Real.sqrt (1/2)) → (1:ℝ) = 1/2) := by
  intro h
  have hmem : Real.pi / 4 ∈ Ioo (0:ℝ) (Real.pi / Real.sqrt (1/2)) := by
    constructor
    · positivity
    · -- π/4 < π/√(1/2)
      ...
  have := h hmem
  norm_num at this
```
The inequality π/4 < π/√(1/2): multiply by 4/π > 0: 1 < 4/√(1/2) ⟺ √(1/2) < 4. Try:
```lean
have hsqrt_half_lt : Real.sqrt (1/2) < 4 := by
  have h2 : Real.sqrt (1/2) < Real.sqrt 16 := by
    apply Real.sqrt_lt_sqrt (by norm_num) (by norm_num)
  simpa using h2
```
`Real.sqrt_lt_sqrt : 0 ≤ a → a < b → sqrt a < sqrt b`. Then:
```lean
have : Real.pi / 4 < Real.pi / Real.sqrt (1/2) := by
  apply div_lt_div_of_pos_left Real.pi_pos ?_ ?_  -- hmm signature
```
Easier: `rw [div_lt_div_iff]`... In mathlib: `div_lt_div_of_pos_left (ha : 0 < a) (hb : 0 < b) (hc : b < c) : a / c < a / b`. Use `div_lt_div_of_pos_left Real.pi_pos (Real.sqrt_pos_of_pos hK) hsqrt_half_lt` gives `π / 4 < π / √(1/2)`? Let's see: with a=π, b=?, c=? Statement `div_lt_div_of_pos_left (ha : 0 < a) (hb : 0 < b) (hc : b < c) : a / c < a / b`. So need b = √(1/2), c = 4: gives π/4 < π/√(1/2). Yes.

Alternatively use existing `sin_zero_interlaces_half_model.2.2 : Real.pi < π/√(1/2)` and `π/4 < π`. Then:
```lean
have hmem : Real.pi / 4 ∈ Ioo (0:ℝ) (Real.pi / Real.sqrt (1/2)) :=
  ⟨by linarith [Real.pi_pos], by linarith [sin_zero_interlaces_half_model.2.2, Real.pi_pos]⟩
```
Wait `sin_zero_interlaces_half_model` — what's its exact statement? Let me grep. If it's `(∃ c ∈ Ioo 0 (π/√(1/2)), sin c = 0) ∧ π ∈ Ioo 0 (π/√(1/2))` then `.2.2` is `π < π/√(1/2)`. Then π/4 < bound by linarith. Good, and that also demonstrates reuse.

Let me grep `sin_zero_interlaces_half_model`.

Also for `firstPositiveZero` non-restatement: `#check` lines don't need proofs. 

Now, one more valuable semantic check I can add cheaply: **witness evaluation** — prove concrete numeric facts about the data used, e.g. `Real.sin (Real.pi) = 0`, `jacobiSol 9 (π/3)=0`... The latter needs `jacobiSol_of_pos` and `sin(3*π/3)=sin π=0`. It's already covered by `jacobiSol K (π/√K)=0` closed form.

I think the probe should be moderate: 8-9 theorems. Let me also verify that `jacobiSol 9 (Real.pi / Real.sqrt 9) = 0` reduces nicely for `firstPositiveZero (jacobiSol 9) = π/√9` via `firstPositiveZero_modelJacobiSol (by norm_num : 0<9)`. Yes.

Let me check the exact statement of `sin_zero_interlaces_half_model` and `sin_zero_linear_zeroFree_interlacing` and `linear_model_no_second_zero`:
dsh: reasoning:
Good. Now I have everything to write the probe. Let me write `tmp/acceptance_probe_r6.lean`.

Design (fresh data, avoiding earlier probes' choices):
- P6-1: literal branch status bundle (uses prior art by name + closed forms + linear zero-free).
- P6-2: negative-shift interlacing with (k₁,k₂) = (9,4), a = -3, b = -3 + π/√4. Witness: c = -3 + π/√9 (explicit) plus the existential.
- P6-3: zero counting with K=4, k=9 (non-model constant curvature, but u = jacobiSol 9 — I'd like a solution that is not the model of either curvature, to reduce "same data" feel. Use u = jacobiSol 9 is the model for k=9 — but the theorem's K is 4, so it's a model of a *different* curvature than K, and the first zero π/3 is strictly inside the window π/2. Prior probes used jacobiSol 10 with K=9 (also strictly inside), pass-3 used sin(4t)/4 with K=9. Fresh enough. But we can do better: construct a *non-model* solution: u(t) = sin(3t)/3 + something? It must satisfy u'' + k u = 0 with k ≥ K; any such u is a combination a sin(√k t) + b cos(√k t) when k constant. Take k = 9, u(t) = (sin(3t) + cos(3t) - 1)/3? Check u(0) = (0 + 1 - 1)/3 = 0 ✓. u'' = -9(sin+cos)/3 = -9u - 9*(1/3)? Let's compute: u = (s + c - 1)/3 where s=sin3t, c=cos3t. u'' = (-9s -9c)/3 = -3(s+c) = -9*(s+c)/3 = -9(u + 1/3) = -9u - 3. So u'' + 9u = -3 ≠ 0. Not a solution.

Take u(t) = (sin(3t) + cos(3t) - 1)/3 + something... The general solution of u''+9u=0 is A sin3t + B cos3t with u(0)=0 → B=0 → u = A sin 3t. So any solution with u(0)=0 and k=9 constant is a multiple of sin 3t (given continuity/global solution; but our hypotheses do NOT require uniqueness — JacobiSolutionOn only requires derivative data; the solution is *some* function satisfying the ODE). We can use a nonstandard solution: on (0, π/2), define u piecewise? Continuity + HasDerivAtR constraints make it determined given the ODE holds pointwise... Actually the ODE u'' = -k u with k = 9 constant and u(0)=0, du(0)=1 forces u = sin3t/3 by uniqueness (which follows from the given derivative data + ODE). But `JacobiSolutionOn` doesn't include u' 0 = 1 necessarily; the theorem `exists_jacobi_zero_on_Ioc_pi_sqrt` doesn't need normalization. Fine — jacobiSol 9 with K=4 is a legitimate fresh instance. Alternatively use k non-constant: k(t) = 9 + t (≥4 on [0,π/2]), and u = jacobiSol 9 still satisfies u''+9u=0, not u''+k(t)u=0. So it wouldn't be a Jacobi solution for that k. Keep k = 9 constant.

Actually here's a genuinely non-model solution with constant k: u = sin(3t)/3 + 0*cos(3t) is the model. What about u(t) = sin(3t)/3 - sin(3t)/3 = 0? Trivial. So for constant k and u(0)=0, all solutions are multiples. But we can take k *non-constant* with k ≥ K and a solution we construct as a *different* model? No: a solution with non-constant k isn't a sinusoid, and we have no existence theorem, so we can't construct it easily. So all our instances are multiples of the model. That's fine and is exactly the D12 engine's design (explicit constructed data). The probe's freshness comes from new constants/windows.

- P6-4: horizon + attainment fresh (K=4, k=9, H=3, u=jacobiSol 9): firstPositiveZero ≤ π/2, and attainment `u (firstPositiveZero u) = 0`, `0 < firstPositiveZero u`. Note `firstPositiveZero_mem_of_normalized` requires hk on Icc 0 (π/√K) and hdu0 = du 0 = 1: `jacobiDeriv_zero 9`.
- P6-5: interior-bound form fresh K=16, k=25 (u=jacobiSol 25): zero in (0, π/4].
- P6-6: equality case fresh K=7: endpoint zero + zero count.
- P6-7: Wronskian fresh data (9 vs 4) on [0, π/6]: AntitoneOn + inequality W(π/6) ≤ W(0) + explicit derivative value? `wronskian_deriv` gives deriv W t = (k₂ - k₁) u₁ u₂ = -5 u₁ u₂. At t = π/12: = -5 * jacobiSol 9 (π/12) * jacobiSol 4 (π/12). Could state `deriv W (π/12) = -(5 * jacobiSol 9 (π/12) * jacobiSol 4 (π/12))` by rw. Keep simple.
- P6-8: engine dichotomy on the acceptance data + the equality alternative is false + delivered strict zero (three conjuncts) — the "not a restatement" semantic witness. I need to instantiate the raw engine. From `sin_zero_interlaces_half_model`'s proof, the raw engine call is available at lines 290-296; let me read that to copy the instantiation correctly.

Let me read lines 282-310 of the module.

- P6-9: `#check` lines for the type comparisons.
- P6-10: `#print axioms` lines for all P6 theorems.

Let me read the raw engine instantiation in the deliverable.
dsh: reasoning:
Now I'll write the probe. For the raw engine call on the half-model data, I can copy the exact call from `sin_zero_interlaces_half_model`'s proof:

```lean
theorem p6_engine_dichotomy_raw :
    (∃ c ∈ Ioo (0 : ℝ) (Real.pi / Real.sqrt (1 / 2)), Real.sin c = 0) ∨
      (∀ ⦃t : ℝ⦄, t ∈ Ioo (0 : ℝ) (Real.pi / Real.sqrt (1 / 2)) → (1 : ℝ) = 1 / 2) := by
  have hK₂ : (0 : ℝ) < 1 / 2 := by norm_num
  have hz : 0 < Real.pi / Real.sqrt (1 / 2) :=
    div_pos Real.pi_pos (Real.sqrt_pos_of_pos hK₂)
  exact sturm_zero_comparison hz (fun t _ => by norm_num)
    (sinJacobiSolutionOn 0 _) (modelJacobiSolutionOn (1 / 2) _)
    Real.sin_zero (jacobiSol_zero (1 / 2)) (modelJacobiSol_firstZero hK₂)
    (modelJacobiSol_pos hK₂) (hasDerivAt_jacobiSol (1 / 2) _)
```
Careful: `sturm_zero_comparison`'s implicit args k₁ k₂ etc. need to be inferred from the expected type. The expected type gives `u₁ = sin`, but `k₁`? `sinJacobiSolutionOn` provides `k₁ = fun _ => 1`. Since the conclusion only mentions `k₁ = k₂` in the second disjunct as `(1:ℝ) = 1/2`, inference should work from the elaborated term: `sturm_zero_comparison hz hk (sinJacobiSolutionOn 0 _) ...`. The `hk` arg `(fun t _ => by norm_num)` has expected type `∀ ⦃t⦄, t ∈ Icc a b → k₂ t ≤ k₁ t`; k₁,k₂ should be determined by unification with the expected conclusion and `h1`. It worked in the deliverable, so copying is safe.

Then:
```lean
theorem p6_equality_alternative_false :
    ¬ (∀ ⦃t : ℝ⦄, t ∈ Ioo (0 : ℝ) (Real.pi / Real.sqrt (1 / 2)) → (1 : ℝ) = 1 / 2) := by
  intro h
  have hmem : Real.pi / 4 ∈ Ioo (0 : ℝ) (Real.pi / Real.sqrt (1 / 2)) :=
    ⟨by linarith [Real.pi_pos],
     by linarith [sin_zero_interlaces_half_model.2.2, Real.pi_pos]⟩
  have := h hmem
  norm_num at this
```
Note `h` has implicit binder `⦃t⦄`, so `h hmem` works.

Then combine:
```lean
theorem p6_engine_alternative_eliminated :
    ((∃ c ∈ Ioo (0 : ℝ) (Real.pi / Real.sqrt (1 / 2)), Real.sin c = 0) ∨
        (∀ ⦃t : ℝ⦄, t ∈ Ioo (0 : ℝ) (Real.pi / Real.sqrt (1 / 2)) → (1 : ℝ) = 1 / 2)) ∧
      (∃ c ∈ Ioo (0 : ℝ) (Real.pi / Real.sqrt (1 / 2)), Real.sin c = 0) ∧
      ¬ (∀ ⦃t : ℝ⦄, t ∈ Ioo (0 : ℝ) (Real.pi / Real.sqrt (1 / 2)) → (1 : ℝ) = 1 / 2) :=
  ⟨p6_engine_dichotomy_raw, sin_zero_interlaces_half_model.1, p6_equality_alternative_false⟩
```

For the negative shift interlacing: I need to prove `jacobiSolShift 9 (-3) (-3 + π/√9) = 0` explicitly maybe; the existential comes from `exists_zero_of_curvature_lt`. The hypotheses:
- `hab : -3 < -3 + π/√4` from `π/√4 > 0`.
- `hk : ∀ t ∈ Icc (-3) (-3 + π/√4), 4 ≤ 9` — norm_num.
- h1 : JacobiSolutionOn (fun _ => 9) (jacobiSolShift 9 (-3)) ... (-3) (-3+π/√4) — `jacobiSolShift_jacobiSolutionOn 9 (-3) _ _`.
- hu2b : jacobiSolShift 4 (-3) (-3 + π/√4) = 0. Prove: `show jacobiSol 4 (-3 + π/√4 - (-3)) = 0; ring_nf`? `jacobiSolShift K a t = jacobiSol K (t - a)`. So = jacobiSol 4 (π/√4) = 0 via `modelJacobiSol_firstZero (by norm_num : (0:ℝ)<4)`. Write:
```lean
have hu2b : jacobiSolShift 4 (-3) (-3 + Real.pi / Real.sqrt 4) = 0 := by
  show jacobiSol 4 ((-3 + Real.pi / Real.sqrt 4) - (-3)) = 0
  rw [show (-3 + Real.pi / Real.sqrt 4) - (-3) = Real.pi / Real.sqrt 4 by ring]
  exact modelJacobiSol_firstZero (by norm_num)
```
- hu2pos : `jacobiSolShift_pos (K:=4) (a:=-3) (by norm_num)` gives ∀ t ∈ Ioo (-3) (-3 + π/√4), 0 < ... — exact match since same expression. ✓
- hdu2b : HasDerivAtR (jacobiSolShift 4 (-3)) (jacobiDerivShift 4 (-3) (-3 + π/√4)) (-3 + π/√4) — `hasDerivAt_jacobiSolShift 4 (-3) _`.
- hstrict : ∃ t ∈ Ioo (-3) (-3+π/√4), 4 < 9 — any point, e.g. midpoint: `⟨-3 + π/8, ⟨by linarith [Real.pi_pos], by linarith [Real.pi_pos]⟩, by norm_num⟩`.
- hu1a : jacobiSolShift 9 (-3) (-3) = 0 — same style.
- hu2a : jacobiSolShift 4 (-3) (-3) = 0.

Then conclusion `∃ c ∈ Ioo (-3) (-3+π/√4), jacobiSolShift 9 (-3) c = 0`.

Additionally, explicit witness: `jacobiSolShift 9 (-3) (-3 + π/√9) = 0` and membership. I'll state the theorem as conjunction: existential + explicit witness zero + membership:
```lean
theorem p6_negative_shift_interlacing :
    (∃ c ∈ Ioo (-3 : ℝ) (-3 + Real.pi / Real.sqrt 4), jacobiSolShift 9 (-3) c = 0) ∧
      jacobiSolShift 9 (-3) (-3 + Real.pi / Real.sqrt 9) = 0 ∧
      (-3 + Real.pi / Real.sqrt 9) ∈ Ioo (-3 : ℝ) (-3 + Real.pi / Real.sqrt 4) := by
  refine ⟨?_, ?_, ?_⟩
  · exact exists_zero_of_curvature_lt ...
  · show jacobiSol 9 ((-3 + Real.pi / Real.sqrt 9) - (-3)) = 0
    rw [show (-3 + Real.pi / Real.sqrt 9) - (-3) = Real.pi / Real.sqrt 9 by ring]
    exact modelJacobiSol_firstZero (by norm_num)
  · constructor
    · linarith [Real.pi_pos, Real.sqrt_pos_of_pos (by norm_num : (0:ℝ)<9)]
    · -- π/√9 < π/√4 since √4 < √9
      have h4 : Real.sqrt 4 < Real.sqrt 9 := Real.sqrt_lt_sqrt (by norm_num) (by norm_num)
      have hpos4 : 0 < Real.sqrt 4 := Real.sqrt_pos_of_pos (by norm_num)
      have hpos9 : 0 < Real.sqrt 9 := Real.sqrt_pos_of_pos (by norm_num)
      rw [add_lt_add_iff_left]
      exact div_lt_div_of_pos_left Real.pi_pos hpos4 h4
```
Check `div_lt_div_of_pos_left (ha : 0 < a) (hb : 0 < b) (hc : b < c) : a / c < a / b`. With a=π, b=√4, c=√9: gives π/√9 < π/√4. ✓

For `hu1a`/`hu2a` (shift at a):
```lean
have hu1a : jacobiSolShift 9 (-3) (-3) = 0 := by
  show jacobiSol 9 ((-3 : ℝ) - (-3)) = 0
  rw [sub_self]
  exact jacobiSol_zero 9
```

Note that `exists_zero_of_curvature_lt` has explicit binder `(hk : ∀ t ∈ Icc a b, ...)` (not implicit ⦃⦄), while `sturm_zero_comparison` has implicit. Fine.

P6-3 zero counting:
```lean
theorem p6_zero_count_strict_inside :
    (∃ c ∈ Ioc (0 : ℝ) (Real.pi / Real.sqrt 4), jacobiSol 9 c = 0) ∧
      firstPositiveZero (jacobiSol 9) ≤ Real.pi / Real.sqrt 4 ∧
      firstPositiveZero (jacobiSol 9) = Real.pi / Real.sqrt 9 := by
  refine ⟨?_, ?_, firstPositiveZero_modelJacobiSol (by norm_num : (0:ℝ) < 9)⟩
  · exact exists_jacobi_zero_on_Ioc_pi_sqrt (K := 4) (k := fun _ : ℝ => 9)
      (u := jacobiSol 9) (du := jacobiDeriv 9) (ddu := fun t => -(9 * jacobiSol 9 t))
      (by norm_num) (fun _ _ => by norm_num) (modelJacobiSolutionOn 9 _) (jacobiSol_zero 9)
  · exact firstPositiveZero_le_pi_sqrt (K := 4) (k := fun _ : ℝ => 9)
      (u := jacobiSol 9) (du := jacobiDeriv 9) (ddu := fun t => -(9 * jacobiSol 9 t))
      (by norm_num) (fun _ _ => by norm_num) (modelJacobiSolutionOn 9 _) (jacobiSol_zero 9)
```

P6-4 horizon + attainment:
```lean
theorem p6_horizon_attainment :
    firstPositiveZero (jacobiSol 9) ≤ Real.pi / Real.sqrt 4 ∧
      jacobiSol 9 (firstPositiveZero (jacobiSol 9)) = 0 ∧
      0 < firstPositiveZero (jacobiSol 9) := by
  have hH : Real.pi / Real.sqrt 4 ≤ 3 := by
    have hsqrt4 : Real.sqrt 4 = 2 := by
      rw [show (4:ℝ) = 2^2 by norm_num, Real.sqrt_sq (by norm_num)]
    rw [hsqrt4]
    linarith [Real.pi_lt_four]
  have hle := firstPositiveZero_le_pi_sqrt_of_horizon (K := 4) (H := 3) (k := fun _ : ℝ => 9)
    (u := jacobiSol 9) (du := jacobiDeriv 9) (ddu := fun t => -(9 * jacobiSol 9 t))
    (by norm_num) (fun _ _ => by norm_num) (modelJacobiSolutionOn 9 3) (jacobiSol_zero 9)
    (jacobiDeriv_zero 9) hH
  have hmem := firstPositiveZero_mem_of_normalized (K := 4) (k := fun _ : ℝ => 9)
    (u := jacobiSol 9) (du := jacobiDeriv 9) (ddu := fun t => -(9 * jacobiSol 9 t))
    (by norm_num) (fun _ _ => by norm_num) (modelJacobiSolutionOn 9 _) (jacobiSol_zero 9)
    (jacobiDeriv_zero 9)
  exact ⟨hle, hmem.1, hmem.2.1⟩
```
Check `firstPositiveZero_le_pi_sqrt_of_horizon` signature: `(hK) (hk : ∀ t ∈ Icc 0 H, K ≤ k t) (h : JacobiSolutionOn k u du ddu 0 H) (hu0) (hdu0) (hH : π/√K ≤ H)`. Yes.

P6-5 interior bound fresh K=16 k=25:
```lean
theorem p6_interior_bound_K16 :
    (∃ c ∈ Ioc (0 : ℝ) (Real.pi / Real.sqrt 16), jacobiSol 25 c = 0) ∧
      firstPositiveZero (jacobiSol 25) ≤ Real.pi / Real.sqrt 16 := by
  have hsqrt16 : Real.sqrt 16 = 4 := by rw [show (16:ℝ)=4^2 by norm_num, Real.sqrt_sq (by norm_num)]
  -- the interior hypothesis is on Ioo 0 (π/√16)
  constructor
  · exact exists_jacobi_zero_on_Ioc_pi_sqrt_of_interior_bound (K := 16) (k := fun _ : ℝ => 25) ...
  · exact firstPositiveZero_le_pi_sqrt_of_interior_bound ...
```
The interior version needs `hk : ∀ t ∈ Ioo 0 (π/√16), 16 ≤ 25` → `fun _ _ => by norm_num`.

P6-6 equality case K=7:
```lean
theorem p6_equality_K7 :
    jacobiSol 7 (Real.pi / Real.sqrt 7) = 0 ∧
      (∃ c ∈ Ioc (0 : ℝ) (Real.pi / Real.sqrt 7), jacobiSol 7 c = 0) := by
  have h7 : (0:ℝ) < 7 := by norm_num
  exact ⟨modelJacobiSol_firstZero h7,
    exists_jacobi_zero_on_Ioc_pi_sqrt (K := 7) (k := fun _ : ℝ => 7) (u := jacobiSol 7)
      (du := jacobiDeriv 7) (ddu := fun t => -(7 * jacobiSol 7 t)) h7 (fun _ _ => le_rfl)
      (modelJacobiSolutionOn 7 _) (jacobiSol_zero 7)⟩
```

P6-7 Wronskian fresh:
```lean
theorem p6_wronskian_fresh_data :
    AntitoneOn (wronskian (jacobiSol 9) (jacobiDeriv 9) (jacobiSol 4) (jacobiDeriv 4))
        (Icc 0 (Real.pi / 6)) ∧
      wronskian (jacobiSol 9) (jacobiDeriv 9) (jacobiSol 4) (jacobiDeriv 4) (Real.pi / 6) ≤
        wronskian (jacobiSol 9) (jacobiDeriv 9) (jacobiSol 4) (jacobiDeriv 4) 0 ∧
      deriv (wronskian (jacobiSol 9) (jacobiDeriv 9) (jacobiSol 4) (jacobiDeriv 4)) (Real.pi / 12)
        = -(5 * (jacobiSol 9 (Real.pi / 12) * jacobiSol 4 (Real.pi / 12))) := by
  have hanti : AntitoneOn (wronskian (jacobiSol 9) (jacobiDeriv 9) (jacobiSol 4) (jacobiDeriv 4))
      (Icc (0:ℝ) (Real.pi / 6)) := by
    refine wronskian_antitoneOn_of_le (by linarith [Real.pi_pos]) (fun t _ => by norm_num) ?_
      (modelJacobiSolutionOn 9 _) (modelJacobiSolutionOn 4 _)
    intro t ht
    have h9 : 0 ≤ jacobiSol 9 t := (modelJacobiSol_pos (by norm_num : (0:ℝ)<9) t
      ⟨ht.1, lt_of_lt_of_le ht.2 ?_⟩).le
    ...
```
Positivity of jacobiSol 9 on (0, π/6): need π/6 ≤ π/√9 = π/3. Prove `π/6 < π/3` by linarith. But `π/√9` vs `π/3` — need `Real.sqrt 9 = 3`. Use `have hsqrt9 : Real.sqrt 9 = 3`. Then:
```lean
    have ht9 : t ∈ Ioo (0:ℝ) (Real.pi / Real.sqrt 9) := ⟨ht.1, by
      have : Real.pi / 6 < Real.pi / Real.sqrt 9 := by
        rw [hsqrt9]; linarith [Real.pi_pos]
      exact lt_of_lt_of_le ht.2 this.le⟩
    have h9 := (modelJacobiSol_pos (show (0:ℝ)<9 by norm_num) t ht9).le
```
Similarly for jacobiSol 4 on (0, π/6): π/6 < π/√4 = π/2. Then `nlinarith` or `exact mul_nonneg h9 h4`.

Then the inequality: `hanti (left_mem_Icc.mpr (by linarith [Real.pi_pos])) ⟨by linarith [Real.pi_pos], by linarith [Real.pi_pos]⟩ (by linarith [Real.pi_pos])` — antitone: `AntitoneOn f s := ∀ ⦃a⦄, a ∈ s → ∀ ⦃b⦄, b ∈ s → a ≤ b → f b ≤ f a`. So with a=0, b=π/6 → f(π/6) ≤ f 0. Good.

Derivative: `wronskian_deriv (modelJacobiSolutionOn 9 _) (modelJacobiSolutionOn 4 _) ht` gives `deriv W t = (k₂ t - k₁ t) * u₁ t * u₂ t = (4-9)*... = -5 * (jacobiSol 9 t * jacobiSol 4 t)`. With t = π/12 ∈ Ioo 0 (π/6): 
```lean
  · rw [wronskian_deriv (modelJacobiSolutionOn 9 _) (modelJacobiSolutionOn 4 _)
      (show Real.pi / 12 ∈ Ioo (0:ℝ) (Real.pi/6) from ⟨by linarith [Real.pi_pos], by linarith [Real.pi_pos]⟩)]
    ring
```
Good. Note `modelJacobiSolutionOn 9 _` for the interval (0, π/6): the implicit a b are inferred from expected type. Fine.

P6-8 as above.

P6-9 type checks and `#print axioms`.

Also maybe add a theorem `p6_two_curvature_general_instance` — nah.

Let me now write the probe file. Then compile it in isolation to fix errors before writing the driver.

One concern: `#check` output and the probe's `#print axioms` lines. I'll put `#check` lines at the end (after the namespace closes) with fully qualified names.

Let me write the file now.
dsh: reasoning:
Now compile it to fix errors. Use `cd release && lake env lean ../tmp/acceptance_probe_r6.lean`.

Note this might take ~10-30s. Let me run it.
dsh: reasoning:
Fix errors:

1. `linear_model_no_second_zero` is `{b : ℝ} (hb : 0 < b) : ...` — implicit b. So `∀ b, 0 < b → ...` can be `fun b hb => linear_model_no_second_zero (b := b) hb`. Fix.

2. `jacobiSolShift_jacobiSolutionOn K a b` — signature is `(K a b : ℝ)` with b as an explicit argument (the right endpoint). From line 197: `theorem jacobiSolShift_jacobiSolutionOn (K a b : ℝ) : JacobiSolutionOn ... a b`. Wait the error says applying `_` twice: `jacobiSolShift_jacobiSolutionOn 9 (-3) ?m` gives `JacobiSolutionOn ... (-3) ?m` and then I applied `_` again. So the function takes exactly 3 explicit args: `jacobiSolShift_jacobiSolutionOn 9 (-3) (-3 + Real.pi / Real.sqrt 4)`. My call had an extra `_`. Fix: remove trailing `_` in both.

Wait look: I wrote `(jacobiSolShift_jacobiSolutionOn 9 (-3) _ _)`. Should be `(jacobiSolShift_jacobiSolutionOn 9 (-3) _)`.

3. The hk error `⊢ ?m.340 x ≤ ?m.339 x` — implicit k₁/k₂ not inferred because hypothesis mismatch. After fixing the JacobiSolutionOn args it may resolve; but the hk is `(fun _ _ => by norm_num)` with expected `∀ t ∈ Icc a b, k₂ t ≤ k₁ t` where k₁,k₂ can't be inferred until h1/h2 are known. The order of arguments in `exists_zero_of_curvature_lt` is `hab hk h1 h2 ...`, so elaboration of hk happens before h1. Lean's expected type for hk has metavariables; `fun _ _ => by norm_num` leaves them unsolved. In the deliverable, they wrote `exists_zero_of_curvature_lt hz (fun t _ => by norm_num) (sinJacobiSolutionOn 0 _) ...` and it worked (Lean postpones?). Hmm, the error at line 77:56 `⊢ ?m.340 x✝¹ ≤ ?m.339 x✝¹` indicates it didn't get resolved. In the deliverable's `sin_zero_interlaces_half_model` it worked, probably because the expected conclusion type pinned `u₁ = sin`... no, k₁/k₂ aren't in the conclusion except via heq disjunct. Hmm, in that call the conclusion is `∃ c ∈ Ioo 0 b, sin c = 0` which doesn't mention k. So how did it infer? Maybe by `h1 : JacobiSolutionOn k₁ u₁ du₁ ddu₁ a b` being elaborated afterward and then the postponed `hk` gets solved. Lean 4 does postpone elaboration of the `hk` metavariables. But here it failed... because I passed `jacobiSolShift_jacobiSolutionOn 9 (-3) _ _` with the extra argument, causing h1's elaboration to fail → k not inferred. Likely fixed once h1 args are correct.

To be safe, I can pass explicit named args: `(k₁ := fun _ : ℝ => 9) (k₂ := fun _ : ℝ => 4)` etc. Let me do that to be robust.

4. `linarith` failure for the strict witness at `-3 + π/8`: need to prove `-3 + π/8 ∈ Ioo (-3) (-3+π/√4)`: first conjunct `-3 < -3 + π/8` ✓ linarith with π_pos. Second: `-3 + π/8 < -3 + π/√4` i.e. `π/8 < π/√4 = π/2` ✓ linarith needs sqrt4=2. The linarith call I gave only had `Real.pi_pos`; it can't know √4 = 2. Fix: provide `hsqrt4 : Real.sqrt 4 = 2` earlier in the proof of the outer theorem and use it. Actually `hpi4 : 0 < π/√4` and `hspan` don't give the ordering π/√4 > π/8. I'll add a `have hpi8 : -3 + Real.pi / 8 < -3 + Real.pi / Real.sqrt 4 := by have : Real.pi / 8 < Real.pi / Real.sqrt 4 := by rw [hsqrt4]; linarith [Real.pi_pos]; linarith`. Then provide hsqrt4 before.

Also for `(-3 + π/√9) ∈ Ioo (-3) (-3+π/√4)`: first conjunct needs π/√9 > 0: linarith with `Real.pi_pos` and `Real.sqrt_pos_of_pos` — should work if given as separate have. It failed with `a✝ : -3 + π/√9 ≤ -3`. linarith can't see that π/√9 > 0 from facts it has: it has `hpi4 : 0 < π/√4` and `Real.pi_pos : 0 < π`, `Real.sqrt_pos_of_pos ... : 0 < √9`. linarith should combine `0 < π` and `0 < √9` to get `0 < π/√9`? linarith doesn't handle division/positive quotient automatically unless it's given as a fact. So add `have hpi9pos : 0 < Real.pi / Real.sqrt 9 := div_pos Real.pi_pos (Real.sqrt_pos_of_pos (by norm_num))` and then linarith.

5. `firstPositiveZero_le_pi_sqrt_of_horizon` signature: from the deliverable line 474-477, the args are: `(hK) (hk) (h) (hu0) (hdu0) (hH)`? Let me re-check: 

```
theorem exists_jacobi_zero_of_horizon {k u du ddu : ℝ → ℝ} {K H : ℝ}
    (hK : 0 < K) (hk : ∀ t ∈ Icc (0 : ℝ) H, K ≤ k t)
    (h : JacobiSolutionOn k u du ddu 0 H) (hu0 : u 0 = 0) (hdu0 : du 0 = 1)
    (hH : Real.pi / Real.sqrt K ≤ H) :
```
That's `exists_jacobi_zero_of_horizon`. But I called `firstPositiveZero_le_pi_sqrt_of_horizon`. Let me check its actual signature (line ~505 region?). Let me grep it.

6. Also `p6_zero_count_strict_inside` etc. after fixes.

7. The probe reported `sorryAx` because of errors. Fine.

Let me check the signature of `firstPositiveZero_le_pi_sqrt_of_horizon`.
dsh: reasoning:
`firstPositiveZero_le_pi_sqrt_of_horizon` has no `hdu0`. Fix that call (remove `jacobiDeriv_zero 9`).

Now let me apply all fixes.
dsh: reasoning:
The probe compiles (exit 0) with all cones in the allowed set. The `#check` output is parseable.

Now note: `exists_jacobi_zero_before_pi_sqrt` (prior art) conclusion is `∃ c ∈ Ioo 0 (π/√K)` while the deliverable's is `∃ c ∈ Ioc 0 (π/√K)` — distinct. Good for the non-duplication check.

The delivered `exists_zero_of_curvature_lt` has no `∨`; engine has `∨`. Good.

Hypothesis counts (arrow counts):
- `no_positive_solution_past_pi_sqrt`: arrows after the initial ∀ block = count `→` = 6 (0<T, Jacobi, u0, positivity, 0<K, K≤k) then conclusion. 
- `conjugate_point_bound`: more arrows (need to see full output). Fine, the driver will count.

Now let me write the mutation test files and the pass-6 driver.

Mutation files:
- `tmp/mutation/poisoned_audit.lean` — imports deliverable, adds axiom + theorem + #print axioms.
- `tmp/mutation/truncated_audit.lean` — copy of poisoned without one `#print axioms` for a declaration that exists in the deliverable (to test coverage detection).
- `tmp/mutation/scan_targets/real_sorry.lean` and `comment_only.lean`.

Careful: the mutation dir is under tmp/, and the release gate's forbidden scan only scans `release`, so no contamination. Also `final_integrity_check.py` doesn't scan tmp. Good.

Let me write those files.

For `poisoned_audit.lean`:
```lean
/-
Pass-6 gate-sensitivity mutation artifact (NOT part of the release tree).
...
-/
import Poincare.L4.GeodesicComparison.SturmInterlacing

namespace Poincare.L4.GeodesicComparison

axiom p6_mutation_axiom : False

theorem p6_mutation_poisoned : False := p6_mutation_axiom

end Poincare.L4.GeodesicComparison

#print axioms Poincare.L4.GeodesicComparison.p6_mutation_poisoned
#print axioms Poincare.L4.GeodesicComparison.modelJacobiSolutionOn
#print axioms Poincare.L4.GeodesicComparison.exists_zero_of_curvature_lt
```
`#print axioms` on an axiom itself also works. Include it: `#print axioms Poincare.L4.GeodesicComparison.p6_mutation_axiom` — reports `[p6_mutation_axiom]`.

For `truncated_audit.lean`: same but only `#print axioms p6_mutation_poisoned` (and maybe the poison axiom), omitting `modelJacobiSolutionOn` and `exists_zero_of_curvature_lt`. The coverage check should detect that those own declarations are not covered.

Scan targets:
`tmp/mutation/scan_targets/real_sorry.lean`:
```lean
/-
Gate-sensitivity mutation target: this file contains a real `sorry` in code and MUST be
flagged by input/d5-tools/scan_forbidden.py.
-/
theorem p6_scan_mutation : False := by sorry
```
`tmp/mutation/scan_targets/comment_only.lean`:
```lean
/-
Gate-sensitivity mutation target (false-positive control): the tokens below occur only in
comments and strings and must NOT be flagged by input/d5-tools/scan_forbidden.py:
  sorry axiom unsafe native_decide proof_wanted sorryAx admit
-/
def p6_scan_clean : String := "sorry axiom unsafe"
theorem p6_scan_clean_thm : True := trivial
```
Careful: the scanner's string handling blanks strings; comment handling blanks comments. So no hard matches. But wait — I put the tokens in a doc comment `/-! ... -/`; that's blanked. And the string `"sorry axiom unsafe"` is blanked. Good. The file doesn't need to compile (we don't compile it), but it should be valid Lean anyway for honesty. `def p6_scan_clean : String := "sorry axiom unsafe"` is fine; theorem trivial fine.

Now the driver `tools/acceptance_pass6.py`. Based on pass5 structure, with:

Checks:
1. `deliverable_hashes_match_checkpoint_cp22` — from checkpoint's last entry `deliverable_hashes` for the 4 core files (note cp22 deliverable_hashes includes tools/card too; use core 4). Careful cp22's `deliverable_hashes` includes SturmInterlacing etc. Also include `SturmZeroCount.lean`? It's an input copy, not in cp22 deliverable_hashes (cp11 had it). Use the 4 core + negcontrol.
2. `input_byte_identity_direct_sha256` — 13/13.
3. `shared_queue_manifest_input_preserved` — mtimes < cp1.
4. `closure_rebuild_exit0_jobs`.
5. `closure_rebuild_left_sources_unchanged`.
6. `probe_compiles_clean` (exit 0, no "error").
7. `probe_axiom_cones_reported_once` (expected == reported, no missing).
8. `probe_axiom_cones_in_allowed_set` (no out-of-cone, no sorryAx).
9. `probe_source_no_forbidden_tokens`.
10. `audit_rerun_all_in_cone`.
11. `audit_covers_every_own_declaration`.
12. `type_nonrestatement_engine_vs_delivered` — parse `#check` blocks from probe output:
    - delivered `exists_zero_of_curvature_lt` != engine `sturm_zero_comparison`
    - `∨` in engine type, not in delivered type
    - `exists_jacobi_zero_on_Ioc_pi_sqrt` != `exists_jacobi_zero_before_pi_sqrt` and `Ioc` in ours vs `Ioo` in prior art
    - `wronskian_sin_linear_antitoneOn` type != `wronskian_antitoneOn_of_le` type
    - arrow count of `no_positive_solution_past_pi_sqrt` < `conjugate_point_bound`
13. **Mutation A**: `gate_sensitivity_axiom_audit_rejects_poison` — compile `tmp/mutation/poisoned_audit.lean`, parse cones with `parse_cones`, compute violations with ALLOWED; require poison present in some cone and violation detected. Also require the pristine run has no violations (already check 10).
14. **Mutation B**: `gate_sensitivity_coverage_rejects_truncation` — compile `truncated_audit.lean`; parse cones; compute own declarations from CORE+CROSS via DECL_RE; require uncovered non-empty (i.e., the coverage detector fires). Also record that the naive expected-vs-reported check does NOT fire (honest reporting), since expected comes from the same truncated source.
15. **Mutation C**: `gate_sensitivity_forbidden_scan_rejects_sorry` — run scanner on `tmp/mutation/scan_targets`; require hard_match_count ≥ 1, the match is in real_sorry.lean, and comment_only.lean contributes 0.
16. `card_verdict_task_done_and_checkpoint_intact` — same as pass5, but allow latest entry to be cp22 (before my new checkpoint). Since I'll write cp23 *after* the driver runs, at driver time latest is cp22. Good.
17. `probe_check_output_covers_expected_type_pairs` maybe folded into 12.

Output `evidence/acceptance-pass6.json`.

Also: the pass-6 driver should log the mutation compile outputs to `logs/80_pass6_*.log`.

Order: rebuild closure first (deletes oleans), then compile probe (rebuilds needed oleans — the probe imports deliverable so lean will use or rebuild the oleans; `lake env lean` doesn't rebuild, it fails if oleans missing! Careful: pass-5 did `lake build MODULES` which rebuilt the deliverable oleans, then compiled the probe with `lake env lean` which uses the existing oleans. So order: closure rebuild via `lake build MODULES` (rebuilds oleans), then probe. Good.

Then mutation compiles: `lake env lean ../tmp/mutation/poisoned_audit.lean` — writes olean to tmp/mutation (name collision: both poisoned_audit and truncated_audit define the same declaration names but compile separately; each writes its own olean; fine).

One risk: compiling `poisoned_audit.lean` from release with `lake env lean` — the file imports `Poincare.L4.GeodesicComparison.SturmInterlacing` which has an olean; good. It writes `tmp/mutation/poisoned_audit.olean`. No conflict with `truncated_audit.lean`'s olean (different names). But both define `Poincare.L4.GeodesicComparison.p6_mutation_axiom`; separate modules so fine.

Let me now write the files.

One more consideration: should the driver also verify that the *pristine* scanner run on `release` still gives hard=0 (as part of gates)? That's `forbidden_token_scan` gate, already in run_sturm_gates; I'll run the scanner on release too in the driver for completeness? The full gate driver isn't re-run in pass-5 (only closure rebuild + audit). I could re-run `run_sturm_gates.py` fully in pass-6 (adds value: fresh gate run). It takes ~30-60s. Hmm, pass-3 did "fresh gate re-run". I'll include an optional full gate run? The driver already does closure rebuild and audit re-run, which covers the heavier parts. The forbidden scan on release is cheap (0.3s) — include it as `release_forbidden_scan_still_clean` with the same scanner on `release` to make the mutation contrast complete (clean release vs poisoned target). Good.

Let me also double check `input/d5-tools/scan_forbidden.py` path exists relative to ROOT. Yes.

Now write mutation artifacts.
dsh: reasoning:
Wait: in `truncated_audit.lean`, both files define the same declaration names `p6_mutation_axiom` and `p6_mutation_poisoned`. When compiling `truncated_audit.lean` after `poisoned_audit.lean`, Lean will compile its own module fresh (it doesn't import poisoned_audit), so no clash. Fine.

Now the scan targets.
dsh: reasoning:
Now write the driver `tools/acceptance_pass6.py`. I'll model it on pass-5 but with the new checks.

Key parser for `#check` output: Lean prints, e.g.:
```
@Poincare.L4.GeodesicComparison.exists_zero_of_curvature_lt : ∀ {k₁ ...} {a b : ℝ},
  a < b →
    ...
```
Blocks separated by newlines where a new block starts at a line beginning with `@` or `Poincare.`. In the output above, the next block starts with `@Poincare...`. So parse: find all matches of `(?m)^@?((?:Poincare|Real|Set|MeasureTheory)[\w.'₀-₉]*) : ` ... Actually names may contain unicode subscripts (k₁) in binders but the declaration name itself is ASCII-ish. Let me find the positions of lines starting with `@Poincare.` and slice between them. Then the type = substring after the first `: ` up to the end of block, normalize whitespace.

I'll implement:
```python
def parse_checks(text):
    # blocks start at a line beginning with '@' followed by a fully-qualified name
    starts = [m.start() for m in re.finditer(r"(?m)^@[A-Za-z]", text)]
    ...
```
Hmm, but the `#print axioms` output lines start with `'...` (quote), so no conflict. Also errors would start with `../tmp...`. There's also the trailing `#check` for `conjugate_point_bound` which continues to the end of output possibly with more content? The probe output may end with `@...conjugate_point_bound : ∀ ...` block. Good.

Simplest robust: iterate over lines; a new block starts when line starts with `@` and contains ` : `; accumulate subsequent lines (indented) into that block.

Then map full name → normalized type string.

Checks on types:
```python
d_int = checks["@Poincare.L4.GeodesicComparison.exists_zero_of_curvature_lt"]
e_int = checks["@Poincare.D12.ComparisonGeodesics.sturm_zero_comparison"]
ok = (norm(d_int) != norm(e_int)) and ("∨" not in d_int) and ("∨" in e_int)
```
For the arrow count:
```python
def arrows(t): return t.count("→")
```
`no_positive_solution_past_pi_sqrt` vs `conjugate_point_bound`: require arrows(delivered) < arrows(leader). Let me verify from the `#check` output: delivered has 6 arrows, leader has? Let me see the truncated output: after `0 < T → 0 ≤ B → 0 < t₀ → t₀ ≤ T → B*t₀ ≤ 1/2 → JacobiSolutionOn ...` — likely at least 10. Fine.

Also check that `Ioc` appears in delivered zero-counting type and `Ioo` in the prior art one:
- delivered `exists_jacobi_zero_on_Ioc_pi_sqrt` contains `Ioc 0 (Real.pi / √K)`
- prior art `exists_jacobi_zero_before_pi_sqrt` contains `Ioo 0 (Real.pi / √K)` and an extra hypothesis `(∃ t ∈ Ioo 0 (Real.pi / √K), K < k t)`. So arrows(prior art) > arrows(delivered) too — nice non-duplication evidence: the delivered closed-interval statement has strictly fewer hypotheses than the prior art strict-excess statement, yet a stronger conclusion. I'll record both arrow counts and require arrows(delivered) < arrows(prior) and conclusion shapes differ (Ioc vs Ioo).

Wait: is that right? Prior art: hypotheses = 0<K, hk, Jacobi, u0=0, ∃t K<k → 5 arrows. Delivered: 0<K, hk, Jacobi, u0=0 → 4 arrows. Conclusion: prior `∃ c ∈ Ioo ...`, delivered `∃ c ∈ Ioc ...`. So yes delivered has fewer hypotheses and a weaker-looking conclusion (Ioc allows endpoint) — actually Ioc ⊇ Ioo so the conclusion is *stronger* in the sense that it includes the endpoint case. And no strict-excess hypothesis. Good: that's genuine added content (the equality case closes the endpoint).

Now Mutation A check details:
- Run compile of `poisoned_audit.lean`: `lake env lean ../tmp/mutation/poisoned_audit.lean` with cwd RELEASE, log `logs/80_pass6_mutation_poisoned.log`.
- parse cones via parse_cones (same regex as gate driver — I should import the function from `tools/run_sturm_gates.py`? Importing it would execute module-level code (makedirs etc.) but `main()` is guarded. However importing `run_sturm_gates` gives `parse_axiom_output` and `ALLOWED_AXIOMS` — reusing the *actual* gate logic rather than a copy strengthens the test: "the same function used by the gate detects the poison". Let me import it:
```python
sys.path.insert(0, os.path.join(ROOT, "tools"))
import run_sturm_gates as gates
cones = gates.parse_axiom_output(out)
violations = [{"declaration": d, "cone": c, "disallowed": [a for a in c if a not in gates.ALLOWED_AXIOMS]} for d,c in cones.items() if any(a not in gates.ALLOWED_AXIOMS for a in c)]
```
That's strong: the gate's own parser + allow-list. 

- Require: compile exit 0; poison reported in the cone of `p6_mutation_poisoned`; violations non-empty; `modelJacobiSolutionOn` cone still in allowed (so the poison is isolated). 

Mutation B check:
- Compile truncated file; parse cones; expected coverage = own declarations from CORE + CROSS. `uncovered = own - {cone keys' last components}` — as in pass-5. Require uncovered non-empty. Also record that naive expected/reported check passes (len(cones)==len(expected from truncated src)) to honestly document which layer catches it.

Mutation C:
- Run `python3 input/d5-tools/scan_forbidden.py tmp/mutation/scan_targets` (cwd ROOT), parse JSON from stdout; require hard_match_count >= 1, files flagged = {real_sorry.lean}, comment_only.lean flagged 0. Note the scanner's `os.walk(root)` with root relative; relpath works. Note exits 1 when hard matches — that's expected, so don't treat as failure.
  Wait: scan_forbidden writes JSON to stdout with `print(json.dumps(...))`; and returns exit 1. Parse out[out.index("{"):]. Good.
- Also run scanner on `release`: require hard_match_count == 0 (and soft == 0 as recorded before).

Now, one nuance: the mutation compiles will produce `.olean` files in tmp/mutation, and since `p6_mutation_axiom` differs, no issue.

Another nuance: `lake env lean ../tmp/mutation/poisoned_audit.lean` — Lean's default output path: it will write `../tmp/mutation/poisoned_audit.olean`. Should be fine (tmp writable). 

Now, the report structure. Let me write the driver.

Also — IMPORTANT — the driver must not modify the release tree (the closure rebuild deletes/recreates oleans but sources unchanged). It writes only evidence/, logs/, tmp/.

Let me write the driver now. I'll be careful with `check()` and final verdict.

Checks list (name, condition):
1. deliverable_hashes_match_checkpoint — compare 5 files (4 core + negcontrol) against cp22 deliverable_hashes. cp22 has keys: release/Poincare/L4/GeodesicComparison/SturmInterlacing.lean, ...ConjugateCrossCheck.lean, ...AxiomAudit.lean, negcontrol/SturmInterlacingNegativeControl.lean, tools/..., longrun/... So use those 4 core + negcontrol keys.
2. inputs_byte_identical_to_leader (13)
3. shared_queue_manifest_input_preserved (mtimes predate cp1)
4. closure_rebuild_exit0_jobs
5. closure_rebuild_left_sources_unchanged
6. probe_compiles_clean
7. probe_axiom_cones_reported_once
8. probe_axiom_cones_in_allowed_set
9. probe_source_no_forbidden_tokens
10. probe_check_surface_parsed (all 9 #check names found with types)
11. type_nonrestatement_strict_gap (delivered vs engine: distinct, no ∨ in delivered, ∨ in engine)
12. type_nonduplication_zero_counting (distinct from prior art; Ioc vs Ioo; fewer arrows)
13. type_engine_instantiated_wronskian (delivered wrapper type distinct from engine's general type)
14. type_hypothesis_strict_weakening_vs_leader (arrows fewer; no B/t₀ in delivered type)
15. audit_rerun_all_in_cone
16. audit_covers_every_own_declaration
17. release_forbidden_scan_still_clean
18. mutation_axiom_audit_rejects_new_axiom
19. mutation_truncation_coverage_detector_fires
20. mutation_forbidden_scan_rejects_sorry_not_comments
21. card_verdict_task_done
22. checkpoint_history_intact_latest_gates_pass

That's a solid pass.

Potential issue with check 14: `no_positive_solution_past_pi_sqrt` type string contains `T` and `K`, no `B`/`t₀`. Good. Require `"B" not in delivered_type`... careful: `B` might appear inside `BddBelow`? The type text of `no_positive_solution_past_pi_sqrt` includes `JacobiSolutionOn`, no B. But variable names in the printed type are from the theorem's binders: `{k u du ddu : ℝ → ℝ} {T K : ℝ}` — no B. However the string contains "ℝ" etc. A naive substring check for `"B"` could match "BddBelow" if present — it isn't, but to be safe use regex word boundary `\bB\b` and `\bt₀\b`. Fine.

Also for check 14, arrow count: delivered 6 vs leader ~12. Require strictly less.

Let me now also decide how `#check` blocks get matched to names: keys are like `@Poincare.L4.GeodesicComparison.exists_zero_of_curvature_lt`. I'll map both with and without leading `@`.

Let me write the driver.
dsh: reasoning:
I left a garbage line in Mutation B:
```python
    mut_b_ok = (mut_b_exit == 0 and bool(mut_b_uncovered)
                and "p6_mutation_axiom" in "".join(
                    mut_b_cones.get(k, [])[0] if mut_b_cones.get(k) else ""
                    for k in [""]) or mut_b_exit == 0)
    mut_b_ok = mut_b_exit == 0 and bool(mut_b_uncovered)
```
Let me remove the first assignment. Also check: `"Ioc" in ty["delivered_zerocount"]` — the type printed contains `Ioc 0 (Real.pi / √K)`; yes the `#check` output shows `∃ c ∈ Ioc 0 (Real.pi / √K)`. And `Ioo` in prior. Good.

Also the `#check` blocks parse: the output lines for blocks start with `@Poincare...`. However, note the `parse_checks` function: continuation lines include blank lines until the next `@` line. The last block ends at end of output. But there's a subtlety: after the last `#check` line, Lean output may contain nothing else. OK.

But there's a risk: the `#print axioms` output lines start with `'Poincare...` (apostrophe), not `@`, so they won't start a block. But wait, in the probe output, I see for the wrapped cone:
```
'Poincare.L4.GeodesicComparison.p6_negative_shift_interlacing' depends on axioms: [propext,
 Classical.choice,
 Quot.sound]
```
No `@` at line start. Good.

Another risk: The `#check` block lines might include a line starting with `@` as a continuation? No.

Now also fix: `CHECKPOINT_CORE` dict values None — used only for keys; fine but sloppy. Keep.

`mut_a_ok` requires `mut_a_exit == 0`. It should compile.

Also: `mut_a_cones` includes the cone of `p6_mutation_axiom` itself → `[p6_mutation_axiom]`.

One thing to double check: does `#print axioms` for an *axiom* itself print "depends on axioms: [p6_mutation_axiom]"? In Lean, `#print axioms foo` for an axiom `foo` prints `'foo' depends on axioms: [foo]`. I believe yes.

Let me fix the driver's mutation B block and then run it.
dsh: reasoning:
Now let me first quickly sanity check the mutation artifacts compile and the scanner behaves, before running the full driver (which takes ~1-2 min due to rebuild).

Test:
1. compile poisoned_audit.lean
2. compile truncated_audit.lean
3. run scanner on tmp/mutation/scan_targets

Let me do these in one bash call.
dsh: reasoning:
All mutation artifacts behave as intended:
- Poisoned audit: exit 0, poison cone = [Poincare.L4.GeodesicComparison.p6_mutation_axiom] → out-of-cone.
- Truncated: exit 0, only 2 declarations; naive expected 2 == reported 2 (would pass naively), coverage should fail.
- Scanner: hard=1 only in real_sorry.lean; comment_only clean.

Note in the driver's Mutation A check I test `"p6_mutation_axiom" in poison_cone` where poison_cone is a list of strings like `['Poincare.L4.GeodesicComparison.p6_mutation_axiom']` — the check `"p6_mutation_axiom" in poison_cone` checks list membership of the exact string, which fails because the cone element is the *fully qualified* name! `parse_axiom_output` returns cone entries as the full string `Poincare.L4.GeodesicComparison.p6_mutation_axiom`. So `"p6_mutation_axiom" in poison_cone` → False. Must check `any("p6_mutation_axiom" in a for a in poison_cone)`.

Also `mut_a_viol` computes `disallowed` correctly since the full name isn't in ALLOWED. Good.

Let me fix that check.

Also, one issue with the driver's `parse_checks`: The output of `#print axioms` lines could be interleaved with `#check` blocks? No, they're sequential in the file: prints first, then checks. Fine.

Another issue: `parse_checks` keys — the check lines print `@Poincare...` exactly. Good.

Let me also double-check the arrow-count comparisons against the actual output. Let me extract the tails for the remaining check lines from the earlier probe output... I truncated at 60 lines. Let me just run a quick parse of the probe output file after recompiling? Rather than recompile, I can compute expected: I'll run the full driver and see.

But first fix the poison check.
dsh: reasoning:
Two failures:

1. `probe_check_surface_parsed`: `delivered_wronskian` (`@Poincare.L4.GeodesicComparison.wronskian_sin_linear_antitoneOn`) wasn't parsed. Why? Probably because the `#check` output for that declaration spans lines and my parser requires the first line to start with `@` and contain ` : `. Let me look at the probe log to see the output. Also, `delivered_arrows=0` for it suggests the type was parsed as empty (missing).

Wait, `type_engine_instantiated_wronskian` FAIL says delivered_arrows=0 — that's because `ty['delivered_wronskian']` is "" and arrows("")=0. So the parse missed the block. Let me check the log.

Possibly the output line for that check is:
```
@Poincare.L4.GeodesicComparison.wronskian_sin_linear_antitoneOn :
  AntitoneOn (wronskian ...) (Icc 0 Real.pi)
```
The first line is `@...wronskian_sin_linear_antitoneOn :` — with ` : ` at end? My parser requires `" : " in line` (space colon space). If the printer breaks the line right after the colon with no trailing space (i.e., line ends with `:`), the condition fails. Let me check the log.

Also note `type_nonrestatement_strict_gap` says `delivered_arrows=12` — hmm, that's the count of "→" in the normalized type of `exists_zero_of_curvature_lt` (implicit binders also generate arrows? In the printed type, each `→` after a hypothesis; 12 arrows vs engine 13). Interesting: the delivered gap theorem has 11 hypotheses (hab, hk, h1, h2, hu1a, hu2a, hu2b, hu2pos, hdu2b, hstrict) = 10 plus maybe the ∀-block separator arrows. Whatever, the comparison passed.

Let me look at the probe log around the `#check` lines.
dsh: reasoning:
The line is `Poincare.L4.GeodesicComparison.wronskian_sin_linear_antitoneOn : AntitoneOn` — it starts with `Poincare.` not `@Poincare.` because the declaration has no implicit arguments to expose, so `#check @foo` prints without the `@` prefix? Actually Lean prints `@foo` only when there are implicit binders to show; with none it prints `foo`. So my parser needs to accept lines starting with `Poincare.` too. Also, keys in CHECKS include the leading `@`. Fix: normalize keys by stripping leading `@` when parsing, and look up by stripped name.

I'll update `parse_checks` to accept lines starting with `@` or a fully qualified name pattern, and normalize keys by removing the leading `@`.

Simple approach: in parse_checks, detect lines matching `^@?(Poincare|Mathlib|Real|Set|MeasureTheory)\.[\w.']* : ` — but easier: treat a line as a new block if it matches `^@?[A-Za-z][\w.']* : ` AND it does not start with whitespace. Since continuation lines are indented (start with two spaces), I can require the line to not start with a space and contain ` : `. The `#print axioms` lines start with `'`, excluded. Let's implement: `if not line.startswith((" ", "\t")) and " : " in line and not line.startswith("'")`. Then cur = name with leading `@` stripped.

Then `ty` lookup: `types.get(v.lstrip("@"))`.

Let me patch both.
dsh: reasoning:
Also, `type_engine_instantiated_wronskian` compares delivered (AntitoneOn with concrete data on Icc 0 π) vs engine (∀-quantified). Distinct ✓. But I should also assert that the delivered type's arrows < engine's? delivered has 0 top-level `→` (closed statement) vs engine 8. The check currently requires distinct only; that's fine, but let me strengthen slightly: require `arrows(delivered) < arrows(engine)` and "AntitoneOn" in both. Actually delivered has no `→` because the conclusion is a closed proposition. Add `"AntitoneOn" in both`. Let me update the check.

Additionally, an important sanity check on `type_nonrestatement_strict_gap`: `delivered_arrows=12` and `engine_arrows=13` — hmm wait, this counts arrows in the full `#check @...` type including the ∀ binder structure. The delivered has 10 hypotheses; the printed format inserts `→` for each hypothesis and for the implicit binder block? Let me not worry.

Now re-run the driver. It takes ~35s total. But wait: the closure rebuild will run again (23s). Fine.

Actually, I realize I should double check the `probe_check_surface_parsed` will now find all 9. The prior-art `exists_jacobi_zero_before_pi_sqrt` starts with `@Poincare...` (has implicit binders) fine. `wronskian_sin_linear_antitoneOn` starts with `Poincare...` now handled.

One more concern: my new block-detection heuristic could pick up a line from the `#print axioms` output that contains " : " and doesn't start with space/quote. Axiom print lines are like `'Poincare...' depends on axioms: [propext,` → starts with `'` → excluded. The continuation line ` Classical.choice,` starts with space → not new block. Good.

Could it pick up error lines? Only if compile errors — then probe check fails anyway.

Let me strengthen the wronskian check and re-run.
dsh: reasoning:
I made a typo in the path (`lean_polincare` instead of `lean_poincare`). Let me retry with correct path.
dsh: reasoning:
Pass-6 verdict: PASS, all 22 checks green.

Now update the todos and then update the card generator to include §8g with the pass-6 findings, regenerate the card, write checkpoint cp23, run final integrity.

Let me look at the card generator sections 8e/8f (lines ~240-330) to add §8g similarly. Let me read the relevant part of make_result_card.py.
dsh: reasoning:
Now let me add the §8g section after ia5_section, and update the JSON output and the reproduction section and provenance wording. Let me view lines 600-650 (the JSON assembly and the reproduction section) and where ia5_section is interpolated.
dsh: reasoning:
Now I'll add the §8g section code after ia5_section (line 348), including:
- summary
- checks list
- closure rebuild/audit
- probe theorems
- mutation results
- type-level checks table

Then interpolate `{ia6_section}` after `{ia5_section}` in the md template, update the provenance paragraph to mention pass-6 artifacts, add the reproduction line, and add `"independent_acceptance_pass6": ia6` to the JSON.

Let me write the §8g code. Include details from the report:
- `ia6["checks"]` — list of check dicts.
- mutation details.
- type_checks arrow counts.

Draft code:

```python
ia6_path = os.path.join(EV, "acceptance-pass6.json")
ia6 = json.load(open(ia6_path)) if os.path.exists(ia6_path) else None
if ia6 is not None:
    _lines = ["## 8g. Independent acceptance pass 6 (continuation invocation, fresh probe, gate mutation tests)",
              "",
              f"- verdict: **{ia6['verdict']}**; artifact `evidence/acceptance-pass6.json`",
              f"- generated: `{ia6['generated']}`; driver `tools/acceptance_pass6.py`",
              "",
              "- this pass was run by a further continuation invocation on the frozen tree.  Its probe "
              "`tmp/acceptance_probe_r6.lean` was written from scratch (it does not import or reuse the "
              "pass-1/2/3/4/5 probes) and uses fresh data (negative shift `a = -3` with `(k₁,k₂) = (9,4)`; "
              "zero counting at `(K,k) = (4,9)`; horizon `H = 3` and attainment on `jacobiSol 9`; "
              "interior-bound form `(K,k) = (16,25)`; equality case `k ≡ K = 7`; Wronskian data "
              "`(9, jacobiSol 9)` vs `(4, jacobiSol 4)` on `[0,π/6]`).  It adds two things the earlier "
              "passes do not:",
              "  1. **machine-checked non-restatement**: the raw engine is invoked directly on the "
              "acceptance data, giving its two-sided alternative `A ∨ B`; the probe proves `¬B` and the "
              "delivered theorem supplies `A`, so on this data the delivered statement is strictly more "
              "informative than the engine's own conclusion;",
              "  2. **gate mutation (sensitivity) testing**: the fail-closed machinery is shown to "
              "*reject* poisoned artifacts, not merely to accept the pristine one.", ""]
    for c in ia6["checks"]:
        _lines.append(f"- [{'OK' if c['ok'] else 'FAIL'}] **{c['check']}** — {c['detail']}")
    cr6 = ia6.get("closure_rebuild", {})
    ar6 = ia6.get("audit_rerun", {})
    _lines += ["",
               f"- pass-6 full project-closure rebuild: `{cr6.get('command')}` exit "
               f"**{cr6.get('exit_code')}**, {cr6.get('jobs')} jobs, {cr6.get('oleans_deleted')} "
               f"project oleans deleted first, {cr6.get('seconds')}s, log `{cr6.get('log')}`; the 79 "
               "`release/**/*.lean` sources are byte-identical before and after the rebuild",
               f"- axiom-audit module re-run on the freshly rebuilt oleans: exit **{ar6.get('exit_code')}**, "
               f"{ar6.get('reported')}/{ar6.get('expected')} declarations reported, "
               f"missing={ar6.get('missing')}, out-of-cone={ar6.get('out_of_cone')}, log `{ar6.get('log')}`",
               "",
               f"- independent probe: `{ia6['probe']['file']}` (sha256 `{ia6['probe']['sha256']}`), "
               f"`{ia6['probe']['command']}`, compile exit **{ia6['probe']['compile_exit']}**, log "
               f"`{ia6['probe']['log']}`; {ia6['probe']['declarations_audited']} declarations audited "
               f"fail-closed, out-of-cone = {ia6['probe']['out_of_cone']}", ""]
    for t in ia6["probe"]["theorems"]:
        _lines.append(f"  - `{t['name']}` — cone `{t['cone']}`")
    mut = ia6.get("mutations", {})
    pa = mut.get("poisoned_audit", {})
    ta = mut.get("truncated_audit", {})
    st = mut.get("scan_targets", {})
    _lines += ["",
               "- **gate mutation tests** (artifacts under `tmp/mutation/`, never in `release/`):",
               f"  - poisoned audit (`{pa.get('file')}`, sha256 `{pa.get('sha256')}`): a new axiom "
               f"`p6_mutation_axiom` is introduced; compile exit **{pa.get('compile_exit')}**, the "
               f"poisoned cone `{pa.get('reported_cones', {}).get('Poincare.L4.GeodesicComparison.p6_mutation_poisoned')}` "
               f"is out of the allowed set, violations reported = **{len(pa.get('violations', []))}** "
               "(the gate driver's own parser and allow-list are reused, so this exercises the "
               "production detector, not a copy);",
               f"  - truncated audit (`{ta.get('file')}`, sha256 `{ta.get('sha256')}`): reports only "
               f"{ta.get('reported')} declarations — the naive expected-vs-reported check would pass "
               f"(**{ta.get('naive_check_would_pass')}**) because the expected list shrinks with the "
               f"source; the fail-closed coverage check catches it with "
               f"**{ta.get('coverage_uncovered')} own declarations uncovered**;",
               f"  - scanner sensitivity (`{st.get('dir')}`, scanner exit {st.get('scanner_exit')}): "
               f"hard matches = **{st.get('hard_match_count')}**, flagged files = {st.get('hard_files')}, "
               f"soft flags = {st.get('soft_files')} — `real_sorry.lean` (a genuine `sorry`) is caught "
               f"while `comment_only.lean` (the same tokens in comments/strings) is not;",
               f"  - the released tree itself remains clean under the same scanner: hard = "
               f"**{ia6.get('release_scan', {}).get('hard_match_count')}** over "
               f"{ia6.get('release_scan', {}).get('lean_files_scanned')} Lean files.", "",
               "- **statement-level (type) checks** parsed from the probe's `#check @...` surface "
               "(arrow counts are top-level `→` counts, a heuristic for hypothesis count):", ""]
    tc = ia6.get("type_checks", {})
    for k, v in (tc.get("arrow_counts") or {}).items():
        _lines.append(f"  - `{k}` — {v} arrows")
    _lines += ["",
               "- the type surface confirms non-restatement structurally as well: the delivered "
               "strict-gap type is not the engine's type and contains no `∨` while the engine's does; "
               "the delivered closed-interval zero-counting type is not the prior-art type (`Ioc` vs "
               "`Ioo`) and drops the prior art's strict-excess hypothesis; the engine-derived "
               "positivity bound has strictly fewer hypotheses than `conjugate_point_bound` and "
               "mentions neither `B` nor `t₀`.", ""]
    ia6_section = "\n".join(_lines)
else:
    ia6_section = ""
```

Then template `{ia5_section}\n{ia6_section}\n## 9....`.

Update the provenance paragraph: add pass-6 artifacts: `tmp/acceptance_probe_r6.lean` (§8g), `tmp/mutation/*` (§8g).

Update reproduction: add `python3 tools/acceptance_pass6.py` line.

Update JSON card: add `"independent_acceptance_pass6": ia6,`.

Let me make these edits.
dsh: reasoning:
Now regenerate the card. But careful: the card's generator reads `evidence/acceptance-pass6.json` — it exists. It also reads checkpoint (`cp['checkpoints'][-1]['id']` = cp22) and includes it in §9 text. Then after card regeneration, I write checkpoint cp23 which records the card hash. That's the established flow.

Let me run make_result_card.py.
dsh: reasoning:
Card regenerated with §8g. Let me check the §8g content quickly (a few lines) to ensure it looks right, then write checkpoint cp23.

Also need to add the new tools/artifacts to `write_checkpoint.py`'s DELIVERABLES list so cp23 records them (pass-6 driver, probe, mutation artifacts, evidence, card). Also `tools/make_result_card.py` is already in the list. Let me update write_checkpoint.py.

Let me read the rest of write_checkpoint.py to see what it records.
dsh: reasoning:
The card §8g looks good. Now update `write_checkpoint.py` DELIVERABLES to include pass-6 artifacts so cp23 records them:
- tools/acceptance_pass6.py
- evidence/acceptance-pass6.json
- tmp/acceptance_probe_r6.lean
- tmp/mutation/poisoned_audit.lean
- tmp/mutation/truncated_audit.lean
- tmp/mutation/scan_targets/real_sorry.lean
- tmp/mutation/scan_targets/comment_only.lean

Then run `write_checkpoint.py "pass-6 ..." complete`.

Note: write_checkpoint records `gates` from evidence/verification.json (the last full gate run, still PASS) and axiom report (62/62). That's consistent: the deliverable sources didn't change in pass-6.

Also, one consideration: the checkpoint's `compile_exits` will be from verification.json steps (the run_sturm_gates run from before) — unchanged. That's fine; the pass-6 report is referenced through hashes.

Let me update and run.
dsh: reasoning:
Now run the final integrity check. It checks:
- card_md_exists, card_ends_task_done, card_json_verdict
- gates_verdict
- axiom_verdict
- axiom_audit_covers_all
- card_module_hashes_current — compares card module hashes to actual files; since pass-6 didn't change deliverable sources, fine.
- checkpoint_records_final_card cp22 [] — this checks that the latest checkpoint records the *current* card hash. I wrote cp23 *after* regenerating the card, so cp23's deliverable_hashes include the new card hash. 
- checkpoint_history_grows (22 → 23)
- checkpoint_latest_gates PASS
- forbidden_scan_clean

Let me run.
dsh: reasoning:
All integrity checks pass. Now, since the card regenerated, the pass-6 report's `deliverable_hashes` don't change (they're only source files). The card hash changed, and cp23 records it.

Wait — one issue: the pass-6 driver's check `card_verdict_task_done` ran *before* the card was regenerated with §8g; the JSON verdict was already TASK_DONE. Fine.

But now the card JSON includes cp22 in §9 text ("the latest entry at card-generation time was cp22") because the card was generated before cp23. That's accurate: at card-generation time, cp22 was latest. And cp23 records the card. Good.

Now: is there anything else needed? Let me consider whether the pass-6 mutations could be considered risky: the poisoned audit file stays in tmp; it defines an axiom but is never imported by release. The forbidden scan covers only `release` in the gate (plus `negcontrol` scanned separately by verify tooling). `final_integrity_check` reports forbidden_scan_clean hard=0 soft=0 — that's from evidence/forbidden-scan.json (release only). Fine.

But hmm — is there a risk that a future project-wide scan over the worktree (e.g., a release collector that packages `tmp/`?) would flag `tmp/mutation/*`? The task says artifacts are ours in tmp; earlier passes also left probes in tmp. The mutation files contain `axiom` and `sorry` tokens. If the *leader* release process scans the whole worktree, that could be a problem. But `tmp/` was already used for probes with `axiom`? No, probes don't contain axioms/sorry. My mutation files do contain the literal tokens `axiom` and `sorry` in code. This is intentional (they're mutation targets), but it could contaminate a broad scan.

Risk assessment: The D5 scanner in this worktree is invoked on `release` (and negcontrol separately). `tools/d6_verify.py` might scan more broadly? Let me check whether anything scans the whole worktree and could pick up tmp/mutation. If a future aggregation step scans the worktree, the mutation files could produce false positives. To be safe, I can rename/obfuscate? No — the whole point is the literal tokens.

Better: make it explicit in the file header that these are mutation artifacts and consider whether to keep them. They are evidence for §8g (hashes recorded in cp23). Removing them would invalidate cp23's hashes. Keeping them in tmp is standard for this task (all probes live in tmp). The release tree is what matters, and the gate explicitly scans `release`. I'll keep them but verify that no *existing* tooling scans tmp in a way that would fail. Let me grep for scripts that scan the worktree root.

Actually, `tools/final_integrity_check.py` reported forbidden_scan_clean hard=0 soft=0 — what does it compute? It reads evidence/forbidden-scan.json (release). Let me check if it scans tmp. Let me grep.

Also let me double check that no file in `release/` was modified by the mutation compile (the oleans are in tmp). `closure_rebuild_left_sources_unchanged` verified 79 source files unchanged. Good.

Let me check final_integrity_check.py's forbidden scan source and d6_verify usage.
dsh: reasoning:
The scanner is only applied to `release` (and my tmp targets). The mutation artifacts in tmp are safe.

Now, one remaining question: should I add my pass-6 mutation artifacts to a note in §9 provenance? I did update the provenance paragraph. Good.

Next: verify that `evidence/acceptance-pass6.json` is internally consistent and that the card JSON includes `independent_acceptance_pass6`. Quick check.

Also verify the checkpoint cp23's hashes match the current files (final_integrity already checked `checkpoint_records_final_card cp23 []` which validates card hashes; write_checkpoint recorded all listed files' hashes).

Let me do a final consistency check comparing cp23 hashes to disk for all entries, and confirm checkpoint latest=cp23.

Then update todos and write the final report. But before that, consider: the task says "Save a compile-checked checkpoint at least every hour" — done. "Preserve queue, checkpoints and provenance" — queue untouched (preservation check), checkpoints appended only.

One more acceptance-oriented thought: the task's acceptance requires "full signatures, source hashes, compile exits, fail-closed axiom audit (cone subset...), non-vacuous witnesses, and a semantic review of expanded hypotheses". These were delivered in passes 1-5 and remain. My pass-6 adds gate-mutation evidence and type-level non-restatement checks. The final card is authoritative.

Let me run a final comprehensive verification of the frozen state: re-run the full gate driver `run_sturm_gates.py` one more time? It was last run before pass-6 (evidence/verification.json from earlier). The pass-6 closure rebuild + audit re-run is equivalent for the artifacts. But re-running the full gate driver would refresh verification.json timestamps and re-confirm the forbidden scan and input hashes — it takes ~30-60s. Hmm, but careful: `run_sturm_gates.py` writes evidence files (verification.json, gates.json, axiom-report.json, source-hashes.json, input-hash-verification.json). If I re-run it now, `evidence/acceptance-pass6.json` stays; the card's §8 gate table would then reflect the *new* run only if I regenerate the card after. If I regenerate the card, its hash changes and I'd need cp24. That's more churn; and it's not necessary. The last full gate run is valid because the deliverable sources haven't changed since (cp22 hash match verified). I'll leave it.

Actually, wait. Let me double check one potential inconsistency: the pass-6 report's `probe.theorems` uses `cones.items()` order — in the card §8g, the list shows 8 declarations. Fine.

Let me do the final consistency check and inspect what files changed in the worktree during this invocation (for provenance): checkpoint.json, card md/json, evidence/acceptance-pass6.json, logs/8x, tools/acceptance_pass6.py, tools/make_result_card.py, tools/write_checkpoint.py, tmp/*. None in release/, manifest/, input/, longrun/queue.

Let me verify the cp23 hash consistency for all entries and list the changed files by mtime.
dsh: reasoning:
The verification script had a bug: `card['module_hashes']` keys are `core`, `cross_check`, etc., not paths. Let me check the card's module_hashes structure and verify against actual files. `final_integrity_check.py` already did this ("card_module_hashes_current []"). But let me run a corrected check for completeness.

Also note: `evidence/final-integrity.json` was updated by final_integrity_check.py — it's now newer than cp23's recorded hash? Wait, cp23's DELIVERABLES list does not include `evidence/final-integrity.json`, so no issue. But the final_integrity_check ran before... hmm, actually the order was: write cp23 → run final_integrity_check (which rewrote evidence/final-integrity.json) → then cp23 was already written. cp23 doesn't record final-integrity.json, so fine.

Let me verify card module hashes correctly.
dsh: reasoning:
Everything is consistent:
- cp23 latest, gates PASS, all recorded hashes match disk.
- Card verdict TASK_DONE, includes pass-6 section and JSON.
- Pass-6 report: 22/22 checks OK, probe hash matches.
- `final_integrity_check.py` all_ok true.
- Deliverable sources unchanged by this invocation (release tree 79 lean files untouched; only oleans rebuilt).

Let me update the todo list to complete and write the final answer. Since the task states "Only your own longrun/results/L4-child-sturm-zero-interlacing.md can complete this task" and it ends with `**TASK_DONE**`, I should report TASK_DONE with a summary of what was verified/delivered.

Should I create/update a goal? There's no goal active (this is a single continuation turn). No need.

Let me also double check that the pass-6 mutation test didn't alter `evidence/verification.json` etc. — it didn't (only acceptance-pass6.json + final-integrity.json).

One more consideration: the Objective mentions "Model: use configured host model only; do not invoke until quota recovery is recorded." I did not invoke any subagents/models. Good.

Final answer: concise summary with:
- continuation pass-6 independent acceptance, no restart of prior work
- fresh probe + new data, machine-checked non-restatement
- gate mutation tests (new)
- results: 22/22 PASS, closure rebuild 2774 jobs, 62/62 audit cones within allowed set, 13/13 inputs byte-identical, hashes match cp22→cp23
- deliverables/artifacts and hashes
- classification: unchanged from previous (literal branch (1) refuted; sharper interlacing + zero-counting + Wronskian delivered); no weakened statements; not a Poincaré proof
- TASK_DONE

Let me finalize the todos first.
**TASK_DONE**

Continuation invocation on the frozen tree (no completed work restarted). This round ran **independent acceptance pass 6**, adding two checks the earlier passes did not perform, and all 22 checks passed.

## What this invocation did

**1. Fresh probe `tmp/acceptance_probe_r6.lean`** (sha256 `49f53273…`) — written from scratch, new constructed data:
- two-curvature interlacing on the negative shifted interval `a = -3`, `(k₁,k₂) = (9,4)`, explicit zero `-3+π/√9` strictly inside `(-3, -3+π/√4)`;
- zero counting at `(K,k) = (4,9)` with first zero `π/3` strictly inside `(0, π/2]`;
- horizon form `H = 3` + attainment `u(firstPositiveZero u)=0`, `0 < firstPositiveZero u`;
- interior-bound form `(K,k) = (16,25)`; equality case `k ≡ K = 7`;
- Wronskian `(9, jacobiSol 9)` vs `(4, jacobiSol 4)` on `[0,π/6]`, including `W'(π/12) = -5·u₁u₂`;
- **machine-checked non-restatement**: the raw engine `sturm_zero_comparison` is invoked directly on the acceptance data, yielding `A ∨ B`; the probe proves `¬B` at `π/4` and the delivered theorem supplies `A` — so the delivered statement is strictly more informative than the engine's own conclusion.

**2. Gate mutation/sensitivity testing (new)** — `tmp/mutation/`, never in `release/`:
- poisoned audit (new axiom) → the gate driver's *own* parser+allow-list reports 2 out-of-cone violations;
- truncated audit → naive expected-vs-reported check would pass (`True`), but the fail-closed coverage check catches it (46 own declarations uncovered);
- scanner: hard match = 1, only `real_sorry.lean`; `comment_only.lean` (same tokens in comments/string) is clean — sensitivity without false positives; the release tree remains hard=0 over 79 files.

**3. Type-level non-restatement checks** from `#check`: delivered strict-gap type ≠ engine type and has no `∨` (engine does); zero-counting `Ioc` ≠ prior-art `Ioo` with fewer hypotheses; engine-derived bound has 7 arrows vs leader's 15 and mentions neither `B` nor `t₀`.

## Fresh fail-closed evidence
- full project-closure rebuild exit **0**, 2774 jobs (48 project oleans deleted first), 79 release sources byte-identical before/after;
- audit re-run **62/62**, missing `[]`, violations `[]`, cone ⊆ `{propext, Classical.choice, Quot.sound}`, probe cones likewise;
- 13/13 read-only inputs byte-identical to the leader release; deliverable hashes still match cp22;
- `manifest/`, `input/`, `longrun/queue*` untouched (2842 inherited files, 0 modified since cp1);
- `evidence/acceptance-pass6.json` (PASS), card §8g regenerated, checkpoint **cp23** written, `tools/final_integrity_check.py` → `all_ok: true`.

## Classification (unchanged, honest)
The literal branch (1) `k₂=0, u₂=t` on `(0,π)` remains **refuted** (engine needs `u₂ b=0`; `sin` has no zero in `(0,π)`, first positive zero exactly `π`); the offered sharper two-curvature interlacing, the zero-counting/first-zero bound `≤ π/√K` (cross-checked against `conjugate_point_bound`, not assumed or duplicated), and explicit Wronskian consumption are delivered. Scalar ODE comparison only — not a Poincaré proof; no forbidden constructs, no weakened statements.

Result card: `longrun/results/L4-child-sturm-zero-interlacing.md` (ends `**TASK_DONE**`).

**TASK_DONE**
