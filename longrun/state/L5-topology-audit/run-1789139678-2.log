dsh: reasoning:
Interesting - my cwd is /data3/guoshaoyang/workdir/lean_poincare/longrun/worktrees/L5-topology-audit but the worktree dir path says worktrees/leaders/L5-topology-audit. Let me check both.

The worktree only has checkpoint.json, comms, research-brief. Where's the repo? Let me look around.
dsh: reasoning:
The worktree is essentially empty (just initialized). The task says "Continue from existing files and checkpoint.json in this worktree; do not restart completed work." But it's a fresh init from D6_weekly_release. I need to find the actual Lean release repository. Let me read the prompt.md in comms/inbox and find CANONICAL-DAG.md and HANDOFF-PRIMARY-CONTROLLER.md.

Let me look at the parent longrun dir for these docs, and D6_weekly_release.
dsh: reasoning:
No CANONICAL-DAG.md found at maxdepth 4. Let me search more widely.
dsh: reasoning:
No CANONICAL-DAG.md exists yet. Let me check the parent longrun dir more thoroughly, and the L1-L5 leader dirs, plus the dispatcher prompts that may define what's expected. Also check `results/L5-topology-audit*` already existing elsewhere.

Let's look at:
- /data3/guoshaoyang/workdir/lean_poincare/longrun/prompts and prompts_v2
- /data3/guoshaoyang/workdir/lean_poincare/longrun/results
- /data3/guoshaoyang/workdir/lean_poincare/longrun/longrun (maybe)

And search for "L5-topology-audit" anywhere.
dsh: reasoning:
Now let me understand the overall project. I'm L5, top-level leader. The project is "lean_poincare" — formalizing Poincaré conjecture in Lean 4, with a release from D6_weekly_release. The named blockers M8, A3, I6, I7 need to be identified.

Let me look at:
1. `longrun/infra/swarm/leader_tasks.json` for L5 task details
2. The queue.json for M8/A3/I6/I7 definitions
3. The DAG / handoff docs
4. Results cards (D12-semantic-ledger, D3-surgery-ledger, D13-vankampen-recognition, D12-triangulation-topology, D12-surgery-recognition)
5. The release source in D6_weekly_release/release

Let me start with leader_tasks.json and search for M8, A3, I6, I7.
dsh: reasoning:
Now the blocker IDs M8, A3, I6, I7 must be defined somewhere. Let me search the whole longrun for these IDs — perhaps in queue.json, D12_semantic_ledger results, etc. Let me check the queue.json (which references prepare_remote.py, L5-topology-audit).
dsh: reasoning:
Now let's find M8 and the manifest/blockers.json. The D12-semantic-ledger is highly relevant. Let me find manifest/blockers.json in D6 release or longrun/integrated_overlay.
dsh: reasoning:
There is no M8 in D6 blockers. M8 is probably a "milestone" blocker — maybe from D12-semantic-ledger or the newest swarm. Let me grep the whole longrun tree for "M8" including worktrees (excluding the ones I excluded before). Actually I excluded ./worktrees/D* — that was wrong; M8 might be in newer worktrees like D13 or in the swarm state. Let me search all files.
dsh: reasoning:
The M8 match in olean files is a false positive (binary). Let me search text source for "M8" — `docs/orchestration_reuse.md` is a hit! Let me read that.
dsh: reasoning:
M8 in docs/orchestration_reuse.md is a *program milestone* (pilot experiment), not a mathematical blocker. But the leader registry says "named_blockers": ["M8", "A3", "I6", "I7"]. Given the other three (A3, I6, I7) are blockers from manifest/blockers.json, M8 is likely a milestone-level blocker defined in some newer registry. Let me check the D12-semantic-ledger.md and D13 results which may define M-milestones. Search for "M8" pattern like "M4" milestones in results. Actually, in the *swarm* context, the leaders are L1..L5 with objectives "M1 baseline", "M2 upstream adapters", "M3 analytic critical path", "M3 geometry critical path", "M4 topology and adversarial audit". So M1-M4 are program milestones of this swarm wave. Then M8 might be... hmm.

Actually, wait: the "M" prefixes might be milestone tags in the "M0-M12" program plan (from orchestration_reuse.md §4). M8 there = "先导实验" pilot experiment. That doesn't fit an auditor's named blocker. More likely there's another registry: `longrun/D12-plan.json`, `longrun/longrun/...`. Let me check D12-plan.json and the `longrun/longrun` directory (nested).

Let me grep for "I6", "I7", "A3" in D12-semantic-ledger.json and check the newest cards (D13-*). Also check `longrun/D12-plan.json`.
dsh: reasoning:
Let me check `longrun/longrun/leader-registry.json` — maybe it has different named blockers with M8.

Actually I already grepped and found M8 in longrun/longrun/leader-registry.json too. Both registries are the same. So M8 is only in the registry.

Hmm. Maybe M8 is defined in the D13 milestone/roadmap: e.g., the "M4 topology" refers to a milestone M4 in a newer M0-M9 program. Let me search for "milestone" definitions with "M1"/"M4" in results or docs, e.g. "M4 topology".

Let me grep for "M4 " and "M1 " in the longrun results.
dsh: reasoning:
This is very detailed and helpful. Now I understand the context well. I need to:

1. M4 topology and adversarial audit: recheck recognition, triangulation and surgery evidence.
2. Named blockers: M8, A3, I6, I7.

I6 = NeckAnalysis/ExtinctionTheorem/MissingInputs statement-only (surgery/neck).
I7 = FiniteMeshConvergence, PerelmanEvolutionBoundary, FiniteRepresentsContinuousPerelman statement-only; FDissipation = 0.
A3 = audit residual: adversarial audit only covers D4 evolution cluster; earlier clusters D2/D3 have axiom audits but no counterexample search.

M8 — need to determine. Possibly it's a milestone-level blocker ID for "topology pipeline", maybe from `poincare_next_wave.py` or `check_leaders.py`. Let me check those scripts and `infra/swarm`. Also `prepare_remote.py`.

Actually, the leader registry named_blockers for L5 = [M8, A3, I6, I7]. The objective says "M4 topology and adversarial audit". So M4 is a milestone in this swarm wave (M1 baseline, M2 upstream adapters, M3 analytic/geometric critical path, M4 topology audit). Then M8 might be from the 13-milestone plan in docs/orchestration_reuse.md (M8 = pilot experiment). But that doesn't fit as a "blocker in scope" for topology audit.

Wait — maybe M8 is a *milestone* in a different numbering: "M8" might be the milestone for "Surgery + geometrization → Poincaré" in a longer M-plan. Let's check `check_leaders.py` and `poincare_next_wave.py` for how named_blockers were generated.
dsh: reasoning:
Let me look for where named_blockers were defined (leader-registry.json is generated by something). Grep in the whole project for `"M8"` in Python scripts.
dsh: reasoning:
M8 not found in py files. Let me check whether M8 refers to something in the D13 cards (e.g. "milestone 8"). Search for "milestone" in the recent D13 results and integrated_overlay. Also check `longrun/results/D13-*` cards.

Actually let me broaden: search for `"M1"` ... in integrated_overlay and results to find a milestone table with M1-M12.
dsh: reasoning:
The `integrated_overlay` is a copy of the release — this is likely the most current integrated source! Let me check what modules exist in `integrated_overlay/release/Poincare/` — especially topology ones: D12/SurgeryRecognition, D12/TriangulationTopology, VKPort, D7/SphereRecognition etc.

Actually the D13-integrated-kernel-audit worktree's release was referenced with `Poincare/VKPort/HatcherLib/Ch1/VanKampen...` oleans. So there's a VKPort (van Kampen port) in the integrated overlay. Let me inventory the topology-relevant sources.

Given time budget (4 hours), I should plan:

Phase 1 (orientation, ~20 min): identify the release to build, topology-related sources, existing evidence and blockers.
Phase 2 (~1h): build from release/, run authored-file checks, axiom audit fail-closed, source hashes.
Phase 3 (~1h): recheck recognition/triangulation/surgery evidence + adversarial screens (A3: counterexample search for D2/D3? I6/I7 checks).
Phase 4: outbox child tasks, result card, checkpoint.

The key insight from the prompt: "Do not promote conditional recognition to Poincare." So the expected outcome is likely TASK_BLOCKED with named blockers preserved, but with rigorous audit evidence.

Let me first find the most current release. Candidates:
- worktrees/D13-integrated-kernel-audit/release (with VKPort)
- longrun/integrated_overlay/release
- worktrees/D13-manifold-ibp-volume-form/release
- worktrees/D6_weekly_release/release (old)

My worktree is empty, so I need to populate it. The protocol says "Workspace initialized from D6_weekly_release". But the acceptance says "Build from release/ with pinned toolchain". Probably I should copy the most recent integrated release into my worktree's release/ so I can build there (can't edit other worktrees). Let me check what D13-integrated-kernel-audit has (probably the freshest integrated state), and check its result card.

Let me look for result cards of D13 tasks in their worktrees.
dsh: reasoning:
D13-integrated-kernel-audit has the richest tree with D11, D12, D13, VKPort. Its checkpoint is Sep 11 18:10, and it's the most recent. Let me read its checkpoint and audit-evidence. This will tell me the current integrated state and what's already been audited.

Note "integrated_overlay" in longrun/ is likely a copy of this. Let me read D13-integrated-kernel-audit/checkpoint.json.
dsh: reasoning:
Excellent. The D13-integrated-kernel-audit is the most complete snapshot: 462 release files. My best strategy for M4 topology audit:

1. Copy the D13-integrated-kernel-audit release into my worktree (as read-only base → my own `release/`), verify hashes against their final-release-hashes.txt, build it.
2. Focus audit on topology lane: recognition (D7 sphere recognition, D12-surgery-recognition, VKPort), triangulation (D10-triangulation-low-dim, D11-triangulation-3d, D12-triangulation-topology), surgery (D3-surgery-ledger, D7-surgery-neck-extinction, D12-surgery-recognition).
3. Replay axiom/source-hash/downstream screens.
4. Adversarial: A3 (counterexample search on D2/D3 clusters), I6, I7.
5. M8: need to identify. Let me look at D13's blocker-verdicts.json and the D12 cards. Also, maybe M8 is defined in the swarm queue as `M8` milestone for "M4 topology". Let me check `infra/swarm/` more thoroughly.

Let me check infra/swarm contents.
dsh: reasoning:
Let me now identify M8 precisely. Maybe it's in the *leader prompt generator* state or in `poincare-lab` / `tasks` docs. But given limited time, I could interpret M8 as "M8 pilot experiment / baseline" — hmm, but as an "auditor" blocker.

Wait: maybe M8 is a *milestone* in the new "M" milestone series used by the swarm: M1 baseline, M2 upstream adapters, M3 analytic/geometric critical path, M4 topology audit... and M8 might be defined in `longrun/README.md`. Let me read longrun/README.md and infra/swarm/README.md. Also check `longrun/inbox` and `decisions`.
dsh: reasoning:
Let me check the inbox imports (D13-wave3.imported) — they may define the M-milestones with M8.
dsh: reasoning:
No M8 definition. Let me try searching for "M4" as a milestone in the swarm state or in the supervisor logs, and check `logs/events.jsonl` for "M8". Also check `longrun/logs/dispatch*.log` for how L5 was created — maybe the generating prompt defined M8.

Actually let me grep the supervisor logs for L5's prompt generation and any M8 reference.
dsh: reasoning:
The supervisor log is empty. So M8's definition is genuinely not present in the provided materials. I'll treat M8 as an unresolved identifier and document that in the audit: "M8 is not defined in any artifact available in the worktree; the closest matches are (a) the program milestone M8 in docs/orchestration_reuse.md §4 (v4-flash pilot baseline experiment) and (b) no manifest entry". Actually wait — let me check D13-critical-path-review, which is the most recent card (Sep 11 18:20) and may define the M-milestones for the current wave. Let me read its result card and checkpoint.
dsh: reasoning:
D13-critical-path-review has a card. Let me read it — it's the most recent planning artifact and may define M-milestones (M1..M12) including M8.
dsh: reasoning:
This is the most current state. Let me read the remainder of this card to see the residual inputs and blocker names — that includes the topology blockers I need to audit. Let me continue reading.
dsh: reasoning:
Now I have a very solid picture. The most current snapshot = D13-critical-path-review (455 files, most complete, gate-replayed 454/454). This is likely the best "release/" base for my audit. It even includes audit-evidence and tools from D13.

My plan for L5-topology-audit (4 hour budget):

**Deliverables** (in my worktree):
1. `release/` — copy of the D13-critical-path-review release (most current snapshot) OR D13-integrated-kernel-audit release. Which to choose? critical-path-review's release = integrated snapshot + D13/CriticalPathReview + maybe D13/IntegratedAudit. Let me check both trees and pick the one with the most complete + most recent content. I need to verify the source hashes match their recorded final-release-hashes.txt.

2. Build with pinned toolchain: `lake build` exit 0, record hashes/commands/exits.

3. Fail-closed axiom audit with only propext/Classical.choice/Quot.sound — focused on topology modules, plus a programmatic audit across all topology-relevant declarations.

4. Topology audit: recognition (D7 SphereRecognition, D12/SurgeryRecognition, VKPort), triangulation (D10/Triangulation*, D11/Triangulation3D, D12/TriangulationTopology), surgery (D3 SurgeryLedger in release? D7/SurgeryNeckExtinction, D12/SurgeryRecognition). Check for forbiddens (sorry/axiom/admit/unsafe/native_decide/proof_wanted), statement-vs-conclusion equivalence, weakened theorems, conditional vs proved classification.

5. Adversarial replay: A3 — counterexample search on earlier clusters (D2/D3). I6, I7 — verify they remain open with exact evidence; find whether any claimed closure exists.
   M8 — identify. If not identifiable, report as undefined/unresolvable in the audit and treat as "M8" milestone-level issue: hmm. I should ask? No human. I'll document: M8 is not defined in the available control plane; nearest candidates listed; classify as `unresolved-identifier`, requiring the controller to bind it. Actually, let me search once more in `poincare-lab`, `tasks/`, and `external/` for "M8" with word boundaries in md files. Also search the *whole* /data3 tree for "M8" in md/json outside longrun.

6. Downstream checked use + semantic class for each rechecked item.

7. Outbox child tasks in JSON with required fields.

8. checkpoint.json hourly, research brief, result card md+json ending TASK_DONE/TASK_BLOCKED.

Time budget: 4 hours. Copying a large .lake may be expensive. Let me check size. The D13-critical-path-review has release/.lake possibly huge. Better: copy only sources (excluding .lake), then rebuild fresh (that takes ~1.5 min per the card because mathlib is prebuilt and shared? It said 9329 jobs 1m28s with shared prebuilt packages). Let me check disk and .lake size, and whether lake-manifest points to a shared mathlib package dir.

Let me check the lakefile and lake-manifest in D13-critical-path-review/release, and how ELAN is set up.

First, let me check sizes.
dsh: reasoning:
2.1G each — mostly mathlib oleans (shared?). I have limited disk/time; copying 2.1G is feasible but the .lake build dir contains mathlib build artifacts. Let me check how the D13 worktree's .lake is organized: `.lake/packages/mathlib/.lake/build` probably. If the mathlib package source is a git clone in `.lake/packages`, 2.1G copy would take a while but is doable. Alternatively I can avoid copying .lake and instead configure `LAKE_HOME`/package dirs to point at the existing mathlib via `lake update`? That requires network or a local cache.

Better approach: check if there's a shared package cache. Look at the lake manifest "dir" fields — they were None. Let me inspect the actual .lake/packages listing and whether they're symlinks.

Also check `D13-critical-path-review/audit-evidence/*.txt` for hashes and reproduce info; and check if `lake build` works with `--no-cache` etc.

An important question: can I have my own release/ dir with symlinked `.lake`? A symlink `.lake -> ../D13.../release/.lake` would be read-only usage of another worktree — "never edit another worktree" — building in it could write. Safer: copy the .lake (or its mathlib packages) into my own worktree. Actually, the D13 approach in `audit-evidence/tools/integrate_snapshot.py` copied sources into a fresh release with `cp -r` including `.lake`? Let me look at how previous audit tasks did it (D13-integrated-kernel-audit built with "fresh project build directory and pinned mathlib"). Let me read their tools.

Let me check .lake structure and sizes.
dsh: reasoning:
The `.lake/packages` is a symlink to D6_weekly_release's packages (read-only shared mathlib). `.lake/build` is 2.1G of build artifacts (mostly mathlib oleans). I can copy just `release/` sources + `.lake/config` and symlink `.lake/packages` to the same shared location. But the build would then need to rebuild mathlib? Let's see: `.lake/build/lib/lean/Mathlib/...` — those are 2.1G. If I copy `.lake/build`, the incremental build will be quick. Copying 2.1G takes maybe 30-60s on NVMe. That's acceptable, and my worktree has space.

Wait — but actually I could also create my own `release/.lake/packages` symlink pointing to the shared packages dir. That doesn't edit the other worktree (reading only), and lake builds into my own `.lake/build`.

Plan: 
1. `mkdir -p release` in my worktree.
2. `cp -a` the D13-critical-path-review/release sources (excluding .lake) + copy `.lake/config` and `.lake/build`? Copying the build dir from another worktree is fine (no edits to the original).

Hmm, but wait — copying `.lake/build` from D13-critical-path-review means my oleans are their build. For an independent audit, an incremental `lake build` reusing another worktree's oleans is not ideal but acceptable if I then do a **fresh** build (`rm -rf .lake/build && lake build`) — that takes 1.5 min per the card apparently (they said 9329 jobs, 1m28.6s, because mathlib oleans come prebuilt from `.lake/packages/mathlib/.lake/build` which is shared). Good: mathlib oleans live in the packages symlink. So a fresh build of my 454 files is ~1.5-7 min.

So: copy sources only (small) + symlink packages + copy `.lake/config` (1.5M) → then `lake build` fresh. Let me verify where mathlib oleans live: `.lake/packages/mathlib/.lake/build`. Yes, in the shared package dir.

Let me now plan the audit content more concretely. Let me first inventory the topology-relevant modules in the D13-critical-path-review release tree.

Modules present:
- Poincare/D12/{SurgeryRecognition, TriangulationTopology, ...}
- Poincare/D7/{SphereRecognition, SurgeryNeckExtinction, ...}?
- Poincare/D10/{Triangulation*}/...
- Poincare/D11/{Triangulation3D?}/...
- Poincare/VKPort/...
- Poincare/Stage6/...
- Poincare/D3? maybe not (D1-D4 clusters in Ledger/Audit dirs).

Let me list all modules with topo keywords.
dsh: reasoning:
Now I have the full module map. Topology lane modules:
- `Poincare/D7/Recognition/{All,Assembly,Audit,Basic,Homeomorphism,Probe}.lean`
- `Poincare/D7/SurgeryFlow/{All,Audit,Basic,Extinction,Probe,Statements,Times}.lean`
- `Poincare/D10/TriangulationLowDim/{Audit,Complex,Models,Moise}.lean`
- `Poincare/D11/...` triangulation? Actually no D11 triangulation in this list — let me check: there is D11-triangulation-3d worktree but maybe not promoted. Let me check D12/TriangulationTopology and D12/SurgeryRecognition files.
- `Poincare/D12/TriangulationTopology/*`, `Poincare/D12/SurgeryRecognition/*`
- `Poincare/D13/CriticalPathReview/*`
- `Poincare/VKPort/*`
- `Poincare/Stage6/{SphereSimplyConnected,TopologyBridge}.lean`
- `Poincare/Longrun/Topology/*`, `Poincare/Longrun/Surgery/*`
- `Poincare/D8/Fidelity/*` (Moise statement bridge)
- Maybe `Poincare/D12/SemanticLedger/*`

Let me list the D12/D13 dirs and the middle chunk of files I skipped.
dsh: reasoning:
Now I have the map. Time to set up my worktree. Let me create a todo list and get to work. Time budget ~4h; started ~23:08 local (15:08 UTC). Let me be efficient.

Plan:
1. Set up release/ in my worktree from D13-critical-path-review (most complete: 454 .lean, gate-replayed) — copy sources (excluding .lake), symlink `.lake/packages` to D6 shared packages, copy `.lake/config` if needed (or regenerate). Verify source hashes against their final-release-hashes.txt.
2. Fresh `lake build` (rm -rf .lake/build) with pinned toolchain; record timing, exit, jobs.
3. Topology audit scripts:
   a. Forbidden-token scan (comment/string-aware) over all authored .lean.
   b. Fail-closed axiom audit: programmatic `#print axioms` over topology-lane declarations + negative control. Reuse the audit machinery already in the release? D13's audit-evidence tools are tailored. I'll write my own `audit-evidence/` scripts to be independent (that's the point of independent audit — replay myself, not reuse).
   c. Source-hash record: sha256 of every topology-lane .lean + all release .lean.
   d. Downstream checked use: reverse-dependency search for constructors in topology lane (grep over sources + probes), plus explicit probe module elaborating `#check`/`#print axioms` of the load-bearing declarations. Better: write a Lean probe in my own worktree outside the release glob (audit-evidence/probes) that imports the topology modules and prints axioms of the key declarations, then a Python script to verify cones.
   e. Semantic class assignment per item.
4. Adversarial screens:
   - A3: counterexample search on D2/D3 clusters. D2/D3 sources in this release? The release has Ledger/, Audit/ (from D1-D4). Let me check `Audit/*.lean` and `Ledger/PerelmanDefinitions.lean`. A3 says earlier clusters D2/D3 have axiom audits but no counterexample search. I can attempt a targeted counterexample search on the D2/D3 interfaces: find definitions with suspicious hypotheses, construct model-level counterexamples... This is heavy. Realistic within budget: build an adversarial probe module that (i) enumerates D2/D3 interface fields, (ii) tests satisfiability/non-vacuity of key structures by constructing instances, (iii) attempts to falsify the strongest concrete claim with a Lean-checked counterexample where feasible. Given D2/D3 are abstract interfaces (MetricFlowData etc.), the achievable output is: non-vacuity checks + statement-shape scan + identification of conclusion-equivalent hypotheses. That's a defensible partial A3 advance, and I must not claim closure.
   - I6: verify `NeckAnalysis`, `ExtinctionTheorem`, `MissingInputs` statement-only status; check for any constructors/consumers. 
   - I7: verify `FiniteMeshConvergence`, `PerelmanEvolutionBoundary`, `FiniteRepresentsContinuousPerelman` statement-only; `FDissipation = 0`.
   - M8: search for the definition; document as unresolved ID in the control plane. Let me do a final thorough search including `input/` dirs, `poincare-lab`, and `/data3/.../longrun/worktrees/*/LONG_PLAN.json`.

Actually! Wait. Let me check `input/` in D13 worktrees — there is `input/terminal/D13-integrated-kernel-audit`. And D13-critical-path-review has `input/`. M8 might appear in some earlier wave's plan (e.g., D12-longplan tasks list has task IDs D12-*). Hmm.

Let me do a comprehensive final search for `\bM8\b` in text files across /data3/guoshaoyang/workdir/lean_poincare using grep with -I (skip binary) and excluding .lake.

5. Outbox child tasks: split blockers into independently verifiable child tasks (I6-neck, I7-mesh, A3-counterexample, M8-identification? plus triangulation/surgery evidence tasks).
6. Result card.

Let me set up the todo list and start the copy in the background, then do the M8 search while copying.

One decision: which release base? D13-critical-path-review (455 files incl. audit-evidence dir inside release? Earlier gate sweep counted 454 .lean in release) vs D13-integrated-kernel-audit (462 files incl. D13/IntegratedAudit). Let me compare the Poincare/D13 dirs:
- integrated-kernel-audit: Poincare/D13/... let me check.
Actually the critical-path-review's release probably includes both D13/IntegratedAudit and D13/CriticalPathReview. Let me verify quickly.
dsh: reasoning:
Both are terminal states, neither superset. Best: build a **union** snapshot in my worktree: base = critical-path-review release (which includes the freshest end-game state: V3 assemblies) + add `Poincare/D13/IntegratedAudit/*` from the integrated-kernel-audit. But careful: `Poincare/D13/IntegratedAudit/SnapshotRoot.lean` may import modules and has its own audit expectations. Adding it is fine as long as it compiles. Also integrated-kernel-audit has VKPort (both do? let me check critical-path-review has VKPort — yes, it listed Poincare/VKPort in the tail).

Hmm, but a union risks compile failures (IntegratedAudit/ExpectedModules may reference exact module list, e.g. `#eval` checks; it could fail if CriticalPathReview modules are extra). Safer: base = critical-path-review release (the newest end-game math, including V3 recognition chain). Plus copy IntegratedAudit modules only if they compile; if not, keep them out and note. Actually, the audit instruction says "Build from release/ with pinned toolchain" — using the D13-critical-path-review release is defensible and reproducible (its hashes are recorded). Adding IntegratedAudit is a bonus for cross-checking the axiom audit machinery (an independent prior audit I can replay). Let me check IntegratedAudit's imports/deps first.

Also, importantly, my own audit must not rely on reusing prior audit verdicts — I need to replay. I'll write my own independent scripts.

Let me now:
1. Start the copy in background.
2. Meanwhile search M8.

Copy commands:
```
mkdir -p release
cd src/release
tar cf - --exclude=.lake . | (cd dst/release && tar xf -)
mkdir -p dst/release/.lake
cp -a src/release/.lake/config dst/release/.lake/
ln -s /data3/.../D6_weekly_release/release/.lake/packages dst/release/.lake/packages
```
Wait, `.lake/packages` symlink target exists (D6). Use the same absolute target as the source symlink. Good.

Then `rm -rf .lake/build` isn't needed if I don't copy build. Fresh build from scratch: `lake build` — will it rebuild mathlib? No: mathlib oleans live in `.lake/packages/mathlib/.lake/build` which is the shared read-only package dir. But lake might try to write there if the manifest changed. The packages are already built; lake should detect they're up to date. However, lake may want to write `.lake/packages/mathlib/.lake/build` lock files — if the shared dir is writable to me (it is, same user) that's technically "editing another worktree" (D6_weekly_release). Hmm. My instruction: never edit another worktree. Building mathlib would write there. But it's already built, so no writes should occur. Risk: lake updates `.lake/packages/*/.lake/build/*.trace` timestamps? Usually lake only writes when rebuilding. The D13-critical-path-review worktree itself points at this shared packages dir and builds fine.

Alternatively copy the packages dir (huge, several GB). `du -sh` of packages? Let me check. If it's e.g. 8G, copying is too slow. Let me check and decide. Actually D6_weekly_release/release/.lake/packages might be large but the mathlib oleans are needed. Hmm, the shared `.lake/packages/mathlib/.lake/build` — check size.

Let me start.
dsh: reasoning:
The shared mathlib packages live in `/data3/guoshaoyang/workdir/lean_poincare/poincare-lab/.lake/packages` (8.2G). I'll symlink `.lake/packages` to that path directly (read-only usage; lake shouldn't write since mathlib is built). This is the main repo's packages, pre-existing.

Copy sources now. I'll tar-copy release sources excluding .lake, should be small.

Note: the D13-critical-path-review release also has `audit-evidence/` inside? Earlier the card said the negcontrol was in `<worktree>/audit-evidence/negcontrol`. And source list has `Poincare/D12/TriangulationTopology/NegControl/NegControl.lean` (axiom False module inside the glob — finding #3). I'll audit that as a packaging finding.

Let's do the copy (background if slow). Also add IntegratedAudit modules from the other worktree afterwards, testing compile.

Actually wait: is it wise to include another worktree's D13/IntegratedAudit modules? It increases audit surface and risks failures unrelated to my lane. But it provides a cross-check on the axiom machinery. The modules' `ExpectedModules.lean` might `#eval` a hardcoded module list that fails with my tree. Let me check quickly first.
dsh: reasoning:
Copy done (5.9M). Now set up .lake. Then start the build in the background while I audit text.

Note IntegratedAudit's ExpectedModules is generated from D11/D12 only; it should be fine in a union tree (extra modules don't hurt), but `KernelAudit`/`FullAudit` might assert exact counts (e.g., "#eval assert modules == expected"). If it fails, I'll exclude it. Let me first do the union copy of IntegratedAudit and see if it builds. Actually, to keep the build clean, I'll add IntegratedAudit and run the build; if failures, remove.

Set up:
- mkdir release/.lake
- cp -a src/.lake/config release/.lake/config (1.5M)
- ln -s /data3/guoshaoyang/workdir/lean_poincare/poincare-lab/.lake/packages release/.lake/packages

Then `lake build` with ELAN_HOME env. Let me check elan: /data3/guoshaoyang/workdir/lean_poincare/elan/bin. And toolchain download: v4.34.0-rc2 should already be in elan toolchains (they used it). Check `elan toolchain list`.

Let me do it.
dsh: reasoning:
Confirmed: the only textual definition of M8 in the entire tree is `docs/orchestration_reuse.md` §4 milestone table: "先导实验 (pilot experiment): 10-20 leaves, v4-flash closed loop; acceptance ≥3 theorems ACCEPTED; baseline report". This is a *program-milestone* M8, not a mathematical blocker. As auditor, I must report that the named blocker ID "M8" in my scope has **no binding definition in the control plane** (no manifest entry, no queue task, no result card) and that the only occurrence is this pilot-experiment milestone. That itself is an audit finding: an unbound blocker ID (process gap). I'll classify it as `unresolved-identifier` and record evidence: exact grep commands + the one hit. That's honest and the correct auditor behavior rather than inventing a blocker.

Now, start the fresh build in background. Commands:
```
cd release && ELAN_HOME=... PATH=... lake build
```
Note: lake needs the toolchain resolution via lean-toolchain file. Let me run with timeout and log.

Actually — first, should I add the IntegratedAudit modules? Let me add them now before the build so a single build verifies everything. Copy Poincare/D13/IntegratedAudit → my release. Risk of failure. I'll try; if build fails on those modules I'll remove them (and record why). Let me copy them and inspect any absolute-path references.

Actually, let me reconsider. My task is topology audit; the IntegratedAudit adds 10 modules of self-audit machinery that will pull in all D11/D12 modules. Almost all are already imported by other roots. The value: replaying a prior programmatic axiom audit. But I will write my own. The risk: build failures consume time. Decision: include them — it's a union snapshot, and `SnapshotRoot` gives a collision check. Any failure is informative. But `ExpectedModules.lean` is a generated list of D11/D12 clean modules, harmless. `FullAudit`/`KernelAudit` might do expensive things (compile-time `#eval` walking the environment). They were built before, so fine.

Let me copy, then run build in background with `env -i`-ish env.
dsh: reasoning:
464 .lean files now. Source-hash baseline: record sha256 of all files. Also diff hashes against D13-critical-path-review's final-release-hashes.txt to verify byte-identical provenance.

Let me check the format of their hash file first, then verify.

Then launch fresh build in background:
```
cd release && ELAN_HOME=/data3/.../elan PATH=$ELAN_HOME/bin:$PATH lake build
```
Note `rm -rf .lake/build` unnecessary (no build dir). Fresh build.

Let me also check that lake won't try to fetch mathlib (manifest present + packages symlink). Set `LAKE_NO_CACHE=1`? lake may try to download cached artifacts — but with packages present and built it should be fine. To avoid network attempts, maybe set `LAKE_NO_CACHE=1`. Hmm, `lake build` will check mathlib's build state; since `.lake/packages/mathlib/.lake/build` exists, no work. Let me just run it.

Start build in background now.
dsh: reasoning:
Build is running in background (job bash-1). Meanwhile, verify source hashes: compute sha256 for all files in my release and compare with their final-release-hashes.txt (460 entries vs my 464 .lean; theirs includes 460 files under release excluding .lake — earlier card said 454 .lean of 460; my tree has 464 .lean because I added 10 IntegratedAudit → 454+10=464. Good).

Let me write a script to do this comparison now.
dsh: reasoning:
Excellent: all 460 files from D13-critical-path-review are byte-identical in my union, and 10 added from D13-integrated-kernel-audit. Let me also verify those 10 against the integrated-kernel-audit's hash file.

Then while the build runs, begin the topology lane inventory/analysis. Let me write an inventory script that maps topology-relevant modules → declarations, and check the D12 cards for recognition/triangulation/surgery.

First verify the 10 added hashes.
dsh: reasoning:
The head of that wc output got cut (tail -50). Let me get the full list of topology lane files with line counts, to define my audit surface precisely. Then read the key source files.

Let me define the topology lane audit surface:
A. Recognition:
 - Poincare/D7/Recognition/{Basic,Assembly,Homeomorphism,Audit,Probe,All}.lean
 - Poincare/D12/SurgeryRecognition/{ConnectedSumTopology,CoveringRecognition,DeckTrivial,SphereOfSpheres,BallGluing,ExpandedInterfaces,Audit,All}.lean
 - Poincare/Stage6/{TopologyBridge,SphereSimplyConnected}.lean
 - Poincare/D8/Fidelity/* (Moise smoothing bridge)
 - Poincare/VKPort/**
 - Poincare/D13/CriticalPathReview/{StatementAudit,UsageProbe,AxiomAudit}.lean
B. Triangulation:
 - Poincare/D10/TriangulationLowDim/*
 - Poincare/D12/TriangulationTopology/*
 - Poincare/Longrun/Topology/*
C. Surgery:
 - Poincare/D7/SurgeryFlow/*
 - Poincare/Longrun/Surgery/*

Let me read the key interface files now (D7/Recognition/Basic.lean, Assembly.lean; Longrun/Surgery/Missing.lean, D7/SurgeryFlow/Statements.lean, Longrun/Topology/MissingTheorems.lean) to verify I6/I7.
dsh: reasoning:
This is a well-structured conditional assembly. Now let me check the build progress, then look at the D12 SurgeryRecognition V3 chain and the exact residual hypotheses.

Let me check build log status.
dsh: reasoning:
Build is at 9013/9347 — nearly done. Let me continue reading ExpandedInterfaces.lean (the V3 hypotheses and assembly).
dsh: reasoning:
Now let me check the build log and then design the audit. Let me also look at the D12 cards for recognition, triangulation, surgery to know their claims (evidence to recheck). Let me find them:
- D12-surgery-recognition card: in D12-surgery-recognition worktree.
- D12-triangulation-topology card.
- D10-triangulation-low-dim card.
- D7-surgery-neck-extinction card.
- D8-moise-statement-bridge card.

Let me check build first.
dsh: reasoning:
Build still going (log shows axiom output from in-file AxiomAudit modules — these run at build). Let me wait a bit and meanwhile read the D12 surgery recognition and triangulation cards. Let me delegate reading of cards to a subagent? No — I need context myself. But cards are long. Let me read the key sections via grep on claims/blockers.

Actually, a good use of parallelism: spin up a background subagent to read the three D12/D10 topology cards and produce a compact claims/evidence summary while I proceed with the build and Lean-side audit. But subagents can't be trusted for kernel facts; they can summarize cards though. Given my context budget, delegating card summarization is sensible. But the subagent would need to read files in the worktrees (read-only is allowed? It shares the same sandbox/workspace). Let me do it: one subagent for the topology cards summary.

Actually, I can grep the verdict/claims sections myself quickly. Cards are ~200-400 lines. Three cards = ~1000 lines, too much. Let me delegate to a subagent with a precise prompt: extract from each card the (1) verdict, (2) exact claimed checkmarks/commands/exits, (3) named constructors and downstream consumers claimed, (4) explicit statement of what's NOT claimed, (5) blocker status. Return compact JSON-ish text. Include file paths.

Meanwhile, I'll write my audit scripts:
1. `forbidden_scan.py`: comment/string-aware scan for `sorry`, `axiom`, `admit`, `unsafe`, `native_decide`, `proof_wanted` in all release .lean files, with awareness of comment blocks and strings.
2. `axiom_audit`: generate a Lean probe importing the topology lane roots and running `#print axioms` for a curated list of declarations. Fail-closed check in Python: parse output, allowed set exactly {propext, Classical.choice, Quot.sound}, and assert every requested declaration appears.
3. A negative control module with `axiom badAxiom : False` and a `#print axioms` of a theorem depending on it → must be detected. Place outside the Poincare glob (audit-evidence/negcontrol) so `lake build` doesn't compile it.
4. `source_hash`: done.
5. `downstream_use`: grep-based + Lean-based. Best: use the compiled environment. `#print axioms` doesn't give consumers. I can write a Lean file using `Lean.Environment` traversal to find constants whose value/type mentions the targets — that's what D13 did (reverse-dependency BFS via `#eval`?). Simpler and robust: use `lake env lean` probes with `#check` and a grep-based consumer count over the release sources (excluding the defining module). I'll combine: (a) syntactic consumer count via regex over sources, (b) `#print axioms` cone check, (c) `#check` type re-elaboration for semantic class.

For fail-closed axiom audit "allowing only propext, Classical.choice, Quot.sound" — I'll do a programmatic environment-wide audit like D13 did, but independently written. Writing an environment-walking Lean program is doable: 

```lean
import Lean
open Lean
run_cmd do
  let env ← getEnv
  let mut bad := #[]
  for (n, ci) in env.constants.toList do
    if ci matches .thm .. then
      let axs := collectAxioms... 
```
Lean 4 has `Lean.collectAxioms : Environment → Name → NameSet`. In `run_cmd`/`#eval` I can iterate `env.constants` and call `collectAxioms`. That's the standard "programmatic axiom audit". I'll write a module under my own worktree (not in release glob — or in release as `Poincare/D13/...`? No: I should not add modules to the release for the audit; keep authored audit code in `audit-evidence/` and elaborate with `lake env lean ../audit-evidence/probes/X.lean` from cwd=release, like the D13 convention).

Let me check if `Lean.collectAxioms` exists in this Lean version. D13's `AuditCore.lean` probably uses it. I can peek at their implementation for API names (not copy the verdicts). Let me look at the top of AuditCore.lean.
dsh: reasoning:
Good — `Lean.Util.CollectAxioms` and `Environment.constants` are available. I'll write my own independent audit core with a different structure (my own code, focused on topology lane + full snapshot).

Let me check build status.
dsh: reasoning:
Build progressing (9155/9347). Note an `info` about `Unknown identifier MeasureTheory.diracDelta` in a Probe file — that's likely inside a comment or a `#check` failure? If it were an error, the build would fail. `info:` level suggests it's in a `#check_failure` or similar. Fine.

While waiting, let me write the audit scripts. First the forbidden-token scanner (comment/string-aware). I'll write it in Python.

Lean comment/string handling: nested block comments `/- ... -/`, line comments `--`, string literals `"..."` with escapes, char literals `'...'`, and quoted identifiers? Also raw strings `r"..."` and `s!"..."` interpolation. For a robust scan I'll implement a small state machine handling nested block comments, line comments, string literals with escapes, and char literals. Then search the code-only text for forbidden tokens with word boundaries: `sorry`, `axiom`, `admit`, `unsafe`, `native_decide`, `proof_wanted`. Also `sorryAx` mention in comments is fine (comments stripped). Also check for `#print axioms` usage? Not forbidden.

Also check for declaration-level "fake/weakened theorem" and "conclusion-equivalent assumptions" — that's semantic, handled by the statement-shape scan: for each theorem in the topology lane, check whether any hypothesis is syntactically identical to the conclusion. D13's StatementAudit already does something similar. I'll write a Lean probe that, for the topology-lane declarations, prints types and compares hypothesis types to conclusion (via `Expr` equality after forall-stripping). This is heavy but feasible: use `Lean.Meta` in a `run_cmd`: for each target constant of kind thm, strip `∀` binders, take conclusion, compare each binder type to conclusion for syntactic equality (`Expr.equal`). Also flag hypotheses that are `Nonempty (X ≃ₜ 𝕊³)`-like conclusion-equivalent.

That's a good independent statement-shape audit. Let me write:

`audit-evidence/probes/L5TopologyAudit.lean`:
- imports all topology roots (D7.Recognition.All, D12.SurgeryRecognition.All, D12.TriangulationTopology.*, D7.SurgeryFlow.All, Longrun.Topology.*, Longrun.Surgery.*, D10.TriangulationLowDim.*, Stage6.*, D8.Fidelity.*, VKPort.*)
- Contains:
  - `L5_AXIOM` fail-closed programmatic axiom audit: iterate over environment constants whose module is in the topology-lane module list; collect axioms; report violations (not in allowed set), report sorryAx/native; fail-closed with `throwError`.
  - `L5_CONSUMER`: reverse-dependency BFS over the imported environment: for each load-bearing constructor name, count constants (in the whole env) whose type or value references it. Emitting counts. (Independent of grep.)
  - `L5_STATEMENT`: for each topology-lane theorem, strip binders and compare hypothesis types against the conclusion; report `L5_EQUIV` findings.
  - `L5_TYPE`: print `#check`-style types of the load-bearing declarations.
- A negative control root outside release: `audit-evidence/negcontrol/L5NegControl.lean` with `axiom l5BadAxiom : False` and a theorem using it, then run the same programmatic audit restricted to that module and confirm it FAILS (exit 1). This is the fail-closed negative control.

Since the programmatic audit functions live in the release probe, the negative control can import it? The probe is outside the package (audit-evidence/probes), imported via `lake env lean ../audit-evidence/probes/X.lean`. For the negative control to reuse the detector, I'd need to `import` the probe file — not possible for a standalone .lean outside the package unless it's in the search path. Simpler: write the detector as a Lean file in `audit-evidence/probes/L5AuditCore.lean` and have the negcontrol do `import «L5AuditCore»`? Lake's LEAN_PATH includes the package's `.lake/build/lib/lean` and root dirs. The `lake env lean <file>` invocation sets LEAN_PATH to package build dirs + `release/`? Not for arbitrary dirs.

Alternative approach: make the negative control a *separate* standalone file that duplicates a minimal detector with a hardcoded failure expectation. Actually the cleanest fail-closed negative control: a small file outside release that declares `axiom l5BadAxiom : False` and a theorem `l5_neg_theorem : False := l5BadAxiom`, then `#print axioms l5_neg_theorem` and a `run_cmd` that asserts the cone contains `l5BadAxiom` and *exits nonzero* if the audit machinery classifies it as clean... Hmm, the goal of the negative control is to show the detector detects a violation. I can structure the detector as a reusable `CommandElabM` in a module that lives *inside* my worktree but not inside the release package glob... 

Option: put the detector into the release package under a non-`Poincare` lib? The lakefile declares libs: Poincare, Probe, Ledger, Audit, ReleaseCheck, ReleaseAudit, D6AuditReport. I could add `[[lean_lib]] name = "L5Audit"` with globs `["L5Audit.+"]` and put my audit code in `release/L5Audit/`. That's a modification of the release (adding files + lakefile). The instruction says build from release/ — adding my audit module to release is what previous tasks did (D13/CriticalPathReview was added to release). But it changes the package. I'd rather keep source hash baseline pristine and put audit code in `audit-evidence/`. 

Solution: use `lake env lean` with `LEAN_PATH` including `audit-evidence/probes` and pass `--root`? Actually `lean` supports `-R <dir>` for root dirs? There's `lean --help`: options include `-o`, `-i`, `-D`, `-s`, `-j`, `--root`, `-R`. In Lean 4, `-R <dir>` sets the root for module resolution (like `LEAN_PATH`). We can do: `lake env lean -R ../audit-evidence/probes -o /tmp/x.olean ../audit-evidence/negcontrol/L5NegControl.lean` where the negcontrol imports `L5AuditCore`. Hmm, `lean -R` semantics: `-R dir` = "add dir to the search path as a root"? Let me check `lean --help` in this toolchain. Alternatively simply set `LEAN_PATH=$LEAN_PATH:$(pwd)/../audit-evidence/probes`.

Actually a simpler robust approach: the negative control *includes* the detector via `import` only if it resolves; test `LEAN_PATH`. Let me test after build.

Even simpler: Keep the detector in one file `audit-evidence/probes/L5TopologyAudit.lean` (positive, must pass) and a separate `audit-evidence/negcontrol/L5NegControl.lean` that contains its own inline copy of the *core assertion function* (a ~15-line `run_cmd` checking that a known-bad declaration's cone is non-empty and unapproved) — the control shows the *method* (collectAxioms + approved-set test) rejects a bad axiom. Since the method is short and copied, that's an acceptable fail-closed negative control: if `collectAxioms` were broken/vacuous, the control would elaborating successfully with a "PASS"? No — the control *must* fail elaboration; if the detector machinery were vacuous, the control would pass (exit 0) and thus signal a defect. That's exactly the semantics wanted.

To make the control meaningful I'll make it use the same names/pattern: declare `axiom l5BadAxiom : False`, theorem using it, then `run_cmd` with the same `approvedAxioms` list and the same violation check → it throws. If someone changes the positive audit to be vacuous (e.g. empty declaration list), the control would still detect the bad axiom, showing the *check function* works. Good enough, and honest in the card (control validates the check function, not the positive audit's coverage; the positive audit's coverage is guaranteed by a hard count assertion).

Coverage non-vacuity: in the positive audit, assert the number of audited declarations ≥ some hard floor (e.g. ≥ 500 in the topology lane), and list module names imported; also `#eval` assert that the topology-lane module list is nonempty and all imported.

Now let me write the detector. Key APIs:
- `import Lean.Util.CollectAxioms`
- `open Lean Elab Command`
- in `run_cmd`:
```lean
let env ← getEnv
for (n, ci) in env.constants.toList do ...
```
`env.constants` is a `NameMap ConstantInfo`? In Lean 4, `Environment.constants : NameMap ConstantInfo`. `.toList` gives `List (Name × ConstantInfo)`.
- `collectAxioms env n : NameSet`? Signature: `def collectAxioms (env : Environment) (constName : Name) : NameSet`. In newer versions it may return `CollectAxioms.State`. D13 uses it; let me check the call site in AuditCore to copy the exact API usage.

Let me read the rest of AuditCore.lean (lines 80-160).
dsh: reasoning:
I now know the API. Note `Lean.collectAxioms n` works inside `CommandElabM` (uses environment from context). Good.

Build at 9221/9347 — almost done. Let me write my audit files now.

Plan for my audit probe (`audit-evidence/probes/L5Audit.lean`):

```lean
import Poincare.D7.Recognition.All
import Poincare.D12.SurgeryRecognition.All
import Poincare.D7.SurgeryFlow.All
import Poincare.D10.TriangulationLowDim.Audit
import Poincare.D12.TriangulationTopology.Downstream
import Poincare.Longrun.Topology.Stage6Bridge  -- etc
import Poincare.Longrun.Surgery.Missing
import Poincare.Stage6.TopologyBridge
import Poincare.D8.Fidelity.MoiseStatement
import Poincare.VKPort.HatcherLib.Ch1.VanKampen
...
import Lean.Util.CollectAxioms
import Lean.Elab.Command
```

Careful: importing `All` modules might pull in `NegControl` (with axiom False) — e.g. `D12/TriangulationTopology` has `NegControl/NegControl.lean` declaring an axiom, and there may be an `All.lean` that imports everything? Let me check `Poincare/D12/TriangulationTopology/` — there's no All.lean listed. And D12/SurgeryRecognition/All.lean imports the clean ones. D7/SurgeryFlow/All.lean. Let me check which modules contain `axiom` declarations — these are the pre-existing negative controls inside the glob (D13 finding P-D13-1: two negative-control modules). Let me grep for `^axiom` in release.

If my probe imports a module containing a project axiom, the programmatic audit will flag it (good — it's a real finding, though pre-existing). For the "clean topology lane" verdict, I should exclude these documented negative-control leaf modules from the *fail* condition but report them (like D13 did: `negativeControlModules`). My independent choice: report them as `L5FAIL project_axiom` but classify separately as `known-negative-control` with evidence, and in the result card report the fail-closed screen catches them. That's the honest approach: full-environment audit flags 2 project axioms; they are intentional controls inside the glob (already flagged by D13); my lane-specific verdict excludes them and says so.

Let me first find the axiom declarations.
dsh: reasoning:
There are 3 real axiom-declaring modules inside the release glob:
1. `Poincare/D13/CriticalPathReview/NegControl.lean` (negControlBadAxiom : False)
2. `Poincare/D12/VolumeIBP/Audit.lean` (negativeControl : False)
3. `Poincare/D12/TriangulationTopology/NegControl/NegControl.lean` (d12NegControlBadAxiom : False)

That's 3 negative-control modules, not 2 as D13 reported (D13 may have counted only its own known ones; critical-path-review's was created after integrated audit). This is an audit finding: **three** intentional negative-control modules now sit inside the `Poincare.+` glob.

Also `Poincare/D12/TriangulationTopology/NegControl/` is inside my lane (triangulation) — relevant to I7/A3 and packaging.

The build (with `Poincare.+` glob) will have built these modules, so `Poincare.D12.TriangulationTopology.NegControl.NegControl` olean exists in my tree. If I import the triangulation Downstream root (which imports... let me check if NegControl is imported anywhere).

Now, write the audit probe. Steps:
1. Check build finished.
2. Write probe + negcontrol + scanner.
3. Run.

Let me check build.
dsh: reasoning:
Still building (9233/9347). ~115 modules to go, several seconds each... maybe 5 more minutes. Meanwhile write the audit files.

Let me define the topology-lane module list precisely. I'll enumerate by prefix over the release tree:
- `Poincare.D7.Recognition.`
- `Poincare.D12.SurgeryRecognition.`
- `Poincare.D7.SurgeryFlow.`
- `Poincare.Longrun.Surgery.`
- `Poincare.Longrun.Topology.`
- `Poincare.D10.TriangulationLowDim.`
- `Poincare.D12.TriangulationTopology.`
- `Poincare.Stage6.`
- `Poincare.D8.Fidelity.`
- `Poincare.VKPort.`
- `Poincare.D13.CriticalPathReview.` (as the reviewed assembly/audit layer in-lane)
- `Poincare.D12.SemanticLedger.` (recognition classification audit? maybe include as evidence layer)

I'll write the module list programmatically from the file tree in Python and embed it into a generated Lean file (like D13's ExpectedModules). Alternatively write the list by prefix matching at runtime with string prefix tests on module names. Prefix matching is simpler and doesn't need generated lists:

```lean
def laneRoots : List Name := ["Poincare.D7.Recognition", "Poincare.D12.SurgeryRecognition", "Poincare.D7.SurgeryFlow", "Poincare.Longrun.Surgery", "Poincare.Longrun.Topology", "Poincare.D10.TriangulationLowDim", "Poincare.D12.TriangulationTopology", "Poincare.Stage6", "Poincare.D8.Fidelity", "Poincare.VKPort", "Poincare.D13.CriticalPathReview", "Poincare.D12.SemanticLedger"]
```
with `isPrefixOf` — careful: `Name.isPrefixOf` works on hierarchical names. E.g. `Poincare.D7.Recognition` is a prefix of `Poincare.D7.Recognition.Basic`. But also careful that `Poincare.D12.TriangulationTopology` prefix doesn't accidentally include `...Topology2`. Fine.

Also note `Poincare.D7.Recognition` vs `Poincare.D7.RecognitionX`: `isPrefixOf` in Lean's Name is hierarchical? `Name.isPrefixOf` checks structural prefix of the name components I believe. Let me be safe.

The probe should:
1. Import all lane modules — plus the union root to have everything.
2. Emit `L5MOD imports=<n>`.
3. Fail-closed declaration audit over lane modules: forbidden axioms, unsafe, sorry, native, proof_wanted, project axioms; count.
4. Statement-shape scan for conclusion-equivalent hypotheses in lane theorems: for each theorem in lane modules, strip foralls and compare binder types with conclusion using `Expr.equal` (or `isDefEq`? syntactic equality is the safe fail-closed check; also check `Meta.isDefEq` would be stronger but can be expensive; use both: syntactic equal OR definitional equality via `withReducible`/`isDefEq` with a timeout). D13 claimed "0 hypotheses syntactically/definitionally equal to the conclusion over 3196 theorems". I'll do syntactic `Expr.equal` + a `Meta.isDefEq` check guarded by `withCurrHeartbeats`? Simpler: syntactic equality only, plus a semantic review of the load-bearing assemblies. Actually for the statement screen, I want to catch "conclusion-equivalent assumptions" — hypotheses that are *logically equivalent* to the conclusion. A syntactic scan is a screen; deeper check: a hypothesis of the form `Nonempty (X.Carrier ≃ₜ SphereThree)` IS literally the conclusion. My scan should detect: after stripping binders, for each binder `(h : T)`, check `Expr.equal T conclusion` (syntactic) and also whether conclusion contains only `X`-variables and T is `Nonempty (_ ≃ₜ SphereThree)`. The syntactic check will catch the direct cases. For the topology lane I'll also manually review the key assemblies.

5. Consumer/downstream-use BFS: for a hardcoded list of load-bearing lane constructors, count users in the environment (constants whose type or value mentions the name). Implement: traverse `env.constants`, and for each constant, walk its type and value expressions collecting constant names (`Expr.getUsedConstants`). Build reverse map Name → Array Name. Then for target names, report count and a few consumers. This is independent and strong. Cost: 9347 modules, maybe ~200k constants... For each constant, `getUsedConstants` traverses its type+value; this could be heavy but D13 did something similar. Limit to line: iterate over all constants once, collect `used`, build map. Let me restrict to constants in lane modules + their targets' consumers among all modules. Simpler: one pass over all env constants, computing used-constants set, and record only edges pointing to any of my ~40 target names (using a HashSet). That's efficient.

Target load-bearing names:
- Poincare.D7.Recognition.SphericalPieceRecognition.recognize? (field)
- Poincare.D12.SurgeryRecognition.stage6Target_of_v3hypotheses
- Poincare.D12.SurgeryRecognition.stage6Target_of_v2hypotheses
- Poincare.D12.SurgeryRecognition.stage6Target_of_v2decomposition
- Poincare.D7.Recognition.stage6Target_of_certificates
- Poincare.D12.SurgeryRecognition.ConnectedSumDecomposition.mkV2
- Poincare.D12.SurgeryRecognition.sphericalPieceRecognition_of
- Poincare.D12.SurgeryRecognition.sphericalPieceRecognition_of_spaceForm
- Poincare.D12.SurgeryRecognition.deckTrivial_of_simplyConnected_quotient
- Poincare.D12.SurgeryRecognition.iteratedSphereSum
- Poincare.D12.SurgeryRecognition.iteratedSphereSum_homeo_sphere
- Poincare.D12.SurgeryRecognition.sphereConnectSum_homeo_sphere
- Poincare.D12.SurgeryRecognition.diskGlueQuotHomeoSphere
- Poincare.D12.TriangulationTopology.SphereSimplyConnectedMain? need names
- Poincare.D12.TriangulationTopology.<gluing names>
- Poincare.Stage6.poincareConjectureTopologicalThree
- Poincare.Longrun.Surgery.ExtinctionTheorem.extincts_of_complexity
- Poincare.Longrun.Surgery.ExtinctionTheorem.finitelyMany_of_complexity
- Poincare.VKPort.* main van Kampen theorem name(s)
- Poincare.D10.TriangulationLowDim.* main declarations

I need actual names. Let me grep declarations in the lane dirs to build the target list automatically: grep `^(theorem|def|structure|noncomputable def|abbrev) Name` in lane files. Then pick the ones referenced by cards. Actually for the consumer BFS I can include ALL lane declarations as targets (maybe several hundred) — fine.

But hardcoding a few hundred names in Lean is awkward; better generate the probe with Python from the source tree: parse declarations per lane module and emit a Lean `def laneTargets : List Name := [...]`. But names include namespaces; parsing is error-prone. Alternative: in Lean, select targets dynamically = all declarations whose module is in lane. That's easy: for each constant with module in lane and kind thm/def, count consumers across the whole environment. That gives a per-declaration consumer count for the whole lane — exactly "downstream checked use" at scale. Output could be big (a few thousand lines) but manageable to a log file.

Let me do that: one pass building edges only to lane-declaration names. Implementation:
```lean
let laneNames : NameSet := ... -- from constants whose module is lane
-- build reverse map
let mut consumers : NameMap (Array Name) := {}
for (n, ci) in env.constants.toList do
  let used := ci.type.getUsedConstants ++ (match ci.value? with | some v => v.getUsedConstants | none => #[])
  for u in used do
    if laneNames.contains u then consumers := consumers.insert u (consumers.findD u #[] |>.push n)
```
`ConstantInfo.value?` exists? There's `ConstantInfo.value? : Option Expr`. Yes.
`Expr.getUsedConstants : Expr → Array Name`.

Note: custom `NameSet` is `Std.TreeSet Name`? In Lean core, `NameSet` is `Std.TreeSet Name`? There is `Lean.NameSet` (a `Std.TreeSet`?) Historically `NameSet` = `RBMap Name Unit`? Let me avoid: use `NameMap Bool` or a `HashSet Name`. `Lean.NameMap` exists in core: `NameMap α`. Is `NameSet` defined in Mathlib or Lean core? `Lean.NameSet` is defined in `Lean/Data/NameSet.lean` I think (as `Std.TreeSet Name compare`). Hmm, in Lean 4.34 `NameSet` might be `Std.TreeSet Name`. Safer: use `NameMap Unit`? Even simpler: `HashSet Name` requires `BEq`/`Hashable` instances which exist for Name. Let me use `Std.HashSet Name`? I'm not certain of the exact module. `Lean.NameSet` is used in `collectAxioms`' return type (`NameSet`), so `NameSet` is available wherever CollectAxioms is imported. I'll use `NameSet` with `.contains` and `NameSet.empty`.

Let me check the API for NameSet in this version by grepping mathlib/lean sources: `Lean/Data/NameSet.lean` exists in the Lean core lib distributed with the toolchain. Let me check the toolchain source dir.

Actually, simplest: use `Array Name` with `.contains` for small sets (lane decls maybe ~2000; consumer loop over ~200k constants × 2000 array membership = too slow). Use `NameMap Bool`.

`consumers : NameMap (Array Name)` and `laneSet : NameMap Unit`. Insert/contains via `NameMap.find?`/`insert`. That's fine.

Then output `L5USE <name> <count> <first few consumers>`.

Also important: the consumer count from `Expr.getUsedConstants` includes references in *types* and *values* — this is the standard "retained use" measure.

6. Print types (`L5TYPE`) for a curated list of load-bearing names — but I need names. I'll print types for all lane declarations matching certain name patterns (e.g. containing "stage6", "recognition", "sphere", "extinct", "vanKampen"/"VanKampen", "Homeo", "homeo"). Actually the full type dump for the lane is large (maybe 1-2 MB) but fine — I can grep it later. Let me dump `L5TYPE` lines for all lane declarations (name, kind, type) to a log; then Python post-processing extracts the ones I care about. This also serves the semantic-class evidence (expanded types).

Hmm, one concern: heavy `run_cmd` at elaboration will take a while (env traversal + pp for thousands of declarations). D13 did 4343 declarations. My lane is maybe ~2000. Should be OK, maybe 1-3 min.

7. Statement-shape scan: for each lane theorem, strip foralls (`forallTelescopeReducing`?) then compare each local's type with conclusion. Use `Meta.forallTelescopeReducing ci.type fun xs body => ...` inside `liftTermElabM`. Compare `← inferType x` with body via `Expr.equal` and maybe `isDefEq`. With ~1500 theorems, isDefEq could be slow; do syntactic equal only, plus a cheap "occurs check": conclusion's used constants ⊆ hyp's used constants? Let me just do syntactic equality and also check if hyp type is `Nonempty (_ ≃ₜ _)`-shaped and conclusion is equal after instantiation. Keep it simple: syntactic `Expr.equal`.

Actually, a stronger and still cheap check: compare the *string* of `Meta.ppExpr` for hyp type and conclusion? That's also syntactic. Fine.

I'll do: for each theorem, strip binders; body = conclusion; for each local decl `x` with type `T` (after instantiation with the bound vars — `forallTelescopeReducing` gives instantiated local context), if `Expr.equal T conclusion` then emit `L5EQUIV`. If none → fine.

Let me now create the files. First check build done.

Also, I need a Python post-processor for:
- forbidden scan (comment/string aware)
- axiom log verification (fail-closed: parse L5AUDIT lines, assert counts; parse L5DECL cones, assert allowed set)
- negcontrol detection
- consumer counts summary
- source hashes (done)

Let me write these files now while build finishes.
dsh: reasoning:
Still building. Let me write the Lean audit probe now. I need to know main declaration names for the VKPort and triangulation modules to include in the curated type dump — but I'll dump all lane types so no need.

Let me write `audit-evidence/probes/L5Audit.lean`. Careful about imports: I want to import the lane roots but avoid the axiom-bearing negcontrol modules (`Poincare.D12.TriangulationTopology.NegControl.NegControl`, `Poincare.D12.VolumeIBP.Audit` (contains axiom negativeControl), `Poincare.D13.CriticalPathReview.NegControl`). If I import only specific clean roots, those modules won't be in the environment. But my lane audit should also *cover* them (they're in the package and in the triangulation lane). To audit them I must import them; then the environment has project axioms → my fail-closed check flags them. That's correct behavior: I'll report them as intended negative controls excluded from the clean-lane verdict (like D13), with their exact names.

Decision: import the full package root? There's no single root importing everything (`SnapshotRoot` covers D11/D12 only). I can generate an import list of all 464 lane/clean modules... Simpler: import a set of lane roots + the three negcontrol modules explicitly:
```
import Poincare.D7.Recognition.All
import Poincare.D12.SurgeryRecognition.All
import Poincare.D7.SurgeryFlow.All
import Poincare.D10.TriangulationLowDim.Audit
import Poincare.D12.TriangulationTopology.Downstream   (check name)
import Poincare.Longrun.Topology.AxiomAudit
import Poincare.Longrun.Surgery.Axioms
import Poincare.Stage6.SphereSimplyConnected
import Poincare.D8.Fidelity.AxiomAudit
import Poincare.VKPort.HatcherLib.Ch1.VanKampen
import Poincare.D13.CriticalPathReview.UsageProbe
import Poincare.D12.SemanticLedger.AxiomAudit
import Poincare.D12.TriangulationTopology.NegControl.NegControl
import Poincare.D12.VolumeIBP.Audit
import Poincare.D13.CriticalPathReview.NegControl
import Poincare.D13.IntegratedAudit.SnapshotRoot   (collision check + all D11/D12)
```
Hmm, importing `Poincare.D13.IntegratedAudit.SnapshotRoot` gives the collision check already. But note `SnapshotRoot` imports D12 modules including `NegControl`? D13's generator excluded negative-control modules — and it lists expectedCleanModules. Let me check if SnapshotRoot imports the NegControl modules. If yes, the axiom flags appear anyway. Either way, importing them explicitly is honest.

Also `Poincare.D13.IntegratedAudit.*Audit*` modules run `#d13_axiom_audit` commands at build time; importing them into my probe would re-run their audit (fine, more evidence, but expensive). I'll import only SnapshotRoot (which imports the clean modules) plus lane roots.

Wait, careful: my audit is over lane *modules*. Which modules are "lane"? Prefix list. If I import roots, all lane modules are in the env. Good.

Let me check some names first: does `Poincare.D12.TriangulationTopology.Downstream` import the whole triangulation layer? Let me look at the All/Downstream imports, and D12/SurgeryRecognition/All.lean.
dsh: reasoning:
Good. Now write the probe. Let me check whether `NameMap` and `Expr.getUsedConstants` compile. I'll write the file and iterate on compile errors.

One important consideration: `run_cmd` inside a module that's elaborated with `lake env lean file.lean` — the file is outside the package root but imports package modules; `lake env lean <path>` from cwd=release works (D13 did it).

Let me write the probe with these sections:
- S1: module inventory
- S2: fail-closed declaration audit (lane modules), plus explicit negative-control registry
- S3: whole-environment forbidden scan? No — lane only + report global project axioms (scan all modules for project axiom declarations). I'll scan all env constants for `.axiomInfo` whose module is in the *package* (any of the package's modules) — but the env only contains imported modules. Since I import lane + SnapshotRoot (all D11/D12) I'll get a good chunk. Let me additionally import the library roots? To cover the whole package I could import every module... The probe could import `Poincare.D13.IntegratedAudit.SnapshotRoot` (D11/D12), `Poincare.D7.Recognition.All`, `Poincare.D7.SurgeryFlow.All`, etc. The D1-D4/Longrun layers: import `Poincare.Longrun.Topology.AxiomAudit`, `Poincare.Longrun.Surgery.Axioms`, `Poincare/D9/...`? Not needed for the topology lane; the union release `lake build` covers compilation of all.

Actually for a *fail-closed axiom audit* of the whole release, the cleanest is to audit every declaration of every module in the package — which requires importing all of them. Since the package in this snapshot is essentially D7-D13 (the D1-D6 base modules are imported transitively by many), importing a handful of roots likely covers nearly all. Let me add `Poincare.D13.IntegratedAudit.SnapshotRoot`, `Poincare.D7.Recognition.All`, `Poincare.D12.SurgeryRecognition.All`, `Poincare.D7.SurgeryFlow.All`, `Poincare.D10.TriangulationLowDim.Audit`, `Poincare.D12.TriangulationTopology.Downstream`, `Poincare.Longrun.Topology.AxiomAudit`, `Poincare.Longrun.Surgery.Axioms`, `Poincare.Longrun.Evolution`, `Poincare.D8.Fidelity.AxiomAudit`, `Poincare.VKPort.HatcherLib.Ch1.VanKampen`, `Poincare.D9.AncientKappa.Basic`, `Poincare.D12.SemanticLedger.AxiomAudit`, `Poincare.D13.CriticalPathReview.AxiomAudit`, and the three negcontrol modules.

Then do a **whole-imported-environment** fail-closed audit (every declaration in every imported module that belongs to the package — I can't easily tell package modules from mathlib ones; but module names starting with "Poincare" or "Probe"/"Ledger"/"Audit"/"Release*"/"D6AuditReport" are package modules). I'll define `isPackageModule` by top-level name in a list.

That gives:
- package-wide: forbidden constructs, project axioms, unapproved cones.
- lane subset: same, plus consumer counts + statement shape.

Let me write it. For output volume: emit `L5DECL` for lane only (with cone), `L5PKG` summary counts for package-wide, `L5PKGFAIL` details. Types: for lane only (could be ~2000 lines).

Consumer BFS: lane declarations as targets, one pass over all env constants. Output `L5USE name count module` sorted descending? Only for lane decls with count > 0 plus all "load-bearing" ones I care about. Print all lane decls with their count (maybe 2500 lines). OK.

Also statement-shape: `L5EQUIV` findings.

Let me write it now.
dsh: reasoning:
Excellent, the subagent gave detailed card evidence. Now I know what to verify:

Card 1 (D12-surgery-recognition): 171/171 declarations axiom-audited; claims SR-5 closed, covering recognition closed, coveringTrivial closed; SR-4 (vanKampen), spaceForm, SR-6, Moise remain open. Note: "port not yet applied" — VKPort compiles but not wired. This is important: the card claims no negative control, while there is `Poincare/D12/TriangulationTopology/NegControl` in a different card. Fine.

Card 2 (D12-triangulation-topology): 348/348; `diskGlueQuotHomeoSphere`, `alexanderHomeo`, `sphereOfTwoDisks`, negative control `d12NegControlBadAxiom`; nodes 1-3 BLOCKER (Moise, Whitehead, Perelman extinction).

Card 3 (D10-triangulation-low-dim): 71 declarations, 0 violations, `MoiseTriangulationTheorem` statement-only Prop; χ(S¹)=0 correction.

My audit will verify:
- compile of the union package (running).
- the claimed declarations exist and their cones.
- the card claims that specific identifiers are load-bearing and consumed.
- whether the triangulation negative control is detected (yes, I import it and register it).
- `MoiseTriangulationTheorem` is a `def : Prop` (statement-only) — verify.
- Check `sphereOfTwoDisks` and `diskGlueQuotHomeoSphere` cones and consumers.
- Check the claimed `348/348` and `171/171` coverage: I can independently compute the number of declarations in those modules from my L5DECL/L5TYPE output and confirm the axiom audit covers all.
- Check "no `sorry`/`axiom`/`unsafe`/`native_decide`" in those modules (my scanner + env audit).
- Check for fake/weakened theorems: statement-shape scan (L5EQUIV) + manual review of key ones. One specific semantic risk: `stage6Target_of_v3hypotheses` — check that `RemainingRecognitionHypothesesV3.spaceForm` isn't conclusion-equivalent. It says every spherical piece is a space-form quotient; the conclusion is `Nonempty (X ≃ₜ 𝕊³)`. Not equivalent (space form model is a structure with Γ and quotient). But wait — is `IsSpaceFormModelOf Y M` perhaps including a homeomorphism to `Y` and `M.quotient`? It's just modeling. OK.

Potential real semantic issue: `SphericalPiece` itself includes a canonical-neighborhood certificate with `kind = compactSpherical` — the certificate is an opaque structure whose geometric content is unproved (CanonicalNeighborhoodCertificate). And `SphericalPieceRecognition.recognize` assumes any simply connected spherical piece is 𝕊³. In V3, this is decomposed into `spaceForm` (spherical piece is a space form quotient) + proved deck triviality. The remaining `spaceForm` field is a *structure-valued* hypothesis, unproved. Good, honest.

Another risk: `ConnectedSumDecompositionV2` — is `sumHomeo : Nonempty (X.Carrier ≃ₜ iteratedSphereSum pieces D.pieceSphere)`? If the V2 structure includes the homeomorphism X ≃ iterated sum as *data*, and `pieceSphere : ∀ P ∈ pieces, Nonempty (P ≃ₜ S3)` as data, then `endGame_finalTopology` composes them with `iteratedSphereSum_homeo_sphere`. The remaining content: existence of that data. The card says `ConnectedSumDecompositionV2` is "data parameter; inhabited once a surgery decomposition certificate exists". Fine.

But here's a subtle risk worth auditing: is `ConnectedSumDecompositionV2.pieceSphere` *requiring* each piece to be 𝕊³ (i.e., assuming the conclusion for pieces)? And `sumHomeo` requiring X to be the iterated sum. If the decomposition is assumed, then the assembly is a conditional implication — fine — but the *downstream* claim "E1: SR-5 eliminated" means the D7 `sphere_of_spheres` field is *constructed* from V2 data. The V2 data still contains `pieceSphere`, which is the per-piece input. Legit.

Let me now check the build, then run the probe. Also, I should double check the claim in card 1 that "port is not yet applied / no D12 deliverable imports it" — D13-critical-path-review's card says VKPort "compiles but is not wired". So SR-4 remains open: I6/I7 + SR-4 as blockers.

Wait — what about M8? Still unbound. Hmm, let me reconsider: maybe "M8" refers to a milestone in the *D-numbered* "M" series where M1=baseline, M2=upstream, M3=analytic/geometric, M4=topology audit... and "M8" might be a *stale/aspirational* ID. Actually! Maybe the named blockers in leader-registry were auto-generated from a source that had "M8" as *milestone 8* = "topology"? Let me check the check_leaders/dispatch generation in `/data3/guoshaoyang/workdir/lean_poincare/longrun/bin/dispatch_loop.py` — maybe it constructs the registry with named_blockers from a dict. Let me grep for "named_blockers" across the repo (including bin/).
dsh: reasoning:
The named_blockers come from an external registry generator not present. So M8 remains unbound in the artifacts. I'll report it as a control-plane gap finding (an honest, verifiable result).

Note the `info: Unknown identifier` messages in `Probe.lean` files — these are likely `#check_failure` or in comments? They're `info:` not `error:`, so elaboration succeeded. Probably deliberate probes of missing mathlib APIs using `#check` inside `/- ... -/`? No, comments are stripped. Maybe they use `#guard_msgs`? Let me look at one to understand: `Poincare/D7/Limit/Probe.lean:96`. If a `#check` fails it's an error, unless they use `set_option autoImplicit` ... Actually in Lean 4, `#check foo` for unknown identifier produces `error`? Hmm, it produces "unknown identifier" as an error normally. Unless inside `run_cmd` logInfo, or `#check_failure`. Let me check that file quickly — it's relevant to "authored-file checks" (a file with deliberate failures could break the gate, but the build passed so it's fine).

Let me look at lines 90-100 of that Probe.
dsh: reasoning:
Good. Now let me check the build finished and run my probe.

Note: I should also verify the *specific claims* independently:
- `#print axioms` for the identifiers claimed (via my L5DECL output covering all lane decls).
- Coverage counts: card says 171/171 for D12 SurgeryRecognition (their module set = 7 modules). My L5TYPE/L5DECL gives declaration counts per module for the lane; I can compute the count for `Poincare.D12.SurgeryRecognition.*` and compare to 171. Similarly 348 for D12 Tri
I'll start by orienting myself in the worktree and reading the canonical state.
