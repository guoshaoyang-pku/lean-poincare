dsh: reasoning:
Let me look at the worktree structure and find the checkpoint.json.
dsh: reasoning:
No checkpoint.json in this worktree. Task says "Continue from existing files and checkpoint.json in this worktree; do not restart completed work." Maybe checkpoint.json is elsewhere. Let me search more broadly.

Also, the parent artifact is "L3-analytic-critical-path". I need to find the L3 analytic artifact. Let me look at the queue.updated.json and the manifest, and search the broader repo for the L3 artifact.

Let me look at what exists in this worktree more comprehensively.
dsh: reasoning:
This worktree is a fork of the D6 weekly release worktree. But my task is SEMREV-L3-analytic-critical-path — independent semantic review of L3 analytic artifact. The parent artifact is "L3-analytic-critical-path".

I need to find the L3 analytic artifact. Let me look at the parent worktree directory to find L3. The worktrees directory is `/data3/guoshaoyang/workdir/lean_poincare/longrun/worktrees/`. Let me list.
dsh: reasoning:
The parent artifact is `/data3/guoshaoyang/workdir/lean_poincare/longrun/worktrees/leaders/L3-analytic-critical-path`. Let me look at that artifact, its longrun/results card, and checkpoint.json.

Also let me check the queue.json entry for SEMREV-L3-analytic-critical-path.
dsh: reasoning:
Now I understand the parent artifact. My task: independent semantic review of the L3 analytic artifact — check Euclidean/model scope, derivative domains, downstream use, axiom cones and blocker classification U6/U8/U12. Produce a review card; no blocker closure without constructed input and consumer.

Acceptance:
- compile/read all cited declarations
- run fail-closed axiom audit (only propext, Classical.choice, Quot.sound)
- record source hashes and exact semantic class per declaration
- No sorry/axiom/admit/unsafe/native_decide/proof_wanted
- End card TASK_DONE or TASK_BLOCKED
- Independent review, never a Poincare proof
- Continue from existing files and checkpoint.json in this worktree; do not restart completed work
- Save compile-checked checkpoint at least every hour
- Four hours limit
- Report genuine mathematical blocker rather than claiming completion with weaker theorem
- Only my own longrun/results/SEMREV-L3-analytic-critical-path.md can complete this task

Let me first check the state of my own worktree (SEMREV-L3-analytic-critical-path). It seems to be a copy of the D6 weekly release, which is odd. Let me check whether there's any prior SEMREV work in it. Also check the prompt in the queue for my task, and check siblings SEMREV-D13-cross-audit-ophis and SEMREV-L5-topology-audit for prior examples of similar review cards.

Let me check the queue.json for my task id.
dsh: reasoning:
My worktree is currently identical to the D6_weekly_release worktree structure (no SEMREV work yet in it, and no checkpoint.json). Sibling SEMREV worktrees also look empty (same structure), meaning siblings may not have started or also clone the same template.

Wait, the task says "Continue from existing files and checkpoint.json in this worktree". The checkpoint.json is in the parent L3 worktree at ./checkpoint.json (leaders/L3-analytic-critical-path/checkpoint.json). My own worktree has no checkpoint.json. Let me check if the parent worktree is accessible and whether I can copy the parent's release package or use it directly.

Key acceptance requirements:
1. Compile/read all cited declarations — I need to compile the L3 artifacts. The best approach: copy the parent L3 leader worktree's `release/` package into my worktree (or just work in the parent's release dir read-only?) Since my worktree is a scratch workspace, I should copy the L3 release package in and build the audit driver there. It says "no blocker closure without constructed input and consumer".

2. Run fail-closed axiom audit (only propext, Classical.choice, Quot.sound)
3. Record source hashes and exact semantic class per declaration
4. No forbidden tokens
5. End card TASK_DONE or TASK_BLOCKED
6. Review card at longrun/results/SEMREV-L3-analytic-critical-path.md

Let me check disk space, lean toolchain availability, and the parent release directory. Also check whether the L3 leader worktree release has .lake build already (so builds are fast). Let me look at the parent worktree structure.

Also I should read the prompt file prompts/L3-analytic-critical-path.md and the dispatch prompt for my own task, to understand exact acceptance.

Let me check prompts directory for SEMREV-L3.
dsh: reasoning:
Now let me look at the audit evidence and the authored files in the parent worktree. Let me read the 6 authored files, the audit file, and the l3-check.json evidence.

First let me check sizes and read the Audit.lean.
dsh: reasoning:
Now let me set up my plan. Key steps:

1. Verify the parent artifact's hashes match what's claimed.
2. Copy the L3 release package into my worktree (or better: work directly in the parent release? The task says "continue from existing files and checkpoint.json in this worktree". My worktree does not have the L3 release. I should stage a review package in my worktree: copy `release/` from the parent L3 worktree, but rebuild byte-identical sources. Actually best: create `review/` directory containing the L3 release package, hard-linked or symlinked so I can independently build.

Actually the safest independent approach: copy the parent's release source *files authored by L3* plus the required D-packages into my own worktree's `review/` package and build from scratch. But the release package appears to contain a full Poincare tree (D1-D13, ~356 lean files). Let me check size.

Let me first check the size of the parent release package and whether `.lake` has prebuilt mathlib.

3. Write an independent axiom audit that:
   - Reads all cited declarations (from the L3 files) and prints their types.
   - Checks `#print axioms` for each declaration, fail-closed.
   - Records source hashes and semantic class per declaration.
   
4. Independent semantic scrutiny:
   - Euclidean/model scope: verify the statements are indeed about EuclideanSpace ℝ (Fin n), check for hidden hypotheses that make them vacuous (e.g., `BUCn` empty? `KernelClassicalHeatSolution` vacuous?).
   - Derivative domains: check `HasDerivAt` claims, the derivative formulas, whether `mildToClassicalBridge` type is what's claimed, whether the derivative is at points t>0 and whether the function is defined on a neighborhood.
   - Downstream use: check the claimed consumers actually exist and are non-vacuous.
   - Axiom cones.
   - Blocker classification U6/U8/U12: check the card's claim that none closed, verify the residual classification is accurate — particularly whether discharging `mildToClassicalBridge` really is only a sub-obligation, and whether `SpatialLaplacianBridge` is statement-only (a Prop with no proof/instance), etc.

Critical semantic checks to catch:
- Is `mildToClassicalBridge n` a meaningful statement or trivially true/vacuous? Let's read the D12 definition.
- Is `KernelClassicalHeatSolution` vacuous — e.g. does it only assert time derivative but not the heat equation? The card says it's "kernel-classical heat solution" with the initial condition, but perhaps it does not include the spatial Laplacian. Need to check.
- `heatConv_classicalHeatSolution : BUCn n → KernelClassicalHeatSolution n` — is `BUCn n` inhabited? If `BUCn n` is a structure that can be empty or the map is trivial... need to check.
- The uniform bridge: check whether the statement is real uniform convergence or something weaker/trivial.
- `timeDerivBCF` continuity/boundedness: check the constructed function is genuinely the integral.
- Check `BUCn`/`BCFn` definitions and whether there is a coercion.
- Check `hasDerivAt_heatOperator` hypothesis `f` a.e.-strongly measurable with bound M; check whether the domination function is integrable on all of the domain (they claim for u ∈ (t/2, 3t/2), and HasDerivAt requires a neighborhood — need t>0 and Ioo (t/2) (3t/2) is a neighborhood of t? yes since t/2 < t < 3t/2. Good).

Also possible issue: `HasDerivAt` in `BCFn n` requires `NormedAddCommGroup`/`NormedSpace` instances for `BCFn n`; check that the norm on `BCFn n` is the sup norm (not e.g. a junk instance). If `BCFn n` is a structure with a norm instance that's not the sup norm, the claim could still be true but semantics differ.

Also `BUCn n` vs `BCFn n` — check whether `heatConv n s f.val` is even in BCFn, i.e. the orbit takes values in BCFn; if `heatConv n s f` for BUC f is bounded continuous, fine.

Another important check: "no blocker closure without constructed input and consumer". So in my review I must verify each claimed sub-obligation discharge has (a) constructed input (e.g., an actual inhabitant of BUCn, a concrete f) and (b) a downstream consumer (a declaration that consumes the theorem in a nontrivial way). If `mildToClassicalBridge_holds` is proved but no non-vacuous consumer exists, classification should say so.

Let me also check whether `Poincare.D12.ParabolicLocal.mildToClassicalBridge` is a Prop that's actually the intended statement, or a weakened one. And whether the `hasDerivAt_heatOperator` has the correct derivative (∂ₜ P_t f) vs. something else.

Let me get started. First, set up the review workspace. Let me check parent release size and toolchain.

Plan:
- Stage: `review/` in my worktree = copy of parent `release/` (source files only) + `.lake` symlink to shared prebuilt mathlib as in parent. Then build.
- Actually to keep it simple and fast: copy the entire parent release dir including `.lake` (which might be big). Let me check sizes first.

Also, I need to save compile-checked checkpoints hourly. Let me set up a todo list.

Let me look at the parent release lakefile.toml and the size.
dsh: reasoning:
Good. Now let me set up a todo list and start the review. First, let me copy the L3 release package into my worktree so I can independently rebuild/compile cited declarations. 1.7G is mostly .lake build; I can copy source only (~? size) and symlink .lake/packages to the shared mathlib prebuild.

Let me check `release/.lake` structure.
dsh: reasoning:
The `.lake/packages` is a symlink to the shared mathlib prebuild. The source is only 4.2M. So I can copy the source tree into my worktree as `review/release/`, and either symlink `.lake/packages` and copy the build artifacts (1.7G — feasible but heavy), or do a fresh build (could take long; the per-file gate was 130s for 356 files, plus mathlib is prebuilt in packages so only the Poincare package + drivers need rebuilding; the full `lake build` took 9239 jobs which includes mathlib? No — mathlib is prebuilt in the shared packages; 9239 jobs likely includes all its own modules).

Actually a fresh build of 356 Poincare files might take a while (the build log was 1.37MB). Let me check the build log timing. Better: copy the .lake/build too so incremental build is fast, but that's 1.7G copy. Alternatively use hard links (`cp -al`)? Different filesystem? Both under /data3 so hard links work and are cheap. But the file ownership is fine.

Actually, an even better approach for independence: DON'T reuse the parent's oleans for the L3 files — rebuild the L3 files from source in my own worktree so I get my own compiled artifacts. But reuse mathlib + D-package oleans from the copied build dir (those are inputs consumed, and I should verify their sources' hashes too...). Hmm, the D12/D10 snapshot declarations are cited; their source hashes should be recorded too. The task says "record source hashes and exact semantic class per declaration" — mainly for the L3 cited declarations. I'll record hashes for the L3 authored files plus the snapshot files that provide the consumed declarations (D12 ParabolicLocal, D12 HeatSemigroup, D10 HeatKernelEuclidean).

Independence plan:
1. Create `review/` in my worktree with a byte-identical copy of the parent release source (excluding .lake), verify hashes.
2. Symlink `.lake/packages` to the shared mathlib path.
3. Copy `.lake/build` via hard links? If I copy with `cp -al`, then editing files won't matter (I won't edit build artifacts). But building in the copied dir may modify oleans in place — with hard links that would corrupt the parent's build dir. Risky. Better: `cp -a --reflink=auto` or plain copy. 1.7G copy is fine (5.1T available); takes maybe 30s.
   But for *independence*, I want to rebuild the L3 modules from source, not trust parent oleans. I can touch the L3 source files (or use `lake build` after modifying a comment? no—must keep byte-identical for review). Alternative: delete the L3 oleans from the copied build dir and rebuild those 6 files (All, Audit, BanachDeriv, Basic, ClassicalBridge, UniformBridge). That forces recompilation of those modules and their dependents, from source. That is a genuinely independent compile of the L3 modules. The D12/D10 dependencies' oleans remain (I can optionally rebuild D12 modules too, but that may be more expensive; let me check how many modules there are... 356 files).

Actually let me just do a full independent rebuild of the whole package in my own worktree: `lake build` with the copied build cache would be incremental; to be sure the L3 files are freshly compiled, delete their oleans (and Audit). The rest being cache-hit is fine, since their sources are hashed and identical (I'll verify the D12/D10 sources' hashes against the parent's ledger/provenance).

Hmm — but is a cached olean "compile-checked" evidence? The acceptance says "compile/read all cited declarations". I will:
- Rebuild the L3 modules from source (fresh).
- Run my own audit driver (fresh) that #checks all cited declarations and collects axioms.
- Cross-check the parent's audit PASS with my own.

Let me also verify the parent's `.lake/build` is up to date w.r.t. sources by checking timestamps... not needed since I rebuild the L3 files.

Also I need to consider: my worktree is currently a D6 release worktree with its own `release/` package (Poincare D1-D6 + L3? Let me check if my worktree's release/Poincare has L3). Let me check: my worktree release/Poincare dirs.

Let me check what's in my worktree's release. Since it's a fresh D6 template, probably no L3. I'll create `review/` subdir.

Plan for the independent package: `review/release` = copy of parent release (source + build). Then rebuild L3 modules fresh.

Let me do this now. First check my worktree's release content and set up.
dsh: reasoning:
Hashes match the parent's claimed authored-hashes.txt exactly. Good — that's hash verification #1 (parent's own claim vs. recomputation).

Now let me set up my independent review package. Copy the parent release to `review/release` in my worktree, including .lake/build (for incremental speed) and symlink packages. Then delete L3 oleans and rebuild.

Actually, to avoid the 1.7G copy, consider: use the parent build dir as a *read* cache? Lake supports `LEAN_PATH`... simpler to copy. 1.7G copy should take <1 min on NVMe. Let me do it in the background while I read the source files.

Actually `cp -a` of 1.7G with many files may take a couple minutes. Let me start it in background.

Also note: sandbox is workspace-write; copying *from* the parent worktree (outside my workspace) should be allowed as read. Destination inside workspace.

Let me start the copy in background and read sources meanwhile.
dsh: reasoning:
Basic.lean looks good. Note: `hasDerivAt_heatOperator` uses `hasDerivAt_integral_of_dominated_loc_of_deriv_le` — I want to check whether this theorem requires the bound to be *integrable* on the whole space, and whether the chosen `bound` indeed dominates for `u ∈ Ioo(t/2, 3t/2)`. It looks OK.

One semantic subtlety: the D10 lemma `hasDerivAt_gaussianKernel n hu_pos (x-y)` gives `HasDerivAt (fun s => gaussianKernel n s z) (gaussianKernel n u z * timeCoeff n u z) u` presumably. Let me check `timeCoeff` matches. In the lemma at line 265 `simpa [timeCoeff] using h` — need to check `hasDerivAt_gaussianKernel`'s derivative expression.

Let me now read ClassicalBridge.lean.
dsh: reasoning:
Copy done. Let me continue reading. Note in `mildToClassicalBridge_holds` the `simpa only [timeDerivIntegral, timeCoeff]` — but `mildToClassicalBridge` is defined in D12; let me read D12 Obligations to check the exact statement.

Very important semantic check: D12's `mildToClassicalBridge` uses `heatConv` and probably `timeDerivKernel` defined in D10 or D12? Let me read it.
dsh: reasoning:
Good. `mildToClassicalBridge` is exactly as claimed. Note the important semantic point: the "derivative in the Banach space of functions" is actually `HasDerivAt` on the Pi type `EuclideanSpace ℝ (Fin n) → ℝ` with the product topology (pointwise convergence), NOT the sup norm. The docstring in D12 says "in the Banach space of functions on ℝⁿ" which is inaccurate — and indeed L3 correctly identifies this: the pointwise bridge is weaker; the uniform statement is the genuine Banach-space statement. That's a semantic finding worth recording: the D12 obligation's prose says "in the Banach space of functions", but the actual type is the product topology HasDerivAt, so the discharged obligation is a pointwise statement. L3's card correctly distinguishes this (claims 4 and the uniform strengthening). Good.

Now let me read UniformBridge.lean and BanachDeriv.lean.
dsh: reasoning:
Uniform bridge looks sound. Note: `exists_slope_eq_timeDerivIntegral` uses MVT requiring differentiability on the open interval (t/2, 3t/2) and continuity on the closed interval; the closed interval [t+h, t] (or [t,t+h]) is contained in uIcc which is shown in Ioo(t/2,3t/2). Good.

Hmm, one thing to check: `hmem` proof for the negative-h case: `Set.uIcc t (t+h)` when t+h < t gives `uIcc t (t+h) = Icc (t+h) t`. The rcases on `Set.mem_uIcc` gives `⟨hv1,hv2⟩ | ⟨hv1,hv2⟩` where the first is t ≤ v ≤ t+h? In Lean, `mem_uIcc` is `a ∈ uIcc b c ↔ b ≤ a ∧ a ≤ c ∨ c ≤ a ∧ a ≤ b`. Yes. For h<0, first disjunct impossible but both branches are handled. Fine.

For the `hht : |h| < t/2` and `hmem` closure: v ∈ uIcc t (t+h), need t/2 < v < 3t/2. Uses |h| < t/2. Fine.

Now BanachDeriv.lean.
dsh: reasoning:
This looks sound. Now important semantic checks:

1. `BCFn n` definition — is it `BoundedContinuousFunction`? Let me check. And `BUCn n`.
2. `heatConv` definition — is it truncated at s ≤ 0? In ClassicalBridge, `heatConv_apply n hs` used for s > 0. The statement `hasDerivAt_heatConv_BCF` is a HasDerivAt at t>0 of the function `s ↦ heatConv n s f.val`; since heatConv is eventually equal to heatOperator near t>0, that's fine.
3. Check `BUCn` is `BoundedContinuousFunction` with a `UniformContinuous` property? Let me read BUC.lean.
4. Check `heatConv_tendsto_self_BUC`.
5. Check the "initial" field in `KernelClassicalHeatSolution` uses `𝓝[>] 0` — strong continuity. Fine.
6. Check whether `KernelClassicalHeatSolution` is vacuous: for n=0 (EuclideanSpace ℝ (Fin 0) = unit), is there a BUCn 0 inhabitant? Yes constants. Fine. But is `heatConv_classicalHeatSolution` a *constructed input* and is there a *consumer*? The `#check` in Audit is a consumer of sorts, but weak. Need to note: no theorem *uses* `KernelClassicalHeatSolution` downstream besides the construction. Let me search for consumers across the release: grep for `KernelClassicalHeatSolution` and `heatConv_classicalHeatSolution` in the whole tree.

Also check `SpatialLaplacianBridge` and `UniformMildToClassicalBridge` have no inhabitants (stuck as Props) — verify no axiom/instance.

Also check: the card claims "The D12 named obligation is discharged: for every n". `mildToClassicalBridge_holds (n) : mildToClassicalBridge n` — but is that theorem's proof non-vacuous? `mildToClassicalBridge n` is a Prop with content, so yes if `BUCn n` is inhabited. Need to check BUCn nonempty. Actually weak: if `BUCn n` were empty, the ∀ would be vacuous. Let me check `BUCn` definition and construct an inhabitant: `BoundedContinuousFunction.const _ 0` should be in `BUCn` if BUCn requires uniform continuity, which const satisfies. Let me read BUC.lean.

Let me now read the definitions in D12 ParabolicLocal/BUC.lean and GaussianConv.lean.
dsh: reasoning:
Interesting! The grep for consumers *outside* L3 found none: no other file in the release package uses `KernelClassicalHeatSolution`, `mildToClassicalBridge_holds`, `hasDerivAt_heatConv_BCF`, etc. That's an important finding for "downstream use": the only consumers of L3's outputs are within L3's own Audit.lean (`#check` probes) and within the L3 files themselves (internal use). No D13/D12 module consumes the discharged obligation, so the D12 obligation is *inhabited* but the D12 assembly does not actually consume it. This is worth recording precisely: "the discharge is a genuine proof of the named Prop, but downstream use is limited to type ascription; no D12/D13 consumer was rebound to it in this worktree." The card itself says "A downstream consumer producing HasDerivAt..." etc. Acceptance says "no blocker closure without constructed input and consumer" — since no blocker is claimed closed, that's fine, but I should flag the downstream-use gap.

Also note the card claim #4: "the heat semigroup is differentiable as a BCFn n-valued curve". Let's verify BCFn is `→ᵇ ℝ` with sup norm — yes, `(EuclideanSpace ℝ (Fin n)) →ᵇ ℝ` is BoundedContinuousFunction with sup norm. Good.

BUCn: `BUCf n` — need to check what that is. Let me read BUC.lean lines around 30-60.

Another thing: `heatConv n t f : BCFn n` for all t; for t ≤ 0 it's presumably defined as f itself (truncated). Let me check GaussianConv.lean lines 174-215.

And `heatConv_tendsto_self_BUC n f : Tendsto (heatConv n · f) (𝓝[>] 0) (𝓝 f.val)` presumably. Let me read.

Let me also check whether `BUCf` is exactly the subtype of bounded continuous uniformly continuous functions and has an inhabited instance. Let me read BUC.lean.
dsh: reasoning:
Important: `heatConv` at t ≤ 0 is defined as `0` (the zero function), NOT `f`. Wait, the docstring says "vanishes for t ≤ 0" — so heatConv n t f = 0 for t ≤ 0. Hmm. But for `heatConv_tendsto_self_BUC` to be true at 0⁺, it must approach f from positive times. That's fine (right-limit). But note `heatConv_classicalHeatSolution`'s `u := fun s => heatConv n s f.val` is defined as 0 for s ≤ 0, and `initial : Tendsto u (𝓝[>] 0) (𝓝 u₀)` — restricted to s>0 so fine.

Hmm — but wait: the D12 `mildToClassicalBridge` statement uses `HasDerivAt (fun s => ... heatConv n s f.val ...) ... t` at t>0. Since heatConv agrees with heatConvPos in a neighborhood of t (t>0), fine. The derivative at t is unaffected by the truncation. Good.

But now there's a subtlety for the `KernelClassicalHeatSolution.u`: is u continuous at 0? Not required. OK.

Also `heatConv n t f = 0` for t ≤ 0 means the "path" is discontinuous at 0 in general. The `initial` field is a `𝓝[>] 0` limit, so fine.

Now, another subtle semantic issue: the D12 obligation's docstring says "the orbit `t ↦ heatConv n t f` has derivative (in the Banach space of functions on ℝⁿ)". The actual statement is HasDerivAt on the function space `EuclideanSpace ℝ (Fin n) → ℝ` with the product topology — I should verify what topology instance Lean uses for the Pi type. For `HasDerivAt` you need NormedAddCommGroup and NormedSpace instances on `EuclideanSpace ℝ (Fin n) → ℝ`. The default instance for `∀ i, E i` is the sup norm (PiLp? no—for a finite index type, Pi instances with sup norm). Actually in Mathlib, `∀ i, E i` with `[Fintype ι]` gets the sup norm `Pi.normedAddCommGroup`? The relevant instance: `Pi.normedAddCommGroup` requires `Fintype` and gives sup norm; also `Pi.instNormedSpace`. Actually the product topology and sup-norm topology coincide for finite index in the sense that the sup norm induces the product topology. For `EuclideanSpace ℝ (Fin n) → ℝ`, the index type is uncountable (not Fintype), so the instance is... Hmm! For an infinite index type, the Pi type `∀ i, ℝ` has instances: `Pi.instTopologicalSpace` (product topology) but for the norm, `Pi.normedAddCommGroup` requires Fintype? Let me check. In Mathlib, `Pi.normedAddCommGroup` is defined for `∀ i, E i` when `[Fintype ι]`. Without Fintype, there's `Pi.instSeminormedAddCommGroup`? Hmm, for infinite products the sup norm is not finite in general. But `HasDerivAt` requires `NormedAddCommGroup` + `NormedSpace`. If there's no such instance, the statement wouldn't elaborate. Since it compiles, there must be an instance in scope. Possibly `Pi.instNormedAddCommGroup` for `∀ i, E i` where each is a normed group and the index is arbitrary — with norm = sup over i of ‖f i‖? For an infinite type sup may be ∞, so it's not a norm. Actually Mathlib does define `Pi.normedAddCommGroup` only for Fintype I believe.

Wait, maybe `HasDerivAt` on `EuclideanSpace ℝ (Fin n) → ℝ` requires only `NormedAddCommGroup` from... Let me check what instance: There is `Pi.instNormedAddCommGroup`? Let me grep mathlib. Actually there's `Pi.normedAddCommGroup` with `[Fintype ι]`. For non-finite index, there's no norm instance in Mathlib (there's `Pi.instSeminormedAddCommGroup`? not with sup over infinite). Hmm, but the code compiles per parent claims, and I'll verify with a fresh build. If it compiled, the instance must exist. Maybe `EuclideanSpace ℝ (Fin n) → ℝ` uses the *pointwise convergence* topology from `Pi.instTopologicalSpace` and a norm instance `Pi.instNormedAddCommGroup` does exist... Let me check mathlib: `Mathlib/Topology/Algebra/Module/Pi.lean` or `Mathlib/Analysis/Normed/Group/Pi.lean` — I recall `Pi.normedAddCommGroup` is for `[Fintype ι]`. Hmm.

Actually, I recall in Mathlib there's an instance for `∀ i, E i` (arbitrary index) with `SeminormedAddCommGroup`? No... There's `Pi.instSeminormedAddCommGroup`? Let me look. Actually, for the derivation on Pi types, `hasDerivAt_pi` requires each coordinate has HasDerivAt and the Pi type has the product topology... `hasDerivAt_pi` is stated for `∀ i, F i` with `[Fintype ι]`? Let me check the actual signature in the local mathlib.

The key question for the review: does `mildToClassicalBridge`'s HasDerivAt use the sup-norm (BCFn-like) topology or the pointwise/product topology? The L3 card claims the D12 obligation is "pointwise" and the uniform version is the "Banach-space" strengthening. Let's verify the actual instance used. Let me check with `#synth` or by inspecting `hasDerivAt_pi`'s signature. We can run Lean to check. That will be part of my independent probe.

Plan for probe file: create `review/SemrevProbe.lean` inside the copied package (as a separate lib or just a file that we `lake env lean`). It will:
- `#check` all cited declarations and print types.
- `#print axioms` all of them.
- `#synth NormedAddCommGroup (EuclideanSpace ℝ (Fin n) → ℝ)` etc. — check the instance.
- Show `hasDerivAt_pi` signature.
- Construct concrete inputs: e.g. `BUCn 3` inhabitant (const 0), `KernelClassicalHeatSolution 3` instance.
- Check non-vacuity: e.g. `heatConv_classicalHeatSolution 1 f` for `f = const 1`; verify `isSolution` has content.
- Check that `UniformMildToClassicalBridge` and `SpatialLaplacianBridge` are Props with no instances (can't prove `¬` easily, but check they're not trivially true: e.g., check `mildToClassicalBridge_of_uniform` direction only; and confirm no axiom supplies them).
- Check the axiom cones using `Lean.collectAxioms` independently with a fail-closed gate over a superset list: all L3 declarations auto-discovered from environment, not a handpicked list! That's a stronger independent audit: enumerate all declarations in `Poincare.L3.HeatTimeDeriv` namespaces via `getEnv` and filter by name prefix, then collectAxioms for each. This catches declarations omitted from the parent's hand-curated list. Great idea — independent and fail-closed.

Also scan for forbidden tokens with my own scanner (comment/string aware-ish). And scan the whole L3 tree for `axiom`, `sorry`, `native_decide`, `unsafe`, `admit`, `proof_wanted`.

Also check: does `All.lean` import all 5 modules? Read All.lean.

Let me also check the D12 `AxiomAudit.lean` to see the prior D12 audit and whether the D12 obligation was reported there as an open obligation.

Then set up the build: copy done. Need to remove L3 oleans to force fresh recompilation of L3 modules, then build `Poincare.L3.HeatTimeDeriv.Audit` and my probe.

Wait — an important independence consideration: the oleans for D10/D12 were built from the parent's sources; I copied both source and build. To ensure the sources I read are the ones compiled, I should verify that the copied sources hash to the same as the *parent's* sources (done for L3; do the same for D10/D12 files consumed) and possibly force rebuild of the D12 modules too. Rebuilding D12 ParabolicLocal modules is probably feasible (maybe 30 files) — let me force rebuild of the modules the L3 files depend on: D12.ParabolicLocal.* and D10/D11 pieces. Actually a simpler approach: delete all oleans under `.lake/build/lib/lean/Poincare` and rebuild the entire Poincare lib. That's the 356 files, which the parent says took 9239 jobs — the parent's full build log. How long? Let me check the build log's start/end. The `l3-check-run.log` says the per-file gate was 130s for 356 files — that's suspiciously fast, likely because oleans were cached (the gate runs `lake env lean` per file which reuses .olean imports but re-elaborates each file; 130s for 356 files means those files are small). But full `lake build` from scratch: the log is 1.37MB; let me check timestamps.

Actually, a full independent rebuild of the whole Poincare package (356 files) is the most defensible, and likely takes ~10-30 min given the parent did it. Let me check the parent build log first/last lines and times.

Let me check what jobs count 9239 means — probably each mathlib dependency module is a job, but cached ones still count? No, jobs are only executed... Hmm, 9239 jobs executed suggests mathlib was rebuilt?! But .lake/packages symlinks to shared prebuild, so mathlib oleans exist. Unless the `lake build` was run in the parent release dir first time and lake rebuilt mathlib because the manifest rev differed. Let me check the build log.

Let me look at the beginning and end of the parent's build log and the l3-check.json.
dsh: reasoning:
Note: "lake build" in 4.0 seconds — because already built. And the log shows "Replayed" for many modules. The `lake-build.log` seems to be a full build from earlier (9239 jobs), captured at some point. But `l3-check.json`'s lake-build was 4s (incremental, no-op).

Note the D6AUDIT reports "partial_declarations 2" — `_unsafe_rec` declarations (compiler-generated for partial defs) exist in D4Audit and SurgeryChain. Not in L3 though. The task acceptance requires no `unsafe` — need to check that L3 files contain no unsafe / partial. The parent's audit reports those 2 partial decls as pre-existing from D4/D9 (in the release package, not L3). I should note that in my review: the *package* contains 2 `_unsafe_rec` partial declarations (pre-existing, unrelated to L3); L3 authored files have none. Let me verify that claim with my own scan.

Now the build: since I copied `.lake/build`, incremental builds will be fast. To force fresh compilation of L3 modules, delete the 6 oleans (and their .ilean/.trace/hash files) and rebuild. But wait: if I delete the olean of `Poincare.L3.HeatTimeDeriv.Basic`, all downstream modules depending on it (including Audit) will rebuild — that's exactly the L3 modules; other Poincare modules don't depend on L3 (grep showed no external consumers). So rebuild will touch just those.

For a stronger independence claim, I might also rebuild the D12 modules that L3 consumes. Rebuilding `Poincare.D12.ParabolicLocal.Obligations` requires its imports (D12 ParabolicLocal chain + D11 etc.). Deleting oleans for all of Poincare/D12/ParabolicLocal/* would rebuild maybe 10 files + dependencies. Let me do that too — but careful, many other files (D13 etc.) may depend on D12.ParabolicLocal, so deleting them forces rebuilding a large downstream cone. Better approach: delete only the L3 oleans, and rebuild; additionally, verify the sources of consumed declarations by hashing them and by running `#check`/`#print` on the *source-level* statements (which are recompiled in my probe's environment, i.e. from the same oleans).

Actually here's a good trick for independence of the *consumed* declarations: do a from-scratch rebuild of everything I need in a separate `.lake` dir? The mathlib prebuild is shared; rebuilding the Poincare lib entirely = 356 files. Let me test how long a full rebuild takes: delete `.lake/build/lib/lean/Poincare` oleans and `lake build Poincare` — the parent's full build finished within its round; the log timestamps? Let me check the log's first line time isn't there. The 9239 jobs include replay of mathlib? "Replayed Poincare.D9..." means it was replayed from cache in that run.

I think a reasonable and time-bounded approach: 
1. delete L3 oleans → rebuild (fresh compile of L3 sources). 
2. delete oleans for the *specific* consumed snapshot modules: D12.ParabolicLocal.Obligations, D12.ParabolicLocal.GaussianConv, D12.ParabolicLocal.GaussianSemigroup, D12.HeatSemigroup.*, D10.HeatKernelEuclidean, D11.HeatKernelBridge.* → rebuild. This forces fresh compilation of the files providing the cited snapshot declarations. Downstream of those modules in the package (D13 etc.) would rebuild too — could be many. Let me check how many files import D12.ParabolicLocal. The earlier grep showed no L3-external consumers of L3, but D13 certainly imports D12. Let me count.

Alternatively: run `lake build` on the specific modules after deleting their oleans; lake will rebuild those and their *dependents within the requested target set only*. If I request `lake build Poincare.L3.HeatTimeDeriv.Audit`, only the L3 cone is requested. The D12 oleans would be reused (cache). If I want D12 fresh, I delete their oleans and build target `Poincare.D12.ParabolicLocal.Obligations` etc. — that rebuilds those modules and their dependencies within that target (upstream only), not downstream. But then rebuilding L3 afterwards is fine.

Plan:
- Delete oleans of D10/D11/D12 modules consumed + L3 modules. Build targets: `Poincare.D12.ParabolicLocal.Obligations`, `Poincare.D12.HeatSemigroup.Smoothing`, `Poincare.D12.HeatSemigroup.StrongContinuityL1`, `Poincare.D11.HeatKernelBridge.InitialCondition`, `Poincare.D10.HeatKernelEuclidean`, then `Poincare.L3.HeatTimeDeriv.Audit`. Actually building `Poincare.L3.HeatTimeDeriv.Audit` after deleting those oleans will rebuild all of them anyway (upstream cone). So simply delete oleans for the entire consumed cone and build Audit. The upstream cone of Audit includes D10/D11/D12 files and mathlib (cached). That's a bounded number of files. Let me count the transitive imports of the L3 modules. Easier: delete oleans for `Poincare/D10*`, `Poincare/D11*`, `Poincare/D12*` and L3, then `lake build Poincare.L3.HeatTimeDeriv.Audit`. That rebuilds those directories + L3. Fine.

But careful: my independent probe file will live maybe at package root as `SemrevProbe.lean` imported via `lake env lean`. If I add a new module file, I need it to build within the package (lake env lean works without registering in lakefile, using the package's LEAN_PATH). I'll use `lake env lean SemrevProbe.lean` for the probe (recompiles the probe each time), which imports `Poincare.L3.HeatTimeDeriv.All` and `Lean.Util.CollectAxioms`. That gives a fresh compile of the probe (its own olean not needed).

Independent axiom audit: write `SemrevAudit.lean` that:
- Enumerates all environment declarations whose name has prefix `Poincare.L3.HeatTimeDeriv.` using `getEnv` + `env.constants` (or `Lean.Environment`), filters `isTheorem`/`isDefinition`... Actually simpler: use `#print axioms` on a comprehensive list, plus auto-enumeration with collectAxioms. I'll auto-enumerate:
  ```lean
  run_cmd do
    let env ← getEnv
    let mut names : Array Name := #[]
    for (n, _) in env.constants.toList do -- env.constants : NameMap ConstantInfo
      if (`Poincare.L3.HeatTimeDeriv).isPrefixOf n then names := names.push n
  ```
  Hmm, `env.constants` is a `NameMap`? In Lean 4, `Environment.constants : NameMap ConstantInfo`? Actually it's `env.constants` of type `NameMap ConstantInfo`? I think `Environment` has `const2ModIdx` and `constants : NameMap ConstantInfo`. Let me check API: In Lean 4 core, `Environment` has field `constants : NameMap ConstantInfo` (yes, `Lean.Environment.constants`). `NameMap` has `.toList : List (Name × α)`. There's also `env.fold`. I'll test compile.

   For each name, skip non-declarations? collectAxioms works on any constant; for definitions/theorems it returns the axioms in the cone. For structure fields/ctors it works too. Fail-closed: collect axioms for every constant with the prefix, report any outside approved set. Also detect `sorryAx` and `Classical.choice` allowed.

   Additionally check for `unsafe`/`partial` declarations: ConstantInfo has `safety` field? `ConstantInfo.safety : DefinitionSafety` (safe/unsafe) — can check `ci.safety == .unsafe`. And `isUnsafe`? Let me use `Lean.ConstantInfo.safety`. Also check for `declaration` kinds: theorem/def/opaque/axiom/ctor/rec. Any `axiom` with the L3 prefix → fail. Also any `ConstantInfo.axiomInfo` → fail.

- Enumerate L3 constants and print types for semantic classification: `#check` each.

This auto-enumeration is a strong independent check: it catches everything under the namespace, including auxiliary private definitions (private defs get mangled names like `_private. ...`; hmm, private declarations have names like `Poincare.L3.HeatTimeDeriv._private.0.foo`? Actually private names are `_private.Poincare.L3.HeatTimeDeriv.0.name`, which do NOT have the namespace prefix. So my prefix enumeration might miss private declarations. To catch them, I should also enumerate names containing `L3.HeatTimeDeriv` or use the module's declarations. Better: enumerate all constants and filter for `Name.toString` containing "HeatTimeDeriv" — catches private ones too. Let me do both.

But wait: the parent's "51 audited declarations" = 42 authored + 9 snapshot. My auto-enumeration will likely find more (e.g., instance declarations, auxiliary lemmas, structure mk, etc.), which is fine — more coverage, fail-closed. If any has an unapproved cone, that's a finding. Note: `KernelClassicalHeatSolution.mk`, `.T`, `.T_pos`, etc. would show up. Those are fine.

Also note: for consistency, I should audit the *entire* L3 prefix plus the 9 snapshot declarations.

Then semantic probes:
- `#print Poincare.D12.ParabolicLocal.mildToClassicalBridge` to get the exact statement.
- `#print Poincare.L3.HeatTimeDeriv.mildToClassicalBridge_holds` type.
- `#check` with explicit type ascriptions.
- Check instance used for `HasDerivAt` on the Pi type: `#synth NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)` and `#print Pi.normedAddCommGroup`? Let me check whether the norm is the sup norm over an uncountable index. If the instance `Pi.instNormedAddCommGroup` requires Fintype, then for `EuclideanSpace ℝ (Fin n) → ℝ` the index is infinite and there is no such instance... but the code compiles, so what instance is used? Possibly `Pi.instNormedAddCommGroup` does exist for arbitrary index with norm = ⨆ i, ‖f i‖? That can't be finite. Hmm, maybe the instance chosen is from `EuclideanSpace`: `EuclideanSpace ℝ (Fin n)` is a *finite-dimensional* type, but instance search works on the type structure, not cardinality. So `∀ x : EuclideanSpace ℝ (Fin n), ℝ` — instance search for NormedAddCommGroup: candidates include `Pi.normedAddCommGroup` (needs Fintype (EuclideanSpace ℝ (Fin n)) — no), and `Pi.instNormedAddCommGroup`? Hmm. Maybe there's an instance for `∀ i, ℝ` using `iSup` and it's a `SeminormedAddCommGroup`? For `NormedAddCommGroup` you need the norm to be finite... 

Actually wait. In Mathlib there is:
```
instance Pi.normedAddCommGroup [∀ i, NormedAddCommGroup (E i)] : NormedAddCommGroup (∀ i, E i)
```
Hmm, I don't think that exists for infinite index. Let me check: Mathlib `Mathlib/Analysis/Normed/Group/Pi.lean` has `Pi.normedAddCommGroup [Fintype ι]`. There's also `Pi.instSeminormedAddCommGroup`? Let me grep the mathlib source in .lake/packages.

Actually, the key: `hasDerivAt_pi` in Mathlib — its statement probably is:
```
theorem hasDerivAt_pi {𝕜} [NontriviallyNormedField 𝕜] {ι : Type*} [Fintype ι] ...
```
Hmm no. Let me grep. This matters for the semantic class of `mildToClassicalBridge`. Actually, let me just check in the source.

Let me grep mathlib for `theorem hasDerivAt_pi`.

I'll do that now, and also check what instance `NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)` resolves to via a probe.

Let me first check the mathlib sources.
dsh: reasoning:
Interesting! `hasDerivAt_pi` is stated for arbitrary index `ι` with pointwise `NormedAddCommGroup (E' i)` instances — but for the *product* `∀ i, E' i` to have a `NormedAddCommGroup` instance (needed for the HasDerivAt on the left side), there must be an instance in scope. Let me search for the Pi instance of NormedAddCommGroup for arbitrary index. Search in Mathlib for `Pi.instNormedAddCommGroup` or instance with `∀ i,`.
dsh: reasoning:
No `Pi.normedAddCommGroup`. So how does `HasDerivAt (fun s : ℝ => fun x => ...) ... t` typecheck for `EuclideanSpace ℝ (Fin n) → ℝ`? Maybe there's an instance for `∀ i, E i` in `Mathlib/Topology/Algebra/Module/Pi.lean` named differently, or via `Pi.instNormedAddCommGroup` from `Mathlib/Analysis/Normed/Group/Constructions.lean`? Let me search for "∀ i" in instance declarations, or check `#synth` directly with Lean. Faster: write a quick probe.

Actually! The key: `HasDerivAt` requires `[NormedAddCommGroup F] [NormedSpace 𝕜 F]`. The type `EuclideanSpace ℝ (Fin n) → ℝ` might get its instance from `EuclideanSpace` being a *finite* type... no.

Wait — maybe `Pi.instNormedAddCommGroup` does exist but with the `sInf`/`iSup`-based norm... Let me search more broadly: `grep -rn "instance.*: NormedAddCommGroup (∀" Mathlib/`. Or check if it's in core Lean (`Init`/`Lean`)? No.

Hmm, maybe `SeminormedAddCommGroup` for Pi with `∀ i` exists in Mathlib as `Pi.instSeminormedAddCommGroup`? Let me search `SeminormedAddCommGroup (∀ i` and `NormedAddCommGroup (Π i`.

Let me just run a probe with `#synth` — this is fast and authoritative. I'll create my probe file now.

Actually wait — perhaps the instance is from `BoundedContinuousFunction`? No, the domain type is a plain function type.

Hold on, maybe there IS an instance: `Pi.normedAddCommGroup` might be in `Mathlib/Analysis/Normed/Group/Pi.lean`, which doesn't exist; let me find the file that defines `Pi.seminormedAddCommGroup`. grep -rn "seminormedAddCommGroup" with "Pi." prefix.
dsh: reasoning:
`Pi.normedAddCommGroup` exists (probably in a file I haven't found, maybe `Mathlib/Topology/Algebra/Module/Pi.lean` or `Mathlib/Analysis/Normed/Group/Basic.lean`) — used in Matrix/Normed. Let me find its definition to see whether it requires Fintype. `grep -rn "def Pi.normedAddCommGroup\|protected def.*normedAddCommGroup"`... Anyway, the key question: for an infinite index type, does `Pi.normedAddCommGroup` apply? `Matrix/Normed.lean:183: fast_instance% Pi.normedAddCommGroup` is for `m : Type` (finite via Fintype in context). Let me find the instance declaration.
dsh: reasoning:
Not finding the definition — maybe it's `Pi.instNormedAddCommGroup` auto-generated from an instance in `Mathlib/Topology/Algebra/Module/Pi.lean`? Let me search for "instNormedAddCommGroup" or a declaration `instance : NormedAddCommGroup (∀ i, ...)`. Or grep the mathlib source for "sInf" ... Actually, simplest: run `#synth` in Lean and `#print` the instance.

Let me now write my probe file. It will be the core independent artifact. I'll create `SemrevProbe.lean` at the review package root.

Probe contents:
1. `import Poincare.L3.HeatTimeDeriv.All` + `import Lean.Util.CollectAxioms`.
2. `#synth NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)` and `#print instNormedAddCommGroup`? To identify the instance name, use `set_option pp.all` or `#synth` output gives the instance name. Actually, `#synth` prints the instance. Let me try.
3. Print types of all cited declarations: `#check @name` and `#print name`.
4. Auto-enumerate all constants containing "HeatTimeDeriv" and collectAxioms with fail-closed gate.
5. Check no axioms/unsafe/partial: iterate constants; check `ConstantInfo.safety`? API: `Lean.ConstantInfo` has field `safety : DefinitionSafety`. Also `ConstantInfo.type`, `ConstantInfo.levelParams`. For detecting axiom declarations: `match ci with | .axiomInfo _ => ...`. For `sorryAx`: collectAxioms would report `sorryAx`.
6. Constructed input: build a `BUCn 3` inhabitant and a `KernelClassicalHeatSolution 3` from it; print `#check`. Also an explicit `n=0` and `n=1` instance.
7. Consumer check: derive from `hasDerivAt_heatConv_BCF` something non-trivial, e.g. apply `.hasDerivAt` at a point? Or compute `timeDerivBCF` value at a point; or use `HasDerivAt.continuousAt` to get continuity of the orbit in BCFn. That's a genuine consumer I can construct *in the probe*: `(hasDerivAt_heatConv_BCF n ht f).continuousAt`. But "consumer" in the acceptance sense probably means a downstream declaration in the artifact. I'll record both: (i) in-artifact consumers (internal: `mildToClassicalBridge_of_uniform`, `heatConv_classicalHeatSolution`, `#check` probes), (ii) absence of external package consumers (grep). And I can add my own probe consumer to demonstrate consumability.

8. Domain checks:
   - Verify `mildToClassicalBridge 3` type printed matches: `∀ {t}, 0<t → (f : BUCn 3) → HasDerivAt ... (fun x => ∫ ...) t`.
   - Verify the instance for `HasDerivAt` on Pi type: print the normed group instance. If it's `Pi.normedAddCommGroup` requiring Fintype... then it can't apply; let me see.
   
Let me check how `Pi.normedAddCommGroup` is defined — maybe Mathlib defines it for arbitrary index using `∀ i, ‖f i‖ ≤ C` (bounded functions) — no. I recall in recent Mathlib, `Pi.normedAddCommGroup` is:
```
instance Pi.normedAddCommGroup [∀ i, NormedAddCommGroup (E i)] : NormedAddCommGroup (∀ i, E i) := ...
```
Hmm, that would be wrong for infinite products (norm would be ⨆ which may be infinite). Actually Mathlib does define `Pi.seminormedAddCommGroup`? Let me grep for "instance Pi.seminormedAddCommGroup" in all of mathlib.

The grep earlier for "Pi.seminormedAddCommGroup" found only Matrix/Normed (which is `fast_instance%` — meaning the instance is a *copy* of an existing one under a different name). Let me find the original: `grep -rn "seminormedAddCommGroup" Mathlib/Analysis/Normed/Group/Pi.lean` — file might not exist. Let me list files in Mathlib/Analysis/Normed/Group/.
dsh: reasoning:
Not found yet. Let me just check with Lean directly. Time to write the probe. I'll also start the build in parallel: first force-rebuild L3 modules.

Plan of commands:
1. Remove copied L3 oleans + D10/D11/D12 oleans for the consumed cone. Actually, let me determine the transitive import cone of the L3 modules. Simplest: `lake env lean --deps`? There's `lake exe`... Alternatively use `lean --src-deps`? Hmm.

Alternative: just delete all oleans in `.lake/build/lib/lean/Poincare/D10*`, `D11*`, `D12*`, `L3*` and then `lake build Poincare.L3.HeatTimeDeriv.Audit`. Note: `lake build <module>` builds the module and *its dependencies* (upstream), not downstream. So only the cone needed will be rebuilt. That's precisely what I want, and the fresh compile of D10/D11/D12 files gives additional independence.

How many files in D10/D11/D12? Let me count. Then time the build.

Also note: deleting oleans directory-wide may leave dangling .trace files; lake handles recompilation based on hash files. Actually lake compares trace/hash; deleting oleans forces rebuild. Let me do it with `find -name '*.olean' -delete` etc. for those dirs.

Wait, but there's an important subtlety: the `D6AuditReport.lean`/`Audit/` files etc. are not in the cone. Fine.

Let me set up:
- Probe file at `review/release/SemrevProbe.lean` (not part of package libs but `lake env lean` can compile it as a standalone file; imports resolve via LEAN_PATH set by lake).

Let me write the probe with:
```lean
import Poincare.L3.HeatTimeDeriv.All
import Lean.Util.CollectAxioms
import Lean.Elab.Command

open Lean Elab Command

-- 1. instance diagnostics
#synth NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)
#synth NormedSpace ℝ (EuclideanSpace ℝ (Fin 3) → ℝ)
#print Pi.normedAddCommGroup
```
Hmm `#print Pi.normedAddCommGroup` may fail if the name is different. Use `#synth` and capture the synthesized instance name from output. Let me first run a tiny experiment to see.

Actually, from `hasDerivAt_pi`'s variables: it's a theorem about `φ : 𝕜 → ∀ i, E' i`, and for the *statement* to make sense, `∀ i, E' i` must have `NormedAddCommGroup`+`NormedSpace` instances in scope at the theorem's declaration. Since the theorem is proved in Mathlib, such instances must exist for arbitrary `ι` — likely `Pi.instNormedAddCommGroup` defined in core or Mathlib. Let me search for "∀ i, E i" in instance syntax with different formatting:
`grep -rn "instance.*: NormedAddCommGroup (∀" Mathlib/` returned nothing. Maybe the Pi instance lives in `Mathlib/Topology/Algebra/Module/Pi.lean` as `Pi.instNormedSpace`? Let me grep `Pi.instNormed` and `Pi.instSeminormed`.
dsh: reasoning:
Let me stop searching textually and just run Lean. I'll create a small probe.

First let me check the toolchain availability: ELAN_HOME. Then write probe files.

Let me write the main probe file now. I'll create it in my worktree under `review/probe/SemrevProbe.lean`, but it must be compiled with the package's LEAN_PATH. `lake env lean <file>` in the release dir should work for any file path.

Let me write probe stages:

Stage A (quick diagnostics): instance resolution and exact types.
Stage B (audit): auto-enumeration + collectAxioms.
Stage C (semantics): constructed inputs and consumers.

Let me write Stage A first and run.
dsh: reasoning:
Now, before running, I need to set up the environment and force a fresh rebuild. Let me first check the elan env and run a trivial `lake env lean` on the probe to see warm-up.

Actually the probe imports Poincare.L3.HeatTimeDeriv.All, whose oleans currently are copied from parent. Let me first delete the L3 oleans and the D10/D11/D12 oleans and rebuild, to ensure my probe compiles against my own fresh oleans.

Wait, order: delete oleans → `lake build Poincare.L3.HeatTimeDeriv.All` (rebuilds cone) → then `lake env lean probe/StageA.lean`. But careful: `lake env lean` on a file outside the package dir? The probe is at review/probe, outside review/release. lake env sets LEAN_PATH relative to the package. It should still work (LEAN_PATH is absolute). Actually `lake env lean <path>` runs `lean` with the environment of the package; the file path can be anywhere. Imports resolve from LEAN_PATH. Fine.

Let me check the environment first.
dsh: reasoning:
The elan default toolchain isn't configured globally but the package has `lean-toolchain` pinning `leanprover/lean4:v4.34.0-rc2`. Running from within the release dir should pick up the toolchain via elan's directory resolution. Let me try running `lake --version` from within the release dir. Also the find for "*.lean" in .lake build dirs obviously returns 0 (that was a silly check).

Let me verify.
dsh: reasoning:
Toolchain resolves inside the package. Now let me force a fresh rebuild of the consumed cone. I'll delete oleans (and hash/trace/ilean) for Poincare/D10, D11, D12, L3 in the copied build dir, then run `lake build Poincare.L3.HeatTimeDeriv.All` in the background and monitor.

Actually wait: is the `.lake/build` layout per-module directories with `.olean` + `.ilean` + `.trace` + `.olean.hash`? Yes as seen. Let me delete all `*.olean*` and `*.ilean*` and `*.trace` files under those dirs. Then build.

But note: the build may also need to rebuild `Poincare.D7` etc. if the D10/D12 files import them — they're cached, so no. Only the deleted ones (and possibly rebuild cascade inside the deleted set).

Let me count how many .lean files are in D10/D11/D12: 
find Poincare/D10 Poincare/D11 Poincare/D12 -name "*.lean" | wc -l

Let me do this and start the build in the background. Also, to be efficient, I'll run the build with `-j` default (lake uses all cores?). Let me check nproc.
dsh: reasoning:
192 cores! Great. 82 .lean files in the consumed cone (D10, D11, D12, L3). A fresh rebuild of those with 192 cores should be quick (a few minutes).

Let me delete the oleans and rebuild. Note the D12 files may be big. Let's go.

Command:
```
cd review/release
find .lake/build/lib/lean/Poincare/D10 .lake/build/lib/lean/Poincare/D11 .lake/build/lib/lean/Poincare/D12 .lake/build/lib/lean/Poincare/L3 -type f \( -name '*.olean' -o -name '*.ilean' -o -name '*.trace' -o -name '*.hash' \) -delete
lake build Poincare.L3.HeatTimeDeriv.All 2>&1 | tail -30
```
Run in background with job output. Also capture full log to file.

Note: `lake build` rebuilds `.olean` and updates `.lake/build/lib/lean/...`, and it may also rebuild modules that import L3? No, building a target builds its dependencies, not dependents.

Let me start it.
dsh: reasoning:
While that builds, let me continue the semantic analysis by reading remaining source files and the parent's other evidence. Let me:
1. Read the parent's `tools/l3_check.py` to know what its forbidden-token scan does.
2. Read the D12 `heatConv_tendsto_self_BUC` statement and `GaussianSemigroup` around line 331.
3. Read the card's JSON (`longrun/results/L3-analytic-critical-path.json`).
4. Check the blocker ledger U6/U8/U12 definitions in the D13 critical-path review (parent artifact references). Where is that? `D13-critical-path-review` worktree. Let me check what U6/U8/U12 are exactly, to verify the classification.
5. Check `manifest/blockers.json` in my worktree for U6/U8/U12 entries (the D6 release has blockers list). Let me grep for U6/U8/U12.

Let me do these now.
dsh: reasoning:
Let me see the rest of that JSON and the D13 critical-path review's U6/U8/U12 definitions. Let me look at the D13 review card lines around 190-260 to get exact U6/U8/U12 definitions.
dsh: reasoning:
Good. Now let me check where U6, U8, U12 are defined precisely (probably in D1_perelman_ledger or the manifest blockers.json in the D6 worktree). Let me grep in the queue/manifest for "U6" definitions.
dsh: reasoning:
Good — U6/U8/U12 are all `upstream-mathlib-gap`, open. The L3 card says none closed — consistent.

Now let me check the build progress.
dsh: reasoning:
Build still running. Meanwhile, let me read the parent's tools/l3_check.py and the negative control, and diff the copied L3 sources vs. parent (already hashed identical). Also examine the claimed parent evidence log `l3-check-run.log` for the build.

Let me also inspect the D6 weekly release's `manifest/verified-declarations.json` for L3-related info? Not needed.

Key remaining semantic questions:

A. `mildToClassicalBridge_holds` for every n — need to verify non-vacuity of `BUCn n` (inhabited) and that `HasDerivAt` in the product space is a genuine derivative statement. Also whether the product topology instance is a normed group instance (if it is the sup-norm over an infinite index — could be `Pi.normedAddCommGroup` with `⨆` and finite values?). Let me wait for the probe.

Actually, I realize there's a subtlety I should check: is there a `NormedAddCommGroup` instance on `EuclideanSpace ℝ (Fin n) → ℝ` at all? Since the statement compiled, yes. It could be `Pi.instNormedAddCommGroup` if Mathlib defines it for arbitrary index (I couldn't find it textually). Hmm, maybe it comes from `Mathlib/Analysis/Normed/Space/Pi.lean`? Let me search names: `grep -rn "Pi.normedAddCommGroup" Mathlib/Analysis/Normed/ | head` found nothing except Matrix. Let me search for the declaration `normedAddCommGroup` in `Mathlib/Topology/Algebra/Module/Pi.lean`. Hmm, that file may not exist. Let me list files: `glob` for "**/Pi.lean" in mathlib.

Whatever, the probe will tell.

B. The derivative domain: `HasDerivAt (fun s => heatConv n s f.val) ... t` — `heatConv` is truncated to 0 at s ≤ 0, but near t>0 it's the convolution. Good.

C. `UniformMildToClassicalBridge` — note it states ∀ h with 0<|h|<δ: |(K_{t+h}f(x) - K_t f(x))/h - D_t f(x)| < ε for all x. This is a genuinely uniform statement (as a *condition*, each ε admitted). The proof is fine. But is it *equivalent* to the sup-norm HasDerivAt? Yes essentially, and BanachDeriv derives the BCFn HasDerivAt from it with ε/2 and norm_le. Good.

D. Does `hasDerivAt_heatConv_BCF` have the right derivative? `timeDerivBCF n ht f` is the bounded continuous function `x ↦ timeDerivIntegral n t f.val x`. Good.

E. Potential semantic issue: In `BanachDeriv.lean`, `dist_eq_norm` + `BoundedContinuousFunction.norm_le` — need the norm on BCFn. Fine.

F. Now the critical independent semantic question: are the *claims* in the result card all supported?
   - Claim 1: repair of Basic.lean — verified by hashes and by reading; the round-1 errors are not independently verifiable, but the file compiles.
   - Claim 2: D12 obligation discharged for every n — yes, and it's exactly typed.
   - Claim 3: uniform strengthening — yes.
   - Claim 4: BCFn-valued HasDerivAt — yes.
   - Claim 5: `heatConv_classicalHeatSolution : BUCn n → KernelClassicalHeatSolution n` on (0,1) with strong continuity as initial condition — yes. But semantic caveat: "kernel-classical heat solution" is *not* the classical heat equation `∂ₜu = Δu`; it's `∂ₜu(t,x) = ∫ ΔK_t(x-y) u₀(y) dy`. The card does say "in kernel-Laplacian form" in the structure docs and notes the spatial residual. The card's claim 5 says "packaged kernel-classical heat solution ... with the D12 strong-continuity theorem as the initial condition" — accurate but the name is stronger than the content (the structure name `KernelClassicalHeatSolution` could be misread). I'll note this as a naming/semantic class caveat: the declaration's exact semantic class is "Euclidean model, kernel-Laplacian form; not the classical heat equation" and the initial-condition field is `𝓝[>] 0` strong continuity.
   - Claim 6: two residuals stated not assumed — verify that there's no `axiom`/`opaque` providing them. Auto-enumeration will show. Also `UniformMildToClassicalBridge` is proved (was a residual in the earlier round). `SpatialLaplacianBridge` is a def : Prop with no inhabitant. Verify its semantic content: `Δ (fun z => heatConv n t f.val z) x = ∫ ΔK_t(x-y) f y`. Yes, statement-only.
   - Claim 7: axiom audit 51 declarations — I'll re-run with auto-enumeration (likely more than 51) and negative control.

G. Blocker classification: U6/U8/U12 open. The L3 card says U6 open with residuals (a) manifold heat kernel/parametrix/regularity untouched, (b) SpatialLaplacianBridge statement-only, (c) continuum parabolic maximum principle untouched. Is this accurate? U6 blocker text: "No heat-equation theory, heat kernel, or parabolic PDE layer; the continuous parabolic maximum principle cannot be proved and is a statement-only interface." L3 added Euclidean time differentiability. Is U6 still open? Yes, since the manifold layer and the maximum principle remain. Since L3 has now proved Euclidean heat semigroup differentiability and initial condition (D12's BUC strong continuity), one could argue U6's "No heat-equation theory" is partially addressed at the Euclidean model level. The classification "open" is conservative and correct. The card explicitly avoids claiming closure. Good.

U8: untouched; the card says the flat model's linear heat part now differentiable as a Banach-space curve "removes one packaging obstacle for the Duhamel upgrade but not the quasilinear derivative loss". This is a claim of relevance, not closure. Plausible: `hasDerivAt_heatConv_BCF` provides the derivative for the linear part; the D13 assembly's `MildClassicalOutput` remains a hypothesis. I should verify `MildClassicalOutput` is still a hypothesis in the D13 Deturck producer package (in this release). Let me grep.

U12: untouched. The card claims "unchanged from the D13 critical-path review". Verify no L3 change touches backward/conjugate heat: L3 is forward heat only. Good.

H. "no blocker closure without constructed input and consumer": Since no blocker is claimed closed, this is satisfied trivially; but I should check the *sub-obligation* discharge has constructed input and consumer:
   - constructed input: `BUCn n` inhabited by e.g. `BoundedContinuousFunction.const`; I'll construct it in my probe. Also `heatConv_classicalHeatSolution 2 f` is applied to a *hypothetical* f in `#check` — but the def itself takes any f. In my probe I'll instantiate with a concrete f and extract `.isSolution`/`.initial`.
   - consumer: internal consumption: `mildToClassicalBridge_of_uniform` consumes `UniformMildToClassicalBridge`; `heatConv_classicalHeatSolution` consumes `hasDerivAt_heatConv_apply_kernelLaplacian`; `mildToClassicalBridge_holds` is consumed by nothing else in the package (only `#check` type ascriptions). That's a real observation: the discharged obligation is not consumed by the D12/D13 assembly. I must check whether D12 has an assembly that expects `mildToClassicalBridge` as a hypothesis. Let me grep the whole package for uses of `mildToClassicalBridge` (not `_holds`).

Let me do that grep now (source only, excluding L3).
dsh: reasoning:
Confirmed: no Lean consumer of `mildToClassicalBridge_holds` outside the L3 directory; D12/D13 only mention it in comments. `MildClassicalOutput` is a structure in D13 PicardModel used as a hypothesis. So the discharge is not wired into any downstream proof. That's a legitimate review finding: "the obligation is inhabited, but the D12/D13 assembly is not rebound to the inhabitant; the consumer chain is only type-ascription + comments."

The acceptance says "no blocker closure without constructed input and consumer" — since L3 does not claim closure, I record this as a downstream-use limitation, not a blocker closure violation. But it affects the semantic classification: "proved (Euclidean model), unconsumed".

Let me read `MildClassicalOutput` and `PicardModel` around 280-330 to describe precisely.

Also check `Poincare/D13/ToppingAdapter/ShortTime.lean` around line 39.

Let me also check whether the L3 card's statement "removes one packaging obstacle for the Duhamel upgrade" is evidenced. In PicardModel, `MildClassicalOutput` is a structure over an abstract `DeTurckParabolicProblem` — the L3 BCFn derivative is about the flat Gaussian semigroup, not the abstract problem. So "removes one packaging obstacle" is a plausible but unformalized bridge claim. I'll note it as "informal relevance claim, not a formal consumer".

Let me read those files.
dsh: reasoning:
Confirmed: `MildClassicalOutput` is an abstract hypothesis over `DeTurckParabolicProblem`, not connected to the L3 BCFn theorem. The D12 `mildToClassicalBridge` is a Euclidean semilinear-heat obligation; `MildClassicalOutput` is the quasilinear abstract continuum problem's classical output. They are different (though related) — L3's discharge of `mildToClassicalBridge` does not provide `MildClassicalOutput`. Good to record precisely.

Let me now check the build status.
dsh: reasoning:
Still running. Let me continue with static analysis.

Let me read the parent's `tools/l3_check.py` to understand its scan and gate, so I can independently replicate the forbidden-token scan.
dsh: reasoning:
The parent's scanner strips comments/strings then regex-searches for tokens. Note: `axiom` as a token — since comments are stripped, docstring mentions don't count. Good. But it only scans `Poincare/L3/**/*.lean`. Fine.

Potential gap in the parent's scan: it strips strings and comments but does NOT handle `--` inside a string... whatever; my independent scan can be stricter/different: I'll scan the raw file for tokens too and inspect any hits manually, plus a stripped scan.

Now, one important check: the parent's authored glob `Poincare/L3/**/*.lean` covers 6 files. My independent check should also confirm there are no other `.lean` files under `Poincare/L3` (e.g., a hidden file with an axiom). Let me list all files under Poincare/L3.

Let me also check `All.lean` imports only 4 modules (Basic, ClassicalBridge, UniformBridge, BanachDeriv) and Audit imports All. Consistent.

Now let me think about what the strongest independent semantic checks are. I want to detect any "weasel" in the claimed theorems:

1. **Non-vacuity of the main derivative statement**: `hasDerivAt_heatOperator` requires `f` a.e.-strongly measurable with uniform bound. Constant f works; the theorem then says derivative = integral; the consistency identity proves the integral is 0 for constants, so the theorem is consistent. Good.

2. **Is `heatOperator` the same as `heatConv`?** `heatOperator` defined in D12.HeatSemigroup as the integral; `heatConv_apply` says heatConv n t f x = ∫ K_t(x-y) f y for t>0. Good.

3. **Does `hasDerivAt_heatConv_apply` prove the derivative of the actual function or of an eventually-equal function?** `HasDerivAt.congr_of_eventuallyEq` — the derivative is for `fun s => heatConv n s f.val x`, and they rewrite to `heatOperator n s f.val x` eventually near t (since s>0 near t>0). HasDerivAt is local, so fine.

4. **Uniform statement quantifier order**: `∀ ε > 0, ∃ δ > 0, ∀ h, 0<|h|<δ → ∀ x, |...| < ε`. Correct uniformity.

5. **Banach derivative**: `HasDerivAt (fun s => heatConv n s f.val) (timeDerivBCF n ht f) t` in `BCFn n` with sup norm. The proof uses `dist_eq_norm` and `BoundedContinuousFunction.norm_le`. Need to confirm the norm on BCFn is sup norm; yes `BoundedContinuousFunction` has sup norm.

6. **Semantic class per declaration**: I'll produce a table of all cited declarations with (name, type summary, class). Classes: def (data), theorem (proved), snapshot-consumed, statement-only Prop (no inhabitant), structure (bundle), etc.

7. **Axiom audit**: auto-enumerate.

8. **Euclidean scope check**: grep for `Manifold`, `Riemannian`, `Compact`, `Ricci` in L3 sources to confirm no manifold content smuggled. Also check hypotheses: everything is `EuclideanSpace ℝ (Fin n)`, `volume`.

9. **Derivative domains check**: 
   - `hasDerivAt_heatOperator` at t>0 (domain (0,∞)).
   - `uniformMildToClassicalBridge_holds` at t>0.
   - `hasDerivAt_heatConv_BCF` at t>0.
   - Note: at t=0 no derivative (kernel singular), correctly excluded.
   - The MVT step restricts |h| < t/2, fine.
   - The function `s ↦ heatConv n s f` is defined as 0 for s ≤ 0 — the statement is at t>0 only. Good.

10. **Check `SpatialLaplacianBridge` statement**: `Δ (fun z => heatConv n t f.val z) x = ∫ ΔK_t(x-y) f y`. The left side is the Laplacian of the *orbit at time t as a function of x*; correct. Statement-only.

11. **Check for hidden `Classical.choice` usage in definitions**: allowed.

12. Check the *negative control*: run it and confirm exit 1 naming the axiom. I'll copy it to my review dir and run `lake env lean` — should fail with error. That verifies fail-closedness of the parent's gate. But my own audit driver needs its own negative control: I'll write one with an axiom and confirm my collectAxioms loop catches `l3NegControlBadAxiom`-like names. Actually more precisely: my run_cmd loop is fail-closed if it throws on unapproved axioms; I can test it by temporarily including a deliberate axiom in a *separate* probe... Simplest: run the parent's negative control file with my own audit-style check? The parent's negcontrol file itself just declares an axiom and `#print axioms` of a theorem depending on it. Running `lake env lean` on it should fail (nonzero exit) because `#print axioms` prints an "depends on axioms" info line that isn't an error... wait, why does it exit 1? Let me read the negative control.

Let me read L3NegControl.lean.
dsh: reasoning:
Good. Now let me check the build. Also, I should plan the core audit file. Let me write it now (it will compile once the build finishes).

`SemrevAudit.lean`:
```lean
import Poincare.L3.HeatTimeDeriv.All
import Lean.Util.CollectAxioms
import Lean.Elab.Command

open Lean Elab Command

namespace SemrevL3

def approved : List Name := [``propext, ``Classical.choice, ``Quot.sound]

/-- every constant whose name textually contains "HeatTimeDeriv" (catches private helpers) -/
run_cmd do
  let env ← getEnv
  let mut all : Array (Name × ConstantInfo) := #[]
  for (n, ci) in env.constants.toList do
    if ("HeatTimeDeriv".isSubstr n.toString) then all := all.push (n, ci)
  ...
```
Need to check the API: `Environment.constants : NameMap ConstantInfo`? In Lean 4 core: 
```
structure Environment where
  constants : NameMap ConstantInfo
```
Hmm, I believe `Environment` has `const2ModIdx : Std.HashMap Name ModuleIdx`, `constants : NameMap ConstantInfo`, `extraConstNames : NameSet`. `NameMap` is `Std.TreeMap Name α` with `.toList`. There's also `env.constants.toList`. I'll try.

For safety detection, `ConstantInfo.safety : DefinitionSafety` — in Lean 4, `structure ConstantInfo` has fields: `name`, `levelParams`, `type`, `safety : DefinitionSafety`, plus `ConstantVal`? Actually `ConstantInfo`:
```
inductive ConstantInfo where
  | axiomInfo (val : AxiomVal)
  | defnInfo (val : DefinitionVal)
  | thmInfo (val : TheoremVal)
  | opaqueInfo (val : OpaqueVal)
  | quotInfo (val : QuotVal)
  | inductInfo (val : InductiveVal)
  | ctorInfo (val : ConstructorVal)
  | recInfo (val : RecursorVal)
```
And `AxiomVal` has `name`, `levelParams`, `type`, `isUnsafe`. `DefinitionVal` has `safety : DefinitionSafety`. I'll pattern match:
- `.axiomInfo _` → record as AXIOM (should be none in L3).
- `.defnInfo v` with `v.safety == .unsafe` → unsafe. `DefinitionSafety` is `.safe | .unsafe`.
- `.opaqueInfo v` → treat as opaque (check safety?).
Also `_unsafe_rec` names: check name suffix `_unsafe_rec`.
- For each constant, `collectAxioms n` and check subset of approved. This will include `propext`, `Classical.choice`, `Quot.sound`, and possibly `Classical.choice` etc.

Also the audit must include the 9 snapshot declarations explicitly (they don't have "HeatTimeDeriv" in the name): `Poincare.D12.ParabolicLocal.{mildToClassicalBridge,heatConv,heatConv_apply,heatConv_tendsto_self_BUC}`, `Poincare.D10.HeatKernelEuclidean.{hasDerivAt_gaussianKernel,laplacian_gaussianKernel}`, `Poincare.D12.HeatSemigroup.{heatOperator,gaussianKernel_interval_bound,gaussianKernel_interval_bound_le}`.

Also for "no sorry": `collectAxioms` returns `sorryAx` if any. Check.

Additionally: verify that every name in the parent's 51-list exists and is in my enumerated set, and that my enumerated set is a superset.

I'll also want a machine-readable output. Lean's `run_cmd` can write files via `IO.FS.writeFile`. That's allowed in the elaborator (`run_cmd` supports IO). I'll emit a JSON-ish TSV to `/data3/.../review/evidence/semrev-axiom-audit.tsv`. Use `IO.FS.writeFile`.

Then for semantic classification, I'll dump `#check` types into a text file too? `#check` output goes to stdout; I can capture the log. Alternatively, in run_cmd, use `logInfo m!"{← Lean.Meta.ppExpr ...}"`. Simpler: I'll rely on `#check` transcript plus manual classification in the card.

Let me write the audit file with:
1. auto-enumeration + collectAxioms + fail-closed gate (throwError on any violation).
2. explicit snapshot list check.
3. print a table of declarations: name, kind (axiom/def/thm/opaque/induct/ctor/rec), safety, axioms.
4. write TSV evidence file.

I need to be careful with Lean API names. Let me write it and iterate on compile errors. To iterate fast, use `lake env lean` on the file (it will recompile the file only).

Also stage C (semantics probes): construct concrete inputs, instantiate consumers. I'll write `SemrevSemantics.lean`:
```lean
-- constructed input
def f3 : BUCn 3 := ⟨BoundedContinuousFunction.const _ 1, uniformContinuous_const⟩
-- hmm BUCf structure fields: val, uniformContinuous_val. Use `⟨BoundedContinuousFunction.const _ 1, (uniformContinuous_const)⟩`
```
Check: `UniformContinuous (fun x => (const _ 1 : BCFn 3) x)` — `uniformContinuous_const` should work. Then:
```lean
example : KernelClassicalHeatSolution 3 := heatConv_classicalHeatSolution 3 f3
-- extract fields
example (t : ℝ) (ht : t ∈ Set.Ioo 0 (heatConv_classicalHeatSolution 3 f3).T) :
  HasDerivAt ... := (heatConv_classicalHeatSolution 3 f3).isSolution t ht
```
And a consumer of `hasDerivAt_heatConv_BCF`: `(hasDerivAt_heatConv_BCF 3 (by norm_num) f3).continuousAt` gives `ContinuousAt (fun s => heatConv n s f3.val) t`, or apply `.hasDerivAt` etc.

Also non-vacuity of the uniform statement: instantiate δ from the theorem? The theorem returns ∃δ..., so I can extract it.

And check the "residual has no inhabitant": I can't prove `¬ SpatialLaplacianBridge n`, but I can show no instance/axiom provides it by auto-enumeration of axioms and by checking `SpatialLaplacianBridge` is a `def` with no accompanying theorem. Additionally, I can check `#print axioms SpatialLaplacianBridge` → `[]` (it's a def). Good.

A sharper check for "statement-only": search for any theorem whose type is `SpatialLaplacianBridge n` or `UniformMildToClassicalBridge n`. Auto-enumeration of all constants with `HeatTimeDeriv` in the name gives us the full list; I can then check none of them has a type that concludes these Props except `mildToClassicalBridge_of_uniform` (whose conclusion is mildToClassicalBridge, not Uniform). And `uniformMildToClassicalBridge_holds : UniformMildToClassicalBridge n` exists. So Uniform is proved; SpatialLaplacianBridge is not. Good.

Let me also verify the parent's negative control compiles with exit 1 using my environment.

Let me now check the build progress.
dsh: reasoning:
Still running. The background job only outputs at the end since I redirected to a file. I can check the log file's progress directly.
dsh: reasoning:
Build is at 8910/8915 — almost done (the 8915 count includes mathlib replay checks). L3 dir empty means L3 files still to compile. Wait — that's concerning: if it's at 8910/8915 and L3 oleans aren't built... maybe the build order is D12 first then L3 last (8911-8915 = 5 L3 modules + All). Let's wait a bit.

Meanwhile let me write the audit probe file. I'll be careful with Lean API.

Draft:

```lean
/-
SEMREV-L3 independent review — Stage B: fail-closed axiom audit + declaration census.
-/
import Poincare.L3.HeatTimeDeriv.All
import Lean.Util.CollectAxioms
import Lean.Elab.Command

open Lean Elab Command

namespace SemrevL3

/-- Approved cone. -/
def approvedAxioms : List Name := [``propext, ``Classical.choice, ``Quot.sound]

/-- Snapshot declarations consumed by the L3 development (from the parent audit list). -/
def snapshotDecls : List Name :=
  [ ``Poincare.D12.ParabolicLocal.mildToClassicalBridge,
    ``Poincare.D12.ParabolicLocal.heatConv,
    ``Poincare.D12.ParabolicLocal.heatConv_apply,
    ``Poincare.D12.ParabolicLocal.heatConv_tendsto_self_BUC,
    ``Poincare.D10.HeatKernelEuclidean.hasDerivAt_gaussianKernel,
    ``Poincare.D10.HeatKernelEuclidean.laplacian_gaussianKernel,
    ``Poincare.D12.HeatSemigroup.heatOperator,
    ``Poincare.D12.HeatSemigroup.gaussianKernel_interval_bound,
    ``Poincare.D12.HeatSemigroup.gaussianKernel_interval_bound_le ]

def kindOf : ConstantInfo → String
  | .axiomInfo _ => "axiom"
  | .defnInfo v => if v.safety == .unsafe then "unsafe-def" else "def"
  | .thmInfo _ => "theorem"
  | .opaqueInfo _ => "opaque"
  | .quotInfo _ => "quot"
  | .inductInfo _ => "induct"
  | .ctorInfo _ => "ctor"
  | .recInfo _ => "rec"

run_cmd do
  let env ← getEnv
  let mut rows : Array (Name × String) := #[]
  let mut violations : Array String := #[]
  let mut nL3 := 0
  for (n, ci) in env.constants.toList do
    if "HeatTimeDeriv".isSubstr n.toString then
      nL3 := nL3 + 1
      let axs ← Lean.collectAxioms n
      ...
```

Hmm: `Lean.collectAxioms` signature: `def collectAxioms (constName : Name) : CommandElabM (Array Name)`? In the parent's Audit.lean they call `← Lean.collectAxioms d` inside `run_cmd do` — so it's a `CommandElabM` computation? Actually `run_cmd` runs in `CommandElabM`. `Lean.collectAxioms : Name → CommandElabM (Array Name)`. Yes.

`"HeatTimeDeriv".isSubstr n.toString` — `String.isSubstr` exists? There's `String.isSubstr (sub : String) : String → Bool`? In Lean 4 core, `String.isSubstr` might not exist; there is `String.contains` which takes Char. There's `List.isSubstr` for List Char. Let me use `(n.toString.splitOn "HeatTimeDeriv").length > 1`. Simple and safe.

For writing the file: `IO.FS.writeFile path content`— in `CommandElabM` we can `liftIO`. `run_cmd` elaborates in `CommandElabM`, and `liftIO` works since `CommandElabM` is a monad with `MonadLift IO`? Yes, `liftIO` is available (CommandElabM has `MonadLift IO`? I believe `CommandElabM` implements `MonadLift IO` via `liftIO`... Actually `CommandElabM` is `ReaderT ... (EIO ...)`, and there's an instance `MonadLift IO CommandElabM`? Hmm. In Mathlib, `run_cmd` with `IO.FS.writeFile` is commonly used, e.g. `run_cmd IO.FS.writeFile "x" "y"`? I've seen `liftIO <| IO.FS.writeFile ...` in mathlib. Let me just use `IO.FS.writeFile` with `liftIO` and see.

Alternatively, print the table with `logInfo` and capture stdout to a file. That avoids IO API risk. I'll do both: log the table, and attempt a file write in a `try`.

To keep the audit simple and robust:
1. `run_cmd` loop: for each L3-ish constant, compute axioms, check against approved; log a line `SEMREV|name|kind|axioms` via `logInfo`; count violations; throw at end if any.
2. `run_cmd` for snapshot decls: same.
3. Also check `#print axioms` of key declarations in the transcript.

I'll capture stdout via shell redirection to `evidence/semrev-axiom-audit.log`.

Let me also handle: constants in `env.constants` include private/internal ones; `collectAxioms` may fail for some (e.g., recursors)? `collectAxioms` should handle any constant. If a name is not a declaration with value (e.g., `quot`), collectAxioms returns []. Fine.

One more: `ConstantInfo.defnInfo v` — does `DefinitionVal` have a `safety` field? In Lean 4 core:
```
structure DefinitionVal extends ConstantVal where
  hints : ReducibilityHints
  safety : DefinitionSafety
  value : Expr
```
Yes. And `DefinitionSafety` has `.safe`, `.unsafe`. Good. `AxiomVal` extends ConstantVal with `isUnsafe : Bool`. In newer Lean, `AxiomVal` has `isUnsafe`. I'll just match `.axiomInfo _ => "AXIOM"`.

Let me write the file now.
dsh: reasoning:
Basic (5.3s) and ClassicalBridge (4.5s) built fresh. Waiting for UniformBridge, BanachDeriv, All. Should be seconds more. Let me wait for the job.
dsh: reasoning:
Fresh rebuild of the consumed cone (82 modules incl. D10/D11/D12) succeeded in 1m25s, exit 0, with 8915 jobs. That includes fresh compilation of D10/D11/D12 and L3. Note: it says "Built" not "Replayed" for the L3 modules — genuinely recompiled.

Now, note the log has warnings (e.g., "Try this: [apply] abel_nf") from D12 sources — those are style suggestions, not errors. Good.

Now run Stage A probe and the audit. Let me first run Stage A (needs Pi.normedAddCommGroup print — may fail if name doesn't exist). Let me run it.
dsh: reasoning:
Stage A exited 1 — likely `#print Pi.normedAddCommGroup` failed (unknown declaration) and/or `#synth` failed. Let me see the head of the log for the error.
dsh: reasoning:
This is a MAJOR finding! `NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)` **fails to synthesize**! `Pi.normedAddCommGroup` requires `[Fintype ι]`, and `EuclideanSpace ℝ (Fin 3)` is not a Fintype instance (only `Fin 3` is finite, and `EuclideanSpace ℝ (Fin 3)` is defined as `Fin 3 → ℝ`, a function type over `Fin 3`, which IS finite — but instance search can't see through the `EuclideanSpace` reducible abbreviation? Hmm, `EuclideanSpace ℝ (Fin 3)` is reducible to `Fin 3 → ℝ`, and `Fintype (Fin 3 → ℝ)` should exist via `Pi.fintype`. So why does `#synth NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)` fail? Because the index type is `EuclideanSpace ℝ (Fin 3)` = `Fin 3 → ℝ` (a function space over ℝ!), and `Fintype (Fin 3 → ℝ)` does NOT exist (ℝ is infinite). Right: the Pi type here is `(Fin 3 → ℝ) → ℝ`, indexed by the uncountable type `EuclideanSpace ℝ (Fin 3)`. So no `Fintype` instance → no `Pi.normedAddCommGroup` → no `NormedAddCommGroup` for the function space!

But wait — the L3 code compiles and uses `HasDerivAt` on exactly this type: `HasDerivAt (fun s : ℝ => fun x : EuclideanSpace ℝ (Fin n) => ...) ... t`. `HasDerivAt` requires `[NormedAddCommGroup F]`. How did it typecheck?

Ah — I see. In `mildToClassicalBridge_pointwise`, they `rw [hasDerivAt_pi]`, which turns the Pi-type derivative into pointwise derivatives. For the `rw` to apply, the Pi type must have the instances. Hmm, but `hasDerivAt_pi` is stated in a context `[∀ i, NormedAddCommGroup (E' i)]` — wait, that's only the *codomain* instances; but the theorem statement `HasDerivAt φ φ' x` for `φ : 𝕜 → ∀ i, E' i` also requires `NormedAddCommGroup (∀ i, E' i)` and `NormedSpace 𝕜 (∀ i, E' i)`. Since `hasDerivAt_pi`'s section variables don't include those, they must be *instance arguments* of the theorem that are implicit and synthesized at use site. Let me look at the full signature: `#check @hasDerivAt_pi` would show the instance arguments. In the file, the variables are:
```
variable {ι : Type*} {E' : ι → Type*} [∀ i, NormedAddCommGroup (E' i)]
  [∀ i, NormedSpace 𝕜 (E' i)] {φ : 𝕜 → ∀ i, E' i} {φ' : ∀ i, E' i}
```
Hmm, for `HasDerivAt φ φ' x` to be well-formed, Lean must synthesize `NormedAddCommGroup (∀ i, E' i)` *at declaration time* from the local instances `[∀ i, NormedAddCommGroup (E' i)]` — but `Pi.normedAddCommGroup` needs `[Fintype ι]`. Unless there's a *different* instance path: maybe `Pi.instNormedAddCommGroup` doesn't exist, but Lean can use... hmm, but Mathlib compiles this file. So at declaration time Lean must synthesize it. Unless `HasDerivAt` is defined with explicit instance args that can be unresolved?? No, Lean would error.

Wait — maybe there IS an instance `NormedAddCommGroup (∀ i, E i)` in Mathlib for *arbitrary* index type via `UniformSpace.Pi`? Let me check `#synth` for `ι` arbitrary inside a section with `[∀ i, NormedAddCommGroup (E' i)]`. Hmm.

But my probe's `#synth NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)` failed. So how does `mildToClassicalBridge_holds` compile?? Let me look: the theorem statement mentions `HasDerivAt (fun s : ℝ => fun x : EuclideanSpace ℝ (Fin n) => (heatConv n s f.val) x) (...) t`. For this to elaborate, Lean must find `NormedAddCommGroup (EuclideanSpace ℝ (Fin n) → ℝ)`... 

Unless `HasDerivAt` doesn't require it at elaboration because it's an implicit instance argument that stays as a metavariable? No — instance implicit arguments are synthesized at elaboration; if synthesis fails, error.

Hmm, but the L3 files compiled successfully! And `#check @mildToClassicalBridge_holds` printed its type with the HasDerivAt. So Lean elaborated it somehow. How?

Possibility: `EuclideanSpace ℝ (Fin n) → ℝ` does get a `NormedAddCommGroup` instance from a *different* source: maybe `Pi.normedAddCommGroup` for infinite index doesn't exist, but there's `BoundedContinuousFunction`-like... no. Or maybe instance search finds `Pi.instNormedAddCommGroup` through `SeminormedAddCommGroup`... 

Wait, actually maybe my `#synth` failed because instance synthesis for `NormedAddCommGroup` requires `SeminormedAddCommGroup` which requires `PseudoMetricSpace`... and the error says "failed to synthesize SeminormedAddCommGroup". So there is NO instance for the full function space. Then how does `HasDerivAt` on that space elaborate?! 

Let me check `#check @mildToClassicalBridge` output from the log more carefully. It shows:
```
HasDerivAt (fun s x => (heatConv n s f.val) x) (fun x => ∫ ...) t
```
No instance arguments shown (they're implicit). The `#check` of `mildToClassicalBridge` succeeded — meaning the definition elaborated when D12 was compiled.

Hmm! Maybe `HasDerivAt` in this version of Mathlib is defined for `[NormedAddCommGroup F]`... but wait, maybe Mathlib's `HasDerivAt` on Pi types works via a *scoped instance* or a local instance that D12 has opened? Instance search is not affected by `open`. 

OR: maybe there IS an instance for function spaces into normed groups for *arbitrary* index: `Pi.instNormedAddCommGroup` defined in Mathlib as:
```
noncomputable instance Pi.instNormedAddCommGroup ...
```
Hmm, but my `#print Pi.normedAddCommGroup` output shows it requires `[Fintype ι]`. And my `#synth` failed. So the instance doesn't exist for infinite index.

So how did the D12/L3 files compile? Let me look at what `hasDerivAt_pi` needs: the LHS `HasDerivAt φ φ' x` where `φ : 𝕜 → ∀ i, E' i`. Maybe `HasDerivAt` has its own Pi instance?? Let's check `#check @hasDerivAt_pi` with all implicit args in the probe. Or maybe the trick is: `HasDerivAt` is `HasFDerivAt` specialized, and for the *statement* to typecheck, Lean needs `NormedAddCommGroup (∀ i, E' i)`. If the theorem `hasDerivAt_pi` is declared in Mathlib, then Mathlib must have such an instance in scope at declaration. So there must be one!

Let me search the Mathlib source for where the instance is declared. The `#print Pi.normedAddCommGroup` showed `[Fintype ι]`. Maybe there are two instances: `Pi.normedAddCommGroup` (Fintype, sup norm) and another one for arbitrary index... The `#synth` failed though, which is decisive: no instance is found for `(EuclideanSpace ℝ (Fin 3)) → ℝ`.

Wait... unless the failure is due to *instance reducibility/priority* and the type isn't syntactically a Pi type? `EuclideanSpace ℝ (Fin 3) → ℝ` is literally `((Fin 3 → ℝ) → ℝ)` — a Pi type with index `Fin 3 → ℝ`. `Fintype (Fin 3 → ℝ)` — is there a Fintype instance for function types into ℝ? No (ℝ infinite, no Fintype). So `Pi.normedAddCommGroup` can't apply.

Therefore `HasDerivAt (fun s : ℝ => fun x : EuclideanSpace ℝ (Fin n) => ...) ... t` should NOT elaborate... unless Lean finds another instance. But the D12 source defines `mildToClassicalBridge` with that exact statement and it compiled. Let me test directly: write a minimal file:

```lean
import Mathlib
example {n : ℕ} (g : ℝ → EuclideanSpace ℝ (Fin n) → ℝ) (g' : EuclideanSpace ℝ (Fin n) → ℝ) (t : ℝ) :
    HasDerivAt g g' t := by sorry
```
This would fail to elaborate if no instance. But D12 compiled... Let me actually check by running `#check` on the *definition* `Poincare.D12.ParabolicLocal.mildToClassicalBridge`: my Stage A log shows `#print Poincare.D12.ParabolicLocal.mildToClassicalBridge` succeeded and printed the full statement including `HasDerivAt (fun s x => ...) ... t`. So the environment contains this declaration — it was elaborated during the fresh rebuild of D12 (which succeeded).

So there must be an instance. Why did my `#synth` fail? OH WAIT. I see it now! Look at my `#synth` line: `#synth NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)`. Hmm. And the error says failed. But the D12 statement uses `HasDerivAt` on `ℝ → (EuclideanSpace ℝ (Fin n) → ℝ)`. `HasDerivAt f f' x` is `HasFDerivAt f (ContinuousLinearMap.smulRight 1 f') x`... For this, need `NormedAddCommGroup F` where F is the codomain of f. Same type.

Unless... `HasDerivAt` in this Mathlib version doesn't require NormedAddCommGroup but only `SeminormedAddCommGroup`? Still needs the instance.

Hmm, is it possible that instance synthesis *inside the D12 module* used a local instance declared somewhere in the package (e.g., a scoped instance in `Poincare.D12.ParabolicLocal`)? A local `instance` in the namespace would be globally active once imported. And my probe imports it! So it would be found too.

Wait, maybe my `#synth` failure is because `#synth` uses a *different* transparency for instance search than elaboration of `HasDerivAt`'s implicit args... unlikely.

Let me actually test: in Stage A, `#check @hasDerivAt_pi` and `#print hasDerivAt_pi` will reveal the instance arguments. And `example : HasDerivAt (fun s : ℝ => fun x : EuclideanSpace ℝ (Fin 3) => s) (fun _ => 0) 0 := by ...` to see if it elaborates.

Hmm wait, maybe I misread the error: the log's first lines are:
```
StageA.lean:19:0: error: failed to synthesize NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)
StageA.lean:20:7: error: failed to synthesize SeminormedAddCommGroup ...
```
Line 19 is `#synth NormedAddCommGroup ...`, line 20 `#synth NormedSpace ...`. So `#synth` at line 19 failed but the `#check` of mildToClassicalBridge (line 28+) succeeded. 

AH WAIT. I bet the difference is `NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)` vs `NormedAddCommGroup (EuclideanSpace ℝ (Fin n) → ℝ)` — no, same.

Hmm, but hold on: `HasDerivAt` might be elaborated with a *different* instance because of the `Pi` instance for `∀ i, E i` where `[Fintype ι]`... no.

Let me think again: is `EuclideanSpace ℝ (Fin 3)` reducible to `Fin 3 → ℝ`? Yes, `abbrev EuclideanSpace (𝕜 : Type*) (ι : Type*) := ι → 𝕜`. So `EuclideanSpace ℝ (Fin 3) → ℝ` = `(Fin 3 → ℝ) → ℝ`. Fintype of `Fin 3 → ℝ`? There's `Fintype (α → β)` only if `β` is finite (`Fintype` of function type requires `Fintype β` and `DecidableEq α`... and `Fintype α`). ℝ has no Fintype. So indeed no instance.

So the only way D12's statement elaborates is if `HasDerivAt` doesn't require the codomain norm instance at all... Let me check the definition of `HasDerivAt`:
```
def HasDerivAt (f : 𝕜 → F) (f' : F) (x : 𝕜) : Prop := HasDerivAtFilter f f' x (𝓝 x)
```
and `HasDerivAtFilter (f : 𝕜 → F) (f' : F) (x : 𝕜) (L : Filter 𝕜) : Prop := HasFDerivAtFilter f (ContinuousLinearMap.smulRight (1 : 𝕜 →L[𝕜] 𝕜) f') x L`
Requires `[NormedAddCommGroup F] [NormedSpace 𝕜 F]` as variables (typeclass args). So elaboration requires the instance.

So there's a contradiction. Unless... the `#check` succeeded at line 28 because that's just printing the type of an already-elaborated constant — but no, `#check` has to *elaborate the type* of the constant to print it! The constant's type is stored in the environment; `#check` needs to instantiate its universe params and print — it doesn't need to re-synthesize instance arguments; the type is already an expression in the environment with the instance arguments *instantiated as expressions* (which may be `instNormedAddCommGroup` metavariables? no, they must be concrete).

Hmm, so what instance expression is stored in the type of `mildToClassicalBridge`? Let me inspect with `set_option pp.all true` and `#print` — that will show the instance arguments explicitly! That's the key diagnostic. `pp.all` shows implicit arguments including instance args. Let me do that.

Also, `#check @hasDerivAt_pi` with pp.all.

Wait — maybe the answer is that D12 was compiled with a *different* mathlib or with an extra instance available in the D12 module (declared locally, e.g. `attribute [local instance]` — local instances don't persist to importers, but the *type* stored in the constant would still reference the local instance's name, which would be inaccessible...). Actually, if a local instance is used in a declaration's type, the declaration would depend on a private/local name — Lean would complain or the name would be inaccessible to importers ("declaration uses 'sorry'"-like? no, it would be a private name that gets exported as `_private...`). Then `#check` from my probe would still print it since the name exists.

Hmm, hold on. Let me reconsider: maybe `HasDerivAt` for the Pi type uses the instance `Pi.normedAddCommGroup` instantiated with `Fintype (EuclideanSpace ℝ (Fin n))`... which would require `Fintype (Fin n → ℝ)`. Is there possibly a `Fintype` instance for `EuclideanSpace ℝ (Fin n)`?! That would be a bug but maybe D11/D12 declared one locally! If a *local instance* `Fintype (EuclideanSpace ℝ (Fin n))` were declared, then `NormedAddCommGroup (EuclideanSpace ℝ (Fin n) → ℝ)` would synthesize via `Pi.normedAddCommGroup` + that bogus Fintype instance... and the norm would be the *sup over a Fintype enumeration* of the function type — which for a false Fintype instance would be nonsense. But you can't fake a Fintype of an infinite type without an axiom... unless the instance is something like `Fintype.ofFinite` requiring `Finite` (which is a Prop, could be obtained from `Classical.choice` of a false statement? no, `Finite ℝ` is not provable).

OK, let me just look at the pp.all output. That's decisive.

Actually — WAIT. I bet I know what's happening: `EuclideanSpace ℝ (Fin 3) → ℝ` — maybe Lean parses this with `→` as *non-dependent* function type, and there's an instance `Pi.instNormedAddCommGroup`? The error message says "failed to synthesize SeminormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)". Hmm.

But then how does D12 compile? Let me check whether D12's `mildToClassicalBridge` statement really is on the raw function space: the `#print` output showed `HasDerivAt (fun s x => ...) (fun x => ...) t`. Yes.

Unless... `HasDerivAt` here is not Mathlib's `HasDerivAt` but a *different* `HasDerivAt` opened from somewhere?! No.

Hmm, one possibility: There's a **local instance in the file** (e.g., in `Poincare.D12.ParabolicLocal` BUC.lean or elsewhere) that provides `NormedAddCommGroup (EuclideanSpace ℝ (Fin n) → ℝ)` via `Pi.normedAddCommGroup` with `Fintype (EuclideanSpace ℝ (Fin n))` derived from `Finite` via `Fintype.ofFinite`... requires `[Finite ι]`. `Finite (EuclideanSpace ℝ (Fin n))` is not provable.

Alternatively, maybe there's an instance for *arbitrary* index in Mathlib via `UniformOnFun`/`Pi.instSeminormedAddCommGroup`... but `#synth` failed, so no.

Wait wait wait. Let me re-read the error message from the log:

```
../probe/StageA.lean:19:0: error: failed to synthesize
  NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)
Hint: Additional diagnostic information may be available using the `set_option diagnostics true` command.
../probe/StageA.lean:20:7: error(lean.synthInstanceFailed): failed to synthesize instance of type class
  SeminormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)
```

Hmm, note line 20 is `#synth NormedSpace ℝ (...)`. The error for line 20 says failed `SeminormedAddCommGroup` — that's the *parent* class needed for NormedSpace.

So both fail. And yet, the subsequent `#check @mildToClassicalBridge_holds` etc. succeeded. Also `#print Poincare.D12.ParabolicLocal.mildToClassicalBridge` succeeded.

So the stored type of `mildToClassicalBridge` contains `HasDerivAt` with instance arguments that are *not* synthesizable in my probe's environment. How? Because instance arguments in a *stored type* are explicit expressions; `#print`/`#check` don't re-synthesize. So the D12 declaration must have been elaborated with some instance that is not available now. Which means: D12 (and L3) were compiled with a **local instance** that was in scope at compile time but is not exported!

This is a classic hazard: if a file declares `attribute [local instance] foo` and uses it in a declaration's type, the resulting constant's type refers to `foo` by name (which is fine, still referenced) — so it WOULD be available to importers (name resolution by environment lookup, not instance search). So my `#synth` failing doesn't prevent the stored type from printing.

But then how did L3's `mildToClassicalBridge_holds` typecheck — it must produce a term of the D12 type. If the instance used in the type is some non-synthesizable local instance, L3's proof would need that same instance in scope. It compiled, so the instance must be globally available (imported).

Hmm, then why does `#synth` fail in my probe?! Unless the instance is a **non-instance theorem** used explicitly... 

OK let me stop speculating and inspect. I'll write a focused diagnostic:

```lean
import Poincare.L3.HeatTimeDeriv.All
set_option pp.all true in
#print Poincare.D12.ParabolicLocal.mildToClassicalBridge
set_option pp.all true in
#check @hasDerivAt_pi
```
and
```lean
example : HasDerivAt (fun s : ℝ => fun x : EuclideanSpace ℝ (Fin 3) => s) (fun _ => 0) 0 := by ...
```
Hmm, `fun x => s` constant in x. Let me see if the `example` elaborates. If it does, then instance search *does* find something in the context of a term elaboration... but `#synth` failing contradicts.

Actually! Maybe the difference: `#synth` uses the **default** transparency/reducibility and doesn't unfold `EuclideanSpace` (an abbrev, so reducible), whereas... no, same.

Hmm, actually, wait. Let me recheck the actual error lines. My StageA.lean line 19 is `#synth NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)`. Line 20 is `#synth NormedSpace ℝ (...)`. Line 23: `#print Pi.normedAddCommGroup` — succeeded. Line 28-29: the `example : (inferInstance : NormedAddCommGroup ...) = inferInstance := rfl` — failed (lines 28:11 and 29:4 errors). Lines 32+ `#check`s succeeded.

So indeed there is no synthesizable instance, yet D12/L3 statements typecheck with `HasDerivAt` on that type. The resolution: **`HasDerivAt` must not require the instance at the point of use because the instance is an implicit argument that got synthesized from a different path**. 

OH! I think I've got it: `HasDerivAt f f' x` where `f : 𝕜 → F` — for `F = EuclideanSpace ℝ (Fin n) → ℝ`, maybe Lean's elaboration of `fun s : ℝ => fun x : EuclideanSpace ℝ (Fin n) => ...` infers the type via *expected type propagation*, and the `HasDerivAt` application's `F` metavariable is resolved to the Pi type; then instance synthesis for `NormedAddCommGroup F` happens... and fails unless there's an instance.

Unless `HasDerivAt` is an *abbrev/def* whose typeclass args are... Let me check `#check @HasDerivAt` with pp.all.

Let me just run the diagnostic. Also check `#synth Fintype (EuclideanSpace ℝ (Fin 3))`.

Actually hold on, maybe I should check the ACTUAL norm instance for the type in the D12 statement by using `#print` with pp.all: the instance argument will appear as an expression like `@Pi.normedAddCommGroup (EuclideanSpace ℝ (Fin n)) (fun _ => ℝ) instFintype instNormed`. That would reveal whether a `Fintype (EuclideanSpace ℝ (Fin n))` instance is being used! If yes, that's a *fake* Fintype instance — meaning the "sup norm" is over a finite enumeration of an infinite type... which would be mathematically disastrous (though if it's a genuine `Fintype` instance from `Classical.choice` of `Finite`... impossible).

But my `#synth NormedAddCommGroup` failed — so the instance used in the stored type cannot be found by synthesis now. This strongly suggests the D12/L3 declarations were compiled with a **`local instance`** that is not exported. But then L3's own modules compiled with their own local instance? They don't declare one (I read all files — no `instance`/`attribute [local instance]` in L3 sources). Hmm, but wait: maybe the local instance is in `BUC.lean` or `GaussianConv.lean` (D12) — but if local, it wouldn't be available in L3's files (different module) — unless it's `scoped` or a plain global `instance` in D12 that IS exported... but then `#synth` would find it after importing L3 (which imports D12).

UNLESS the instance is in a namespace and marked `@[instance]` but *not* exported? No such thing.

Hmm, one more possibility: the instance search failure is because `#synth` requires the instance at *default priority with default transparency*, but the relevant instance is behind `attribute [local instance]` inside the module that's imported... local instances don't persist across modules. So no.

Let me consider: maybe L3's `mildToClassicalBridge_holds` does NOT actually apply `hasDerivAt_pi` on the raw Pi type; look at the proof:
```lean
theorem mildToClassicalBridge_holds (n : ℕ) : mildToClassicalBridge n := by
  intro t ht f
  have h := mildToClassicalBridge_pointwise n ht f
  simpa only [timeDerivIntegral, timeCoeff] using h
```
and
```lean
theorem mildToClassicalBridge_pointwise (n : ℕ) :
    ∀ {t : ℝ}, 0 < t → ∀ f : BUCn n,
      HasDerivAt (fun s : ℝ => fun x : EuclideanSpace ℝ (Fin n) => (heatConv n s f.val) x)
        (fun x : EuclideanSpace ℝ (Fin n) => timeDerivIntegral n t f.val x) t := by
  intro t ht f
  rw [hasDerivAt_pi]
  ...
```
For `rw [hasDerivAt_pi]` to work, the goal must be an instance of the LHS pattern, which requires unifying `HasDerivAt φ φ' x` with the goal — the implicit instance arguments of both sides unify up to *definitional equality of instance expressions*, which may include metavariables that get assigned to the goal's instance args. `rw` doesn't need to synthesize instances (they're already in the goal's type expression from elaboration).

But the *goal itself*, when elaborated, needed the instance. So how did the goal elaborate? This brings us back.

Hmm, is it possible that `HasDerivAt`'s typeclass assumptions are **auto-bound implicit variables that remain as free variables in the declaration's type**? No — for a theorem, the type must be a closed expression; typeclass arguments that can't be synthesized cause an error "failed to synthesize" during elaboration.

OK, decisive test: compile a minimal example in the `Poincare.L3.HeatTimeDeriv` namespace with the same statement. If it compiles, instance search somehow succeeds in that context; if it fails, then something is off with my probe (e.g., missing imports?) — but my probe imports the same module.

Hmm wait, maybe my `#synth` failed because of **`open scoped`** or `noncomputable section`? No, instance search is unaffected by opens... except *scoped instances*! If the instance is declared with `attribute [scoped instance]` in a namespace that's opened via `open scoped X` in the D12/L3 files, then it's only active when that scope is open. My probe *does* `open scoped Topology InnerProductSpace Laplacian RealInnerProductSpace` — the same opens as L3 files, but NOT necessarily the scope where the instance lives! E.g., if D12 has `attribute [scoped instance] ...` inside namespace `Poincare.D12...`, one would need `open scoped Poincare.D12...`. 

That's plausible: a scoped instance for `NormedAddCommGroup (EuclideanSpace ℝ (Fin n) → ℝ)`?? Hmm, that seems like an odd thing for D12 to add. But maybe they added an instance for the *Pi function space* with the **sup norm over a Fintype**? Wait, but `EuclideanSpace ℝ (Fin n)` isn't finite.

Hold on, hold on. Let me reconsider `Pi.normedAddCommGroup`: it needs `[Fintype ι]`. But maybe the D12 files define an instance `NormedAddCommGroup (EuclideanSpace ℝ (Fin n) → ℝ)` using `Pi.normedAddCommGroup` with a *fake* `Fintype` instance obtained via `Fintype.ofFinite` + `Finite` from `Classical.choice`? You can't get `Finite (Fin n → ℝ)`.

Alternatively, maybe the instance is the sup norm restricted to *bounded* functions? That's `BoundedContinuousFunction`, different type.

Hmm, actually maybe I'm wrong that the index type is `EuclideanSpace ℝ (Fin n)`. Let me look at the pp.all print — it will show exactly.

Let me run the diagnostic now. This is the crux of the review: if the D12 obligation and its discharge live in a *nonstandard normed structure* (some scoped/local instance that is not available to consumers), that's a MAJOR semantic finding: the claimed "pointwise" interpretation might be wrong and the Banach-space claim muddled. Actually, L3's `hasDerivAt_heatConv_BCF` is on `BCFn n = (EuclideanSpace ℝ (Fin n)) →ᵇ ℝ`, which properly has the sup norm. The `mildToClassicalBridge` (D12) is on the raw function space.

Let me run:
```lean
import Poincare.L3.HeatTimeDeriv.All
set_option pp.all true in
#print Poincare.D12.ParabolicLocal.mildToClassicalBridge
```
and a minimal example elaborating `HasDerivAt` on that type.

Let me write StageA2.lean.
dsh: reasoning:
Wait, my last example uses `sorry` which will fail compilation and it's also inappropriate. Let me remove that last block and instead use a lambda with `HasDerivAt` statement only (no proof). Actually to inspect the stored instance in a freshly elaborated HasDerivAt type, I can use `#check (fun (g : ℝ → EuclideanSpace ℝ (Fin 4) → ℝ) (g' : EuclideanSpace ℝ (Fin 4) → ℝ) => (HasDerivAt g g' 0 : Prop))` — this requires elaborating `HasDerivAt g g' 0` which requires instance synthesis; if it fails, that itself is informative (it would mean the D12 statement can't be re-elaborated!). Let me use:
```
set_option pp.all true in
#check (fun (g : ℝ → EuclideanSpace ℝ (Fin 4) → ℝ) (g' : EuclideanSpace ℝ (Fin 4) → ℝ) =>
  (HasDerivAt g g' 0 : Prop))
```
No sorry. Let me rewrite the file.

Also the second `example` uses `mildToClassicalBridge.1 ht f` — `mildToClassicalBridge` is a def returning a Prop; `mildToClassicalBridge n` unfolds to `∀ {t}, 0<t → ...`. Since it's a def (not theorem), `.1`? For a `∀` over an implicit arg, `.1` doesn't apply. Better:
```
example ... : ... := by
  have h := Poincare.D12.ParabolicLocal.mildToClassicalBridge (n := n)
  exact h ht f
```
Hmm, `mildToClassicalBridge n` has binders `∀ {t} (ht) (f)`, so `h ht f` — but `ht` is explicit first arg? The def is `∀ {t : ℝ}, 0 < t → ∀ f, ...`, so `h (t := t) ht f` or `h ht f`. Let me write `exact (Poincare.D12.ParabolicLocal.mildToClassicalBridge n) ht f`. But the instance synthesis in the *example's statement* is what matters — if it fails, we get an error and learn that restating requires the scoped instance.

Actually the cleanest decisive probe: re-elaborate the statement from scratch and see whether it succeeds. If it fails with the same synth error, then the D12/L3 declarations were compiled using an instance that is NOT available via plain import — which would be a serious finding about the artifact's consumability (downstream users cannot even state the theorem without the special instance). But hold on — the L3 modules themselves are downstream consumers of the D12 type, and they compiled... They're in the same *package* but different modules, so if a `local instance` in D12 were needed, L3 couldn't compile. Unless the instance is a *global* instance that is somehow not found by `#synth` due to... hmm.

Wait, actually there's one more possibility I keep dancing around: **`Classical.choice`-based `NormedAddCommGroup` on function spaces does exist in Mathlib for arbitrary index via `Pi.instNormedAddCommGroup`?** My `#synth` failed though — but maybe `#synth` failure is due to a *missing* `SeminormedAddCommGroup` because the Pi instance requires `[Fintype ι]`, full stop.

So how? Let me look at the actual import list: maybe `Poincare.L3.HeatTimeDeriv.All` -> ... -> D12 files declare `instance : NormedAddCommGroup (EuclideanSpace ℝ (Fin n) → ℝ)` somewhere. Let me grep for "NormedAddCommGroup" and "instance" in D10-D13 sources! That's a direct check.

Let me grep the whole Poincare tree for `NormedAddCommGroup (EuclideanSpace`.
dsh: reasoning:
Excellent! This is the decisive evidence. Look at the stored type of `mildToClassicalBridge`:

```
@HasDerivAt Real ... ((x : EuclideanSpace ℝ (Fin n)) → ℝ)
  (@Pi.addCommGroup ...)                       -- AddCommGroup ✓
  (@Pi.Function.module ...)                    -- Module ✓
  (@Pi.topologicalSpace ...)                   -- TopologicalSpace = product topology ✓
  (Poincare.D12.ParabolicLocal.mildToClassicalBridge._proof_1 n)   -- ← the "normed group" argument??
  ...
```

So the `NormedAddCommGroup` instance argument is a **proof** `mildToClassicalBridge._proof_1 n`! Wait, the order of `HasDerivAt`'s arguments: `@HasDerivAt 𝕜 F [NontriviallyNormedField 𝕜] [NormedAddCommGroup F] [NormedSpace 𝕜 F] f f' x`? Hmm, the printed arg list:
1. `Real`
2. `NontriviallyNormedField Real`
3. `(x : EuclideanSpace ℝ (Fin n)) → Real`   ← F
4. `@Pi.addCommGroup ...`                    ← this looks like the `AddCommGroup` part of a NormedAddCommGroup *structure*? No...
5. `@Pi.Function.module ...`                 ← Module
6. `@Pi.topologicalSpace ...`                ← TopologicalSpace
7. `mildToClassicalBridge._proof_1 n`        ← ???
8. the function
...

Hmm, the instance arguments shown are `AddCommGroup`, `Module`, `TopologicalSpace`, then a mysterious `_proof_1`, then f. That doesn't match `[NormedAddCommGroup F] [NormedSpace 𝕜 F]` which would be single instance arguments whose *structures* would be printed as their own names (e.g., `instNormedAddCommGroup`), not exploded into fields. Unless `pp.all` unfolds instance arguments *structurally*? No, pp.all prints constant names for instances.

Wait, maybe those are the fields of `NormedAddCommGroup` because the instance was constructed **anonymously by a `where`/structure literal**! I.e., someone wrote an instance as a structure literal inline, producing a term whose head is `NormedAddCommGroup.mk` (an anonymous constructor), and `pp.all` prints it in *projection/application* form. The `_proof_1` is an auxiliary declaration generated by Lean for a proof field in a structure literal inside a definition — i.e., the instance was created with a `where` clause containing a proof that got lifted to `mildToClassicalBridge._proof_1`.

So D12 defines a **local/anonymous NormedAddCommGroup instance** on the raw function space?! Let me see the full pp.all output to understand. Let me grep for `_proof_1` and look at the D12 Obligations.lean source near the top... Actually the grep for "instance.*Normed.*EuclideanSpace" found nothing, but the instance may be written as:
```
instance : NormedAddCommGroup (EuclideanSpace ℝ (Fin n) → ℝ) where
  ...
```
My grep pattern was too narrow. Let me look at the full head of Obligations.lean and search for `NormedAddCommGroup` and `TopologicalSpace`/`PseudoMetricSpace` in D12 files.

Actually, hold on. Look again at the argument list: after `F` comes `@Pi.addCommGroup`, then `@Pi.Function.module`, then `@Pi.topologicalSpace`, then `_proof_1`. This is exactly the signature of... hmm, `HasDerivAt` in Lean 4 Mathlib:
```
def HasDerivAt (f : 𝕜 → F) (f' : F) (x : 𝕜) : Prop :=
  HasDerivAtFilter f f' x (𝓝 x)
```
with variables `[NontriviallyNormedField 𝕜] [NormedAddCommGroup F] [NormedSpace 𝕜 F]`. The instance args would be `[NormedAddCommGroup F]` then `[NormedSpace 𝕜 F]`. In pp.all, an instance argument that is a *local hypothesis* would print as a free variable `_inst_1`? No, in a theorem's type, instance args are explicit applications like `@Pi.normedAddCommGroup ...` or an anonymous structure.

The printed sequence `AddCommGroup`, `Module`, `TopologicalSpace`, `_proof_1` looks like the *fields* of a `NormedAddCommGroup` structure literal: `NormedAddCommGroup` extends `SeminormedAddCommGroup` extends `PseudoMetricSpace`, `AddCommGroup`, `Module`... Actually `NormedAddCommGroup` structure:
```
structure NormedAddCommGroup (α) extends SeminormedAddCommGroup α, AddCommGroup α
structure SeminormedAddCommGroup (α) extends PseudoMetricSpace α, AddCommGroup α, Module ℝ α?
```
Hmm, `SeminormedAddCommGroup` extends `PseudoMetricSpace` and `AddCommGroup`; `NormedAddCommGroup` extends `SeminormedAddCommGroup` with `eq_of_dist_eq_zero`. And `NormedSpace` extends `Module` + `SeminormedSpace`...

So the printed args: `Pi.addCommGroup`, `Pi.Function.module`, `Pi.topologicalSpace`, `_proof_1` — that's not the full structure.

Let me just look at more of the pp.all output (the lines after the function) and find where `NormedAddCommGroup` is constructed. Also let me grep the D12 sources for "NormedAddCommGroup" and "PseudoMetricSpace" / "dist".

I should also address: how can `HasDerivAt`'s `NormedSpace` argument be synthesized if `#synth SeminormedAddCommGroup` fails? If the instance is an **anonymous local instance** created by `letI`/`haveI` inside a *theorem statement*? You can't put `letI` in a def's type... but you CAN write:
```
def foo : ∀ ..., @HasDerivAt ℝ F (by infer_instance) ... := ...
```
Hmm.

Actually, wait! There's a much simpler explanation: the instance is supplied *explicitly* as a term! For instance, the D12 author might have written:

```lean
noncomputable instance piEuclideanNormedAddCommGroup : NormedAddCommGroup (EuclideanSpace ℝ (Fin n) → ℝ) := ...
```
inside a `section` with `variable {n : ℕ}` — but as I grepped, no such instance exists textually... unless it's written via `BoundedContinuousFunction`-style with `where`.

Let me grep D12 sources for "PseudoMetricSpace", "dist_", "NormedAddCommGroup", "SeminormedAddCommGroup".
dsh: reasoning:
Now I see. In the stored type of `mildToClassicalBridge`, the `NormedAddCommGroup` instance argument is `Poincare.D12.ParabolicLocal.mildToClassicalBridge._proof_1 n` — an auxiliary declaration auto-generated by Lean from a **structure literal proof field** within the definition. The instance's other fields: `Pi.addCommGroup`, `Pi.Function.module`, `Pi.topologicalSpace`, and `_proof_1`.

Hmm, that combination exactly matches a **`NormedAddCommGroup` instance constructed by `AddGroupNorm.toNormedAddCommGroup`** or by a structure literal where most fields are inherited from Pi instances. Wait: `Pi.addCommGroup` (from `AddCommGroup`), `Pi.Function.module` (Module), `Pi.topologicalSpace` (Topology), `_proof_1` (the norm proof?). This is what `NormedAddCommGroup.mk`? Hmm.

Let me look at the hasDerivAt_pi pp.all output (lines 180-220): I see `[inst_1 : (i : ι) → NormedAddCommGroup (E' i)]` and `inst_2 : ∀ i, NormedSpace 𝕜 (E' i)`, and then the LHS `HasDerivAt` uses:
- `Pi.addCommGroup ...`
- `NormedSpace.toModule ...`
- `Pi.topologicalSpace ...`
- then some proof terms (line 214-216 mention PseudoMetricSpace from SeminormedAddCommGroup from NormedAddCommGroup inst_1).

Hmm, so where's the `NormedAddCommGroup (∀ i, E' i)` instance in hasDerivAt_pi? It must be `mildToClassicalBridge._proof_1`-like. Let me look at lines 180-260 of the log in full to see the structure of the instance for the Pi type.

Actually — I recall now! In Mathlib there IS an instance for Pi types of *bounded* functions? No. Let me think about what instance has fields: AddCommGroup, Module, TopologicalSpace, and a proof `_proof_1`. This looks like `NormedAddCommGroup.ofCore`? Or maybe it's the **`Pi.normedAddCommGroup`** instance *specialized* via `Fintype` — no, it would print as `Pi.normedAddCommGroup`.

Hold on: maybe `mildToClassicalBridge._proof_1` is not the NormedAddCommGroup instance but the **`NormedSpace`** instance. The argument order of `HasDerivAt`:
```
def HasDerivAt (f : 𝕜 → F) (f' : F) (x : 𝕜) : Prop
```
with variable declarations in `Deriv/Defs.lean`:
```
variable {𝕜 : Type u} [NontriviallyNormedField 𝕜]
variable {F : Type v} [NormedAddCommGroup F] [NormedSpace 𝕜 F]
```
The `#print HasDerivAt` would show. In the pp.all output, arguments after `F`:
1. `@Pi.addCommGroup ...` — hmm, this is the AddCommGroup *field*? In pp.all, an instance argument like `NormedAddCommGroup F` is a single argument whose value is a constant (e.g. `instNormedAddCommGroup`). But here we see a *sequence* of structure fields. That means the instance value is a structure literal (anonymous constructor), printed with `pp.all` in "structure eta" form? Actually pp.all does NOT unfold structure instances. But it DOES print structure *literals* in the form `@NormedAddCommGroup.mk ... field1 field2 ...` or as `{ toNorm := ..., ... }`. Hmm, the output shows them as separate arguments of `@HasDerivAt`, which would be wrong unless...

Wait, maybe I misparsed the output. The output is:
```
@HasDerivAt.{0, 0} Real (...) 
  ((x : EuclideanSpace ℝ (Fin n)) → Real)
  (@Pi.addCommGroup ...)
  (@Pi.Function.module ...)
  (@Pi.topologicalSpace ...)
  (mildToClassicalBridge._proof_1 n)
  (fun s x => ...)
  ...
```
For this to typecheck, `HasDerivAt` would need 4+ instance arguments. Let me check the actual signature of `HasDerivAt` in this Mathlib version: maybe it is
```
def HasDerivAt (f : 𝕜 → F) (f' : F) (x : 𝕜) : Prop := ...
```
with instances `[NontriviallyNormedField 𝕜] [NormedAddCommGroup F] [NormedSpace 𝕜 F]`... that's 3 instance args, not 4.

Unless the `AddCommGroup`, `Module`, `TopologicalSpace` args are *fields of a `NormedAddCommGroup` instance literal* being passed as ONE argument, and pp.all prints nested structure literals in flattened form... no, it wouldn't flatten across argument boundaries.

Hmm, look at the hasDerivAt_pi pp.all (lines 180+). It shows `[inst_1 : (i : ι) → NormedAddCommGroup (E' i)]` — these are *instance implicit* binders printed in the binder list. Then in the type, the LHS `HasDerivAt φ φ' x` is printed with its instance arguments: I see `Pi.addCommGroup`, `NormedSpace.toModule`, `Pi.topologicalSpace`, and something with PseudoMetricSpace. That's 4 explicit args where I expected 3 (NormedAddCommGroup, NormedSpace, ...). 

Oh! Maybe I'm wrong about the version's `HasDerivAt`: In Lean 4 Mathlib (recent), `HasDerivAt` may be defined as:
```
def HasDerivAt (f : 𝕜 → F) (f' : F) (x : 𝕜) : Prop :=
  HasDerivAtFilter f f' x (𝓝 x)
```
and `HasDerivAtFilter`:
```
def HasDerivAtFilter (f : 𝕜 → F) (f' : F) (x : 𝕜) (L : Filter 𝕜) : Prop :=
  HasFDerivAtFilter f (ContinuousLinearMap.smulRight (1 : 𝕜 →L[𝕜] 𝕜) f') x L
```
The instance args of `HasFDerivAtFilter` etc. would be `[NontriviallyNormedField 𝕜] [NormedAddCommGroup F] [NormedSpace 𝕜 F]`.

The `smulRight` requires `ContinuousLinearMap` instances for `𝕜 →L[𝕜] 𝕜`, and there might be `[SeminormedAddCommGroup]`-style args appearing because Lean stores them explicitly.

OK, rather than reverse-engineering pp.all printing, let me directly search: does the environment contain a *declaration* named `Poincare.D12.ParabolicLocal.mildToClassicalBridge._proof_1`? And what is its type? `#print` it, plus `#check`. That will tell us what instance it is. If it's a `NormedAddCommGroup` instance on the function space constructed from `AddGroupNorm`—wait, but the grep for instances found nothing in the D12 source. But the `_proof_1` is generated from a *proof obligation inside the definition body*, e.g. if the author wrote:

```lean
def mildToClassicalBridge (n : ℕ) : Prop :=
  ∀ {t} (ht) (f), @HasDerivAt ℝ (EuclideanSpace ℝ (Fin n) → ℝ) _ _ _ _ ...
```

Hmm, but where would a structure literal with proof obligations come from? Maybe from `letI` inside the *statement*? You can't have letI in a type (well, you can: `letI : NormedAddCommGroup F := ...; HasDerivAt ...` is a let-expression in the type! Actually yes! Types can contain `let` expressions. But the printed type shows `@HasDerivAt ... (mildToClassicalBridge._proof_1 n) ...` — a reference to an auxiliary proof, not a let.

Wait — actually, maybe the source uses `ContinuousLinearMap.smulRight`? No.

Let me just check whether the D12 `Obligations.lean` file (around line 208) is really the source of the definition; maybe there's an `instance` nearby in the file I haven't read (lines 1-100). Let me read the whole Obligations.lean head and also search the entire D12 directory for `AddGroupNorm`, `_proof_1`, `where`, `instance`.

Actually, here's a cleaner hypothesis: maybe the source writes

```lean
def mildToClassicalBridge (n : ℕ) : Prop :=
  ∀ {t : ℝ} (ht : 0 < t) (f : BUCn n),
    @HasDerivAt ℝ (EuclideanSpace ℝ (Fin n) → ℝ) inferInstance inferInstance
      (fun s x => ...) (fun x => ...) t
```

and `inferInstance` for `NormedAddCommGroup` at *elaboration time of D12* succeeded because of a **local instance** in the file or namespace... and Lean stored the resolved instance term, which involves an auxiliary `_proof_1`. The presence of `_proof_1` as an auto-generated auxiliary declaration for an instance literal strongly suggests a structure literal with a proof field that Lean lifted out of the def.

Hmm, but there's no `instance ... where` in the source per grep (grep for "NormedAddCommGroup" in ParabolicLocal showed only BUCn instances and theorem variables). Let me grep for `_proof_1`-generating patterns: `instance :` in all of D12, plus `AddGroupNorm`.

Actually — WAIT. I bet this is `Pi.normedAddCommGroup` applied with a **`Fintype (EuclideanSpace ℝ (Fin n))` instance**... and the `_proof_1` is a proof inside that Fintype instance? Hmm.

Let me look at the *whole* pp.all output section for the instance. The key line 29 is `(mildToClassicalBridge._proof_1 n)`. Let me look at the lines 15-35 of the log to count the arguments. Then `#print Poincare.D12.ParabolicLocal.mildToClassicalBridge._proof_1`. But private auxiliary names may be inaccessible from my module (`_proof_1` is a private name — accessible via the full name for printing? `#print` of a private name might fail: "unknown declaration"? Actually private names are `_private.Poincare.D12.ParabolicLocal.0.mildToClassicalBridge._proof_1`; the printed `mildToClassicalBridge._proof_1` is the *user-facing* name? Hmm, private aux names in Lean are `_private.<module>.<index>.<name>`. The pp.all printer shows them with a simplified name. `#print` might fail.

Alternative approach: use `#print HasDerivAt` and `#check @HasDerivAt` (pp.all) to count instance args, then use `set_option trace.Meta.synthInstance true` on the `#synth` to see the failure, and try `#synth` inside the same namespace with `open scoped` etc.

Actually, simplest decisive test: **write an example in the exact form** and see if it elaborates. My StageA2 `example` tries exactly that (restating the goal). Let me check the log tail for whether that example succeeded (errors?).

Let me look at the error lines of stageA2.log.
dsh: reasoning:
Line 293 has an error: "Function expected at ..." for the `example` at line 34? Wait, line 34 in StageA2 is the `example ... := (Poincare.D12.ParabolicLocal.mildToClassicalBridge n) ht f`. The error "Function expected at" suggests the application `(mildToClassicalBridge n) ht f` is malformed — because `mildToClassicalBridge n` is `∀ {t}, 0<t → ...`; applying to `ht` (with t implicit) might work... Actually since t is implicit and ht : 0 < t, `(mildToClassicalBridge n) ht` should infer t from ht. Hmm, "Function expected at ..." means the thing being applied is not a function — maybe `(mildToClassicalBridge n) ht f` parsed as `mildToClassicalBridge n ht f` where `n` is applied to `ht`? No, parentheses...

Wait, the last `#check` (line 40-ish) succeeded and printed the full type with a mysterious instance! Its output is at the tail. So **the freshly elaborated `HasDerivAt` on the function space DID work** in that `#check`! That means instance synthesis succeeds during term elaboration but `#synth` fails?!

Let me scroll up in the log to see the `_proof_1` context and the exact instance. The tail shows the #check for `(g : ℝ → EuclideanSpace ℝ (Fin 4) → ℝ) (g' : ...)` elaborated fine — with instance args. So Lean CAN synthesize the instance in a `#check` inside this file, but my earlier `#synth NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)` failed. Why the difference?

OH. I think I finally get it: **`NormedSpace ℝ (EuclideanSpace ℝ (Fin n) → ℝ)`** — the module structure `Pi.Function.module` exists for arbitrary index. And there's an instance of `NormedAddCommGroup` on a Pi type of *normed groups* that exists for arbitrary index?? But no.

Hmm, the difference between `#check (fun g g' => (HasDerivAt g g' 0 : Prop))` and `#synth NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)`: the former elaborates `HasDerivAt g g' 0` where `g : ℝ → EuclideanSpace ℝ (Fin 4) → ℝ`. Instance synthesis for `NormedAddCommGroup ((EuclideanSpace ℝ (Fin 4)) → ℝ)`. Same type shape. Unless... the binder `(g : ℝ → EuclideanSpace ℝ (Fin 4) → ℝ)` is *parsed as* `ℝ → (EuclideanSpace ℝ (Fin 4) → ℝ)`, so g's codomain is the Pi type. Yes, same.

So why did one succeed and the other fail? Let me look at the middle of the log: maybe the `#check` at the end succeeded because the instance was found via a **local instance created by the `example` failure**? No.

Actually, maybe the `#check` output at the tail is from the *failed line 34* somehow? Let me look at the structure of the log more carefully: grep for the line markers. Let me search for "StageA2.lean" error lines and check what's around line 293.

Better: let me write a minimal focused file:

```lean
import Poincare.L3.HeatTimeDeriv.All
#synth NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)
example (g : ℝ → EuclideanSpace ℝ (Fin 3) → ℝ) (g' : EuclideanSpace ℝ (Fin 3) → ℝ) : HasDerivAt g g' 0 := by
  sorry  -- just to check elaboration of the statement
```
But `sorry` is forbidden in *my* artifacts? The acceptance forbids sorry in the reviewed artifact; my probe should avoid sorry too for cleanliness (it would also taint any axiom checks if I audited my own file — I don't). Better to avoid sorry: use `by exact hasDerivAt_const 0`? That's a different type. Or use `example ... : HasDerivAt g g' 0 → True := fun _ => trivial` — no, elaboration of the statement still happens.

Hmm wait, actually the tail of the log shows the #check output of the LAST `#check` — and it printed a fully elaborated type! So elaboration of `HasDerivAt g g' 0` succeeded there. Let me confirm by searching the log for the #check output start and looking at which instance it used. Let me grep for "HasDerivAt" and surrounding lines, and find the instance argument (right after the domain type). Let me extract the region around the start of the pp.all #check.

Let me find line numbers: the log is 552 lines; the last #check starts maybe around line 300. Let me print lines 280-340.
dsh: reasoning:
VERY interesting! The last `#check` (line 40 of my file) *succeeded* and its elaborated type includes instance arguments:

```
@HasDerivAt ℝ (EuclideanSpace ℝ (Fin 4) → ℝ)
  (@Pi.addCommGroup ...)            -- argument 3
  (@Pi.Function.module ...)         -- argument 4
  (@Pi.topologicalSpace ...)        -- argument 5
  (@instContinuousSMulForall ...)   -- argument 6
  ...
```

Hmm, so `HasDerivAt` here takes *many* instance args — this is not the usual Mathlib `HasDerivAt`! It seems `HasDerivAt` in this environment takes the *unbundled* typeclass assumptions: `AddCommGroup`, `Module`, `TopologicalSpace`, `ContinuousSMul`, `IsTopologicalAddGroup`, ... i.e., the old-style `NormedSpace` core typeclass fields?

Hold on. Look: this looks like the **`NormedAddCommGroup.ofCore`-style "core" typeclass** from Lean core: in Lean 4 core, `NormedAddCommGroup` is defined via `NormedAddCommGroup.ofCore` with fields... Hmm.

Actually, I recognize this pattern now! Recent Lean/Mathlib (v4.34) may have changed `NormedAddCommGroup` to be built from `AddCommGroup`, `Module`, `TopologicalSpace`, `ContinuousSMul`, `IsTopologicalAddGroup`, `SeminormedAddCommGroup`... no.

Wait, look at argument 6: `@instContinuousSMulForall` — an instance for `ContinuousSMul ℝ (∀ x, ℝ)` — that's the **product topology** continuous scalar multiplication. And argument 5 is `Pi.topologicalSpace`. So `HasDerivAt`'s assumptions here are of the form used by **`HasFDerivAt` in a "locally convex space" / topological vector space setting**? Hmm.

Let me check the actual definition of `HasDerivAt` in this environment: `#print HasDerivAt`. And `#print HasDerivAtFilter`. I suspect that in this Mathlib version, `HasDerivAt` requires only `[NontriviallyNormedField 𝕜] [AddCommGroup F] [Module 𝕜 F] [TopologicalSpace F] [IsTopologicalAddGroup F] [ContinuousSMul 𝕜 F] [SeminormedAddCommGroup?]...` Hmm no.

Hmm wait, actually: the printed instance args might be the *fields of a NormedSpace instance literal* being printed as separate args because `HasDerivAt` has instance-implicit binders that were **flattened**? No.

Let me look at the FULL type of `HasDerivAt` via `#check @HasDerivAt` with pp.all. And the definition `#print HasDerivAt`.

Hypothesis: In Lean 4.34/Mathlib master, `HasDerivAt` might be generalized to work over `NormedAddCommGroup`... no, the args clearly show unbundled fields.

Actually, here's a much more likely explanation: **`HasDerivAt` is fine, but the types displayed are the fields of the anonymous `NormedAddCommGroup`/`NormedSpace` instances**, and pp.all prints *structure literals* by their *fields as separate arguments of the constructor*, e.g. `@NormedAddCommGroup.mk α addCommGroup ... `. But the printer would show `@NormedAddCommGroup.mk ...` with the constructor name. Unless the structure is a **class with `extends` where the constructor is `NormedAddCommGroup.mk`** and pp.all shows eta-expanded... it would still show `.mk`.

Hmm, look closely at the hasDerivAt_pi pp.all output (lines ~180-293): the LHS `HasDerivAt` application has args:
- `Pi.addCommGroup`
- `NormedSpace.toModule ...`
- `Pi.topologicalSpace ...`
- `PseudoMetricSpace.toUniformSpace ...` (part of a `@UniformSpace.toTopologicalSpace` arg?)
- ...
- `instPolynormableSpace ...`  ← **PolynormableSpace**! 

`PolynormableSpace` is a very new Mathlib class (2025). And `instContinuousSMulForall` is a Pi instance for `ContinuousSMul`. So the assumption set of `HasDerivAt` in this pin seems to be the *unbundled* form: `[AddCommGroup F] [Module 𝕜 F] [TopologicalSpace F] [IsTopologicalAddGroup F] [ContinuousSMul 𝕜 F] [PolynormableSpace ...]`? That's the **`NormedSpace`-free derivative**? 

I recall that in 2025 Mathlib, `HasFDerivAt` was generalized to `NormedAddCommGroup`... no.

Hmm, hold on. Let me reconsider: maybe these aren't `HasDerivAt`'s args at all! The `#check` term is:
```
fun (g : ℝ → EuclideanSpace ℝ (Fin 4) → ℝ) (g' : EuclideanSpace ℝ (Fin 4) → ℝ) =>
  @HasDerivAt ℝ _ (EuclideanSpace ℝ (Fin 4) → ℝ) <arg3> <arg4> <arg5> ... g g' 0
```
This IS `@HasDerivAt` applied to a bunch of instance args. So `HasDerivAt` has that many instance args in this pin.

Wait... unless the identifier `HasDerivAt` here resolves to a *different* declaration than Mathlib's — e.g., a D12-authored `HasDerivAt`! Let me check: `#check @HasDerivAt` would show its full name. In pp.all output the name is printed as `@HasDerivAt.{0,0}` without a namespace — because it's in the root namespace (Mathlib's is `HasDerivAt` in root). If D12 had defined its own, it would be namespaced.

So Mathlib's `HasDerivAt` in v4.34.0-rc2 / mathlib 7974e75 has ~7 instance args including `PolynormableSpace`. This suggests the derivatives library was recently refactored to use **`PolynormableSpace`** (a class for spaces where the topology comes from a family of seminorms?) instead of `NormedAddCommGroup`... Actually! I remember: Mathlib recently generalized `HasFDerivAt` to "locally convex spaces"? Hmm, no. Let me check the source of `HasDerivAt` in the pinned mathlib.

Let me just read `Mathlib/Analysis/Calculus/Deriv/Defs.lean` (or wherever HasDerivAt is defined) in the pin. And `Mathlib/Topology/Algebra/Module/...`. Let me look at the variables in Deriv/Defs.lean.

This matters enormously for the review: if `HasDerivAt` in this pin requires only `PolynormableSpace` (a *property* class) rather than a norm, then the D12 obligation on the raw function space with the product topology is an *honest* derivative statement in the product topology (not a normed topology), and the L3 card's description ("pointwise convergence topology") would be exactly right, and the `#synth NormedAddCommGroup` failure is irrelevant (the pin's HasDerivAt doesn't need it). The "Banach-space strengthening" claim then needs checking: `HasDerivAt` on `BCFn n` (a normed space) — fine.

Wait, but the `#synth` failure for `NormedAddCommGroup` is still correct: the raw function space has NO norm in this pin. And D12's `mildToClassicalBridge` docstring says "in the Banach space of functions on ℝⁿ" — which is *wrong*: the raw function space with the product topology is not a Banach space (not even normed). L3's card correctly identifies this (calls it pointwise, and the uniform version is the genuine Banach statement). Good.

But hmm, how does `@HasDerivAt` work with a `PolynormableSpace`? Let me look at the actual definition to understand the semantics: `HasDerivAt` needs a topology and a notion of derivative; for `F` with only a topological vector space structure? The Fréchet derivative requires a norm to define `HasFDerivAt`... unless it's generalized to "locally convex" or "polynormable" spaces via `ContinuousLinearMap`. Hmm, in 2025 there was a Mathlib PR "generalize derivative to topological vector spaces"? I'm not sure.

Let me inspect the pinned mathlib: `grep -rn "def HasDerivAt\b" Mathlib/Analysis/Calculus/Deriv/Defs.lean` and read the variables.

This is a crucial semantic point for the review: what is the exact ambient structure of the "pointwise" bridge, and is the claim "the product topology HasDerivAt" accurate?

Let me check.
dsh: reasoning:
HUGE finding. In this mathlib pin (rev 7974e751), `HasDerivAt` has been generalized to **topological vector spaces** (`section TVS`):

```lean
variable {𝕜 : Type u} [NontriviallyNormedField 𝕜]
variable {F : Type v} [AddCommGroup F] [Module 𝕜 F] [TopologicalSpace F]
section
variable [ContinuousSMul 𝕜 F]
def HasDerivAtFilter ...
def HasDerivAt ...
```

So `HasDerivAt` requires only `[AddCommGroup F] [Module 𝕜 F] [TopologicalSpace F] [ContinuousSMul 𝕜 F]` — no norm! Hence the raw function space `EuclideanSpace ℝ (Fin n) → ℝ` with the product topology qualifies: it has AddCommGroup, Module, TopologicalSpace (product), ContinuousSMul (Pi instance). The derivative is defined via `HasFDerivAtFilter f (toSpanSingleton 𝕜 f') L` which presumably requires a `ContinuousLinearMap` structure — in the TVS setting, `toSpanSingleton` requires `PolynormableSpace`? The trace showed `instPolynormableSpace` among the instance args, plus `IsTopologicalAddGroup`? Let me look at `HasFDerivAtFilter` in FDeriv/Defs.lean to see the exact requirements. And what is `PolynormableSpace`?

So the semantics: `HasDerivAt` in this pin is a *topological* derivative notion (Fréchet derivative in a TVS setting, where `E →L[𝕜] F` continuous linear maps still make sense). For the product topology on `EuclideanSpace ℝ (Fin n) → ℝ`, this is a genuine (TVS) derivative statement — the "pointwise" derivative. But the *meaning* of derivative in the TVS setting: is it equivalent to pointwise differentiability of all coordinates? `hasDerivAt_pi` indeed gives exactly that equivalence. Good.

So the L3 card's semantic description is essentially right: the D12 obligation is a pointwise-derivative statement in the product topology, and the L3 uniform statement is the norm-topology statement on BCFn. I need to be careful in the card to describe this precisely, and to note that "Banach-space derivative" claims are genuinely about `BCFn n` (a normed space; and `HasDerivAt` in TVS form still works).

Now: the `#synth NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)` failure is expected and *not* a problem: the function space is a topological vector space (product topology), not a normed space. My earlier confusion resolved. It's actually an important precise semantic point for the review: the D12 obligation's ambient space is `EuclideanSpace ℝ (Fin n) → ℝ` **with the product topology**, which is *not* normable in the way BCFn is; it is NOT a Banach space despite the D12 docstring's wording. The L3 card says "the orbit ... differentiable in the product (pointwise-convergence) topology" (mildToClassicalBridge_pointwise docstring) and describes the uniform version as the Banach-space strengthening. Accurate. Excellent.

But: does `HasDerivAt` in TVS form require `PolynormableSpace F`? The trace showed `instPolynormableSpace` in the instance args. Let me check `HasFDerivAtFilter`'s requirements in FDeriv/Defs.lean:

```lean
def HasFDerivAtFilter (f : E → F) (f' : E →L[𝕜] F) (x : E) (L : Filter E) :=
  (fun x' => f x' - f x - f' (x' - x)) =o[L] (fun x' => x' - x)
```
This needs `E →L[𝕜] F` (ContinuousLinearMap) which requires... for `E = 𝕜` (the domain of HasDerivAtFilter is `𝕜 → F` and the filter is on `𝕜 × 𝕜`), `toSpanSingleton 𝕜 f' : 𝕜 →L[𝕜] F` requires `[ContinuousSMul 𝕜 F]`?? and maybe `PolynormableSpace F` for `toSpanSingleton` to be continuous? Hmm.

Anyway, the key semantic points:
1. `HasDerivAt` in this pin is a TVS-level notion; the L3 use is legitimate.
2. The D12 `mildToClassicalBridge` is stated in the product topology (pointwise convergence), NOT the sup-norm topology. This is exactly what L3's `uniformMildToClassicalBridge` upgrades. So the "discharge" is a pointwise statement; the D12 docstring's phrase "in the Banach space of functions on ℝⁿ" is inaccurate.
3. The claim "hasDerivAt_heatConv_BCF is the Banach-space form" needs checking: `BCFn n = (EuclideanSpace ℝ (Fin n)) →ᵇ ℝ` with sup norm — genuinely normed (Banach since ℝ complete and domain arbitrary). So that's the genuine Banach-space statement. 

Now I need to check `PolynormableSpace` — what is it, and does the product topology on `EuclideanSpace ℝ (Fin n) → ℝ` satisfy it? It must, since the statement elaborated (instance `instPolynormableSpace` appeared). Fine.

Also, `HasDerivAt` TVS-level: is the *uniqueness* of the derivative guaranteed in a non-Hausdorff TVS? Products of ℝ are Hausdorff, fine. In `mildToClassicalBridge_holds` they use `.unique`? In `integral_timeDerivKernel_mul_const` (Basic.lean) they use `h1.unique h2` — `HasDerivAt.unique` requires `[T2Space F]`? Hmm, in the TVS generalization, uniqueness may require `T2Space`. The raw function space is T2 (product of T2). Fine.

Wait, but this also raises a question about `hasDerivAt_heatOperator` in `Basic.lean`: it's stated for `f : EuclideanSpace ℝ (Fin n) → ℝ` and says `HasDerivAt (fun s : ℝ => heatOperator n s f x) (...) t` — that's an ℝ-valued function, ordinary. Fine.

Now, the deeper question for the review: does `hasDerivAt_pi` (Mathlib) hold in the TVS setting? It's proved in Mathlib for `[∀ i, NormedAddCommGroup (E' i)]`?? Wait, the file I read (`Deriv/Prod.lean`) had:
```
variable {ι : Type*} {E' : ι → Type*} [∀ i, NormedAddCommGroup (E' i)]
  [∀ i, NormedSpace 𝕜 (E' i)] {φ : 𝕜 → ∀ i, E' i} {φ' : ∀ i, E' i}
```
Hmm, but the pp.all output of `hasDerivAt_pi` showed binders `[inst_1 : (i : ι) → NormedAddCommGroup (E' i)]` and `[inst_2 : ∀ i, NormedSpace 𝕜 (E' i)]`. So the *hypotheses* are coordinatewise normed, but the *conclusion* `HasDerivAt φ φ' x` is in the TVS sense on the product (using the coordinatewise norm-induced topologies to build the product topology and the Pi instances). This is fine and standard.

So, how is `mildToClassicalBridge_pointwise` proved? `rw [hasDerivAt_pi]` then per-coordinate `hasDerivAt_heatConv_apply`. With the TVS HasDerivAt, `hasDerivAt_pi` applies given coordinatewise `NormedAddCommGroup` instances (ℝ has one) and the Pi type gets AddCommGroup/Module/TopologicalSpace/ContinuousSMul instances — which exist for arbitrary index. 

Everything is consistent now. My Stage A `#synth NormedAddCommGroup` was a red herring; the space isn't normed. But there IS a subtlety worth verifying: is the *topology* used by `HasDerivAt` on the raw function space the product topology? In the pp.all print: `@Pi.topologicalSpace ... (fun i => @UniformSpace.toTopologicalSpace ℝ Real.pseudoMetricSpace)` — YES, product topology of the usual topology on ℝ. 

And the `ContinuousSMul` instance: `@instContinuousSMulForall ...` — the Pi instance for continuous scalar multiplication, which is valid in the product topology. Good.

So the L3 card's description is right and D12's docstring is loose. My review should note this as a *docstring/semantic-class* correction: mildToClassicalBridge is a TVS-derivative statement in the product topology; its name "bridge (in the Banach space)" is misleading but L3 characterizes it correctly.

Now, one more important check: `mildToClassicalBridge_of_uniform` claims the uniform statement implies the pointwise one. With the TVS setting, the proof uses `hasDerivAt_pi` and `hasDerivAt_iff_tendsto_slope_zero`. Fine.

Next: run the audit probe (SemrevAudit.lean) and the semantic probes. Let me first fix StageA.lean (remove the erroneous `#synth`s and the failed example) or just keep the evidence and move on. I'll keep the log as evidence of the TVS finding but write a corrected Stage A.

Actually, for the review card, the evidence I need:
- The exact instance/topology analysis (have it in stageA2.log).
- A clean, compiling diagnostic file.

Let me now write Stage C (semantics: constructed inputs, consumers) and Stage D (non-vacuity checks). And run the audit.

Let me first run SemrevAudit.lean to see if it compiles.
dsh: reasoning:
Two compile errors:
1. `AxiomVal.mk n` — the AxiomVal structure signature changed; I used `.axiomInfo ⟨n, [], .const n [], false⟩`.
2. `Bool` expected in `.endsWith "|ok"` — `String.endsWith` returns Bool but `!` needs `not`? Actually error: "Invalid ⟨...⟩ notation: The expected type Bool has more than one constructor" — that's from the `getD` fallback expression I wrote: `.getD (.axiomInfo ⟨n, [], .const n [], false⟩)` — `.getD` expects a value of type ConstantInfo, but Lean inferred Bool? Because `Option.getD` argument order? `Option.getD : Option α → α → α`. Fine. The error line 79:65 is the `⟨...⟩` inside. The AxiomVal constructor requires specific fields.

Simplest fix: avoid the fallback entirely — I already check `contains n` before, so use `(← getEnv).constants.find? n` and `match` it:
```lean
match (← getEnv).constants.find? n with
| some ci => ...
| none => -- missing
```
Let me restructure to avoid constructing ConstantInfo.

Let me rewrite the relevant part:
```lean
  for n in snapshotDecls do
    match (← getEnv).find? n with
    | none => ...
    | some ci => ...
```
`Environment.find?` exists: `env.find? n : Option ConstantInfo`. Yes.

Also `.endsWith` returns Bool; `!(r.endsWith "|ok")` is `!Bool`? In Lean 4, `!` is Bool not. That's fine. The error about Bool was from the `⟨...⟩` in the fallback being elaborated against `Bool` due to `Option.getD`'s implicit α being inferred from the `!...`? Whatever — restructure.

Let me also double check `env.constants.toList` works: the run_cmd got past that loop (11 lines of output, error at line 79 which is in the second loop). Actually the errors are at line 79:65; let me see the head of log to know whether the first loop worked.
dsh: reasoning:
Only the fallback expression failed. The first loop didn't run because errors abort before execution? `run_cmd` executes after elaboration; since the file has errors, nothing ran. Let me fix.
dsh: reasoning:
Excellent! My independent audit:
- 85 constants mentioning HeatTimeDeriv (vs the parent's 42 authored + 9 snapshot = 51 hand-picked).
- 9 snapshot declarations.
- 0 violations.
- All depend only on [propext, Classical.choice, Quot.sound].

Let me look at the full audit list to classify kinds and check for any `AXIOM`/`UNSAFE-DEF`/`rec`/`ctor` etc. Also check that the 85 includes the 42 parent-listed authored declarations. Let me parse the log with python and compare with the parent's list (from Audit.lean).

Then write the semantic probes (Stage C) and run.

Let me first analyze the audit log.
dsh: reasoning:
My audit found:
- 85 constants under L3.HeatTimeDeriv (parent hand-picked 42; my enumeration includes auxiliary/private-generated constants like `_proof_1`, `_unary`, `mk`, `rec`, projections, etc.).
- 9 snapshot declarations (cone [propext] only).
- 94 rows total, all `ok`, no AXIOM/UNSAFE kinds, no `sorryAx`.

Wait: 85 L3 constants but 68+23+1+1+1 = 94 total rows including snapshot 9 → 85 L3: kinds distribution includes the induct/ctor/rec (KernelClassicalHeatSolution structure). Good.

Note the parent's 42 authored declarations are presumably a subset of my 85. Let me verify each of the 42 appears in my enumeration with verdict ok.

Let me do that comparison. Also let me check what the extra 43 constants are (auxiliary) — mostly harmless but good to enumerate in evidence.

Next: Stage C semantics probes. Let me write:

```lean
import Poincare.L3.HeatTimeDeriv.All

namespace SemrevL3
open Poincare.D12.ParabolicLocal Poincare.L3.HeatTimeDeriv

-- (1) constructed input: a concrete BUC datum on ℝ^3
def f3 : BUCn 3 :=
  ⟨BoundedContinuousFunction.const (EuclideanSpace ℝ (Fin 3)) (1 : ℝ), uniformContinuous_const⟩

example : (f3 : EuclideanSpace ℝ (Fin 3) → ℝ) 0 = 1 := rfl

-- (2) the discharged obligation instantiated at the concrete datum
example (t : ℝ) (ht : 0 < t) :
    HasDerivAt (fun s : ℝ => fun x : EuclideanSpace ℝ (Fin 3) => (heatConv 3 s f3.val) x)
      (fun x => timeDerivIntegral 3 t f3.val x) t :=
  mildToClassicalBridge_pointwise 3 ht f3

-- (3) the packaged solution at the concrete datum, with fields consumed
example : (heatConv_classicalHeatSolution 3 f3).T = 1 := rfl
example (t : ℝ) (ht : t ∈ Set.Ioo (0:ℝ) (heatConv_classicalHeatSolution 3 f3).T)
    (x : EuclideanSpace ℝ (Fin 3)) :
    HasDerivAt (fun s : ℝ => (heatConv_classicalHeatSolution 3 f3).u s x)
      (∫ y, (Δ (gaussianKernel 3 t) (x - y)) * (heatConv_classicalHeatSolution 3 f3).u₀ y) t :=
  (heatConv_classicalHeatSolution 3 f3).isSolution t ht x

-- (4) initial-condition field consumed: Tendsto ... (𝓝[>] 0) (𝓝 u₀)
example : Tendsto (heatConv_classicalHeatSolution 3 f3).u (𝓝[>] (0:ℝ)) (𝓝 (heatConv_classicalHeatSolution 3 f3).u₀) :=
  (heatConv_classicalHeatSolution 3 f3).initial

-- (5) a genuine consumer of the Banach-space derivative: continuity of the orbit in BCFn
example (t : ℝ) (ht : 0 < t) : ContinuousAt (fun s : ℝ => heatConv 3 s f3.val) t :=
  (hasDerivAt_heatConv_BCF 3 ht f3).continuousAt

-- (6) uniform statement instantiated: extract δ and use it at a concrete x
example (t : ℝ) (ht : 0 < t) : ∃ δ > 0, ∀ h, 0 < |h| → |h| < δ → ∀ x, |(heatConv 3 (t+h) f3.val x - heatConv 3 t f3.val x)/h - timeDerivIntegral 3 t f3.val x| < 1 :=
  uniformMildToClassicalBridge_holds 3 ht f3 1 (by norm_num)
```

Careful: `BUCn 3` is `BUCf 3` structure with fields `val` and `uniformContinuous_val`. `BoundedContinuousFunction.const` + `uniformContinuous_const`. The statement `UniformContinuous (fun x => (BoundedContinuousFunction.const (EuclideanSpace ℝ (Fin 3)) 1 : BCFn 3) x)` — `uniformContinuous_const` should unify. Let me try.

Also verify non-vacuity of `heatOperator_const`: `heatOperator_const 3 ht 5 x : heatOperator 3 t (fun _ => 5) x = 5`.

And derivative-domain check: at t=0, the theorem precondition fails; can I show the derivative doesn't exist? Not needed. But I can show the *statement* excludes t=0 (hypothesis `0 < t`).

Also check that `SpatialLaplacianBridge` has no inhabitant in the environment: I can enumerate all constants whose type ends in `SpatialLaplacianBridge n` — my audit loop can do that. Simpler: check `#print axioms SpatialLaplacianBridge` (a def, no axioms) and grep that no theorem name mentions it except the def/statement itself. I already know from my full enumeration that the only constants are those in the audit list; let me check whether any L3 constant's *type* mentions `SpatialLaplacianBridge` or `UniformMildToClassicalBridge`. I can add to the audit a scan: for each enumerated constant, check if `SpatialLaplacianBridge` is a subexpression of its type and whether the constant is a theorem concluding it. Doing this in Lean metaprogramming is a bit involved; a simpler approach: print all 85 constants' types with `#check` via `logInfo` and inspect manually. Actually I can add a `run_cmd` loop that prints `SEMREV-DECL|name|type` for all 85 L3 constants. That gives a definitive census with types (for the semantic class table per declaration!). Let me do that in a separate probe `SemrevCensus.lean` that prints name + type + kind + axioms. The type printing with `ppExpr` may be long but fine (85 lines).

Then I can classify each declaration semantically by reading the census.

Let me write `SemrevCensus.lean`:
```lean
import Poincare.L3.HeatTimeDeriv.All
import Lean.Util.CollectAxioms
import Lean.Elab.Command
open Lean Elab Command
run_cmd do
  let env ← getEnv
  let mut rows := #[]
  for (n, ci) in env.constants.toList do
    if (n.toString.splitOn "HeatTimeDeriv").length > 1 then
      let ty ← Lean.Meta.ppExpr ci.type
      let axs ← Lean.collectAxioms n
      logInfo s!"SEMREV-DECL|{n}|{kindOf ci}|{ty}|{String.intercalate "," (axs.toList.map Name.toString)}"
```
`Lean.Meta.ppExpr` in CommandElabM: need `MetaM`; there's `Lean.Meta.ppExpr (e : Expr) : MetaM String`. From CommandElabM, use `liftTermElabM`? Hmm. Alternatively use `Command.liftCoreM`? The simplest: use `Lean.PrettyPrinter.ppExpr`? There's `ppExpr` in `Lean.Meta` requiring MetaM. In `run_cmd` (CommandElabM), we can do `let s ← liftTermElabM <| Lean.Meta.ppExpr ci.type`? `liftTermElabM : TermElabM α → CommandElabM α` exists. And `TermElabM` can lift MetaM via `MetaM.run'`? Hmm.

Alternative approach: use `#check` for each constant — but I'd need to generate the `#check` commands. I can do that in run_cmd with `elabCommand (← `(#check $(mkIdent n)))`. That's doable:
```lean
for (n, _) in ... do
  elabCommand (← `(command| #check $(mkIdent n)))
```
Hmm, `mkIdent` needs the name as syntax; for a fully qualified name `Poincare.L3...`, `mkIdent n` produces an ident with that name. This prints the type via the standard #check formatter (readable). That's nice and avoids ppExpr API.

But careful: `#check` prints to the info log which we capture. And the volume: 85 declarations × multi-line types → maybe 500 lines. Fine.

Actually simpler: since I need the types for the semantic table, I can just use the parent's `#print axioms` transcript + `#check` list. But the census gives exact types for *all* 85 including auxiliaries, which supports "record exact semantic class per declaration". Let me do it with `elabCommand #check`.

Hmm, `#check` on private auxiliary names might fail ("unknown declaration") since private names can't be referenced by the user-facing name. But I can use `#check` with the *full* name from the environment; for private constants the name is `_private.<module>.<n>.<name>` which might not be resolvable via `mkIdent` (the parser would treat `_private...` as an identifier — should be OK if the name is a valid identifier). Let's try; failures are informative.

Even better: use `#print` on each? Might be huge. `#check` is enough.

Let me also make the census print the *axiom cone* per declaration (already have that from the audit).

Let me write SemrevCensus.lean now. And run it. Then Stage C semantics. Then I'll assemble the card.

Time check: started ~00:05, now ~00:25. Good.

One more important check I should do: the `SpatialLaplacianBridge` non-inhabitation. And the `UniformMildToClassicalBridge` now has an inhabitant `uniformMildToClassicalBridge_holds`. Let me verify by `#check uniformMildToClassicalBridge_holds`.

Also verify that the parent card's claim "the D12 obligation discharged ... type ascription against the D12 name checked in Audit.lean" — verify by an independent type ascription in my probe (done in Stage C).

Another key check: the negative control. Run `lake env lean ../audit-evidence/negcontrol/L3NegControl.lean` from my copied package: need to copy the negcontrol file into my worktree. Let me copy it and run; expect exit 1 with `L3NegControlAxiomCheck: FAIL (expected)`.

Also my own audit should have a negative control: I can produce a modified copy of my audit with an intentional axiom and show it fails. That's good practice: `SemrevAuditNegControl.lean` = same predicate but with `axiom semrevBad : False` and the loop over a name list including `semrevBad`. Simplest: a small file that declares an axiom and runs the same `row`-style predicate; expect exit 1. Let me write it.

Let me write all these probes now.
dsh: reasoning:
Two small errors in my semantics probe:
1. `gaussianKernel` is in namespace `Poincare.D10.HeatKernelEuclidean` — need `open Poincare.D10.HeatKernelEuclidean`.
2. `heatOperator 3 t (fun _ => 5) x` — `heatOperator` takes a BCFn (bounded continuous function), not a raw function. In Basic.lean `heatOperator_const` was stated with `(fun _ => c)` and worked because `heatOperator`'s argument type is... let me check: in Basic.lean, `heatOperator n t (fun _ => c) x` — so `heatOperator` takes a raw function `EuclideanSpace ℝ (Fin n) → ℝ`? The error says expected BCFn. Hmm, in Basic.lean `heatOperator` is opened from `Poincare.D12.HeatSemigroup`; let me check its type: `def heatOperator (n : ℕ) (t : ℝ) (f : EuclideanSpace ℝ (Fin n) → ℝ) (x) : ℝ := ∫ ...`. But the error says it expects `BCFn 3` for the argument. Wait: in my probe I wrote `heatOperator 3 t (fun _ => (5:ℝ)) x`. If `heatOperator` has signature `(n) (t) (f : BCFn n) (x)`, then `fun _ => 5` fails. But Basic.lean's `heatOperator_const` states `heatOperator n t (fun _ => c) x = c` — so `heatOperator`'s second argument must be a raw function there. Unless there's a coercion from `EuclideanSpace ℝ (Fin n) → ℝ` to BCFn? No.

Hmm, maybe there are two `heatOperator`s: D12.HeatSemigroup.heatOperator (raw function) and another one? Let me check the signature. `#check @Poincare.D12.HeatSemigroup.heatOperator` in the census log. Let me grep.

Actually the error says "expected to have type BCFn 3" — so `heatOperator 3 t` expects `BCFn 3`. But Basic.lean's `heatOperator_const` (which compiled) uses `heatOperator n t (fun _ => c)`. So `heatOperator`'s f argument type must be `EuclideanSpace ℝ (Fin n) → ℝ`, and `fun _ => c` elaborates to that. Unless the option `autoImplicit`... no.

Wait, maybe the issue is my call `heatOperator 3 t (fun _ => (5:ℝ)) x` is ambiguous because `(fun _ => (5:ℝ))` could be `BCFn` via coercion? The error is explicit: expected `BCFn 3`. So this `heatOperator` in scope is the one taking BCFn. Which one is in scope? I opened `Poincare.L3.HeatTimeDeriv`, `Poincare.D12.ParabolicLocal`. `heatOperator` is defined in `Poincare.D12.HeatSemigroup` — not opened! So how is `heatOperator` resolved at all? Via `open Poincare.L3.HeatTimeDeriv`? No... Hmm, maybe `heatOperator` here resolves to something else in scope, e.g. a local `heatOperator` in `Poincare.D12.ParabolicLocal`? Let me grep for `def heatOperator` across the package.

Earlier, Basic.lean's `hasDerivAt_heatOperator` uses `heatOperator n s f x` where `f : EuclideanSpace ℝ (Fin n) → ℝ` — and Basic.lean has `open Poincare.D12.HeatSemigroup`. So `Poincare.D12.HeatSemigroup.heatOperator` takes a raw function.

In my probe, I didn't open `Poincare.D12.HeatSemigroup`, so `heatOperator` should be unknown... but the error indicates it resolved to something expecting BCFn. Possibly `Poincare.D12.ParabolicLocal` has a `heatOperator` (taking BCFn)! Since I opened that namespace. Let me check with grep.

Let me grep `def heatOperator` and `abbrev heatOperator`.
dsh: reasoning:
Right: there are two `heatOperator` definitions — `Poincare.D12.HeatSemigroup.heatOperator` (raw function) and `Poincare.D12.ParabolicLocal.heatOperator` (BCFn linear map). In my probe, opening `Poincare.D12.ParabolicLocal` made `heatOperator` resolve to the latter. I need to qualify: `Poincare.D12.HeatSemigroup.heatOperator`.

Note this ambiguity is worth mentioning in the review: the L3 files `open Poincare.D12.HeatSemigroup` and `open Poincare.D12.ParabolicLocal`; in `ClassicalBridge.lean` they use `heatConv` (ParabolicLocal) and in `Basic.lean` `heatOperator` (HeatSemigroup). Since `Poincare.D12.ParabolicLocal.heatOperator` also exists, the unqualified `heatOperator` in L3 files could be ambiguous — but Lean resolves by namespace order/priority (later opens have priority). In `Basic.lean`, they open D10, D11, D12.HeatSemigroup (line 55) — and not ParabolicLocal — so `heatOperator` = HeatSemigroup's. In `ClassicalBridge.lean` they open all four: D10, D11, D12.HeatSemigroup, D12.ParabolicLocal — and in `ClassicalBridge.lean` they don't use bare `heatOperator`, only `heatConv`. In `UniformBridge.lean`, `timeDerivIntegral_eq_translate` etc. use `timeDerivKernel` — fine. In `BanachDeriv.lean` — `heatConv`. So no ambiguity issues in the artifact. But `Basic.lean`'s `hasDerivAt_heatOperator` is about `D12.HeatSemigroup.heatOperator` (raw function), consistent.

Wait, in Basic.lean: `heatOperator_const` uses `heatOperator n t (fun _ => c) x` with `open Poincare.D12.HeatSemigroup` — correct.

Let me fix my probe: qualify with `Poincare.D12.HeatSemigroup.heatOperator` and open `Poincare.D10.HeatKernelEuclidean` for `gaussianKernel`.

Also note: `integral_timeDerivKernel_mul_const 0 ht x` for n=0: `EuclideanSpace ℝ (Fin 0)` is a single point; fine.

Let me fix and re-run.
dsh: reasoning:
The semantics probe compiles cleanly (exit 0), with all constructed inputs and consumers. 

Now:
1. Copy and run the negative control.
2. Write my own negative control against my audit predicate.
3. Analyze the census for semantic classes.
4. Check the extra 43 constants (auxiliaries).
5. Verify hash list vs. parent.
6. Check for forbidden tokens independently.
7. Check `All.lean` imports and that nothing else in the package is affected.
8. Verify no external consumers (done).

Let me do the negative controls.
dsh: reasoning:
Both negative controls fail as expected (exit 1, naming the bad axiom). The fail-closed predicate is verified non-vacuous.

Now the independent forbidden-token scan. I'll write my own Python scanner (different implementation than parent's) that:
- Scans the 6 L3 files plus verifies there are exactly 6 `.lean` files under `Poincare/L3/`.
- Strips nested comments/strings (I'll implement a tokenizer similar but independent), and also reports raw-text hits for manual review.
- Checks the specific forbidden tokens: sorry, axiom, admit, unsafe, native_decide, proof_wanted.
- Also check for `partial`, `opaque`, `set_option` tricks? The acceptance lists the six tokens; I'll also report `partial` and `opaque` as informational.

Additionally, check compiled environment: my audit found no AXIOM/UNSAFE-DEF kinds and no sorryAx, which is stronger than a text scan.

Let me write the scanner as a small script in my review dir and run it over the copied package.

Also let me verify the source hashes of *all* files in the L3 tree and the consumed snapshot files (D10/D12), and record them. For the snapshot consumed declarations, record:
- `Poincare/D12/ParabolicLocal/Obligations.lean`
- `Poincare/D12/ParabolicLocal/GaussianConv.lean`
- `Poincare/D12/ParabolicLocal/GaussianSemigroup.lean`
- `Poincare/D10/HeatKernelEuclidean.lean` (find actual filename)
- `Poincare/D12/HeatSemigroup/*.lean` (Basic, StrongContinuityL1, Smoothing, ...)

Let me find the files defining the 9 snapshot declarations:
- `Poincare.D12.ParabolicLocal.mildToClassicalBridge` → Obligations.lean
- `heatConv`, `heatConv_apply` → GaussianConv.lean
- `heatConv_tendsto_self_BUC` → GaussianSemigroup.lean
- `Poincare.D10.HeatKernelEuclidean.hasDerivAt_gaussianKernel`, `laplacian_gaussianKernel` → file?
- `Poincare.D12.HeatSemigroup.heatOperator`, `gaussianKernel_interval_bound{,_le}` → files?

Let me grep for those definitions to map to files. Then hash them.

Also should verify: do the copied sources match the *parent's* sources byte-for-byte for these snapshot files (they should, since I copied the whole release dir). And do the parent's release sources match the D12/D10 artifacts from their own accepted worktrees? That's a deeper provenance question. The parent's D6 release contains the promoted D1-D4 modules... Actually the L3 release package includes all Poincare modules. The provenance chain: D5 clean rebuild → D6 weekly release → L3 leader worktree added L3 files and (possibly) regenerated D12/D13 files? The L3 card says the release package is that of D6 plus L3. Since L3's `Basic.lean` "repaired" only L3 files, D10/D12 files should be the D6-promoted ones. I can verify the D12/D10 files' hashes against the D6 worktree's copies (in my own worktree's `release/` or in the D6_weekly_release worktree). My worktree (SEMREV) has its own `release/` from the D6 template! Let me compare hashes of D10/D12 files between my worktree's release and my copied review/release. That verifies the snapshot provenance. Also compare against the parent L3 release.

Let me do that now.

Actually, my worktree's release seems to lack L3 (correct) and to be a D6 package. Let me check whether the D12/D10 files are identical between:
(a) my worktree's `release/Poincare/D12/...` (D6 baseline), and
(b) `review/release/Poincare/D12/...` (L3's package).

If identical → L3 did not modify the consumed snapshot modules (only added L3 files). That's a strong provenance statement.

Note: my worktree's release might not include the same file set (D6 release has 58 promoted modules + base...). Let me check if Poincare/D12 exists in my worktree.
dsh: reasoning:
My worktree's `release/` is the D6 weekly release package which has only D1-D6 modules (Stage1, Stage6, Longrun, Basic) — not D10-D13. So I can't compare D10/D12 against a local baseline. But I can compare against the *leaders* directories for D10/D12 worktrees (D10-heat-kernel-euclidean, D12-parabolic-local-existence etc. are symlinks to /data1/... offload paths). Those are other worktrees that may contain the accepted D10/D12 packages. The L3 release package should be the D6 package + D7-D13 additions promoted by later work.

Actually, let me check the D13-critical-path-review worktree's release, which should be a superset (D1-D13 + L3?). The L3 leader release was copied from... Let me simply compare the D10/D12 source hashes between:
- review/release (L3's release)
- worktrees/D13-critical-path-review/release (D13 leader, if it exists)
- worktrees/D12-parabolic-local-existence (symlink)

This is a useful provenance cross-check, though not strictly required by the acceptance ("record source hashes and exact semantic class per declaration"). I'll do a lightweight version: compare the hash of the consumed snapshot files between L3's release and D13-critical-path-review's release (if present), and record any differences.

Let me check what exists.
dsh: reasoning:
Both D13-critical-path-review and D12-parabolic-local-existence have release packages. Importantly, D13-critical-path-review's release has no L3 directory (it's the D13 leader, pre-L3). Let me compare the consumed D10/D12 files' hashes between:
- review/release (L3's)
- D13-critical-path-review/release
- D12-parabolic-local-existence/release

If L3's D12/D10 files are identical to D13's/D12's, then L3 only added its own files.

Let me do a systematic hash comparison of the D10/D11/D12 trees.
dsh: reasoning:
Excellent provenance result:
- The L3 package's D10/D11/D12 files (76 files) are a strict subset of D13's (181) with **zero differing hashes**. So the L3 package's D10-D12 modules are byte-identical to the D13 leader's release copies (which is a later/parallel leader package). 
- vs the D12-parabolic-local-existence package (46 files): 30 files only in L3 (D11/D12 files added later), 0 only in D12 package, 0 differing. So L3's D10-D12 files are byte-identical to the D12 worktree's accepted package for the overlapping 46.

This is strong evidence that L3 did not modify the consumed snapshot modules; the consumed declarations come from the previously accepted D10/D12 packages.

Now let me also check that the L3 release's D13 modules match D13-critical-path-review — not needed for our audit, but the L3 card's U8/U12 claims reference D13. Optional.

Next: independent forbidden-token scan. Let me write the scanner script in my review dir (tools/semrev_check.py) — it will:
1. Verify the L3 file set is exactly 6 files.
2. Hash each; compare to the parent claim (authored-hashes.txt).
3. Comment/string-aware forbidden scan.
4. Raw-text scan (report hits with context).
5. Verify no other `.lean` file under Poincare/L3.

Then produce JSON evidence.

Let me write it.
dsh: reasoning:
The scan itself works; the "hash mismatch" is a path-prefix issue: the parent's authored-hashes.txt uses paths like `release/Poincare/L3/...` while my records use `Poincare/L3/...`. Let me normalize the parent paths by stripping a leading `release/`. Also the raw-text hits are expected (docstrings say "no `sorry`, `axiom`, ..."), which is fine — the code-level scan is what matters, and it's clean. I'll adjust the script to normalize and to keep raw hits informational (not part of ok).

Let me fix.
dsh: reasoning:
My script's print JSON doesn't include 'files' (I print a subset). The pipe consumed the summary. Let me just print the summary and check the JSON file.
dsh: reasoning:
All 6 authored files hash-match the parent's claim; the code-level forbidden scan is clean (only docstring mentions in raw text). The `ok: false` is only because the parent's hash file also lists `lake-manifest.json` and `lean-toolchain`, which I excluded from the L3-file comparison. Let me handle those two pins in the script (check them as pins, not L3 files). Quick fix: treat non-`Poincare/` entries as pins and verify separately.

Let me update the script to record pins and set ok accordingly.
dsh: reasoning:
The scan now passes (exit 0, ok=True). The printed summary doesn't include pins (my print filter excludes it), but the JSON has them.

Now let me analyze the census log to build the semantic class table. Let me extract the 85 L3 declarations with types, and identify the parent's 42.

Also, I should verify the claim "51 audited declarations" in the parent's audit — my enumeration found 85 L3 constants + 9 snapshot. The parent audited 42 authored (a curated list of the "real" declarations, excluding auto-generated auxiliaries). Let me list the extra 43 and confirm they're auxiliaries (e.g., `_proof_1`, `_unary`, `mk`, `rec`, `T`, `T_pos`, `u`, `u₀`, `initial`, `isSolution`, `timeDerivBCF._proof_*`, etc.).

Let me parse the census log.
dsh: reasoning:
The census (85 L3 constants) includes the 42 parent-listed authored declarations plus 43 auto-generated auxiliaries (structure projections, `eq_1` lemmas, `_proof_*`, `_simp_*`, `mk`, `rec`, `casesOn`, etc.). All 85 have approved cones (from the audit).

The 9 "missing" are the snapshot declarations, which my audit covers separately. Good.

So my independent audit is a strict superset: 85 + 9 = 94 vs parent's 51.

Now let me build the semantic class table for the *cited* declarations. The card cites:
1. hasDerivAt_heatOperator
2. hasDerivAt_heatOperator_kernelLaplacian
3. integral_timeDerivKernel_mul_const
4. mildToClassicalBridge_holds
5. uniformMildToClassicalBridge_holds
6. hasDerivAt_heatConv_BCF
7. heatConv_classicalHeatSolution
8. SpatialLaplacianBridge (statement-only)
9. heatConv_tendsto_self_BUC (snapshot)
10. hasFDerivAt_heatOperator (D12 spatial smoothing — cited in text as existing)
11. hasDerivAt_gaussianKernel, laplacian_gaussianKernel (D10 snapshot)
12. heatConv_apply (D12 snapshot)
13. timeDerivBCF, timeDerivBCF_apply
14. mildToClassicalBridge_pointwise, mildToClassicalBridge_const
15. mildToClassicalBridge_of_uniform
16. UniformMildToClassicalBridge (proved now)
17. KernelClassicalHeatSolution

Let me extract the exact types of the cited ones from the census log for the card. I'll produce a compact table.

Let me also now double-check a few semantic issues that could be findings:

Finding candidates:
(F1) **D12 docstring says "in the Banach space of functions"** but the actual ambient space is the product topology (TVS), not the sup-norm Banach space. L3 correctly identifies and strengthens; the parent card's §0 claim 2 says "the D12 named obligation is discharged" (true) and describes it as pointwise elsewhere. The parent card's §2 item 4 says "the Pi-valued orbit". OK. But the parent card §0 claim 3 says the uniform strengthening is proved; yes. The card's title for the discharge could mislead a reader into thinking the Banach-space obligation was discharged; the card is careful in §2 and §8. Review note: the D12 obligation is *formally* discharged, but its docstring's "Banach space" phrase is inaccurate; the card's semantic classification ("pointwise") is the correct reading. Not a blocker; a documentation finding.

(F2) **No downstream consumer**: `mildToClassicalBridge_holds` is not consumed by D12/D13 (only `#check` type ascriptions). The parent card §4 lists "downstream use | Audit.lean #check probes" — accurate but weak. The acceptance criterion "no blocker closure without constructed input and consumer" — no blocker was closed, so no violation; but for the *sub-obligation* discharge, the reviewer should note there is no consumer beyond type ascription. My probe demonstrates the theorem is consumable with constructed inputs. Classification: the discharge is a genuine theorem about constructed data, but it is *unconsumed* in the package. Worth stating as an evidence limitation (not a blocker).

(F3) **`KernelClassicalHeatSolution` is a misnomer w.r.t. the classical heat equation**: its `isSolution` field asserts the kernel-Laplacian pairing, not `∂ₜu = Δu`. The card says "kernel-classical" and flags the spatial residual. But the *type name* could mislead. Note: the card's §0 claim 5 says "packaged kernel-classical heat solution" — accurate within its own definition. I'll record the exact semantic class.

(F4) **`SpatialLaplacianBridge` is statement-only** — confirmed (no inhabitant; only the `def`). The card says so. Also note: the card's claim 6 says "Two residuals are stated, not assumed" — `SpatialLaplacianBridge` and the Duhamel F-term; but `UniformMildToClassicalBridge` is no longer a residual (it's proved). In §0 claim 6 it says "Two residuals are stated" — the two named are SpatialLaplacianBridge and the Leibniz rule (described, not formalized as a Prop). Hmm: is that accurate? Let me re-read claim 6: "**Two residuals are stated, not assumed:** `SpatialLaplacianBridge` (the second spatial derivative under the integral, ...) and the Leibniz rule for the Duhamel `F`-term (described precisely in §5)." OK: one is a formal Prop; the other is described in prose only (not a Lean declaration). That's accurate but the second residual is not machine-checkable in the artifact. Note.

(F5) **U6 classification**: card says U6 open with residual (c) "continuum parabolic maximum principle: untouched". The blocker U6 text says "No heat-equation theory, heat kernel, or parabolic PDE layer; the continuous parabolic maximum principle cannot be proved and is a statement-only interface". After L3, the Euclidean heat-equation time-derivative layer exists. Blocker remains open (manifold layer + maximum principle). Conservative and correct. But there's a nuance: L3's discharge addresses part of the *D12 sub-obligation*, not U6 directly. The card says U6 "open" and lists what changed. Good.

(F6) **U8 classification**: card says "nothing proved"; but claim 2 in §2 for U8 says "the flat model's linear heat part is now differentiable as a Banach-space curve, which removes one packaging obstacle". The `MildClassicalOutput` hypothesis in D13 `PicardModel` is about `DeTurckParabolicProblem.IsDeTurckSolutionOn` — not the same as the L3 BCFn theorem; L3's `hasDerivAt_heatConv_BCF` is the semilinear flat heat semigroup derivative. So the claim "removes one packaging obstacle for the Duhamel upgrade" is an informal relevance statement; there is no formal connection in the package between `hasDerivAt_heatConv_BCF` and `MildClassicalOutput`. That's a "classification caution": U8 remains open (correct), but the connection is not formalized. Fine.

(F7) **U12 classification**: untouched. Verify: L3 files contain no backward/conjugate heat or monotonicity. Grep for "conjugate", "backward", "Perelman", "monoton" in L3 → none. Good.

(F8) The card's §4 trust table says "literal cones: #print axioms on all 42 authored declarations — only the three standard axioms". My audit confirms all 85 (+9 snapshot) — a superset. Good.

(F9) **Derivative domains**: 
   - `hasDerivAt_heatOperator` at t>0, f a.e. strongly measurable and bounded; domination on (t/2,3t/2) — correct neighborhood.
   - `hasDerivAt_heatConv_BCF` at t>0 in BCFn.
   - No statement at t=0 (correct, singular).
   - `KernelClassicalHeatSolution` slab (0,T) with T=1.
   - `SpatialLaplacianBridge` at t>0.
   All domains are positive-time; no claim extends to t≤0. One caveat: `heatConv n t f` is defined as 0 for t ≤ 0; the "orbit" is discontinuous at 0 in general and statements are only for t>0. The `initial` field uses one-sided limit `𝓝[>] 0`. Fine.

(F10) **Euclidean scope**: L3 files mention only `EuclideanSpace ℝ (Fin n)`, `volume`, D10 Gaussian kernel. Let me grep for manifold/Ricci terms to confirm. Also the docstrings explicitly say Euclidean model. Good.

(F11) Check that the parent card's hash `da970151...` for lakefile.toml isn't in authored-hashes; not needed.

(F12) The parent card's claim "9239 jobs" for the full build vs my fresh rebuild of 8915 jobs for the cone — both fine; 9239 was the parent's full package build. My independent full `lake build` of the entire package (not just the cone) hasn't been run in my copy. Should I? The acceptance says "compile/read all cited declarations". I rebuilt the cone fresh (82 modules) and compiled my probes. A full package `lake build` would be a stronger check but takes longer (the parent's full build of 356 files). Since my copy's `.lake/build` contains parent-built oleans for the rest, a full build would be mostly cache-hit (fast) — but cache hits aren't independent compiles. The cited declarations are all in the cone I rebuilt, so I have genuine compile checks for every cited declaration. Additionally, I can force a full rebuild of the *entire* Poincare lib in the background to be thorough; with 192 cores and mathlib cached, 356 files may take ~5-10 min. That would let me state "full package rebuilt from source in the review worktree". Let me do it in the background — it's affordable and strengthens the review. But careful: it could invalidate my fresh L3 oleans? No, rebuild would just recompile everything (or reuse). To force, delete all oleans under `.lake/build/lib/lean/Poincare` and rebuild `Poincare` + drivers. Actually `lake build` builds all defaultTargets including Audit/ReleaseCheck/ReleaseAudit/D6AuditReport; the release package has Audit/, Probe/, Ledger/, etc. Let me just delete all Poincare oleans and run `lake build` (all targets) in the background. Risk: it may take a long while and consume CPU. The parent's full build was 9239 jobs (including mathlib replay?). Since mathlib oleans exist in the shared packages dir, only the local 356 files + drivers compile. Let me estimate: 82 files took 85s with 192 cores. 356 files → ~6 min. Acceptable.

Actually, wait. Is it wise? The main risk is time/CPU contention. We have 4 hours. Let's do it in the background after I finish the key analysis, and meanwhile write the card.

Actually, let me think about what else might be needed:
- The acceptance mentions "compile/read all cited declarations". I've compiled the cone and the probes. I should also run a *per-file* gate on all L3 files (each file compiled standalone via `lake env lean`) — the parent did this for all 356; I can do it for the L3 files (6) quickly. Plus my probe files.
- I should verify the `All.lean` aggregate imports all modules and that `Audit.lean` (the parent's audit driver) itself compiles and PASSes in my copy. Let me run `lake build Poincare.L3.HeatTimeDeriv.Audit` — this compiles the parent's audit driver freshly and should print the PASS message. That's an independent re-run of the parent's own gate. Let me do that now.

Let me run these two now:
1. `lake build Poincare.L3.HeatTimeDeriv.Audit` → capture output; check PASS 51.
2. per-file `lake env lean` on the 6 L3 files.

Then background full rebuild.

Also: the parent's Audit.lean line 191-193 logs "PASS — all 51 audited declarations...". My separate audit found 94. Both consistent.

Let me run.
dsh: reasoning:
The parent's audit driver was freshly compiled (in my copy) and PASSed with 51 declarations. 73 "depends on axioms" lines (from the #print axioms transcripts).

Now, per-file gate on the 6 L3 files standalone: `lake env lean Poincare/L3/HeatTimeDeriv/X.lean` for each. Note Basic.lean etc. should compile standalone.

Let me run them, and also start the full package rebuild in the background.

For the full rebuild: delete all oleans under `.lake/build/lib/lean/Poincare` (and the driver modules Audit/, Probe/, Ledger/, ReleaseCheck, ReleaseAudit, D6AuditReport, D6LedgerProbe) and `lake build`. Let me kick that off in the background with a log. Note: it will recompile L3 too (fine — same sources). This may take several minutes.

Wait — one concern: `lake build` with default targets includes `Poincare`, `Probe`, `Ledger`, `Audit`, `ReleaseCheck`, `ReleaseAudit`, `D6AuditReport`. The last ones are D6 drivers. If any of them fails (e.g., D6LedgerProbe references declarations that changed?), the build fails — but the parent's full build passed, and I haven't changed sources. Fine.

Let me start the full rebuild now (background), then do the per-file gate on L3 (quick, but it will contend with the rebuild... the rebuild uses all cores; the per-file gate is 6 files, fine).

Actually, to avoid confusing interference, run per-file gate first (fast, ~30s), then start full rebuild in background.

Let me do the per-file gate.
dsh: reasoning:
All 6 L3 files compile standalone (exit 0). Audit.lean produces 122 lines (the #print axioms transcript + PASS).

Now start the full package rebuild in the background. I'll delete all oleans under the build dir for local modules (Poincare, Audit, Probe, Ledger, and the root drivers), then `lake build`.

Actually, to be careful about time, maybe I don't need to force-rebuild *everything*: the acceptance is about the L3 cited declarations, which I've freshly compiled. A full rebuild is "nice to have". Given 192 cores and ~6 min expected, I'll do it — it also validates that the release package as a whole compiles in my independent copy.

Let me start it in the background and continue with the review card drafting.

Command:
```
cd review/release
find .lake/build/lib/lean -type f \( -name '*.olean' -o -name '*.ilean' -o -name '*.trace' -o -name '*.hash' \) -delete
lake build > ../evidence/full-rebuild.log 2>&1; echo EXIT=$?
```
Careful: `.lake/build/lib/lean` may contain only our modules (mathlib is in `.lake/packages/mathlib/.lake/build`). Yes, `.lake/build/lib/lean` is the package's own build dir. Deleting all is right.

Let me start it.
dsh: reasoning:
While full rebuild runs, let me continue the semantic analysis and draft the review card.

Let me extract exact types of the cited declarations from the census for the card. Let me write a script to pull specific declarations' types.

Also I want to double-check a few mathematical points in the proofs that could hide issues:

1. `norm_timeDerivKernel_le` bounding `|timeCoeff n u z| ≤ ‖z‖²/t² + n/t` for u ∈ (t/2, 3t/2): the `n/(2u) ≤ n/t` step uses `div_le_div_of_nonneg_left (by positivity) (by positivity) (linarith [hu.1])`. Since 2u ≥ t... wait, hu.1 : t/2 < u → t < 2u → 1/(2u) < 1/t, so n/(2u) ≤ n/t for n ≥ 0. Correct.

2. `gaussianKernel_interval_bound`: a D12 lemma (consumed). It says for u ∈ (t/2, 3t/2), K_u(z) ≤ (2πt)^{-n/2} exp(-‖z‖²/(6t)). Let me spot-check that D12 lemma's statement to make sure the L3 usage matches (it's a snapshot declaration; my audit checked axioms, not truth — but it's a proved theorem in D12, and its correctness was presumably verified by D12's own review). I could read it.

3. `hasDerivAt_integral_of_dominated_loc_of_deriv_le` usage: need `F'` to be the derivative for each y in the interior, domination integrable, etc. Fine.

4. `exists_slope_eq_timeDerivIntegral`: uses `exists_hasDerivAt_eq_slope` (MVT). Fine.

5. `hasDerivAt_heatConv_BCF`: uses `dist_eq_norm` + `BoundedContinuousFunction.norm_le` with ε/2; fine.

6. Check `timeDerivBCF` construction: `BoundedContinuousFunction.ofNormedAddCommGroup f hcont C hbound`. Fine.

Potential semantic subtlety in `timeDerivBCF`: the bound `‖f.val‖ * ∫ timeDerivBound n t 1` — but `timeDerivBound` depends on `z` and the integral could be `0`? No issue.

7. In `UniformBridge.uniformMildToClassicalBridge_holds`: the choice `M := ‖f.val‖`, `ε/(M+1)`. Fine.

8. `mildToClassicalBridge_of_uniform`: uses `hasDerivAt_iff_tendsto_slope_zero`. The statement direction: uniform ⇒ pointwise. Fine.

Now, one thing I should double check is `hasDerivAt_heatConv_apply`'s use of `HasDerivAt.congr_of_eventuallyEq`: The theorem `hasDerivAt_heatOperator` gives derivative of `s ↦ heatOperator n s f.val x`; then congr to `s ↦ heatConv n s f.val x` on `Ioi 0` (neighborhood of t). Since HasDerivAt is a `HasDerivAtFilter` w.r.t. `𝓝 t`, eventual equality on a neighborhood suffices. Correct.

Another check: in `Basic.lean`, `hasDerivAt_heatOperator`'s `hmain.2` — `hasDerivAt_integral_of_dominated_loc_of_deriv_le` returns a pair `(measurability, HasDerivAt)`. Fine.

Now, an important *non-vacuity* question for `hasDerivAt_heatOperator`: the hypothesis `hfm : AEStronglyMeasurable f volume` and `∀ y, ‖f y‖ ≤ M`. If M=0 then f=0. Fine. Constants satisfy. The theorem is non-vacuous: for constant f, derivative = 0 (proved separately via uniqueness). 

Now `mildToClassicalBridge_holds n` for all n including n=0: `BUCn 0` = BUC functions on the one-point space. Inhabited (f0 in my probe). Fine.

Now, let me also verify the claim in the card §2 item 2: "`hasDerivAt_heatOperator_kernelLaplacian`: the same derivative as `∫ ΔK_t(x-y) f(y) dy` (D10 identity ∂ₜK = ΔK)". `laplacian_gaussianKernel n ht z : Δ (gaussianKernel n t) z = gaussianKernel n t z * timeCoeff n t z`? Let me check the exact statement to ensure the sign/convention matches. Let me read D10's `laplacian_gaussianKernel`.

Let me find and read it.
dsh: reasoning:
Good — the D10 kernel identities are genuine and match the L3 usage (∂ₜK = K·c = ΔK with c = ‖x‖²/(4t²) − n/(2t)).

Now let me look at the census types for the key cited declarations to include exact type signatures in the card. Let me extract them.

Also, I should check the card's claim about "the D12 strong-continuity theorem as the initial condition": `heatConv_tendsto_self_BUC n f : Tendsto (heatConv n · f) (𝓝[>] 0) (𝓝 f)` — let me confirm the exact statement from the census (snapshot declaration). Let me grep the census log for it... it's not in the census (only L3 constants). Let me `#check` the snapshot ones in a small probe or just read GaussianSemigroup.lean line 331.

Let me read that.
dsh: reasoning:
I now have the exact types for all cited declarations. 

Notice `hasDerivAt_heatConv_apply_kernelLaplacian`'s type prints `Laplacian.laplacian (gaussianKernel n t) (x-y)`, i.e. `Δ` is the mathlib Laplacian.

Now let me check the full rebuild progress.

Meanwhile, let me consider whether there are any *genuine mathematical blockers* to report. The task says: "Report a genuine mathematical blocker rather than claiming completion with a weaker theorem." That's about not overclaiming. The L3 artifact is honest: no blocker closed. My review should not claim any blocker closed either.

The verdict of my review: I expect TASK_DONE (review completed, artifact's claims verified as accurate with noted limitations). The review card itself must end TASK_DONE or TASK_BLOCKED. TASK_DONE = review complete. TASK_BLOCKED would be if I could not complete the review (e.g., couldn't compile). I can complete it, so TASK_DONE, with the caveat that the *reviewed artifact* is not a Poincaré proof.

But careful: the instruction says "Report a genuine mathematical blocker rather than claiming completion with a weaker theorem." This is about the review's honesty: if the L3 artifact's central claim were false/weaker, I should report a blocker. I've found no false central claim — the theorem is genuinely proved for the Euclidean model and correctly classified. Limitations are documented. So TASK_DONE with findings.

Let me now check whether there are any semantic *errors* in the card:

1. Card §0 claim 1: "It now proves the time derivative ... for a.e.-strongly-measurable bounded data, with an explicit integrable domination on (t/2, 3t/2), the kernel-Laplacian form, and constant-data non-vacuity." ✓.

2. Claim 2: "The D12 named obligation is discharged: `mildToClassicalBridge_holds (n) : mildToClassicalBridge n` for every n. The type ascription against the D12 name is checked in Audit.lean." ✓ (my audit and probe confirm).

3. Claim 3: uniform ✓.

4. Claim 4: BCFn-valued HasDerivAt ✓.

5. Claim 5: every BUC datum → packed kernel-classical solution on (0,1) with D12 strong-continuity as initial condition ✓ (T=1, initial = heatConv_tendsto_self_BUC, isSolution = kernel-Laplacian form).

6. Claim 6: two residuals stated, not assumed ✓ — but note `UniformMildToClassicalBridge` is now proved, and the second residual (Duhamel F-term Leibniz rule) is *described only*, not formalized as a Prop. The card says "described precisely in §5" — accurate.

7. Claim 7: fail-closed axiom audit 51 declarations ✓ (my superset: 94).

Now, "§2 item 4: mildToClassicalBridge_holds: for f : BUCn n, the Pi-valued orbit s ↦ (fun x => (K_s * f)(x)) is differentiable at t with derivative the integral above — exactly Poincare.D12.ParabolicLocal.mildToClassicalBridge n." ✓.

"§2 item 3: integral_timeDerivKernel_mul_const: ∫ K_t(x-y) c_t(x-y) c = 0 — the formula is consistent with mass one (non-vacuity)." ✓.

"§3 table: U6 open ... (b) SpatialLaplacianBridge: statement-only" ✓.

"§4 trust table: 'literal cones: #print axioms on all 42 authored declarations — only the three standard axioms (transcript in the build log)'" ✓ (73 depends-on-axioms lines in my fresh build log; 42 + 31? — the parent's #print axioms list has 51 entries; the log had 73 "depends on axioms" lines because the Audit module's `#print axioms` also prints for the type ascription constants? Whatever, consistent.)

"§4 per-file gate: 356/356 .lean files exit 0" — the parent's evidence; I independently gated the 6 L3 files. ✓.

Now some deeper questions about the math I should verify as an independent reviewer:

(a) Is `hasDerivAt_heatOperator`'s domination argument airtight? The theorem `hasDerivAt_integral_of_dominated_loc_of_deriv_le` requires:
- `∀ᶠ u in 𝓝[t], AEStronglyMeasurable (F u) μ` ✓ (they give for u ∈ Ioo).
- `Integrable (bound)` ✓.
- `∀ᶠ y in μ, ∀ u ∈ s, ‖F' u y‖ ≤ bound y` ✓.
- `∀ᶠ y in μ, HasDerivAt (F · y) (F' t y) t` — they use `hasDerivAt_gaussianKernel n hu_pos (x-y)` where the `u` from the filter is inside the set; note the *derivative is taken at the point u*, not at t. Let me look at the exact signature of `hasDerivAt_integral_of_dominated_loc_of_deriv_le`. It typically requires `∀ᶠ y in μ, ∀ u ∈ s, HasDerivAt (F · y) (F' u y) u` (derivative at each u). Looking at the L3 proof: the last goal is `∀ᶠ y in μ, ∀ u ∈ s, HasDerivAt (fun u => F u y) (F' u y) u` (line 263-266: `filter_upwards with y u hu` then `hasDerivAt_gaussianKernel n hu_pos (x - y)`), giving derivative at u. ✓.

- The first argument of the returned pair is AEStronglyMeasurable of the limit function; they use `hmain.2`. ✓.

(b) Does `integrable_timeDerivBound` correctly dominate `timeDerivBound` by `6^{n/2} * gaussianKernel n (3t) z * (‖z‖²/t² + n/t) * M`? They use `gaussianKernel_interval_bound_le n ht z`, a D12 lemma. Let me check its statement to make sure it's `(2πt)^{-n/2} exp(-‖z‖²/(6t)) ≤ 6^{n/2} K_{3t}(z)`. Let me read it.

(c) `timeDerivBound_nonneg` etc. fine.

(d) In `UniformBridge.tendsto_integral_abs_timeDerivKernel_sub`: uses dominated convergence with `f := fun _ => 0` and pointwise convergence `timeDerivKernel n u z → timeDerivKernel n t z` (continuity at t). ✓.

(e) `abs_timeDerivIntegral_sub_le`: uses `integral_sub` and `integral_mono_of_nonneg`. Fine.

(f) `exists_slope_eq_timeDerivIntegral`: MVT. Fine.

(g) `uniformMildToClassicalBridge_holds`: choose δ = min(t/2, ρ). For |h| < δ: MVT gives ξ with |ξ-t|<|h|; then `hsmall` via L¹ continuity; final bound M·ε/(M+1) < ε. ✓.

(h) `hasDerivAt_heatConv_BCF`: uses `hasDerivAt_iff_tendsto_slope_zero`; gets uniform hpoint bound < ε/2 at each x, then sup norm ≤ ε/2 < ε. ✓.

Wait — subtle: `timeDerivBCF` vs the slope: `h⁻¹ • (heatConv n (t+h) f.val - heatConv n t f.val) - timeDerivBCF n ht f`. The uniform bridge statement gives `|(heatConv n (t+h) f.val x - heatConv n t f.val x)/h - timeDerivIntegral n t f.val x| < ε/2`. In the proof, they `simpa [BoundedContinuousFunction.coe_sub, coe_smul, Pi.sub_apply, Pi.smul_apply, smul_eq_mul, div_eq_inv_mul, Real.norm_eq_abs] using hx`. Fine.

(i) `timeDerivBCF` uses `BoundedContinuousFunction.ofNormedAddCommGroup`. Need to check the signature: `ofNormedAddCommGroup (f : α → β) (hf : Continuous f) (C : ℝ) (hC : ∀ x, ‖f x‖ ≤ C) : α →ᵇ β`. ✓.

(j) One more: is `heatConv`'s truncation at t ≤ 0 an issue for `HasDerivAt` at t>0? No, local.

So the math looks sound. The main review findings are classification/consumer/documentation issues, none of which invalidate the artifact.

Let me now check the full rebuild status.
dsh: reasoning:
Full rebuild running (started 16:16 UTC). Let me continue while waiting.

Let me now verify the "no blocker closure" and exact U6/U8/U12 classification against the current queue/manifest and the artifact. Also verify that the child tasks proposed are consistent.

Also, I should write the first hourly checkpoint (per the task instruction: "Save a compile-checked checkpoint at least every hour"). It's been ~20 minutes. I'll write a checkpoint after the full rebuild completes, or now with what's compiled. The checkpoint should go in my worktree — where? The task says "Continue from existing files and checkpoint.json in this worktree". There's no checkpoint.json in my worktree; I'll create one at the worktree root (`checkpoint.json`) documenting the review state, and update it hourly. Let me create it now with the current verified facts, then update after the full rebuild.

Also, I should write an intermediate evidence manifest.

Let me draft the checkpoint JSON now.
dsh: reasoning:
Checkpoint created (will be updated hourly).

Now let me examine remaining semantic verification tasks:

1. Verify the parent card's statement "all 356 `.lean` files exit 0" — my full rebuild covers this.

2. Verify that `All.lean` is imported by `Audit.lean` and that `Audit.lean` is not imported anywhere (leaf) — checked.

3. Check for any *statement-shape* triviality in the L3 theorems: e.g., a theorem whose conclusion is one of its hypotheses. The D13 audit had a statement-shape scan. For L3, the main theorems are non-trivial (integrals, HasDerivAt). I can do a quick manual check.

4. Check the "derivative domains" more carefully:
   - `hasDerivAt_heatOperator`: domain t > 0 only. ✓
   - The uniform bridge: t > 0, h with 0<|h|<δ. ✓
   - `exists_slope_eq_timeDerivIntegral`: requires |h| < t/2 so t+h > 0. ✓
   - `hasDerivAt_heatConv_BCF`: t>0. ✓
   - `KernelClassicalHeatSolution`: T>0, t ∈ Ioo 0 T. ✓
   - `SpatialLaplacianBridge`: t>0. ✓
   - No claim at t=0. ✓

5. Verify `heatConv` truncation semantics: `heatConv n t f = 0` for t ≤ 0. Does any card claim depend on values at t ≤ 0? The `KernelClassicalHeatSolution.u = fun s => heatConv n s f.val` has u(s)=0 for s≤0, and initial is one-sided. Fine. But note: the structure's `initial : Tendsto u (𝓝[>] 0) (𝓝 u₀)` is the right-limit only. Documented as "initial datum is attained in the supremum norm" — technically the one-sided limit; the parent's `heatConv_tendsto_self_BUC` is stated with 𝓝[>] 0. Accurate enough; I'll note the one-sidedness.

6. One more check on the claim "for **every** `n`": `mildToClassicalBridge_holds (n : ℕ)` — yes, all n including 0.

7. Check that the L3 files don't `import` anything beyond Mathlib/D10/D11/D12 (scope check): imports are D12.HeatSemigroup.Smoothing, StrongContinuityL1, D11.HeatKernelBridge.InitialCondition, D12.ParabolicLocal.Obligations. All Euclidean/model-level. ✓.

8. "axiom cones": all audited declarations have cone either [propext, Classical.choice, Quot.sound] (85) or [propext] (9 snapshot). Both within the approved set. ✓. Note: some declarations have *empty* cones in the parent's report? The parent's README mentions cones `{}`, `{propext}`, `{propext, Quot.sound}`, `{propext, Classical.choice, Quot.sound}` for the whole D6 package. For L3, my census shows 9 with `[propext]` — wait, the 9 are the snapshot declarations; but the L3 85 all have the full three. Let me double check whether any L3 declaration has an empty cone. From my summary: 85 with [propext,Classical.choice,Quot.sound], 9 with [propext]. So none empty. Fine.

Hmm wait, that's a bit odd: `timeCoeff` is a plain def with no proofs; collectAxioms of a def with a computable body should be empty unless the body uses Classical.choice. `timeCoeff` uses division on ℝ which uses Classical.choice? Division in a field is noncomputable and uses `Classical.choice`? Yes, `inv` is noncomputable, defined with `Classical.choice` for the field inverse (in Lean's `Inv` instance for ℝ it's `NNReal`-based? probably uses Classical.choice). So full cone. Fine.

9. What about the negative control: the parent's negcontrol is *outside* the release package. ✓ (I copied it into review/negcontrol, outside review/release).

10. Check that no `axiom` is declared anywhere in the release package's Poincare tree? The D6 audit claims 0 project axioms over 1619 declarations. The L3 audit checks only L3 + snapshot. Should I run a package-wide axiom audit as part of the independent review? The acceptance says "run fail-closed axiom audit (only propext, Classical.choice, Quot.sound)" — presumably over the cited declarations. I've done that, plus a superset within L3. A package-wide audit would be a bigger job (1619 declarations), but the D6 release already did it and my work focuses on L3. Since the L3 artifact *consumes* D10/D12 declarations, their cones are audited (9 snapshot decls). But their *proofs* depend on many other D-package declarations; the cone check via collectAxioms is transitive by construction (collectAxioms traverses the whole proof term), so checking the 9 snapshot declarations' cones covers the transitive axiom usage of everything they depend on. That's exactly the right closure. ✓.

Actually — the 9 snapshot declarations' cones being [propext] doesn't cover the *definitions* they use (e.g., `gaussianKernel`'s definition body might use Classical.choice, and collectAxioms for a theorem includes axioms used in the *types* and *proofs* of the constants it references; collectAxioms traverses constants in the proof term including definitions' values? `collectAxioms` collects the axioms of the declaration's type and value, traversing constants and their values recursively (it does look at the values of definitions). I believe `collectAxioms` uses `collectAxiomsCore` which visits the value of definitions too. So the 9 cones cover the transitive dependencies. Fine.

11. Let me also verify `hasDerivAt_heatConv_BCF`'s conclusion isn't vacuous because `BCFn n` might be trivial? `BCFn n = (EuclideanSpace ℝ (Fin n)) →ᵇ ℝ` is a nontrivial Banach space. Fine.

12. Non-vacuity of `UniformMildToClassicalBridge`: proved; instantiated at f3 in my probe. ✓.

13. Non-vacuity of `SpatialLaplacianBridge`: it's a Prop; no inhabitant. Note: could it be *false*? It's a true statement mathematically (the heat semigroup is smooth), but unproved in Lean. The card says statement-only. ✓.

Now, let me check the full rebuild progress and then write the card.

Actually, one more potentially important check: the L3 card's §4 says "axiom audit | `lake build Poincare.L3.HeatTimeDeriv.Audit` | exit 0, PASS 51". My fresh build confirms. ✓.

And §4 "full build | `lake build` | exit 0 — see audit-evidence/logs/lake-build.log; 9239 jobs, D6AUDIT PASS". My full rebuild is running.

Let me check the full rebuild now.
dsh: reasoning:
The full rebuild is progressing; 7010 lines of log already (it's compiling many files). Note some "info: Unknown identifier" messages from a D7 Probe file — those are inside `#check` or error-recovery? "info:" not "error:" — possibly from a `try?`/`#check` failing as info. Let me not worry; the exit code will tell.

While waiting, let me examine the remaining review dimensions:

**Semantic class per declaration**: I'll produce a table in the card for the ~25 load-bearing declarations with class labels:
- `def` data (timeCoeff, timeDerivKernel, timeDerivBound, timeDerivIntegral, timeDerivBCF)
- `theorem` proved (Euclidean model): the main results
- `def : Prop` statement-only: SpatialLaplacianBridge (no inhabitant), UniformMildToClassicalBridge (now inhabited by uniformMildToClassicalBridge_holds)
- `structure`: KernelClassicalHeatSolution (data bundle with predicates)
- snapshot consumed: D10/D12 declarations with their own provenance.

Let me also verify one specific thing in the card: "the D12 obligation is discharged ... (`mildToClassicalBridge_holds`) ... for every n". My probe checks `mildToClassicalBridge_holds 0`. ✓.

Now let me think about whether the review should flag anything as a *genuine mathematical blocker*. The task says: "Report a genuine mathematical blocker rather than claiming completion with a weaker theorem." I interpret: if I find the artifact claimed something it didn't prove, I should report that rather than paper over it. I found no such overclaim. The artifact's own limitations are accurately stated. However, there is one claim in the parent card that I should scrutinize hard:

Card §0 claim 3: "**The uniform (Banach-space) strengthening is proved:** the difference quotients converge uniformly in the space variable (`uniformMildToClassicalBridge_holds`), by L¹-continuity in time of `K_u·c_u` + a uniform difference estimate + the mean value theorem."

Is `uniformMildToClassicalBridge_holds` really a *strengthening* of `mildToClassicalBridge`? `mildToClassicalBridge_of_uniform` proves uniform ⇒ pointwise. And uniform is not implied by pointwise in general (pointwise convergence of difference quotients on a non-compact space isn't uniform). So yes, strictly stronger. But careful: both are about the same function; the uniform statement is a standard consequence of the smoothness of the heat semigroup. Proved. ✓.

Another claim: §0 claim 5 "Every BUC datum yields a packaged kernel-classical heat solution (`heatConv_classicalHeatSolution : BUCn n → KernelClassicalHeatSolution n`) on `(0,1)`". ✓.

One more: the parent card §0 claim 1 says the round-1 file "had 7 elaboration errors and had never compiled; it is repaired and completed". I cannot verify the round-1 history (no git history?). Let me check if the worktree has git. If there's a git repo, I could check the diff. Let me check `git -C parent status/log`. This is a historical claim; my review can note it as "not independently verifiable from the artifact; the final file compiles".

Let me check git.
dsh: reasoning:
No git repo in the parent worktree, so the round-1 history isn't verifiable. I'll note that.

Let me read the parent's comms outbox README to cross-check the proposed child tasks and the "superseded" claim about L3-U6a-uniform-bridge. Also the queue entry L3-U6a-uniform-bridge is `queued` with acceptance criteria; L3 claims it's already met. Let me verify the acceptance of L3-U6a-uniform-bridge:
"Compiled proof of Poincare.L3.HeatTimeDeriv.UniformMildToClassicalBridge n for all n, or a precise reduction splitting it into (i) L1-continuity ... and (ii) mean-value/FTC packaging; a downstream consumer producing HasDerivAt of s ↦ heatConv n s f in the BCFn sup norm; fail-closed axiom audit ...; no forbidden tokens; independent rebuild and semantic review before any blocker closure claim."

So the task's acceptance includes "independent rebuild and semantic review before any blocker closure claim" → that's me (this SEMREV task). The task acceptance is met by L3's artifact modulo my review. Good: my review card is the independent semantic review required by the child-task acceptance. I should note that in the card: for L3-U6a-uniform-bridge, acceptance criteria (i) compiled proof ✓, (ii) consumer producing BCFn HasDerivAt ✓ (hasDerivAt_heatConv_BCF), axiom audit ✓, no forbidden tokens ✓, independent rebuild ✓ (mine), semantic review ✓ (this card). But no blocker closure claimed (U6 remains open), so the "before any blocker closure claim" clause is moot.

Let me read the parent's comms/outbox/README.md quickly for completeness.

Also let me check the queue entry for `L3-child-conjugate-heat-euclidean` etc. — already seen.

Let me read the outbox README.
dsh: reasoning:
Consistent with the card.

Now let me do a couple more independent semantic checks while the rebuild runs:

**Check A: statement-shape / hypothesis-equal-to-conclusion scan for the L3 theorems.** I can do a lightweight version manually: the main theorems' conclusions are `HasDerivAt`, `Integrable`, `Tendsto`, `=`, etc., with hypotheses about measurability/bounds — not identical. I'll note that no L3 theorem's conclusion is syntactically one of its hypotheses (manual inspection of the 42 authored types). I have the census with all 85 types; let me do a quick automated check: for each L3 theorem, check if any hypothesis (for-all/arrow antecedent) is syntactically equal to the conclusion. Hard to automate simply; manual review suffices for the load-bearing ones. Let me at least list the 42 authored types and eyeball them.

**Check B: verify no `set_option` / `maxHeartbeats` tricks or `native_decide` in L3.** The forbidden scan found no informational hits (`set_option`, `partial`, `opaque`, `#eval`, `implemented_by`, `extern` all zero). ✓.

**Check C: verify the exact "semantic class" of each of the 42 authored declarations** — I'll classify them into:
- data definitions (non-propositional): timeCoeff, timeDerivKernel, timeDerivBound, timeDerivIntegral, timeDerivBCF, KernelClassicalHeatSolution (structure/type), heatConv_classicalHeatSolution (data constructor/def producing a structure).
- proved theorems (Euclidean model): all the rest.
- statement-only Prop definitions: SpatialLaplacianBridge (no inhabitant), UniformMildToClassicalBridge (inhabited).
- kernel-level identity wrappers.

For the card, I'll produce a per-declaration table of the *cited/load-bearing* ~30 declarations with: name, kind, semantic class, axiom cone, and consumer. The full 85-declaration census lives in `review/evidence/semrev-census.log` and the audit TSV.

**Check D: confirm no `sorryAx` anywhere in the L3 + snapshot cones.** My audit's `row` checks `axs.contains ``sorryAx` → none. ✓.

**Check E: confirm `All.lean` imports everything and nothing under Poincare/L3 is orphaned.** `All.lean` imports Basic, ClassicalBridge, UniformBridge, BanachDeriv; Audit imports All. All 6 files accounted for. ✓.

**Check F: check that the parent's negative control file is genuinely outside the release package** (so the dispatcher's gate doesn't compile it): yes, `audit-evidence/negcontrol/`. ✓.

**Check G: does any L3 declaration depend on `Classical.choice` in a way that could hide a nonconstructive existence claim?** Allowed axioms are fine; the artifact is noncomputable.

Now let me also quantify the "no external consumer" finding more rigorously: grep for the L3 names across the *entire* release package source (not just Poincare). I did: only comments and #check probes in L3's own Audit.lean. Let me also grep the D13 worktree (the critical-path review package) for uses of L3 names to see if any later package consumes them. Not necessary for this artifact review, but the acceptance mentions "downstream use". The L3 child tasks in the queue reference the L3 modules as deps; they will consume them. But within the reviewed artifact, the only consumer is the Audit probe. Let me record that as the "downstream use" status: the *proposed* child tasks declare dependency on these modules, but no compiled module in the artifact consumes the main theorem besides internal lemmas.

Wait — actually, `mildToClassicalBridge_of_uniform` consumes `UniformMildToClassicalBridge`; `heatConv_classicalHeatSolution` consumes `hasDerivAt_heatConv_apply_kernelLaplacian`; `hasDerivAt_heatConv_BCF` consumes `uniformMildToClassicalBridge_holds`; `mildToClassicalBridge_holds` consumes `mildToClassicalBridge_pointwise`. So there IS a real internal consumer chain ending at `mildToClassicalBridge_holds` (the D12 obligation) and `heatConv_classicalHeatSolution` (bundled output) — but nothing consumes *those* two outside L3. That's the precise statement.

Now let me check the full rebuild.
dsh: reasoning:
9192/9239 done, no errors so far. It's nearly finished. Note grep -c returned 1 exit code because 0 matches — fine.

Let me wait a bit and then check the job.
dsh: reasoning:
Full independent rebuild of the entire release package: **exit 0, 9239 jobs, 1m44s**. The final line: "Build completed successfully (9239 jobs)" — the same job count as the parent claimed. And the D6AUDIT/L3 audit PASS lines are in the log.

Let me verify the D6AUDIT verdict and other key lines in the full rebuild log, then check that the build log has the L3 axiom PASS. Also count errors.

Then I'll assemble the final evidence summary and write the review card.

Let me grep the full rebuild log for the audit verdict lines.
dsh: reasoning:
Full rebuild: 0 errors, 142 warnings (style suggestions), D6AUDIT VERDICT PASS, L3 audit PASS, 9239 jobs, exit 0. Excellent.

Let me check what the D6AUDIT says about partial declarations (2 `_unsafe_rec`) to be precise in the card about `unsafe`: the D6AUDIT says "no unsafe" but earlier the parent's build log tail showed `partial_declarations 2` and `partial_decl D4Audit.sqTraj._unsafe_rec`, `Poincare.Longrun.Surgery.SurgeryChain.append._unsafe_rec`. Those are compiler-generated `_unsafe_rec` helpers for `partial def`s (not `unsafe` declarations per se, but they are `unsafe`-marked generated constants). Let me check my full rebuild's D6AUDIT section for those lines. This matters for the acceptance "No sorry/axiom/admit/unsafe/native_decide/proof_wanted" — the L3 authored files have none, but the *package* has 2 `_unsafe_rec` auxiliaries from D4/D9 (pre-existing, not L3). I should report this precisely: the L3 artifact files contain no unsafe; the release package (unchanged in that respect) contains 2 partial-def recursors in unrelated D4/D9 modules. My audit over the L3 prefix found no UNSAFE-DEF constants (kindOf checks `.defnInfo v` safety; `_unsafe_rec` are `defnInfo` with safety unsafe — but they're not under the L3 prefix, so not enumerated).

Let me verify from my full rebuild log the D6AUDIT partial declarations lines.
dsh: reasoning:
Confirmed: package-wide, `unsafe_declarations 0`, but `partial_declarations 2` (compiler-generated `_unsafe_rec` for `partial def` in D4Audit and SurgeryChain — unrelated to L3). I'll record this precisely in the review card as a package-level note, not an L3 issue.

Now let me finalize the evidence set:
1. `review/evidence/semrev-axiom-audit.log` — independent audit (94 declarations).
2. `review/evidence/semrev-census.log` — 85 L3 constants with types.
3. `review/evidence/semrev-semantics.log` — constructed inputs/consumers.
4. `review/evidence/stageA-typenotes.log`, `stageA2.log` — TVS/instance analysis.
5. `review/evidence/perfile-gate.log` — 6/6.
6. `review/evidence/l3-audit-build.log` — parent audit driver PASS.
7. `review/evidence/full-rebuild.log` — full package rebuild, 9239 jobs, exit 0.
8. `review/evidence/negcontrol-parent.log`, `negcontrol-reviewer.log` — negative controls exit 1.
9. `review/evidence/semrev-forbidden-scan.json` — hashes + forbidden scan.
10. `review/rebuild-l3-cone.log` — cone rebuild.

Let me also record a consolidated `review/evidence/semrev-evidence.json` summarizing everything (hashes, counts, verdict) for the card to reference. And then write the card.

Before writing the card, let me create a source-hash manifest for the cited snapshot declarations' files (D10/D12), as the acceptance says "record source hashes and exact semantic class per declaration". I'll record:
- The 6 authored files (done).
- The files containing the 9 snapshot declarations: let me determine which files:
  - `Poincare.D12.ParabolicLocal.mildToClassicalBridge` → Poincare/D12/ParabolicLocal/Obligations.lean
  - `heatConv`, `heatConv_apply` → Poincare/D12/ParabolicLocal/GaussianConv.lean
  - `heatConv_tendsto_self_BUC` → Poincare/D12/ParabolicLocal/GaussianSemigroup.lean
  - `Poincare.D10.HeatKernelEuclidean.hasDerivAt_gaussianKernel`, `laplacian_gaussianKernel` → Poincare/D10/HeatKernelEuclidean/HeatEquation.lean
  - `Poincare.D12.HeatSemigroup.heatOperator` → Poincare/D12/HeatSemigroup/Basic.lean
  - `gaussianKernel_interval_bound`, `gaussianKernel_interval_bound_le` → need to find (probably Poincare/D12/HeatSemigroup/StrongContinuityL1.lean or Basic.lean).

Let me locate them and hash those files. I'll write this into an evidence file `review/evidence/snapshot-source-hashes.txt`.

Let me do that.
dsh: reasoning:
Snapshot source files hashed. Let me also verify these hashes are identical in the D13-critical-path-review package (provenance cross-check already done at the tree level: 0 differing hashes for all D10/D11/D12 files between L3's package and D13's). Good.

Now let me write a consolidated evidence JSON, then the review card.

Actually first, let me double check a couple of remaining acceptance items:

**"compile/read all cited declarations"**: 
- Compiled: fresh cone rebuild (all D10/D11/D12/L3), full package rebuild (9239 jobs), per-file gate on 6 L3 files, parent audit driver rebuild, probes.
- Read: I've read all 6 L3 files in full, plus D12 Obligations (the obligation), BUC/GaussianConv/GaussianSemigroup excerpts, D10 HeatEquation excerpt, D13 consumers. ✓.

**"run fail-closed axiom audit (only propext, Classical.choice, Quot.sound)"**: done, superset, PASS, with two negative controls. ✓.

**"record source hashes and exact semantic class per declaration"**: I'll produce the table for cited + load-bearing declarations and reference the full census for all 85. ✓.

**"No sorry/axiom/admit/unsafe/native_decide/proof_wanted"**: code-level scan clean; environment audit shows no axiom/unsafe/sorryAx under L3; package has 2 pre-existing partial `_unsafe_rec` (outside L3). ✓.

**"End the card TASK_DONE or TASK_BLOCKED"**: TASK_DONE.

**"this is independent review, never a Poincare proof"**: card will state clearly no Poincaré/Perelman claim.

**"Continue from existing files and checkpoint.json in this worktree; do not restart completed work"**: There was no checkpoint.json in my worktree; the parent's checkpoint.json was read and used. I created my own checkpoint at the worktree root. I should mention in the card that the parent checkpoint was consumed (round 2 of L3) and no prior SEMREV work existed in this worktree (it was a D6 template). Hmm — wait. Let me double check there isn't prior SEMREV work somewhere in my worktree (e.g., in `longrun/results/`). Earlier listing showed only `D6-weekly-release.{md,json}` in `longrun/results/`. So this is round 1 of the SEMREV task. Good.

**"Save a compile-checked checkpoint at least every hour"**: checkpoint.json created at 00:20 UTC; will update at the end. The task started 00:05 UTC (16:05 UTC? times confusing). Let me note: the queue started_at is 2026-09-12T00:05:10+0800 = 2026-09-11T16:05:10Z. My session times in bash show 16:0x UTC. So the four-hour limit ends ~20:05 UTC. I have plenty of time. I'll update the checkpoint again at the end (within the hour).

Now let me also think about whether to include a **statement-shape scan** for triviality. Let me do a quick automated check: for each L3 theorem, extract the "conclusion" and check no hypothesis is syntactically identical. This is hard in general; instead, I'll document a manual review of the 42 authored declaration types (I have them all in the census). Let me dump the 42 authored types compactly and eyeball for anything suspicious like `P → P`.

Let me print the 42 authored declarations with types (from census, filtered to parent list).
dsh: reasoning:
All 42 authored declaration types look substantive; no `P → P` trivialities. The theorem set is a coherent chain.

Now let me write the review card. It must be at `longrun/results/SEMREV-L3-analytic-critical-path.md` in MY worktree.

Structure of the card:
1. Header: task id, worktree, parent artifact, date, verdict TASK_DONE, scope statement (independent semantic review; not a Poincaré proof).
2. What was reviewed (parent artifact, round 2, hashes, pins).
3. Independent verification performed (rebuilds, audit, negative controls, scan, probes) with exact commands and results.
4. Source hashes table (6 authored + 6 snapshot source files + pins).
5. Per-declaration semantic class + axiom cone table for cited/load-bearing declarations (with exact types).
6. Semantic findings:
   a. Ambient space of the D12 obligation is product topology (TVS `HasDerivAt`), not Banach; D12 docstring inaccurate; L3 card correct; the uniform/BCFn strengthening is the genuine norm statement.
   b. Downstream use: only internal + type-ascription probes; no external consumer; `MildClassicalOutput` not rebound; my probe constructs inputs and consumers.
   c. `KernelClassicalHeatSolution` name vs content (kernel-Laplacian pairing, not ∂t u = Δu); initial condition one-sided.
   d. Residuals: `SpatialLaplacianBridge` statement-only (no inhabitant); Duhamel F-term residual is prose-only, not a Lean Prop.
   e. Derivative domains: all t>0 or t ∈ (0,T); no t≤0 claims; heatConv truncation to 0 at t≤0 irrelevant locally.
   f. Euclidean/model scope: no manifold/Ricci content; confirmed by imports and grep.
   g. Axiom cones: 85+9 all within approved; no sorryAx/axiom/unsafe in L3; package-level 2 pre-existing `_unsafe_rec` partials outside L3.
   h. Blocker classification U6/U8/U12: all open, upstream-mathlib-gap; no closure claimed; classification conservative and accurate; U8 "removes one packaging obstacle" is an informal relevance claim not formalized; U12 untouched.
7. Limitations of this review (e.g., round-1 repair history not verifiable; parent's per-file gate of 356 files not re-run in full—wait, I did full rebuild but not per-file `lake env lean` on all 356. I did per-file on the 6 L3 files. I should state exactly that.)
8. Verdict: TASK_DONE, with the artifact classified as "proved (Euclidean model) + statement-only residuals; no blocker closed; no Poincaré content".
9. Evidence index (files).
10. TASK_DONE line at the end.

Let me also decide on the exact verdict semantics. The task: "End the card TASK_DONE or TASK_BLOCKED; this is independent review, never a Poincare proof." So TASK_DONE means the review completed. I'll state: "TASK_DONE — independent review completed; reviewed artifact's claims verified within the stated Euclidean scope; no blocker closure, no Poincaré proof."

I must be careful with the "no blocker closure without constructed input and consumer" requirement. I'll include a section "Blocker-closure audit" that says: the artifact claims zero blocker closures; I verified that (a) U6/U8/U12 remain open in the manifest and are untouched or only partially advanced; (b) the one discharged sub-obligation (`mildToClassicalBridge`) has constructed inputs available and I constructed them (f3 : BUCn 3), and has consumers (internal chain + my probe); (c) no closure claim is made without them.

Also the acceptance says "record source hashes and exact semantic class per declaration". I'll provide the table with class labels for the 42 authored declarations in a compact form (grouped), plus exact types for the ~20 load-bearing ones, and point at the full census evidence file for all 85 (with types) and the audit log for all 94 cones.

Let me also compute the exact evidence file hashes for the card (so the evidence itself is hashed). Let me generate an evidence manifest with sha256 of each evidence file.

Let me do that, then write the card.
dsh: reasoning:
I have all evidence hashes. Now let me also create the evidence manifest JSON (with these hashes + summary) and update the checkpoint. Then write the card.

Let me save the hash list into the evidence dir as `evidence-hashes.txt` and write `review/evidence/semrev-evidence.json`.

Then the card.

One more thing: I should double-check the claim about the *parent's* "51 declarations" and my "94" more carefully for the card: 
- Parent's list: 42 authored + 9 snapshot = 51.
- My enumeration: 85 constants under L3 (42 curated + 43 auto-generated auxiliaries, incl. structure projections/recursors and `eq_1`/`_proof_*`/`_simp_*` helper lemmas) + 9 snapshot = 94.
- All 94 pass.

Also verify: are the 9 snapshot declarations really *consumed* by L3? Yes: heatConv/heatConv_apply/heatConv_tendsto_self_BUC (used), mildToClassicalBridge (the target Prop), D10 kernel identities (used), heatOperator (used), gaussianKernel_interval_bound{,_le} (used in Basic/UniformBridge). ✓.

Now let me write the evidence JSON and the card. The card should be thorough but readable. Let me draft it carefully.

I'll write the card in the style of the other result cards (like D13-critical-path-review.md), with sections.

Key content:

# SEMREV-L3-analytic-critical-path — independent semantic review card

- Task id, worktree, parent artifact (path), lane auditor, date, verdict TASK_DONE.
- Reviewed revision: parent round 2, checkpoint 2026-09-11T15:43:36Z, card + JSON.
- Scope statement.

## 0. Verdict summary
- TASK_DONE: review completed. The parent artifact's load-bearing claims are **verified as stated within their Euclidean-model scope**, with the corrections/limitations listed in §5. **No named blocker (U6/U8/U12) is closed by the artifact, and none is closed by this review.** This card is not a Poincaré/Perelman proof and asserts nothing about the conjecture.
- Parent semantic class confirmed: "proved (Euclidean model) + statement-only residuals", with the refinement that the discharged D12 obligation is a **TVS-level (product-topology) derivative**, and the genuinely Banach-space statement is the separate uniform/BCFn theorem.

## 1. What was reviewed (hashes)
Table: 6 authored files + pins + 6 snapshot source files (hashes + provenance cross-check).

## 2. Independent verification performed
Table of checks: fresh cone rebuild, full package rebuild (9239 jobs), per-file gate 6/6, parent audit driver PASS 51, independent audit PASS 94, negative controls, forbidden scan, semantics probe, provenance comparison, instance/scope diagnostic.

## 3. Axiom cones (fail-closed)
- Approved cone.
- Results: 85 L3 constants + 9 snapshot; cones histogram; no axiom/unsafe/sorryAx; negative controls.
- Package-level note: D6AUDIT 2 `_unsafe_rec` partials outside L3 (pre-existing).

## 4. Exact semantic class per cited declaration
Table with name | kind | class | axiom cone | consumer/use.
Plus reference to the full 85-constant census.

## 5. Semantic findings and corrections
F1 TVS/product topology (with evidence from pp.all).
F2 Downstream-use gap.
F3 KernelClassicalHeatSolution naming/content.
F4 Residual formalization status.
F5 Derivative domains.
F6 Euclidean scope.
F7 Blocker classification U6/U8/U12.

## 6. Blocker-closure audit (constructed input & consumer)
- exact_blockers_closed = [] verified.
- constructed input demonstration.
- consumer chain.

## 7. Limitations of this review
- Round-1 repair history not verifiable (no git).
- Per-file standalone gate run only on the 6 L3 files (the other 356 compiled in the full package build; the parent's 356-file sweep was not re-run file-by-file).
- Snapshot math assumed correct at the statement level; only their axiom cones checked (their proofs are prior artifacts).
- `SpatialLaplacianBridge` non-inhabitation established by absence (no constant of that type in the environment), not by a refutation.
- Review instrumented by probes that are not part of the artifact.

## 8. Evidence index (hashes)

## 9. Verdict
TASK_DONE.

Let me be careful to include "No sorry/axiom/admit/unsafe/native_decide/proof_wanted" statement with the precise scope (L3 authored files: none; package: 2 pre-existing partials outside L3).

Now write the file. First, the evidence JSON + hash file.
dsh: reasoning:
Now let me write the review card. This is the main deliverable. Let me compose it carefully and completely.

I need to also double check one more thing for the card's accuracy: the parent card claims "51 declarations (42 authored + 9 load-bearing snapshot)". My superset audit confirms 42+9 are all in the approved cone.

Also: the parent card says "no project axioms, 0 unsafe..." for the D6 release (README), but the L3 card's claim 7 only concerns the 51.

Let me now write the card. I'll aim for a thorough but focused document (~250-350 lines).

Also need to be precise about the tvS finding. Let me re-verify the exact instance arguments of `HasDerivAt` in the pinned mathlib to describe it correctly:
- `HasDerivAt` requires `[NontriviallyNormedField 𝕜]`, `[AddCommGroup F]`, `[Module 𝕜 F]`, `[TopologicalSpace F]`, `[ContinuousSMul 𝕜 F]` — plus internally `HasFDerivAtFilter` requires more (PolynormableSpace? Let me check FDeriv/Defs.lean for the TVS section requirements).

Let me read the relevant part of `Mathlib/Analysis/Calculus/FDeriv/Defs.lean` around line 100-140 to get the exact class requirements.
dsh: reasoning:
Now precise. The derivative definitions live in a **TVS setting** (`AddCommGroup E/F`, `Module 𝕜 E/F`, `TopologicalSpace E/F`) but require the derivative `f'` to be a `ContinuousLinearMap E →L[𝕜] F`, which itself requires suitable classes on E and F (for `toSpanSingleton 𝕜 f'` etc.). Let me check the variables in `Deriv/Basic.lean`: it had `variable [ContinuousSMul 𝕜 F]`, and `PolynormableSpace` appears in elaborated instance args. Let me check `ContinuousLinearMap` requirements: `E →L[𝕜] F` requires `[SeminormedAddCommGroup E] [SeminormedAddCommGroup F]`? No — in modern Mathlib, `ContinuousLinearMap` may be generalized to TVS via `PolynormableSpace`? The trace showed `instPolynormableSpace` and `instContinuousSMulForall`. Let me check `Mathlib/Topology/Algebra/Module/ContinuousLinearMap/Defs.lean` for the class requirements.

Actually, for the review card, the key points are:
- `HasDerivAt` in this pin is stated for TVS `F` with `AddCommGroup`, `Module`, `TopologicalSpace`, `ContinuousSMul` (and the tangent map `toSpanSingleton` requires whatever `ContinuousLinearMap` needs, which the product topology satisfies via `PolynormableSpace` instances).
- The D12 obligation's ambient space `EuclideanSpace ℝ (Fin n) → ℝ` with the product topology is the canonical example: it has these instances (all Pi instances), but **no norm** (`#synth NormedAddCommGroup` fails since `Fintype (EuclideanSpace ℝ (Fin n))` is unavailable).
- Hence the D12 obligation is *not* a Banach-space statement despite its docstring; L3's card is right.

Let me check `ContinuousLinearMap` requirements quickly to make the statement precise: does `E →L[𝕜] F` require `PolynormableSpace`? Let me grep the definition.
dsh: reasoning:
`toSpanSingleton` at line 742 in `ContinuousLinearMap/Basic.lean` — variables at 721: `[Module R₁ M₂] [TopologicalSpace R₁] [ContinuousSMul R₁ M₂]`, and line 737 `[ContinuousSMul R₁ M₁]`. So `ContinuousLinearMap` is now TVS-level (no norm). Great: the derivative notions in this pin are TVS-level, requiring `ContinuousSMul` and `ContinuousAdd` (and maybe `IsTopologicalAddGroup`).

So my characterization is right: `HasDerivAt` here is a TVS derivative (Fréchet derivative in a topological vector space with continuous linear maps), and the product topology on `EuclideanSpace ℝ (Fin n) → ℝ` is a legitimate TVS instance, not a normed one. The `PolynormableSpace` seen in the elaborated arg list must come from some instance path (`instPolynormableSpace` for the function space) — perhaps needed for `ContinuousAdd` or `toSpanSingleton` continuity. Either way, the product space satisfies the requirements.

Let me quickly confirm the exact binders of `HasDerivAtFilter` from the pinned source (lines 116-131 of Deriv/Basic.lean):
```
variable {𝕜 : Type u} [NontriviallyNormedField 𝕜]
variable {F : Type v} [AddCommGroup F] [Module 𝕜 F] [TopologicalSpace F]
section
variable [ContinuousSMul 𝕜 F]
def HasDerivAtFilter ...
```
So `HasDerivAt` requires `[AddCommGroup F] [Module 𝕜 F] [TopologicalSpace F] [ContinuousSMul 𝕜 F]`. The trace additionally showed `instPolynormableSpace` and `instContinuousSMulForall`, consistent: `ContinuousSMul` was one of the args; `PolynormableSpace` perhaps required by `ContinuousLinearMap`'s `toSpanSingleton` continuity via `PolynormableSpace` instance. Actually `toSpanSingleton (x : M₁) : R₁ →L[R₁] M₁` requires `[ContinuousSMul R₁ M₁]` and `[IsTopologicalAddGroup M₁]`? Line 736-737: `variable (R₁) [ContinuousSMul R₁ M₁]`. Fine.

Good enough for the card: I'll say "TVS-level `HasDerivAt` (`[AddCommGroup] [Module] [TopologicalSpace] [ContinuousSMul]`), product topology, no norm instance".

Now, let me also double-check the card statement "the heat semigroup is differentiable as a BCFn n-valued curve" is really in a Banach space: `BCFn n = (EuclideanSpace ℝ (Fin n)) →ᵇ ℝ` has a NormedAddCommGroup (sup norm) and is complete (Banach). So "Banach-space" is accurate for `BCFn n`. ✓.

One question: in the TVS-level `HasDerivAt` signature, the derivative `f' : F` must be paired with `toSpanSingleton 𝕜 f'`, and the little-o is w.r.t. the TVS structure. For `BCFn n` (normed), this coincides with the usual `HasDerivAt`. ✓.

Now let me write the card. I'll create `longrun/results/SEMREV-L3-analytic-critical-path.md`.

Also, maybe create a JSON companion `longrun/results/SEMREV-L3-analytic-critical-path.json` (like other cards). The task says "Only your own longrun/results/SEMREV-L3-analytic-critical-path.md can complete this task" — the .md is the completion artifact. I can add a JSON companion for the dispatcher, but the .md is what matters. I'll add both (JSON optional, harmless). Actually, to avoid confusion, I'll write the .md and also a small .json with the structured verdict (helps the controller). The queue's card path for my task isn't set. I'll do both.

Let me write the card now. Content plan (detailed):

---
# SEMREV-L3-analytic-critical-path — independent semantic review card

Header bullets.

## 0. Verdict (compact)
TASK_DONE. Reviewed semantic class confirmed/refined. No blocker closed. Not a Poincaré proof.

## 1. Reviewed revision and hashes
- parent path, round, checkpoint
- 6 authored files hashes (match)
- pins
- 6 snapshot source files hashes
- provenance cross-check

## 2. What was independently compiled/read
- table of checks with commands and results
- fresh cone rebuild, full package rebuild, per-file gate, parent audit, independent audit, negative controls, forbidden scan, semantics probe, TVS/instance diagnostic, census

## 3. Fail-closed axiom audit
- method (auto-enumeration; superset)
- results histogram
- no axiom/unsafe/sorryAx
- negative controls
- package-level note (2 partial `_unsafe_rec` outside L3)

## 4. Per-declaration semantic classes
- grouped table for 42 authored + 9 snapshot; exact types for load-bearing; full census reference
- classification labels

## 5. Semantic findings (F1..F7) with evidence

## 6. Blocker classification audit (U6/U8/U12)
- definitions from manifest; status; what L3 changed; no closure
- child-task acceptance mapping

## 7. Blocker-closure rule check (constructed input & consumer)

## 8. Limitations of this review

## 9. Evidence index

## 10. Verdict and TASK_DONE

Let me write it.

For the per-declaration table, I'll list the 42 authored declarations grouped:

**Data definitions (6):** timeCoeff, timeDerivKernel, timeDerivBound, timeDerivIntegral, timeDerivBCF, heatConv_classicalHeatSolution (def producing structure).
**Structure (1):** KernelClassicalHeatSolution.
**Statement-only Props (1):** SpatialLaplacianBridge (no inhabitant).
**Props with an inhabitant (1):** UniformMildToClassicalBridge ← uniformMildToClassicalBridge_holds.
**Proved theorems (33):** the rest.

I can enumerate all 42 with one-line classes. Let me count: 42 = 3 defs (timeCoeff, timeDerivKernel, timeDerivBound) + ... let me count from the list: defs = timeCoeff, timeDerivKernel, timeDerivBound, timeDerivIntegral, timeDerivBCF, heatConv_classicalHeatSolution, UniformMildToClassicalBridge, SpatialLaplacianBridge = 8 defs; induct = KernelClassicalHeatSolution = 1; theorems = 33. Total 42. ✓.

Now write the card. I'll include exact types for the ~20 load-bearing declarations and group the rest.

Let me be careful about the phrase for the review's verdict: "TASK_DONE" at the very end (the task says end the card TASK_DONE or TASK_BLOCKED).

Let me now write the file.
dsh: reasoning:
Now let me verify the card's factual claims once more and check details I might have gotten wrong:

1. "`SpatialLaplacianBridge` cone is `[]` as a def" — I wrote "its cone is `[]` as a def and nothing inhabits it". Let me verify: my audit enumerated it and its row would show the cone. Let me check the audit log for `SpatialLaplacianBridge`. Earlier the summary showed ALL 85 L3 constants with cone `[propext,Classical.choice,Quot.sound]`. So SpatialLaplacianBridge's cone is NOT empty — it's the full three (since the Prop mentions integrals/Laplacian which use Classical.choice). Let me check the actual log and fix the card.

2. "85 × {propext, Classical.choice, Quot.sound}" — from my histogram: 85 with the full cone, 9 snapshot with [propext]. And note the 85 includes the `SpatialLaplacianBridge` def. OK.

3. In §4.2/§3 I said 42 curated + 43 aux: 42+43=85 ✓.

4. In the evidence index, I list `review/rebuild-l3-cone.log` with "—" hash. It exists at review/rebuild-l3-cone.log. Fine but let me add its hash for completeness.

5. The card says "`Poincare/L3/` contains exactly these six `.lean` files — verified by scanner ✓.

6. Verify the claim "no `partial`, `opaque`, `set_option`, `#eval`, `implemented_by` or `extern` occurs" — my scanner's informational hits were empty ✓.

7. Verify claim "The parent's claim 51 declarations ... confirmed and strengthened" ✓.

8. Check the statement "the D12 obligation's ambient space ... `#synth NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)` fails" ✓ (Stage A log).

9. "`@Pi.topologicalSpace … (fun i => @UniformSpace.toTopologicalSpace ℝ Real.pseudoMetricSpace)`" — from the pp.all output ✓.

10. The claim about `HasDerivAt` variables — I quoted the mathlib source ✓.

11. "grep: no `conjugate`, backward, `F`-functional or monotonicity declaration" in L3 — let me actually run that grep to be sure (I didn't run it explicitly).

12. The claim "`MildClassicalOutput` ... `u : ℝ → P.MetricState`, `P.IsDeTurckSolutionOn T u`" ✓ from reading.

13. "no git repository in the parent worktree" ✓.

14. The `L3-U6a-uniform-bridge` acceptance text: I quoted it. Let me re-check the exact acceptance text from the queue: "Compiled proof of Poincare.L3.HeatTimeDeriv.UniformMildToClassicalBridge n for all n, or a precise reduction splitting it into (i) L1-continuity in time of z ↦ K_u(z)c_u(z) and (ii) mean-value/FTC packaging; a downstream consumer producing HasDerivAt of s ↦ heatConv n s f in the BCFn sup norm; fail-closed axiom audit allowing only propext, Classical.choice, Quot.sound; no sorry/axiom/admit/unsafe/native_decide/proof_wanted; independent rebuild and semantic review before any blocker closure claim." ✓ matches my summary.

15. HashSet: the `checkpoint.json` at my worktree root says "RUNNING" — I'll update to TASK_DONE at the end.

16. In §4.1 I list `mildToClassicalBridge_holds` consumer as "type ascription only" ✓.

17. Need to double check my claim "no theorem concludes SpatialLaplacianBridge". Let me verify via census: search for theorems whose type is `SpatialLaplacianBridge n`. From the census output, only the def itself. Let me confirm programmatically by grepping the census log for "SpatialLaplacianBridge" occurrences.

18. The card's §2 table row 2 says full rebuild "0 errors" — verified.

19. "D6AUDIT VERDICT PASS" ✓.

Let me run the checks for items 1, 11, 17 now and correct the card.
dsh: reasoning:
Confirmed:
1. `SpatialLaplacianBridge` cone is `[propext, Classical.choice, Quot.sound]`, NOT empty. I must fix that sentence in the card.
2. Scope grep: only docstring mentions of "no manifold/Ricci" — no actual content. ✓.

Let me fix the card's F4 sentence and add the rebuild-l3-cone.log hash. Let me also double check a couple of other statements in the card:

- §3 table: "85 × {propext, Classical.choice, Quot.sound}" — correct (including the two Props).
- In §4.1 `SpatialLaplacianBridge` row says "no inhabitant — statement-only residual" ✓.

Let me fix F4: "its cone is `[]` as a def and nothing inhabits it" → "its axiom cone is the standard `{propext, Classical.choice, Quot.sound}` like every other L3 constant, and no constant in the environment has a type concluding it".

Let me also compute the rebuild-l3-cone.log hash and add it to the evidence hashes file + card.

Also, I should double-check the card statement in §1.2: "all 76 .lean files under Poincare/D10, D11, D12 in the L3 package were compared ... with the D13 package and the D12 package: 0 differing hashes in either comparison". Precision: vs D13: only_L3=0, only_D13=105, differing=0 — the L3 set is a strict subset of D13's, so "all 76 ... compared" is right (76 common with D13). vs D12 package: only_L3=30, only_D12=0, differing=0 — so 46 files common. My sentence says "0 differing hashes in either comparison" ✓. But "all 76 ... compared with ... the D12-parabolic-local-existence package" is not exact (only 46 common). Let me fix to say: 76 files compared with D13 (0 differing; D13 has 105 more), and the 46 overlapping files with the D12 package (0 differing).

Let me make these edits.
dsh: reasoning:
Also fix the §7 limitation #4 which repeats the "non-inhabitation by absence" claim — it's fine as is ("established by enumerating every environment constant mentioning HeatTimeDeriv and finding no inhabitant").

Now, update the evidence hashes file to include rebuild-l3-cone.log and the card hash. And update the card's evidence index row for rebuild-l3-cone.log with a hash.

Let me compute hashes.
dsh: reasoning:
Wait: the evidence-hashes.txt now includes itself? `sha256sum review/evidence/*` includes evidence-hashes.txt (self-reference) and semrev-evidence.json. That's fine but the self-hash is stale after writing. Let me regenerate carefully: exclude evidence-hashes.txt itself from the manifest, or accept. Better: list all evidence files except evidence-hashes.txt, plus probes/tools/negcontrol, plus the card. Let me redo.

Also update the card's evidence row for rebuild-l3-cone.log with hash `498fbc29`.

Then final checkpoint update.

Let me redo the manifest and patch the card.
dsh: reasoning:
Now, since I edited the card, its hash changed. The manifest's card hash is stale. I'll update the manifest at the very end (after the card is final). Let me also consider adding a JSON companion.

Before finalizing, let me do a final self-review of the card for accuracy and completeness:

1. Read the card fully to check formatting and claims.
2. Verify all cited paths exist.
3. Update checkpoint.json to TASK_DONE.
4. Regenerate the evidence hash manifest last (after all edits).
5. Optionally write the JSON companion.

One more substantive check I should do before finalizing: the card claims "the parent's claim 51 declarations ... confirmed and strengthened". Also I claim "no theorem concludes SpatialLaplacianBridge" — verified by census (only one mention). Good.

Another check: my card says in §4.2 "`heatConv` truncated heat convolution" — the truncation is `0` for t≤0, and I mention it in F5. Fine.

Also §2 row 1 says "fresh rebuild of the whole consumed cone (D10/D11/D12 + L3, 82 modules)". The 82 is the count of .lean files in D10/D11/D12/L3. ✓.

Also I should double check the exact statement in §5 F1: "`#synth NormedAddCommGroup (EuclideanSpace ℝ (Fin 3) → ℝ)` fails (Fintype (EuclideanSpace ℝ (Fin 3)) is unavailable, so Pi.normedAddCommGroup cannot apply)". Verified by Stage A log and by reading `Pi.normedAddCommGroup` (requires [Fintype ι]). ✓.

One nuance: `EuclideanSpace ℝ (Fin 3)` is a Type, and `Fintype (EuclideanSpace ℝ (Fin 3))` would require `Fintype (Fin 3 → ℝ)`; there's no such instance since ℝ is infinite. ✓.

Let me now read the card to double check formatting.
dsh: reasoning:
The card looks good so far. Note the generated timestamp says 2026-09-11T16:20Z (that's UTC of my session, which is 2026-09-12 00:20 +0800). Fine.

One issue: I wrote "the D13-critical-path-review package has 105 further D11/D12 files" — actual: only_D13=105 across D10/D11/D12. Let me fix to "105 further D10/D11/D12 files". Minor.

Let me continue reading the rest of the card.
dsh: reasoning:
The card is in good shape. A few corrections:

1. Line 123: "85 × {propext, Classical.choice, Quot.sound}" — this is right, but `SpatialLaplacianBridge` as a def has that cone; fine.

2. Line 133-135: "the word 'axiom' in the audit module's prose" ✓.

3. §4.1 line 177: `mildToClassicalBridge_holds` consumer "type ascription only (see F2)" ✓.

4. F1: the claim "`Fintype (EuclideanSpace ℝ (Fin 3))` is unavailable, so `Pi.normedAddCommGroup` cannot apply" — precise.

5. F3: "The structure's `isSolution` field asserts `∂ₜ u(t,x) = ∫ y, ΔK_t(x−y)·u₀(y) dy`" ✓.

6. In §5 F2 line 241: comment references `PicardModel.lean:289`, `ToppingAdapter/ShortTime.lean:39`, `Obligations.lean:31`, `DerivativeLoss.lean:435` — these were the grep hits (all comments). ✓.

Now let me read the rest (lines 280-399) to check the final sections.
dsh: reasoning:
The card is complete and accurate. A few final fixes:

1. Line 75: "the D13 package has 105 further D11/D12 files" → should be "105 further D10/D11/D12 files".
2. Line 296: "the packaged solution's fields (`T = 1`, `isSolution`, `initial`) consumed at `f3`" ✓.
3. §7 limitation 3 says "`heatConv_tendsto_self_BUC` in sup norm" — correct.
4. Line 363: "`evidence/semrev-evidence.json` contain sha256 of every file below" — the semrev-evidence.json has evidence_hashes embedded; ✓ (though it was generated before the card/rebuild-log hash update; let me regenerate it to be consistent).
5. The card's header says Generated 2026-09-11T16:20Z — it's now ~16:35Z. Minor; I'll update to 16:35Z at final edit.

Also, I want to add one more valuable piece: a short "Search for statement-shape triviality" note is already in F7. Fine.

Let me make these final edits, then:
- Regenerate semrev-evidence.json with updated hashes.
- Regenerate evidence-hashes.txt (final).
- Update checkpoint.json to TASK_DONE.
- Write the JSON companion card.

Note the ordering problem: evidence-hashes.txt includes hashes of the card and semrev-evidence.json; semrev-evidence.json includes evidence_hashes mapping. Circular. Solution: make evidence-hashes.txt the authoritative manifest (excluding itself), compute it last; semrev-evidence.json can include a note pointing to evidence-hashes.txt rather than embedding hashes. Let me restructure: regenerate semrev-evidence.json without the embedded hashes (or with a pointer), then regenerate evidence-hashes.txt last (which includes semrev-evidence.json's hash).

Let me do that.
dsh: reasoning:
Evidence finalized. Now let me write the JSON companion for the card at `longrun/results/SEMREV-L3-analytic-critical-path.json` (optional but consistent with other result cards). Then update checkpoint.json to TASK_DONE and add the final state.

Then I should do a final validation pass:
1. Re-verify the card renders (no broken tables) — read it once more quickly? I've read it fully. 
2. Verify all referenced evidence files exist.
3. Confirm the card ends with TASK_DONE. ✓
4. Run the scanner one more time to ensure nothing changed in the reviewed package (hashes unchanged) — the package is untouched since staging except oleans. Let me re-run `semrev_check.py` to confirm hashes still match (it checks source files only).
5. Maybe re-run the independent audit one final time (already done; the environment is unchanged since).

Also: should I re-run the semantics probe after the full rebuild? The full rebuild recompiled everything with the same sources; my probes were run before the full rebuild but after the cone rebuild. The oleans are the same content. For rigor, let me re-run all three probes (audit, semantics) once more now that the full rebuild has completed — cheap (~10s) and gives final-state evidence. Actually, re-running them would change their log files (same content likely) and thus hashes; the evidence-hashes.txt would need regeneration. Let me do it: re-run probes, then regenerate hashes, then finalize. This ensures the evidence corresponds to the final compiled state.

Let me re-run: SemrevAudit, SemrevSemantics, SemrevCensus, and the negative controls + scanner. Then regenerate the manifest.

Actually, the evidence logs were generated from the cone-rebuilt oleans, which are byte-identical to the full-rebuild oleans (same sources, same toolchain, deterministic compile). The content would be identical anyway. But for cleanliness, let me re-run the audit + semantics and diff against the existing logs to confirm reproducibility (a nice extra evidence point: deterministic re-run). Let me do that.
dsh: reasoning:
All three probes reproduce byte-identically after the full rebuild. Excellent — that's a strong reproducibility point to add to the card.

Let me add a line to the card §2 about reproducibility, then write the JSON companion, update the checkpoint, and finalize.

Card edit: add to §2 after the table: "All three probes were re-run after the full package rebuild and produced byte-identical output (`diff` clean), i.e. the evidence is reproducible against the final compiled state."

Let me do the edits now.
dsh: reasoning:
Now finalize:
1. Update checkpoint.json to TASK_DONE with final summary.
2. Regenerate evidence-hashes.txt (includes the card .md; should it include the .json? Let me include both the card md and json).
3. Re-run the scanner one final time to confirm the reviewed package is unchanged.
4. Verify the card ends with TASK_DONE and all evidence files exist.

Let me update the checkpoint.
dsh: reasoning:
Everything is finalized:
- Card ends with TASK_DONE.
- Scan passes (ok: true, pins match).
- Hash manifest has 25 entries.

A couple of final considerations:

1. The task says "Save a compile-checked checkpoint at least every hour." I created the checkpoint at ~00:20 (+0800) and updated at ~00:40. Within the hour. Good. Since I'm finishing within ~40 min of start, one checkpoint is enough — but the final checkpoint is the TASK_DONE one, which is also compile-checked (the card's evidence). Fine.

2. Should I also update the queue/register the review? The dispatcher handles that via its own scripts (register_reviews.py). My task says only my own card can complete the task. I should not touch the shared queue (outside my worktree write access anyway).

3. Let me do a final sanity check of the whole deliverable: list the files in longrun/results and confirm the card, JSON, checkpoint, evidence.

4. One more potential improvement: the card's §8 says "evidence/evidence-hashes.txt and evidence/semrev-evidence.json contain sha256 of every file below" — now evidence-hashes.txt includes the card+json+checkpoint too. Fine.

5. Verify the card claims the checkpoint path correctly. Yes.

Let me do the final listing and a last read of the top of the card to make sure the TASK_DONE verdict is unambiguous.

Also, I realize I should double check one potential issue: the card says "the parent's claim ... 356/356 ... was reproduced only by a full package rebuild plus a 6/6 standalone gate" — accurate.

And "The only substantive evidence gaps are (i) ... (ii) ..." — fine.

Let me also consider whether the review should mark TASK_BLOCKED instead. The task instructions: "Report a genuine mathematical blocker rather than claiming completion with a weaker theorem." My review completed with a TASK_DONE verdict on the *review*. The reviewed artifact does not prove Poincaré; that's not my task's blocker. No genuine mathematical blocker prevents the review. So TASK_DONE is correct.

Final listing.
dsh: reasoning:
Everything's in place. Let me do one last careful check of a detail I want to be sure about before declaring done:

The task's acceptance says: "compile/read all cited declarations, run fail-closed axiom audit (only propext, Classical.choice, Quot.sound), record source hashes and exact semantic class per declaration. No sorry/axiom/admit/unsafe/native_decide/proof_wanted. End the card TASK_DONE or TASK_BLOCKED."

All satisfied:
- compile/read all cited declarations ✓ (fresh cone rebuild + full rebuild + per-file gate; all 6 files read; snapshot sources read).
- fail-closed axiom audit ✓ (94 rows, two negative controls).
- source hashes ✓ (§1.1, §1.2, evidence manifest).
- exact semantic class per declaration ✓ (§4.1/§4.2 with the full 85-row census; classes P/D/S/SP/SP+/K).
- No forbidden tokens ✓ (scanner + environment audit).
- Card ends TASK_DONE ✓.
- Independent review, never a Poincaré proof ✓ (stated repeatedly).
- Continue from existing files and checkpoint.json; no restart ✓ (used parent's checkpoint/artifact; no prior SEMREV work existed).
- Compile-checked checkpoint ✓.
- No blocker closure without constructed input and consumer ✓ (no closure claimed; constructed input + consumers documented).

One question: the deliverable path. The task statement says "Only your own longrun/results/SEMREV-L3-analytic-critical-path.md can complete this task." — I created exactly that at the worktree root's longrun/results/. Note that the task prompt says "Worktree: /data/home/guoshaoyang/workdir/lean_poincare/longrun/worktrees/SEMREV-L3-analytic-critical-path", while the actual path is /data3/... (there's likely a /data/home symlink or the prompt uses a logical path). Let me check whether /data/home/guoshaoyang/... exists and is the same as /data3/... — if the dispatcher reads from the /data/home path, my file must be visible there. Let me check.
dsh: reasoning:
The `/data/home/guoshaoyang/...` path resolves to the same directory (`readlink -f` shows /data3/...), and the file is visible there. 

Everything is complete. Let me do a final quick review of the card's opening and the JSON to make sure nothing is off, then report to the user.

Actually, one more thing to double check: the card's §0 says "the parent's '356/356 per-file gate' was reproduced only by a full package rebuild plus a 6/6 standalone gate..." — accurate.

And I should mention in my final message the key findings and that no blocker was closed, no Poincaré claim.

Let me also clean up: the `tmp/` files in my worktree are from the D6 template; irrelevant. The `/tmp/parent_audited.txt` is outside the worktree; fine.

One last check: make sure the evidence-hashes.txt entries are correct/stable (it was generated before the last card edit? Let me verify: I regenerated it in the command right after the card's final edit... Let me trace:
1. Edited card (reproducibility paragraph) — card changed.
2. Wrote JSON companion.
3. Wrote checkpoint.json.
4. Ran final scan + regenerated evidence-hashes.txt in the same command (after the card edit and JSON write). ✓
5. No edits after that.
So the manifest is current. Let me verify by re-hashing the card and comparing to the manifest entry.
dsh: reasoning:
All 25 hashed files verify. The review is complete.

Let me write the final summary to the user. I should mention:
- Verdict: TASK_DONE at longrun/results/SEMREV-L3-analytic-critical-path.md.
- What I did: staged a byte-identical copy, fresh rebuilds, independent superset axiom audit, negative controls, forbidden scan, semantics probes, provenance cross-check.
- Key findings: TVS/product-topology refinement (D12 docstring inaccurate; L3 card correct); no external consumers; KernelClassicalHeatSolution naming; statement-only residual; U6/U8/U12 all confirmed open and unclosed.
- No blocker closed, no Poincaré claim.
- Evidence locations.

Keep it concise.
The independent semantic review is complete. Verdict: **TASK_DONE**.

## Deliverable
`longrun/results/SEMREV-L3-analytic-critical-path.md` (+ `.json` companion, `checkpoint.json` updated to TASK_DONE). All 25 evidence files verify against `review/evidence/evidence-hashes.txt`.

## What was done
- **Staged a byte-identical copy** of the parent L3 `release/` at `review/release/`; all 6 authored files recomputed to the parent's claimed sha256.
- **Compiled from source**: deleted oleans and rebuilt the whole consumed cone (D10/D11/D12 + L3, 82 modules, exit 0, 8915 jobs), then the full package (exit 0, 9239 jobs, 0 errors, D6AUDIT PASS, L3 audit PASS 51 decls), plus a 6/6 standalone per-file gate on the L3 files and exit-0 probes. Probes re-ran byte-identically after the full rebuild.
- **Fail-closed axiom audit (superset)**: auto-enumerated *every* environment constant mentioning `HeatTimeDeriv` (85, including auto-generated auxiliaries) + the 9 snapshot decls → 94 rows, **0 violations**, cones only `{propext, Classical.choice, Quot.sound}` / `{propext}`; no `sorryAx`/`axiom`/unsafe. Both the parent's and my own negative controls exit 1 naming their bad axiom.
- **Forbidden-token scan** (independent implementation): 0 code-level hits in the 6 files; hashes/pins match.
- **Provenance**: 0 differing D10/D11/D12 source hashes vs the D13 and D12 packages — L3 did not modify the snapshot modules.
- **Constructed input + consumer probe**: `f3 : BUCn 3`, `f0 : BUCn 0`, the discharged obligation, packaged-solution fields, uniform statement and `hasDerivAt_heatConv_BCF` all instantiated/consumed (exit 0).

## Key semantic findings
1. **F1 (refinement, artifact already correct in body)**: in this mathlib pin `HasDerivAt` is TVS-level; the D12 obligation's space `EuclideanSpace ℝ (Fin n) → ℝ` carries the **product topology** (no `NormedAddCommGroup` instance), so the discharge is a *pointwise* statement. The D12 docstring's "in the Banach space of functions" is inaccurate (pre-existing snapshot text); the genuine Banach statement is the separate `UniformMildToClassicalBridge`/`hasDerivAt_heatConv_BCF` on `BCFn`.
2. **F2**: no compiled consumer of `mildToClassicalBridge_holds`/`heatConv_classicalHeatSolution` outside `Poincare/L3`; the D13 `MildClassicalOutput` hypothesis is a different abstract object and was not rebound.
3. **F3/F4**: `KernelClassicalHeatSolution.isSolution` is the kernel-Laplacian pairing against `u₀`, not `∂ₜu = Δu`; `SpatialLaplacianBridge` has no inhabitant; the Duhamel F-term residual is prose-only.
4. **U6/U8/U12**: all `upstream-mathlib-gap`, **open — none closed** by the artifact or this review (`exact_blockers_closed = []` accurate). The `L3-U6a-uniform-bridge` sub-obligation discharge meets its stated acceptance as a sub-obligation only.

No blocker closure was claimed, and this card is an independent review — never a Poincaré proof.
